## 1. Контракт в репозитории

- [ ] 1.1 Добавить канонический каталог MCP (например `mcp/catalog.json`) с LAN URL профилей `dev` / `analytics` / `test` / `docs` без секретов; не делать `.cursor/mcp.json` источником истины
- [ ] 1.2 Добавить сниппеты подключения: Claude Desktop (`url` в `mcpServers`) и ChatGPT/Copilot connector; опционально Claude Code `.mcp.json` и зеркало `.cursor/mcp.json`
- [ ] 1.3 Обновить `AGENTS.md` и добавить `CLAUDE.md`: агенты = предоплаченные Claude и GPT; Cursor не обязателен; прод вне MCP; EDT не обязателен; секреты только в клиенте на ПК
- [ ] 1.4 В `openspec/project.md` зафиксировать командный MCP-контур, клиенты Claude/GPT и что код для агента берётся из файловой выгрузки, а не из конкретной EDT

## 2. Runbook шлюза (LAN, вне git-секретов)

- [ ] 2.1 Описать в `openspec/changes/team-shared-mcp-anthropic/runbook.md` хост `srv-mcp`, reverse proxy, выдачу токенов, запрет публикации в интернет
- [ ] 2.2 Описать перенос текущего `1c-syntax-checker-mcp` с `localhost:8002` на шлюз и проверку с двух рабочих станций (Claude Desktop и/или ChatGPT)

- [ ] 2.3 Зафиксировать список разрешённых ИБ (аналитическая копия, тестовая) и явный запрет регистрации прода

## 3. Профили инструментов

- [ ] 3.1 В runbook/правиле расписать allowlist `dev`: syntax/BSL, справка платформы, чтение выгрузки `uhnp` / `uhnp.НП` / `uhnp.ТН`
- [ ] 3.2 Allowlist `analytics`: метаданные, запрос, журнал на копии; без проведения и `execute_code`
- [ ] 3.3 Allowlist `test`: runner только на выделенной тестовой ИБ, отдельно от аналитической копии
- [ ] 3.4 Allowlist `docs`: метаданные + OpenSpec + `AGENTS.md` + Instructions; живые документы/ПДн не источник инструкций

## 4. Фазы живых баз и EDT

- [ ] 4.1 Описать фазу B: HTTP-сервис на аналитической копии УХ (пакет выбирается при внедрении под 8.3.27 / УХ 3.2.8.25)
- [ ] 4.2 Описать фазу C: отдельная тестовая ИБ и запуск проверок
- [ ] 4.3 Явно оставить EDT-MCP вне первой поставки: отдельный change после выбора EDT, без ломки HTTP-профилей

## 5. Проверка контракта

- [ ] 5.1 Проверить, что в git нет токенов, паролей ИБ и ключей Claude/GPT
- [ ] 5.2 Сверить тексты с specs (четыре профиля, изоляция сессий, Claude+GPT без обязательного Cursor, EDT-optional)
