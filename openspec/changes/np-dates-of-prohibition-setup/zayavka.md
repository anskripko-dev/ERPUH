# ЗАЯВКА НА ДОРАБОТКУ В 1С ERP.УХ

Номер заявки: _ERP.2026.14_

Дата регистрации: _19.08.2026___

Инициатор (ФИО, подразделение): _бухгалтерия холдинга__________________________

Приоритет (Высокий / Средний / Низкий): ________________________________

Тип изменения (Ошибка / Новая функциональность / Законодательство / Оптимизация/Права доступа): _Новая функциональность______________________

## 1. Описание проблемы (как есть)

Тема заявки: Автоматическая установка дат запрета изменения и заявка на открытие периода.

Подсистема / Объект 1С: Даты запрета изменения (типовой регистр сведений ДатыЗапретаИзменения), расширение НП.

Описание текущего процесса и проблемы:

Типовой механизм БСП «Даты запрета изменения» закрывает период: объекты с датой не позднее установленной границы записывать нельзя.

Для организаций граница задаётся абсолютной датой. Эту дату приходится регулярно переносить вручную.

Отдельной заявки на временное открытие уже закрытого периода с согласованием в 1С:Документооборот 3 в типовой конфигурации нет.

Типовая проверка даты запрета при записи объектов работает и изменяться не должна.

## 2. Требования к доработке (как должно быть)

Предлагаемое решение:

Реализовать в расширении НП:

- автоматическую установку абсолютной даты запрета по правилу «сегодня минус N дней»;
- документ заявки на временное открытие периода с бесшовным согласованием в 1С:Документооборот 3.

### 2.1. Автоматическая установка

Настройки ведутся в регистре сведений нп_НастройкиАвтоматическойУстановкиДатЗапрета:

- организация;
- Пользователи (пользователь/группа пользователей);
- раздел учёта (необязательно);
- число дней N;
- признак включения.

Пустое значение Пользователи означает «для всех пользователей».

Пустой раздел означает все разделы учёта, где объект - организация (бухгалтерский учёт, продажи, международный учёт и аналогичные).

При пустом разделе не заполняются:

- касса (объект - касса);
- склады (объект - склад);
- сценарии бюджетирования (объект - сценарий);
- банковские счета (объект - банковский счёт).

У этих разделов даты в типовой форме живут отдельно и этой доработкой не перезаписываются.

Регламентное задание нп_СдвигДатЗапретаИзменения записывает в типовой регистр ДатыЗапретаИзменения абсолютную дату: начало дня текущей даты сеанса минус N дней настройки.

Для одной организации допускается несколько настроек с разным N:

- по Пользователям (пользователь/группа пользователей);
- при необходимости - по разделу учёта.

Изменяются только записи, которые есть в настройках. Даты, заданные только в типовой форме, не перезаписываются.

### 2.2. Заявка на открытие периода

Документ нп_ЗаявкаНаОткрытиеПериода содержит:

- организации;
- срок действия открытого периода в днях (K);
- период «с» и «по»;
- раздел учёта (необязательно).

Согласование выполняется штатными средствами бесшовной интеграции с 1С:Документооборот 3.

Пока заявка не согласована, типовой регистр не изменяется.

После состояния «Согласован»:

- срок действия открытого периода отсчитывается от даты согласования (день согласования - день 1);
- в типовой регистр записывается отсечка «Период с минус 1 день» по организациям, разделу и Пользователям из настроек автоматической установки;
- если настроек по организации нет, записывается «для всех пользователей».

Пока срок действия открытого периода не истёк, автоматическая установка эти записи не перезаписывает.

После истечения срока при включённой автоматической установке восстанавливается своё «сегодня минус N» каждой настройки.

На форме заявки нужно пояснение: отсечка БСП откроет всё после даты «Период с минус 1 день», а не только выбранный месяц.

### 2.3. Не входит в заявку

Постоянно:

1. Изменение типовых подписок и модулей проверки даты запрета.
2. Новый относительный вариант БСП вида «прошло N дней».
3. Открытие только середины закрытого диапазона.
4. Правки форм расширения ТН.

Отложено (возможен возврат отдельной заявкой):

1. Форма бухгалтера без прав на весь типовой регистр.
2. Массовая загрузка из Excel.

## 3. Экономическое обоснование

Цель доработки (снижение трудозатрат / снижение ошибок / ускорение процесса / финансовый эффект): _снижение трудозатрат_

Ожидаемый результат:

1. Ночью абсолютная дата запрета сама сдвигается на один день при неизменном N.
2. Разные N могут действовать для разных Пользователей (пользователь/группа пользователей) и разделов учёта одной организации.
3. Согласованная заявка временно открывает период на K дней, затем граница возвращается к автоматической установке.
4. Типовая проверка периода при записи документов и справочников не меняется.

## 4. Оценка и реализация (заполняет подрядчик)

Оценка трудозатрат (часы): ________________

Срок реализации: ________________

Ответственный разработчик: ________________

Техническое решение (краткое описание):

1. Регистр сведений нп_НастройкиАвтоматическойУстановкиДатЗапрета (организация, Пользователи, раздел учёта необязательно, N, включено) с формой списка/записи.
2. Регламентное задание нп_СдвигДатЗапретаИзменения и общий модуль нп_ДатыЗапретаИзменения: запись абсолютной даты в типовой регистр ДатыЗапретаИзменения только по записям из настроек.
3. Документ нп_ЗаявкаНаОткрытиеПериода с бесшовной интеграцией 1С:Документооборот 3 (ИнтеграцияС1СДокументооборот3, ОбъектыИнтегрированныеС1СДокументооборотом, СостоянияСогласованияВДокументообороте). Запись в типовой регистр только после состояния «Согласован».
4. Типовые подписки проверки даты запрета и формы расширения ТН не изменяются.

Какие объекты конфигурации затронуты, влияние на другие подсистемы:

Расширение НП (новые объекты с префиксом нп_).

Типовой регистр ДатыЗапретаИзменения - только запись абсолютных дат по правилам заявки.

Расширение ТН не затрагивается.

Инструкция для пользователя:

Администратор дат запрета заполняет нп_НастройкиАвтоматическойУстановкиДатЗапрета.

Пользователь создаёт нп_ЗаявкаНаОткрытиеПериода и отправляет на согласование в 1С:Документооборот 3.

Подробные правила, алгоритмы и критерии приёмки - в техническом задании к заявке ERP.2026.14.
