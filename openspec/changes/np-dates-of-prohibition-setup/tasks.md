## 1. Пакет расширения

- [ ] 1.1 Создать `Extensions/нп_ДатыЗапретаИзменения/` (префикс `нп_`, purpose Customization) по образцу `нп_КонтрольПроведенияПоДО`
- [ ] 1.2 Подсистема `нп_ДатыЗапретаИзменения`; общий модуль `нп_ДатыЗапретаИзменения` (сервер) с областями ПрограммныйИнтерфейс / СлужебныеПроцедурыИФункции
- [ ] 1.3 Роль `нп_БазовыеПрава`: документ заявки, отчёт, чтение регистра состояния; без регистра настроек
- [ ] 1.4 Константа `нп_ПолучателиУведомленийСдвигаДатЗапрета`
- [ ] 1.5 README: сборка `.cfe`, включение ФО дат запрета, настройка ДО 3, первый запуск на копии

## 2. Регистр настроек автоустановки

- [ ] 2.1 РС `нп_НастройкиАвтоматическойУстановкиДатЗапрета`: измерения ОбъектДатыЗапрета / Пользователь / РазделДатыЗапрета, ресурсы ЧислоДней и Включено; формы списка и записи
- [ ] 2.2 Типы объекта — как у измерения «Объект» типового РС (организации, кассы, склады, банковские счета, сценарии, сценарии товарного планирования)
- [ ] 2.3 Запрет записи с пустыми объектом и разделом одновременно (пояснение)
- [ ] 2.4 Закрытое соответствие «тип объекта → разделы» по таблице заявки 2.1.2 в модуле (без `УчетПоМСФО`)

## 3. Регистр состояния и документ заявки

- [ ] 3.1 РС `нп_СостояниеОткрытыхПериодов` (служебный, без интерактивного редактирования): измерения объект/пользователь/раздел; ресурсы заявка, ДатаОкончанияДействия, ДатаЗапретаДоОткрытия, ЗаписьБылаУстановленаАвтоматически
- [ ] 3.2 Документ `нп_ЗаявкаНаОткрытиеПериода`: ТЧ Объекты, ПериодС, СрокДействия, РазделДатыЗапрета, Пользователь (обязательный), ПричинаОткрытия, Ответственный (автозаполнение, только просмотр)
- [ ] 3.3 Форма документа: постоянное пояснение про одностороннюю отсечку и ночной возврат; команда «Закрыть период досрочно» (полные права)
- [ ] 3.4 Запрет изменения после `Согласован`; запрет пометки удаления и отмены проведения при действующем состоянии
- [ ] 3.5 Интеграция формы с БИД / `ИнтеграцияС1СДокументооборот3.НачатьОбработку`
- [ ] 3.6 `&После` на `ПриИзмененииСостоянияСогласования`: при `Согласован` — запись отсечки и состояния; при недоступности ДО — журнал, без записи РС

## 4. Запись в типовой РС

- [ ] 4.1 Канонические значения: «для всех» = `ВидыНазначенияДатЗапрета.ДляВсехПользователей`; «общая дата раздела» = `Объект = Раздел`; абсолютная `ДатаЗапрета` без `ОписаниеДатыЗапрета`
- [ ] 4.2 `Комментарий` для групп: `нп_авто|00NNN`; для прочих автозаписей: `нп_авто`
- [ ] 4.3 `ОтключитьПроверкуДатЗапрета` + привилегированный режим; после набора — `ОбновитьВерсиюДатЗапретаИзменения`
- [ ] 4.4 Не изменять комбинации вне включённых настроек и вне состояния открытых периодов

## 5. Регламентное задание

- [ ] 5.1 Задание `нп_СдвигДатЗапретаИзменения`, расписание ежедневно 23:00
- [ ] 5.2 Порядок: истёкшие открытые периоды (возврат/удаление типовой записи, удаление состояния) → применение включённых настроек
- [ ] 5.3 Расчёт даты по нормативной таблице (N включая текущий день); идемпотентность в пределах суток
- [ ] 5.4 Ошибка одной настройки не прерывает остальные; журнал с объектом/разделом/пользователем; уведомление по константе
- [ ] 5.5 Действующее состояние блокирует автоустановку по этой комбинации

## 6. Отчёт

- [ ] 6.1 Отчёт `нп_ДействующиеДатыЗапрета` (СКД): типовой РС + соединение с настройками и состоянием
- [ ] 6.2 Классификация источника: автоустановка (настройка, N) / открытый период (заявка, окончание, ответственный) / вручную
- [ ] 6.3 Отборы: объект, раздел, пользователь

## 7. Проверка (сценарии приёмки заявки §5)

- [ ] 7.1 Сценарии 1–3, 5–6: N=5, повторный запуск, сдвиг на день, отключение и удаление настройки
- [ ] 7.2 Сценарий 4: «для всех» N=5 и группа N=30 — две записи, у участника группы действует более ранняя граница
- [ ] 7.3 Сценарии 7, 7а, 7б, 7в, 7г: касса, общая дата раздела, запрет пустой пары, ручная запись склада, риск общей даты адресата
- [ ] 7.4 Сценарии 8, 8а, 9–15: заявка (обязательный пользователь, ответственный, отсечка, блокировка задания, возврат, смешанная ТЧ, запрет изменения, досрочное закрытие, пересечение)
- [ ] 7.5 Сценарии 12, 12а: возврат без настроек автоустановки; смешанные типы объектов
- [ ] 7.6 Сценарии 16–20: ошибка одной настройки, права на регистр настроек, типовое сообщение запрета, запись в открытом периоде, отчёт
- [ ] 7.7 Сверить стиль со стандартами 1С и префиксом `нп_`
