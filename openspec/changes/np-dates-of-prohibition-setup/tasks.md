## 1. Отдельное расширение и подсистема

- [x] 1.1 Создать `Extensions/нп_ДатыЗапретаИзменения/` как **отдельное** расширение (префикс `нп_`, purpose Customization) по образцу `нп_КонтрольПроведенияПоДО`; в `uhnp.НП` на этом этапе не добавлять объекты
- [x] 1.2 Подсистема `нп_ДатыЗапретаИзменения` (`IncludeInCommandInterface = true`): все новые объекты; служебный регистр состояния в состав входит, в командный интерфейс — нет
- [x] 1.3 Общий модуль `нп_ДатыЗапретаИзменения` (сервер) с областями ПрограммныйИнтерфейс / СлужебныеПроцедурыИФункции
- [x] 1.4 Роль `нп_БазовыеПраваДатЗапрета` этого расширения: документ заявки, отчёт, чтение регистра состояния; без регистра настроек и без константы уведомлений
- [x] 1.5 Константа `нп_ПолучателиУведомленийСдвигаДатЗапрета` и **форма константы** в подсистеме (полные права)
- [x] 1.6 README: сборка `.cfe`, загрузка отдельным расширением, перенос в НП после теста, ФО дат запрета, настройка ДО 3, первый запуск на копии

## 2. Регистр настроек автоустановки

- [x] 2.1 РС `нп_НастройкиАвтоматическойУстановкиДатЗапрета`: измерения ОбъектДатыЗапрета / Пользователь / РазделДатыЗапрета, ресурсы ЧислоДней и Включено; формы списка и записи
- [x] 2.2 Типы объекта — как у измерения «Объект» типового РС (организации, кассы, склады, банковские счета, сценарии, сценарии товарного планирования)
- [x] 2.3 Запрет записи с пустыми объектом и разделом одновременно (пояснение)
- [x] 2.4 Закрытое соответствие «тип объекта → разделы»: таблица заявки 2.1.2 **плюс** `УчетПоМСФО` для организаций (как «Продажи»)

## 3. Регистр состояния и документ заявки

- [x] 3.1 РС `нп_СостояниеОткрытыхПериодов` (служебный, без интерактивного редактирования): измерения объект/пользователь/раздел; ресурсы заявка, ДатаОкончанияДействия, ДатаЗапретаДоОткрытия, ЗаписьБылаУстановленаАвтоматически
- [x] 3.2 Документ `нп_ЗаявкаНаОткрытиеПериода`: ТЧ Объекты (колонки Раздел и Объект), ПериодС, СрокДействия, Пользователь (обязательный, «кому»), ПричинаОткрытия, Ответственный, СпособУказания (по разделам / по разделам и объектам)
- [x] 3.3 Форма документа: понятное пояснение; переключатель как в типовых датах запрета; несколько разделов в таблице; команда «Закрыть период досрочно» (полные права)
- [x] 3.4 Запрет изменения после `Согласован`; запрет пометки удаления и отмены проведения при действующем состоянии
- [x] 3.5 Интеграция формы с БИД / `ИнтеграцияС1СДокументооборот3Клиент.НачатьОбработку`
- [x] 3.6 `&После` на `ПриИзмененииСостоянияСогласования`: при `Согласован` — запись отсечки и состояния; при недоступности ДО — журнал, без записи РС

## 4. Запись в типовой РС

- [x] 4.1 Канонические значения: «для всех» = `ВидыНазначенияДатЗапрета.ДляВсехПользователей`; «общая дата раздела» = `Объект = Раздел`; абсолютная `ДатаЗапрета` без `ОписаниеДатыЗапрета`
- [x] 4.2 `Комментарий` для групп: `нп_авто|00NNN`; для прочих автозаписей: `нп_авто`
- [x] 4.3 `ОтключитьПроверкуДатЗапрета` + привилегированный режим; после набора — `ОбновитьВерсиюДатЗапретаИзменения`
- [x] 4.4 Не изменять комбинации вне включённых настроек и вне состояния открытых периодов

## 5. Регламентное задание

- [x] 5.1 Задание `нп_СдвигДатЗапретаИзменения`, расписание ежедневно 23:00
- [x] 5.2 Порядок: истёкшие открытые периоды (возврат/удаление типовой записи, удаление состояния) → применение включённых настроек
- [x] 5.3 Расчёт даты по нормативной таблице (N включая текущий день); идемпотентность в пределах суток
- [x] 5.4 Ошибка одной настройки не прерывает остальные; журнал с объектом/разделом/пользователем; уведомление по константе
- [x] 5.5 Действующее состояние блокирует автоустановку по этой комбинации

## 6. Отчёт

- [x] 6.1 Отчёт `нп_ДействующиеДатыЗапрета` (СКД): типовой РС + соединение с настройками и состоянием
- [x] 6.2 Классификация источника: автоустановка (настройка, N) / открытый период (первая заявка из состояния, окончание, ответственный) / вручную; вторая пересекающаяся заявка в отчёт не выводится
- [x] 6.3 Отборы: объект, раздел, пользователь

## 7. Проверка (сценарии приёмки заявки §5)

Живая ИБ 1С в этой среде недоступна. Правила расчёта, UUID разделов, права роли и состав метаданных — `Extensions/нп_ДатыЗапретаИзменения/tests/test_rules.py`. Сценарии 1–20 выполнять после загрузки `.cfe` на копии (см. README расширения).

- [ ] 7.1 Сценарии 1–3, 5–6: N=5, повторный запуск, сдвиг на день, отключение и удаление настройки
- [ ] 7.2 Сценарий 4: «для всех» N=5 и группа N=30 — две записи, у участника группы действует более ранняя граница
- [ ] 7.3 Сценарии 7, 7а, 7б, 7в, 7г: касса, общая дата раздела, запрет пустой пары, ручная запись склада, риск общей даты адресата
- [ ] 7.4 Сценарии 8, 8а, 9–15: заявка (обязательный пользователь, ответственный, отсечка, блокировка задания, возврат, смешанная ТЧ, запрет изменения, досрочное закрытие, пересечение)
- [ ] 7.5 Сценарии 12, 12а: возврат без настроек автоустановки; смешанные типы объектов
- [ ] 7.6 Сценарии 16–20: ошибка одной настройки, права на регистр настроек, типовое сообщение запрета, запись в открытом периоде, отчёт
- [x] 7.7 Сверить стиль со стандартами 1С и префиксом `нп_`
- [ ] 7.8 Проверить, что пользователь с `нп_БазовыеПраваДатЗапрета` без полных прав создаёт и заполняет заявку, а регистр настроек и форму константы не видит
- [ ] 7.9 После автоустановки по организации без раздела в типовом РС есть запись по разделу `УчетПоМСФО`
