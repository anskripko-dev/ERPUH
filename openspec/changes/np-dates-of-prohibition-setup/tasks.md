## 1. Метаданные расширения НП

- [ ] 1.1 Создать роль `нп_УстановкаДатЗапретаПоОрганизациям` (обработка, документ заявки, РС настроек; без прав на весь типовой РС и типовую форму дат)
- [ ] 1.2 Создать РС `нп_НастройкиАвтосдвигаДатЗапрета` (организация, N, включено)
- [ ] 1.3 Создать документ `нп_ЗаявкаНаОткрытиеПериода` (ТЧ организации; K дней; период с/по; раздел опционально) со стандартными подписками БИД на форме
- [ ] 1.4 Создать обработку `нп_УстановкаДатЗапретаИзменения` (форма бухгалтера: организации, N, Excel, создание заявки)
- [ ] 1.5 Создать регламентное задание `нп_СдвигДатЗапретаИзменения` и общий модуль `нп_ДатыЗапретаИзменения`
- [ ] 1.6 Включить объекты в подсистему расширения НП; префикс `нп_`; области модулей по стандартам

## 2. Запись в типовой РС

- [ ] 2.1 Реализовать запись ключей механизма: раздел (организации-типы или явный), объект = организация, адресат «для всех пользователей», абсолютная `ДатаЗапрета`, пустое относительное описание
- [ ] 2.2 Серверная проверка: организация из доступного пользователю списка; отказ по чужим
- [ ] 2.3 Не изменять персональные строки, кассу/склад/сценарии/БС
- [ ] 2.4 После набора записей — типовое обновление действующих дат запрета

## 3. Форма бухгалтера и Excel

- [ ] 3.1 Форма: список доступных организаций, N/автосдвиг, без открытия типовой формы администрирования
- [ ] 3.2 Загрузка Excel: табличный документ, сопоставление колонок (организация, N, дата, раздел), построчный протокол
- [ ] 3.3 Применение строк: upsert настроек автосдвига при N; разовая дата в типовой РС без включения автосдвига; пропуск чужих организаций
- [ ] 3.4 Команда создания заявки с подстановкой выбранных организаций
- [ ] 3.5 (Опционально) Собрать `.epf` в `ExternalDataProcessors/нп_УстановкаДатЗапретаИзменения/`

## 4. Автосдвиг

- [ ] 4.1 Обработчик задания: по включённым настройкам вычислить `сегодня − N`
- [ ] 4.2 Если по организации есть живое окно согласованной заявки — не писать `сегодня − N` по ключам этой заявки
- [ ] 4.3 Привилегированная запись, не блокируемая датой запрета
- [ ] 4.4 Журнал регистрации: организация, старая/новая дата, режим (сдвиг / окно заявки)

## 5. Заявка и ДО 3

- [ ] 5.1 Проверка заполнения: организации, K > 0, период с ≤ по
- [ ] 5.2 Интеграция формы с `ИнтеграцияС1СДокументооборот3` / базовой функциональностью БИД (как у пилотных документов)
- [ ] 5.3 По `Согласован`: записать в типовой РС отсечку `ПериодС − 1 день` по ключам заявки
- [ ] 5.4 Несогласованная / на согласовании заявка РС не меняет
- [ ] 5.5 Подсказка на форме: отсечка БСП откроет всё после `ПериодС − 1`, не только выбранный месяц
- [ ] 5.6 Ошибка сервиса ДО при отправке — заявка не считается согласованной

## 6. Проверка

- [ ] 6.1 Автосдвиг без заявки: граница = сегодня − N, на следующий день сдвигается
- [ ] 6.2 Заявка 5 дней: окно от даты согласования; внутри окна задание не откатывает; после истечения снова N
- [ ] 6.3 Excel: сопоставление, чужая организация, пустой и явный раздел
- [ ] 6.4 Пользователь без роли админа РС не открывает типовую форму и не пишет чужие организации
- [ ] 6.5 Персональная дата пользователя не перезаписывается
- [ ] 6.6 Сверить стиль со стандартами 1С и префиксом `нп_`
