## 1. Implementation

- [x] 1.1 Ранний выход в `УправлениеОтчетамиУХ.ПриИзмененииРегистраБухгалтерииТрансляцияПриЗаписи` для `РаспределениеРасходовБудущихПериодов`
- [x] 1.2 Тот же выход в `КорректировкиЗначенийПоказателей32.ПриИзмененииРегистраБухгалтерииТрансляция` (оба ветвления: есть движения / пустой набор)
- [ ] 1.3 Перенести вставку в расширение `uhnp.НП` (`&Вместо` подписки) при ближайшем обновлении УХ — не блокирует выкат в дамп `uhnp`

## 2. Data / close month

- [ ] 2.1 После выката повторить «Отражение документов в регламентированном учете» за август 2026, «Завод Николь-Пак»
- [ ] 2.2 Проверить РС `ОтражениеДокументовПоШаблонам` по РБП `0000-000047`, `0000-000053`, `0000-000054`: новых записей нет, закрытие не падает
- [ ] 2.3 Вручную снять незавершённые (`ОбработкаВыполнена = нет`) записи этих РБП, чтобы регламентное задание не создало `Трансляция`
