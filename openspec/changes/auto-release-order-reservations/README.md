# Автоснятие резерва заказов клиента

OpenSpec change `auto-release-order-reservations`.

**Правила v1:** срок = дата заказа + N; полная оплата — не трогать; частичная — снимать; обособление — пропускать; dry-run + журнал.

| Артефакт | Содержание |
|---|---|
| `proposal.md` | Зачем / что / scope |
| `specs/reservation-timeout/spec.md` | Требования + Given/When/Then |
| `design.md` | Расширение, алгоритм, оплата, риски, open questions |
| `tasks.md` | Чеклист реализации |
| `estimate.md` | Трудоёмкость: **8 чд** к оценке (базовый 6–8) |

Префикс объектов: **`нп_`**. Реализация — после `/opsx-apply`, в `uhnp.НП`.

Перед кодированием зафиксировать сборку и статусы «не трогать» (см. Open Questions в `design.md`).
