## 1. Метаданные расширения НП

- [x] 1.1 ~~Перечисление политик~~ не используется: при включённом контроле всегда требуется согласование
- [x] 1.2 Создать РС `нп_НастройкиКонтроляПроведенияПоДО` (измерения: полное имя + организация; ресурс: контролировать проведение)
- [x] 1.3 Создать роль `нп_ОбходКонтроляСогласованияДО` и включить в нужные профили/описание поставки расширения
- [x] 1.4 Создать общий модуль `нп_КонтрольПроведенияПоСогласованиюДО` (сервер; области ПрограммныйИнтерфейс / Служебные по стандартам)
- [x] 1.5 Создать подписку `нп_КонтрольПроведенияПоДОПередЗаписьюДокумента` → `ПроверитьПередЗаписьюДокумента`

## 2. Серверная логика контроля

- [x] 2.1 Реализовать чтение настройки по полному имени метаданных и организации с кэшем на сеанс (ПовтИсп)
- [x] 2.2 Реализовать `СостояниеСогласованияДО(Ссылка)` по запросу к типовым РС связи и состояний (без веб-вызова)
- [x] 2.3 Реализовать `РазрешеноПроведениеПоСогласованиюДО` с учётом констант ДО, настройки, политики, роли обхода
- [x] 2.4 Реализовать `ПроверитьПередЗаписьюДокумента`: фильтр загрузки/режима записи, сообщение пользователю, `Отказ`
- [x] 2.5 Добавить процедуру начальной настройки для `Документ.ПеремещениеТоваров` по организации

## 3. UX статуса на форме

- [x] 3.1 Подключить вывод статуса через `&После` на `МодификацияКонфигурацииПереопределяемый.ПриСозданииНаСервере` **или** точечно на форму `ПеремещениеТоваров` (зафиксировать выбранный вариант в комментарии модуля)
- [x] 3.2 Реализовать `ПриСозданииФормыДокумента`: для типов из настроек добавить/заполнить реквизит/декорацию состояния согласования
- [ ] 3.3 (Опционально) Ограничить доступность команды проведения на клиенте при несогласованном состоянии без ослабления серверной проверки

## 4. Внедрение и проверка

- [x] 4.1 Описать в change/README шаги: константы БИД, правило интеграции ДО для перемещения, заполнение РС настроек, роль обхода
- [ ] 4.2 Проверить в ИБ: перемещение без связи / на согласовании / согласован / запись без проведения / отмена проведения / обход ролью
- [ ] 4.3 Проверить, что тип без настройки настройки не затрагивается
- [x] 4.4 Сверить стиль модулей со `стандарты-разработки-1с.md` и паттернами SSL (области, документирование экспорта)
