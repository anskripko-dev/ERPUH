
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ПланСчетовМСФО = Неопределено;
	Если ТипЗнч(ПараметрКоманды) = Тип("СправочникСсылка.СчетаБД") Тогда
		ПланСчетовМСФО = ОбщегоНазначенияУХ.ЗначениеРеквизитаОбъекта(ПараметрКоманды, "Владелец");
	ИначеЕсли ТипЗнч(ПараметрКоманды) = Тип("СправочникСсылка.ПланыСчетовБД") Тогда
		ПланСчетовМСФО = ПараметрКоманды;
	Иначе 
		ПланСчетовМСФО = ОбщегоНазначенияУХ.ЗначениеРеквизитаОбъекта(ПараметрКоманды, "ПланСчетовБД");
	КонецЕсли;
	
	ПараметрыОповещения = Новый Структура("УникальныйИдентификатор, Команда, ПланСчетовМСФО", 
		Новый УникальныйИдентификатор, "ЗагрузитьПланСчетовМСФО", ПланСчетовМСФО);
	
	ПараметрыГрафика = Новый Структура("Заголовок", НСтр("ru = 'Данные для загрузки плана счетов'"));
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ЗаголовокФормы", 			НСтр("ru = 'Загрузка плана счетов '") + ПланСчетовМСФО);
	ПараметрыФормы.Вставить("ЗаголовокКнопкиЗагрузить", НСтр("ru = 'Загрузить план счетов'"));
	ПараметрыФормы.Вставить("АдресТаблицы", 			ПолучитьТекущийПС(ПараметрыОповещения.УникальныйИдентификатор, ПланСчетовМСФО));
	
	ОписаниеОповещения = Новый ОписаниеОповещения("Подключаемый_ПриЗакрытии", ЭтотОбъект, ПараметрыОповещения);
	
	ОткрытьФорму("Обработка.ЗагрузкаДанныхТаблицей.Форма.Форма", ПараметрыФормы, ЭтотОбъект, , , , ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПриЗакрытии(РезультатЗакрытия = Неопределено, ДополнительныеПараметры = Неопределено) Экспорт
	
	Если Не ЭтоАдресВременногоХранилища(РезультатЗакрытия) Тогда
		Возврат;
	КонецЕсли;
	
	ЗагрузитьПланСчетов(РезультатЗакрытия, ДополнительныеПараметры);	
	Оповестить("ЗаписанСчетМСФО", ДополнительныеПараметры.ПланСчетовМСФО);
	ОповеститьОбИзменении(Тип("ПланСчетовСсылка.МСФО"));
	
КонецПроцедуры

&НаСервере
Функция ПолучитьТекущийПС(УникальныйИдентификатор, ПланСчетовМСФО)

	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	МСФО.Код КАК Код,
	|	МСФО.Родитель.Код КАК Родитель,
	|	МСФО.Наименование КАК Наименование,
	|	МСФО.Вид КАК Вид,
	|	МСФО.Забалансовый КАК Забалансовый,
	|	МСФО.ВидКурса КАК ВидКурса,
	|	МСФО.ВидОтложенногоНалога КАК ВидОтложенногоНалога,
	|	МСФО.СпособРасчетаКурсовыхРазниц КАК СпособРасчетаКурсовыхРазниц,
	|	МСФО.ДетализацияКорреспонденцииСчета КАК ДетализацияКорреспонденцииСчета,
	|	МСФО.РазделПланаСчетов КАК РазделПланаСчетов,
	|	МСФО.СчетИсточник КАК СчетИсточник,
	|	МСФО.Валютный КАК Валютный,
	|	МСФО.Количественный КАК Количественный,
	|	МСФО.УчетПоПодразделениям КАК УчетПоПодразделениям,
	|	МСФО.УчетПоНаправлениямДеятельности КАК УчетПоНаправлениямДеятельности,
	|	МСФОВидыСубконто1.ВидСубконто КАК ВидСубконто1,
	|	МСФОВидыСубконто1.Валютный КАК Валютный1,
	|	МСФОВидыСубконто1.Количественный КАК Количественный1,
	|	МСФОВидыСубконто2.Суммовой КАК Суммовой1,
	|	МСФОВидыСубконто2.ТолькоСальдо КАК ТолькоСальдо1,
	|	МСФОВидыСубконто2.ТолькоОбороты КАК ТолькоОбороты1,
	|	МСФОВидыСубконто2.ВидСубконто КАК ВидСубконто2,
	|	МСФОВидыСубконто2.Валютный КАК Валютный2,
	|	МСФОВидыСубконто2.Количественный КАК Количественный2,
	|	МСФОВидыСубконто3.Суммовой КАК Суммовой2,
	|	МСФОВидыСубконто3.ТолькоСальдо КАК ТолькоСальдо2,
	|	МСФОВидыСубконто3.ТолькоОбороты КАК ТолькоОбороты2,
	|	МСФОВидыСубконто3.ВидСубконто КАК ВидСубконто3,
	|	МСФОВидыСубконто3.Валютный КАК Валютный3,
	|	МСФОВидыСубконто3.Количественный КАК Количественный3,
	|	МСФОВидыСубконто1.Суммовой КАК Суммовой3,
	|	МСФОВидыСубконто1.ТолькоСальдо КАК ТолькоСальдо3,
	|	МСФОВидыСубконто1.ТолькоОбороты КАК ТолькоОбороты3,
	|	МСФО.Наименование1 КАК Наименование1,
	|	МСФО.Наименование2 КАК Наименование2
	|ИЗ
	|	ПланСчетов.МСФО КАК МСФО
	|		ЛЕВОЕ СОЕДИНЕНИЕ ПланСчетов.МСФО.ВидыСубконто КАК МСФОВидыСубконто1
	|		ПО МСФО.Ссылка = МСФОВидыСубконто1.Ссылка
	|			И (МСФОВидыСубконто1.НомерСтроки = 1)
	|		ЛЕВОЕ СОЕДИНЕНИЕ ПланСчетов.МСФО.ВидыСубконто КАК МСФОВидыСубконто2
	|		ПО МСФО.Ссылка = МСФОВидыСубконто2.Ссылка
	|			И (МСФОВидыСубконто2.НомерСтроки = 2)
	|		ЛЕВОЕ СОЕДИНЕНИЕ ПланСчетов.МСФО.ВидыСубконто КАК МСФОВидыСубконто3
	|		ПО МСФО.Ссылка = МСФОВидыСубконто3.Ссылка
	|			И (МСФОВидыСубконто3.НомерСтроки = 3)
	|ГДЕ
	|	МСФО.ПланСчетовБД = &ПланСчетовМСФО");
	Запрос.УстановитьПараметр("ПланСчетовМСФО", ПланСчетовМСФО);
	
	Результат = Запрос.Выполнить().Выгрузить();
	Если СтрНайти(ПланСчетовМСФО.МаскаКода, ".") > 0 Тогда
		Результат.Удалить("Родитель");
	КонецЕсли;
	
	псМСФО = Метаданные.ПланыСчетов.МСФО;
	
	ГруппыРеквизитов = Новый Структура;
	ГруппыРеквизитов.Вставить("Реквизиты", 				псМСФО.Реквизиты);
	ГруппыРеквизитов.Вставить("СтандартныеРеквизиты",	псМСФО.СтандартныеРеквизиты);
	ГруппыРеквизитов.Вставить("ПризнакиУчета", 			псМСФО.ПризнакиУчета);
	ГруппыРеквизитов.Вставить("ПризнакиУчетаСубконто",	псМСФО.ПризнакиУчетаСубконто);
	ГруппыРеквизитов.Вставить("ВидыСубконто", 			псМСФО.СтандартныеТабличныеЧасти.ВидыСубконто.СтандартныеРеквизиты);
	
	Для каждого ГруппаРеквизитов Из ГруппыРеквизитов Цикл
		Для каждого ТекущийРеквизит Из ГруппаРеквизитов.Значение Цикл
			
			Если ГруппаРеквизитов.Ключ = "ВидыСубконто" Тогда
				Для НомерСубконто = 1 По 3 Цикл
					ЗаменыЗаголовков = Новый Структура(ТекущийРеквизит.Имя + НомерСубконто, ТекущийРеквизит.Синоним + " " + НомерСубконто);
				КонецЦикла;
			Иначе
				ЗаменыЗаголовков = Новый Структура(ТекущийРеквизит.Имя, ТекущийРеквизит.Синоним);
			КонецЕсли;
			
			Для каждого ТекущаяЗамена Из ЗаменыЗаголовков Цикл
			
				ТекущаяКолонка = Результат.Колонки.Найти(ТекущаяЗамена.Ключ);
				Если ТекущаяКолонка <> Неопределено Тогда
					ТекущаяКолонка.Заголовок = ТекущаяЗамена.Значение;
				КонецЕсли;
			
			КонецЦикла;
			
		КонецЦикла;
	КонецЦикла;
	
	Возврат ПоместитьВоВременноеХранилище(Результат, УникальныйИдентификатор);
	
КонецФункции

&НаСервере
Процедура ЗагрузитьПланСчетов(РезультатЗакрытия, ДополнительныеПараметры)
	
	ПланыСчетов.МСФО.ЗагрузитьПланСчетов(
		ПолучитьИзВременногоХранилища(РезультатЗакрытия), Ложь, ДополнительныеПараметры.ПланСчетовМСФО);
		
КонецПроцедуры
