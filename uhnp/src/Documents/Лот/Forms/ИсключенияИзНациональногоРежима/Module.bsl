&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Вставить содержимое обработчика
	Номенклатура = Параметры.Номенклатура;
	ИсключениеНациональногоРежима = Параметры.Исключение;
	ДополнительнаяИнформация = Параметры.ДополнительнаяИнформация;
	ОКПД2 = Параметры.Номенклатура.КодОКПД2;
КонецПроцедуры  

&НаКлиенте
Процедура ПеренестиВДокумент(Команда) 
	
	//Если  НЕ ЗначениеЗаполнено(ОКПД2) И ЗначениеЗаполнено(Исключение) Тогда
	//	Сообщить("Необходимо заполнить поле ""ОКПД2"""); 
	//	Возврат;
	//КонецЕсли;		
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Номенклатура",Номенклатура);
	ПараметрыФормы.Вставить("Исключение",ИсключениеНациональногоРежима);
	ПараметрыФормы.Вставить("ДополнительнаяИнформация",ДополнительнаяИнформация);
	
	ОповеститьОВыборе(ПараметрыФормы);
	Если ЭтаФорма.Открыта() Тогда
		ЭтаФорма.Закрыть();
	Иначе
		// Форма уже закрыта.
	КонецЕсли;

КонецПроцедуры

