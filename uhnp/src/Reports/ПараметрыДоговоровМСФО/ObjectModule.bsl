
Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	Есть9 = Ложь;
	Есть16 = Ложь;	
	НастройкиСКД  = КомпоновщикНастроек.ПолучитьНастройки();
	
	ПараметрСКД = НастройкиСКД.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("Стандарт9"));
	Если ПараметрСКД <> Неопределено И ПараметрСКД.Использование Тогда
		Есть9 = ПараметрСКД.Значение;
	КонецЕсли;
	
	ПараметрСКД = НастройкиСКД.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("Стандарт16"));
	Если ПараметрСКД <> Неопределено И ПараметрСКД.Использование Тогда
		Есть16 = ПараметрСКД.Значение;
	КонецЕсли;
	
	ВидимостьПолей = Новый Соответствие;
	
	ВидимостьПолей.Вставить(НСтр("ru = 'Финансовый инструмент(IFRS 9)'"), 	Есть9);	
	ВидимостьПолей.Вставить(НСтр("ru = 'Аренда (IFRS 16)'"), 				Есть16);	
	
	Для каждого ЭлементВыбора Из КомпоновщикНастроек.Настройки.Выбор.Элементы Цикл
		
		Если ЭлементВыбора.Поле <> Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Попытка
			Использование = ВидимостьПолей.Получить(ЭлементВыбора.Заголовок);
		Исключение
			Использование = Неопределено;
		КонецПопытки;
		
		Если Использование = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ЭлементВыбора.Использование = Использование;
		
		Если ТипЗнч(ЭлементВыбора) = Тип("ГруппаВыбранныхПолейКомпоновкиДанных") Тогда
			Для каждого ЭлементВыбораДетальные Из ЭлементВыбора.Элементы Цикл
				ЭлементВыбораДетальные.Использование = Использование;				
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры
