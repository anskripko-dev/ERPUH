#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс
#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПолучитьТаблицуПримененияФорматов() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуПримененияФорматов();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2020_1";
	Стр.КНД = "1125332";
	Стр.ВерсияФормата = "5.01";
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2021_1";
	Стр.КНД = "1125332";
	Стр.ВерсияФормата = "5.02";
	
	Возврат Результат;
КонецФункции

Функция ДанноеУведомлениеДоступноДляОрганизации() Экспорт 
	Возврат Истина;
КонецФункции

Функция ДанноеУведомлениеДоступноДляИП() Экспорт 
	Возврат Ложь;
КонецФункции

Функция ПолучитьОсновнуюФорму() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьФормуПоУмолчанию() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьТаблицуФорм() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуФормУведомления();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2021_1";
	Стр.ОписаниеФормы = "Форма учетной политики организации в формате 5.02";
	Стр.ДатаНачала = '20201001';
	Стр.ДатаКонца = '20991231';
	
	Возврат Результат;
КонецФункции

Функция ЭлектронноеПредставление(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт
	Если ИмяФормы = "Форма2020_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2020_1(Объект, УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2021_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2021_1(Объект, УникальныйИдентификатор);
	КонецЕсли;
	Возврат Неопределено;
КонецФункции

Функция ПроверитьДокумент(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт
	Если ИмяФормы = "Форма2020_1" Или ИмяФормы = "Форма2021_1" Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru='Электронный формат для данной формы не опубликован'"));
		Возврат Неопределено;
	КонецЕсли;
КонецФункции

Функция СформироватьСписокЛистов(Объект) Экспорт
	Если Объект.ИмяФормы = "Форма2020_1" Тогда 
		Возврат УведомлениеОСпецрежимахНалогообложения.ПечатьВСледующихВерсиях(Объект);
	ИначеЕсли Объект.ИмяФормы = "Форма2021_1" Тогда 
		Возврат СформироватьСписокЛистовФорма2021_1(Объект);
	КонецЕсли;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт 
	Если ИмяФормы = "Форма2020_1" Тогда 
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2020_1(УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2021_1" Тогда 
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2021_1(УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	КонецЕсли;
КонецФункции

Функция ИдентификаторФайлаЭлектронногоПредставления_Форма2020_1(СведенияОтправки)
	Префикс = "ON_UCHPOLNALMON";
	Возврат Документы.УведомлениеОСпецрежимахНалогообложения.ИдентификаторФайлаЭлектронногоПредставления(Префикс, СведенияОтправки);
КонецФункции

Процедура Проверить_ОбщПолож(Данные, ТаблицаОшибок)
	Страница = "ОбщПолож";
	ДанныеСтраницы = Данные.ДанныеУведомления[Страница];
	
	Если Не ЗначениеЗаполнено(ДанныеСтраницы["ПорядВеденПрПорВеден"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ПорядВеденПрПорВеден"));
	КонецЕсли;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр1 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["ВидДеятПрДеят"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ВидДеятПрДеят___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр2 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["НаличОППрНалОП"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "НаличОППрНалОП___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр3 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["СпособВеденПрПорВеден"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "СпособВеденПрПорВеден___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура Проверить_НДС_2021(Данные, ТаблицаОшибок)
	Страница = "НДС";
	ДанныеСтраницы = Данные.ДанныеУведомления[Страница];
	
	Если Не ЗначениеЗаполнено(ДанныеСтраницы["НалМетНДСПрМетНДС"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "НалМетНДСПрМетНДС"));
	КонецЕсли;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр4 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["ПорРасхПрПорРасх"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ПорРасхПрПорРасх___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр5 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["РаспРасхПрРаспРасх"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "РаспРасхПрРаспРасх___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура Проверить_Приб_2021(Данные, ТаблицаОшибок)
	Страница = "Приб";
	ДанныеСтраницы = Данные.ДанныеУведомления[Страница];
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр7 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["РегистрУчПрРегистрУч"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "РегистрУчПрРегистрУч___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;

	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр9 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["РезервОргКратОпис"]) И Не ЗначениеЗаполнено(Стр["РезервОргПрРезервОрг"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "РезервОргПрРезервОрг___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр10 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["МетСписЦБКратОпис"]) И Не ЗначениеЗаполнено(Стр["МетСписЦБПрМетСписЦБ"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "МетСписЦБПрМетСписЦБ___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр11 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["ДолПрибОПКратОпис"]) И Не ЗначениеЗаполнено(Стр["ДолПрибОППрДолПрибОП"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ДолПрибОППрДолПрибОП___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр12 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["ПорСделЦБКратОпис"]) И Не ЗначениеЗаполнено(Стр["ПорСделЦБПрПорСделЦБ"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ПорСделЦБПрПорСделЦБ___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр15 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["АмортПремКратОпис"]) И Не ЗначениеЗаполнено(Стр["АмортПремПрАмортПрем"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "АмортПремПрАмортПрем___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр16 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["НачислАмортПрНачислАморт"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "НачислАмортПрНачислАморт___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр17 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["АмортИмущПрАмортИмущ"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "АмортИмущПрАмортИмущ___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр18 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["ОценМатПроизвПрОцМатПроизв"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ОценМатПроизвПрОцМатПроизв___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр19 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["ПерПрямРасхКратОпис"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указано описание", Страница, "ПерПрямРасхКратОпис___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр21 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["ОценПокТовКратОпис"]) И Не ЗначениеЗаполнено(Стр["ОценПокТовПрОценПокТов"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ОценПокТовПрОценПокТов___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Элт Из СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок("ОтчПерПрОтчПер,
		|АвансПлатПрАвансПлат,МетДохРасхПрМетДохРасх,
		|УчетТМЦПрУчетТМЦ,РасхПриобрИмПрРасхПриобрИм", ",", Истина, Истина) Цикл 
		
		Если Не ЗначениеЗаполнено(ДанныеСтраницы[Элт]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, Элт));
		КонецЕсли;
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ДанныеСтраницы["ДохРабДТЦКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ДохРабДТЦПрДохРабДТЦ"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ДохРабДТЦПрДохРабДТЦ"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["ДохНИОКРКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ДохНИОКРПрДохНИОКР"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ДохНИОКРПрДохНИОКР"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["ДохАрендКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ДохАрендПрДохАренд"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ДохАрендПрДохАренд"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["АмортАрендОСКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["АмортАрендОСПрАмортАрендОС"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "АмортАрендОСПрАмортАрендОС"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["СрокИспОСРекКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["СрокИспОСРекПрСрокИспОСРек"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "СрокИспОСРекПрСрокИспОСРек"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["СрокИспОСБУКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["СрокИспОСБУПрСрокИспОСБУ"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "СрокИспОСБУПрСрокИспОСБУ"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["КоэфАмортКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["КоэфАмортПрКоэфАморт"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "КоэфАмортПрКоэфАморт"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["ЦенРынокЦБКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ЦенРынокЦБПрЦенРынокЦБ"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ЦенРынокЦБПрЦенРынокЦБ"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["РаспРасхНедрКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["РаспРасхНедрПрРаспРасхНедр"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "РаспРасхНедрПрРаспРасхНедр"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["РасхПравЗУКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["РасхПравЗУПрРасхПравЗУ"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "РасхПравЗУПрРасхПравЗУ"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["ПорРаспПрРасхКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ПорРаспПрРасхПрПорРаспРасх"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ПорРаспПрРасхПрПорРаспРасх"));
	КонецЕсли;
КонецПроцедуры

Процедура Проверить_НалИмущ(Данные, ТаблицаОшибок)
	Страница = "НалИмущ";
	Инд = 0;
	ЕстьСтроки = Ложь;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр6 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["НалИмущКратОпис"]) И Не ЗначениеЗаполнено(Стр["НалИмущПрНалИмущ"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "НалИмущПрНалИмущ___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
		ЕстьСтроки = ЕстьСтроки Или ЗначениеЗаполнено(Стр["НалИмущКратОпис"]) Или ЗначениеЗаполнено(Стр["НалИмущПрНалИмущ"]);
	КонецЦикла;
	
	Если Не ЕстьСтроки Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не заполнены данные по налогу на имущество", Страница, "НалИмущПрНалИмущ___1"));
	КонецЕсли;
КонецПроцедуры

Процедура Проверить_НДПИ_2021(Данные, ТаблицаОшибок)
	Страница = "НДПИ";
	ДанныеСтраницы = Данные.ДанныеУведомления[Страница];
	Если Не ЗначениеЗаполнено(ДанныеСтраницы["РаздУчРасхПрРаздУчРасх"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "РаздУчРасхПрРаздУчРасх"));
	КонецЕсли;
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр13 Цикл 
		Инд = Инд + 1;
		Если Не ЗначениеЗаполнено(Стр["ОпрКолДПИПрОпрКолДПИ"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "ОпрКолДПИПрОпрКолДПИ___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
	Инд = 0;
	Для Каждого Стр Из Данные.ДанныеДопСтрокБД.МнгСтр14 Цикл 
		Инд = Инд + 1;
		Если ЗначениеЗаполнено(Стр["СтоимДПИКратОпис"]) И Не ЗначениеЗаполнено(Стр["СтоимДПИПрСтоимДПИ"]) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан обязательный признак", Страница, "СтоимДПИПрСтоимДПИ___" + Формат(Инд, "ЧГ=")));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура Проверить_Акциз_2021(Данные, ТаблицаОшибок)
	Страница = "Акциз";
	ДанныеСтраницы = Данные.ДанныеУведомления[Страница];
	Если ЗначениеЗаполнено(ДанныеСтраницы["КолАкцКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["КолАкцПрКолАкц"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "КолАкцПрКолАкц"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["ВычАкцКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ВычАкцПрВычАкц"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ВычАкцПрВычАкц"));
	КонецЕсли;
КонецПроцедуры

Процедура Проверить_ПрибКГН(Данные, ТаблицаОшибок)
	Страница = "ПрибКГН";
	ДанныеСтраницы = Данные.ДанныеУведомления[Страница];
	Если ЗначениеЗаполнено(ДанныеСтраницы["РасчНалБазКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["РасчНалБазПрРасчНалБаз"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "РасчНалБазПрРасчНалБаз"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["ОпрНалБазКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["ОпрНалБазПрОпрНалБаз"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "ОпрНалБазПрОпрНалБаз"));
	КонецЕсли;
	Если ЗначениеЗаполнено(ДанныеСтраницы["УчУбытКГНКратОпис"]) И Не ЗначениеЗаполнено(ДанныеСтраницы["УчУбытКГНПрУчУбытКГН"]) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан обязательный признак", Страница, "УчУбытКГНПрУчУбытКГН"));
	КонецЕсли;
КонецПроцедуры

Функция ПроверитьДокументСВыводомВТаблицу_Форма2020_1(Данные, УникальныйИдентификатор)
	Возврат УведомлениеОСпецрежимахНалогообложения.ТаблицаОшибокУстаревшаяФорма();
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2021_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	УведомлениеОСпецрежимахНалогообложения.ПроверкаАктуальностиФормыПриВыгрузке(
		Данные.Объект.ИмяФормы, ТаблицаОшибок, ПолучитьТаблицуФорм());
	
	Титульная = Данные.ДанныеУведомления.Титульная;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаКодаНО(Титульная.КодНО, ТаблицаОшибок, "Титульная");
	Если Не ЗначениеЗаполнено(Титульная.Наименование) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано наименование организации", "Титульная", "Наименование"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ДАТА_ПОДПИСИ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата формирования документа", "Титульная", "ДАТА_ПОДПИСИ"));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Титульная.ГодУчПол) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан год начала действия учетной политики", "Титульная", "ГодУчПол"));
	ИначеЕсли СтрДлина(СокрЛП(Титульная.ГодУчПол)) <> 4 Тогда
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Неправильно указан год начала действия учетной политики", "Титульная", "ГодУчПол"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ДатаУчПол) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата приказа утверждения учетной политики организации", "Титульная", "ДатаУчПол"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Титульная.ПриказУчПол) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан номер  приказа утверждения учетной политики организации", "Титульная", "ПриказУчПол"));
	КонецЕсли;
	
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаИННКПП(Истина, Титульная, ТаблицаОшибок);
	Если Не ЗначениеЗаполнено(Титульная.ПРИЗНАК_НП_ПОДВАЛ) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан признак подписанта", "Титульная", "ПРИЗНАК_НП_ПОДВАЛ"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложенияСлужебный.ПроверкаПодписантаНалоговойОтчетности(Данные, ТаблицаОшибок, "Титульная", Истина);
	Если Не ЗначениеЗаполнено(Титульная.Долж) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана должность подписанта", "Титульная", "Долж"));
	КонецЕсли;
	
	Если Титульная.ПРИЗНАК_НП_ПОДВАЛ = "2" И Не ЗначениеЗаполнено(Титульная.НаимДок) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указан документ подписанта", "Титульная", "НаимДок"));
	КонецЕсли;
	
	Проверить_ОбщПолож(Данные, ТаблицаОшибок);
	Проверить_НДС_2021(Данные, ТаблицаОшибок);
	Проверить_Приб_2021(Данные, ТаблицаОшибок);
	Проверить_НалИмущ(Данные, ТаблицаОшибок);
	Проверить_НДПИ_2021(Данные, ТаблицаОшибок);
	Проверить_Акциз_2021(Данные, ТаблицаОшибок);
	Проверить_ПрибКГН(Данные, ТаблицаОшибок);
	
	Для Каждого Стр Из Данные.ПриложенныеФайлы.Строки Цикл 
		Если Стр.Строки.Количество() = 0 Тогда
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"К каждому документу необходимо приложить файлы", "ПриложенныеФайлы", ""));
			Прервать;
		КонецЕсли;
	КонецЦикла;
	УведомлениеОСпецрежимахНалогообложения.ПроверкаДатВУведомлении(Данные, ТаблицаОшибок);
	УведомлениеОСпецрежимахНалогообложения.ПолнаяПроверкаЗаполненныхПоказателейНаСоотвествиеСписку("СпискиВыбора2020_1",
		"СхемаВыгрузкиФорма2021_1", Данные.Объект.ИмяОтчета, ТаблицаОшибок, Данные);
	Возврат ТаблицаОшибок;
КонецФункции

Функция ОсновныеСведенияЭлектронногоПредставления_Форма2020_1(Объект, УникальныйИдентификатор)
	ОсновныеСведения = Документы.УведомлениеОСпецрежимахНалогообложения.НачальнаяИнициализацияОбщихРеквизитовВыгрузки(Объект);
	
	Данные = Объект.ДанныеУведомления.Получить();
	КолФайл = 0;
	Для Каждого Стр Из Данные.ПриложенныеФайлы.Строки Цикл 
		КолФайл = КолФайл + Стр.Строки.Количество();
	КонецЦикла;
	ОсновныеСведения.Вставить("КолФайл", КолФайл);
	
	ИдентификаторФайла = ИдентификаторФайлаЭлектронногоПредставления_Форма2020_1(ОсновныеСведения);
	ОсновныеСведения.Вставить("ИдФайл", ИдентификаторФайла);
	
	Возврат ОсновныеСведения;
КонецФункции

Функция ЭлектронноеПредставление_Форма2020_1(Объект, УникальныйИдентификатор)
	СписокФайлов = Новый СписокЗначений;
	ИменаФайлов = Новый СписокЗначений;
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2020_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2020_1(Объект, УникальныйИдентификатор);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2020_1");
	ЗаполнитьДанными_Форма2020_1(Объект, ОсновныеСведения, СтруктураВыгрузки, СписокФайлов, ИменаФайлов);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.РаботаСФайлами") Тогда
		МодульРаботаСФайлами = ОбщегоНазначения.ОбщийМодуль("РаботаСФайлами");
		Для Инд = 0 По СписокФайлов.Количество() - 1 Цикл 
			Файл = СписокФайлов[Инд];
			ИмяФайлаПрод = ИменаФайлов[Инд];
			СтрокаСведенийЭлектронногоПредставления = СведенияЭлектронногоПредставления.Добавить();
			СтрокаСведенийЭлектронногоПредставления.ИмяФайла = ИмяФайлаПрод;
			СтрокаСведенийЭлектронногоПредставления.ДвоичныеДанныеФайла = МодульРаботаСФайлами.ДвоичныеДанныеФайла(Файл.Значение);
		КонецЦикла;
	КонецЕсли;
	
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция ЭлектронноеПредставление_Форма2021_1(Объект, УникальныйИдентификатор)
	СписокФайлов = Новый СписокЗначений;
	ИменаФайлов = Новый СписокЗначений;
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2021_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2020_1(Объект, УникальныйИдентификатор);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2021_1");
	ЗаполнитьДанными_Форма2020_1(Объект, ОсновныеСведения, СтруктураВыгрузки, СписокФайлов, ИменаФайлов);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.РаботаСФайлами") Тогда
		МодульРаботаСФайлами = ОбщегоНазначения.ОбщийМодуль("РаботаСФайлами");
		Для Инд = 0 По СписокФайлов.Количество() - 1 Цикл 
			Файл = СписокФайлов[Инд];
			ИмяФайлаПрод = ИменаФайлов[Инд];
			СтрокаСведенийЭлектронногоПредставления = СведенияЭлектронногоПредставления.Добавить();
			СтрокаСведенийЭлектронногоПредставления.ИмяФайла = ИмяФайлаПрод;
			СтрокаСведенийЭлектронногоПредставления.ДвоичныеДанныеФайла = МодульРаботаСФайлами.ДвоичныеДанныеФайла(Файл.Значение);
		КонецЦикла;
	КонецЕсли;
	
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Процедура ДополнитьПараметры_2020(Параметры)
КонецПроцедуры

Процедура ЗаполнитьДанными_Форма2020_1(Объект, Параметры, ДеревоВыгрузки, СписокФайлов, ИменаФайлов)
	Документы.УведомлениеОСпецрежимахНалогообложения.ОбработатьУсловныеЭлементы(Параметры, ДеревоВыгрузки);
	Документы.УведомлениеОСпецрежимахНалогообложения.ЗаполнитьПараметрыСРазделами(Параметры, ДеревоВыгрузки);
	ДанныеУведомления = Объект.ДанныеУведомления.Получить();
	ДополнитьПараметры_2020(ДанныеУведомления);
	УведомлениеОСпецрежимахНалогообложения.ЗаполнитьДаннымиУзелНов(ДанныеУведомления, ДеревоВыгрузки);
	
	ДатаИмФайла = Формат(ТекущаяДатаСеанса(), "ДФ=_yyyyMMdd_");
	Узел_Документ = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(ДеревоВыгрузки, "Документ");
	Узел_СведУчПол = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(Узел_Документ, "СведУчПол");
	Узел_ДокСкан = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(Узел_СведУчПол, "ДокСкан");
	Для Каждого Стр0 Из ДанныеУведомления.ПриложенныеФайлы.Строки Цикл
		УИД1 = Строка(Новый УникальныйИдентификатор);
		НовУзел_ДокСкан = Документы.УведомлениеОСпецрежимахНалогообложения.НовыйУзелИзПрототипа(Узел_ДокСкан);
		Документы.УведомлениеОСпецрежимахНалогообложения.УстановитьЗначениеЭлемента(НовУзел_ДокСкан, "НаимДок", Стр0.Документ);
		
		Узел_ИмяФайл = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(НовУзел_ДокСкан, "ИмяФайл");
		Для Каждого Стр1 Из Стр0.Строки Цикл
			УИД2 = Строка(Новый УникальныйИдентификатор);
			НовУзел_ИмяФайл = Документы.УведомлениеОСпецрежимахНалогообложения.НовыйУзелИзПрототипа(Узел_ИмяФайл);
			ИмяФайлаПрод = "1125332_" + Параметры.ИННЮЛ + Параметры.КПП + "_"  + Параметры.КодНО 
							+ "_" + УИД1 + ДатаИмФайла + УИД2 + ".pdf";
			РегламентированнаяОтчетность.ВывестиПоказательСтатистикиВXML(НовУзел_ИмяФайл, ИмяФайлаПрод);
			
			ИменаФайлов.Добавить(ИмяФайлаПрод);
			СписокФайлов.Добавить(Стр1.ПрисоединенныйФайл);
		КонецЦикла;
	КонецЦикла;
	РегламентированнаяОтчетность.УдалитьУзел(Узел_ДокСкан);
	Документы.УведомлениеОСпецрежимахНалогообложения.ОтсечьНезаполненныеНеобязательныеУзлы(ДеревоВыгрузки);
КонецПроцедуры

Функция ПараметрВОбласти(ОбластьПечати, ИмяПараметра)
	УИД = Новый УникальныйИдентификатор;
	ПроверочнаяСтруктура = Новый Структура(ИмяПараметра, УИД);
	ЗаполнитьЗначенияСвойств(ПроверочнаяСтруктура, ОбластьПечати.Параметры);
	Возврат (УИД <> ПроверочнаяСтруктура[ИмяПараметра]);
КонецФункции

Процедура УстановитьПараметрПоКоду(Данные, ОбластьПечати, РеквизитКод, РеквизитОписание)
	Инд = 0;
	Пока Истина Цикл 
		Инд = Инд + 1;
		ИмяПараметра = РеквизитКод + "_" + Инд;
		Если Не ПараметрВОбласти(ОбластьПечати, ИмяПараметра) Тогда 
			Прервать;
		КонецЕсли;
		ОбластьПечати.Параметры[РеквизитКод + "_" + Инд] = "-";
	КонецЦикла;
	
	ЕстьОтветДа = Ложь;
	Если ТипЗнч(Данные) = Тип("ТаблицаЗначений") Тогда 
		Для Каждого Стр Из Данные Цикл 
			ДанныеПечати = ОбщегоНазначения.СтрокаТаблицыЗначенийВСтруктуру(Стр);
			ИмяИтоговогоПараметра = РеквизитОписание + "_" + ДанныеПечати[РеквизитКод];
			ЗаполнитьЗначенияСвойств(ОбластьПечати.Параметры, Новый Структура(ИмяИтоговогоПараметра, ДанныеПечати[РеквизитОписание]));
			
			Если ЗначениеЗаполнено(ДанныеПечати[РеквизитКод]) Тогда 
				ИмяПараметра = РеквизитКод + "_" + ДанныеПечати[РеквизитКод];
				Если ПараметрВОбласти(ОбластьПечати, ИмяПараметра) Тогда 
					ОбластьПечати.Параметры[ИмяПараметра] = "да";
					ЕстьОтветДа = Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	ИначеЕсли ТипЗнч(Данные) = Тип("Структура") Тогда 
		ИмяИтоговогоПараметра = РеквизитОписание + "_" + Данные[РеквизитКод];
		ЗаполнитьЗначенияСвойств(ОбластьПечати.Параметры, Новый Структура(ИмяИтоговогоПараметра, Данные[РеквизитОписание]));
		
		Если ЗначениеЗаполнено(Данные[РеквизитКод]) Тогда 
			ИмяПараметра = РеквизитКод + "_" + Данные[РеквизитКод];
			Если ПараметрВОбласти(ОбластьПечати, ИмяПараметра) Тогда 
				ОбластьПечати.Параметры[ИмяПараметра] = "да";
				ЕстьОтветДа = Истина;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ПараметрВОбласти(ОбластьПечати, РеквизитКод + "_0") Тогда
		ОбластьПечати.Параметры[РеквизитКод + "_0"] = ?(ЕстьОтветДа, "да", "-");
	КонецЕсли;
КонецПроцедуры

Функция СформироватьСписокЛистовФорма2021_1(Объект) Экспорт
	Листы = Новый СписокЗначений;
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
	МакетПФ = Отчеты[Объект.ИмяОтчета].ПолучитьМакет("ПечатныйБланк_2021");
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Заголовок");
	
	ДанныеУведомления = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.Ссылка, "ДанныеУведомления").Получить();
	Титульная = ДанныеУведомления.ДанныеУведомления.Титульная;
	ЗаполнитьЗначенияСвойств(ОбластьПечати.Параметры, Титульная);
	УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Титульная.ИНН, "ИНН", ОбластьПечати.Области, "-");
	УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Титульная.КПП, "КПП", ОбластьПечати.Области, "-");
	УведомлениеОСпецрежимахНалогообложения.ВывестиСтрокуНаПечать(Титульная.КодНО, "КодНО", ОбластьПечати.Области, "-");
	ОбластьПечати.Параметры.НаименованиеНО = "ИФНС " + Титульная.КодНО;
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	ОбщПолож = ДанныеУведомления.ДанныеУведомления.ОбщПолож;
	ОбластьПечати = МакетПФ.ПолучитьОбласть("ОбщПолож");
	УстановитьПараметрПоКоду(ОбщПолож, ОбластьПечати, "ПорядВеденПрПорВеден", "ПорядВеденКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр1, ОбластьПечати, "ВидДеятПрДеят", "ВидДеятКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр2, ОбластьПечати, "НаличОППрНалОП", "НаличОПКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр3, ОбластьПечати, "СпособВеденПрПорВеден", "СпособВеденКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	НДС = ДанныеУведомления.ДанныеУведомления.НДС;
	ОбластьПечати = МакетПФ.ПолучитьОбласть("НДС");
	УстановитьПараметрПоКоду(НДС, ОбластьПечати, "НалМетНДСПрМетНДС", "НалМетНДСКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр4, ОбластьПечати, "ПорРасхПрПорРасх", "ПорРасхКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр5, ОбластьПечати, "РаспРасхПрРаспРасх", "РаспРасхКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	Приб = ДанныеУведомления.ДанныеУведомления.Приб;
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Приб_1");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "ОтчПерПрОтчПер", "ОтчПерКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "АвансПлатПрАвансПлат", "АвансПлатКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "МетДохРасхПрМетДохРасх", "МетДохРасхКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "ДохРабДТЦПрДохРабДТЦ", "ДохРабДТЦКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "ДохНИОКРПрДохНИОКР", "ДохНИОКРКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "ДохАрендПрДохАренд", "ДохАрендКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр7, ОбластьПечати, "РегистрУчПрРегистрУч", "РегистрУчКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр11, ОбластьПечати, "ДолПрибОППрДолПрибОП", "ДолПрибОПКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Приб_39_1");
	ОбластьПечати.Параметры.ПерПрямРасхКратОписНаличие = "-";
	Для Каждого Стр Из ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр19 Цикл 
		ОбластьПечати.Параметры.ПерПрямРасхКратОпис = Стр.ПерПрямРасхКратОпис;
		ОбластьПечати.Параметры.ПерПрямРасхКратОписНаличие = ?(ЗначениеЗаполнено(Стр.ПерПрямРасхКратОпис), "да", "-");
		ПечатнаяФорма.Вывести(ОбластьПечати);
		ОбластьПечати = МакетПФ.ПолучитьОбласть("Приб_39_2");
	КонецЦикла;
	Если ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр19.Количество() = 0 Тогда 
		ПечатнаяФорма.Вывести(ОбластьПечати);
	КонецЕсли;
	
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Приб_2");
	ЗаполнитьЗначенияСвойств(ОбластьПечати.Параметры, Приб);
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "ПорРаспПрРасхПрПорРаспРасх", "ПорРаспПрРасхКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "УчетТМЦПрУчетТМЦ", "УчетТМЦКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "РасхПриобрИмПрРасхПриобрИм", "РасхПриобрИмКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "АмортАрендОСПрАмортАрендОС", "АмортАрендОСКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "СрокИспОСРекПрСрокИспОСРек", "СрокИспОСРекКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "СрокИспОСБУПрСрокИспОСБУ", "СрокИспОСБУКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "КоэфАмортПрКоэфАморт", "КоэфАмортКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "ЦенРынокЦБПрЦенРынокЦБ", "ЦенРынокЦБКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "РаспРасхНедрПрРаспРасхНедр", "РаспРасхНедрКратОпис");
	УстановитьПараметрПоКоду(Приб, ОбластьПечати, "РасхПравЗУПрРасхПравЗУ", "РасхПравЗУКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр8, ОбластьПечати, "ПокРаспПрРасхПрПокРаспРасх", "ПокРаспПрРасхКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр21, ОбластьПечати, "ОценПокТовПрОценПокТов", "ОценПокТовКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр18, ОбластьПечати, "ОценМатПроизвПрОцМатПроизв", "ОценМатПроизвКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр17, ОбластьПечати, "АмортИмущПрАмортИмущ", "АмортИмущКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр16, ОбластьПечати, "НачислАмортПрНачислАморт", "НачислАмортКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр15, ОбластьПечати, "АмортПремПрАмортПрем", "АмортПремКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр9, ОбластьПечати, "РезервОргПрРезервОрг", "РезервОргКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр12, ОбластьПечати, "ПорСделЦБПрПорСделЦБ", "ПорСделЦБКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр10, ОбластьПечати, "МетСписЦБПрМетСписЦБ", "МетСписЦБКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	ОбластьПечати = МакетПФ.ПолучитьОбласть("НалИмущ");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр6, ОбластьПечати, "НалИмущПрНалИмущ", "НалИмущКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	НДПИ = ДанныеУведомления.ДанныеУведомления.НДПИ;
	ОбластьПечати = МакетПФ.ПолучитьОбласть("НДПИ");
	УстановитьПараметрПоКоду(НДПИ, ОбластьПечати, "РаздУчРасхПрРаздУчРасх", "РаздУчРасхКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр13, ОбластьПечати, "ОпрКолДПИПрОпрКолДПИ", "ОпрКолДПИКратОпис");
	УстановитьПараметрПоКоду(ДанныеУведомления.ДанныеДопСтрокБД.МнгСтр14, ОбластьПечати, "СтоимДПИПрСтоимДПИ", "СтоимДПИКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	Акциз = ДанныеУведомления.ДанныеУведомления.Акциз;
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Акциз");
	УстановитьПараметрПоКоду(Акциз, ОбластьПечати, "КолАкцПрКолАкц", "КолАкцКратОпис");
	УстановитьПараметрПоКоду(Акциз, ОбластьПечати, "ВычАкцПрВычАкц", "ВычАкцКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	ПрибКГН = ДанныеУведомления.ДанныеУведомления.ПрибКГН;
	ОбластьПечати = МакетПФ.ПолучитьОбласть("ПрибКГН");
	УстановитьПараметрПоКоду(ПрибКГН, ОбластьПечати, "РасчНалБазПрРасчНалБаз", "РасчНалБазКратОпис");
	УстановитьПараметрПоКоду(ПрибКГН, ОбластьПечати, "ОпрНалБазПрОпрНалБаз", "ОпрНалБазКратОпис");
	УстановитьПараметрПоКоду(ПрибКГН, ОбластьПечати, "УчУбытКГНПрУчУбытКГН", "УчУбытКГНКратОпис");
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Иное");
	ЗаполнитьЗначенияСвойств(ОбластьПечати.Параметры, ДанныеУведомления.ДанныеУведомления.Иное);
	ПечатнаяФорма.Вывести(ОбластьПечати);
	
	ОбластьПечати = МакетПФ.ПолучитьОбласть("Подвал");
	ЗаполнитьЗначенияСвойств(ОбластьПечати.Параметры, Титульная);
	ОбластьПечати.Параметры.Должность = Титульная.ФИО_РУКОВОДИТЕЛЯ_ПРЕДСТАВИТЕЛЯ + "  " + Титульная.Долж;
	ПечатнаяФорма.Вывести(ОбластьПечати);
	УведомлениеОСпецрежимахНалогообложения.ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, 1, Ложь);
	Возврат Листы;
КонецФункции

#КонецОбласти
#КонецЕсли
