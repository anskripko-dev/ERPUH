
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	Отчеты.РасхожденияВГОДляПисьма.ЗаполнитьЗаголовки(СхемаКомпоновкиДанных);
	
	Настройки = КомпоновщикНастроек.ПолучитьНастройки();
	
	ЭлементыОтбора = ОбщегоНазначенияКлиентСервер.НайтиЭлементыИГруппыОтбора(Настройки.Отбор, "Организация");
	
	ПараметрОрганизация = КомпоновщикНастроек.Настройки.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("Организация"));
	ПараметрОрганизация.Значение = Справочники.Организации.ПустаяСсылка();
	ПараметрОрганизация.Использование = Истина;
	
	Для каждого ЭлементОтбора Из ЭлементыОтбора Цикл
		Если ЭлементОтбора.Использование Тогда
			ПараметрОрганизация.Значение = ЭлементОтбора.ПравоеЗначение;
		КонецЕсли;
	КонецЦикла;
	
	ЕстьРасчет = ОбщегоНазначенияПовтИспУХ.РассчитыватьРасхожденияПорталаВГО();
	Если ЕстьРасчет Тогда
		Возврат;
	КонецЕсли;
	
	НастройкиСКД = КомпоновщикНастроек.ПолучитьНастройки();
	
	Отбор = Новый Структура("ПериодСценария,Сценарий");
	Отбор.Вставить("ПериодСценария", 	ТиповыеОтчетыУХ.ПолучитьЗначениеПараметра(НастройкиСКД, "ПериодСверки") );
	Отбор.Вставить("Сценарий", 			ТиповыеОтчетыУХ.ПолучитьЗначениеПараметра(НастройкиСКД, "Сценарий") );
	
	ТекстЗапроса = СверкаВГОУХ.ТекстЗапроса_ОтчетВГО(Отбор, "ПериодСверки");
	ЭтотОбъект.СхемаКомпоновкиДанных.НаборыДанных[0].Запрос = СтрЗаменить(ТекстЗапроса, "СверкаЗакрыта", "СверкаЗавершена");
			
КонецПроцедуры

#КонецЕсли 
