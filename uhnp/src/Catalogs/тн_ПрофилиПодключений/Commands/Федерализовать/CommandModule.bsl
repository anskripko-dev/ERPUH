
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	// Борисенко И.Н. 23.07.2021 Выполнение задач №000035594: закомментировал

	//Если ПараметрКоманды = Неопределено Тогда
	//	Возврат;
	//КонецЕсли;
	//
	//Если Не Доступно(ПараметрКоманды) Тогда
	//	Сообщить("Объект уже федерализован");
	//	Возврат;
	//КонецЕсли;	
	//
	//Ответ = Вопрос("Перенести элемент "+Строка(ПараметрКоманды)+" в федеральную часть элементов справочника ?", РежимДиалогаВопрос.ДаНет);
	//Если Ответ = КодВозвратаДиалога.Да Тогда
	//	ОбщегоНазначения.ПереводОбъектовВЦБЧасть(ПараметрКоманды);
	//КонецЕсли;
	//
	//Если ПараметрыВыполненияКоманды.Источник <> Неопределено Тогда 
	//	Если ПараметрыВыполненияКоманды.Источник.ИмяФормы = "Справочник.ТН_ПрофилиПодключений.Форма.ФормаЭлемента" Тогда 
	//		ПараметрыВыполненияКоманды.Источник.Прочитать();
	//		ПараметрыВыполненияКоманды.Источник.УстановитьВидимостьКоманд();
	//	Иначе 
	//		ПараметрыВыполненияКоманды.Источник.ОтобразитьИзменениеДанных(ПараметрКоманды, ВидИзмененияДанных.Изменение);
	//		ПараметрыВыполненияКоманды.Источник.УстановитьВидимомостьКомандДляЭлемента(ПараметрыВыполненияКоманды.Источник.ТекущийЭлемент);
	//	КонецЕсли;
	//КонецЕсли;
	
КонецПроцедуры // ОбработкаКоманды()

&НаСервере
Функция Доступно(ПараметрКоманды)
	
	Возврат НЕ ПараметрКоманды.ЯвляетсяОбъектомЦБ;
	
КонецФункции // Доступно()
