Перем Используется77; // Признак использования обращения к ВИБ на платформе 7.7
Перем ТекСоединениеВИБ Экспорт; // Текущее соединение с внешней информационной базой
Перем РегистрБД;
Перем ТекущаяИБ;
Перем мОписаниеТиповСтрока;
Перем мОписаниеТиповБулево;
Перем мОписаниеТиповТЗ;
Перем мОписаниеТиповСписокЗначений;

Процедура ЗаполнитьРеквизитыОбъекта(Кэш = Неопределено, ЕстьИзменения = Истина) Экспорт
	
	Если Не ПроверитьНаименование() Тогда		
		Возврат;		
	КонецЕсли;
	
	ТекущаяИБ = (Владелец = Справочники.ТипыБазДанных.ТекущаяИБ);
	ПолеСиноним=РаботаСОбъектамиМетаданныхУХ.ПолучитьПолеСиноним();
	
	Если НЕ ТекущаяИБ Тогда
		
		Если ТекСоединениеВИБ = Неопределено Тогда
			
			ТекСоединениеВИБ = ОбщегоНазначенияУХ.ПолучитьСоединениеСВИБПоУмолчанию(Владелец, 1);
			
			Если ТекСоединениеВИБ = Неопределено Тогда
				
				Возврат;
				
			КонецЕсли;
			
		КонецЕсли;
		
	Иначе
		
		ТекСоединениеВИБ = РаботаСОбъектамиМетаданныхУХ.КонтекстМетаданных();
		
	КонецЕсли;
	
	СтруктураОписания = РаботаСОбъектамиМетаданныхУХ.ПолучитьСтруктуруОписанияРегистраБухгалтерииБД(
		Наименование, ТекСоединениеВИБ, Использование77(), Кэш);
	Если НЕ ЗначениеЗаполнено(СтруктураОписания.Регистраторы) Тогда
		СтруктураОписания.Регистраторы = "";
	КонецЕсли;	
	
	Если СтруктураОписания.Свойство("ТекстОшибки") Тогда		
		ОбщегоНазначенияУХ.СообщитьОбОшибке(СтруктураОписания.ТекстОшибки, Истина,, СтатусСообщения.Внимание);
		Возврат;		
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСерверУХ.УстановитьНовоеЗначение(ЭтотОбъект[ПолеСиноним], СтруктураОписания.Синоним, ЕстьИзменения);
	
	РаботаСОбъектамиМетаданныхУХ.ЗаполнитьИзмененныеРеквизиты(ЭтотОбъект, СтруктураОписания, "Реквизиты,Измерения,Ресурсы,Синоним", ЕстьИзменения);
	
	ТабНовыеРесурсы=РаботаСОбъектамиМетаданныхУХ.ПолучитьНовыеДанныеТабличнойЧасти(ПолеСиноним, Ресурсы,СтруктураОписания.Ресурсы);
	РаботаСОбъектамиМетаданныхУХ.ЗагрузитьИзмененнуюТабЧасть(Ресурсы, ТабНовыеРесурсы, ЕстьИзменения);
	
	ТабНовыеИзмерения=РаботаСОбъектамиМетаданныхУХ.ПолучитьНовыеДанныеТабличнойЧасти(ПолеСиноним, Измерения,СтруктураОписания.Измерения);	
	РаботаСОбъектамиМетаданныхУХ.ЗагрузитьИзмененнуюТабЧасть(Измерения, ТабНовыеИзмерения, ЕстьИзменения); 
	
	ТабНовыеРеквизиты=РаботаСОбъектамиМетаданныхУХ.ПолучитьНовыеДанныеТабличнойЧасти(ПолеСиноним, Реквизиты,СтруктураОписания.Реквизиты);	
	РаботаСОбъектамиМетаданныхУХ.ЗагрузитьИзмененнуюТабЧасть(Реквизиты, ТабНовыеРеквизиты, ЕстьИзменения);

	ОбщегоНазначенияКлиентСерверУХ.УстановитьНовоеЗначение(ЭтотОбъект.ПланСчетов, ПолучитьПланСчетовБД(), ЕстьИзменения);
		
КонецПроцедуры // ЗаполнитьРеквизитыОбъекта()

Функция ПолучитьПланСчетовБД()

	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ ПЕРВЫЕ 1
	|	ПланыСчетовБД.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ПланыСчетовБД КАК ПланыСчетовБД
	|ГДЕ
	|	НЕ ПланыСчетовБД.ПометкаУдаления
	|	И НЕ ПланыСчетовБД.ОтсутствуетВКонфигурации
	|	И ПланыСчетовБД.ПланСчетовМетаданные = &ПланСчетовМетаданные
	|	И ПланыСчетовБД.Наименование = &ПланСчетовМетаданные
	|	И ПланыСчетовБД.Владелец = &Владелец";
	
	Запрос.УстановитьПараметр("ПланСчетовМетаданные",ПланСчетовМетаданные);
	Запрос.УстановитьПараметр("Владелец",Владелец);
	
	Результат=Запрос.Выполнить().Выбрать();
	
	Если Результат.Следующий() Тогда
		
		Возврат Результат.Ссылка;
		
	Иначе
		
		Возврат Справочники.ПланыСчетовБД.ПустаяСсылка();
		
	КонецЕсли;	
	
КонецФункции // ПолучитьПланСчетовБД() 


	
Функция ПроверитьНаименование()
	
	ЕстьОшибки=Ложь;
	
	Если НЕ ЗначениеЗаполнено(Наименование) Тогда
		
		ОбщегоНазначенияУХ.СообщитьОбОшибке(Нстр("ru = 'Для обрабатываемого плана счетов внешней информационной базы не указано наименование.'"),ЕстьОшибки,,СтатусСообщения.Внимание);
		
	КонецЕсли;
	
	Возврат НЕ ЕстьОшибки;
			
КонецФункции // ПроверитьНаименование()

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	Иначе	
		Отказ = НЕ ПроверитьНаименование();
	КонецЕсли;
		
КонецПроцедуры

Функция Использование77() Экспорт
		
	Если Используется77 = Неопределено Тогда
		Используется77 = (Владелец.ВерсияПлатформы = Перечисления.ПлатформыВнешнихИнформационныхБаз.Предприятие77);	
	КонецЕсли;
	
	Возврат Используется77;
	
КонецФункции

