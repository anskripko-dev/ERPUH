
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура РеструктурироватьПериоды(Команда)
	
	СтруктураПараметров = Новый Структура;
	ТекстВопроса = НСтр("ru = 'Иерархия всех имеющихся элементов справочника Периоды будет автоматически
		| перестроена на основе их периодичности и даты начала. Например, если в справочнике
		| на первом уровне иерархии создано три элемента: Январь 2020 года, I квартал 2020 года
		| и 2020 год, то январь будет подчинен первому кварталу, а тот, в свою очередь,
		| войдет в 2020 год.'");
	Режим = РежимДиалогаВопрос.ДаНет;
	Оповещение = Новый ОписаниеОповещения("ПодтверждениеРеструктуризацииПериодов", ЭтаФорма, СтруктураПараметров);
	ПоказатьВопрос(Оповещение, ТекстВопроса, Режим, 0);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПериод(Команда)
	
	Если ЗначениеЗаполнено(Элементы.Список.ТекущаяСтрока) Тогда
		ОткрытьФорму("Справочник.Периоды.Форма.ФормаЗаполненияПериодов", Новый Структура("ПериодРодитель", Элементы.Список.ТекущаяСтрока), ЭтаФорма);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ПодтверждениеРеструктуризацииПериодов(Результат, ДополнительныеПараметры) Экспорт

	Если Результат = КодВозвратаДиалога.Да Тогда
		РеструктурироватьПериодыПродолжение();
		КартинкаОповещения = БиблиотекаКартинок.Успешно32;
		ТекстОповещения = НСтр("ru = 'Реструктуризация'");
		ПояснениеОповещения = НСтр("ru = 'Реструктуризация периодов завершена'");
		ПоказатьОповещениеПользователя(ТекстОповещения, , ПояснениеОповещения, КартинкаОповещения);
		Элементы.Список.Обновить();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура РеструктурироватьПериодыПродолжение()
	
	Справочники.Периоды.РеструктурироватьПериоды();
	
КонецПроцедуры

#КонецОбласти
