
////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ РЕДАКТИРОВАНИЯ ПРОЦЕДУР ПРАВИЛА ОБРАБОТКИ.
//

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
	// СтандартныеПодсистемы.УправлениеДоступом
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.УправлениеДоступом") Тогда
		МодульУправлениеДоступом = ОбщегоНазначения.ОбщийМодуль("УправлениеДоступом");
		МодульУправлениеДоступом.ПриЧтенииНаСервере(ЭтотОбъект, ТекущийОбъект);
	КонецЕсли;
	// Конец СтандартныеПодсистемы.УправлениеДоступом
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Параметры.ЗначениеКопирования) Тогда
		ЗначениеКопирования = Параметры.ЗначениеКопирования;
	КонецЕсли;
	
	Если Параметры.Ключ.Пустая() Тогда 
		
		Если ЗначениеЗаполнено(Объект.Владелец) Тогда
			
			Если ТипЗнч(Объект.Владелец) = Тип("СправочникСсылка.ШаблоныТрансляций") Тогда
				ВызватьИсключение НСтр("ru = 'Создание дополнительных правил расчета для шаблонов трансляций не предусмотрено.'");
			КонецЕсли;
			
			ПараметрыОСВ=Справочники.ВидыОтчетов.ПолучитьПараметрыПравилаОСВ(Объект.Владелец);
			
			Если ПараметрыОСВ.Предназначение=Перечисления.ПредназначенияЭлементовСтруктурыОтчета.ОборотноСальдоваяВедомость Тогда
				
				ЗаполнитьЗначенияСвойств(Объект,ПараметрыОСВ);
				
			КонецЕсли; 
			
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(Объект.ТипБД) Тогда
			
			Объект.ТипБД=Справочники.ТипыБазДанных.ТекущаяИБ;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ОбщегоНазначенияПовтИспУХ.ИспользуетсяМногопериоднаяОтчетность() Тогда
		Элементы.ФормаПроцедураЗаполнения.Видимость = Ложь;
		Элементы.ФормаПроцедураВычисления.Заголовок = Нстр("ru = 'Процедура до вычисления'");
	КонецЕсли;
	
	Если Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеРегистрБухгалтерии
		ИЛИ  Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнешниеДанныеРегистрБухгалтерии
		ИЛИ   Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнешниеДанныеОстаткиИОборотыИзПланаСчетов Тогда
		
		ОбновитьСписокРегистровБухгалтерии();
		
		Если ЗначениеЗаполнено(Объект.РегистрБухгалтерииПоУмолчанию) Тогда
			ОбновитьСписокРесурсовРегистра();
		КонецЕсли;
		
	КонецЕсли;
	
	СформироватьСписокВозможныхСпособовПолучения(); 
	ПолучитьИспользуемыеВидыИтогов();

	ОпределитьТипБДПоУмолчанию();
	УстановитьВидимость();
	
	Элементы.ГруппаТрансляция.Видимость=(ТипЗнч(Объект.Владелец)=Тип("СправочникСсылка.ШаблоныТрансляций"));
	
	Элементы.ФормаЗаполнитьПоБюджетам.Видимость=Справочники.ВидыОтчетов.ЕстьПоказателиБюджетов(Объект.Владелец);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если НЕ ТипЗнч(Параметр)=Тип("Структура") Тогда
		Возврат;
	КонецЕсли;
	
	ДобавитьТекстПроцедуры(ИмяСобытия, Параметр)
		
КонецПроцедуры

&НаСервере
Процедура ДобавитьТекстПроцедуры(ИмяСобытия, ТекПараметры)
	
	Если ИмяСобытия = "ИзмененаПроцедураЗаполнения" Тогда
		Объект.ПроцедураЗаполнения = ТекПараметры.ТекстПроцедуры;
	ИначеЕсли ИмяСобытия = "ИзмененаПроцедураВычисления" Тогда
		Объект.ПроцедураВычисления = ТекПараметры.ТекстПроцедуры;
	ИначеЕсли ИмяСобытия = "ИзмененаПроцедураПослеВычисления" Тогда
		Объект.ПроцедураПослеВычисления = ТекПараметры.ТекстПроцедуры;
	Иначе
		Возврат;
	КонецЕсли;
	
	Если ТекПараметры.Свойство("ИспользуемыеОтчеты") Тогда
		Для Каждого СтрВидОтчета Из ТекПараметры.ИспользуемыеОтчеты Цикл
			НайденныеСтроки = Объект.ИспользуемыеОтчеты.НайтиСтроки(Новый Структура("ВидОтчета", СтрВидОтчета.Значение));
			Если НайденныеСтроки.Количество() = 0 Тогда
				НСтрока = Объект.ИспользуемыеОтчеты.Добавить();
				НСтрока.ВидОтчета = СтрВидОтчета.Значение;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	ЭтаФорма.Модифицированность = Истина;
	
КонецПроцедуры

&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	Если ЗначениеЗаполнено(ЗначениеКопирования) Тогда
		
		ТекущийОбъект.мОснование=ЗначениеКопирования.ПолучитьОбъект();
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПослеЗаписиНаСервере(ТекущийОбъект, ПараметрыЗаписи)
	
	// СтандартныеПодсистемы.УправлениеДоступом
	УправлениеДоступом.ПослеЗаписиНаСервере(ЭтотОбъект, ТекущийОбъект, ПараметрыЗаписи);
	// Конец СтандартныеПодсистемы.УправлениеДоступом
	
	УправлениеОтчетамиУХ.СформироватьДанныеРегистраПараметрическихНастроек(ТекущийОбъект.Ссылка,Истина);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ПроцедураЗаполнения(Команда)
	
	СтруктураПараметров=Новый Структура;
	СтруктураПараметров.Вставить("ТекстПроцедуры", Объект.ПроцедураЗаполнения);
	СтруктураПараметров.Вставить("ВидОтчета", Объект.Владелец);
	СтруктураПараметров.Вставить("НазначениеРасчетов", Объект.Ссылка);
	СтруктураПараметров.Вставить("ИмяПроцедуры", "ПроцедураЗаполнения");
	СтруктураПараметров.Вставить("СпособОбработки", ПредопределенноеЗначение("Перечисление.СпособыОбработкиПоказателейОтчетов.Заполнение"));
	СтруктураПараметров.Вставить("Заголовок", НСтр("ru = 'Процедура заполнения'"));
	
	СписокВидовОтчетов=Новый СписокЗначений;
	Для Каждого СтрВидОтчета ИЗ Объект.ИспользуемыеОтчеты Цикл
		СписокВидовОтчетов.Добавить(СтрВидОтчета.ВидОтчета);
	КонецЦикла;
	
	СтруктураПараметров.Вставить("ИспользуемыеВидыОтчетов",СписокВидовОтчетов);
	
	ОткрытьФорму("ОбщаяФорма.ФормаРедактированияПроцедур", СтруктураПараметров, ЭтаФорма);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроцедураВычисления(Команда)
	
	СтруктураПараметров=Новый Структура;
	СтруктураПараметров.Вставить("ТекстПроцедуры", Объект.ПроцедураВычисления);
	СтруктураПараметров.Вставить("ВидОтчета", Объект.Владелец);
	СтруктураПараметров.Вставить("ИмяПроцедуры", "ПроцедураВычисления");
	СтруктураПараметров.Вставить("НазначениеРасчетов", Объект.Ссылка);
	СтруктураПараметров.Вставить("СпособОбработки", ПредопределенноеЗначение("Перечисление.СпособыОбработкиПоказателейОтчетов.Заполнение"));
	СтруктураПараметров.Вставить("Заголовок", НСтр("ru = 'Процедура до вычисления'"));
	
	СписокВидовОтчетов=Новый СписокЗначений;
	Для Каждого СтрВидОтчета ИЗ Объект.ИспользуемыеОтчеты Цикл
		СписокВидовОтчетов.Добавить(СтрВидОтчета.ВидОтчета);
	КонецЦикла;
	
	СтруктураПараметров.Вставить("ИспользуемыеВидыОтчетов",СписокВидовОтчетов);
	
	ОткрытьФорму("ОбщаяФорма.ФормаРедактированияПроцедур", СтруктураПараметров, ЭтаФорма);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьВидимость()
		
	Элементы.ГруппаИнформационныеБазы.Видимость=(НЕ Объект.ТипБД.ВерсияПлатформы=Перечисления.ПлатформыВнешнихИнформационныхБаз.ADO);
	
	Элементы.ГруппаРегистрБухгалтерии.Видимость=(Не Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеПоказательОтчета);
	Элементы.ВидОтчетаОСВ.Видимость=(Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеПоказательОтчета);
	
	Элементы.ГруппаФормулыОСВ.Видимость=ОстаткиИОборотыПоСчетам;
	
	Если ОстаткиИОборотыПоСчетам тогда
		
		ОбновитьПараметрыФормулОСВ();
		
	КонецЕсли;
	
	Если ТипЗнч(Объект.Владелец)=Тип("СправочникСсылка.ШаблоныТрансляций") Тогда
		
		ЭтотОбъект.ТолькоПросмотр=Истина;
		ЭтотОбъект.ПоложениеКоманднойПанели=ПоложениеКоманднойПанелиФормы.Нет;
		Элементы.Утверждено.Видимость=Ложь;
		
	ИначеЕсли ТипЗнч(Объект.Владелец)=Тип("СправочникСсылка.ШаблоныТрансформационныхКорректировок") Тогда
		
		Элементы.ФормаЗаполнитьПоказателиОСВ.Видимость=Ложь;
		Элементы.ФормаЗаполнитьПоБюджетам.Видимость=Ложь;
		Элементы.РедактированиеПроцедур.Видимость=Ложь;
		Элементы.Утверждено.Видимость=Ложь;
						
	КонецЕсли;
	
КонецПроцедуры // УстановитьВидимость(); 

&НаСервереБезКонтекста
Процедура УстановитьПроцедуруПоУмолчанию(Элемент, ВидОтчета)
	
	УправлениеОтчетамиУХ.ЗаписатьНастройкуПоУмолчаниюОбработкиШаблонов(ВидОтчета, "ПравилоОбработки", Элемент);

КонецПроцедуры

&НаСервере
Процедура ОбновитьСписокРесурсовРегистра()
	
	ОбщегоНазначенияСерверУХ.ОбновитьСписокРесурсовРегистра(Объект.РегистрБухгалтерииПоУмолчанию,Объект.РесурсРегистраПоУмолчанию,Элементы.РесурсРегистраПоУмолчанию);	
	УстановитьВидимость();
	
КонецПроцедуры // ПолучитьСписокРесурсовРегистра()

&НаСервере
Процедура СформироватьСписокВозможныхСпособовПолучения()
	
	СписокСпособов=Новый СписокЗначений;
	СписокСпособов.Добавить(Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеПоказательОтчета, Нстр("ru = 'Показатель другого отчета текущей ИБ (простая настройка)'"));

	Если Метаданные.РегистрыБухгалтерии.Количество()>0 Тогда
				
		СписокСпособов.Добавить(Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеРегистрБухгалтерии, Нстр("ru = 'Регистр бухгалтерии текущей ИБ'"));
			
	КонецЕсли;
	
	Если НЕ Объект.ТипБД=Справочники.ТипыБазДанных.ТекущаяИБ Тогда
		
		Если Объект.ТипБД.ВерсияПлатформы=Перечисления.ПлатформыВнешнихИнформационныхБаз.Предприятие77 Тогда
			
			СписокСпособов.Добавить(Перечисления.СпособыПолученияОперандов.ВнешниеДанныеОстаткиИОборотыИзПланаСчетов,Строка(Перечисления.СпособыПолученияОперандов.ВнешниеДанныеОстаткиИОборотыИзПланаСчетов));
			
		ИначеЕсли НЕ Объект.ТипБД.ВерсияПлатформы=Перечисления.ПлатформыВнешнихИнформационныхБаз.ADO Тогда	
			
			СписокСпособов.Добавить(Перечисления.СпособыПолученияОперандов.ВнешниеДанныеРегистрБухгалтерии, Нстр("ru = 'Регистр бухгалтерии внешней ИБ'"));
			
		КонецЕсли;
		
	КонецЕсли;
	
	ОбщегоНазначенияУХ.ЗагрузитьВСписокЗначений(СписокСпособов,Элементы.СпособПолучения.СписокВыбора);
	ОпределитьТипБДПоУмолчанию();
	
КонецПроцедуры // СформироватьСписокВозможныхСпособовПолучения()

&НаСервере
Процедура ОпределитьТипБДПоУмолчанию() 
	
	Если Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнешниеДанныеРегистрБухгалтерии
		ИЛИ Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнешниеДанныеОстаткиИОборотыИзПланаСчетов Тогда
		
		ТипБДПоУмолчанию=Объект.ТипБД;
		
	Иначе
		
		ТипБДПоУмолчанию=Справочники.ТипыБазДанных.ТекущаяИБ;
		
	КонецЕсли;	
	
КонецПроцедуры // ОпределитьТипБДПоУмолчанию() 

&НаСервере
Процедура ПриИзмененииПланаСчетов()
	
	Если Объект.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеПоказательОтчета Тогда
					
		Объект.ВидОтчетаОСВ=Справочники.ВидыОтчетов.ПолучитьВидОтчетаОСВ(Объект.ПланСчетовПоУмолчанию);					
		
	Иначе
		
		ОбновитьСписокРегистровБухгалтерии();
		
		Если Элементы.РегистрБухгалтерииПоУмолчанию.СписокВыбора.Количество()>0 Тогда
			
			Объект.РегистрБухгалтерииПоУмолчанию=Элементы.РегистрБухгалтерииПоУмолчанию.СписокВыбора[0].Значение;
			ОбновитьСписокРесурсовРегистра();
							
		Иначе
			
			Объект.РегистрБухгалтерииПоУмолчанию="";
			
		КонецЕсли;
		
	КонецЕсли;	
	
КонецПроцедуры // ПриИзмененииПланаСчетов()

&НаСервере
Процедура ОбновитьСписокРегистровБухгалтерии()

	Элементы.РегистрБухгалтерииПоУмолчанию.СписокВыбора.Очистить();
	
	Если НЕ ЗначениеЗаполнено(Объект.ПланСчетовПоУмолчанию) Тогда
		
		Возврат;
		
	КонецЕсли; 
	
	МассивРегистров=УправлениеОтчетамиУХ.ПолучитьМассивРегистровБухгалтерии(Объект.ПланСчетовПоУмолчанию);
	
	Для Каждого Регистр ИЗ МассивРегистров Цикл
		
		Элементы.РегистрБухгалтерииПоУмолчанию.СписокВыбора.Добавить(Регистр);
		
	КонецЦикла;
	
	
КонецПроцедуры // ОбновитьСписокРегистровБухгалтерии() 

&НаКлиенте
Процедура УстановитьПоУмолчанию_Завершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Да Тогда
		ЭтаФорма.Записать();
		УстановитьПроцедуруПоУмолчанию(Объект.Ссылка, Объект.Владелец);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьПоУмолчанию(Команда)
	
	Если НЕ ЗначениеЗаполнено(Объект.Ссылка) Тогда
		ТекстВопроса = НСтр("ru = 'Требуется сохранить объект. Продолжить?'");
		Режим = РежимДиалогаВопрос.ДаНет;
		Оповещение = Новый ОписаниеОповещения("УстановитьПоУмолчанию_Завершение", ЭтаФорма);
		ПоказатьВопрос(Оповещение, ТекстВопроса, Режим, 0);
	Иначе
		УстановитьПроцедуруПоУмолчанию(Объект.Ссылка, Объект.Владелец);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрБухгалтерииПоУмолчаниюПриИзменении(Элемент)
	
	ОбновитьСписокРесурсовРегистра();
		
КонецПроцедуры

&НаКлиенте
Процедура ТипБДПриИзменении(Элемент)
	
	СформироватьСписокВозможныхСпособовПолучения();
	
КонецПроцедуры 

&НаСервере
Процедура ПриИзмененииСпособаПолучения()
	
	ОпределитьТипБДПоУмолчанию();
	УстановитьВидимость();
	
КонецПроцедуры // ПриИзмененииСпособаПолучения() 

&НаКлиенте
Процедура СпособПолученияПриИзменении(Элемент)
	
	ПриИзмененииСпособаПолучения();
	
КонецПроцедуры

&НаКлиенте
Процедура ПланСчетовПоУмолчаниюПриИзменении(Элемент)
	
	ПриИзмененииПланаСчетов();
	
КонецПроцедуры

&НаКлиенте
Процедура ПроцедураПослеВычисления(Команда)
	
	СтруктураПараметров=Новый Структура;
	СтруктураПараметров.Вставить("ТекстПроцедуры", Объект.ПроцедураПослеВычисления);
	СтруктураПараметров.Вставить("ВидОтчета", Объект.Владелец);
	СтруктураПараметров.Вставить("ИмяПроцедуры", "ПроцедураПослеВычисления");
	СтруктураПараметров.Вставить("НазначениеРасчетов", Объект.Ссылка);
	СтруктураПараметров.Вставить("СпособОбработки", ПредопределенноеЗначение("Перечисление.СпособыОбработкиПоказателейОтчетов.Заполнение"));
	СтруктураПараметров.Вставить("Заголовок", НСтр("ru = 'Процедура после вычисления'"));
	
	СписокВидовОтчетов=Новый СписокЗначений;
	Для Каждого СтрВидОтчета ИЗ Объект.ИспользуемыеОтчеты Цикл
		СписокВидовОтчетов.Добавить(СтрВидОтчета.ВидОтчета);
	КонецЦикла;
	
	СтруктураПараметров.Вставить("ИспользуемыеВидыОтчетов",	СписокВидовОтчетов);
	
	ОткрытьФорму("ОбщаяФорма.ФормаРедактированияПроцедур", СтруктураПараметров, ЭтаФорма);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоБюджетамПрогноз(Команда)
	
	Если НЕ ЗначениеЗаполнено(Объект.Ссылка) Тогда
		Записать();
	КонецЕсли;
	
	ЗаполнитьПоБюджетамНаСервере(Объект.Владелец,Объект.Ссылка,"Сумма");
	
	Оповестить("ЗаполненыФормулыПоБюджетам");
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ЗаполнитьПоБюджетамНаСервере(ВидОтчета,ПравилоОбработки,ИмяРесурса)
	
	Справочники.ВидыОтчетов.ОбновитьПроцедурыРасчетаПоБюджетам(ВидОтчета,ПравилоОбработки,ИмяРесурса);
		
КонецПроцедуры

&НаКлиенте
Процедура ИспользуемыеОтчетыПриНачалеРедактирования(Элемент, НоваяСтрока, Копирование)
	
	Если НоваяСтрока Тогда
		
		Элементы.ИспользуемыеОтчеты.ТекущиеДанные.ДобавленВручную		= Истина;
		Элементы.ИспользуемыеОтчеты.ТекущиеДанные.АвтоматическийПересчет= Истина;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИспользуемыеОтчетыПередУдалением(Элемент, Отказ)
	
	Если НЕ Элементы.ИспользуемыеОтчеты.ТекущиеДанные.ДобавленВручную Тогда
		
		Отказ=Истина;
		Элементы.ИспользуемыеОтчеты.ТекущиеДанные.АвтоматическийПересчет= Ложь;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьПоказателиОСВСервер(ОбновлятьПоказатели)
	
	ДанныеДляФормул=Новый Структура;
	ДанныеДляФормул.Вставить("ВидОтчета",Объект.Владелец);
	ДанныеДляФормул.Вставить("ПравилоОбработки",Объект.Ссылка);
	ДанныеДляФормул.Вставить("ПравилаФормированияФормул",Объект.ПравилаФормированияФормул);
	ДанныеДляФормул.Вставить("ФормулыДляНачальныхСальдо",Объект.ФормулыДляНачальныхСальдо);
	ДанныеДляФормул.Вставить("ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам",Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам);
	ДанныеДляФормул.Вставить("РазрешитьРучноеРедактирование",Объект.РазрешитьРучноеРедактирование);

	
	Справочники.ВидыОтчетов.СформироватьФормулыРасчетаОСВ(ДанныеДляФормул,ОбновлятьПоказатели);
	
	
	
КонецПроцедуры // ЗаполнитьПоказателиОСВСервер()

&НаКлиенте
Процедура ЗаполнитьПоказателиОСВ(Команда)

	Если НЕ ЗначениеЗаполнено(Объект.Наименование) Тогда
		
		ПоказатьПредупреждение(,НСтр("ru = 'Не заполнено наименование'"));
		Возврат;
		
	КонецЕсли;
	
	Текст = НСтр("ru = 'Обновлять существующие формулы?'");
	
	Оповещение = Новый ОписаниеОповещения("ВыполнитьЗаполнениеОСВ", ЭтотОбъект);
	ПоказатьВопрос(Оповещение, Текст, РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьЗаполнениеОСВ(Результат, ДополнительныеПараметры) Экспорт

	Если НЕ ЗначениеЗаполнено(Объект.Ссылка) Тогда 
				
		Записать();
		
	КонецЕсли;
	
	ЗаполнитьПоказателиОСВСервер(Результат=КодВозвратаДиалога.Да);
	
КонецПроцедуры

&НаКлиенте
Процедура ВидОтчетаОСВПриИзменении(Элемент)
	
	УстановитьВидимость();
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоБюджетамФакт(Команда)
	
	Если НЕ ЗначениеЗаполнено(Объект.Ссылка) Тогда
		Записать();
	КонецЕсли;
	
	ЗаполнитьПоБюджетамНаСервере(Объект.Владелец,Объект.Ссылка,"СуммаИсполнено");
	
	Оповестить("ЗаполненыФормулыПоБюджетам");
	
КонецПроцедуры   

&НаСервере
Процедура ПолучитьИспользуемыеВидыИтогов()
	
	Если НЕ ТипЗнч(Объект.Владелец)=Тип("СправочникСсылка.ВидыОтчетов") Тогда
		
		Возврат;
		
	КонецЕсли;
	
	ПараметрыОтчетаОСВ=Справочники.ВидыОтчетов.ПолучитьПараметрыПравилаОСВ(Объект.Владелец);
	ОстаткиИОборотыПоСчетам=(ПараметрыОтчетаОСВ.Предназначение=Перечисления.ПредназначенияЭлементовСтруктурыОтчета.ОборотноСальдоваяВедомость);	
	
	Если НЕ ОстаткиИОборотыПоСчетам Тогда
		
		Возврат;
		
	КонецЕсли;
	
	ИтогиСуммой=НЕ Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам;
	НеВыводитьГруппирующиеСчета=ПараметрыОтчетаОСВ.НеВыводитьГруппирующиеСчета;
	
	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ
	             |	ВидыОтчетовИспользуемыеВидыИтоговПоСчетам.ВидИтогаПоСчету КАК ВидИтогаПоСчету
	             |ИЗ
	             |	Справочник.ВидыОтчетов.ИспользуемыеВидыИтоговПоСчетам КАК ВидыОтчетовИспользуемыеВидыИтоговПоСчетам
	             |ГДЕ
	             |	ВидыОтчетовИспользуемыеВидыИтоговПоСчетам.Ссылка = &ВидОтчета";
	
	Запрос.УстановитьПараметр("ВидОтчета",Объект.Владелец);
	ИспользуемыеВидыИтоговПоСчетам=Запрос.Выполнить().Выгрузить();
	
	Для Каждого Строка ИЗ ИспользуемыеВидыИтоговПоСчетам Цикл
		
		Если Строка.ВидИтогаПоСчету=Перечисления.ВидыБухгалтерскихИтогов.СНД Тогда
			
			СНД=Истина;
			
		ИначеЕсли Строка.ВидИтогаПоСчету=Перечисления.ВидыБухгалтерскихИтогов.СНК Тогда
			
			СНК=Истина;
			
		ИначеЕсли Строка.ВидИтогаПоСчету=Перечисления.ВидыБухгалтерскихИтогов.ДО Тогда
			
			ДО=Истина;
			
		ИначеЕсли Строка.ВидИтогаПоСчету=Перечисления.ВидыБухгалтерскихИтогов.КО Тогда
			
			КО=Истина;
		ИначеЕсли Строка.ВидИтогаПоСчету=Перечисления.ВидыБухгалтерскихИтогов.СКД Тогда
			
			СКД=Истина;
			
		ИначеЕсли Строка.ВидИтогаПоСчету=Перечисления.ВидыБухгалтерскихИтогов.СКК Тогда
			
			СКК=Истина;
			
		КонецЕсли;
		
	КонецЦикла; 
	
	ОбновитьСписокСпособовФормирования();
	
КонецПроцедуры // ОтобразитьИспользуемыеВидыИтогов()

&НаКлиенте
Процедура ИтогиСуммойПриИзменении(Элемент)
	
	Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам=Не ИтогиСуммой;
	
КонецПроцедуры  

&НаКлиенте
Процедура ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетамПриИзменении(Элемент)
	
	ИтогиСуммой=НЕ Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам;
	
КонецПроцедуры

&НаКлиенте
Процедура ПравилаФормированияФормулПриИзменении(Элемент)
	
	Если Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам
		И (НЕ Объект.ПравилаФормированияФормул=0) Тогда
		
		Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам=Ложь;
		ИтогиСуммой=Истина;
		
	КонецЕсли;
			
	ОбновитьПараметрыФормулОСВ(); 
	
КонецПроцедуры 

&НаСервере
Процедура ОбновитьПараметрыФормулОСВ()
	
	Элементы.ГруппаИтогиПоСчетам.Видимость=(Объект.ПравилаФормированияФормул=0);
	
	Элементы.ГруппаПереходящиеОстатки.Видимость=(Объект.ПравилаФормированияФормул=3) 
		И ((СНД И СКД) ИЛИ (СНК И СКК));

	Если НЕ ((СНД И СКД) ИЛИ (СНК И СКК)) Тогда
		
		Объект.ФормулыДляНачальныхСальдо=Ложь;
		
	КонецЕсли;
	
	Если (НЕ (ДО И КО) И ((СНД И СКД) ИЛИ (СНК И СКК)))
		И Объект.ПравилаФормированияФормул=3 Тогда
		
		Объект.ПравилаФормированияФормул=1;
		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Объект.РегистрБухгалтерииПоУмолчанию) Тогда
		
		Объект.ПолучатьБухгалтерскиеИтогиПоГруппирующимСчетам=Ложь;
		Элементы.ГруппаИтогиПоСчетам.Видимость=Ложь;	
		
	Иначе
		
		Элементы.ГруппаИтогиПоСчетам.Видимость=Объект.ПравилаФормированияФормул=0;	
		
	КонецЕсли; 
		
	ОбновитьСписокСпособовФормирования();
	ОбновитьТекстИнформацииФормулы();
				
КонецПроцедуры // ОбновитьПараметрыФормулОСВ()

&НаСервере
Процедура ОбновитьТекстИнформацииФормулы()
			
	Если Объект.ПравилаФормированияФормул=0 Тогда
		
		Элементы.ДекорацияПравила.Заголовок=Нстр("ru = 'Создаются формулы для получения остатков и оборотов по счетам из регистра бухгалтерии'");
		
	ИначеЕсли Объект.ПравилаФормированияФормул=1 Тогда
		
		Элементы.ДекорацияПравила.Заголовок=Нстр("ru = 'Создаются только формулы для расчета итогов по иерархии счетов'");
		
	ИначеЕсли Объект.ПравилаФормированияФормул=2 Тогда
		
		Элементы.ДекорацияПравила.Заголовок=Нстр("ru = 'Формулы расчета значений показателей не создаются, существующие удаляются'");
		
	ИначеЕсли Объект.ПравилаФормированияФормул=3 Тогда
		
		Элементы.ДекорацияПравила.Заголовок=Нстр("ru = 'Создаются формулы для расчета:
		| - конечного сальдо на основании начального и оборотов дт/кт
		| - итогов по иерархии счетов'");
		
	КонецЕсли;	
	
КонецПроцедуры // ОбновитьТекстИнформацииФормулы()

&НаСервере
Процедура ОбновитьСписокСпособовФормирования()

	Элементы.ПравилаФормированияФормул.СписокВыбора.Очистить();
	
	Если ЗначениеЗаполнено(Объект.РегистрБухгалтерииПоУмолчанию)
		ИЛИ ЗначениеЗаполнено(Объект.ВидОтчетаОСВ) Тогда
		
		Элементы.ПравилаФормированияФормул.СписокВыбора.Добавить(0,Нстр("ru = 'Остатки и обороты по счетам'"));
		
	КонецЕсли;
	
	Если Не НеВыводитьГруппирующиеСчета Тогда
		
		 Элементы.ПравилаФормированияФормул.СписокВыбора.Добавить(1,Нстр("ru = 'Расчет итогов по группирующим счетам'"));

	 КонецЕсли;
	 
	 Если (ДО И КО) И ((СНД И СКД) ИЛИ (СНК И СКК)) Тогда
		
		Элементы.ПравилаФормированияФормул.СписокВыбора.Добавить(3,Нстр("ru = 'Расчет конечного сальдо'"));
		
	КонецЕсли;
	 	
КонецПроцедуры // ОбновитьСписокСпособовФормирования()
