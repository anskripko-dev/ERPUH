
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	БланкОтчета = ЗначениеНастройки(ПараметрКоманды);
	
	Если Не ЗначениеЗаполнено(БланкОтчета) Тогда
		Если ИдентификацияПродуктаУХКлиентСервер.ЭтоУправлениеХолдингом() Тогда
			
			СтруктураПараметров = Новый Структура("ВидОтчета", ПараметрКоманды);
			ИмяФормы_ = "Обработка.НастройкаСтруктурыОтчета.Форма.ФормаКонструктора";
			ОткрытьФорму(ИмяФормы_,СтруктураПараметров,
				ПараметрыВыполненияКоманды.Источник,
				ПараметрыВыполненияКоманды.Уникальность,
				ПараметрыВыполненияКоманды.Окно);	
			
		Иначе
			
			ПоказатьПредупреждение(,Нстр("ru = 'Не указан бланк экземпляра отчета по умолчанию.'"));
			
		КонецЕсли;
	Иначе
		ОткрытьФорму("Справочник.БланкиОтчетов.ФормаОбъекта", 
			Новый Структура("Ключ", БланкОтчета),
			ПараметрыВыполненияКоманды.Источник,
			ПараметрыВыполненияКоманды.Уникальность,
			ПараметрыВыполненияКоманды.Окно);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ЗначениеНастройки(ВидОтчета)
	
	Возврат Справочники.ВидыОтчетов.ЗначениеНастройкиПоУмолчанию(ВидОтчета, "БланкДляОтображения");
	
КонецФункции