 
#Область ОбработчикиСобытийФормы
 
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЭтаФорма.КэшируемыеЗначения = Новый Структура;	
	ЭтаФорма.КэшируемыеЗначения.Вставить("ИменаСубконто", Справочники.СчетаУчетаКурсовыхРазниц.ПолучитьИменаСубконто());
	
	МСФОКлиентСерверУХ.УстановитьПланСчетов(ЭтаФорма);

	УправлениеФормой(ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти
 
#Область ОбработчикиСобытийЭлементовФормы 
 
&НаКлиенте
Процедура СчетУчетаПрибылиПриИзменении(Элемент)
	МСФОКлиентСерверУХ.ОбновитьСубконтоСчета(ЭтотОбъект, Элемент.Имя);
КонецПроцедуры

&НаКлиенте
Процедура СчетУчетаУбыткаПриИзменении(Элемент)
	МСФОКлиентСерверУХ.ОбновитьСубконтоСчета(ЭтотОбъект, Элемент.Имя);
КонецПроцедуры

&НаКлиенте
Процедура ПланСчетовБДПриИзменении(Элемент)
	МСФОКлиентСерверУХ.УстановитьПланСчетов(ЭтотОбъект);
КонецПроцедуры

&НаКлиенте
Процедура ВыделятьРКРПриИзменении(Элемент)
	УправлениеФормой(ЭтотОбъект)
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УправлениеФормой(Форма)

	Элементы = Форма.Элементы;
	
	ВыделятьРКР = Форма.Объект.ВыделятьРКР;
	
	Элементы.ГруппаСчетУчетаПрибылиРКР.ТолькоПросмотр = Не ВыделятьРКР;
	Элементы.ГруппаСчетУчетаУбыткаРКР.ТолькоПросмотр = Не ВыделятьРКР;
	
	Элементы.СчетУчетаПрибылиРКР.АвтоОтметкаНезаполненного = ВыделятьРКР;
	Элементы.СчетУчетаУбыткаРКР.АвтоОтметкаНезаполненного = ВыделятьРКР;

КонецПроцедуры

#КонецОбласти
