
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	
	URLСхемы 			= Параметры.URLСхемы;
	АдресТаблицыДанных  = Параметры.АдресТаблицыДанных;
	Сценарий            = Параметры.ОбщийОтбор.Сценарий;
	Валюта       		= Параметры.ОбщийОтбор.Валюта;
	ПакетДанных         = Параметры.ПакетДанных;
	
	Попытка
		КомпоновщикНастроек.Инициализировать(Новый ИсточникДоступныхНастроекКомпоновкиДанных(URLСхемы));
	Исключение
		
		СообщениеПользователю = Новый СообщениеПользователю;
		
		СтрокаШаблона = Нстр("ru = 'При инициализации отчета возникла ошибка:
		|%1'");
		
		Если Не ПустаяСтрока(СтрокаШаблона) тогда			
			СообщениеПользователю.Текст = СтрШаблон(СтрокаШаблона, ОписаниеОшибки());
		КонецЕсли;
		
		СообщениеПользователю.Сообщить();		
		КомпоновщикНастроек = Новый КомпоновщикНастроекКомпоновкиДанных;
		
	КонецПопытки;
	
	// Настройки получены извне. Данные из сохраненной настройки не подтягиваются.
	Если Параметры.Настройки <> Неопределено Тогда 		
		КомпоновщикНастроек.ЗагрузитьНастройки(Параметры.Настройки);	
	КонецЕсли;

	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)

	ОбновитьОтчет();

КонецПроцедуры


&НаСервере
Процедура ОбновитьОтчет()
	
	ДанныеРасшифровки = Неопределено;
	КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
	
	//	
	ВнешниеНаборыДанных = Новый Структура;		
	ВнешниеНаборыДанных.Вставить("ИсточникДанныхДляРасчетов", ПолучитьИзВременногоХранилища(АдресТаблицыДанных));
	
	СКДНастройки    = ПолучитьИзВременногоХранилища(ЭтаФорма.URLСхемы);
	
	МакетКомпоновки = КомпоновщикМакета.Выполнить(СКДНастройки, КомпоновщикНастроек.Настройки, ДанныеРасшифровки);
	
	ПроцессорКомпоновки = Новый ПроцессорКомпоновкиДанных;
	ПроцессорКомпоновки.Инициализировать(МакетКомпоновки, ВнешниеНаборыДанных, ДанныеРасшифровки, Истина);
	
	//Создадим и инициализируем процессор вывода результата
	ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
	ПроцессорВывода.УстановитьДокумент(Результат);
	ПроцессорВывода.Вывести(ПроцессорКомпоновки);
	
	АдресДанныхРасшифровки = ПоместитьВоВременноеХранилище(ДанныеРасшифровки,ЭтаФорма.УникальныйИдентификатор);	
	
КонецПроцедуры


#Область ОбработкаРасшифровкиАналитическихОтчетов

&НаКлиенте
Процедура РезультатОбработкаРасшифровки(Элемент, Расшифровка, СтандартнаяОбработка)
		
	ОбработатьРасшифровкуТекущегоЭлемента(Элемент,Расшифровка, СтандартнаяОбработка);
		
КонецПроцедуры


&НаКлиенте
Процедура Подключаемый_ОбработатьВыборДействия(ВыполненноеДействие,  Настройки, ДополнительныеПараметры) Экспорт
    
    Расшифровка = ДополнительныеПараметры.Расшифровка;
        
    // Осуществим выбор действия расшифровки пользователем
        
    Если Настройки <> Неопределено Тогда
        // Пользователь выбрал действие, для которого нужно менять настройки
        Если ВыполненноеДействие = ДействиеОбработкиРасшифровкиКомпоновкиДанных.Упорядочить Тогда
            // Если требуется упорядочить - упорядочим в текущем отчете
            КомпоновщикНастроек.ЗагрузитьНастройки(Настройки);
        ИначеЕсли ВыполненноеДействие = ДействиеОбработкиРасшифровкиКомпоновкиДанных.ОткрытьЗначение Тогда
            ПоказатьЗначение(, Настройки);
        Иначе
            
			Вн_Настройки =ПрименитьНастройкиКД(Расшифровка, Настройки);
			
			ОбщийОтбор = Новый Структура;
			ОбщийОтбор.Вставить("Сценарий",Сценарий);
			ОбщийОтбор.Вставить("ПериодОтчета",ПериодОтчета);
			ОбщийОтбор.Вставить("ПериодОкончания",ПериодОкончания);
			//ОбщийОтбор.Вставить("Организация",Организация);
			ОбщийОтбор.Вставить("Валюта",Валюта);

			
			ПараметрыФормы = Новый Структура("URLСхемы, Настройки,ОбщийОтбор, ЭтоРасшифровка, АдресТаблицыДанных,ПакетДанных", 
												URLСхемы, Вн_Настройки,ОбщийОтбор, Истина, АдресТаблицыДанных,ПакетДанных);
            ОткрытьФорму("Справочник.АналитическиеПанели.Форма.ФормаРасшифровкиЗначения", ПараметрыФормы, , Истина);
			
        КонецЕсли;
        
    КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбработатьРасшифровкуТекущегоЭлемента(Элемент,Расшифровка, СтандартнаяОбработка) Экспорт
	
	Перем СписокДоступныхРасшифровок;
	Перем АдресМассиваДоступныхРасшифровок;
	
	СтандартнаяОбработка = Ложь;
		
	СтруктураОтвета = ВернутьСписокДоступныхРасшифровок(Расшифровка,АдресДанныхРасшифровки);
	СтруктураОтвета.Свойство("СписокДоступныхРасшифровок", СписокДоступныхРасшифровок);
	СтруктураОтвета.Свойство("АдресМассиваДоступныхРасшифровок", АдресМассиваДоступныхРасшифровок);
	
	Если СписокДоступныхРасшифровок.Количество() = 1 Тогда
		ВыбранноеЗначение = СписокДоступныхРасшифровок[0];
	Иначе
		
		ДопПараметры =Новый Структура("Расшифровка", Расшифровка);
		Оповещение = Новый ОписаниеОповещения("Подключаемый_ОбработкаРасшифровки", ЭтотОбъект, ДопПараметры);
		ЭтаФорма.ПоказатьВыборИзМеню(Оповещение, СписокДоступныхРасшифровок,Элемент);
		
		Возврат;
		
	КонецЕсли;
	
	ОбработкаРасшифровкиЗавершение(Элемент,ВыбранноеЗначение, Расшифровка);
	
КонецПроцедуры

&НаКлиенте
Процедура СтандартнаяОбработкаРасшифровки(Элемент, Расшифровка, СтандартнаяОбработка = Ложь)

	Перем ВыполненноеДействие;
	Перем Настройки;
	
	// Запретим стандартную обработку расшифровки
	СтандартнаяОбработка = Ложь;
	
	
	ОбработкаРасшифровки = Новый ОбработкаРасшифровкиКомпоновкиДанных(АдресДанныхРасшифровки, 
		Новый ИсточникДоступныхНастроекКомпоновкиДанных(URLСхемы));

	ДопПараметры = Новый Структура("ВыполненноеДействие, Настройки, Расшифровка", ВыполненноеДействие, Настройки, Расшифровка);
	Оповещение = Новый ОписаниеОповещения("Подключаемый_ОбработатьВыборДействия", ЭтотОбъект, ДопПараметры);
	
	ДоступныеДействия = Новый Массив();
	ДоступныеДействия.Добавить(ДействиеОбработкиРасшифровкиКомпоновкиДанных.ОткрытьЗначение);
	ДоступныеДействия.Добавить(ДействиеОбработкиРасшифровкиКомпоновкиДанных.Расшифровать);

	ЭтаФорма.ТекущийЭлемент = Элемент;
	
	ОбработкаРасшифровки.ПоказатьВыборДействия(Оповещение, Расшифровка,ДоступныеДействия,,,Элемент);

КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбработкаРасшифровки(ВыбранныйЭлемент, ДополнительныеПараметры) Экспорт
    
    Расшифровка = ДополнительныеПараметры.Расшифровка;    
    
    ВыбранноеЗначение = ВыбранныйЭлемент;
    
    ОбработкаРасшифровкиЗавершение(ВыбранныйЭлемент,ВыбранноеЗначение, Расшифровка);

КонецПроцедуры

&НаКлиенте
Процедура ВывестиРасшифровку(Операнд, Контекст, Показатель)
	
	РезультатРасшифровки = ПолучитьРезультатРасшифровкиОперанда(Операнд, Контекст);
	
	Если ТипЗнч(РезультатРасшифровки)=Тип("ТабличныйДокумент") Тогда
		
		ОткрытьФорму("Документ.НастраиваемыйОтчет.Форма.РасшифровкаВИБ", новый Структура("ИспользуемаяИБ, Результат", Контекст.ИспользуемаяИБ, РезультатРасшифровки));
		
	ИначеЕсли ТипЗнч(РезультатРасшифровки)=Тип("Структура") Тогда
		
		Если РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнешниеДанныеРегистрБухгалтерии")
			ИЛИ РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеРегистрБухгалтерии")  Тогда
			
			ОткрытьФорму("Отчет.ОтчетПоРегиструБухгалтерииБД.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
			
		ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнешниеДанныеРегистрНакопления")
			ИЛИ РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеРегистрНакопления")Тогда
			
			ОткрытьФорму("Отчет.ОтчетПоРегиструНакопленияБД.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
			
		ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнешниеДанныеПроизвольныйЗапрос") Тогда
			
			ОткрытьФорму("Отчет.ОтчетПоПроизвольномуЗапросуВИБ.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
			
		ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеПроизвольныйЗапрос") Тогда
			
			ОткрытьФорму("Отчет.ОтчетПоПроизвольномуЗапросуТек.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
			
		ИначеЕсли РезультатРасшифровки.СпособПолучения = ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеПоказательОтчета") Тогда
			
			СтруктураОтбора = Новый Структура;
			СтруктураОтбора.Вставить("ОтборПоВалюте", Валюта);
			//СтруктураОтбора.Вставить("ОтборПоОрганизации", Организация);
			СтруктураОтбора.Вставить("ПериодПланированияОтборОкончание", ПериодОтчета);
			СтруктураОтбора.Вставить("ОтборПоСценарию", Сценарий);
			СтруктураОтбора.Вставить("ОтборПоПроекту", Неопределено);
			//НовыйПериодСравнения = ОпределитьПериодСравнения(ПериодОтчета);
			//Контекст.Вставить("ПериодСравнения", НовыйПериодСравнения);
			АдресХранилищаПеременныхДляРасчета = БизнесАнализВызовСервераУХ.ПолучитьАдресОбъектаРасчетаОперанда(Показатель, Операнд, Контекст, СтруктураОтбора, УникальныйИдентификатор, "ФактическоеЗначениеТекущегоПериода");
			СтруктураОтбора = Новый Структура;
			СтруктураПараметров = Новый Структура;
			ПоказательОтчета = ОбщегоНазначенияУХ.ПолучитьЗначениеРеквизита(Операнд, "ПоказательОтбор");
			СтруктураПараметров.Вставить("ПоказательОтчета", РезультатРасшифровки.ПоказательОтбор);
			СтруктураПараметров.Вставить("АдресХранилищаПеременныхДляРасчета", АдресХранилищаПеременныхДляРасчета);
			СтруктураПараметров.Вставить("мТекущаяВалюта", Валюта);
			СтруктураПараметров.Вставить("СтруктураОтбора", СтруктураОтбора);
			ОткрытьФорму("Обработка.РасшифровкаРассчитанныхЗначений.Форма",СтруктураПараметров);
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаРасшифровкиЗавершение(Элемент,ВыбранноеЗначение, Расшифровка)
    
	ИспользуемаяИБ = Неопределено;
	
	Если ВыбранноеЗначение = Неопределено Тогда
        Возврат;
    КонецЕсли;
    
    Если ВыбранноеЗначение.Значение = "СтандартнаяРасшифровка" Тогда
        
        СтандартнаяОбработкаРасшифровки(Элемент,Расшифровка);
        
    ИначеЕсли ВыбранноеЗначение.Значение = "ТиповыеОтчетыУХ.ОткрытьНастраиваемыйОтчет" Тогда
        
        ОткрытьНастраиваемыйОтчет(Расшифровка, АдресДанныхРасшифровки);
        
    ИначеЕсли ТипЗнч(ВыбранноеЗначение) = Тип("ЭлементСпискаЗначений") Тогда
        
           Если ТипЗнч(ВыбранноеЗначение.Значение) = Тип("ФиксированнаяСтруктура") И ВыбранноеЗначение.Значение.Свойство("Операнд") Тогда
            
            Контекст = ВыбранноеЗначение.Значение.Контекст;
            Контекст.Вставить("УправляемыйРежим", Истина);	
			Контекст.Вставить("ЧтениеНеактуальныхЗаписей", Истина);
           			
			//РезультатРасшифровки = ВернутьСтруктуруВызоваОперанда(ВыбранноеЗначение.Значение.Операнд, Контекст, ВыбранноеЗначение.Значение.ДополнительнаяРасшифровка);
			
			Если ТипЗнч(ВыбранноеЗначение.Значение.Операнд) = Тип("СправочникСсылка.ИсточникиДанныхДляРасчетов") Тогда
				РезультатРасшифровки =РасшифроватьОперанд(Контекст,ВыбранноеЗначение.Значение.Операнд, Контекст.ИспользуемаяИБ);
            Иначе
				РезультатРасшифровки =РасшифроватьПоказатель(Контекст,ВыбранноеЗначение.Значение.Операнд, Контекст.ИспользуемаяИБ,Расшифровка);
			КонецЕсли;	
				
            Если ТипЗнч(РезультатРасшифровки)=Тип("ТабличныйДокумент") Тогда
                
                ОткрытьФорму("Документ.НастраиваемыйОтчет.Форма.РасшифровкаВИБ", новый Структура("ИспользуемаяИБ, Результат",ИспользуемаяИБ, РезультатРасшифровки));
                
			ИначеЕсли ТипЗнч(РезультатРасшифровки)=Тип("Структура") Тогда
				
				Если РезультатРасшифровки.Свойство("Экземпляр") Тогда
					
					СтруктураПараметров = Новый Структура();
					СтруктураПараметров.Вставить("Ключ", РезультатРасшифровки.Экземпляр);
					СтруктураПараметров.Вставить("мТекущаяВалюта",Валюта);
					СтруктураПараметров.Вставить("МассивПоказателей",РезультатРасшифровки.МассивПоказателей);
					СтруктураПараметров.Вставить("ПериодРасшифровки",РезультатРасшифровки.ПериодРасшифровки);
					
					СтруктураПараметров.Вставить("СтруктураОтбора",РезультатРасшифровки.СтруктураДопОтбора);
										
					ОткрытьФорму("Документ.НастраиваемыйОтчет.ФормаОбъекта", СтруктураПараметров); 
										
				ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнешниеДанныеРегистрБухгалтерии")
					ИЛИ РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеРегистрБухгалтерии")  Тогда
					
					ОткрытьФорму("Отчет.ОтчетПоРегиструБухгалтерииБД.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
					
				ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнешниеДанныеРегистрНакопления")
					ИЛИ РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеРегистрНакопления")Тогда
					
					ОткрытьФорму("Отчет.ОтчетПоРегиструНакопленияБД.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
					
				ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнешниеДанныеПроизвольныйЗапрос") Тогда
					
					ОткрытьФорму("Отчет.ОтчетПоПроизвольномуЗапросуВИБ.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
					
				ИначеЕсли РезультатРасшифровки.СпособПолучения=ПредопределенноеЗначение("Перечисление.СпособыПолученияОперандов.ВнутренниеДанныеПроизвольныйЗапрос") Тогда
					
					ОткрытьФорму("Отчет.ОтчетПоПроизвольномуЗапросуТек.Форма.ФормаОтчета",РезультатРасшифровки,,Новый УникальныйИдентификатор);
					
				КонецЕсли;
                
                
            КонецЕсли;
            
		Иначе
			
			СтруктураЗначения = ВыбранноеЗначение.Значение;
			ТипОбъектаМетаданных = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(СтруктураЗначения, "ТипОбъектаМетаданных", "");
			Если ЗначениеЗаполнено(ТипОбъектаМетаданных) Тогда
				// Задан объект метаданных. Расшифруем по нему.
				НаименованиеОбъектаМетаданных	 = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(СтруктураЗначения, "НаименованиеОбъектаМетаданных", "");
				ФормаРасшифровки				 = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(СтруктураЗначения, "ФормаРасшифровки", "");
				ОтборыРасшифровки 				 = ВернутьНастройкиОтбораРасшифровки(КомпоновщикНастроек, Расшифровка, АдресДанныхРасшифровки);
				//СтруктураПараметров = ПолучитьСтруктуруПараметровРасшифровкиПоОбъекту(ТипОбъектаМетаданных, НаименованиеОбъектаМетаданных, ФормаРасшифровки, ПроизвольныйОтчет, ОтборыРасшифровки);
				//БизнесАнализКлиентУХ.ОтобразитьРаскрытиеОбъектомИнформационнойБазы(ТипОбъектаМетаданных, НаименованиеОбъектаМетаданных, ФормаРасшифровки, СтруктураПараметров);
			Иначе				
				// Расшифруем аналитическим отчётом.
				ПараметрыФормы = Новый Структура;
				ПараметрыФормы.Вставить("Ключ",						 СтруктураЗначения.Отчет);
				ПараметрыФормы.Вставить("Расшифровка",				 Расшифровка);
				ПараметрыФормы.Вставить("АдресДанныхРасшифровки",	 АдресДанныхРасшифровки); 
				ПараметрыФормы.Вставить("Настройки",				 КомпоновщикНастроек.Настройки);
				ПараметрыФормы.Вставить("СохраненнаяНастройка",		 СтруктураЗначения.СохраненнаяНастройка);
				ПараметрыФормы.Вставить("ФормироватьПриОткрытии",	 Истина); 
				ПараметрыФормы.Вставить("ЭтоРасшифровка",			 Истина);
				ОткрытьФорму("Справочник.ПроизвольныеОтчеты.Форма.ФормаПроизвольногоОтчетаПоИсточнику", ПараметрыФормы);
				
			КонецЕсли;
			
		КонецЕсли;
		
    КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНастраиваемыйОтчет(Расшифровка, АдресДанныхРасшифровок) Экспорт
	
	НастраиваемыеОтчеты = АналитическиеОтчетыУХ.ПолучитьНастраиваемыеОтчетыДляРасшифровки_УправляемоеПриложение(ПоместитьВоВременноеХранилище(Расшифровка), АдресДанныхРасшифровок);
	
	Если НастраиваемыеОтчеты = Неопределено Тогда
		
		ПоказатьПредупреждение(, НСтр("ru = 'Не найдено ни одного экземпляра отчета'"), 60);		
		Возврат;
		
	ИначеЕсли ТипЗнч(НастраиваемыеОтчеты) = Тип("Строка") Тогда
		
		ДокументСсылка = Неопределено;

		ПараметрыФормы = Новый Структура("Расшифровка, АдресДанныхРасшифровки, ИсточникДанныхОтчета", Расшифровка, АдресДанныхРасшифровок, ПакетДанных);
		ДопПараметры = Новый Структура("НастраиваемыеОтчеты, Расшифровка", НастраиваемыеОтчеты, Расшифровка);
		Оповещение = Новый ОписаниеОповещения("Подключаемый_ОткрытьФормуОтчета", ЭтотОбъект, ДопПараметры);
		ОткрытьФорму("ОбщаяФорма.ФормаВыбораНастраиваемогоОтчета_Управляемая", 
						ПараметрыФормы,,,,, Оповещение, РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
        Возврат;
		
	Иначе
		ДокументСсылка = НастраиваемыеОтчеты;
	КонецЕсли;
	
	ОткрытьФормуОтчета(ДокументСсылка, Расшифровка);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОткрытьФормуОтчета(Результат, ДополнительныеПараметры) Экспорт
    
    НастраиваемыеОтчеты = ДополнительныеПараметры.НастраиваемыеОтчеты;
    Расшифровка = ДополнительныеПараметры.Расшифровка;
        
    ДокументСсылка = Результат;
    
    ОткрытьФормуОтчета(ДокументСсылка, Расшифровка);

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтчета(ДокументСсылка, Расшифровка)
    
    Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.НастраиваемыйОтчет") Тогда
        
        АдресПоказателей = АналитическиеОтчетыУХ.ПолучитьПоказателиНастраиваемогоОтчетаДляРасшифровки_УправляемоеПриложение(ПоместитьВоВременноеХранилище(Расшифровка), АдресДанныхРасшифровки, ПоместитьВоВременноеХранилище(ДокументСсылка));
        Если ЭтоАдресВременногоХранилища(АдресПоказателей) Тогда
			
			Показатели = ПолучитьИзВременногоХранилища(АдресПоказателей);
            
            #Если ТолстыйКлиентОбычноеПриложение Тогда
                
                ДокументОбъект = ДокументСсылка.ПолучитьОбъект();
                
                ФормаОтчета = ДокументОбъект.ПолучитьФорму();
                ФормаОтчета.Открыть();
                Если ТипЗнч(ДокументОбъект) <> Тип("ДокументОбъект.КорректировкаЗначенийПоказателей") Тогда
                    Если Показатели.Количество() = 1 Тогда
                        МассивПолей = Новый Массив;
                        МассивПолей.Добавить(Показатели[0]);
                        ФормаОтчета.ВыделитьОбластиПоказателей(МассивПолей);
                    КонецЕсли;
                КонецЕсли;
                
			#Иначе
				
				ПараметрыФормы = Новый Структура("Ключ, МассивПоказателей", ДокументСсылка, Показатели);
                ОткрытьФорму("Документ.НастраиваемыйОтчет.ФормаОбъекта", ПараметрыФормы);
				
			#КонецЕсли
			
        Иначе
			
			Сообщить(НСтр("ru = 'Ошибка получения данных'"));
			
		КонецЕсли;
		
    КонецЕсли;

КонецПроцедуры

&НаСервере
Функция РасшифроватьОперанд(ОбъектРасчета,Операнд, ИспользуемаяИБ,ИмяВладельца="")
	
		
	СтрОперанд= ПолучитьДанныеОперанда(Операнд);
	
	Если СтрОперанд.СпособПолучения=Перечисления.СпособыПолученияОперандов.ВнутренниеДанныеПоказательОтчета Тогда
		
		Если Не ЗначениеЗаполнено(ОбъектРасчета.Сценарий) Тогда
			
			Сообщить(НСтр("ru = 'Не заполнен сценарий отчета'"));
			Возврат Неопределено;	
		ИначеЕсли Не ЗначениеЗаполнено(ОбъектРасчета.Организация) Тогда
			
			Сообщить(НСтр("ru = 'Не заполнена Организация отчета'"));
			Возврат Неопределено;	
		ИначеЕсли Не ЗначениеЗаполнено(ОбъектРасчета.ПериодОтчета) Тогда
			
			Сообщить(НСтр("ru = 'Не заполнена Период отчета'"));
			Возврат Неопределено;	
		КонецЕсли;
		
		
		МассивСервер=Новый Массив;
		МассивСервер.Добавить(СокрЛП(СтрОперанд.ПоказательОтборКод));
		
		МассивПоказателей=Новый ФиксированныйМассив(МассивСервер);
		
		ПоказательРасшифровкаСсылка = СтрОперанд.Ссылка.ПоказательОтбор;
		
		Экземпляр=Документы.НастраиваемыйОтчет.ПолучитьЭкземплярОтчета(ОбъектРасчета.Организация,ОбъектРасчета.ПериодОтчета,ОбъектРасчета.Сценарий,Операнд.ПоказательОтбор.Владелец,,,,,ОбъектРасчета.Проект);

		Если Экземпляр = Неопределено Тогда
			
			Сообщить(НСтр("ru = 'Не удалось определить экземпляр отчета'"));
			Возврат Неопределено;
			
		Иначе			
			
			СтруктураОтвета = Новый Структура;
			СтруктураОтвета.Вставить("Экземпляр",Экземпляр);				
			СтруктураОтвета.Вставить("МассивПоказателей",МассивПоказателей);
			СтруктураОтвета.Вставить("ПериодРасшифровки",ОбъектРасчета.ПериодОтчета);
				
			Возврат СтруктураОтвета;
			
		КонецЕсли;
		
	Иначе
		
		КонтекстОтчета = ОбъектРасчета;
		СтруктураОтбора = Новый Структура;
		Результат = УправлениеОтчетамиУХ.РасшифроватьДанныеОперанда(СтрОперанд.Ссылка, ОбъектРасчета,СтруктураОтбора,ОбъектРасчета);
		Если ТипЗнч(КонтекстОтчета) = Тип("Структура") Тогда
			КонтекстОтчета.Свойство("ИспользуемаяИБ", ИспользуемаяИБ);
		КонецЕсли;
		
		Возврат Результат;
		
	КонецЕсли;
			
КонецФункции // РасшифроватьОперанд()

&НаСервере
Функция РасшифроватьПоказатель(ОбъектРасчета,Операнд, ИспользуемаяИБ,Расшифровка)
		
	
	ДанныеРасшифровки = ПолучитьИзВременногоХранилища(АдресДанныхРасшифровки);
	
	СтруктураПолейИсточника = Новый Структура;
	ПолучитьПоляПоказателияВИсточнике(Операнд,СтруктураПолейИсточника);
		
	СтруктураПолей = Новый Структура;
	Для Каждого Стр Из СтруктураПолейИсточника Цикл
		СтруктураПолей.Вставить(Стр.Ключ);
	КонецЦикла;	
	
	СводнаяТаблицаУХ.ПолучитьРасшифровкуГруппировок(ДанныеРасшифровки.Элементы[Расшифровка],СтруктураПолей);
	
	СтруктураДопОтбора = Новый Структура;
	
	Для Каждого стрПоле Из СтруктураПолей Цикл
		
		Если ОбъектРасчета.Свойство(стрПоле.Ключ)
			И НЕ стрПоле.Значение = Неопределено Тогда
			   ОбъектРасчета.Вставить(стрПоле.Ключ,стрПоле.Значение);
		КонецЕсли;	
		
		Если НЕ ОбъектРасчета.Свойство(стрПоле.Ключ)
			И НЕ стрПоле.Значение = Неопределено Тогда
			   СтруктураДопОтбора.Вставить(СтруктураПолейИсточника[стрПоле.Ключ],стрПоле.Значение);
		КонецЕсли;	
		
	КонецЦикла;	
	
	Для Каждого стрПоле Из КомпоновщикНастроек.Настройки.Отбор.Элементы Цикл
		
		Если СтрНайти(Строка(стрПоле.ЛевоеЗначение),".")>0 Тогда
			Продолжить;
		КонецЕсли;	
				
		Если СтруктураПолейИсточника.Свойство(Строка(стрПоле.ЛевоеЗначение)) Тогда	
			Если ОбъектРасчета.Свойство(Строка(стрПоле.ЛевоеЗначение))
				И стрПоле.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно 
				И НЕ стрПоле.ПравоеЗначение = Неопределено Тогда
				ОбъектРасчета.Вставить(Строка(стрПоле.ЛевоеЗначение),стрПоле.ПравоеЗначение);
			КонецЕсли;	
			
			Если Не ОбъектРасчета.Свойство(Строка(стрПоле.ЛевоеЗначение)) 
				И стрПоле.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно 
				И НЕ стрПоле.ПравоеЗначение = Неопределено Тогда
				СтруктураДопОтбора.Вставить(СтруктураПолейИсточника[Строка(стрПоле.ЛевоеЗначение)],стрПоле.ПравоеЗначение);
			КонецЕсли;	
			
		КонецЕсли;
		
	КонецЦикла;	

	
	Если Не ЗначениеЗаполнено(ОбъектРасчета.Сценарий) Тогда
		
		Сообщить(НСтр("ru = 'Не заполнен сценарий отчета'"));
		Возврат Неопределено;	
	ИначеЕсли Не ЗначениеЗаполнено(ОбъектРасчета.Организация) Тогда
		
		Сообщить(НСтр("ru = 'Не заполнена Организация отчета'"));
		Возврат Неопределено;	
	ИначеЕсли Не ЗначениеЗаполнено(ОбъектРасчета.ПериодОтчета) Тогда
		
		Сообщить(НСтр("ru = 'Не заполнена Период отчета'"));
		Возврат Неопределено;	
	КонецЕсли;
	
	
	МассивСервер=Новый Массив;
	МассивСервер.Добавить(СокрЛП(Операнд.Код));
	
	МассивПоказателей=Новый ФиксированныйМассив(МассивСервер);
	
	ПоказательРасшифровкаСсылка = Операнд;
	
	Экземпляр=Документы.НастраиваемыйОтчет.ПолучитьЭкземплярОтчета(ОбъектРасчета.Организация,ОбъектРасчета.ПериодОтчета,ОбъектРасчета.Сценарий,Операнд.Владелец,,,,,ОбъектРасчета.Проект);
	
	Если Экземпляр = Неопределено Тогда
		
		Сообщить(НСтр("ru = 'Не удалось определить экземпляр отчета'"));
		Возврат Неопределено;
		
	Иначе			
		
		СтруктураОтвета = Новый Структура;
		СтруктураОтвета.Вставить("Экземпляр",Экземпляр);			
		СтруктураОтвета.Вставить("МассивПоказателей",МассивПоказателей);
		СтруктураОтвета.Вставить("ПериодРасшифровки",ОбъектРасчета.ПериодОтчета);
		СтруктураОтвета.Вставить("СтруктураДопОтбора",СтруктураДопОтбора);
		
		Возврат СтруктураОтвета;
		
	КонецЕсли;
						
				
КонецФункции // РасшифроватьОперанд()

Процедура ПолучитьПоляПоказателияВИсточнике(Источник,СтруктураПолей)
	
	СтруктураПолей = Новый Структура;
	
	ИзмеренияИсточника = ПакетДанных.ИзмеренияИсточников.НайтиСтроки(Новый Структура("Источник",Источник));
	Для Каждого Стр Из ИзмеренияИсточника Цикл 
		СтруктураПолей.Вставить(Стр.ИмяВКубе,Стр.ПутьКИзмерению);
	КонецЦикла;	   
	
КонецПроцедуры	




&НаСервере
Функция ПолучитьДанныеОперанда(Операнд)
	
	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ
	|	ИсточникиДанныхДляРасчетов.Ссылка,
	|	ИсточникиДанныхДляРасчетов.СпособПолучения,
	|	ИсточникиДанныхДляРасчетов.ПоказательТекущегоОтчета,
	|	ИсточникиДанныхДляРасчетов.ПоказательОтбор.Код КАК ПоказательОтборКод,
	|	ИсточникиДанныхДляРасчетов.ПравилаИспользованияПолейЗапроса.(
	|		Ссылка,
	|		НомерСтроки,
	|		Поле,
	|		Синоним,
	|		АналитикаОперанда,
	|		КодАналитики,
	|		НастройкаСоответствия,
	|		РеквизитАналитики,
	|		ТаблицаАналитики,
	|		ТаблицаАналитикиВИБ,
	|		НеИспользоватьДляСинхронизации,
	|		СпособЗаполнения,
	|		ФиксированноеЗначение,
	|		РеквизитОбъекта
	|	) КАК ПравилаИспользованияПолейЗапроса
	|ИЗ
	|	Справочник.ИсточникиДанныхДляРасчетов КАК ИсточникиДанныхДляРасчетов
	|ГДЕ
	|	ИсточникиДанныхДляРасчетов.Ссылка = &Операнд";
	
	Запрос.УстановитьПараметр("Операнд",Операнд);
	
	Возврат Запрос.Выполнить().Выгрузить()[0];
					 	
КонецФункции

&НаСервере
Функция ПрименитьНастройкиКД(Расшифровка, Настройки)
	
	ОбработкаРасшифровки = Новый ОбработкаРасшифровкиКомпоновкиДанных(АдресДанныхРасшифровки, 
		Новый ИсточникДоступныхНастроекКомпоновкиДанных(URLСхемы));

	Возврат  ОбработкаРасшифровки.ПрименитьНастройки(Расшифровка, Настройки);
	 
КонецФункции

&НаСервере
Функция  ВернутьСписокДоступныхРасшифровок(Расшифровка,АдресДанныхРасшифровки)
		
	Возврат Справочники.АналитическиеПанели.ВернутьСписокДоступныхРасшифровок(ЭтаФорма,Расшифровка,АдресДанныхРасшифровки);
	  
КонецФункции

&НаСервере
Функция ПолучитьРезультатРасшифровкиОперанда(Операнд, Контекст)
	
	Возврат  Справочники.АналитическиеПанели.ПолучитьРезультатРасшифровкиОперанда(Операнд, Контекст);
	
КонецФункции

&НаСервере
Функция ВернутьНастройкиОтбораРасшифровки(КомпоновщикНастроекВход, РасшифровкаВход, АдресДанныхРасшифровкиВход) 
	
	Возврат Справочники.АналитическиеПанели.ВернутьНастройкиОтбораРасшифровки(КомпоновщикНастроекВход, РасшифровкаВход, АдресДанныхРасшифровкиВход); 
	
КонецФункции


#КонецОбласти


