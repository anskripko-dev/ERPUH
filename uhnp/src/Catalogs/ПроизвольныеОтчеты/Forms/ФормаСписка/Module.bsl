
&НаКлиенте
Процедура ОткрытьОтчет(Команда)
	
	ТекСтрока = Элементы.Список.ТекущиеДанные;
	Если ТекСтрока <> Неопределено Тогда
		// Проверим, что выбрана не группа, и откроем форму выбранного отчета.
		Если НЕ ТекСтрока.ЭтоГруппа Тогда
			Если ТекСтрока.ВидПроизвольногоОтчета = 0 Тогда
				ТекстСообщения = НСтр("ru = 'Отчет по источнику больше не поддерживается. Используйте аналитические панели.'");	
				ОбщегоНазначенияУХ.СообщитьОбОшибке(ТекстСообщения);	
			ИначеЕсли ТекСтрока.ВидПроизвольногоОтчета = 1 Тогда
				СтруктураПараметровФормы = Новый Структура;
				СтруктураПараметровФормы.Вставить("Ключ", ТекСтрока.Ссылка);
				СтруктураПараметровФормы.Вставить("ФормироватьПриОткрытии", Истина);
				ОткрытьФорму("Справочник.ПроизвольныеОтчеты.Форма.ФормаПроизвольногоОтчетаПоМКП", СтруктураПараметровФормы);	
			Иначе 
				ТекстСообщения = НСтр("ru = 'Отчет по бланку-источнику больше не поддерживается. Используйте форму видов и бланков отчетов.'");	
				ОбщегоНазначенияУХ.СообщитьОбОшибке(ТекстСообщения);
			КонецЕсли;		
		Иначе
			ТекстСообщения = НСтр("ru = 'Невозможно открыть группу отчетов. Операция отменена.'");
			ОбщегоНазначенияУХ.СообщитьОбОшибке(ТекстСообщения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Владелец = ОбщегоНазначенияУХ.ПолучитьЗначениеПеременной("глТекущийПользователь");
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	МоиОтчеты = Ложь;
	УстановитьОтборПоОтчетамПользователя();
	
КонецПроцедуры

Процедура УстановитьОтборПоОтчетамПользователя()
	
	СписокЗначений = Новый СписокЗначений;
	СписокЗначений.Добавить(Владелец);
	СписокЗначений.Добавить(ПредопределенноеЗначение("Справочник.ГруппыПользователей.ВсеПользователи"));
	ТиповыеОтчетыУХ.ДобавитьОтбор(Список.Отбор, Новый ПолеКомпоновкиДанных("Владелец"), СписокЗначений, ВидСравненияКомпоновкиДанных.ВСписке);
	
КонецПроцедуры

&НаКлиенте
Процедура ВладелецПриИзменении(Элемент)
	
	УстановитьОтборПоОтчетамПользователя();
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьНовуюАналитическуюПанель(Команда)
	
	СписокОтчетов = новый СписокЗначений;
	
	Для Каждого ВыделенныйЭлемент Из Элементы.Список.ВыделенныеСтроки Цикл
		
		СписокОтчетов.Добавить(ВыделенныйЭлемент);
		
	КонецЦикла;
	
	ОткрытьФорму("Справочник.УдалитьПанелиОтчетов.Форма.ФормаНастройки_Управляемая", Новый Структура("СписокНачальногоЗаполнения", СписокОтчетов));
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПроизвольныйОтчетБланк(Команда)
	
	СтруктураПараметров = Новый Структура;
	ПараметрыЗаполнения = Новый Структура;
	ПараметрыЗаполнения.Вставить("ВидПроизвольногоОтчета", 2);
	
	ОткрытьФорму("Справочник.ПроизвольныеОтчеты.Форма.ФормаНастройкиБланк", ПараметрыЗаполнения, ВладелецФормы); 
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПроизвольныйОтчетИсточник(Команда)
	
	СтруктураПараметров = Новый Структура;
	ПараметрыЗаполнения = Новый Структура;
	ПараметрыЗаполнения.Вставить("ВидПроизвольногоОтчета", 0);
	
	ОткрытьФорму("Справочник.ПроизвольныеОтчеты.Форма.ФормаНастройкиИсточник", ПараметрыЗаполнения, ВладелецФормы); 
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьМониторКлючевыхПоказателей(Команда)
	
	СтруктураПараметров = Новый Структура;
	ПараметрыЗаполнения = Новый Структура;
	ПараметрыЗаполнения.Вставить("ВидПроизвольногоОтчета", 1);
	
	ОткрытьФорму("Справочник.ПроизвольныеОтчеты.Форма.ФормаНастройкиМКП", ПараметрыЗаполнения, ВладелецФормы); 
	

КонецПроцедуры
