Перем ОбъектОснование;

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	
КонецПроцедуры


&НаКлиенте
Процедура ДоступностьЭлементаНастройкиВывода()
	
	Элементы.ОтображатьВВидеСхемы.Доступность = Объект.ОтображатьЦели;
	Если Объект.ОтображатьВВидеСхемы И НЕ Объект.ОтображатьЦели Тогда		
		Объект.ОтображатьВВидеСхемы = Ложь;		
	КонецЕсли;
	
КонецПроцедуры


&НаКлиенте
Процедура ОткрытьОтчет(Команда)
	
	Если Параметры.Ключ.Пустая() ИЛИ Модифицированность Тогда
		
		Оповещение = Новый ОписаниеОповещения("Подключаемый_ОбработатьЗаписьОтчета", ЭтотОбъект);
		ПоказатьВопрос(Оповещение, НСтр("ru = 'Для продолжения нужно записать изменения в отчете. Записать?'"), РежимДиалогаВопрос.ДаНет);
        Возврат;
		
	КонецЕсли;
	
	ОткрытьФормуОтчета();
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбработатьЗаписьОтчета(РезультатВопроса, ДополнительныеПараметры) Экспорт
    
    Если РезультатВопроса = КодВозвратаДиалога.Нет Тогда
        Возврат;
    КонецЕсли;
    
    Если НЕ Записать() Тогда
        ОбщегоНазначенияУХ.СообщитьОбОшибке(Нстр("ru = 'Не удалось записать отчет'"));
        Возврат;
    КонецЕсли;
        
    ОткрытьФормуОтчета();

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтчета()
	
	ПараметрыФормы = Новый Структура("Монитор, ФормироватьПриОткрытии", Параметры.Ключ, Истина);
	ОткрытьФорму("Справочник.МониторыКлючевыхПоказателей.Форма.ФормаПроизвольногоОтчетаПоМКП", ПараметрыФормы);
					
КонецПроцедуры


&НаКлиенте
Процедура ОтображатьЦелиПриИзменении(Элемент)
	
	ДоступностьЭлементаНастройкиВывода();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ДоступностьЭлементаНастройкиВывода();
	
КонецПроцедуры

&НаКлиенте
Процедура НастройкаРассылкиПоРасписанию(Команда)
	
	Если ЭтаФорма.Модифицированность Тогда	
		Оповещение = Новый ОписаниеОповещения("Рассылка_ВопросСохранитьЗавершение", ЭтотОбъект);
		ПоказатьВопрос(Оповещение, НСтр("ru = 'Записать изменения ?'"), РежимДиалогаВопрос.ДаНетОтмена);
	Иначе	
		ЗначениеОтбора = Новый Структура("РассылаемыйОбъект", Объект.Ссылка);
		ПараметрыВыбора_ = Новый Структура("Отбор", ЗначениеОтбора);		
		ОткрытьФорму("Справочник.АналитическаяПодписка.ФормаСписка",ПараметрыВыбора_,ЭтаФорма,Истина,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Рассылка_ВопросСохранитьЗавершение(Результат, ДополнительныеПараметры) Экспорт      
	
	Если Результат = Неопределено  Или Результат = КодВозвратаДиалога.Отмена  Тогда
		Возврат;
	КонецЕсли;	
	
	Если Результат = КодВозвратаДиалога.Да Тогда
		ЭтаФорма.Записать();
		
		ЗначениеОтбора = Новый Структура("РассылаемыйОбъект", Объект.Ссылка);
		ПараметрыВыбора_ = Новый Структура("Отбор", ЗначениеОтбора);
		
		ОткрытьФорму("Справочник.АналитическаяПодписка.ФормаСписка",ПараметрыВыбора_,ЭтаФорма,Истина,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;	
		
КонецПроцедуры





