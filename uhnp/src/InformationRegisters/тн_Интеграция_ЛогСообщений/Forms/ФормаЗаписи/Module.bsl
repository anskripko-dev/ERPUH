
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КлючЗаписи = Запись.Ключ;
		
	ДатаВходящееСообщение          = Дата("00010101") + Цел(Запись.УниверсальнаяДатаВходящееСообщение  / 1000);
	МиллиСекундыВходящееСообщение  = Формат(Запись.УниверсальнаяДатаВходящееСообщение % 1000, "ЧЦ=3; ЧВН=");
		
	ДатаИсходящееСообщение         = Дата("00010101") + Цел(Запись.УниверсальнаяДатаИсходящееСообщение / 1000);
	МиллиСекундыИсходящееСообщение = Формат(Запись.УниверсальнаяДатаИсходящееСообщение % 1000, "ЧЦ=3; ЧВН=");
	

	// Борисенко И.Н. 23.07.2021 Выполнение задач №000035594: закомментировал
	//// НАЧАЛО Соколов А.А. 01.03.2016 Выполнение задач 000019945
	//Если Запись.ВидИнтеграции = ПредопределенноеЗначение("Перечисление.тн_ВидыИнтеграций.Обмены_Получение") Тогда 
	//	Запись.XML_ПакетИсходящий = тн_Интеграция.РазСжатьСтроку(Запись.XML_ПакетВходящий);
	//ИначеЕсли Запись.ВидИнтеграции = ПредопределенноеЗначение("Перечисление.тн_ВидыИнтеграций.Обмены_Отправка") Тогда 
	//	Запись.XML_ПакетВходящий = тн_Интеграция.РазСжатьСтроку(Запись.XML_ПакетИсходящий);		
	//КонецЕсли;		
	//// КОНЕЦ Соколов А.А. 01.03.2016 Выполнение задач 000019945
	
КонецПроцедуры
