#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

#Область ОбновлениеИнформационнойБазы

// Добавляет в список процедуры-обработчики обновления данных ИБ
// для всех поддерживаемых версий библиотеки или конфигурации.
// Вызывается перед началом обновления данных ИБ для построения плана обновления.
//
//  Обработчики - См. ОбновлениеИнформационнойБазы.НоваяТаблицаОбработчиковОбновления
//
// Пример добавления процедуры-обработчика в список:
//  Обработчик = Обработчики.Добавить();
//  Обработчик.Версия              = "1.1.0.0";
//  Обработчик.Процедура           = "ОбновлениеИБ.ПерейтиНаВерсию_1_1_0_0";
//  Обработчик.МонопольныйРежим    = Ложь;
//
// Параметры:
// 	Обработчики - см. ОбновлениеИнформационнойБазы.НоваяТаблицаОбработчиковОбновления
//
Процедура ПриДобавленииОбработчиковОбновления(Обработчики) Экспорт

	Обработчик = Обработчики.Добавить();
	Обработчик.Процедура = "РегистрыСведений.УдалитьНастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Версия = "2.5.21.38";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("ba287e94-af93-4fde-ec87-ad3580734567");
	Обработчик.Многопоточный = Истина;
	Обработчик.Порядок = Перечисления.ПорядокОбработчиковОбновления.Некритичный;
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "РегистрыСведений.УдалитьНастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ЗарегистрироватьДанныеКОбработкеДляПереходаНаНовуюВерсию";
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.Комментарий = НСтр("ru = 'Переносит настройки счетов международного учета с объектов учета типа
		|перечисление ""Объекты финансового учета"" на объекты учета типа план видов характеристик ""Статьи активов и пассивов"".';
		|en = 'Transfers settings of financial accounting accounts from accounting objects of the ""Financial accounting objects""
		|enumeration type to accounting objects of the ""Asset and liability items"" chart of characteristic types type.'");
	
	Читаемые = Новый Массив;
	Читаемые.Добавить(Метаданные.РегистрыСведений.УдалитьНастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ПолноеИмя());
	Обработчик.ЧитаемыеОбъекты = СтрСоединить(Читаемые, ",");
	
	Изменяемые = Новый Массив;
	Изменяемые.Добавить(Метаданные.РегистрыСведений.УдалитьНастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ПолноеИмя());
	Изменяемые.Добавить(Метаданные.РегистрыСведений.НастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");
	
	Блокируемые = Новый Массив;
	Блокируемые.Добавить(Метаданные.РегистрыСведений.УдалитьНастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ПолноеИмя());
	Блокируемые.Добавить(Метаданные.РегистрыСведений.НастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ПолноеИмя());
	Обработчик.БлокируемыеОбъекты = СтрСоединить(Блокируемые, ",");

КонецПроцедуры

// Параметры:
// 	Параметры - см. ОбновлениеИнформационнойБазы.ОсновныеПараметрыОтметкиКОбработке
//
Процедура ЗарегистрироватьДанныеКОбработкеДляПереходаНаНовуюВерсию(Параметры) Экспорт
	
	МетаданныеРегистра = СоздатьНаборЗаписей().Метаданные();
	ПолноеИмяРегистра = МетаданныеРегистра.ПолноеИмя();
	
	ПараметрыВыборки = Параметры.ПараметрыВыборки;
	ПараметрыВыборки.ПолныеИменаРегистров = ПолноеИмяРегистра;
	ПараметрыВыборки.СпособВыборки = ОбновлениеИнформационнойБазы.СпособВыборкиИзмеренияНезависимогоРегистраСведений();
	
	ДополнительныеПараметры = ОбновлениеИнформационнойБазы.ДополнительныеПараметрыОтметкиОбработки();
	ДополнительныеПараметры.Вставить("ЭтоНезависимыйРегистрСведений", Истина);
	ДополнительныеПараметры.Вставить("ПолноеИмяРегистра", ПолноеИмяРегистра);
	
	ТекстЗапроса = "
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	НастройкиЗаполненияСубконто.ПланСчетов КАК ПланСчетов,
	|	НастройкиЗаполненияСубконто.НастройкаФормированияПроводок КАК НастройкаФормированияПроводок,
	|	НастройкиЗаполненияСубконто.СчетУчета КАК СчетУчета
	|ИЗ
	|	РегистрСведений.УдалитьНастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета КАК НастройкиЗаполненияСубконто
	|ГДЕ
	|	НастройкиЗаполненияСубконто.ОбъектУчета <> ЗНАЧЕНИЕ(Перечисление.УдалитьОбъектыФинансовогоУчета.ПустаяСсылка)
	|	И НастройкиЗаполненияСубконто.СтатьяАктивовПассивов = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка)
	|	И НЕ НастройкиЗаполненияСубконто.НеОбработанаАвтоматически
	|";
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Результат = Запрос.Выполнить();
	
	НастройкиСчетов = Результат.Выгрузить();
	ОбновлениеИнформационнойБазы.ОтметитьКОбработке(Параметры, НастройкиСчетов, ДополнительныеПараметры);
	
КонецПроцедуры

// Обработчик обновления
// 
// Параметры:
// 	Параметры - См. ОбновлениеИнформационнойБазы.ДополнительныеПараметрыВыборкиДанныхДляМногопоточнойОбработки 
//
Процедура ОбработатьДанныеДляПереходаНаНовуюВерсию(Параметры) Экспорт
	
	МетаданныеРегистра = СоздатьНаборЗаписей().Метаданные();
	ПолноеИмяРегистра = МетаданныеРегистра.ПолноеИмя();
	
	ОбновляемыеДанные = ОбновлениеИнформационнойБазы.ДанныеДляОбновленияВМногопоточномОбработчике(Параметры);
	
	Если ОбновляемыеДанные.Количество() = 0 Тогда
		Параметры.ОбработкаЗавершена = ОбновлениеИнформационнойБазы.ОбработкаДанныхЗавершена(Параметры.Очередь, ПолноеИмяРегистра);
		Возврат;
	КонецЕсли;
	
	СоответствиеОбъектовСтатьям = Перечисления.УдалитьОбъектыФинансовогоУчета.СоответствиеОбъектовФинансовогоУчетаСтатьямАктивовПассивов();
	ЗаменяемыеОбъектыУчета = Перечисления.УдалитьОбъектыФинансовогоУчета.ЗаменяемыеОбъектыФинансовогоУчета();
	
	Для Каждого Выборка Из ОбновляемыеДанные Цикл
		НачатьТранзакцию();
		
		Попытка
			
			БлокировкаДанных = Новый БлокировкаДанных();
			БлокировкаДанных.Добавить(ПолноеИмяРегистра);
			БлокировкаДанных.Добавить(Метаданные.РегистрыСведений.НастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.ПолноеИмя());
			
			Для Каждого ЭлементБлокировки Из БлокировкаДанных Цикл
				ЭлементБлокировки.УстановитьЗначение("ПланСчетов", Выборка.ПланСчетов);
				ЭлементБлокировки.УстановитьЗначение("НастройкаФормированияПроводок", Выборка.НастройкаФормированияПроводок);
				ЭлементБлокировки.УстановитьЗначение("СчетУчета", Выборка.СчетУчета);
				ЭлементБлокировки.Режим = РежимБлокировкиДанных.Исключительный;
			КонецЦикла;
			
			БлокировкаДанных.Заблокировать();
			
			НаборЗаписейЭтогоРегистра = СоздатьНаборЗаписей();
			НаборЗаписейЭтогоРегистра.Отбор.ПланСчетов.Установить(Выборка.ПланСчетов);
			НаборЗаписейЭтогоРегистра.Отбор.НастройкаФормированияПроводок.Установить(Выборка.НастройкаФормированияПроводок);
			НаборЗаписейЭтогоРегистра.Отбор.СчетУчета.Установить(Выборка.СчетУчета);
			НаборЗаписейЭтогоРегистра.Прочитать();
			
			НаборЗаписейНовогоРегистра = РегистрыСведений.НастройкиЗаполненияСубконтоНаСчетахМеждународногоУчета.СоздатьНаборЗаписей();
			НаборЗаписейНовогоРегистра.Отбор.ПланСчетов.Установить(Выборка.ПланСчетов);
			НаборЗаписейНовогоРегистра.Отбор.НастройкаФормированияПроводок.Установить(Выборка.НастройкаФормированияПроводок);
			НаборЗаписейНовогоРегистра.Отбор.СчетУчета.Установить(Выборка.СчетУчета);
			
			ТаблицаЗаписей = НаборЗаписейЭтогоРегистра.Выгрузить();
			
			Для Каждого ЗаписьЭтогоРегистра Из НаборЗаписейЭтогоРегистра Цикл
				Если ЗаписьЭтогоРегистра.ОбъектУчета <> Перечисления.УдалитьОбъектыФинансовогоУчета.ПустаяСсылка()
					И ЗаписьЭтогоРегистра.СтатьяАктивовПассивов = ПланыВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка()
					И НЕ ЗаписьЭтогоРегистра.НеОбработанаАвтоматически Тогда
					СтатьяАктивовПассивов = СоответствиеОбъектовСтатьям[ЗаписьЭтогоРегистра.ОбъектУчета];
					
					Если ЗначениеЗаполнено(СтатьяАктивовПассивов) Тогда
						ЗаписьЭтогоРегистра.СтатьяАктивовПассивов = СтатьяАктивовПассивов;
					Иначе
						ЗаписьЭтогоРегистра.НеОбработанаАвтоматически = Истина;
						Продолжить;
					КонецЕсли;
					
					ЗаписатьВНовыйРегистр = Истина;
					ЗаменяющийОбъектУчета = ЗаменяемыеОбъектыУчета[ЗаписьЭтогоРегистра.ОбъектУчета];
					
					Если ЗаменяющийОбъектУчета <> Неопределено Тогда
						Для Каждого ЗаписьТаблицы Из ТаблицаЗаписей Цикл
							Если ЗаписьТаблицы.ОбъектУчета = ЗаменяющийОбъектУчета Тогда
								ЗаписатьВНовыйРегистр = Ложь;
								Для Каждого Ресурс Из МетаданныеРегистра.Ресурсы Цикл
									Если ЗаписьТаблицы[Ресурс.Имя] <> ЗаписьЭтогоРегистра[Ресурс.Имя] Тогда
										ЗаписьЭтогоРегистра.НеОбработанаАвтоматически = Истина;
										Прервать;
									КонецЕсли;
								КонецЦикла;
								Прервать;
							КонецЕсли;
						КонецЦикла;
					КонецЕсли;
					
					Если ЗаписатьВНовыйРегистр Тогда
						ЗаписьНовогоРегистра = НаборЗаписейНовогоРегистра.Добавить();
						ЗаполнитьЗначенияСвойств(ЗаписьНовогоРегистра, ЗаписьЭтогоРегистра,, "ОбъектУчета");
						ЗаписьНовогоРегистра.ОбъектУчета = СтатьяАктивовПассивов;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			
			Если НаборЗаписейЭтогоРегистра.Модифицированность() Тогда
				ОбновлениеИнформационнойБазы.ЗаписатьНаборЗаписей(НаборЗаписейЭтогоРегистра);
			Иначе
				ОбновлениеИнформационнойБазы.ОтметитьВыполнениеОбработки(НаборЗаписейЭтогоРегистра);
			КонецЕсли;
			
			Если НаборЗаписейНовогоРегистра.Количество() > 0 Тогда
				ОбновлениеИнформационнойБазы.ЗаписатьНаборЗаписей(НаборЗаписейНовогоРегистра);
			КонецЕсли;
			
			ЗафиксироватьТранзакцию();
			
		Исключение
			
			ОтменитьТранзакцию();
			
			Шаблон = НСтр("ru = 'Не удалось записать данные в регистр %1 по причине: %2';
							|en = 'Cannot save the data to the %1 register. Reason: %2'");
			
			ТекстСообщения = 	СтрШаблон(
				Шаблон,
				ПолноеИмяРегистра,
				ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			
			ЗаписьЖурналаРегистрации(
				ОбновлениеИнформационнойБазы.СобытиеЖурналаРегистрации(),
				УровеньЖурналаРегистрации.Ошибка,
				МетаданныеРегистра,,
				ТекстСообщения);
			
		КонецПопытки;
		
	КонецЦикла;
	
	Параметры.ОбработкаЗавершена = Не ОбновлениеИнформационнойБазы.ЕстьДанныеДляОбработки(Параметры.Очередь, ПолноеИмяРегистра);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#КонецЕсли