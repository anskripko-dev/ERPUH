#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура - Очистить все записи из регистра сведений "ХранилищаПараметрическойНастройкиРасширенный"
//
// Параметры:
//  Источник - Произвольный - Служебный параметр
//  Отказ	 - Булево - В случае ошибки устанавливается в значение "Истина"
//
Процедура ОчиститьЗаписи(Источник = Неопределено, Отказ = Ложь) Экспорт
	
	НаборЗаписей = РегистрыСведений.ХранилищаПараметрическойНастройкиРасширенный.СоздатьНаборЗаписей();

	Если Источник <> Неопределено Тогда
		НаборЗаписей.Отбор.ИсточникПараметрическойНастройки.Установить(Источник);
	КонецЕсли;
	
	Попытка
		НаборЗаписей.Записать();
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ТекстСообщения = НСтр("ru = 'Не удалось очистить регистр сведений ""Хранилища параметрической настройки (расширенный)"", по причине: %1'");
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		ОбщегоНазначенияУХ.СообщитьОбОшибке(ТекстСообщения, Отказ);
	КонецПопытки;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли