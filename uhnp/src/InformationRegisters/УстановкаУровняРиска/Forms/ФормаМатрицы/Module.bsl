
&НаСервере
Процедура СформироватьНаСервере()
	  //Получаем схему из макета
    СхемаКомпоновкиДанных = РегистрыСведений.УстановкаУровняРиска.ПолучитьМакет("Матрица");

    //создадим компоновщик настроек и загрузим настройки по умолчанию, вместо настроек по умолчанию можно использовать восстановленные настройки
    КомпоновщикНастроек = Новый КомпоновщикНастроекКомпоновкиДанных();
    КомпоновщикНастроек.Инициализировать(Новый ИсточникДоступныхНастроекКомпоновкиДанных(СхемаКомпоновкиДанных));
    КомпоновщикНастроек.ЗагрузитьНастройки(СхемаКомпоновкиДанных.НастройкиПоУмолчанию);
    Настройки = КомпоновщикНастроек.Настройки;
    
    
      //Помещаем в переменную данные о расшифровке данных - здесь ненужный пункт, но пусть будет.
    ДанныеРасшифровки = Новый ДанныеРасшифровкиКомпоновкиДанных;

    //Формируем макет, с помощью компоновщика макета
    КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;

    //Передаем в макет компоновки схему, настройки и данные расшифровки
    МакетКомпоновки = КомпоновщикМакета.Выполнить(СхемаКомпоновкиДанных, Настройки, ДанныеРасшифровки);

    //Выполним компоновку с помощью процессора компоновки
    ПроцессорКомпоновкиДанных = Новый ПроцессорКомпоновкиДанных;
    ПроцессорКомпоновкиДанных.Инициализировать(МакетКомпоновки, , ДанныеРасшифровки);

    //Очищаем поле табличного документа
    Результат = Новый ТабличныйДокумент();
    //Выводим результат в табличный документ
    ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
    ПроцессорВывода.УстановитьДокумент(Результат);

    ПроцессорВывода.Вывести(ПроцессорКомпоновкиДанных);
	
	АдресДанныхРасшифровки = ПоместитьВоВременноеХранилище(ДанныеРасшифровки, УникальныйИдентификатор);
	
    Результат.ОтображатьЗаголовки = Ложь;
    Результат.ОтображатьСетку = Ложь;
 
КонецПроцедуры

&НаКлиенте
Процедура Сформировать(Команда)
	СформироватьНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура РезультатОбработкаРасшифровки(Элемент, Расшифровка, СтандартнаяОбработка, ДополнительныеПараметры)
	СтандартнаяОбработка = Ложь;
	ВыборУровня(Расшифровка);
КонецПроцедуры

&НаСервере
Функция ПолучитьДанныеПоля(ИдентификаторРасшифровки)
	Если ИдентификаторРасшифровки = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	МассивДанныхРасшифровки = ПолучитьИзВременногоХранилища(АдресДанныхРасшифровки); //ДанныеРасшифровки
	Если МассивДанныхРасшифровки = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	МассивДанных = Новый Массив;
	Если ТипЗнч(ИдентификаторРасшифровки) = Тип("Массив") Тогда
		Для каждого ЭлементРасшифровки Из ИдентификаторРасшифровки Цикл
			Элемент = МассивДанныхРасшифровки.Элементы[ЭлементРасшифровки];
			Если Элемент.ПолучитьРодителей().Количество() < 2 Тогда 
				Продолжить;
			КонецЕсли;
			СтруктураПолей = Новый Структура("Вероятность", "Ущерб");
			Родитель1 = Элемент.ПолучитьРодителей()[0].ПолучитьПоля()[0];
			Родитель2 = Элемент.ПолучитьРодителей()[1].ПолучитьПоля()[0];
			СтруктураПолей.Вставить(Родитель1.Поле, Родитель1.Значение);
			СтруктураПолей.Вставить(Родитель2.Поле, Родитель2.Значение);
			МассивДанных.Добавить(СтруктураПолей);
		КонецЦикла;
	Иначе
		Элемент = МассивДанныхРасшифровки.Элементы[ИдентификаторРасшифровки];
		Если Элемент.ПолучитьРодителей().Количество() < 2 Тогда 
			Возврат Ложь;
		КонецЕсли;
		СтруктураПолей = Новый Структура("Вероятность", "Ущерб");
		Родитель1 = Элемент.ПолучитьРодителей()[0].ПолучитьПоля()[0];
		Родитель2 = Элемент.ПолучитьРодителей()[1].ПолучитьПоля()[0];
		СтруктураПолей.Вставить(Родитель1.Поле, Родитель1.Значение);
		СтруктураПолей.Вставить(Родитель2.Поле, Родитель2.Значение);
		МассивДанных.Добавить(СтруктураПолей);
	КонецЕсли;
	ПереченьДанных = Новый ФиксированныйМассив(МассивДанных);
	Возврат Истина;
КонецФункции

&НаКлиенте
Процедура ВыборУровня(Расшифровка)
	Если ПолучитьДанныеПоля(Расшифровка) Тогда
		ПараметрыФормы  = Новый Структура("РежимВыбора, ЗакрыватьПриВыборе", Истина, Истина);
    	ОбработкаВыбора = Новый ОписаниеОповещения("ВыбратьЭлементСправочникаКонец", ЭтаФорма);
        ОткрытьФорму("Справочник.УровниРиска.ФормаВыбора", ПараметрыФормы, ЭтаФорма, , , , ОбработкаВыбора);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьЭлементСправочникаКонец(Значение, ДопПараметры) Экспорт
	Если Значение = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Для каждого ЭлементМассива Из ПереченьДанных Цикл
		ЗаписатьВБазе(ЭлементМассива.Вероятность, ЭлементМассива.Ущерб, Значение);
	КонецЦикла;
	СформироватьНаСервере();
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ЗаписатьВБазе(Вероятность, Ущерб, УровеньРиска)
	МенеджерЗаписи = РегистрыСведений.УстановкаУровняРиска.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Вероятность = Вероятность;
	МенеджерЗаписи.Ущерб = Ущерб;
	МенеджерЗаписи.УровеньРиска = УровеньРиска;
	МенеджерЗаписи.Записать();
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОчиститьВБазе(Вероятность, Ущерб)
	Набор = РегистрыСведений.УстановкаУровняРиска.СоздатьНаборЗаписей();
	Набор.Отбор.Вероятность.Установить(Вероятность);
	Набор.Отбор.Ущерб.Установить(Ущерб);
	Набор.Записать();
КонецПроцедуры

&НаКлиенте
Процедура Выбрать(Команда)
	Если Элементы.Результат.ТекущаяОбласть <> Неопределено Тогда
		ВыборУровня(Элементы.Результат.ТекущаяОбласть.Расшифровка);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Очистить(Команда)
	Если Элементы.Результат.ТекущаяОбласть = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Если ПолучитьДанныеПоля(Элементы.Результат.ТекущаяОбласть.Расшифровка) Тогда
		Для каждого ЭлементМассива Из ПереченьДанных Цикл
			ОчиститьВБазе(ЭлементМассива.Вероятность, ЭлементМассива.Ущерб);
		КонецЦикла;
	КонецЕсли;
	СформироватьНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	СформироватьНаСервере();
КонецПроцедуры
