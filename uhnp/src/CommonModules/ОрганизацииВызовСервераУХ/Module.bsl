#Область ПрограммныйИнтерфейс

#Область УХ_Встраивание

Функция ПолучитьКонтрагентовПоОрганизации(ОрганизацияИсточник) Экспорт
	
	Если Не ЗначениеЗаполнено(ОрганизацияИсточник) Тогда
		// Для обеспечения совместимости с вызовами, которые ожидают только массив.
		Возврат Новый Массив;
	КонецЕсли;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	Контрагенты.Ссылка
	|ИЗ
	|	Справочник.Контрагенты КАК Контрагенты
	|ГДЕ
	|	Контрагенты.ОрганизационнаяЕдиница В (&ОрганизационнаяЕдиница)");
	
	Запрос.УстановитьПараметр("ОрганизационнаяЕдиница", ОрганизацияИсточник);
		
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
		
КонецФункции

Функция КонтрагентПоОрганизации(Организация) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Контрагенты.Ссылка КАК Элемент
	|ИЗ
	|	Справочник.Контрагенты КАК Контрагенты
	|ГДЕ
	|	Контрагенты.ОрганизационнаяЕдиница = &Организация
	|
	|УПОРЯДОЧИТЬ ПО
	|	Контрагенты.ПометкаУдаления";

	Запрос.УстановитьПараметр("Организация", Организация);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Элемент = Справочники.Контрагенты.ПустаяСсылка();
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Элемент = ВыборкаДетальныеЗаписи.Элемент;	
	КонецЕсли;
	
	Возврат Элемент;
	
КонецФункции

Функция ТипОЕ(Параметры) Экспорт

	ТипОЕ = Новый Массив;
	Если Параметры.Свойство("РО") Тогда
		
		ТипОЕ.Добавить(0);
		ТипОЕ.Добавить(1);
		ТипОЕ.Добавить(2);
		
	ИначеЕсли Параметры.Свойство("ЦФО") Тогда
		ТипОЕ.Добавить(4);
	ИначеЕсли Параметры.Свойство("ЦФОРО") Тогда
		
		ТипОЕ.Добавить(0);
		ТипОЕ.Добавить(1);
		ТипОЕ.Добавить(2);
		ТипОЕ.Добавить(4);
		
	ИначеЕсли Параметры.Свойство("ЭО") Тогда
		ТипОЕ.Добавить(3);
	ИначеЕсли Параметры.Свойство("ТипОЕ") Тогда
		ТипОЕ = Параметры.ТипОЕ;
	КонецЕсли;
	
	Возврат ТипОЕ;
	
КонецФункции

Процедура ДобавитьПараметрыВыбораОрганизацийРО(Форма, СтрокаИменЭлементов)Экспорт       
	
	Элементы = Форма.Элементы;
	
	ИменаЭлементов = СтрРазделить(СтрокаИменЭлементов,",");
	
	Для Каждого ИмяЭлемента Из ИменаЭлементов Цикл   
		
		ИмяЭлемента = СокрЛП(ИмяЭлемента);
		Если Элементы.Найти(ИмяЭлемента) = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		МассивПараметров = Новый Массив;
		ТекПараметрыВыбора = Элементы[ИмяЭлемента].ПараметрыВыбора;
		
		Для Каждого КлючЗначение Из ТекПараметрыВыбора Цикл      
			МассивПараметров.Добавить(Новый ПараметрВыбора(КлючЗначение.Имя, КлючЗначение.Значение));
		КонецЦикла;
		
		МассивПараметров.Добавить(Новый ПараметрВыбора("РО", Истина));

		ПараметрыВыбора = Новый ФиксированныйМассив(МассивПараметров);
		Элементы[ИмяЭлемента].ПараметрыВыбора = ПараметрыВыбора;	
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОграничениеСпискаРеглОтчетность

Функция ЗаменитьРегламентированнымиЗапрос(ТекстЗапроса) Экспорт

	Возврат СтрЗаменить(
				ТекстЗапроса, 
				"Справочник.Организации", 
				СтрШаблон("(%1)", ТекстЗапроса_РегламентированныеОрганизации())
			);

КонецФункции

#КонецОбласти

#Область ФормыСпискаВыбора

Процедура ПриСозданииНаСервереУХ_Организации(Форма, Отказ, СтандартнаяОбработка) Экспорт

	Список = Форма.Список;
	
	ДоработатьРеквизитСписок(Список);
	ДоработатьЭлементыФормы(Форма);

	ВидРавно = ВидСравненияКомпоновкиДанных.Равно;
	
	// Оформление Элиминирующие организации
	ЭлементУО = Список.УсловноеОформление.Элементы.Добавить();
	ПолеЭлиминирующая = "Ссылка.ЭлиминирующаяОрганизация";
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ЭлементУО.Отбор, ПолеЭлиминирующая, ВидРавно, Истина);
	ЭлементУО.Оформление.УстановитьЗначениеПараметра("ЦветТекста", WebЦвета.Серый);

	// Неактивный отбор по Регламентированным	
	ТолькоРегл = "Ссылка.ИспользоватьВРегламентированномУчете";
	ЭлементыОтбора = ОбщегоНазначенияКлиентСервер.НайтиЭлементыИГруппыОтбора(Список.Отбор, ТолькоРегл);
	Если ЭлементыОтбора.Количество() = 0 Тогда
		
		Режим = РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
		ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(Список.Отбор, ТолькоРегл, ВидРавно, Истина, , Ложь, Режим);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ТекстЗапроса_РегламентированныеОрганизации()

	Возврат
	"ВЫБРАТЬ 
	|	* 
	|ИЗ 
	|	Справочник.Организации КАК СправочникОрганизации
	|ГДЕ 
	|	СправочникОрганизации.ИспользоватьВРегламентированномУчете";

КонецФункции

Процедура ДоработатьЭлементыФормы(Форма)
	
	ЕстьОтборы = Ложь;
	 	
	Если Форма.Параметры.Свойство("Отбор") И Форма.Параметры.Отбор.Количество() Тогда
		
		// Например, для налоговых резидентов(РФ, иностранные) отображаем списком, 
		// т.к. иерархия может скрывать резидентов, когда родитель не резидент 
		ЕстьОтборы = Истина;
			
	КонецЕсли;
	
	Если ЕстьОтборы Тогда		
		Форма.Элементы.Список.Отображение = ОтображениеТаблицы.Список;		
	КонецЕсли;
	
	ЭлементЮрФизЛицо = Форма.Элементы.Найти("ЮрФизЛицо");
	Если ЭлементЮрФизЛицо <> Неопределено Тогда
		ЭлементЮрФизЛицо.Видимость = Ложь;
	КонецЕсли;
	
	ФормыУХ.СоздатьПолеФормы(Форма.Элементы, "СписокТипОрганизации", , 
		"Список.ТипОрганизации", ВидПоляФормы.ПолеВвода, Форма.Элементы.Список);
	
КонецПроцедуры

Процедура ДоработатьРеквизитСписок(Список)
	
	Если Не Список.ПроизвольныйЗапрос Тогда
		
		Список.ПроизвольныйЗапрос = Истина;
		
		ТекстЗапроса = 
		"ВЫБРАТЬ 
		|	* 
		|ИЗ 
		|	%1 КАК СправочникОрганизации";
		Список.ТекстЗапроса = СтрШаблон(ТекстЗапроса, Список.ОсновнаяТаблица);
		
	КонецЕсли;
	
	ТекстЗамены = "ВЫБРАТЬ " + ТекстЗапроса_ПоляОрганизаций();
	Список.ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, "ВЫБРАТЬ", ТекстЗамены);

КонецПроцедуры

Функция ТекстЗапроса_ПоляОрганизаций(ИмяТаблицы = "СправочникОрганизации")

	ТекстЗапроса =	
	"
	|//#Область УХ_Организации
	|	СправочникОрганизации.ИспользоватьВРегламентированномУчете КАК ИспользоватьВРегламентированномУчете,
	|	СправочникОрганизации.ИностранныйНалоговыйРезидент КАК ИностранныйНалоговыйРезидент,
	|	СправочникОрганизации.ЭлиминирующаяОрганизация КАК ЭлиминирующаяОрганизация,
	|	СправочникОрганизации.ИспользоватьВРегламентированномУчете
	|		ИЛИ СправочникОрганизации.ИностранныйНалоговыйРезидент КАК ЯвляетсяНалоговымРезидентом,
	|	ВЫБОР
	|		КОГДА СправочникОрганизации.ИспользоватьВРегламентированномУчете
	|			ТОГДА 0
	|		КОГДА СправочникОрганизации.ИностранныйНалоговыйРезидент И СправочникОрганизации.ИностраннаяСтруктураБезОбразованияЮрЛица
	|			ТОГДА 2
	|		КОГДА СправочникОрганизации.ИностранныйНалоговыйРезидент
	|			ТОГДА 1
	|		КОГДА СправочникОрганизации.ЭлиминирующаяОрганизация
	|			ТОГДА 3
	|		ИНАЧЕ 4
	|	КОНЕЦ КАК ТипОЕ,	
	|	ВЫБОР
	|		КОГДА СправочникОрганизации.ИспользоватьВРегламентированномУчете
	|			ТОГДА ""%1""
	|		КОГДА СправочникОрганизации.ИностранныйНалоговыйРезидент
	|			ТОГДА ""%2""
	|		КОГДА СправочникОрганизации.ЭлиминирующаяОрганизация
	|			ТОГДА ""%3""
	|		ИНАЧЕ ""%4""
	|	КОНЕЦ КАК ТипОрганизации,	
	|	//#КонецОбласти
	|";
	
	Если ИмяТаблицы <> "СправочникОрганизации" Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "СправочникОрганизации", ИмяТаблицы);
	КонецЕсли;
	
	Возврат СтрШаблон(
				ТекстЗапроса, 
				НСтр("ru = 'Налоговый резидент РФ'"),
				НСтр("ru = 'Иностранный резидент'"),
				НСтр("ru = 'Элиминация'"),
				НСтр("ru = 'ЦФО'")				
			);
			
КонецФункции

Процедура ОрганизацииОбработкаПолученияДанныхВыбораУХ(Источник, ДанныеВыбора, Параметры, СтандартнаяОбработка) Экспорт

	ТипОЕ = ТипОЕ(Параметры);
	Если ТипОЕ.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;  
	
	СтандартнаяОбработка = Ложь;
	
	Если ДанныеВыбора = Неопределено Тогда
		ДанныеВыбора = Новый СписокЗначений;
	КонецЕсли;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 5
	|	т.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.Организации КАК т
	|ГДЕ
	|	ВЫБОР
	|		КОГДА т.ИспользоватьВРегламентированномУчете
	|			ТОГДА 0
	|		КОГДА т.ИностранныйНалоговыйРезидент И т.ИностраннаяСтруктураБезОбразованияЮрЛица
	|			ТОГДА 2
	|		КОГДА т.ИностранныйНалоговыйРезидент
	|			ТОГДА 1
	|		КОГДА т.ЭлиминирующаяОрганизация
	|			ТОГДА 3
	|		ИНАЧЕ 4
	|	КОНЕЦ В (&ТипОЕ)");
	Запрос.УстановитьПараметр("ТипОЕ", ТипОЕ);

	Если Параметры.Свойство("СтрокаПоиска") И ЗначениеЗаполнено(Параметры.СтрокаПоиска) Тогда
		Запрос.Текст = Запрос.Текст + " И (т.Наименование ПОДОБНО &СтрокаПоиска ИЛИ т.Код ПОДОБНО &СтрокаПоиска)";
		Запрос.УстановитьПараметр("СтрокаПоиска", Параметры.СтрокаПоиска + "%");
	КонецЕсли;
	
	Для Каждого ЭлементОтбора Из Параметры.Отбор Цикл
		
		ИмяПараметра = СтрЗаменить(ЭлементОтбора.Ключ, ".", "_");
		Запрос.УстановитьПараметр(ИмяПараметра, ЭлементОтбора.Значение);
	   	Запрос.Текст = Запрос.Текст + " И т." + ЭлементОтбора.Ключ + " В (&" + ИмяПараметра + ")";
			
	КонецЦикла;	
	
	Запрос.Текст = Запрос.Текст + " УПОРЯДОЧИТЬ ПО т.Наименование";
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Если ДанныеВыбора.НайтиПоЗначению(Выборка.Ссылка) = Неопределено Тогда
			ДанныеВыбора.Добавить(Выборка.Ссылка);
		КонецЕсли;
	КонецЦикла; 
	
КонецПроцедуры

#КонецОбласти
