#Область ПрограммныйИнтерфейс
Функция РасшифровкаПлатежаВТабличнойЧасти(ВидОперацииУХ) Экспорт	
	Если Не ЗначениеЗаполнено(ВидОперацииУХ) Тогда
		Возврат Ложь;
	КонецЕсли;
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОперацииУХ, "РасшифровкаПлатежаВТабличнойЧасти");
КонецФункции  

Функция ЭтоОплатаДенежныхСредствВДругуюОрганизацию(ВидОперацииУХ) Экспорт	
	
	Если Не ЗначениеЗаполнено(ВидОперацииУХ) Тогда
		Возврат Ложь;
	КонецЕсли; 
	
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОперацииУХ, "ВидОперацииДДСБезналичныйРасчет")
						= ОбщегоНазначения.ПредопределенныйЭлемент("Перечисление.ХозяйственныеОперации.ОплатаДенежныхСредствВДругуюОрганизацию");
                                    
КонецФункции  

Функция ЭтоВозвратДенежныхСредствВДругуюОрганизацию(ВидОперацииУХ) Экспорт	
	
	Если Не ЗначениеЗаполнено(ВидОперацииУХ) Тогда
		Возврат Ложь;
	КонецЕсли; 
	
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОперацииУХ, "ВидОперацииДДСБезналичныйРасчет")
						= ОбщегоНазначения.ПредопределенныйЭлемент("Перечисление.ХозяйственныеОперации.ВозвратДенежныхСредствВДругуюОрганизацию");
								
	
КонецФункции 

Функция ВидДоговора(ВидОперацииУХ) Экспорт


	Если Не ЗначениеЗаполнено(ВидОперацииУХ) Тогда
		Возврат "";
	КонецЕсли;     
	
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОперацииУХ, "ВидДоговора");


КонецФункции // ()

					


#КонецОбласти


