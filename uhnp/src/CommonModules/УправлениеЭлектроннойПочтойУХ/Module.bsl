
////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ РАБОТЫ С ЭЛЕКТРОННЫМИ ПИСЬМАМИ

// Создает профиль переданной учетной записи для подключения к почтовому серверу.
//
// Параметры:
//  УчетнаяЗапись - СправочникСсылка.УчетныеЗаписиЭлектроннойПочты - учетная запись.
//
// Возвращаемое значение:
//  ИнтернетПочтовыйПрофиль - профиль учетной записи;
//  Неопределено - не удалось получить учетную запись по ссылке.
//
Функция ИнтернетПочтовыйПрофиль(УчетнаяЗапись, ДляПолучения)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	УчетныеЗаписиЭлектроннойПочты.СерверВходящейПочты КАК АдресСервераIMAP,
	|	УчетныеЗаписиЭлектроннойПочты.ПортСервераВходящейПочты КАК ПортIMAP,
	|	УчетныеЗаписиЭлектроннойПочты.ИспользоватьЗащищенноеСоединениеДляВходящейПочты КАК ИспользоватьSSLIMAP,
	|	УчетныеЗаписиЭлектроннойПочты.Пользователь КАК ПользовательIMAP,
	|	УчетныеЗаписиЭлектроннойПочты.СерверВходящейПочты КАК АдресСервераPOP3,
	|	УчетныеЗаписиЭлектроннойПочты.ПортСервераВходящейПочты КАК ПортPOP3,
	|	УчетныеЗаписиЭлектроннойПочты.ИспользоватьЗащищенноеСоединениеДляВходящейПочты КАК ИспользоватьSSLPOP3,
	|	УчетныеЗаписиЭлектроннойПочты.Пользователь КАК Пользователь,
	|	УчетныеЗаписиЭлектроннойПочты.СерверИсходящейПочты КАК АдресСервераSMTP,
	|	УчетныеЗаписиЭлектроннойПочты.ПортСервераИсходящейПочты КАК ПортSMTP,
	|	УчетныеЗаписиЭлектроннойПочты.ИспользоватьЗащищенноеСоединениеДляИсходящейПочты КАК ИспользоватьSSLSMTP,
	|	УчетныеЗаписиЭлектроннойПочты.ТребуетсяВходНаСерверПередОтправкой КАК POP3ПередSMTP,
	|	УчетныеЗаписиЭлектроннойПочты.ПользовательSMTP КАК ПользовательSMTP,
	|	УчетныеЗаписиЭлектроннойПочты.ВремяОжидания КАК Таймаут,
	|	УчетныеЗаписиЭлектроннойПочты.ПротоколВходящейПочты КАК Протокол
	|ИЗ
	|	Справочник.УчетныеЗаписиЭлектроннойПочты КАК УчетныеЗаписиЭлектроннойПочты
	|ГДЕ
	|	УчетныеЗаписиЭлектроннойПочты.Ссылка = &Ссылка";
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Ссылка", УчетнаяЗапись);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Неопределено;
	Если Выборка.Следующий() Тогда
		СписокСвойствIMAP = "АдресСервераIMAP,ПортIMAP,ИспользоватьSSLIMAP,ПользовательIMAP";
		СписокСвойствPOP3 = "АдресСервераPOP3,ПортPOP3,ИспользоватьSSLPOP3,Пользователь";
		СписокСвойствSMTP = "АдресСервераSMTP,ПортSMTP,ИспользоватьSSLSMTP,ПользовательSMTP";
		
		Результат = Новый ИнтернетПочтовыйПрофиль;
		Если ДляПолучения Тогда
			Если Выборка.Протокол = "IMAP" Тогда
				ТребуемыеСвойства = СписокСвойствIMAP;
				УстановитьПривилегированныйРежим(Истина);
				Результат.ПарольIMAP = ОбщегоНазначения.ПрочитатьДанныеИзБезопасногоХранилища(УчетнаяЗапись);
				УстановитьПривилегированныйРежим(Ложь);
			Иначе
				ТребуемыеСвойства = СписокСвойствPOP3;
				УстановитьПривилегированныйРежим(Истина);
				Результат.Пароль = ОбщегоНазначения.ПрочитатьДанныеИзБезопасногоХранилища(УчетнаяЗапись);
				УстановитьПривилегированныйРежим(Ложь);
			КонецЕсли;
		Иначе
			ТребуемыеСвойства = СписокСвойствSMTP;
			Если Выборка.Протокол <> "IMAP" И Выборка.POP3ПередSMTP Тогда
				ТребуемыеСвойства = ТребуемыеСвойства + ",POP3ПередSMTP," + СписокСвойствPOP3;
			КонецЕсли;
			УстановитьПривилегированныйРежим(Истина);
			Результат.ПарольSMTP = ОбщегоНазначения.ПрочитатьДанныеИзБезопасногоХранилища(УчетнаяЗапись, "ПарольSMTP");
			УстановитьПривилегированныйРежим(Ложь);
		КонецЕсли;
		ТребуемыеСвойства = ТребуемыеСвойства + ",Таймаут";
		ЗаполнитьЗначенияСвойств(Результат, Выборка, ТребуемыеСвойства);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Формирует почтовый профиль по учетной записи
//
// Параметры
//  УчетнаяЗапись
//
// Возвращаемое значение:
//   ИнтернетПочтовыйПрофиль
//
Функция ПолучитьИнтернетПочтовыйПрофиль(УчетнаяЗапись) Экспорт

	УстановитьПривилегированныйРежим(Истина);
	Пароли = ОбщегоНазначения.ПрочитатьДанныеИзБезопасногоХранилища(УчетнаяЗапись, "Пароль, ПарольSMTP");
	УстановитьПривилегированныйРежим(Ложь);
	Пароль = ?(ЗначениеЗаполнено(Пароли.Пароль), Пароли.Пароль, "");
	ПарольSMTP = ?(ЗначениеЗаполнено(Пароли.ПарольSMTP), Пароли.ПарольSMTP, "");
	
	ЭтоИМАП = (УчетнаяЗапись.ПротоколВходящейПочты = "IMAP");
	
	Профиль = Новый ИнтернетПочтовыйПрофиль;
	
	ВремяОжидания_ = УчетнаяЗапись.ВремяОжидания;
	Если ВремяОжидания_ > 0 Тогда
		Профиль.ВремяОжидания = ВремяОжидания_;
	КонецЕсли; 
	Профиль.Пароль = Пароль;
	Профиль.Пользователь = УчетнаяЗапись.Пользователь;
	
	Если ЭтоИМАП Тогда
		Профиль.АдресСервераIMAP = УчетнаяЗапись.СерверВходящейПочты;
		Профиль.ПортIMAP = УчетнаяЗапись.ПортСервераВходящейПочты;
		Профиль.ИспользоватьSSLIMAP = УчетнаяЗапись.ИспользоватьЗащищенноеСоединениеДляВходящейПочты;
		Профиль.ПарольIMAP = Пароль;
		Профиль.ПользовательIMAP = УчетнаяЗапись.Пользователь;
		
	Иначе
		Профиль.АдресСервераPOP3 = УчетнаяЗапись.СерверВходящейПочты;
		Профиль.ПортPOP3 = УчетнаяЗапись.ПортСервераВходящейПочты;
		Профиль.ИспользоватьSSLPOP3 = УчетнаяЗапись.ИспользоватьЗащищенноеСоединениеДляВходящейПочты;
		
	КонецЕсли;
	
	Профиль.АдресСервераSMTP = УчетнаяЗапись.СерверИсходящейПочты;
	Профиль.ПортSMTP = УчетнаяЗапись.ПортСервераИсходящейПочты;
	Профиль.ИспользоватьSSLSMTP = УчетнаяЗапись.ИспользоватьЗащищенноеСоединениеДляИсходящейПочты;
	Профиль.ПарольSMTP = ПарольSMTP;
	Профиль.ПользовательSMTP = УчетнаяЗапись.ПользовательSMTP;
		
	Если УчетнаяЗапись.ТребуетсяВходНаСерверПередОтправкой Тогда
		Профиль.POP3ПередSMTP = Истина;
		Профиль.АутентификацияSMTP = СпособSMTPАутентификации.ПоУмолчанию;
	//Иначе
	//	Профиль.АутентификацияSMTP = СпособSMTPАутентификации.БезАутентификации;
	КонецЕсли; 
	
	Возврат Профиль;

КонецФункции

// Устанавливает соединение с сервером электронной почты.
//
// Параметры:
//  УчетнаяЗапись - СправочникСсылка.УчетныеЗаписиЭлектроннойПочты - учетная запись для соединения.
//  ДляПолучения - Булево - Если Истина, то соединение может быть использовано для загрузки почтовых сообщений.
//  Протокол - ПротоколИнтернетПочты - Протокол используемый для загрузки почтовых сообщений. По умолчанию - POP3.
//
// Возвращаемое значение:
//  ИнтернетПочта - если с сервером удалось установить соединение.
//  Неопределено - если при установки соединения возникло исключение.
//
Функция УстановитьСоединениеССервером(УчетнаяЗапись, ДляПолучения = Ложь, Протокол = Неопределено) Экспорт
	
	Соединение = Новый ИнтернетПочта;
	ИнтернетПочтовыйПрофиль = ИнтернетПочтовыйПрофиль(УчетнаяЗапись, ДляПолучения);
	
	Если Протокол = Неопределено Тогда
		Протокол = ПротоколИнтернетПочты.POP3;
	КонецЕсли;
	
	Попытка
		Соединение.Подключиться(ИнтернетПочтовыйПрофиль, Протокол);
		Возврат Соединение;
		
	Исключение
		КодОсновногоЯзыка  = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Установка соединения с сервером электронной почты'", КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(ИмяСобытия, УровеньЖурналаРегистрации.Ошибка, , , 
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		Возврат Неопределено;
		
	КонецПопытки;
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ РАБОТЫ С ТЕКСТАМИ ЭЛЕКТРОННЫХ ПИСЕМ И ХТМЛ ТЕКСТАМИ

// Функция ищет вхождение в переданную строку значений из списка значений
//
// Параметры
//  Строка - исходная строка для поиска
//  ПодстрокиПоиска - список значений с коллекцией подстрок для поиска
//  СтрокаПоиска - элемент, в который возвращается найденное значение строки подпоиска
//
// Возвращаемое значение:
//  Наименьшая позиция найденного значения
//
Функция ПоискПервойПодстроки(Строка,ПодстрокиПоиска,СтрокаПоиска)
	
	Результат = 0;
	
	Для каждого Подстрока из ПодстрокиПоиска Цикл
		Нашли = СтрНайти(Строка,Подстрока.Значение);
		Если Нашли > 0 Тогда
			Если Результат=0 ИЛИ Нашли < Результат Тогда
				Результат = Нашли;
				СтрокаПоиска = Подстрока.Значение;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Функция возвращает по переданному тексту строку
// в формате HTML с выделенными ссылками на ресурсы www
//
// Параметры
//  ТекстПисьма - текстовая строка
//
// Возвращаемое значение:
//  Текст в формате HTML
//
Функция ВернутьТекстПисьмаВФорматеHTML(ТекстПисьма) Экспорт
	Перем СтрокаПоиска;
	
	Текст = ТекстПисьма;
	ТекстПоиска = Текст;
	
	СмещениеВТексте = 0;
	
	ПодстрокиПоиска = Новый СписокЗначений;
	ПодстрокиПоиска.Добавить("http://");
	ПодстрокиПоиска.Добавить("www.");
	ПодстрокиПоиска.Добавить("mailto:");

	ПозицияПризнакаСсылки = ПоискПервойПодстроки(ТекстПоиска,ПодстрокиПоиска,СтрокаПоиска);

	// пробежимся по всем найденным ссылкам
	Пока ПозицияПризнакаСсылки>0 Цикл
		
		НачалоСсылки = ПозицияПризнакаСсылки;
		
		// найдем последний символ ссылки
		КонецСсылки = НачалоСсылки+СтрДлина(СтрокаПоиска)-1;
		Для а=КонецСсылки+1 по СтрДлина(ТекстПоиска) Цикл
			ТекСимвол = Сред(ТекстПоиска,а,1);
			Если КодСимвола(ТекСимвол) < 33 ИЛИ КодСимвола(ТекСимвол)>127 ИЛИ КодСимвола(ТекСимвол)=91 ИЛИ КодСимвола(ТекСимвол)=93 Тогда
				Прервать;
			КонецЕсли;
			КонецСсылки = а;
		КонецЦикла;
		
		Если КонецСсылки > НачалоСсылки+СтрДлина(СтрокаПоиска)-1 Тогда
			
			ТекстСсылки   = Сред(ТекстПоиска,НачалоСсылки,КонецСсылки-НачалоСсылки+1);
			ТекстСсылкиHTML = "<a href="""+?(СтрокаПоиска="www.","http://","")+ТекстСсылки+""">"+ТекстСсылки+"</a>";
			
			ТекстДоСсылки = Лев(Текст,НачалоСсылки+СмещениеВТексте-1);
			ТекстПослеСсылки = Прав(Текст,СтрДлина(Текст)-(КонецСсылки+СмещениеВТексте));
			
			Текст = ТекстДоСсылки + ТекстСсылкиHTML + ТекстПослеСсылки;
			
			СмещениеВТексте = СмещениеВТексте + КонецСсылки + (СтрДлина(ТекстСсылкиHTML)-СтрДлина(ТекстСсылки));

		Иначе
			
			СмещениеВТексте = СмещениеВТексте + КонецСсылки;
			
		КонецЕсли;
		
		ТекстПоиска = Прав(ТекстПоиска,СтрДлина(ТекстПоиска)-КонецСсылки);
		
		ПозицияПризнакаСсылки = ПоискПервойПодстроки(ТекстПоиска,ПодстрокиПоиска,СтрокаПоиска);
		
	КонецЦикла;
	
	Текст = СтрЗаменить(Текст, Символы.ПС, "<BR>");
	
	ТекстВформатеHTML = "<HTML>        
	|<HEAD>
	|<META http-equiv=Content-Type content=""text/html; charset=utf-8"">
	|<META content=""MSHTML 6.00.2800.1400"" name=GENERATOR>
	|<STYLE>
	|body
	|{
	|	font-size:15px;
	|	font-family:Arial,Helvetica,Sans-Serif;
	|}
	|a
	|{	
	|	font-size:15px;
	|	font-family:Arial,Helvetica,Sans-Serif;
	|}
	|</STYLE>
	|</HEAD>
	|<BODY scroll=""auto"">" + Текст + "</BODY>
	|</HTML>";
	Возврат ТекстВформатеHTML;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// УПРАВЛЕНИЕ УВЕДОМЛЕНИЯМИ ПО ЭЛЕКТРОННОЙ ПОЧТЕ.
//

Функция ВернутьТекстПисьма(Заголовок, Содержание) Экспорт
	
	ТекстПисьма = 
	"<HTML>
	|<BODY>
	|<H3>" + Заголовок + "</H3> 
	|<HR>
	|<BR>" + Содержание + "
	|</BODY>
	|</HTML>";
	
	Возврат ТекстПисьма;
	
КонецФункции

Процедура ЗаписатьПисьмоВРегистр(Письмо, Источник) Экспорт
	НовыйИдентификатор = Новый УникальныйИдентификатор;
	МенеджерЗаписей = РегистрыСведений.УведомленияПоЭлектроннойПочте.СоздатьМенеджерЗаписи();
	МенеджерЗаписей.Активность = Истина;
	МенеджерЗаписей.ДатаСоздания		 = ТекущаяДатаСеанса();
	МенеджерЗаписей.Источник			 = Источник;
	МенеджерЗаписей.Письмо				 = Новый ХранилищеЗначения(Письмо);
	МенеджерЗаписей.ИдентификаторПисьма	 = Строка(НовыйИдентификатор);
	МенеджерЗаписей.Записать(Истина);
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// УПРАВЛЕНИЕ УВЕДОМЛЕНИЯМИ ПО ЭЛЕКТРОННОЙ ПОЧТЕ.
//

Процедура РазослатьПротоколыПоОтветственным(СоответствиеПротоколов) Экспорт
	
	МассивДокументов	= Новый Массив;
	МассивОрганизаций	= Новый Массив;
	
	Для Каждого ОбъектПротокол ИЗ СоответствиеПротоколов Цикл
		
		Если ТипЗнч(ОбъектПротокол.Ключ)=Тип("ДокументСсылка.НастраиваемыйОтчет")
			ИЛИ ТипЗнч(ОбъектПротокол.Ключ)=Тип("ДокументСсылка.Трансляция")
			ИЛИ ТипЗнч(ОбъектПротокол.Ключ)=Тип("ДокументСсылка.ТрансформационнаяКорректировка") Тогда
			
			МассивДокументов.Добавить(ОбъектПротокол.Ключ);
			
		ИначеЕсли ТипЗнч(ОбъектПротокол.Ключ)=Тип("СправочникСсылка.Организации") Тогда
			
			МассивОрганизаций.Добавить(ОбъектПротокол.Ключ);
			
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблицаОтветственных = Новый ТаблицаЗначений;
	
	ТаблицаОтветственных.Колонки.Добавить("Пользователь",				);
	ТаблицаОтветственных.Колонки.Добавить("ДокументыПользователя",		ОбщегоНазначенияУХ.ПолучитьОписаниеТиповСписокЗначений());
	ТаблицаОтветственных.Колонки.Добавить("ОрганизацииПользователя",	ОбщегоНазначенияУХ.ПолучитьОписаниеТиповСписокЗначений());
	
	Для Каждого Документ ИЗ МассивДокументов Цикл
				
		ФизЛицоОтветственныйОрганизация = Документ.Организация.ОсновноеКонтактноеЛицо;
		
		ДобавитьДокументОтветственногоЛица(Документ, ФизЛицоОтветственныйОрганизация, ТаблицаОтветственных);
		
		Ответ = ПолучитьТекущегоИсполнителяИПроверяющего(Документ);
		
		Если Ответ.Количество() > 0 Тогда
			
			ДобавитьДокументОтветственногоЛица(Документ, Ответ[0].Исполняющий, ТаблицаОтветственных);
			ДобавитьДокументОтветственногоЛица(Документ, Ответ[0].Согласование, ТаблицаОтветственных);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Для Каждого Организация ИЗ МассивОрганизаций Цикл		
		ДобавитьОрганизациюОтветственногоЛица(Организация, Документ.Организация.ОсновноеКонтактноеЛицо, ТаблицаОтветственных);					
	КонецЦикла;
	
	КэшЭлАдресов = ПолучитьКэшЭлАдресов(ТаблицаОтветственных);
	
	Для Каждого СтрОтветственный ИЗ ТаблицаОтветственных Цикл
		
		Если НЕ ЗначениеЗаполнено(СтрОтветственный.Пользователь) Тогда
			Продолжить;
		КонецЕсли;
		
		ТекущийЭлАдрес = КэшЭлАдресов.Получить(СтрОтветственный.Пользователь);
		Если Не ЗначениеЗаполнено(ТекущийЭлАдрес) Тогда
			Продолжить;
		КонецЕсли;		
		
		// Формируем письмо пользователю по протоколам загрузки и записи объектов
		
		Если СтрОтветственный.ДокументыПользователя.Количество()>0 Тогда
			
			ПротоколОтветственного=Новый ТабличныйДокумент;
			ДокументовСОшибками=0;
			ДокументовОбработано=0;
			МассивОрганизаций=Новый Массив;
			
			Для Каждого СтрДокумент ИЗ СтрОтветственный.ДокументыПользователя Цикл
				
				ПротоколОбъекта=СоответствиеПротоколов[СтрДокумент.Значение];
				
				Если ТипЗнч(ПротоколОбъекта)=Тип("ТабличныйДокумент") Тогда
					
					ПротоколОтветственного.Вывести(ПротоколОбъекта);
					
				КонецЕсли;
				
				Если ПротоколируемыеСобытияУХ.ЕстьПротоколыПоОбъекту(СтрДокумент.Значение) Тогда
					
					ДокументовСОшибками=ДокументовСОшибками+1;
					
				КонецЕсли;
				
				ДокументовОбработано=ДокументовОбработано+1;
				
				Если МассивОрганизаций.Найти(СтрДокумент.Значение.Организация)=Неопределено Тогда
					МассивОрганизаций.Добавить(СтрДокумент.Значение.Организация);
				КонецЕсли;
				
			КонецЦикла;
			
			Расширение 	= ".htm";
			ТипФайла 	= ТипФайлаТабличногоДокумента.HTML;
			
			ИмяВременногоФайла = ПолучитьИмяВременногоФайла(Расширение);
			ПротоколОтветственного.Записать(ИмяВременногоФайла,ТипФайла);
			
			ДанныеДокумента 	= Новый ДвоичныеДанные(ИмяВременногоФайла);	
			
			Попытка
				УдалитьФайлы(ИмяВременногоФайла);
			Исключение
			КонецПопытки;
			
			Письмо = Новый ИнтернетПочтовоеСообщение;
			
			Письмо.Получатели.Добавить(ТекущийЭлАдрес);
			
			Если МассивОрганизаций.Количество()=1 Тогда
				
				Письмо.Тема=СтрШаблон(Нстр("ru = 'По организации %1 обработано %2 документов, признано ошибочными %3'"), 
					МассивОрганизаций[0], ДокументовОбработано, ДокументовСОшибками);
				
				ТекстЗаголовка=Письмо.Тема;
				ТекстСодержания=Нстр("ru = '.</LI></UL>
				|<P>Подробности во вложенном файле ""Протокол обработки""</P>'");
				
			Иначе
				
				Письмо.Тема=СтрШаблон(Нстр("ru = 'Обработано %1 документов по %2, признано ошибочными %3'"), ДокументовОбработано, 
					МассивОрганизаций.Количество(), ДокументовСОшибками);
				
				ТекстЗаголовка=Письмо.Тема;
				ТекстСодержания=Нстр("ru = 'Организации:<BR>
				|<UL>'");
				
				Для Каждого Организация ИЗ МассивОрганизаций Цикл
					ТекстСодержания=ТекстСодержания+"
					|  <LI>" + Организация;
				КонецЦикла;
				
				ТекстСодержания=СтрШаблон(Нстр("ru = '%1.</LI></UL>
				|<P>Подробности во вложенном файле ""Протокол обработки""</P>'"), ТекстСодержания);
				
			КонецЕсли;
			
			Письмо.Тексты.Добавить(ВернутьТекстПисьма(ТекстЗаголовка,ТекстСодержания),ТипТекстаПочтовогоСообщения.HTML);
			
			Письмо.Вложения.Добавить(ДанныеДокумента,СтрШаблон(Нстр("ru = 'Протокол обработки%1'"), Расширение));
			
			ЗаписатьПисьмоВРегистр(Письмо,СтрОтветственный.Пользователь);
			
		КонецЕсли;
		
		Если СтрОтветственный.ОрганизацииПользователя.Количество()>0 Тогда
			
			ПротоколОтветственного=Новый ТабличныйДокумент;
			
			Для Каждого СтрОрганизация ИЗ СтрОтветственный.ОрганизацииПользователя Цикл
				
				ПротоколОбъекта=СоответствиеПротоколов[СтрОрганизация.Значение];
				
				Если ТипЗнч(ПротоколОбъекта)=Тип("ТабличныйДокумент") Тогда
					
					ПротоколОтветственного.Вывести(ПротоколОбъекта);
					
				КонецЕсли;
								
			КонецЦикла;
			
			Расширение 	= ".htm";
			ТипФайла 	= ТипФайлаТабличногоДокумента.HTML;
			
			ИмяВременногоФайла = ПолучитьИмяВременногоФайла(Расширение);
			ПротоколОтветственного.Записать(ИмяВременногоФайла,ТипФайла);
			
			ДанныеДокумента 	= Новый ДвоичныеДанные(ИмяВременногоФайла);	
			
			Попытка
				УдалитьФайлы(ИмяВременногоФайла);
			Исключение
			КонецПопытки;
			
			Письмо = Новый ИнтернетПочтовоеСообщение;
			
			Письмо.Получатели.Добавить(ТекущийЭлАдрес);
			
			Если СтрОтветственный.ОрганизацииПользователя.Количество()=1 Тогда
				
				Письмо.Тема=СтрШаблон(Нстр("ru = 'При сверке внутригрупповых отношений по организации %1 обнаружены расхождения.'"), 
					СтрОтветственный.ОрганизацииПользователя[0].Значение); ;
				
				ТекстЗаголовка=Письмо.Тема;
				ТекстСодержания=Нстр("ru = '.</LI></UL>
				|<P>Подробности во вложенном файле ""Протокол сверки ВГО""</P>'");
				
			Иначе
				
				Письмо.Тема=СтрШаблон(Нстр("ru = 'При сверке внутригрупповых отношений обнаружены расхождения по %1 организациям.'"), 
					МассивОрганизаций.Количество());
				
				ТекстЗаголовка=Письмо.Тема;
				ТекстСодержания=Нстр("ru = 'Организации:<BR>
				|<UL>'");
				
				Для Каждого СтрОрганизация ИЗ СтрОтветственный.ОрганизацииПользователя Цикл
					ТекстСодержания=ТекстСодержания+"
					|  <LI>" + СтрОрганизация.Значение;
				КонецЦикла;
				
				ТекстСодержания=СтрШаблон(Нстр("ru = '%1.</LI></UL>
				|<P>Подробности во вложенном файле ""Протокол сверки ВГО""</P>'"), ТекстСодержания);
				
			КонецЕсли;
			
			Письмо.Тексты.Добавить(ВернутьТекстПисьма(ТекстЗаголовка,ТекстСодержания),ТипТекстаПочтовогоСообщения.HTML);
			
			Письмо.Вложения.Добавить(ДанныеДокумента,СтрШаблон(Нстр("ru = 'Протокол сверки ВГО%1'"), Расширение));
			
			ЗаписатьПисьмоВРегистр(Письмо,СтрОтветственный.Пользователь);
				
		КонецЕсли;
		
	КонецЦикла;
		
КонецПроцедуры

Функция ПолучитьТекущегоИсполнителяИПроверяющего(ЭкземплярОтчета) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	Запрос.УстановитьПараметр("ЭкземплярОтчета", ЭкземплярОтчета);
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастраиваемыйОтчет.ВидОтчета КАК ВидДокументаБД,
	|	НастраиваемыйОтчет.Организация КАК Организация,
	|	УправлениеПериодомСценария.ВерсияОрганизационнойСтруктуры КАК ВерсияОрганизационнойСтруктуры,
	|	НастраиваемыйОтчет.Ссылка
	|ПОМЕСТИТЬ НастраиваемыйОтчет
	|ИЗ
	|	Документ.НастраиваемыйОтчет КАК НастраиваемыйОтчет
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.УправлениеПериодомСценария КАК УправлениеПериодомСценария
	|		ПО НастраиваемыйОтчет.ПериодОтчета = УправлениеПериодомСценария.ПериодСценария
	|			И НастраиваемыйОтчет.Сценарий = УправлениеПериодомСценария.Сценарий";
	
	Если ТипЗнч(ЭкземплярОтчета) = Тип("Массив") Тогда
		ТекстЗапроса = ТекстЗапроса + "
		|ГДЕ НастраиваемыйОтчет.Ссылка В (&ЭкземплярОтчета)";
	Иначе
		ТекстЗапроса = ТекстЗапроса + "
		|ГДЕ НастраиваемыйОтчет.Ссылка = &ЭкземплярОтчета";
	КонецЕсли;
	
	ТекстЗапроса = ТекстЗапроса + "
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ВидДокументаБД,
	|	Организация,
	|	ВерсияОрганизационнойСтруктуры
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастраиваемыйОтчет.Ссылка КАК ЭкземплярОтчета,
	|	РегистрМатрицыПолномочий.Исполняющий,
	|	РегистрМатрицыПолномочий.Исполняющий_Организация,
	|	РегистрМатрицыПолномочий.Согласование,
	|	РегистрМатрицыПолномочий.Согласование_Организация
	|ПОМЕСТИТЬ ТаблицаИспСогл
	|ИЗ
	|	НастраиваемыйОтчет КАК НастраиваемыйОтчет
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.РегистрМатрицыПолномочий КАК РегистрМатрицыПолномочий
	|		ПО НастраиваемыйОтчет.ВидДокументаБД = РегистрМатрицыПолномочий.ШаблонДокументаБД
	|			И НастраиваемыйОтчет.Организация = РегистрМатрицыПолномочий.Организация
	|			И НастраиваемыйОтчет.ВерсияОрганизационнойСтруктуры = РегистрМатрицыПолномочий.ВерсияРегламентаПодготовкиОтчетности
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Исполнитель.Пользователь КАК Исполняющий,
	|	Согласование.Пользователь КАК Согласование,
	|	Исполнитель.ЭкземплярОтчета КАК ЭкземплярОтчета,
	|	ПРЕДСТАВЛЕНИЕ(Исполнитель.Пользователь) КАК ИсполнительПредставление,
	|	ПРЕДСТАВЛЕНИЕ(Согласование.Пользователь) КАК СогласованиеПредставление
	|ИЗ
	|	(ВЫБРАТЬ
	|		ТаблицаИспСогл.Исполняющий КАК Пользователь,
	|		ТаблицаИспСогл.ЭкземплярОтчета КАК ЭкземплярОтчета
	|	ИЗ
	|		ТаблицаИспСогл КАК ТаблицаИспСогл
	|	ГДЕ
	|		ТаблицаИспСогл.Исполняющий ССЫЛКА Справочник.Пользователи
	|	
	|	ОБЪЕДИНИТЬ
	|	
	|	ВЫБРАТЬ
	|		ОтветственныеОрганизаций.Пользователь,
	|		ТаблицаИспСогл.ЭкземплярОтчета
	|	ИЗ
	|		ТаблицаИспСогл КАК ТаблицаИспСогл
	|			ПОЛНОЕ СОЕДИНЕНИЕ РегистрСведений.ОтветственныеОрганизаций КАК ОтветственныеОрганизаций
	|			ПО (ОтветственныеОрганизаций.Организация = ВЫБОР
	|					КОГДА ТаблицаИспСогл.Исполняющий_Организация = ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|						ТОГДА ТаблицаИспСогл.ЭкземплярОтчета.Организация
	|					ИНАЧЕ ТаблицаИспСогл.Исполняющий_Организация
	|				КОНЕЦ)
	|				И (ОтветственныеОрганизаций.Роль = ТаблицаИспСогл.Исполняющий)
	|	ГДЕ
	|		ТаблицаИспСогл.Исполняющий ССЫЛКА Справочник.РолиКонтактныхЛиц) КАК Исполнитель
	|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
	|			ТаблицаИспСогл.Согласование КАК Пользователь,
	|			ТаблицаИспСогл.ЭкземплярОтчета КАК ЭкземплярОтчета
	|		ИЗ
	|			ТаблицаИспСогл КАК ТаблицаИспСогл
	|		ГДЕ
	|			НЕ ТаблицаИспСогл.Согласование ССЫЛКА Справочник.РолиКонтактныхЛиц
	|		
	|		ОБЪЕДИНИТЬ
	|		
	|		ВЫБРАТЬ
	|			ОтветственныеОрганизаций.Пользователь,
	|			ТаблицаИспСогл.ЭкземплярОтчета
	|		ИЗ
	|			ТаблицаИспСогл КАК ТаблицаИспСогл
	|				ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ОтветственныеОрганизаций КАК ОтветственныеОрганизаций
	|				ПО (ОтветственныеОрганизаций.Организация = ВЫБОР
	|						КОГДА ТаблицаИспСогл.Согласование_Организация = ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|							ТОГДА ТаблицаИспСогл.ЭкземплярОтчета.Организация
	|						ИНАЧЕ ТаблицаИспСогл.Согласование_Организация
	|					КОНЕЦ)
	|					И (ОтветственныеОрганизаций.Роль = ТаблицаИспСогл.Согласование)
	|		ГДЕ
	|			ТаблицаИспСогл.Согласование ССЫЛКА Справочник.РолиКонтактныхЛиц) КАК Согласование
	|		ПО Исполнитель.ЭкземплярОтчета = Согласование.ЭкземплярОтчета";
	
	Запрос.Текст = ТекстЗапроса;
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция ПолучитьКэшЭлАдресов(ТаблицаОтветственных)
	
	Результат = Новый Соответствие;
	
	КэшПользователиФизЛица = Новый Массив;
	Для каждого СтрОтветственный Из ТаблицаОтветственных Цикл		
		
		Если Не ЗначениеЗаполнено(СтрОтветственный.Пользователь) Тогда
			Продолжить;
		ИначеЕсли ОбщегоНазначенияУХ.ПроверитьТипЗначенияВПодсистеме(СтрОтветственный.Пользователь,
					"СправочникСсылка.КонтактныеЛицаОрганизаций", "БухгалтерияПредприятияПодсистемы") Тогда	
			Результат.Вставить(СтрОтветственный.Пользователь, СтрОтветственный.Пользователь.ОсновнойАдресЭлектроннойПочты);
		Иначе
			КэшПользователиФизЛица.Добавить(СтрОтветственный.Пользователь);
		КонецЕсли;
		
	КонецЦикла;
	
	ТекущийВидыКИ = Новый Массив;
	ТекущийВидыКИ.Добавить(ОбщегоНазначения.ПредопределенныйЭлемент("Справочник.ВидыКонтактнойИнформации.EMailФизическиеЛица"));
	ТекущийВидыКИ.Добавить(ОбщегоНазначения.ПредопределенныйЭлемент("Справочник.ВидыКонтактнойИнформации.EmailПользователя"));
	
	ТабКИ = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъектов(
						КэшПользователиФизЛица, 
						ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты),
						ТекущийВидыКИ
					);
		
	Для каждого ТекущаяКИ Из ТабКИ Цикл
	
		Результат.Вставить(ТекущаяКИ.Объект, СокрЛП(ТекущаяКИ.Представление));
	
	КонецЦикла;				
					
	Возврат Результат;

КонецФункции

Процедура ДобавитьДокументОтветственногоЛица(Документ,Ответственный,ТаблицаОтветственных)
	
	СтрОтветственный=ТаблицаОтветственных.Найти(Ответственный,"Пользователь");
	
	Если СтрОтветственный=Неопределено Тогда
		
		СтрОтветственный=ТаблицаОтветственных.Добавить();
		СтрОтветственный.Пользователь=Ответственный;
		
	КонецЕсли;
	
	Если СтрОтветственный.ДокументыПользователя.НайтиПоЗначению(Документ)=Неопределено Тогда
		
		СтрОтветственный.ДокументыПользователя.Добавить(Документ);
		
	КонецЕсли;
			
КонецПроцедуры // ДобавитьДокументОтветственногоЛица()

Процедура ДобавитьОрганизациюОтветственногоЛица(Организация,Ответственный,ТаблицаОтветственных)
	
	СтрОтветственный=ТаблицаОтветственных.Найти(Ответственный,"Пользователь");
	
	Если СтрОтветственный=Неопределено Тогда
		
		СтрОтветственный=ТаблицаОтветственных.Добавить();
		СтрОтветственный.Пользователь=Ответственный;
		
	КонецЕсли;
	
	Если СтрОтветственный.ОрганизацииПользователя.НайтиПоЗначению(Организация)=Неопределено Тогда
		
		СтрОтветственный.ОрганизацииПользователя.Добавить(Организация);
		
	КонецЕсли;
			
КонецПроцедуры // ДобавитьДокументОтветственногоЛица()

Функция ПолучитьСсылкуНаОбъектПочта(УчетнаяЗапись, ТихийРежим = Ложь)
	
	УчетнаяЗапись = РаботаСПочтовымиСообщениями.СистемнаяУчетнаяЗапись();
	
	Если НЕ РаботаСПочтовымиСообщениями.УчетнаяЗаписьНастроена(УчетнаяЗапись,Истина,Ложь) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Профиль = УправлениеЭлектроннойПочтойУХ.ПолучитьИнтернетПочтовыйПрофиль(УчетнаяЗапись);
	Почта   = Новый ИнтернетПочта;
	
	Попытка
		Почта.Подключиться(Профиль);
	Исключение
		
		Если НЕ ТихийРежим Тогда
			СообщениеПользователю = Новый СообщениеПользователю;
			СообщениеПользователю.Текст = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			СообщениеПользователю.Сообщить();
			Возврат Неопределено;
		КонецЕсли;
		
	КонецПопытки;
	
	Возврат Почта;
	
КонецФункции

Процедура ОтправитьПисьмаИзРегистра() Экспорт
	
	Перем УчетнаяЗапись;
	
	Почта = ПолучитьСсылкуНаОбъектПочта(УчетнаяЗапись, Истина);
	
	Если Почта = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	УведомленияПоЭлектроннойПочте.Источник,
	|	УведомленияПоЭлектроннойПочте.ДатаСоздания,
	|	УведомленияПоЭлектроннойПочте.Письмо,
	|	УведомленияПоЭлектроннойПочте.ИдентификаторПисьма
	|ИЗ
	|	РегистрСведений.УведомленияПоЭлектроннойПочте КАК УведомленияПоЭлектроннойПочте
	|ГДЕ
	|	УведомленияПоЭлектроннойПочте.ДатаОтправки = ДАТАВРЕМЯ(1, 1, 1)";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Попытка
			Письмо = Выборка.Письмо.Получить();
			Письмо.Кодировка = "utf-8";
						
			СтруктураАдресаЭлектроннойПочты = РазложитьАдресЭлектроннойПочты(УчетнаяЗапись.АдресЭлектроннойПочты);
			
			Если ТипЗнч(Письмо.Отправитель) = Тип("Строка") Тогда
				Если СокрЛП(Письмо.Отправитель) = "" Тогда
					Письмо.Отправитель = СтруктураАдресаЭлектроннойПочты.Адрес;
				КонецЕсли;
			ИначеЕсли ТипЗнч(Письмо.Отправитель) = Тип("ИнтернетПочтовыйАдрес") Тогда
				Если Письмо.Отправитель.Адрес = "" Тогда	
					ЗаполнитьЗначенияСвойств(Письмо.Отправитель, СтруктураАдресаЭлектроннойПочты);
				КонецЕсли;
			КонецЕсли;
			
			Почта.Послать(Письмо);
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ОбщегоНазначенияУХ.СообщитьОбОшибке(ИнформацияОбОшибке.Описание);
			Продолжить;
		КонецПопытки;
		
		НоваяЗапись = РегистрыСведений.УведомленияПоЭлектроннойПочте.СоздатьМенеджерЗаписи();
		НоваяЗапись.Источник			 = Выборка.Источник;
		НоваяЗапись.ДатаСоздания		 = Выборка.ДатаСоздания;
		НоваяЗапись.ИдентификаторПисьма	 = Выборка.ИдентификаторПисьма;
		НоваяЗапись.Письмо				 = Новый ХранилищеЗначения(Письмо, Новый СжатиеДанных());
		НоваяЗапись.ДатаОтправки		 = ТекущаяДата();
		
		НоваяЗапись.Записать();
	КонецЦикла;
	
	Почта.Отключиться();
	
КонецПроцедуры

Функция РазложитьАдресЭлектроннойПочты(АдресЭлектроннойПочты) Экспорт
	
	Результат = Новый Структура("Адрес, Пользователь, Сервер", "", "", "");
	
	Адрес = СокрЛП(АдресЭлектроннойПочты);
	
	ПозицияУгловойСкобкиНачало = СтрНайти(Адрес, "<");
	ПозицияУгловойСкобкиКонец = СтрНайти(Адрес, ">");
	Если ПозицияУгловойСкобкиНачало > 0 И ПозицияУгловойСкобкиКонец > 0
		И ПозицияУгловойСкобкиНачало < ПозицияУгловойСкобкиКонец Тогда
		
		Адрес = Сред(Адрес, ПозицияУгловойСкобкиНачало + 1, ПозицияУгловойСкобкиКонец - ПозицияУгловойСкобкиНачало - 1);
	КонецЕсли;
	
	Результат.Вставить("Адрес", Адрес);
	
	ПозицияА = СтрНайти(Адрес, "@");
	Если ПозицияА > 0 Тогда
		Результат.Вставить("Пользователь", Лев(Адрес, ПозицияА - 1));
		Результат.Вставить("Сервер", Сред(Адрес, ПозицияА + 1));
	КонецЕсли;
	
	Возврат Результат; 
	
КонецФункции

