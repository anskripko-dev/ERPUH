
#Область СлужебныйПрограммныйИнтерфейс

Функция СформироватьРасшифровкуКонтроля(СтрокаКонтроля) Экспорт
	
	Проверка = КонтрольУХПовтИсп.ПроверкаПоВидуКонтроля(СтрокаКонтроля.ВидКонтроля);
	Если Не ЗначениеЗаполнено(Проверка) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Проверка.Объект.СформироватьРасшифровкуКонтроля(Проверка, СтрокаКонтроля);
	
КонецФункции

Процедура ВыполнитьИнтерактивныйКонтроль(Знач Объект, ПараметрыКонтроля) Экспорт
	
	ПараметрыКонтроля.Вставить("Документ",	Объект.Ссылка);
	ПараметрыКонтроля.Вставить("ИмяДокумента", Объект.Ссылка.Метаданные().Имя);
	ПараметрыКонтроля.Вставить("Источники", КонтрольУХ.ПолучитьИсточникиДокумента(Объект, ПараметрыКонтроля));
	ПараметрыКонтроля.Вставить("РеквизитыКонтроляВДокументе", КонтрольУХ.ТипыДокументовИсключенияДляПодключаемойКоманды());
	
	Если ПараметрыКонтроля.Источники <> неопределено Тогда
		КонтрольУХ.ВыполнитьКонтрольИнтерактивно(ПараметрыКонтроля);
	Иначе
		РеквизитыДокумента = Новый Структура;
		// Обнуляем значения реквизитов документа
		Для Каждого Проверка Из КонтрольУХПовтИсп.ЗарегистрированныеПроверки(ПараметрыКонтроля.ИмяДокумента) Цикл
			Если ЗначениеЗаполнено(Проверка.ИмяРеквизита) Тогда
				РеквизитыДокумента.Вставить(Проверка.ИмяРеквизита, Ложь);
			КонецЕсли;
		КонецЦикла;
		
		// Установка признака наличия превышений в документе
		РеквизитыЭлементы = КонтрольУХКлиентСервер.ИменаРеквизитовЭлементовКонтроля();
		РеквизитыДокумента.Вставить(РеквизитыЭлементы.ЕстьНарушения, Ложь);
		ПараметрыКонтроля.Вставить("РеквизитыДокумента", РеквизитыДокумента);
		
	КонецЕсли;
	
	ПараметрыКонтроля.Удалить("Источники");
	
КонецПроцедуры

Функция ПараметрыЛимитированияПериода(Знач ВидБюджета, Знач ОтчетнаяДата, Знач Направление) Экспорт
	
	Перем ПараметрыЛимитирования;
	
	Если Направление = 0 Тогда
		ПараметрыЛимитирования = ПолучитьПараметрыЛимитированияБюджетаНаДату(ВидБюджета, ОтчетнаяДата);	
	Иначе
		ПараметрыЛимитирования = ПолучитьПараметрыЛимитированияБюджетаНаДатуПоНаправлению(ВидБюджета, ОтчетнаяДата, Направление);
	КонецЕсли;      
	
	Возврат ПараметрыЛимитирования;

КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПолучитьПараметрыЛимитированияБюджетаНаДату(ВидБюджета, Дата)

	Результат = Новый Структура("ВидБюджета, Период, ИспользоватьЛимитирование, ИспользоватьРезервирование, СпособОпределенияВалютыЛимитирования, 
								|ВалютаПоУмолчанию, ПериодичностьЛимитирования, КоличествоПериодовЛимитирования");
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ВидБюджета", ВидБюджета);
	Запрос.УстановитьПараметр("НаДату",     Дата);
	
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ПараметрыЛимитирования.Период КАК Период,
	|	ПараметрыЛимитирования.ВидБюджета КАК ВидБюджета,
	|	ПараметрыЛимитирования.ПериодичностьПланирования КАК ПериодичностьПланирования,
	|	ПараметрыЛимитирования.ПериодичностьЛимитирования КАК ПериодичностьЛимитирования,
	|	ПараметрыЛимитирования.ИспользоватьЛимитирование КАК ИспользоватьЛимитирование,
	|	ПараметрыЛимитирования.ИспользоватьРезервирование КАК ИспользоватьРезервирование,
	|	ПараметрыЛимитирования.СпособОпределенияВалютыЛимитирования КАК СпособОпределенияВалютыЛимитирования,
	|	ПараметрыЛимитирования.КоличествоПериодовЛимитирования КАК КоличествоПериодовЛимитирования,
	|	ПараметрыЛимитирования.КонтрольЛимитов КАК КонтрольЛимитов,
	|	ПараметрыЛимитирования.КонтрольРезервов КАК КонтрольРезервов,
	|	ПараметрыЛимитирования.УчитыватьАналитикиСтатьиБюджетаПриКонтроле КАК УчитыватьАналитикиСтатьиБюджетаПриКонтроле,
	|	ПараметрыЛимитирования.ВалютаПоУмолчанию КАК ВалютаПоУмолчанию,
	|	ПараметрыЛимитирования.ПланированиеЗакупок КАК ПланированиеЗакупок
	|ИЗ
	|	РегистрСведений.ПараметрыЛимитирования.СрезПоследних(&НаДату, ) КАК ПараметрыЛимитирования
	|ГДЕ
	|	ПараметрыЛимитирования.ВидБюджета = &ВидБюджета";
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Результат = Новый Структура;
	Для Каждого Колонка Из РезультатЗапроса.Колонки Цикл
		Результат.Вставить(Колонка.Имя);
	КонецЦикла;
	
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, Выборка);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции   

Функция ПолучитьПараметрыЛимитированияБюджетаНаДатуПоНаправлению(ВидБюджета, Дата, Направление)

	Результат = Новый Структура("ВидБюджета, Период, ИспользоватьЛимитирование, ИспользоватьРезервирование, СпособОпределенияВалютыЛимитирования, 
								|ВалютаПоУмолчанию, ПериодичностьЛимитирования, КоличествоПериодовЛимитирования");
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ВидБюджета", ВидБюджета);
	Запрос.УстановитьПараметр("НаДату",     Дата);
	
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ПараметрыЛимитирования.Период КАК Период,
	|	ПараметрыЛимитирования.ВидБюджета КАК ВидБюджета,
	|	ПараметрыЛимитирования.ПериодичностьПланирования КАК ПериодичностьПланирования,
	|	ПараметрыЛимитирования.ПериодичностьЛимитирования КАК ПериодичностьЛимитирования,
	|	ПараметрыЛимитирования.ИспользоватьЛимитирование КАК ИспользоватьЛимитирование,
	|	ПараметрыЛимитирования.ИспользоватьРезервирование КАК ИспользоватьРезервирование,
	|	ПараметрыЛимитирования.СпособОпределенияВалютыЛимитирования КАК СпособОпределенияВалютыЛимитирования,
	|	ПараметрыЛимитирования.КоличествоПериодовЛимитирования КАК КоличествоПериодовЛимитирования,
	|	ПараметрыЛимитирования.КонтрольЛимитов КАК КонтрольЛимитов,
	|	ПараметрыЛимитирования.КонтрольРезервов КАК КонтрольРезервов,
	|	ПараметрыЛимитирования.УчитыватьАналитикиСтатьиБюджетаПриКонтроле КАК УчитыватьАналитикиСтатьиБюджетаПриКонтроле,
	|	ПараметрыЛимитирования.ВалютаПоУмолчанию КАК ВалютаПоУмолчанию,
	|	ПараметрыЛимитирования.ПланированиеЗакупок КАК ПланированиеЗакупок
	|ИЗ
	|	РегистрСведений.ПараметрыЛимитирования КАК ПараметрыЛимитирования
	|ГДЕ
	|	ПараметрыЛимитирования.ВидБюджета = &ВидБюджета
	|	И &НаДату
	|
	|УПОРЯДОЧИТЬ ПО
	|	Период УБЫВ";             
	
	Если Направление = -1 Тогда  
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&НаДату", "ПараметрыЛимитирования.Период < &НаДату");
	Иначе
		Запрос.Текст = СтрЗаменить(Запрос.Текст, " УБЫВ", "");
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&НаДату", "ПараметрыЛимитирования.Период > &НаДату");	
	КонецЕсли;
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Результат = Новый Структура;
	Для Каждого Колонка Из РезультатЗапроса.Колонки Цикл
		Результат.Вставить(Колонка.Имя);
	КонецЦикла;
	
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, Выборка);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции   

#КонецОбласти


