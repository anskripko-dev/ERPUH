///////////////////////////////////////////////////////////////////////////////////////////////////////
// Расширение состава документов обработки этапов процесса подготовки отчетности
// 
// Для подключения документа в процесс подготовки отчетности необходимо включить его в
// 	определяемый тип "ДокументыПроцессаПодготовкиОтчетности" и перезаписать соответствующий
// 	элемент справочника "ДокументБД" для установки реквизита "ИспользуетсяВПроцессе" в 
// 	значение Истина.
// 
///////////////////////////////////////////////////////////////////////////////////////////////////////


#Область ПрограммныйИнтерфейс

// Вызывается перед созданием документа процесса подготовки отчетности
//
// Параметры:
//  СтруктураПараметров	 - Структура - Структура параметров полученная функцией УправлениеПроцессамиПодготовкиОтчетности.ПараметрыОбработкиЭтапов() 
//  ДанныеДокументаБД	 - ВыборкаЗапроса - Выборка запроса с данными документа БД
// 
// Возвращаемое значение:
//  ДокументОбъект - ДокументОбъект - Созданный документ, Неопределено - будет выполнена стандартаная 
//		обрабокта создания документа
//
Функция СоздатьДокументПроцесса(СтруктураПараметров, ДанныеДокументаБД) Экспорт
	
	Возврат Неопределено;
	
КонецФункции

// Вызывается перед обработкой документа процесса подготовки отчетности
//
// Параметры:
//  СтруктураПараметров	 - Структура - Структура параметров полученная функцией УправлениеПроцессамиПодготовкиОтчетности.ПараметрыОбработкиЭтапов() 
//  ДокументОбъект		 - ДокументОбъект - Обрабатываемый объект
// 
// Возвращаемое значение:
//  Булево - Истина - документ обработан, Ложь - будет выполнена стандартная обработка документа
//
Функция ОбработатьДокументПроцесса(СтруктураПараметров, ДокументОбъект) Экспорт
	
	Возврат Ложь;
	
КонецФункции

// Вызывается перед изменением состояния документа процесса подготовки отчетности
//
// Параметры:
//  СтруктураПараметров	 - Структура - Структура параметров полученная функцией УправлениеПроцессамиПодготовкиОтчетности.ПараметрыОбработкиЭтапов() 
//  ДокументОбъект		 - ДокументОбъект - Обрабатываемый объект
// 
// Возвращаемое значение:
//  Булево - Истина - состояние документа изменено, Ложь - будет выполнена стандартная процедура изменения состояния документа
//
Функция ИзменитьСостояниеДокументаПроцесса(СтруктураПараметров, ДокументОбъект) Экспорт
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти