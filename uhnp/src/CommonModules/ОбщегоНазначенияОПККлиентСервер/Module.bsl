
#Область СлужебныйПрограммныйИнтерфейс    

#Область ПредставлениеПериодовСКД

Функция ПредставлениеПолугодия(Дата) Экспорт
	Возврат Формат(Дата, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку("ДФ='''%1 полугодие'' yyyy ''г.'''", Полугодие(Дата)));
КонецФункции

Функция ПредставлениеНеделяГода(Дата) Экспорт
	ПредставлениеНедели = Формат(Дата, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку("ДФ='''%1 нелеля'' yyyy ''г.'''", НеделяГода(Дата)));
	НачалоНедели = Формат(НачалоНедели(Дата), "ДФ=dd.MM");
	ОкончаниеНедели = Формат(КонецНедели(Дата), "ДФ=dd.MM");
	Возврат СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1 (%2-%3)'"), ПредставлениеНедели, НачалоНедели, ОкончаниеНедели);
КонецФункции

Функция ПредставлениеДекадаГода(Дата) Экспорт
	Возврат Формат(Дата, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку("ДФ='''%1 декада'' yyyy ''г.'''", ДекадаГода(Дата)));
КонецФункции

Функция ПредставлениеПериодаПоПериодичности(Дата, Периодичность) Экспорт
	
	Если Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Год") Тогда
		Возврат Формат(Дата, "ДФ='yyyy ''г.'''");
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Полугодие") Тогда
		Возврат ПредставлениеПолугодия(Дата);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Квартал") Тогда
		Возврат Формат(Дата, "ДФ='q ''квартал ''yyyy ''г.'''");
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Месяц") Тогда
		Возврат Формат(Дата, "ДФ='MMMM yyyy ''г.'");
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Декада") Тогда
		Возврат ПредставлениеДекадаГода(Дата);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Неделя") Тогда
		Возврат ПредставлениеНеделяГода(Дата);
	Иначе // День
		Возврат Формат(Дата, "ДФ=dd.MM.yyyy");
	КонецЕсли;
	
КонецФункции   

Функция НачалоПериодаПоПериодичности(Дата, Периодичность) Экспорт
	
	Если Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Год") Тогда
		Возврат НачалоГода(Дата);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Полугодие") Тогда
		Возврат ДобавитьМесяц(НачалоГода(Дата), Полугодие(Дата)*6-6);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Квартал") Тогда
		Возврат НачалоКвартала(Дата);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Месяц") Тогда
		Возврат НачалоМесяца(Дата);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Декада") Тогда
		НомерДня = 1+(Дата - НачалоМесяца(Дата))/24/3600;
		Если НомерДня <=10 Тогда
			Возврат НачалоМесяца(Дата);
		ИначеЕсли НомерДня <=20 И НомерДня > 10 Тогда
			Возврат НачалоМесяца(Дата)+10*24*60*60;
		Иначе
			Возврат НачалоМесяца(Дата)+20*24*60*60;			
		КонецЕсли;
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Неделя") Тогда
		Возврат НачалоНедели(Дата);
	Иначе // День
		Возврат Дата;
	КонецЕсли;
	
КонецФункции     

Функция ОкончаниеПериодаПоПериодичности(Дата, Периодичность) Экспорт
	
	Если Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Год") Тогда
		Возврат КонецГода(Дата);                  
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Полугодие") Тогда
		Возврат ДобавитьМесяц(КонецГода(Дата), -((2-Полугодие(Дата))*6));   
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Квартал") Тогда
		Возврат КонецКвартала(Дата);
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Месяц") Тогда
		Возврат КонецМесяца(Дата);  
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Декада") Тогда
		НомерДня = 1+(Дата - НачалоМесяца(Дата))/24/3600;
		Если НомерДня <=10 Тогда
			Возврат КонецДня(НачалоМесяца(Дата)+9*24*60*60);
		ИначеЕсли НомерДня <=20 И НомерДня > 10 Тогда
			Возврат КонецДня(НачалоМесяца(Дата)+19*24*60*60);
		Иначе
			Возврат КонецМесяца(Дата);			
		КонецЕсли;     
	ИначеЕсли Периодичность = ПредопределенноеЗначение("Перечисление.Периодичность.Неделя") Тогда
		Возврат КонецНедели(Дата);
	Иначе // День
		Возврат Дата;
	КонецЕсли;
	
КонецФункции



#КонецОбласти

Процедура ЗаменитьУдалитьПараметрВыбора(Элемент, Имя, НовыйПараметрВыбора = Неопределено) Экспорт

	МассивПараметров = Новый Массив;
	Для Каждого СвязьПараметровВыбора Из Элемент.ПараметрыВыбора Цикл
		Если ВРег(СвязьПараметровВыбора.Имя) <> ВРег(Имя) Тогда
			МассивПараметров.Добавить(СвязьПараметровВыбора);
		КонецЕсли;
	КонецЦикла;

	Если НовыйПараметрВыбора <> Неопределено Тогда
		МассивПараметров.Добавить(НовыйПараметрВыбора);
	КонецЕсли;
	
	Элемент.ПараметрыВыбора = Новый ФиксированныйМассив(МассивПараметров);

КонецПроцедуры

// Процедура устанавливает отбор по функциональным направлениям ЦФО
Процедура УстановитьОтборПоФункциональнымНаправлениям(Знач ЦФО, ЭлементыФормы) Экспорт
	
	ФункциональныеНаправления = ПланированиеПотребностейВызовСервера.ФункциональныеНаправленияЦФО(ЦФО);
	ИмяПараметраВыбора = "Отбор.ФункциональноеНаправление";
	ПараметрВыбора = Новый ПараметрВыбора(ИмяПараметраВыбора, Новый ФиксированныйМассив(ФункциональныеНаправления));
	
	Для Каждого ТекЭлемент Из ЭлементыФормы Цикл
		ЗаменитьУдалитьПараметрВыбора(ТекЭлемент, ИмяПараметраВыбора, ПараметрВыбора);
	КонецЦикла;
	
КонецПроцедуры

// Перенесена из ОперативноеПланированиеФормыУХКлиентСервер
Процедура ПересчитатьСуммуНДС(СтрокаРасшифровки) Экспорт
	
	Если СтрокаРасшифровки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	ЗначениеСтавкиНДС = ВстраиваниеОПКВызовСервера.ПолучитьСтавкуНДС(СтрокаРасшифровки.СтавкаНДС);
	СтрокаРасшифровки.СуммаНДС = СтрокаРасшифровки.Сумма * ЗначениеСтавкиНДС / (100 + ЗначениеСтавкиНДС);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции 

Функция Полугодие(Дата)
	Возврат ?(Месяц(Дата) >6, 2, 1);
КонецФункции

Функция ДекадаГода(Дата)
	Возврат Цел(ДеньГода(Дата)/10)+1;
КонецФункции
	
#КонецОбласти
 
