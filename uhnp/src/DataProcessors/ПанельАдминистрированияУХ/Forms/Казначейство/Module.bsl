
#Область ОписаниеПеременных

&НаКлиенте
Перем ОбновитьИнтерфейс;

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
			
	УстановитьНастройкиЗаданий();
	
	УстановитьЗаголовокРедактированиеСтавкиДисконтирования();
	
	ЗаполнитьНастройкиОптимизацииПлатежногоКалендаря();
	
	ЗаполнитьПлатежныеДниНедели();
	
	УстановитьЗаголовокПериодичностьГлубиныАнализа();
	
	УстановитьВидимостьДоступность();
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	ОбновитьИнтерфейс();
КонецПроцедуры

&НаСервере
Процедура ОбработкаПроверкиЗаполненияНаСервере(Отказ, ПроверяемыеРеквизиты)
	
	Если НаборКонстант.ПорогКритичногоИзлишка < НаборКонстант.ПорогНормыОстатка Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru = 'Граница значительного излишка должна быть больше, чем граница незначительного излишка'"),,"НаборКонстант.ПорогКритичногоИзлишка",,Отказ);
	КонецЕсли;
	Если  ФормированиеЗаявокПоГрафикамОплаты 
		И (НЕ НаборКонстант.СоздаватьЗаявкиНаОплатуПланируемыеПоступленияДС
		И НЕ НаборКонстант.СоздаватьЗаявкиНаРасходПланируемыеДоходы)	Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru = 'Регламентное задание ""Формирование заявок по графикам оплаты"" включено, но формирование заявок не выбрано'"),,"НаборКонстант.АвтоматическиАктуализироватьЗаявкиПоГрафикамДоговоров",,Отказ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ФункциональностьПлатежногоКалендаряПриИзменении(Элемент)
	
	Подключаемый_ПриИзмененииРеквизита(Элемент); 
	
	УстановитьВидимостьДоступность(); 
	
КонецПроцедуры

&НаКлиенте
Процедура ГоризонтОперативногоПланированияПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура КоличествоПериодовДолгосрочногоПланированияПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ГлубинаОтображенияПросроченныхПлатежейПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ПорогКритичногоИзлишкаПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ПорогНормыОстаткаПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ПорогКритичногоИзлишка1ПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ПорогНормыОстатка1ПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ПорогНекритичногоНедостатка1ПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СтоимостьПереводаМеждуСчетамиПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ПроцентКомиссииПриКонвертацииВалютыПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СущественнаяРазницаСтоимостиКредитовПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическиФормироватьИдентификаторПлатежаВДоговорахПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ГоризонтФормированияЗаявокПоГрафикамПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура РежимАктуализацииГрафикаПриИзмененииПервичныхДокументовПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьЗаявкиНаПривлечениеФинансированияПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ГоризонтФормированияЗаявокПоГрафикам1ПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьРеестрПлатежейПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическиФормироватьПлатежныеПорученияПриУтвержденииРеестраПлатежейПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СоздаватьЗаявкиНаОплатуВместоПлатежныхПорученийПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьПодтверждающиеДокументыПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ОбъединятьПозицииПриСозданииПлатежныхПорученийПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическиФормироватьОФДПоДокументамДДСПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическиФормироватьОФДПоДокументамПТУРТУПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СрокФормированияСВОПоПоступлениямПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СрокФормированияСПДПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ФормироватьОповещенияОПриближенииДатыПлатежаЗаКоличествоДнейПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура КоличествоДнейПросрочкиПоПлатежамДляФормированияПретензийПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура УчитыватьАккредитивыИГарантииВЛимитахРазмещенияСредствПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ПереоценкаЗаявокНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ОтправкаНаСогласованиеЗаявокНаОперацииНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ОповещениеОПросроченныхПлатежахНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ПереводИзлишковДенежныхСредствНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_АвтоматическоеСозданиеРеестровПлатежейНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ЗагрузкаКурсовВалютНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ОповещениеОПриближенииСрокаОплатыНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_АвтоматическоеФормированиеПретензийНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ОповещениеОНеобходимостиИзмененияСтатусаПретензииНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ФормированиеЗаявокПоГрафикамОплатыНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура Расписание_АктуализацияГрафиковНажатие(Элемент)  
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", "")); 
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ЗагрузкаСпискаБанковОбменСБанкамиНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура ДоступныеВариантыОптимизацииПлатежногоКалендаряПриИзменении(Элемент)
	СохранитьДоступныеВариантыОптимизацииПлатежногоКалендаря();
КонецПроцедуры  

&НаКлиенте
Процедура СтавкаДисконтированияПриИзменении(Элемент)
	УстановитьЗаголовокРедактированиеСтавкиДисконтирования();
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура НадписьНастроитьВесаИсточниковДанныхНажатие(Элемент)
	ОткрытьФорму("РегистрСведений.МатрицаВесовЛиквидности.Форма.МатрицаВесовЛиквидности");
КонецПроцедуры

&НаКлиенте
Процедура ПорогНекритичногоНедостаткаПриИзменении(Элемент)
	
	Если НаборКонстант.ПорогНекритичногоНедостатка > 0 Тогда
		НаборКонстант.ПорогНекритичногоНедостатка = -НаборКонстант.ПорогНекритичногоНедостатка;
		Подключаемый_ПриИзмененииРеквизита(Элемент);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьФормированиеЗаявокПоГрафикамОплатыПриИзменении(Элемент)
	ПроверитьЗаполнениеУсловийФормированияЗаявок();
	ФормированиеЗаявокПоГрафикамОплаты = НаборКонстант.АвтоматическиАктуализироватьЗаявкиПоГрафикамДоговоров;
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"ФормированиеЗаявокПоГрафикамОплаты",Истина); 
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СоздаватьЗаявкиНаОплатуПланируемыеПоступленияДСПриИзменении(Элемент)
	ПроверитьЗаполнениеУсловийФормированияЗаявок();	    
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СоздаватьЗаявкиНаРасходПланируемыеДоходыПриИзменении(Элемент)
	ПроверитьЗаполнениеУсловийФормированияЗаявок(); 
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьОповещениеОПриближенииСрокаОплатыПриИзменении(Элемент)
	
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"ОповещениеОПриближенииСрокаОплаты",Истина);

КонецПроцедуры

&НаКлиенте
Процедура АвтоматическиФормироватьПретензииПоПросроченнойЗадолженностиПриИзменении(Элемент)
	
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"АвтоматическоеФормированиеПретензий",Истина);

КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьОповещениеОНеобходимостиИзмененияСтатусаПретензииПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"ОповещениеОНеобходимостиИзмененияСтатусаПретензии",Истина);

КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьПереоценкаЗаявокПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"ПереоценкаЗаявок",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьПроверкуИсполненияПлатежейВСрокПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"ОповещениеОПросроченныхПлатежах",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьПереводИзлишковДенежныхСредствПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "ПереводИзлишковДенежныхСредств",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьАвтоматическоеСозданиеРеестровПлатежейПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "АвтоматическоеСозданиеРеестровПлатежей",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьЗагрузкаСпискаБанковОбменСБанками(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "ЗагрузкаСпискаБанковОбменСБанками",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьЗагрузкаКурсовВалютПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "ЗагрузкаКурсовВалют",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьЗагрузкаСтавокЦБРФПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "ЗагрузкаСтавокЦБРФ",Истина);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьОтправкаНаСогласованиеЗаявокНаОперацииПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "ОтправкаНаСогласованиеЗаявокНаОперации",Истина);  
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьДоверительныеКоэффициентыПриИзменении(Элемент)
    РасчетДоверительныхКоэффициентов = НаборКонстант.ИспользоватьДоверительныеКоэффициенты;
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект,"РасчетДоверительныхКоэффициентов",Истина); 
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ГлубинаАнализаЭкстраполяцииДенежныхПотоковПриИзменении(Элемент)
	УстановитьЗаголовокПериодичностьГлубиныАнализа();
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура НоменклатураОтраженияЦессииПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СтатьяОтраженияЦессииПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СтатьяДоходовЦессииПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура СтатьяРасходовЦессииПриИзменении(Элемент)
	Подключаемый_ПриИзмененииРеквизита(Элемент);
КонецПроцедуры

&НаКлиенте
Процедура ИспользоватьСинхронизацияСтатусовЗаявокНаРасходованиеДенежныхСредствПриИзменении(Элемент)
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, "СинхронизацияСтатусовЗаявокНаРасходованиеДенежныхСредств",Истина);
КонецПроцедуры

&НаКлиенте
Процедура Расписание_СинхронизацияСтатусовЗРДСНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

#Область ОбработчикиСобытийЭлементов_ПлатежныеДни 

&НаКлиенте
Процедура ПлатежныйДеньНедели1ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

&НаКлиенте
Процедура ПлатежныйДеньНедели2ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

&НаКлиенте
Процедура ПлатежныйДеньНедели3ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

&НаКлиенте
Процедура ПлатежныйДеньНедели4ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

&НаКлиенте
Процедура ПлатежныйДеньНедели5ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

&НаКлиенте
Процедура ПлатежныйДеньНедели6ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

&НаКлиенте
Процедура ПлатежныйДеньНедели7ПриИзменении(Элемент)
	СохранитьПлатежныеДниНедели();
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОткрытьСопоставлениеСтатейБюджетовОперациямФинансовыхИнструментов(Команда)
	ОткрытьФорму("Обработка.НастройкаСтатейБюджетовДляОпераций.Форма.Форма");
КонецПроцедуры

&НаКлиенте
Процедура Расписание_ИспользоватьДоверительныеКоэффициентыНажатие(Элемент)
	ОткрытьНастройкуРасписания(СтрЗаменить(Элемент.Имя, "Расписание_", ""));
КонецПроцедуры

&НаКлиенте
Процедура ДекорацияПерейтиВНастройкиОповещенийНажатие(Элемент)
	
	СтруктураОтбора = Новый Структура;
	СтруктураОтбора.Вставить("ВидСобытияОповещения",	ПредопределенноеЗначение("Справочник.ВидыСобытийОповещений.ПлатежНеИсполненВСрок"));
	СтруктураОтбора.Вставить("КатегорияОповещения",		ПредопределенноеЗначение("Справочник.КатегорииСобытийОповещений.ОплатаЗаявок"));
	
	ОткрытьФорму("РегистрСведений.НастройкиОповещений.ФормаСписка",Новый Структура("Отбор,НастройкаОповещений",СтруктураОтбора));
	
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьСтавкуДисконтирования(Команда)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("РедактироватьСтавкуДисконтированияЗавершение", ЭтотОбъект);
	СтруктураОтбор = Новый Структура("ВидКотировки", НаборКонстант.СтавкаДисконтирования);
	СтруктураПараметров = Новый Структура("Отбор", СтруктураОтбор);
	ОткрытьФорму("РегистрСведений.ЗначенияКотировокФИ.ФормаСписка",СтруктураПараметров,ЭтотОбъект,,,,ОписаниеОповещения,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УстановитьВидимостьДоступность()    

	ЭтоРасширеннаяФункциональность = 
		(НаборКонстант.ФункциональностьПлатежногоКалендаря = Перечисления.ФункциональностьПлатежногоКалендаря.Расширенная);
		
	Элементы.КоличествоПериодовДолгосрочногоПланирования.Видимость = ЭтоРасширеннаяФункциональность;	
	Элементы.НадписьНастроитьВесаИсточниковДанных.Видимость = ЭтоРасширеннаяФункциональность;	
	Элементы.НастроитьСоответствиеДанныхБюджетированияПлатежномуКалендарю.Видимость = ЭтоРасширеннаяФункциональность;
    Элементы.ГруппаИспользованиеИсторическихДанных.Видимость = ЭтоРасширеннаяФункциональность;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПриИзмененииРеквизита(Элемент, ОбновлятьИнтерфейс = Истина)
	
	ИмяКонстанты = ПриИзмененииРеквизитаСервер(Элемент.Имя);
	ОбновитьПовторноИспользуемыеЗначения();
	
	Если ОбновлятьИнтерфейс Тогда
		ОбновитьИнтерфейс = Истина;
		ПодключитьОбработчикОжидания("ОбновитьИнтерфейсПрограммы", 2, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьИнтерфейсПрограммы()
	
	Если ОбновитьИнтерфейс = Истина Тогда
		ОбновитьИнтерфейс = Ложь;
		ОбщегоНазначенияКлиент.ОбновитьИнтерфейсПрограммы();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПриИзмененииРеквизитаСервер(ИмяЭлемента)
	
	РеквизитПутьКДанным = Элементы[ИмяЭлемента].ПутьКДанным;
	ИмяКонстанты = СохранитьЗначениеРеквизита(РеквизитПутьКДанным);
	ОбновитьПовторноИспользуемыеЗначения();
	Возврат ИмяКонстанты;
	
КонецФункции

&НаСервере
Функция СохранитьЗначениеРеквизита(РеквизитПутьКДанным)
	
	ЧастиИмени = СтрРазделить(РеквизитПутьКДанным, ".");
	Если ЧастиИмени.Количество() <> 2 Тогда
		Возврат "";
	КонецЕсли;
	
	ИмяКонстанты = ЧастиИмени[1];
	КонстантаМенеджер = Константы[ИмяКонстанты];
	КонстантаЗначение = НаборКонстант[ИмяКонстанты];
	
	Если КонстантаМенеджер.Получить() <> КонстантаЗначение Тогда
		КонстантаМенеджер.Установить(КонстантаЗначение);
	КонецЕсли;
	
	Возврат ИмяКонстанты;
	
КонецФункции

&НаСервере
Процедура ЗаполнитьНастройкиОптимизацииПлатежногоКалендаря()
	
	ТаблицаВозможныхВариантовОптимизации = ОптимизацияПлатежногоКалендаря.ТаблицаДоступныхВариантовОптимизации();
	ДоступныеВариантыОптимизацииПлатежногоКалендаря.Загрузить(ТаблицаВозможныхВариантовОптимизации);
	
	НаименованиеВалюты = СокрЛП(Константы.ВалютаРегламентированногоУчета.Получить().Наименование);
	Элементы.ДекорацияСтоимостьПереводаМеждуСчетамиВалюта.Заголовок = НаименованиеВалюты;
	Элементы.ДекорацияСущественнаяРазницаСтоимостиКредитовВалюта.Заголовок = НаименованиеВалюты;
	
КонецПроцедуры

&НаСервере
Процедура СохранитьДоступныеВариантыОптимизацииПлатежногоКалендаря()
	
	ОптимизацияПлатежногоКалендаря.СохранитьДоступныеВариантыОптимизации(ДоступныеВариантыОптимизацииПлатежногоКалендаря);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция РегламентныеЗаданияНастраиваемыеИзФормы()
	
	Результат = Новый Структура;
	Результат.Вставить("ФормированиеЗаявокПоГрафикамОплаты");
	Результат.Вставить("ОповещениеОПриближенииСрокаОплаты");
	Результат.Вставить("АвтоматическоеФормированиеПретензий");
	Результат.Вставить("ОповещениеОНеобходимостиИзмененияСтатусаПретензии");
	Результат.Вставить("ПереоценкаЗаявок");
	Результат.Вставить("ПереводИзлишковДенежныхСредств");
	Результат.Вставить("ЗагрузкаСпискаБанковОбменСБанками");
	Результат.Вставить("ЗагрузкаКурсовВалют");
	Результат.Вставить("ОтправкаНаСогласованиеЗаявокНаОперации");
	Результат.Вставить("ЗагрузкаСтавокЦБРФ");
	Результат.Вставить("АвтоматическоеСозданиеРеестровПлатежей");
	Результат.Вставить("ОповещениеОПросроченныхПлатежах");
	Результат.Вставить("РасчетДоверительныхКоэффициентов");
	Результат.Вставить("АктуализацияГрафиковРасчетов");
	Результат.Вставить("СинхронизацияСтатусовЗаявокНаРасходованиеДенежныхСредств");
	
	Возврат Результат;  
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗначениеСтавкиДисконтирования(СтавкаДисконтирования)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ЗначенияКотировокФИСрезПоследних.Значение
	|ИЗ
	|	РегистрСведений.ЗначенияКотировокФИ.СрезПоследних(&Период, ВидКотировки = &СтавкаДисконтирования) КАК ЗначенияКотировокФИСрезПоследних";

	Запрос.УстановитьПараметр("Период", ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("СтавкаДисконтирования", СтавкаДисконтирования);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Значение;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
КонецФункции

&НаСервере
Процедура УстановитьЗаголовокРедактированиеСтавкиДисконтирования()
	
	ЗначениеСтавки = ЗначениеСтавкиДисконтирования(НаборКонстант.СтавкаДисконтирования);
	ТекстЗаголовкаКоманды = НСтр("ru = 'Текущее значение: %1'");
	Элементы.РедактироватьСтавкуДисконтирования.Заголовок = СтрШаблон(ТекстЗаголовкаКоманды, ?(ЗначениеСтавки = Неопределено, НСтр("ru = '<не задано>'"), ЗначениеСтавки));
	
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьСтавкуДисконтированияЗавершение(Результат, Параметры) Экспорт
	
	УстановитьЗаголовокРедактированиеСтавкиДисконтирования();
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьПлатежныеДниНедели()
	
	Для ном = 1 По 7 Цикл
		ЭтотОбъект["ПлатежныйДеньНедели" + ном] = Ложь;
	КонецЦикла;
	
	ПлатежныеДниНедели = РегистрыСведений.ПлатежныеДниНедели.ПлатежныеДниНедели();
	Для каждого День Из ПлатежныеДниНедели Цикл
		ЭтотОбъект["ПлатежныйДеньНедели" + День] = Истина;
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура СохранитьПлатежныеДниНедели()
	
	НаборЗаписей = РегистрыСведений.ПлатежныеДниНедели.СоздатьНаборЗаписей();
	
	Для НомерДня = 1 По 7 Цикл
		ПлатежныйДень = ЭтотОбъект["ПлатежныйДеньНедели" + НомерДня];
		Если ПлатежныйДень Тогда
			Строка = НаборЗаписей.Добавить();
			Строка.НомерДняНедели = НомерДня;
		КонецЕсли;
	КонецЦикла;
	
	НаборЗаписей.Записать();
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьЗаполнениеУсловийФормированияЗаявок()
	
	Если  НаборКонстант.АвтоматическиАктуализироватьЗаявкиПоГрафикамДоговоров  
		И (НЕ НаборКонстант.СоздаватьЗаявкиНаОплатуПланируемыеПоступленияДС
		И НЕ НаборКонстант.СоздаватьЗаявкиНаРасходПланируемыеДоходы)	Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru = 'Регламентное задание ""Формирование заявок по графикам оплаты"" включено, но формирование заявок не выбрано'"),,"НаборКонстант.АвтоматическиАктуализироватьЗаявкиПоГрафикамДоговоров",,);
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура УстановитьЗаголовокПериодичностьГлубиныАнализа()

	ПараметрыБюджета = ОперативноеПланированиеПовтИспУХ.ПолучитьПараметрыОперПланирования(Перечисления.ПредназначенияЭлементовСтруктурыОтчета.БюджетДвиженияДенежныхСредств);
	ПериодичностьОперативногоПланирования = ПараметрыБюджета.ПериодичностьОперативногоПланирования;
	Элементы.НадписьПериодГлубиныАнализа.Заголовок = ПериодыУХ.ПросклонятьПериодичность(ПериодичностьОперативногоПланирования,
																						НаборКонстант.ГлубинаАнализаЭкстраполяцииДенежныхПотоков);
КонецПроцедуры

#Область Служебные_РеглЗадания

&НаКлиентеНаСервереБезКонтекста
Процедура ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(Форма, ИмяЗадания, ИзменитьЗадание = Ложь)
	
	Элементы = Форма.Элементы;
	
	Использование = Форма[ИмяЗадания];
	ИмяРасписание = "Расписание_" + ИмяЗадания;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "Настройки"+ИмяЗадания, "Доступность", Использование);
	
	Если ИзменитьЗадание Тогда
		ИзменитьЗадание(ИмяЗадания, Использование, Неопределено);
	КонецЕсли;
	
	Расписание = ПолучитьРасписаниеЗадания(ИмяЗадания);	
	УстановитьТекстНадписиРегламентнойНастройки(Элементы[ИмяРасписание], Использование, Расписание);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_УстановитьРасписаниеЗадания(РасписаниеЗадания, ДополнительныеПараметры) Экспорт
	
	Если РасписаниеЗадания = Неопределено Тогда
		Возврат;
	КонецЕсли;
		
	ИмяЗадания = ДополнительныеПараметры.ИмяЗадания;
	
	Использование = Неопределено;
	Если ИмяЗадания = "АктуализацияГрафиковРасчетов" Тогда
		Использование = Истина;	 
		ЭтотОбъект[ИмяЗадания] = Истина;
	КонецЕсли;

	ИзменитьЗадание(ИмяЗадания, Использование, РасписаниеЗадания);
	
	ИмяРасписание = "Расписание_" + ИмяЗадания;
	УстановитьТекстНадписиРегламентнойНастройки(Элементы[ИмяРасписание], ЭтотОбъект[ИмяЗадания], РасписаниеЗадания);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УстановитьТекстНадписиРегламентнойНастройки(Элемент, Использование, Расписание)
	
	Если Использование Тогда
		ПрефиксРасписания = НСтр("ru = 'Расписание: %1'");
	Иначе
		ПрефиксРасписания = НСтр("ru = 'Расписание (НЕ АКТИВНО): %1'");
	КонецЕсли;
	
	Элемент.Заголовок = СтрШаблон(ПрефиксРасписания, Строка(Расписание));

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНастройкуРасписания(ИмяЗадания)
	
	ДопПараметры = Новый Структура("ИмяЗадания", ИмяЗадания);
	ОписаниеОповещения = Новый ОписаниеОповещения("Подключаемый_УстановитьРасписаниеЗадания", ЭтотОбъект, ДопПараметры);
	
	Диалог = Новый ДиалогРасписанияРегламентногоЗадания(ПолучитьРасписаниеЗадания(ИмяЗадания));
	Диалог.Показать(ОписаниеОповещения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьРасписаниеЗадания(ИмяЗадания)
	
	ТекущееЗадание = РегламентныеЗадания.НайтиПредопределенное(ИмяЗадания);
	Возврат РегламентныеЗаданияСервер.РасписаниеРегламентногоЗадания(ТекущееЗадание);

КонецФункции

&НаСервере
Процедура УстановитьНастройкиЗаданий()
	
	УстановитьПривилегированныйРежим(Истина);
	
	РегламентныеЗаданияДляНастройки = РегламентныеЗаданияНастраиваемыеИзФормы();
	
	Для Каждого КлючЗначение Из РегламентныеЗаданияДляНастройки Цикл
		
		УстановитьНастройкиЗадания(КлючЗначение);
		
	КонецЦикла;
		                                        
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьНастройкиЗадания(КлючЗначение)
	
	ИмяЗадания = ?(ЗначениеЗаполнено(КлючЗначение.Значение), КлючЗначение.Значение, КлючЗначение.Ключ);
	
	Задание = Метаданные.РегламентныеЗадания.Найти(ИмяЗадания);
	ЭтотОбъект[ИмяЗадания] = РегламентныеЗаданияСервер.РегламентноеЗаданиеИспользуется(Задание);
	Элементы["Расписание_" + ИмяЗадания].Заголовок = РегламентныеЗаданияСервер.РасписаниеРегламентногоЗадания(Задание);
	ИзменитьДоступностьПодчиненныхНастроекРегламентногоЗадания(ЭтотОбъект, ИмяЗадания);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИзменитьЗадание(ИмяЗадания, Использование = Неопределено, Расписание = Неопределено)
	
	ИдентификаторЗадания = Метаданные.РегламентныеЗадания[ИмяЗадания];
	
	Если Расписание <> Неопределено Тогда
		РегламентныеЗаданияСервер.УстановитьРасписаниеРегламентногоЗадания(ИдентификаторЗадания, Расписание);
	КонецЕсли;

	Если ЗначениеЗаполнено(Использование) Тогда
		РегламентныеЗаданияСервер.УстановитьИспользованиеРегламентногоЗадания(ИдентификаторЗадания, Использование);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти










