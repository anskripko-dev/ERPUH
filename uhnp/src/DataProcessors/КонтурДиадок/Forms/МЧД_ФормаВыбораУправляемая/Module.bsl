////////////////////////////////////////////////////////////////////////////////
// ПЕРЕМЕННЫЕ МОДУЛЯ

// BSLLS:MissingVariablesDescription-off
// BSLLS:UnusedLocalVariable-off
&НаСервере
Перем _ОбработкаОбъект;

&НаСервере
Перем _МодульЯдроНаСервере;

&НаКлиенте
Перем _МодульПлатформаНаКлиенте;
// BSLLS:MissingVariablesDescription-on
// BSLLS:UnusedLocalVariable-on

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ ФОРМЫ

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Параметры.Объект.Свойство("ОбщийКонтекстКлиентСервер", Объект.ОбщийКонтекстКлиентСервер);
	
	ИнициализироватьФорму(Параметры);
	
КонецПроцедуры  

// Служебный метод для передачи параметров формы.
//
&НаСервере
Процедура ИнициализироватьФорму(ПараметрыФормы) Экспорт
	
	Модуль_Ядро = Модуль_ЯдроНаСервере();
	
	Заголовок								 = ПараметрыФормы.ЗаголовокФормы;
	ИдентификаторЯщика						 = ПараметрыФормы.ИдентификаторЯщика;
	ЗаголовокКнопкиДействия					 = ПараметрыФормы.ЗаголовокКнопкиДействия;
	ДействиеЭДО								 = ПараметрыФормы.ДействиеЭДО;
	КатегорияМетрикиДокумента				 = ПараметрыФормы.КатегорияМетрикиДокумента;
	TraceId									 = Модуль_Ядро.TraceId();
	КонтрагентПоддерживаетМЧД				 = ПараметрыФормы.КонтрагентПоддерживаетМЧД;
	
	ДопПеременныеМетрик = Неопределено;
	Если ПараметрыФормы.Свойство("ДопПеременныеМетрик", ДопПеременныеМетрик) Тогда
		Для Каждого ДопПеременная Из ДопПеременныеМетрик Цикл
			СписокДопПеременныхМетрик.Добавить(ДопПеременная.Ключ, ДопПеременная.Значение);
		КонецЦикла;
	КонецЕсли;
	
	ПредставлениеПодписанта = Модуль_Ядро.Подписант_ПредставлениеПодписанта(ИдентификаторЯщика);
	
	НастроитьЭлементыЭтойФормы();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Метрика_НаДействияСФормой(Истина);
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	
	Если ЗавершениеРаботы = Истина Тогда
		Возврат;
	КонецЕсли;
	
	Метрика_НаДействияСФормой(Ложь);
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// СЕРВИСНЫЕ МЕТОДЫ

&НаСервере
Функция ОбработкаОбъект()
	
	Если _ОбработкаОбъект = Неопределено Тогда
		_ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	КонецЕсли;
	
	Возврат _ОбработкаОбъект;
	
КонецФункции

// Возвращает форму для вызова сервера без контекста вызывающей формы
// 
// Возвращаемое значение:
//  ФормаКлиентскогоПриложения
//
&НаКлиенте
Функция Модуль_Платформа() Экспорт
	
	Если _МодульПлатформаНаКлиенте = Неопределено Тогда
		_МодульПлатформаНаКлиенте = ВладелецФормы.Модуль_Платформа();
	КонецЕсли;
	
	Возврат _МодульПлатформаНаКлиенте;
	
КонецФункции

&НаСервере
Функция Модуль_ЯдроНаСервере()
	
	Если _МодульЯдроНаСервере = Неопределено Тогда
		Модуль = ОбработкаОбъект();
		_МодульЯдроНаСервере = Модуль.Модуль_Ядро();
	КонецЕсли;
	
	Возврат _МодульЯдроНаСервере;
	
КонецФункции

&НаКлиенте
Функция Модуль_ЯдроНаКлиенте()
	
	Платформа = Модуль_Платформа();
	Результат = Платформа.Модуль_ЯдроНаКлиенте();
	
	Возврат Результат;
	
КонецФункции


////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ КОМАНД

&НаКлиенте
Процедура ВыполнитьДействие(Команда)
	
	Модуль_Платформа = Модуль_Платформа();
	Оповещение = Модуль_Платформа.НовыйОписаниеОповещения("ВыполнитьДействиеПослеПроверкиМЧД", ЭтаФорма);
	КонтрактВыбраннойМЧД = КонтекстМЧД.СписокМЧД.Получить(ВыбраннаяМЧД);
	ПараметрыДляПроверкиМЧД = Модуль_Платформа.ПараметрыДляПроверкиМЧД(
		Оповещение,
		ИдентификаторЯщика,
		КонтрактВыбраннойМЧД,
		ДействиеЭДО
	);
	
	Для Каждого ДопПеременная Из СписокДопПеременныхМетрик Цикл
		ПараметрыДляПроверкиМЧД.ДопПеременныеМетрик.Вставить(ДопПеременная.Значение, ДопПеременная.Представление);
	КонецЦикла;
	
	ДопПеременные = Метрика_ОбщиеДополнительныеПараметры(ПараметрыДляПроверкиМЧД);
	
	Ядро = Модуль_ЯдроНаКлиенте();
	ДействияЭДО = Ядро.Перечисление_ДействияЭДО();
	НазванияДействийЭДО = Ядро.Метрика_НазваниеДействий();

	Если ДействиеЭДО = ДействияЭДО.ЗапроситьУточнение Тогда
		ДействиеМетрики = НазванияДействийЭДО.ЗапроситьУточнение;
	ИначеЕсли ДействиеЭДО = ДействияЭДО.Аннулировать Тогда
		ДействиеМетрики = НазванияДействийЭДО.Аннулировать;	
	ИначеЕсли ДействиеЭДО = ДействияЭДО.ОтказатьВАннулировании Тогда
		ДействиеМетрики = НазванияДействийЭДО.ОтказатьВАннулировании;
	Иначе
		ДействиеМетрики = ЗаголовокКнопкиДействия;
	КонецЕсли;
	
	КатегорияМетрики = КатегорияМетрикиДокумента;
	НазваниеФормыМетрики = НазваниеФормы(); 
	ЛейблМетрики = НазванияДействийЭДО.НажатиеКнопки;
	ПредставлениеМетрики = ПредставлениеНазванияФормы();
	
	Метрика_ДобавитьПоведение_НажатиеКнопки(
		НазваниеФормыМетрики,
		КатегорияМетрики,
		ДействиеМетрики,
		ЛейблМетрики,
		ПредставлениеМетрики,
		TraceId
	);
	Метрика_ДобавитьСтатистику_ДляОрганизации(
		ИдентификаторЯщика,
		КатегорияМетрики,
		ДействиеМетрики,
		,
		ДопПеременные,
		ПредставлениеМетрики,
		TraceId
	);
	
	Если КонтрагентПоддерживаетМЧД Тогда
		Модуль_Платформа.ПроверитьМЧДИВыполнитьОповещение(ПараметрыДляПроверкиМЧД);
	Иначе
		РезультатВыбораМЧД = Модуль_Платформа.РезультатВыбораМЧД(Истина, Неопределено);
		Модуль_Платформа.ВыполнитьОбработкуОповещенияЛок(Оповещение, РезультатВыбораМЧД);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьДействиеПослеПроверкиМЧД(Результат, ДополнительныеПараметры) Экспорт
	
	Ядро = Модуль_ЯдроНаКлиенте();
	РазрешитьДействие = Ядро.СвойствоСтруктуры(Результат, "РазрешитьДействие");
	
	Если РазрешитьДействие Тогда
		Закрыть(Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть(Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура НастроитьМЧД(Команда)
	
	Метрика_НастроитьМЧД();
	
	Модуль_Платформа = Модуль_Платформа();
	Модуль_Платформа.ОткрытьФормуСпискаДоверенностей(ИдентификаторЯщика);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбраннаяМЧДПриИзменении(Элемент) 
	
	Метрика_ВыбраннаяМЧДПриИзменении();

КонецПроцедуры

&НаКлиенте
Процедура ВыбраннаяМЧДОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если ЗначениеЗаполнено(ВыбраннаяМЧД) Тогда
		КонтрактМЧД = КонтекстМЧД.СписокМЧД.Получить(ВыбраннаяМЧД);
		Модуль_Платформа = Модуль_Платформа();
		Модуль_Платформа.МЧД_ПоказатьСведенияОДоверенности(КонтрактМЧД, ЭтаФорма);
		Метрика_ПоказатьСведенияОДоверенности();
	КонецЕсли;

КонецПроцедуры


////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

&НаСервере
Процедура НастроитьЭлементыЭтойФормы()

	Модуль_Ядро = Модуль_ЯдроНаСервере();
	КоллекцияКартинок = Модуль_Ядро.БиблиотекаКартинок();
	
	КартинкаСертификата = КоллекцияКартинок.КартинкаЛокальныйСертификат;
	АдресКартинкиПодписанта = ПоместитьВоВременноеХранилище(КартинкаСертификата, УникальныйИдентификатор);
	
	Элементы.ВыполнитьДействие.Заголовок = ЗаголовокКнопкиДействия;
	
	НастроитьМЧДНаФорме();
	
КонецПроцедуры

&НаСервере
Процедура НастроитьМЧДНаФорме()
	
	Если КонтрагентПоддерживаетМЧД Тогда
		ЗаполнитьСписокДоступныхМЧД(ИдентификаторЯщика);
		Элементы.ГруппаКонтрагентНеПоддерживаетМЧД.Видимость = Ложь;
	Иначе
		ЗаполнитьСписокДоступныхМЧД("");
		Элементы.ГруппаМЧД.Видимость = Ложь;
		Элементы.ГруппаКонтрагентНеПоддерживаетМЧД.Видимость = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьСписокДоступныхМЧД(BoxID)
	
	КонтекстМЧД			 = Модуль_ЯдроНаСервере().МЧД_ДанныеПоИспользованиюДоверенностей(BoxID);
	СписокВыбораМЧД		 = Элементы.ВыбраннаяМЧД.СписокВыбора;
	
	Для Каждого КлючЗначение Из КонтекстМЧД.СписокМЧД Цикл
		ДанныеМЧД = КлючЗначение.Значение;
		СписокВыбораМЧД.Добавить(ДанныеМЧД.Идентификатор, ДанныеМЧД.ПредставлениеМЧД);
	КонецЦикла;
	
	ВыбраннаяМЧД = КонтекстМЧД.МЧД;
	
КонецПроцедуры

&НаКлиенте
Функция НазваниеФормы()
	
	Результат = "ФормаВыбораМЧД";
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПредставлениеНазванияФормы()

	Возврат "Форма выбора МЧД";

КонецФункции 

&НаКлиенте
Процедура Метрика_НаДействияСФормой(ЭтоОткрытиеФормы)
	
	Ядро = Модуль_ЯдроНаКлиенте();
	ДопПеременные = Метрика_ОбщиеДополнительныеПараметры();
	
	Если ЭтоОткрытиеФормы Тогда
		ДействиеМетрики = Ядро.Метрика_НазваниеДействий().ОткрытиеФормы;
	Иначе
		ДействиеМетрики = Ядро.Метрика_НазваниеДействий().ЗакрытиеФормы;
	КонецЕсли;
	
	НазваниеФормыДляМетрики = НазваниеФормы();
	КатегорияМетрики		= Ядро.Метрика_НазваниеКатегории().ВыборДоверенности;
	ЛейблМетрики			= Ядро.Метрика_НазваниеДействий().НажатиеКнопки;
	ПредставлениеМетрики	= ПредставлениеНазванияФормы();
	
	Ядро.Метрика_ДобавитьПоведение_ДействиеСФормой(
		НазваниеФормыДляМетрики,
		КатегорияМетрики,
		ЭтоОткрытиеФормы,
		ЛейблМетрики,
		ПредставлениеМетрики,
		TraceId
	);
	Метрика_ДобавитьСтатистику_ДляОрганизации(
		ИдентификаторЯщика,
		КатегорияМетрики,
		ДействиеМетрики,
		,
		ДопПеременные,
		ПредставлениеМетрики,
		TraceId
	);
	
КонецПроцедуры

&НаКлиенте
Функция Метрика_ОбщиеДополнительныеПараметры(Знач ПараметрыДляПроверкиМЧД = Неопределено)
	
	Модуль_Платформа = Модуль_Платформа();
	
	Если НЕ ЗначениеЗаполнено(ПараметрыДляПроверкиМЧД) Тогда
		КонтрактВыбраннойМЧД = КонтекстМЧД.СписокМЧД.Получить(ВыбраннаяМЧД);
		ПараметрыДляПроверкиМЧД = Модуль_Платформа.ПараметрыДляПроверкиМЧД(
			Неопределено,
			ИдентификаторЯщика,
			КонтрактВыбраннойМЧД,
			ДействиеЭДО
		);
	КонецЕсли;
	
	Результат = Модуль_Платформа.Метрика_ДополнительныеПеременныеМЧД(ПараметрыДляПроверкиМЧД);
	
	Возврат Результат;
	
КонецФункции 

&НаКлиенте
Процедура Метрика_НастроитьМЧД()
	
	Ядро = Модуль_ЯдроНаКлиенте();
	
	КатегорияМетрики	= Ядро.Метрика_НазваниеКатегории().ВыборДоверенности;
	ДействиеМетрики		= Ядро.Метрика_НазваниеДействий().НастроитьДоверенности;
	НазваниеФормы		= НазваниеФормы();
	ЛейблМетрики		= Ядро.Метрика_НазваниеДействий().НажатиеКнопки;
	ПредставлениеМетрики= ПредставлениеНазванияФормы();
	ДопПеременные		= Метрика_ОбщиеДополнительныеПараметры();
	
	Метрика_ДобавитьПоведение_НажатиеКнопки(НазваниеФормы, КатегорияМетрики, ДействиеМетрики, ЛейблМетрики, ПредставлениеМетрики, TraceId);
	Метрика_ДобавитьСтатистику_ДляОрганизации(ИдентификаторЯщика, КатегорияМетрики, ДействиеМетрики, , ДопПеременные, ПредставлениеМетрики, TraceId);
	
КонецПроцедуры

&НаКлиенте
Процедура Метрика_ВыбраннаяМЧДПриИзменении()
	
	Ядро = Модуль_ЯдроНаКлиенте();
	
	КатегорияМетрики	= Ядро.Метрика_НазваниеКатегории().ВыборДоверенности;
	ДействиеМетрики		= Ядро.Метрика_НазваниеДействий().РедактироватьПоле;
	НазваниеФормы		= НазваниеФормы();
	ЛейблМетрики		= Ядро.Метрика_НазваниеДействий().НажатиеКнопки;
	ПредставлениеМетрики= ПредставлениеНазванияФормы();
	ДопПеременные		= Метрика_ОбщиеДополнительныеПараметры();
	
	Метрика_ДобавитьПоведение_НажатиеКнопки(НазваниеФормы, КатегорияМетрики, ДействиеМетрики, ЛейблМетрики, ПредставлениеМетрики, TraceId);
	Метрика_ДобавитьСтатистику_ДляОрганизации(ИдентификаторЯщика, КатегорияМетрики, ДействиеМетрики, , ДопПеременные, ПредставлениеМетрики, TraceId);
	
КонецПроцедуры

&НаКлиенте
Процедура Метрика_ПоказатьСведенияОДоверенности()
	
	Ядро = Модуль_ЯдроНаКлиенте();
	
	КатегорияМетрики = Ядро.Метрика_НазваниеКатегории().ВыборДоверенности;
	ДействиеМетрики	= Ядро.Метрика_НазваниеДействий().ПосмотретьДоверенность;
	НазваниеФормы = НазваниеФормы();
	ПредставлениеМетрики = ПредставлениеНазванияФормы();
	
	ДопПеременные = Новый Соответствие;
	ДопПеременные.Вставить("Рег.номер МЧД", ВыбраннаяМЧД);
	
	Метрика_ДобавитьПоведение_НажатиеКнопки(
		НазваниеФормы,
		КатегорияМетрики,
		ДействиеМетрики,
		,
		ПредставлениеМетрики,
		TraceId
	);
	Метрика_ДобавитьСтатистику_ДляОрганизации(
		ИдентификаторЯщика,
		КатегорияМетрики,
		ДействиеМетрики,
		,
		ДопПеременные,
		ПредставлениеМетрики,
		TraceId
	);
	
КонецПроцедуры

&НаКлиенте
Процедура Метрика_ДобавитьПоведение_НажатиеКнопки(
	НазваниеФормы,
	НазваниеКатегории,
	НазваниеКнопки,
	НазваниеМетки = "",
	ПредставлениеМетрики = "",
	TraceId = "")
	
	Модуль_ЯдроНаКлиенте().Метрика_ДобавитьПоведение_НажатиеКнопки(
		НазваниеФормы,
		НазваниеКатегории,
		НазваниеКнопки,
		НазваниеМетки,
		ПредставлениеМетрики,
		TraceId
	);
	
КонецПроцедуры

// BSLLS:NumberOfOptionalParams-off
&НаКлиенте
Процедура Метрика_ДобавитьСтатистику_ДляОрганизации(
	ИдентификаторЯщика,
	КатегорияМетрики,
	ДействиеМетрики,
	НазваниеМетки = Неопределено,
	ДопПеременные = Неопределено,
	ПредставлениеМетрики = "",
	TraceId = "")
	
	Модуль_ЯдроНаКлиенте().Метрика_ДобавитьСтатистику_ДляОрганизации(
		ИдентификаторЯщика,
		КатегорияМетрики,
		ДействиеМетрики,
		НазваниеМетки,
		ДопПеременные,
		ПредставлениеМетрики,
		TraceId
	);
	
КонецПроцедуры
// BSLLS:NumberOfOptionalParams-on


