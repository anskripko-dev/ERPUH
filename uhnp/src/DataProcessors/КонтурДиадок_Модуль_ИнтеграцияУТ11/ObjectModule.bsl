
#Область ПЕРЕМЕННЫЕ_ПЛАТФОРМЫ
// BSLLS:MissingVariablesDescription-off
// BSLLS:UnusedLocalVariable-off
Перем Манифест 				Экспорт;
Перем ОбработкаОбъект 		Экспорт;
Перем ПараметрыКлиентСервер Экспорт;

Перем НомерИтерацииВызоваМодуля;
// BSLLS:MissingVariablesDescription-on
// BSLLS:UnusedLocalVariable-on
#КонецОбласти

#Область ПРОЦЕДУРЫ_И_ФУНКЦИИ_ПЛАТФОРМЫ

Функция МетодСервера(ИмяМодуля = "", ИмяМетода,
		Параметр0 = NULL, Параметр1 = NULL, Параметр2 = NULL, Параметр3 = NULL, Параметр4 = NULL,
		Параметр5 = NULL, Параметр6 = NULL, Параметр7 = NULL, Параметр8 = NULL, Параметр9 = NULL)
	
	Возврат ОбработкаОбъект.МетодСервера(ИмяМодуля, ИмяМетода,
	Параметр0, Параметр1, Параметр2, Параметр3, Параметр4, 
	Параметр5, Параметр6, Параметр7, Параметр8, Параметр9);
	
КонецФункции

Процедура Инициализировать(ИмяМодуля) Экспорт
	
	Если НомерИтерацииВызоваМодуля = Неопределено Тогда
		НомерИтерацииВызоваМодуля= 0;
	КонецЕсли;
	
	НомерИтерацииВызоваМодуля= НомерИтерацииВызоваМодуля + 1;
	
	Если Манифест = Неопределено Тогда
		ОбработкаОбъект.ЗаполнитьМанифест(ЭтотОбъект, ИмяМодуля);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОчиститьПеременные() Экспорт
	
	// Разрывается циклическая ссылка, для того чтобы исключить утечку памяти.
	// Модуль может быть вызван повторно во вложенных методах,
	// поэтому очищаем переменные, ТОЛЬКО если это начальная итерация вызова модуля.
	
	НомерИтерацииВызоваМодуля= НомерИтерацииВызоваМодуля - 1;
	
	Если НомерИтерацииВызоваМодуля = 0 Тогда
		ОбработкаОбъект= 	   Неопределено;
		ПараметрыКлиентСервер= Неопределено;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриглашениеОтладки() Экспорт
	
	//// Удалите комментарий напротив оператора "ВызватьИсключение", чтобы получить приглашение для начала отладки модуля.
	//ВызватьИсключение "Нажмите кнопку ""Подробно"", а затем ""Конфигуратор"", чтобы начать отладку модуля!"; 
	
КонецПроцедуры

#КонецОбласти

#Область МАНИФЕСТ

Функция ФункцииМодуля() Экспорт
	
	СтруктураМетодов = Новый Структура;
	
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьТекстЗапросаДокументыОснованияСчетаФактурыВыданного");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьВидКИ");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьДанныеТЧ");
	
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "СформироватьМетуСчетаНаОплату");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "СформироватьМетуАктаВыполненныхРабот");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "СформироватьМетуАктаСверки");
	
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПодготовитьВТДляСпискаОтправкиДокументов");
	
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьМассивСсылокРТУПоСчетуНаОплату");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьТаблицуНакладныхПоСчетуФактуреВыданному");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПодготовитьДопДанныеДляСозданияНакладной");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьСсылкуСчетаФактурыВыданногоПоДокументуОснованию");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьДокОснованиеКорректировкиРеализации");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьСчетНаОплатуНаОснованииРТУ");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьЗаказПокупателяНаОснованииРТУ");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьСФОснованиеПоИСФ_КСФ");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ВалютаРегламентированногоУчета", , "НаВремяСеанса");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "РезультатВизуализацииСчетаНаОплатуСредствамиДиадок");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПараметрыСчетаФактуры");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПараметрыСчетаНаОплату");
		
	// Признаки
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоСчетНаОплату");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоЗаказПокупателя");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоКорректировкаРеализации");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоКСФ");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоИсправление");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоНакладная");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "СчетФактуранаАванс");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоВозвратТоваровОтКлиента");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ЭтоВыбытиеИзОборотаМаркируемойПродукции");
	
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "ПолучитьОбстоятельствоФормированияСчетФактуры");

	// Машиночитаемые доверенности
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "МЧД_ИмяСправочникаВИнформационнойБазе");
	ОбработкаОбъект.ДобавитьФункциюВМанифест(СтруктураМетодов, "МЧД_ДанныеДоверенности");
	
	Возврат СтруктураМетодов;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Вызывается из Модуль_ИнтеграцияУниверсальный.ОписаниеПрикладногоДокумента
//
// Параметры:
//  ТребуемыеРеквизиты	 - Структура	 - коллекция описаний полей запроса: ключ - имя поля, значение - выражение.
//  СсылкаНаОбъект		 - ЛюбаяСсылка	 - ссылка на объект, описание которого надо получить.
//
Процедура ПриПолученииОписанияПрикладногоДокумента(ТребуемыеРеквизиты, СсылкаНаОбъект) Экспорт
	
	МетаданныеОбъекта = СсылкаНаОбъект.Метаданные();
	ПсевдонимОсновнойТаблицы = "";
	
	Если МетаданныеОбъекта = Метаданные.Документы.КорректировкаРеализации Тогда
		
		ТребуемыеРеквизиты.ЭтоИсправление = ТекстЗапроса_КРТУ_ЭтоИсправление(ПсевдонимОсновнойТаблицы);
		ТребуемыеРеквизиты.ЭтоКорректировка = ТекстЗапроса_КРТУ_ЭтоКорректировка(ПсевдонимОсновнойТаблицы);
		
	ИначеЕсли МетаданныеОбъекта = Метаданные.Документы.СчетФактураВыданный Тогда
		
		ТребуемыеРеквизиты.ЭтоИсправление = ТекстЗапроса_СчетФактура_ЭтоИсправление(ПсевдонимОсновнойТаблицы);
		ТребуемыеРеквизиты.ЭтоКорректировка = ТекстЗапроса_СчетФактура_ЭтоКорректировка(ПсевдонимОсновнойТаблицы);
		
	ИначеЕсли МетаданныеОбъекта = Метаданные.Документы.СчетФактураВыданныйАванс Тогда
		
		ТребуемыеРеквизиты.ЭтоИсправление = ТекстЗапроса_СчетФактура_ЭтоИсправление(ПсевдонимОсновнойТаблицы);
		ТребуемыеРеквизиты.ЭтоКорректировка = ТекстЗапроса_СчетФактураАванс_ЭтоКорректировка(ПсевдонимОсновнойТаблицы);
		
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ВидДокументаИнформационнойБазы
//
// Параметры:
//  ВидДокумента	 - Строка - (возвращаемый параметр) вид документа с точки зрения ЭДО (см. ВидыЭлектронныхДокументов);
//  СсылкаНаОбъект	 - ЛюбаяСсылка - объект информационной базы, вид которого необходимо определить;
//  ЭтоКорректировка - Булево - Истина, если объект отражает операцию корректировки;
//
Процедура ОпределитьВидДокумента(ВидДокумента, СсылкаНаОбъект, ЭтоКорректировка) Экспорт
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	МетаданныеОбъекта = СсылкаНаОбъект.Метаданные();
	МетаданныеДокументов = Метаданные.Документы;
	
	ЭтоСФ = ЭтоСчетФактура(МетаданныеОбъекта);
	
	Если ЭтоСФ И Не ЭтоКорректировка Тогда
		
		ВидДокумента = ВидыЭД.СчетФактура;
		
	ИначеЕсли ЭтоСФ И ЭтоКорректировка Тогда
		
		ВидДокумента = ВидыЭД.КорректировочныйСчетФактура;
		
	ИначеЕсли МетаданныеОбъекта = МетаданныеДокументов.РеализацияТоваровУслуг 
		Или (МетаданныеОбъекта = МетаданныеДокументов.КорректировкаРеализации И Не ЭтоКорректировка) Тогда
		
		ТолькоУслуги = ДокументРеализацииСодержитТолькоУслуги(СсылкаНаОбъект);
		
		Если ТолькоУслуги Тогда
			ВидДокумента = ВидыЭД.АктОбОказанииУслуг;
		Иначе
			ВидДокумента = ВидыЭД.ТоварнаяНакладная;
		КонецЕсли;
		
	ИначеЕсли МетаданныеОбъекта = МетаданныеДокументов.КорректировкаРеализации И ЭтоКорректировка Тогда
		
		ВидДокумента = ВидыЭД.СоглашениеОбИзмененииСтоимости;
		
	Иначе
		
		СоответствиеМетаданных = Новый Соответствие;
		СоответствиеМетаданных.Вставить(МетаданныеДокументаСверкаВзаиморасчетов(), ВидыЭД.АктСверки);
		СоответствиеМетаданных.Вставить(МетаданныеДокументов.СчетНаОплатуКлиенту, ВидыЭД.СчетНаОплату);
		СоответствиеМетаданных.Вставить(МетаданныеДокументов.АктВыполненныхРабот, ВидыЭД.АктОбОказанииУслуг);
		СоответствиеМетаданных.Вставить(МетаданныеДокументов.РеализацияУслугПрочихАктивов, ВидыЭД.АктОбОказанииУслуг);
		СоответствиеМетаданных.Вставить(МетаданныеДокументов.ВозвратТоваровПоставщику, ВидыЭД.ТоварнаяНакладная);
		СоответствиеМетаданных.Вставить(МетаданныеДокументов.ОтчетКомитенту, ВидыЭД.АктОбОказанииУслуг);
		
		Если ИспользуетсяОтчетДавальцу() Тогда
			СоответствиеМетаданных.Вставить(МетаданныеДокументов.ОтчетДавальцу, ВидыЭД.ТоварнаяНакладная);
		КонецЕсли;
		
		ВидДокумента = СоответствиеМетаданных.Получить(МетаданныеОбъекта);
		
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДатаНомерДокумента
//
// Параметры:
//  Результат		 	- Структура - (возвращаемый параметр) сведения о дате и номере документа;
//  ОписаниеДокумента	- Структура - параметры объекта ИБ (см. Модуль_ИнтеграцияУниверсальный.ОписаниеПрикладногоДокумента);
//  ЦифровойИндексОП 	- Число 	- цифровой индекс обособленного подразделения;
//
Процедура ЗаполнитьСведенияОДатеИНомереДокумента(Результат, ОписаниеДокумента, ЦифровойИндексОП) Экспорт

	СсылкаНаОбъект = ОписаниеДокумента.Ссылка;
	ЭтоИсправление = ОписаниеДокумента.ЭтоИсправление;

	ТребуемыеРеквизиты = Новый Структура;
	ТребуемыеРеквизиты.Вставить("Номер");
	ТребуемыеРеквизиты.Вставить("Дата");
	
	ТипСсылки = ТипЗнч(СсылкаНаОбъект);
	
	УчитыватьИсправлениеДокумента = ЭтоИсправление;
	
	ЕстьПредставлениеНомера = ОбщегоНазначения.ЕстьРеквизитОбъекта("ПредставлениеНомера", СсылкаНаОбъект.Метаданные());
	Если ЕстьПредставлениеНомера Тогда
		ТребуемыеРеквизиты.Вставить("ПредставлениеНомера");
	КонецЕсли;
	
	Если ТипСсылки = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		// Переопределить дату и номер документа
		ТребуемыеРеквизиты.Номер = "ДокументОснование.Номер";
		ТребуемыеРеквизиты.Дата	 = "ДокументОснование.Дата";
		
		УчитыватьИсправлениеДокумента = Ложь; // У КРТУ нет соответствующих реквизитов
		
	ИначеЕсли ТипСсылки = Тип("ДокументСсылка.СчетФактураВыданный")
		Или ТипСсылки = Тип("ДокументСсылка.СчетФактураВыданныйАванс") Тогда
		
		Если ЭтоИсправление Тогда
			
			// Переопределить дату и номер
			ТребуемыеРеквизиты.Номер = "ЕСТЬNULL(СчетФактураОснование.Номер, ""--"")";
			ТребуемыеРеквизиты.Дата	 = "ЕСТЬNULL(СчетФактураОснование.Дата, ДАТАВРЕМЯ(1, 1, 1))";
			
			// Добавить реквизиты исправления
			ТребуемыеРеквизиты.Вставить("НомерИсправления");
			ТребуемыеРеквизиты.Вставить("ДатаИсправления", "Дата");
			
		КонецЕсли;
		
	КонецЕсли;
	
	ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(СсылкаНаОбъект, ТребуемыеРеквизиты);
	
	Результат.ДатаДокумента = ЗначенияРеквизитов.Дата;
	
	Если ЕстьПредставлениеНомера Тогда
		Результат.НомерДокумента = ЗначенияРеквизитов.ПредставлениеНомера;
	Иначе
		Результат.НомерДокумента = ПредставлениеНомера(ЗначенияРеквизитов.Номер);
	КонецЕсли;
	
	Если УчитыватьИсправлениеДокумента Тогда
		Результат.НомерИсправления	 = Формат(ЗначенияРеквизитов.НомерИсправления, "ЧГ=0");
		Результат.ДатаИсправления	 = ЗначенияРеквизитов.ДатаИсправления;
	КонецЕсли;

	ДополнитьНомерЦифровымИндексомОП(Результат, ЦифровойИндексОП, СсылкаНаОбъект);

КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ЗаполнитьСведенияОДокументахПодтверждающихОтгрузку820
//
// Параметры:
//  ДокументыОбОтгрузке	- Массив - (возвращаемый параметр) Массив элементов "Новый_DocumentShipment";
//  СсылкаНаОбъект		- ДокументСсылка.СчетФактураВыданный - СФ для которой определяем реквизиты документов отгрузки.
//
Процедура ПриПолученииДанныхДокументовОбОтгрузке(Результат, СсылкаНаОбъект, СтандартнаяОбработка) Экспорт

КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДатаНомерДокумента
//
// Параметры:
//  Результат		 	- Структура - (возвращаемый параметр) сведения о дате и номере документа;
//  ОписаниеДокумента	- Структура - параметры объекта ИБ (см. Модуль_ИнтеграцияУниверсальный.ОписаниеПрикладногоДокумента);
//  ЦифровойИндексОП 	- Число 	- цифровой индекс обособленного подразделения;
//
Процедура ЗаполнитьСведенияОДатеИНомереКорректировочногоДокумента(Результат, ОписаниеДокумента, ЦифровойИндексОП) Экспорт

	СсылкаНаОбъект = ОписаниеДокумента.Ссылка;
	ЭтоИсправление = ОписаниеДокумента.ЭтоИсправление;

	ТипСсылки = ТипЗнч(СсылкаНаОбъект);
	
	ЕстьПредставлениеНомера = ОбщегоНазначения.ЕстьРеквизитОбъекта("ПредставлениеНомера", СсылкаНаОбъект.Метаданные());
	
	РеквизитыДокумента = Новый Структура;
	РеквизитыДокумента.Вставить("НомерДокумента", "Номер");
	РеквизитыДокумента.Вставить("ДатаДокумента"	, "Дата");
	
	Если ТипСсылки = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		РеквизитыДокумента.Вставить("НомерИсходногоДокумента", "ДокументОснование.Номер");
		РеквизитыДокумента.Вставить("ДатаИсходногоДокумента", "ДокументОснование.Дата");
		
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(СсылкаНаОбъект, РеквизитыДокумента);
		ЗаполнитьЗначенияСвойств(Результат, ЗначенияРеквизитов);
		
		ОписаниеРеквизитов = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_ДатаНомерДокументаОснования");
		ОписаниеРеквизитов.НомерИсходногоДокумента	 = ПредставлениеНомера(ЗначенияРеквизитов.НомерИсходногоДокумента);
		ОписаниеРеквизитов.ДатаИсходногоДокумента	 = ЗначенияРеквизитов.ДатаИсходногоДокумента;
		Результат.ДатаНомерДокументовОснований.Добавить(ОписаниеРеквизитов);
		
	ИначеЕсли ТипСсылки = Тип("ДокументСсылка.СчетФактураВыданный") Тогда
		
		Если ЭтоИсправление Тогда
			РеквизитыДокумента.Вставить("НомерДокумента", "СчетФактураОснование.Номер");
			РеквизитыДокумента.Вставить("ДатаДокумента"	, "СчетФактураОснование.Дата");
			РеквизитыДокумента.Вставить("НомерИсправления", "НомерИсправления");
			РеквизитыДокумента.Вставить("ДатаИсправления", "Дата");
		КонецЕсли;
		
		Если ЕстьПредставлениеНомера Тогда
			РеквизитыДокумента.Вставить("ПредставлениеНомера", "ПредставлениеНомера");
		КонецЕсли;
		
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(СсылкаНаОбъект, РеквизитыДокумента);
		ЗаполнитьЗначенияСвойств(Результат, ЗначенияРеквизитов);
		
		ДатаНомерДокументовОснований = РеквизитыКорректировочногоСчетаФактуры(СсылкаНаОбъект);
		Результат.ДатаНомерДокументовОснований = ДатаНомерДокументовОснований;
		
	Иначе
		
		ШаблонОшибки = "Не удалось получить реквизиты корректировочного документа %1!
		|Тип документа не поддерживается (%2)";
		
		ТекстОшибки = СтрШаблон(ШаблонОшибки, Строка(СсылкаНаОбъект), ТипСсылки);
		
		ЗаписьЖурналаРегистрации(ИмяСобытияЖурналаЗагрузка()
			, УровеньЖурналаРегистрации.Ошибка
			, СсылкаНаОбъект.Метаданные()
			, СсылкаНаОбъект
			, ТекстОшибки);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Если ЕстьПредставлениеНомера Тогда
		Результат.НомерДокумента = ЗначенияРеквизитов.ПредставлениеНомера;
	Иначе
		Результат.НомерДокумента = ПредставлениеНомера(ЗначенияРеквизитов.НомерДокумента);
	КонецЕсли;
	
	Результат.НомерДокумента	 = ?(ЗначениеЗаполнено(Результат.НомерДокумента)	, Результат.НомерДокумента		, "--");
	Результат.ДатаДокумента		 = ?(ЗначениеЗаполнено(Результат.ДатаДокумента)		, Результат.ДатаДокумента		, "--");
	
	Если ЭтоИсправление Тогда
		Результат.НомерИсправления	 =  Формат(Результат.НомерИсправления, "ЧГ=0");
		Результат.НомерИсправления	 = ?(ЗначениеЗаполнено(Результат.НомерИсправления)	, Результат.НомерИсправления, "--");
		Результат.ДатаИсправления	 = ?(ЗначениеЗаполнено(Результат.ДатаИсправления)	, Результат.ДатаИсправления, "--");
	Иначе
		Результат.НомерИсправления	 = "";
		Результат.ДатаИсправления	 = "";
	КонецЕсли;
	
	ДополнитьНомерЦифровымИндексомОП(Результат, ЦифровойИндексОП, СсылкаНаОбъект);
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДанныеШапки
//
// Параметры:
//  Реквизиты			 - Структура		 - коллекция описаний полей запроса: ключ - имя поля, значение - выражение.
//  МетаданныеИсточника	 - ОбъектМетаданных	 - Метаданные документа-источника
//
Процедура ПриПодготовкеЗапросаПоШапкеДокумента(Реквизиты, МетаданныеИсточника) Экспорт
	
	Реквизиты.Вставить("НалогообложениеНДС");
	
	Реквизиты.ВалютаДокумента			 = "Валюта";
	Реквизиты.ВалютаДокументаКод		 = "ВалютаКод";
	Реквизиты.КурсДокумента				 = "Курс";
	Реквизиты.КратностьДокумента		 = "Кратность";
	
	Если Не ЕстьРеквизитМетаданных(МетаданныеИсточника, "Курс") Тогда 
		Реквизиты.КурсДокумента		 = "1";
		Реквизиты.КратностьДокумента = "1";
	КонецЕсли;
	
	Если Не ЕстьРеквизитМетаданных(МетаданныеИсточника, "БанковскийСчетОрганизации") И ЕстьРеквизитМетаданных(МетаданныеИсточника, "БанковскийСчет") Тогда 
		Реквизиты.БанковскийСчетОрганизации = "БанковскийСчет";
	КонецЕсли;
	
	Если ЕстьРеквизитМетаданных(МетаданныеИсточника, "ЦенаВключаетНДС") Тогда 
		Реквизиты.ЦенаВключаетНДС = "ЦенаВключаетНДС";
	ИначеЕсли ЕстьРеквизитМетаданных(МетаданныеИсточника, "СуммаВключаетНДС") Тогда 
		Реквизиты.ЦенаВключаетНДС = "СуммаВключаетНДС";
	Иначе 
		Реквизиты.ЦенаВключаетНДС = "ЛОЖЬ";
	КонецЕсли;
	
	#Область ОбособленныеПодразделения
	
	Реквизиты.Вставить("Подразделение"); // Временное поле, требуется для определения ОП, невыделенного на отдельный баланс.
	
	Если НРег(МетаданныеИсточника.Имя) = НРег("СчетФактураВыданный") Тогда
		
		Реквизиты.Подразделение = "НЕОПРЕДЕЛЕНО";
		
	ИначеЕсли ЕстьРеквизитМетаданных(МетаданныеИсточника, "Склад") Тогда
		
		Реквизиты.Подразделение =
		"ВЫБОР
		|	КОГДА Склад.Подразделение ЕСТЬ NULL ИЛИ Склад.Подразделение = ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)
		|		ТОГДА Подразделение
		|	ИНАЧЕ Склад.Подразделение
		|КОНЕЦ";
		
	КонецЕсли;
	
	#КонецОбласти
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДанныеШапки
// после формирования структуры шапки документа.
//
// Параметры:
//  ЗначенияРеквизитов - Структура - Структура со значениями реквизита документа
//
Процедура ПослеПолученияДанныхШапкиДокумента(ДанныеШапки) Экспорт
	
	ОписаниеДокумента = ДанныеШапки.ОписаниеДокумента;
	СсылкаНаОбъект = ОписаниеДокумента.Ссылка;
	
	Если ЭтоСчетФактура(СсылкаНаОбъект) Тогда
		ДополнитьШапкуСчетаФактурыПоДокументуОснованию(ДанныеШапки);
	КонецЕсли;
	
	Если ЭтоСчетФактураКомиссионеру(СсылкаНаОбъект)
		ИЛИ ЭтоСчетФактураНаРеализациюЧерезКомиссионера(СсылкаНаОбъект)
		ИЛИ ЭтоСчетФактураНаРеализациюЧерезКомиссионера_25(СсылкаНаОбъект) Тогда
		ДополнитьШапкуСчетаФактурыКомиссионеру(ДанныеШапки);
	КонецЕсли;
	
	Если ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетНаОплатуКлиенту") Тогда
		ДополнитьШапкуСчетаНаОплатуПоДокументуОснованию(ДанныеШапки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШапки.НалогообложениеНДС)
		И ДанныеШапки.НалогообложениеНДС = ПредопределенноеЗначение("Перечисление.ТипыНалогообложенияНДС.ОблагаетсяНДСУПокупателя")
		Тогда
		ДанныеШапки.НДСИсчисляетсяНалоговымАгентом = Истина;
	Иначе
		ДанныеШапки.НДСИсчисляетсяНалоговымАгентом = Ложь;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДанныеШапки.БанковскийСчетОрганизации) Тогда
		ДанныеШапки.БанковскийСчетОрганизации = ПолучитьБанковскийСчет(СсылкаНаОбъект);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДанныеШапки.БанковскийСчетКонтрагента) Тогда
		ДанныеШапки.БанковскийСчетКонтрагента = ПолучитьБанковскийСчетПокупателя(СсылкаНаОбъект);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДанныеШапки.БанковскийСчетГрузоотправителя) Тогда
		ДанныеШапки.БанковскийСчетГрузоотправителя = ПолучитьБанковскийСчетГрузоотправителя(СсылкаНаОбъект);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДанныеШапки.БанковскийСчетГрузополучателя) Тогда
		ДанныеШапки.БанковскийСчетГрузополучателя = ПолучитьБанковскийСчетГрузополучателя(СсылкаНаОбъект);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДанныеШапки.НазначениеПлатежа) Тогда
		НомерОбъекта = ПредставлениеНомера(ОписаниеДокумента.Номер);
		ДанныеШапки.НазначениеПлатежа = Документы.СчетНаОплатуКлиенту.СформироватьНазначениеПлатежа(НомерОбъекта, СсылкаНаОбъект);
	КонецЕсли;
	
	#Область ОбособленныеПодразделения
	
	РегистрацияПодразделения = РегистрацияПодразделенияВНалоговомОргане(ДанныеШапки.Подразделение, ДанныеШапки.Организация, ОписаниеДокумента.Дата);
	
	Если ЗначениеЗаполнено(РегистрацияПодразделения) Тогда
		ДанныеШапки.ОбособленноеПодразделение = РегистрацияПодразделения;
	ИначеЕсли ЭтоОбособленноеПодразделение(ДанныеШапки.Организация) Тогда
		ДанныеШапки.ОбособленноеПодразделение = ДанныеШапки.Организация;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШапки.ОбособленноеПодразделение) Тогда
		
		ДанныеШапки.ЦифровойИндексОбособленногоПодразделения = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДанныеШапки.ОбособленноеПодразделение, "ЦифровойИндексОбособленногоПодразделения");
		
		Если НЕ ЗначениеЗаполнено(ДанныеШапки.Грузоотправитель) Тогда
			// При отгрузке от ОП грузоотправитель должен быть указан всегда (нельзя писать "он же")
			ДанныеШапки.Грузоотправитель = ДанныеШапки.ОбособленноеПодразделение;
		КонецЕсли;
		
	КонецЕсли;
	
	#КонецОбласти
	
	АктуализироватьРасчетыСКонтрагентами(ДанныеШапки);
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДанныеТабличныхЧастей()
//
// Параметры:
//  МетаданныеОбъекта	 - ОбъектМетаданных - объект метаданных.
//  ТабличныеЧасти		 - Массив - массив, в который необходимо добавить имена табличных частей, из которых нужно получить данные.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриПолученииИменТабличныхЧастей(МетаданныеОбъекта, ТабличныеЧасти, СтандартнаяОбработка) Экспорт
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДанныеТабличныхЧастей()
//
// Параметры:
//  Шапка				 - Структура - см. Модуль_ИнтеграцияУниверсальный.ДанныеШапки()
//  ИмяТабличнойЧасти	 - Строка	 - имя табличной части объекта.
//  РеквизитыТЧ			 - Структура, ФиксированнаяСтруктура - в качестве ключа передается
//                                     псевдоним поля, а в качестве значения (опционально) фактическое имя поля в таблице.
//                                     Если значение не определено, то имя поля берется из ключа.
//  ПараметрыЗапроса	 - Структура - дополнительные параметры, будут переданы в исполняемый запрос.
//
Процедура ПриПодготовкеЗапросаПоТоварнойЧастиДокумента(Шапка, ИмяТабличнойЧасти, РеквизитыТЧ, ПараметрыЗапроса) Экспорт
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ДанныеТабличныхЧастей()
//
// Параметры:
//  Шапка			 - Структура		 - см. Модуль_ИнтеграцияУниверсальный.ДанныеШапки.
//  ТабличнаяЧасть	 - ТаблицаЗначений	 - см. Модуль_ИнтеграцияУниверсальный.ДанныеТабличныхЧастей.
//
Процедура ПослеПолученияТоварнойЧастиДокумента(Шапка, ТабличнаяЧасть) Экспорт
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.РасчетноеПредставлениеНомера()
//
// Параметры:
//  НомерОбъекта		 - Строка - номер или код объекта, который преобразуется для вывода на печать.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриПолученииНомераНаПечать(НомерОбъекта, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	НомерОбъекта		 = ПредставлениеНомера(НомерОбъекта);
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ИмяКолонкиКодТовара.
//
// Параметры:
//  ИмяКолонки			 - Строка - имя реквизита номенклатуры, содержащего представление артикула.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриОпределенииДополнительнойКолонкиПечатныхФормДокументов(ИмяКолонки, СтандартнаяОбработка = Истина) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ИмяКолонки = ИмяДополнительнойКолонки();
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ВалютаРегламентированногоУчета.
//
// Параметры:
//  ВалютаРеглУчета		 - СправочникСсылка.Валюты - валюта регламентированного учета.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриОпределенииВалютыРегламентированногоУчета(ВалютаРеглУчета, СтандартнаяОбработка = Истина) Экспорт
	
	СтандартнаяОбработка = Ложь;
	ВалютаРеглУчета		 = ВалютаРегламентированногоУчета();
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.РеквизитыЮрФизЛица.
//
// Параметры:
//  Реквизиты			 - Структура - возвращаемые сведения.
//  ЮрФизЛицо			 - СправочникСсылка.Организации, СправочникСсылка.Контрагенты - компания, реквизиты которой нужно получить
//  Период				 - Дата - дата, на которую нужно получить значения периодических реквизитов
//  БанковскийСчет		 - СправочникСсылка.БанковскиеСчета - банковский счет, реквизиты которого нужно включить в реквизиты компании.
//                         Если не передан, будут включены реквизиты основного банковского счета.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриПолученииРеквизитовЮрФизЛица(Реквизиты, ЮрФизЛицо, Период, БанковскийСчет, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.РегистрацииВНалоговомОргане") Тогда // Это ОП без выделения на отдельный баланс
		
		ВерсияУТ = ВерсияТорговойПодсистемы();
		
		Если СравнитьВерсии(ВерсияУТ, "11.4") >= 0 Тогда 
			Буфер = Справочники.РегистрацииВНалоговомОргане.СведенияОПодразделении(ЮрФизЛицо, Период);
		Иначе 
			Буфер = Справочники.РегистрацииВНалоговомОргане.СведенияОПодразделении(ЮрФизЛицо);
		КонецЕсли;
		
		Буфер.Вставить("ЮрФизЛицо", Перечисления.ЮрФизЛицо.ЮрЛицо);
		Буфер.Вставить("Телефоны", "");
		
	Иначе // Это Контрагент или Организация
		
		Буфер = ФормированиеПечатныхФорм.СведенияОЮрФизЛице(ЮрФизЛицо, Период, Ложь, БанковскийСчет);
		
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(Реквизиты, Буфер);
	
	Если Буфер.ЮрФизЛицо = Перечисления.ЮрФизЛицо.ЮрЛицо Тогда
		Реквизиты.ТипЮрФизЛица = "ЮридическоеЛицо";
	ИначеЕсли Буфер.ЮрФизЛицо = Перечисления.ЮрФизЛицо.ЮрЛицоНеРезидент Тогда
		Реквизиты.ТипЮрФизЛица = "ИностраннаяКомпания";
	Иначе
		Реквизиты.ТипЮрФизЛица = "ФизическоеЛицо";
	КонецЕсли;
	
КонецПроцедуры

// Заполняет развернутую таблицу кодов маркировки штатными средствами конфигурации.
// В этой процедуре можно отключить стандартную обработку получения таблицы
// с помощью подсистемы БиблиотекаИнтеграцииИСМП.
//
// Параметры:
//  Результат - ТаблицаЗначений - Заполняемая таблица (см. МенеджерИнтеграции.НоваяРазвернутаяТаблицаУпаковок)
//  СсылкаНаДокумент - ЛюбаяСсылка - объект-владелец штрихкода
//  СтандартнаяОбработка - Булево - если установить Ложь, то заполнение таблицы кодов средствами
//                                  типовой библиотеки интеграции с ИС МП будет отключено.
//
Процедура ЗаполнитьРазвернутуюТаблицуКодов(Результат, СсылкаНаОбъект, СтандартнаяОбработка) Экспорт
	

	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.РеквизитыДоговора.
//
// Параметры:
//  Реквизиты			 - Структура - возвращаемые сведения.
//  Шапка				 - Структура - реквизиты шапки документа (см. Модуль_ИнтеграцияУниверсальный.ДанныеШапки).
//  СтандартнаяОбработка - Булево	 - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриПолученииРеквизитовДоговора(Реквизиты, Шапка, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ОписаниеДокумента = Шапка.ОписаниеДокумента;
	ДокументСсылка = ОписаниеДокумента.Ссылка;
	
	Основание = СтруктурированноеПредставлениеОснования(ДокументСсылка);
	
	Если Не ЗначениеЗаполнено(Основание)
		И ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.СчетНаОплатуКлиенту") Тогда 
		ОснованиеСчета = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "ДокументОснование");
		Основание = СтруктурированноеПредставлениеОснования(ОснованиеСчета);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Основание) Тогда
		Возврат;
	КонецЕсли;
	
	Реквизиты.Представление	 = Основание.Presentation;
	Реквизиты.Номер			 = Основание.GroundNumber;
	Реквизиты.Дата			 = Основание.GroundDate;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ЭтапыГрафикаОплатПоДокументу.
//
// Параметры:
//  ЭтапыОплаты			 - ТаблицаЗначений	 - возвращаемые сведения;
//  Шапка				 - Структура		 - реквизиты шапки документа (см. Модуль_ИнтеграцияУниверсальный.ДанныеШапки);
//  СтандартнаяОбработка - Булево			 - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриПолученииЭтаповГрафикаОплат(ЭтапыОплаты, Шапка, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ОписаниеДокумента = Шапка.ОписаниеДокумента;
	ДокументСсылка = ОписаниеДокумента.Ссылка;
	
	МассивОбъектов = Новый Массив;
	МассивОбъектов.Добавить(ДокументСсылка);
	
	ПараметрыПечати = Новый Структура;
	
	МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоСсылке(ДокументСсылка);
	ДанныеДляПечати = МенеджерОбъекта.ПолучитьДанныеДляПечатнойФормыСчетаНаОплату(ПараметрыПечати, МассивОбъектов);
	
	ТаблицаЭтаповОплаты = ДанныеДляПечати.РезультатПоЭтапамОплаты.Выгрузить();
	
	Для Каждого Стр Из ТаблицаЭтаповОплаты Цикл 
		ЗаполнитьЗначенияСвойств(ЭтапыОплаты.Добавить(), Стр);
	КонецЦикла;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ПредставлениеНоменклатуры.
//
// Параметры:
//  Представление			 - Строка - параметр, в который возвращается полученное представление номенклатуры;
//  Номенклатура			 - Строка, СправочникСсылка.Номенклатура				 - ссылка на номенклатуру;
//  Характеристика			 - Строка, СправочникСсылка.ХарактеристикиНоменклатуры	 - характеристика номенклатуры;
//  ДополнительныеПараметры	 - Неопределено, Структура								 - см. функцию Модуль_ИнтеграцияУниверсальный.ДополнительныеПараметрыПредставленияНоменклатуры;
//  СтандартнаяОбработка	 - Булево - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриПолученииПредставленияНоменклатуры(Представление, Номенклатура, Характеристика, ДополнительныеПараметры, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Если ТипЗнч(Номенклатура) = Тип("Строка") Тогда 
		
		НоменклатураНаименование = СокрЛП(Номенклатура);
												
	ИначеЕсли ЗначениеЗаполнено(Номенклатура) Тогда
										
		РеквизитыНоменклатуры	 = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Номенклатура, "НаименованиеПолное, Наименование");
		НоменклатураНаименование = ?(ЗначениеЗаполнено(РеквизитыНоменклатуры.НаименованиеПолное)
										, РеквизитыНоменклатуры.НаименованиеПолное
										, РеквизитыНоменклатуры.Наименование);
										
	Иначе
		
		НоменклатураНаименование = "";
		
	КонецЕсли;
	
	Если ТипЗнч(Характеристика) = Тип("Строка") Тогда 
		
		ХарактеристикаНаименование	 = СокрЛП(Характеристика);
		
	ИначеЕсли ЗначениеЗаполнено(Характеристика) Тогда 
		
		РеквизитыХарактеристики		 = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Характеристика, "НаименованиеПолное, Наименование");
		ХарактеристикаНаименование	 = ?(ЗначениеЗаполнено(РеквизитыХарактеристики.НаименованиеПолное)
											, РеквизитыХарактеристики.НаименованиеПолное
											, РеквизитыХарактеристики.Наименование);
		
	Иначе 
		ХарактеристикаНаименование = "";
	КонецЕсли;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Если СравнитьВерсии(ВерсияУТ, "11.3") > 0 Тогда 
		
		Представление = НоменклатураКлиентСервер.ПредставлениеНоменклатурыДляПечати(НоменклатураНаименование
																				  , ХарактеристикаНаименование
																				  , ДополнительныеПараметры.Упаковка
																				  , ДополнительныеПараметры.Серия
																				  , ДополнительныеПараметры);
		
	Иначе 
		
		Представление = НоменклатураКлиентСервер.ПредставлениеНоменклатурыДляПечати(НоменклатураНаименование
																				  , ХарактеристикаНаименование
																				  , ДополнительныеПараметры.Упаковка
																				  , ДополнительныеПараметры.Серия
																				  , ДополнительныеПараметры.Содержание
																				  , ДополнительныеПараметры.ВозвратнаяТара);
		
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ПараметрыУчетаНоменклатуры.
//
// Параметры:
//  ПараметрыНоменклатуры	 - Структура - Коллекция параметров учета номенклатуры в текущей ИБ;
//  СтандартнаяОбработка	 - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЗаполненииПараметровУчетаНоменклатуры(ПараметрыНоменклатуры, СтандартнаяОбработка) Экспорт
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ХранилищеКонтактнойИнформацииОбъекта.
//
// Параметры:
//  ХранилищеКИ			 - ЛюбаяСсылка	 - место хранения контактной информации;
//  СсылкаНаОбъект		 - ЛюбаяСсылка	 - объект-владелец контактной информации;
//  СтандартнаяОбработка - Булево		 - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриОпределенииМестаХраненияКонтактнойИнформацииОбъекта(ХранилищеКИ, СсылкаНаОбъект, СтандартнаяОбработка) Экспорт
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ВидыИсходящихДокументов.
//
// Параметры:
//  ВидыДокументов - ТаблицаЗначений - см. функцию Модуль_ИнтеграцияУниверсальный.ВидыИсходящихДокументов;
//
Процедура ПриПолученииВидовИсходящихДокументов(ВидыДокументов) Экспорт
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	
	ПсевдонимТаблицы = "ОсновнаяТаблица";
	
	КРТУ_ЭтоКорректировка = ТекстЗапроса_КРТУ_ЭтоКорректировка(ПсевдонимТаблицы);
	КРТУ_ЭтоНеКорректировка = "НЕ " + КРТУ_ЭтоКорректировка; 
	
	СФ_ЭтоКорректировка = ТекстЗапроса_СчетФактура_ЭтоКорректировка(ПсевдонимТаблицы);
	СФ_ЭтоНеКорректировка = "НЕ " + СФ_ЭтоКорректировка;
	
	СФАванс_ЭтоКорректировка = ТекстЗапроса_СчетФактураАванс_ЭтоКорректировка(ПсевдонимТаблицы);
	СФАванс_ЭтоНеКорректировка = "НЕ " + СФАванс_ЭтоКорректировка;
	
	СФКомиссионеру_ЭтоКорректировка = ТекстЗапроса_СчетФактураКомиссионеру_ЭтоКорректировка(ПсевдонимТаблицы);
	СФКомиссионеру_ЭтоНеКорректировка = "НЕ " + СФКомиссионеру_ЭтоКорректировка;
	
	СФНалоговыйАгент_ЭтоКорректировка = ТекстЗапроса_СчетФактураНалоговыйАгент_ЭтоКорректировка(ПсевдонимТаблицы);
	СФНалоговыйАгент_ЭтоНеКорректировка = "НЕ " + СФНалоговыйАгент_ЭтоКорректировка;
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураВыданный
		, ВидыЭД.СчетФактура
		, СФ_ЭтоНеКорректировка);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураВыданныйАванс
		, ВидыЭД.СчетФактура
		, СФАванс_ЭтоНеКорректировка);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураКомиссионеру
		, ВидыЭД.СчетФактура
		, СФКомиссионеру_ЭтоНеКорректировка);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураНалоговыйАгент
		, ВидыЭД.СчетФактура
		, СФНалоговыйАгент_ЭтоНеКорректировка);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураНаНеподтвержденнуюРеализацию0
		, ВидыЭД.СчетФактура
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураВыданный
		, ВидыЭД.КорректировочныйСчетФактура
		, СФ_ЭтоКорректировка);
		
	Если ИспользуетсяКорректировкаСчетаФактурыНаАванс() Тогда 
		
		ДобавитьВидДокументаДляОбмена(ВидыДокументов
			, Метаданные.Документы.СчетФактураВыданныйАванс
			, ВидыЭД.КорректировочныйСчетФактура
			, СФАванс_ЭтоКорректировка);
		
	КонецЕсли;
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетНаОплатуКлиенту
		, ВидыЭД.СчетНаОплату
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.ЗаказКлиента
		, ВидыЭД.СчетНаОплату
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.РеализацияТоваровУслуг
		, ВидыЭД.СчетНаОплату
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.РеализацияТоваровУслуг
		, ВидыЭД.ТоварнаяНакладная
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.РеализацияТоваровУслуг
		, ВидыЭД.АктОбОказанииУслуг
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.РеализацияУслугПрочихАктивов
		, ВидыЭД.АктОбОказанииУслуг
		, Неопределено);
	
	Если ИспользуетсяОтчетДавальцу() Тогда
		
		ДобавитьВидДокументаДляОбмена(ВидыДокументов
			, Метаданные.Документы.ОтчетДавальцу
			, ВидыЭД.АктОбОказанииУслуг
			, Неопределено);
		
	КонецЕсли;
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.КорректировкаРеализации
		, ВидыЭД.ТоварнаяНакладная
		, КРТУ_ЭтоНеКорректировка);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.КорректировкаРеализации
		, ВидыЭД.АктОбОказанииУслуг
		, КРТУ_ЭтоНеКорректировка);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.КорректировкаРеализации
		, ВидыЭД.СоглашениеОбИзмененииСтоимости
		, КРТУ_ЭтоКорректировка);
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ВидыВходящихДокументов.
//
// Параметры:
//  ВидыДокументов - ТаблицаЗначений - см. функцию Модуль_ИнтеграцияУниверсальный.ВидыВходящихДокументов;
//
Процедура ПриПолученииВидовВходящихДокументов(ВидыДокументов) Экспорт
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	
	СоответствиеРеквизитовСФ = НовоеПравилоКонвертацииСвойствЭД();
	СоответствиеРеквизитовСФ.Дата	 = "Дата";
	СоответствиеРеквизитовСФ.Номер	 = "Номер";
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураПолученный
		, ВидыЭД.СчетФактура
		, Неопределено
		, СоответствиеРеквизитовСФ);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураПолученныйАванс
		, ВидыЭД.СчетФактура
		, Неопределено
		, СоответствиеРеквизитовСФ);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.ПриобретениеТоваровУслуг
		, ВидыЭД.ТоварнаяНакладная
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.ПриобретениеУслугПрочихАктивов
		, ВидыЭД.ТоварнаяНакладная
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.ПриобретениеТоваровУслуг
		, ВидыЭД.АктОбОказанииУслуг
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.ПриобретениеУслугПрочихАктивов
		, ВидыЭД.АктОбОказанииУслуг
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.СчетФактураПолученный
		, ВидыЭД.КорректировочныйСчетФактура
		, Неопределено);
	
	ДобавитьВидДокументаДляОбмена(ВидыДокументов
		, Метаданные.Документы.КорректировкаПриобретения
		, ВидыЭД.СоглашениеОбИзмененииСтоимости
		, Неопределено);
		
	Если МетодСервера(, "СуществуетОбъектМетаданных", "Документ.СчетФактураПолученныйНалоговыйАгент") Тогда
		
		ДобавитьВидДокументаДляОбмена(ВидыДокументов
			, Метаданные.Документы.СчетФактураПолученныйНалоговыйАгент
			, ВидыЭД.СчетФактура
			, Неопределено
			, СоответствиеРеквизитовСФ);
		
		ДобавитьВидДокументаДляОбмена(ВидыДокументов
			, Метаданные.Документы.СчетФактураПолученныйНалоговыйАгент
			, ВидыЭД.КорректировочныйСчетФактура
			, Неопределено
			, СоответствиеРеквизитовСФ);
		
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ПриПолученииВладельцаОбщихНастроекМодуля.
//
// Параметры:
//  СсылкаНаОбъект		 - ЛюбаяСсылка	 - объект, выступающий в качестве константы для хранения дополнительных свойств;
//  СтандартнаяОбработка - Булево		 - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриПолученииВладельцаОбщихНастроекМодуля(СсылкаНаОбъект, СтандартнаяОбработка) Экспорт
	
	// остается значение по умолчанию
	
КонецПроцедуры


#Область СопоставлениеНоменклатуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.СвойстваНоменклатурыИнформационнойБазы.
// Определяет свойства набора номенклатур информационной базы.
// Устанавливает обязательность использования характеристик и упаковок номенклатуры.
//
// Параметры:
//  НаборНоменклатуры - Массив из ОпределяемыйТип.НоменклатураБЭД - ссылки на номенклатуру ИБ, для которой требуется определить свойства.
//  СвойстваНоменклатурИБ - Соответствие - содержит:
//  * Ключ     - ОпределяемыйТип.НоменклатураБЭД - ссылка на номенклатуру ИБ.
//  * Значение - Структура                       - содержит свойства номенклатуры ИБ. См. Модуль_ИнтеграцияУниверсальный.НовыеСвойстваНоменклатурыИБ.
//
Процедура ПриОпределенииСвойствНоменклатурыИнформационнойБазы(Знач НаборНоменклатуры, СвойстваНоменклатурИБ) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Номенклатура.Ссылка КАК Номенклатура,
	|	ВидыНоменклатуры.ИспользоватьХарактеристики КАК ИспользоватьХарактеристики,
	|	Номенклатура.ИспользованиеХарактеристик КАК ИспользованиеХарактеристик,
	|	Номенклатура.ИспользоватьУпаковки КАК ИспользоватьУпаковки
	|ИЗ
	|	Справочник.Номенклатура КАК Номенклатура
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ВидыНоменклатуры КАК ВидыНоменклатуры
	|		ПО Номенклатура.ВидНоменклатуры = ВидыНоменклатуры.Ссылка
	|ГДЕ
	|	Номенклатура.Ссылка В(&НаборНоменклатуры)";
	
	Запрос.УстановитьПараметр("НаборНоменклатуры", НаборНоменклатуры);
	РезультатЗапроса = Запрос.Выполнить();
	
	ФОИспользоватьХарактеристикиНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьХарактеристикиНоменклатуры");
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Если Выборка.ИспользованиеХарактеристик = Перечисления.ВариантыИспользованияХарактеристикНоменклатуры.НеИспользовать Тогда
			ИспользоватьХарактеристики = Ложь;
		Иначе
			ВидНоменклатурыИспользоватьХарактеристики = Выборка.ИспользоватьХарактеристики;
			ИспользоватьХарактеристики = ФОИспользоватьХарактеристикиНоменклатуры И ВидНоменклатурыИспользоватьХарактеристики;
		КонецЕсли;
		ИспользоватьУпаковки = Выборка.ИспользоватьУпаковки;
		
		СвойстваНоменклатуры = НовыеСвойстваНоменклатурыИБ();
		СвойстваНоменклатуры.Вставить("ИспользоватьХарактеристики", ИспользоватьХарактеристики);
		СвойстваНоменклатуры.Вставить("ИспользоватьУпаковки"      , ИспользоватьУпаковки);
		
		СвойстваНоменклатурИБ.Вставить(Выборка.Номенклатура, СвойстваНоменклатуры);
		
	КонецЦикла;
	
КонецПроцедуры

// Выполняется при подборе недостающих данных о номенклатуре информационной базы по уже имеющимся,
// а также номенклатуре контрагента.
//
// Параметры:
//  НоменклатураКонтрагента - Структура - данные номенклатуры контрагента (см. НоваяНоменклатураКонтрагента)
//  НоменклатураИБ - Структура - данные о номенклатуре информационной базы (имеющиеся или требующие подбора).
//                               См. НоваяНоменклатураИнформационнойБазы.
//
Процедура ПриПодбореНоменклатурыИнформационнойБазы(Знач НоменклатураКонтрагента, НоменклатураИБ) Экспорт
	
	ЕдиницаИзмерения = ЕдиницаХраненияНоменклатуры(НоменклатураИБ.Номенклатура);
	Если ЗначениеЗаполнено(ЕдиницаИзмерения) Тогда
		НоменклатураИБ.Упаковка = ЕдиницаИзмерения;
	КонецЕсли;
	
КонецПроцедуры

// Выполяется при записи соответствия номенклатуры контрагента и номенклатуры информационной базы.
// Позволяет переопределить место хранения соответствия номенклатуры в текущей конфигурации.
//
// Параметры:
//  НоменклатураКонтрагента - Структура - данные номенклатуры контрагента (см. НоваяНоменклатураКонтрагента)
//  НоменклатураИБ - Структура - данные о номенклатуре информационной базы (имеющиеся или требующие подбора).
//                               См. НоваяНоменклатураИнформационнойБазы.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЗаписиСоответствияНоменклатуры(НоменклатураКонтрагента, НоменклатураИБ, СтандартнаяОбработка) Экспорт
	
	Если Не ИспользуетсяНоменклатураКонтрагентовБЭД() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

// Выполяется при чтении соответствия номенклатуры контрагента и номенклатуры информационной базы.
//
// Параметры:
//  Результат - Соответствие - (возвращаемый параметр) содержит элементы:
//    * Ключ - Строка - идентификатор номенклатуры контрагента;
//    * Значение - Структура - номенклатура информационной базы (см. НоваяНоменклатураИнформационнойБазы)
//  ВладелецНоменклатуры - ЛюбаяСсылка - Владелец номенклатуры (контрагент/партнер).
//  ИдентификаторыНоменклатуры - Массив из Строка - идентификаторы номенклатуры контрагентов.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЧтенииСоответствияНоменклатуры(Результат, ВладелецНоменклатуры, ИдентификаторыНоменклатуры, СтандартнаяОбработка) Экспорт
	
	Если Не ИспользуетсяНоменклатураКонтрагентовБЭД() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

// Выполняется при заполнении описания и взаимосвязей объектов, составляющих номенклатуру информационной базы.
//
// Параметры:
//  МетаданныеСопоставления - Структура - метаданные и взаимосвязь объектов, составляющих номенклатуру ИБ.
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЗаполненииМетаданныхСопоставленияНоменклатуры(МетаданныеСопоставления, СтандартнаяОбработка) Экспорт
	
КонецПроцедуры

// Запоняет варианты сопоставления номенклатурыИБ по полям НоменклатураКонтрагента
//
// Параметры:
//  Результат				 - Массив - Используется для заполнения контрактами НоваяНоменклатураИнформационнойБазы
//  НоменклатураКонтрагента	 - Структура - Структура полей см. Контракт_НоменклатураКонтрагента
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события.
//
Процедура ПриЗаполненииВариантовСопоставленияНоменклатуры(Результат, НоменклатураКонтрагента, СтандартнаяОбработка) Экспорт
	
КонецПроцедуры

Функция ИспользуетсяТипНомераТД()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Результат = СравнитьВерсии(ВерсияУТ, "11.5.10") >= 0;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ЗагрузкаДанных

Процедура ЗаполнитьСоответствиеПолейВходящегоДокумента(ОбъектМетаданных, ИменаПолей) Экспорт
	
	ВидДокумента = ОбъектМетаданных.Имя;
	
	ВидыСФ = Новый Структура;
	ВидыСФ.Вставить("СчетФактураПолученный");
	ВидыСФ.Вставить("СчетФактураПолученныйАванс");
	ВидыСФ.Вставить("СчетФактураПолученныйНалоговыйАгент");
	
	Если ВидыСФ.Свойство(ВидДокумента) Тогда
		
		ИменаПолей.Номер = "Номер";
		ИменаПолей.Дата	 = "ДатаСоставления";
		
	КонецЕсли;
	
КонецПроцедуры

// Заполняет таблицу правил обработки входящих электронных документов.
//
// Параметры:
//  ПравилаОбработки - ТаблицаЗначений - правила обработки документов (см. ПравилаОбработкиВходящихДокументов);
//
// Пример:
//	Если ПолучитьФункциональнуюОпцию("ВедетсяПроизводственнаяДеятельность") Тогда
//		
//		ДобавитьПравилоОбработкиДокументов(ПравилоОбработки
//			, "ПоступлениеВПереработку" // Описание правила задается в процедуре ПриПолученииПравилаОбработкиЭлектронногоДокумента
//			, "ТоварнаяНакладная" 		// Используется для накладных
//			, Ложь); 					// Для исправлений не используется, только для первичных документов
//		
//	КонецЕсли;
//
Процедура ЗаполнитьПравилаОбработкиВходящихДокументов(ПравилаОбработки) Экспорт
	
	ЗаполнитьПравилаОбработкиПервичныхДокументов(ПравилаОбработки);
	ЗаполнитьПравилаОбработкиСчетовФактур(ПравилаОбработки);
	ЗаполнитьПравилаОбработкиДокументовОРасхождении(ПравилаОбработки);
	ЗаполнитьПравилаОбработкиАктовСверки(ПравилаОбработки);
	ЗаполнитьПравилаОбработкиСчетовНаОплату(ПравилаОбработки);
	
КонецПроцедуры

// Обработчик получения правила отражения электронного документа в учетной системе.
// Вызывается из Модуль_ИнтеграцияУниверсальный.ПравилоОбработкиЭлектронныхДокументов.
//
// Параметры:
//  ПравилоОбработки	 - Структура - описание способа обработки электронного документа (см. ПравилоОбработкиЭлектронныхДокументов);
//  СтандартнаяОбработка - Булево - Истина, если специальная обработка не требуется,
//                                  а идентификатор совпадает с именем метаданных документа;
//
// Пример:
//
//	Если ПравилоОбработки.Идентификатор = "ПоступлениеВПереработку" Тогда
//		
//		СтандартнаяОбработка = Ложь;
//		
//		ДополнительныеПараметры = Новый Структура;
//		ДополнительныеПараметры.Вставить("ВидОперации", Перечисления.ВидыОперацийПоступлениеТоваровУслуг.ВПереработку);
//		
//		ПравилоОбработки.Представление = "Поступление материалов в переработку";
//		ПравилоОбработки.ОбъектМетаданных = Метаданные.Документы.ПоступлениеТоваровУслуг.ПолноеИмя();
//		ПравилоОбработки.ДополнительныеПараметры = ДополнительныеПараметры;
//		
//	КонецЕсли;
//
Процедура ПриПолученииПравилаОбработкиЭлектронногоДокумента(ПравилоОбработки, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.НовыйДокументИнформационнойБазы.
//
// Параметры:
//  ДокументОбъект		 - ДокументОбъект	 - новый, еще не записанный, документ-объект;
//  ДанныеЭД			 - Структура		 - содержит данные электронного документа
//                                             (см. Модуль_ИнтеграцияУниверсальный.ДанныеЭлектронногоДокумента);
//  ПараметрыЗаполнения	 - Структура		 - содержит дополнительные параметры заполнения;
//  СтандартнаяОбработка - Булево			 - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриСозданииДокументаИнформационнойБазы(ДокументОбъект, ДанныеЭД, ПараметрыЗаполнения, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	МетаданныеДокумента = ДокументОбъект.Метаданные();
	
	Если МетаданныеДокумента = Метаданные.Документы.ПриобретениеТоваровУслуг Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияПриобретенияТоваровУслуг(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьПриобретениеТоваровУслуг(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.ПриобретениеУслугПрочихАктивов Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияПриобретенияУслугПрочихАктивов(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьПриобретениеУслугПрочихАктивов(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.ВозвратТоваровОтКлиента Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияВозвратаТоваровОтКлиента(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьВозвратТоваровОтКлиента(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.КорректировкаПриобретения Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияКорректировкиПоступления(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьКорректировкуПоступления(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.СчетФактураПолученный Тогда
		
		ФорматЭД = ДанныеЭД.ЭлектронныйДокумент.Формат;
		
		Если ФорматЭД.ЭтоКорректировка Тогда
			СтруктураЗаполнения = СтруктураЗаполненияКорректировочногоСчетаФактуры(ДанныеЭД, ПараметрыЗаполнения);
		Иначе
			СтруктураЗаполнения = СтруктураЗаполненияСчетаФактуры(ДанныеЭД, ПараметрыЗаполнения);
		КонецЕсли;
		
		ЗаполнитьСчетФактуруПолученный(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.СчетФактураПолученныйАванс Тогда
		
		//ПроверитьВозможностьЗаполненияСчетаФактурыНаАванс(ДанныеЭД);
		Если Не ДанныеЭД.ЭтоАванс Тогда
			Сообщить("Электронный документ не является счетом-фактурой на аванс!");
			СтандартнаяОбработка = Истина;
			Возврат;
		КонецЕсли;
		
		СтруктураЗаполнения = СтруктураЗаполненияСчетаФактуры(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьСчетФактуруПолученныйНаАванс(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.АктОРасхожденияхПослеОтгрузки Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияАктаОРасхожденияхПослеОтгрузки(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьАктОРасхожденияхПослеОтгрузки(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = МетаданныеДокументаСверкаВзаиморасчетов() Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияСверкиВзаиморасчетов(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьСверкуВзаиморасчетов(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли ИспользуетсяСверкаВзаиморасчетов2_5_11()
		И МетаданныеДокумента = Метаданные.Документы.СверкаВзаиморасчетов2_5_11 Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияСверкиВзаиморасчетов2_5_11(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьСверкуВзаиморасчетов2_5_11(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.ЗаказПоставщику Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияЗаказаПоставщику(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьЗаказПоставщику(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.ЗаявкаНаРасходованиеДенежныхСредств Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияЗаявкиНаРасходованиеДС(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьЗаявкуНаРасходованиеДС(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.СписаниеБезналичныхДенежныхСредств Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияСписанияБезналичныхДС(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьСписаниеБезналичныхДС(ДокументОбъект, СтруктураЗаполнения);
		
	ИначеЕсли МетаданныеДокумента = Метаданные.Документы.РасходныйКассовыйОрдер Тогда
		
		СтруктураЗаполнения = СтруктураЗаполненияРасходногоКассовогоОрдера(ДанныеЭД, ПараметрыЗаполнения);
		ЗаполнитьРасходныйКассовыйОрдер(ДокументОбъект, СтруктураЗаполнения);
		
	Иначе
		
		// Специальная обработка заполнения не реализована
		СтандартнаяОбработка = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ИмяСправочникаВладельцаНоменклатуры.
//
// Параметры:
//  ИмяСправочника		 - Строка - имя справочника, которому подчинен справочник "НоменклатураПоставщиков";
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриОпределенииИмениСправочникаВладельцаНоменклатуры(ИмяСправочника, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	ИмяСправочника = "Партнеры";
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.МетаданныеПрикладногоСправочника.
//
// Параметры:
//  СоответствиеСправочников - Соответствие - ключом является имя справочника в терминах сервиса, а значением - метаданные прикладного справочника;
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриПолученииСоответствияСправочников(СоответствиеСправочников, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	СоответствиеСправочников.Вставить("Валюты"			, Метаданные.Справочники.Валюты);
	СоответствиеСправочников.Вставить("СтраныМира"		, Метаданные.Справочники.СтраныМира);
	
	СоответствиеСправочников.Вставить("Организации"				, Метаданные.Справочники.Организации);
	СоответствиеСправочников.Вставить("Контрагенты"				, Метаданные.Справочники.Контрагенты);
	СоответствиеСправочников.Вставить("Партнеры"				, Метаданные.Справочники.Партнеры);
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.СсылкаОбъектаПоПолямПоиска.
//
// Параметры:
//  СсылкаНаОбъект		 - Произвольный - ссылка на искомый объект;
//  ПоляПоиска			 - Структура - содержит параметры поиска;
//  ОбъектМетаданных	 - ОбъектМетаданных - метаданные искомого объекта;
//  СтандартнаяОбработка - Булево - Ложь, чтобы отключить стандартную обработку события;
//
// Пример:
//
//  НаименованиеПолное = Неопределено;
//  Если ПоляПоиска.Свойство("НаименованиеПолное", НаименованиеПолное) 
//      И Не ЕстьРеквизитМетаданных(ОбъектМетаданных, "НаименованиеПолное") 
//      И ЕстьРеквизитМетаданных(ОбъектМетаданных, "ПолноеНаименование") Тогда
//          
//          ПоляПоиска.Удалить("НаименованиеПолное");
//          ПоляПоиска.Вставить("ПолноеНаименование", НаименованиеПолное);
//          
//  КонецЕсли;
//
Процедура ПриПолученииСсылкиПоПолямПоиска(СсылкаНаОбъект, ПоляПоиска, ОбъектМетаданных, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.СчетаФактурыПоОснованию.
//
// Параметры:
//  НайденныеДокументы	 - Массив	 - Ссылки на найденные счета-фактуры;
//  ПараметрыРегистрации - Структура - см. Модуль_ИнтеграцияУниверсальный.ПараметрыРегистрацииСчетаФактуры();
//  СтандартнаяОбработка - Булево	 - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриПолученииСчетовФактурПоОснованию(НайденныеДокументы, ПараметрыРегистрации, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	ТаблицаСчетовФактур	 = Документы.СчетФактураПолученный.СчетаФактурыПоОснованию(ПараметрыРегистрации.Ссылка, , , Ложь);
	
	Если ЗначениеЗаполнено(ТаблицаСчетовФактур) Тогда 
		НайденныеДокументы = ТаблицаСчетовФактур.ВыгрузитьКолонку("Ссылка");
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из Модуль_ИнтеграцияУниверсальный.ПараметрыРегистрацииСчетаФактурыПоОснованию.
//
// Параметры:
//  ПараметрыРегистрации - Структура	 - см. Модуль_ИнтеграцияУниверсальный.ПараметрыРегистрацииСчетаФактуры();
//  ДокументОснование	 - ЛюбаяСсылка	 - документ, на основании которого необходимо получить параметры;
//  СтандартнаяОбработка - Булево		 - Ложь, чтобы отключить стандартную обработку события;
//
Процедура ПриПодготовкеПараметровРегистрацииСчетаФактуры(ПараметрыРегистрации, ДокументОснование, СтандартнаяОбработка) Экспорт
	
	
	
КонецПроцедуры

#КонецОбласти

#Область СборПротоконтента

Процедура ЗаполнитьКонтентXmlUTD(Content, ДокСсылка, ПараметрыФормирования) Экспорт
	
	ПараметрыФормированияXML = ПараметрыФормирования.ПараметрыФормированияФайла;
	Точность = ПараметрыФормированияXML.ТочностьЦены;
	
	ФункцияУПД = ПараметрыФормирования.ФункцияДокумента;
	
	ДанныеДляПечати = ПолучитьДанныеДляФормированияУПД(ДокСсылка, ФункцияУПД);
	ДанныеШапки     = ДанныеДляПечати.ДанныеШапки;
	ТаблицаТоваров  = ДанныеДляПечати.ТаблицаТоваров;
	ОписаниеДокумента = ДанныеШапки.ОписаниеДокумента;
	
	ДобавитьОшибкиВалидацииИзДанныхШапки(ДанныеШапки, Content);
	
	ЭтоИсправление = ОписаниеДокумента.ЭтоИсправление;
	ДатаНомер = ДанныеШапки.ДатаНомерДокумента;
	
	Content.Number 	= ДатаНомер.НомерДокумента;
	Content.Date 	= ДатаНомер.ДатаДокумента;
	
	Если ЭтоИсправление Тогда
		Content.RevisionNumber	 = ДатаНомер.НомерИсправления;
		Content.RevisionDate	 = ДатаНомер.ДатаИсправления;
	КонецЕсли;
	
	Если ДанныеШапки.Свойство("СчетФактураСсылка") Тогда
		Content.СчетФактураСсылка = ДанныеШапки.СчетФактураСсылка;
	КонецЕсли;
	
	Покупатель = ПолучитьГоловноеЮрлицо(ДанныеШапки.Контрагент);
	Заполнить_ExtendedOrganizationInfo(Content.Buyer, Покупатель, ДатаНомер.ДатаДокумента, Истина, ДанныеШапки.БанковскийСчетКонтрагента);
	
	Если ЗначениеЗаполнено(ПараметрыФормирования.Получатель_FnsParticipantId) Тогда
		Content.Buyer.FnsParticipantId = ПараметрыФормирования.Получатель_FnsParticipantId;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШапки.КонтрагентКПП) Тогда
		Content.Buyer.kpp =  ДанныеШапки.КонтрагентКПП;
	КонецЕсли;
	
	КППГрузоотправителя = "";
	
	Если ДанныеШапки.ЕстьТовары Тогда
	
		Если ЗначениеЗаполнено(ДанныеШапки.Грузоотправитель) И НЕ ТипЗнч(ДанныеШапки.Грузоотправитель) = Тип("Строка") Тогда
			ПодставлятьКППГрузоотправителя = МетодСервера(,"ПолучитьЗначениеСвойства", ДанныеШапки.Организация, МетодСервера(,"ИдентификаторСвойстваПодставлятьКППГрузоотправителя"));
			Если (НЕ ПодставлятьКППГрузоотправителя = Неопределено И ПодставлятьКППГрузоотправителя) Тогда
				КППГрузоотправителя = ?(ЗначениеЗаполнено(ДанныеШапки.Грузоотправитель.КПП), ДанныеШапки.Грузоотправитель.КПП, "");
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ДанныеШапки.Грузоотправитель) Тогда 
			Заполнить_ExtendedOrganizationInfo(Content.Shipper.OrganizationInfo, ДанныеШапки.Грузоотправитель, ДатаНомер.ДатаДокумента, Ложь, ДанныеШапки.БанковскийСчетГрузоотправителя, , ДанныеШапки.ОбособленноеПодразделение);
			Content.Shipper.SameAsSeller = Ложь;
		Иначе
			Content.Shipper.SameAsSeller = Истина;
		КонецЕсли;
		
		ГрузополучательСсылка = ?(ЗначениеЗаполнено(ДанныеШапки.Грузополучатель), ДанныеШапки.Грузополучатель, ДанныеШапки.Контрагент);
		Заполнить_ExtendedOrganizationInfo(Content.Consignee, ГрузополучательСсылка, ДатаНомер.ДатаДокумента, Ложь, ДанныеШапки.БанковскийСчетГрузополучателя);
							
	КонецЕсли;
	
	Продавец = ПолучитьГоловноеЮрлицо(ДанныеШапки.Организация);
	Заполнить_ExtendedOrganizationInfo(Content.Seller, Продавец, ДатаНомер.ДатаДокумента, Истина, ДанныеШапки.БанковскийСчетОрганизации, КППГрузоотправителя, ДанныеШапки.ОбособленноеПодразделение);
	
	Если ЗначениеЗаполнено(ПараметрыФормирования.Отправитель_FnsParticipantId) Тогда
		Content.Seller.FnsParticipantId = ПараметрыФормирования.Отправитель_FnsParticipantId;
	КонецЕсли;
	
	Content.Creator = Content.seller.name + ", ИНН/КПП " + МетодСервера(, "ПредставлениеИННКПП", Content.seller);
	Content.Function = ДанныеШапки.Функция;
	
	Если ДанныеШапки.Свойство("ДатаНомерДокументовОплаты") Тогда
		СтруктураPaymentDocuments= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_PaymentDocument");
		Для Каждого ПРД Из ДанныеШапки.ДатаНомерДокументовОплаты Цикл
			ЗаполненнаяСтруктураPaymentDocuments = СкопироватьСтруктуру(СтруктураPaymentDocuments);
			ЗаполненнаяСтруктураPaymentDocuments.Number = ПРД.НомерПлатежноРасчетногоДокумента;
			ЗаполненнаяСтруктураPaymentDocuments.Date   = ПРД.ДатаПлатежноРасчетногоДокумента;
			Content.PaymentDocuments.Добавить(ЗаполненнаяСтруктураPaymentDocuments);
		КонецЦикла;	
	КонецЕсли;
	
	Content.currency = ДанныеШапки.ВалютаДокумента.Код;
	Content.ВалютаСсылка = ДанныеШапки.ВалютаДокумента;
	
	ДанныеШапки.Свойство("ИдентификаторГосконтракта", Content.GovernmentContractInfo);
	
	ЗаполнитьStructedAdditionalInfos(Content.AdditionalInfoId.StructedAdditionalInfos, ПараметрыФормирования.ДополнительныеСведенияСтрокой);
	
	TotalNet = 0;
	Total 	 = 0;
	Vat 	 = 0;
	TotalWithVatExcluded = 0;
	
	СФНаАванс = ДанныеШапки.Свойство("СФНаАванс") И ДанныеШапки.СФНаАванс;
	
	ВсеСтроки_СтавкаНДС_БезНДС	 = Истина;
	ВсеСтроки_СтавкаНДС_ИсчНалАг = Истина;
	
	СтавкаНДС_БезНДС   = МетодСервера(, "СтавкаНДС_БезНДС");
	СтавкаНДС_ИсчНалАг = МетодСервера(, "СтавкаНДС_ИсчНалАг");
	
	ПересчитыватьВВалютуРегл = ПересчитыватьВВалютуРегламентированногоУчета(ДокСсылка);
	
	Для Каждого СтрокаДанных Из ТаблицаТоваров Цикл 
		
		Item = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_ExtendedInvoiceItem");
		ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Item, СтрокаДанных);
		Content.InvoiceTable.items.Добавить(Item);
		
		Item.Product 	=  СтрокаДанных.НоменклатураПредставление;
		Item.VendorCode =  СтрокаДанных.КодНоменклатуры;
		Item.ItemMark   =  СтрокаДанных.ПризнакТоварРаботаУслуга;
		Item.TaxRate = ПредставлениеСтавкиНДС(СтрокаДанных.СтавкаНДС);
		
		НДС_БезНДС	 = (НРег(Item.TaxRate) = НРег(СтавкаНДС_БезНДС));
		НДС_ИсчНалАг = (Item.TaxRate = СтавкаНДС_ИсчНалАг);
		
		Если НЕ НДС_БезНДС Тогда
			ВсеСтроки_СтавкаНДС_БезНДС = Ложь;
		КонецЕсли;
		Если НЕ НДС_ИсчНалАг Тогда
			ВсеСтроки_СтавкаНДС_ИсчНалАг = Ложь;
		КонецЕсли;
		
		// Сумма без НДС
		Если НДС_ИсчНалАг Тогда
			Item.SubtotalWithVatExcluded = СтрокаДанных.Сумма;
		ИначеЕсли НЕ СФНаАванс Тогда 	
			Item.SubtotalWithVatExcluded = СтрокаДанных.Сумма - ?(СтрокаДанных.СуммаВключаетНДС, СтрокаДанных.СуммаНДС, 0);	
		КонецЕсли;
		
		// Сумма НДС
		Если НДС_ИсчНалАг Тогда
			Item.Vat = 0;
		ИначеЕсли НЕ НДС_БезНДС Тогда 	
			Item.Vat = СтрокаДанных.СуммаНДС;
		КонецЕсли;
		
		// Сумма всего
		Если НДС_ИсчНалАг Тогда
			Item.Subtotal = 0;
		Иначе 	
			Item.Subtotal = СтрокаДанных.Сумма + ?(СтрокаДанных.СуммаВключаетНДС, 0, СтрокаДанных.СуммаНДС);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаДанных.Количество) Тогда
			
			Item.Quantity	 = ?(СФНаАванс И СтрокаДанных.Количество = 0, Неопределено, СтрокаДанных.Количество); //для отображения прочерка в вэбе
			
			ИсключитьИзЦеныНДС = ДанныеШапки.ЦенаВключаетНДС И ЗначениеЗаполнено(СтрокаДанных.СуммаНДС);
			ПересчитатьЦенуСУчетомСкидок = ЗначениеЗаполнено(СтрокаДанных.СуммаСкидок);
			
			Если ИсключитьИзЦеныНДС
				Или ПересчитыватьВВалютуРегл
				Или ПересчитатьЦенуСУчетомСкидок Тогда
				// Цены вычисляем расчетным путем, если нужно исключить НДС или пересчитать из валюты в рубли
				Item.Price = ?(ЗначениеЗаполнено(Item.Quantity), Окр(Item.SubtotalWithVatExcluded / Item.Quantity, Точность), Неопределено);
			Иначе
				// Без пересчета, т.к. цена уже без НДС
				Item.Price = Окр(СтрокаДанных.Цена, Точность);
			КонецЕсли;
			
			Если ЗначениеЗаполнено(СтрокаДанных.ЕдиницаИзмерения) Тогда
				
				Item.UnitName = СокрЛП(СтрокаДанных.ЕдиницаИзмерения);
				Item.UnitCode = МетодСервера(, "XML_КодЕдиницыИзмерения", СокрЛП(СтрокаДанных.КодЕдиницыИзмерения));
				Item.ЕдиницаИзмеренияССылка = СтрокаДанных.ЕдиницаИзмерения;
				
			КонецЕсли;
			
		КонецЕсли;
		
		TotalNet = TotalNet + МетодСервера(, "ПривестиКЧислу", Item.Quantity);
		Total 	 = Total 	+ МетодСервера(, "ПривестиКЧислу", Item.Subtotal);
		Vat 	 = Vat 		+ МетодСервера(, "ПривестиКЧислу", Item.Vat);
		
		Если НЕ СФНаАванс ИЛИ НДС_ИсчНалАг Тогда
			TotalWithVatExcluded = TotalWithVatExcluded + МетодСервера(, "ПривестиКЧислу", Item.SubtotalWithVatExcluded);
		КонецЕсли;
			
		Если ЗначениеЗаполнено(СтрокаДанных.СтранаПроисхождения) Тогда
			
			CustomDeclaration = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_CustomDeclaration");
			Item.CustomDeclarations.Добавить(CustomDeclaration);
			
			CustomDeclaration.CountryCode = СокрЛП(СтрокаДанных.СтранаПроисхождения.Код);
			
			Если ЗначениеЗаполнено(СтрокаДанных.НомерГТД) Тогда
				
				CustomDeclaration.DeclarationNumber = ?(ТипЗнч(СтрокаДанных.НомерГТД) = Тип("Строка"),
														СокрЛП(СтрокаДанных.НомерГТД),
														СокрЛП(СтрокаДанных.НомерГТД.Код));
				
			КонецЕсли; 
			
			Если ЗначениеЗаполнено(СтрокаДанных.НомерГТД) И НЕ МетодСервера(, "СтранаЧленТС", СтрокаДанных.СтранаПроисхождения.Код) Тогда
				CustomDeclaration.СтранаПроисхожденияСсылка = СтрокаДанных.СтранаПроисхождения;
			Иначе
				CustomDeclaration.СтранаПроисхожденияСсылка = Справочники.СтраныМира.ПустаяСсылка();
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Content.InvoiceTable.TotalNet = TotalNet;
	Content.InvoiceTable.Total 	  = Total;
	Content.InvoiceTable.Vat 	  = ?(ВсеСтроки_СтавкаНДС_БезНДС, Неопределено, ?(ВсеСтроки_СтавкаНДС_ИсчНалАг, 0, Vat));
	
	Если Не СФНаАванс ИЛИ ВсеСтроки_СтавкаНДС_ИсчНалАг Тогда
		Content.InvoiceTable.TotalWithVatExcluded = TotalWithVatExcluded;
	КонецЕсли;
	
	Content.TransferInfo.OperationInfo = "Товары переданы, работы сданы, услуги оказаны";
	
	//TransferDate заполняем только, если дата отгрузки не совпадает с датой счета-фактуры.
	Если ДанныеШапки.Свойство("ДатаОтгрузки") И НачалоДня(ДанныеШапки.ДатаОтгрузки) <> НачалоДня(ДатаНомер.ДатаДокумента) Тогда 
		Content.TransferInfo.TransferDate = ДанныеШапки.ДатаОтгрузки;
	КонецЕсли;
	
	ЗаполнитьСведенияОПередаче(Content, ДокСсылка, ФункцияУПД, ПараметрыФормирования);
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЗаполнитьСведенияОЛицеПередавшемТовар"
		, Content
		, ДанныеШапки
		, ПараметрыФормирования);
	
	Если ДанныеШапки.Функция = "InvoiceAndBasic" Тогда
		Content.Name = "Счет-фактура и документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";
	ИначеЕсли ДанныеШапки.Функция = "Basic" Тогда
		Content.Name = "Документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";
	КонецЕсли;
	
	ДополнитьКонтентДокументаСлужебнойИнформацией(Content, ДанныеШапки);
	ДобавитьДокументыОснованияВСлужебнуюИнформацию(Content);
	
КонецПроцедуры

Процедура ДобавитьДокументыОснованияВСлужебнуюИнформацию(Контент)
	
	СсылкаНаОбъект = Контент.СлужебнаяИнформация.Ссылка;
	
	Если Не ЭтоСчетФактура(СсылкаНаОбъект)
		Или СчетФактураНаАванс(СсылкаНаОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	ДокументыОснования = ВсеДокументыОснования(СсылкаНаОбъект);
	Контент.СлужебнаяИнформация.Вставить("ДокументыОснования", ДокументыОснования);
	
КонецПроцедуры

Процедура ЗаполнитьКонтентXmlUCD(Content, ДокСсылка, ПараметрыФормирования) Экспорт
	
	ПараметрыФормированияXML = ПараметрыФормирования.ПараметрыФормированияФайла;
	Точность = ПараметрыФормированияXML.ТочностьЦены;
	
	ФункцияУПД = ПараметрыФормирования.ФункцияДокумента;
	
	ДанныеДляПечати = ДанныеДляФормированияУКД(ДокСсылка, Новый Структура("ФункцияУПД", ФункцияУПД));
	
	ШапкаУКД 		= ДанныеДляПечати.Шапка;
	ТаблицаТоваров  = ДанныеДляПечати.ТабличнаяЧасть;
	ОписаниеДокумента = ШапкаУКД.ОписаниеДокумента;
	
	Если ПараметрыФормирования.ФорматОтправки.БазовыйФормат = "ucd736" Тогда
		УдалитьСтрокиНеСоответствующие736Формату(ТаблицаТоваров);
	КонецЕсли;
	
	ДобавитьОшибкиВалидацииИзДанныхШапки(ШапкаУКД, Content);
	
	ЭтоИсправление = ОписаниеДокумента.ЭтоИсправление;
	ДатаНомер = ШапкаУКД.ДатаНомерДокумента;
	
	Content.Number 	= ДатаНомер.НомерДокумента;
	Content.Date	= ДатаНомер.ДатаДокумента;
	
	Если ЭтоИсправление Тогда
		Content.RevisionNumber	 = ДатаНомер.НомерИсправления;
		Content.RevisionDate	 = ДатаНомер.ДатаИсправления;
	КонецЕсли;
	
	Для каждого ДатаНомерДокументаОснования Из ДатаНомер.ДатаНомерДокументовОснований Цикл
		ЗаполнитьInvoiceForCorrectionInfo(Content.Invoices, ДатаНомерДокументаОснования);
	КонецЦикла;
	
	Content.Function = ШапкаУКД.Функция;
	
	Если ШапкаУКД.Функция = "InvoiceAndBasic" Тогда
		Content.Name = "Корректировочный счет-фактура и документ об изменении стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";
	ИначеЕсли ШапкаУКД.Функция = "Basic" Тогда
		Content.Name = "Документ об изменении стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";
	КонецЕсли;
	
	Продавец = ПолучитьГоловноеЮрлицо(ШапкаУКД.Организация);
	Заполнить_ExtendedOrganizationInfo(Content.Seller, Продавец, ДатаНомер.ДатаДокумента, Истина, ШапкаУКД.БанковскийСчетОрганизации, Неопределено, ШапкаУКД.ОбособленноеПодразделение);
	Если ЗначениеЗаполнено(ПараметрыФормирования.Отправитель_FnsParticipantId) Тогда
		Content.Seller.FnsParticipantId = ПараметрыФормирования.Отправитель_FnsParticipantId;
	КонецЕсли;
	
	Если ШапкаУКД.Свойство("ДоговорАгента") И ЗначениеЗаполнено(ШапкаУКД.ДоговорАгента) Тогда
										
		Content.Creator  	= ШапкаУКД.Агент.НаименованиеПолное + ", " + МетодСервера(, "СформироватьПредставлениеИННКПП", ШапкаУКД.Агент.ИНН, ШапкаУКД.Агент.КПП);
		Content.CreatorBase = ШапкаУКД.ДоговорАгента.Наименование;
		
	Иначе
		Content.Creator = Content.Seller.Name + ", ИНН/КПП: " + МетодСервера(, "ПредставлениеИННКПП", Content.Seller);
	КонецЕсли;
	
	Покупатель = ПолучитьГоловноеЮрлицо(ШапкаУКД.Контрагент);
	Заполнить_ExtendedOrganizationInfo(Content.Buyer, Покупатель, ДатаНомер.ДатаДокумента, Истина);
	Если ЗначениеЗаполнено(ПараметрыФормирования.Получатель_FnsParticipantId) Тогда
		Content.Buyer.FnsParticipantId = ПараметрыФормирования.Получатель_FnsParticipantId;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ШапкаУКД.КонтрагентКПП) Тогда
		Content.Buyer.kpp = ШапкаУКД.КонтрагентКПП;
	КонецЕсли;
	
	Content.ВалютаСсылка 	= ШапкаУКД.ВалютаДокумента;
	Content.Currency 		= ?(ЗначениеЗаполнено(Content.ВалютаСсылка), Строка(Content.ВалютаСсылка.Код), "");
					
	ШапкаУКД.Свойство("ИдентификаторГосконтракта", Content.GovernmentContractInfo);
							
	ЗаполнитьStructedAdditionalInfos(Content.AdditionalInfoId.StructedAdditionalInfos, ПараметрыФормирования.ДополнительныеСведенияСтрокой);
	
	Если ЭтоСчетФактура(ДокСсылка) Тогда
		ДокументОтгрузки = ПервыйДокументОснование(ДокСсылка);
	Иначе
		ДокументОтгрузки = ДокСсылка;
	КонецЕсли;
	
	ЗаполнитьInvoiceCorrectionTable(Content.InvoiceCorrectionTable, ТаблицаТоваров, Точность, ДокументОтгрузки);
	
	ЗаполнитьEventContent(Content.EventContent, ШапкаУКД);
	
	ДополнитьКонтентДокументаСлужебнойИнформацией(Content, ШапкаУКД);
	
	Content.СлужебнаяИнформация.Вставить("ПередаточныеДокументы", ШапкаУКД.ПередаточныеДокументы);
	Content.СлужебнаяИнформация.Вставить("ИсправляемыеДокументы", ШапкаУКД.ИсправляемыеДокументы);
	
КонецПроцедуры

Процедура ЗаполнитьКонтентXmlTorg12(Content, ДокСсылка, ПараметрыФормирования) Экспорт
	
	ДанныеШапки		 = ДанныеШапки(ДокСсылка);
	ТаблицаТоваров	 = ПолучитьДанныеТЧ(ДокСсылка, Неопределено, Неопределено, Ложь);
	ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(ДанныеШапки, ТаблицаТоваров);
	
	ДатаНомер = ДанныеШапки.ДатаНомерДокумента;
	Content.Number = ДатаНомер.НомерДокумента;
	Content.Date   = ДатаНомер.ДатаДокумента;
	
	Если (Метаданные.Имя = "УправлениеТорговлей" И МетодСервера(, "СравнитьВерсии", ПараметрыКлиентСервер.ВерсияКонфигурации, "11.2") < 0)
		Или (Метаданные.Имя = "УправлениеПредприятием" И МетодСервера(, "СравнитьВерсии", ПараметрыКлиентСервер.ВерсияКонфигурации, "2.1") < 0) Тогда
		КоэффициентПересчетаВТонны = Константы.КоэффициентПересчетаВТонны.Получить();
	Иначе
		КоэффициентПересчетаВТонны = НоменклатураСервер.КоэффициентПересчетаВТонны(Константы.ЕдиницаИзмеренияВеса.Получить());
	КонецЕсли;
	
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Seller, ДанныеШапки.Организация, ДатаНомер.ДатаДокумента, ДанныеШапки.БанковскийСчетОрганизации, Истина);
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Buyer, ДанныеШапки.Контрагент, ДатаНомер.ДатаДокумента, ДанныеШапки.БанковскийСчетКонтрагента, Истина);
	
	Грузоотправитель = ?(ЗначениеЗаполнено(ДанныеШапки.Грузоотправитель), ДанныеШапки.Грузоотправитель, ДанныеШапки.Организация);
	СчетГрузоотправителя = ?(ЗначениеЗаполнено(ДанныеШапки.БанковскийСчетГрузоотправителя), ДанныеШапки.БанковскийСчетГрузоотправителя, ДанныеШапки.БанковскийСчетОрганизации);
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Shipper, Грузоотправитель, ДатаНомер.ДатаДокумента, СчетГрузоотправителя, Ложь);
	
	Content.ShipperDepartment = ПолучитьПодразделениеИзДокумента(ДокСсылка);
	
	Грузополучатель = ?(ЗначениеЗаполнено(ДанныеШапки.Грузополучатель), ДанныеШапки.Грузополучатель, ДанныеШапки.Контрагент);
	СчетГрузополучателя = ДанныеШапки.БанковскийСчетГрузополучателя;
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Consignee,  Грузополучатель, ДатаНомер.ДатаДокумента, СчетГрузополучателя, Ложь, , , ДанныеШапки.АдресДоставки);
	
	НомерЗаказа = ПараметрыФормирования.НомерЗаказа;
	Если ЗначениеЗаполнено(НомерЗаказа) Тогда 
		Content.GroundName = "Заказ";
		Content.GroundNumber = СокрЛП(НомерЗаказа);
	Иначе
		СтруктураДоговора = СтруктурированноеПредставлениеОснования(ДокСсылка);
		Если ЗначениеЗаполнено(СтруктураДоговора) Тогда
			ЗаполнитьЗначенияСвойств(Content, СтруктураДоговора);
		КонецЕсли;
	КонецЕсли;
	
	ИдКонтрагента = МетодСервера(,"Контрагент_2_CounteragentBoxID", ДанныеШапки.Контрагент);
	
	ДопСведения = ПараметрыФормирования.ДополнительныеСведенияСтрокой;
	Если МетодСервера(, "НужноФормироватьДопИнфоПоШаблону", ИдКонтрагента) = Истина Тогда
		content.AdditionalInfo = МетодСервера(, "СформироватьСтрокуДопИнфоПоШаблону", ДопСведения);
	Иначе
		content.AdditionalInfo = ДопСведения;
	КонецЕсли;
	
	Если ДанныеШапки.НДСИсчисляетсяНалоговымАгентом Тогда
		МетодСервера(, "ЗаполнитьВТаблице_СтавкаНДС_ИсчНалАг", ТаблицаТоваров);
	КонецЕсли;
	
	TotalParcelsQuantity 	= 0;
	TotalGrossQuantity 		= 0;
	TotalNetQuantity 		= 0;
	TotalMassQuantity		= 0;
	TotalWithVatExcluded 	= 0;
	Total 					= 0;
	Vat 					= 0;

	СтруктураItems = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_Torg12Item");
	ЕстьСтрокиСНДС = Ложь;
					
	Для Каждого СтрокаТаблицы Из ТаблицаТоваров Цикл 
		
		Item = СкопироватьСтруктуру(СтруктураItems);
		
		Item.Product =		СтрокаТаблицы.НоменклатураПредставление;
		Item.ProductCode =	СтрокаТаблицы.КодНоменклатуры;
		Item.UnitCode = 		МетодСервера(, "Item_UnitCode", СтрокаТаблицы.ЕдиницаИзмерения, СокрЛП(СтрокаТаблицы.КодЕдиницыИзмерения));
		Item.UnitName =  	Строка(СтрокаТаблицы.ЕдиницаИзмерения);
		Item.Quantity =		СтрокаТаблицы.Количество;
		Item.TaxRate = ПредставлениеСтавкиНДС(СтрокаТаблицы.СтавкаНДС);
		
		Если ЗначениеЗаполнено(Item.TaxRate) И НЕ МенеджерИнтеграции_ЭтоСтавкаБезНДС(Item.TaxRate) Тогда
			ЕстьСтрокиСНДС = Истина;	
		КонецЕсли;
		
		Item.Vat =			СтрокаТаблицы.СуммаНДС;
		Item.Total =			СтрокаТаблицы.Сумма + ?(СтрокаТаблицы.СуммаВключаетНДС, 0, СтрокаТаблицы.СуммаНДС);
		Item.TotalWithVatExcluded =	Item.Total - Item.Vat;
		Item.Price =			окр(Item.TotalWithVatExcluded / ?(Item.Quantity = 0, 1, Item.Quantity), 2);
		
		Item.СсылкаНаЕИ =	СтрокаТаблицы.ЕдиницаИзмерения;
		
		Если ЗначениеЗаполнено(СтрокаТаблицы.КоличествоМест) Тогда
			Item.ParcelsQuantity = СтрокаТаблицы.КоличествоМест;
			Item.ParcelCapacity  = СтрокаТаблицы.КоличествоВОдномМесте;
			Item.ParcelType      = ?(ЗначениеЗаполнено(строкаТаблицы.ВидУпаковки), строкаТаблицы.ВидУпаковки.Наименование, "");
			Item.GrossQuantity   = СтрокаТаблицы.Масса;
			TotalGrossQuantity   = TotalGrossQuantity + ?(ЗначениеЗаполнено(Item.GrossQuantity), Item.GrossQuantity, 0);
			TotalNetQuantity     = TotalNetQuantity + ?(ЗначениеЗаполнено(строкаТаблицы.МассаНетто), строкаТаблицы.МассаНетто, 0);
			TotalParcelsQuantity = TotalParcelsQuantity + Item.ParcelsQuantity;
		КонецЕсли;	
		
		ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Item, СтрокаТаблицы);
		
		Content.items.Добавить(Item);
		
		TotalMassQuantity	 	= TotalMassQuantity + Item.Quantity;
		TotalWithVatExcluded 	= TotalWithVatExcluded + Item.TotalWithVatExcluded;
		Total 					= Total + Item.Total;
		Vat 					= Vat + Item.Vat;
		
		Если МенеджерИнтеграции_ЭтоСтавкаБезНДС(Item.TaxRate) Тогда
			Item.Vat = Неопределено;
		КонецЕсли;
		
	КонецЦикла;
	
	Content.Totals.ParcelsQuantity 		= TotalParcelsQuantity;
	Content.Totals.GrossQuantity 		= TotalGrossQuantity;
	Content.Totals.Quantity 			= TotalMassQuantity;
	Content.Totals.TotalWithVatExcluded = TotalWithVatExcluded;
	Content.Totals.Total 				= Total;
	Content.Totals.Vat 					= ?(ЕстьСтрокиСНДС, Vat, Неопределено);
				
	Content.SupplyDate 					= Content.Date;
	
	Content.commons.TotalSumInWords = РаботаСКурсамиВалют.СформироватьСуммуПрописью(Total, ДанныеШапки.ВалютаДокумента);
	Если ЗначениеЗаполнено(TotalParcelsQuantity) Тогда
		Content.commons.TotalParcelsQuantityInWords
			= Стрзаменить(ЧислоПрописью(TotalParcelsQuantity, "ДП = Ложь"), "  00", "");
	КонецЕсли;	
	
	Если ЗначениеЗаполнено(TotalGrossQuantity) Тогда
		ПараметрыМассыПрописью = ПараметрыМассыПрописью(TotalGrossQuantity, КоэффициентПересчетаВТонны);
		Content.commons.TotalGrossQuantityInWords
			= ЧислоПрописью(TotalGrossQuantity * ПараметрыМассыПрописью.Коэффициент, "ДП=Истина", ПараметрыМассыПрописью.Формат);
	КонецЕсли;	
	
	Если ЗначениеЗаполнено(TotalNetQuantity) Тогда
		ПараметрыМассыПрописью = ПараметрыМассыПрописью(TotalNetQuantity, КоэффициентПересчетаВТонны);
		Content.commons.TotalQuantityInWords
			= ЧислоПрописью(TotalNetQuantity * ПараметрыМассыПрописью.Коэффициент, "ДП=Истина", ПараметрыМассыПрописью.Формат);
	КонецЕсли;	
	
	ФИОСогласователя = "";
	
	ДолжностьСогласователя = "";
	
	ПараметрыОтправкиНаСогласование = ПараметрыФормирования.ПараметрыСогласования;
	
	Если НЕ ПараметрыОтправкиНаСогласование = Неопределено Тогда
		Если ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПодписанта) Тогда
			ФИОСогласователя 		= ПараметрыОтправкиНаСогласование.ФИОПодписанта;
			ДолжностьСогласователя 	= ПараметрыОтправкиНаСогласование.ДолжностьПодписанта;
		Иначе
			Если ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПолучателя) Тогда 
				ФИОСогласователя 	= ПараметрыОтправкиНаСогласование.ФИОПолучателя;
			КонецЕсли;
			ДолжностьСогласователя 	= ПараметрыОтправкиНаСогласование.ДолжностьПолучателя;
		КонецЕсли;
	КонецЕсли;	
	
	
	Если ФИОСогласователя = "" Тогда
		ПараметрыПодписания = МетодСервера(, "ПолучитьПараметрыПодписанияТ12вДиадок", ДанныеШапки.Организация);
		
		Если ПараметрыПодписания.СпособПодписания = "4" Тогда
			МетодСервера(,"ЗаполнитьПодписантаДляXmlTorg12", Content.SupplyPerformedBy, МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания));
			МетодСервера(,"ЗаполнитьПодписантаДляXmlTorg12", Content.SupplyAllowedBy, МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания));
			
			ПараметрыПодписания.ДолжностьПодписанта = "Главный (старший) бухгалтер";
			МетодСервера(,"ЗаполнитьПодписантаДляXmlTorg12", Content.ChiefAccountant, МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания));
		ИначеЕсли параметрыПодписания.СпособПодписания = "2" Тогда 
			ПараметрыПодписания.ДолжностьПодписанта = "Главный (старший) бухгалтер";
			МетодСервера(,"ЗаполнитьПодписантаДляXmlTorg12", Content.ChiefAccountant, МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания));
		ИначеЕсли параметрыПодписания.СпособПодписания = "3" Тогда
			МетодСервера(,"ЗаполнитьПодписантаДляXmlTorg12", Content.SupplyPerformedBy, МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания));
		Иначе	
			МетодСервера(,"ЗаполнитьПодписантаДляXmlTorg12", Content.SupplyAllowedBy, МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания));
		КонецЕсли;
							
	Иначе 
		МетодСервера(,"ЗаполнитьФИОПодписанта", Content.SupplyAllowedBy, ФИОСогласователя);
		Content.SupplyAllowedBy.JobTitle = СокрЛП(?(НЕ ПустаяСтрока(ДолжностьСогласователя), ДолжностьСогласователя, "_"));
	КонецЕсли;
	
	ДополнитьКонтентДокументаСлужебнойИнформацией(Content, ДанныеШапки);
	
КонецПроцедуры

Процедура ЗаполнитьКонтентXmlAcceptanceCertificate(Content, ДокСсылка, ПараметрыФормирования) Экспорт
	
	ДанныеШапки		 = ДанныеШапки(ДокСсылка);
	ТаблицаТоваров	 = ПолучитьДанныеТЧ(ДокСсылка, Неопределено, Неопределено, Ложь);
	ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(ДанныеШапки, ТаблицаТоваров);
	
	ДатаНомер = ДанныеШапки.ДатаНомерДокумента;
	Content.Number = ДатаНомер.НомерДокумента;
	Content.Date   = ДатаНомер.ДатаДокумента;
	
	//поставщик: организация-продавец
	БанковскийСчетОрганизации = ДанныеШапки.БанковскийСчетОрганизации;
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Seller
		, ДанныеШапки.Организация
		, ДатаНомер.ДатаДокумента
		, БанковскийСчетОрганизации
		, Истина);
	
	content.SignatureDate = content.Date;
	
	ЗаполнитьЗаголовокАкта172(Content, ДанныеШапки);
	ЗаполнитьДополнительныеСведения(Content, ДанныеШапки, ПараметрыФормирования);
	
	СтруктураItems = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_AcceptanceCertificateItem");
	
	Если ДанныеШапки.НДСИсчисляетсяНалоговымАгентом Тогда
		МетодСервера(, "ЗаполнитьВТаблице_СтавкаНДС_ИсчНалАг", ТаблицаТоваров);
	КонецЕсли;
	
	Для Каждого СтрокаТаблицы Из ТаблицаТоваров Цикл
		
		Item = СкопироватьСтруктуру(СтруктураItems);
		
		Если Не ЗначениеЗаполнено(СтрокаТаблицы.Количество)
			Или Не ЗначениеЗаполнено(СтрокаТаблицы.КодЕдиницыИзмерения) Тогда
			Item.Description = СтрокаТаблицы.НоменклатураПредставление;
		Иначе 
			Item.Name = СтрокаТаблицы.НоменклатураПредставление;
		КонецЕсли;
		
		Item.UnitCode = "";
		
		Если ЗначениеЗаполнено(СтрокаТаблицы.ЕдиницаИзмерения) Тогда
			
			Item.UnitCode = МетодСервера(, "Item_UnitCode"
				, СтрокаТаблицы.ЕдиницаИзмерения
				, СокрЛП(СтрокаТаблицы.КодЕдиницыИзмерения));
			
			Если Item.UnitCode = "" И ЗначениеЗаполнено(Item.Name) Тогда
				Item.Description = Item.Name;
				Item.Name		 = Неопределено;
			КонецЕсли;
			
		КонецЕсли;
		
		Item.UnitName = Строка(СтрокаТаблицы.ЕдиницаИзмерения);
		Если ЗначениеЗаполнено(Item.Name) Тогда
			Item.Quantity = СтрокаТаблицы.Количество;
		КонецЕсли;
		
		Если Не МенеджерИнтеграции_ЭтоСтавкаБезНДС(СтрокаТаблицы.СтавкаНДС) Тогда
			Item.Vat = СтрокаТаблицы.СуммаНДС;
		КонецЕсли;
	
		Item.Total					 = СтрокаТаблицы.Сумма + ?(СтрокаТаблицы.СуммаВключаетНДС, 0, СтрокаТаблицы.СуммаНДС);
		Item.TotalWithVatExcluded	 = Item.Total - СтрокаТаблицы.СуммаНДС;
		Item.СсылкаНаЕИ				 = СтрокаТаблицы.ЕдиницаИзмерения;
		
		Если ЗначениеЗаполнено(item.Quantity) Тогда
			Item.Price = Окр(item.TotalWithVatExcluded / item.Quantity, 2);
		КонецЕсли;
		
		ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Item, СтрокаТаблицы);
		
		Content.items.Добавить(Item);
		
	КонецЦикла;
	
	ЗаполнитьПодписантаАкта172(Content, ДанныеШапки, ПараметрыФормирования);
	
	ДополнитьКонтентДокументаСлужебнойИнформацией(Content, ДанныеШапки);
	
КонецПроцедуры

Процедура ЗаполнитьКонтентInvoice(Content, ДокСсылка, ПараметрыФормирования) Экспорт
	
	ПараметрыФормированияXML = ПараметрыФормирования.ПараметрыФормированияФайла;
	Точность = ПараметрыФормированияXML.ТочностьЦены;
	
	ДанныеДляПечати = СформироватьДанныеДляПечати(ДокСсылка, Ложь);
	ОписаниеДокумента = ДанныеДляПечати.ОписаниеДокумента;
	
	ЭтоИсправление = ОписаниеДокумента.ЭтоИсправление;
	ДатаНомер = ДанныеДляПечати.ДатаНомерДокумента;
	
	Content.Number 	= ДатаНомер.НомерДокумента;
	Content.Date	= ДатаНомер.ДатаДокумента;
	
	Если ЭтоИсправление Тогда
		Content.InvoiceRevisionNumber = ДатаНомер.НомерИсправления;
		Content.InvoiceRevisionDate = ДатаНомер.ДатаИсправления;
	КонецЕсли;
	
	СтруктураPaymentDocuments=	МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_PaymentDocument");

	Для Каждого ПРД Из ДанныеДляПечати.ДатаНомерДокументовОплаты Цикл
		
		PaymentDocumentContent=	СкопироватьСтруктуру(СтруктураPaymentDocuments);
		
		PaymentDocumentContent.Number = ПРД.НомерПлатежноРасчетногоДокумента;
		PaymentDocumentContent.date   = ПРД.ДатаПлатежноРасчетногоДокумента;
		
		Content.PaymentDocuments.Добавить(PaymentDocumentContent);
		
	КонецЦикла;
	
	Content.currency = ДанныеДляПечати.ВалютаДокумента.Код;
	Content.ВалютаСсылка = ДанныеДляПечати.ВалютаДокумента;
	
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Buyer,
		ДанныеДляПечати.Контрагент, ДатаНомер.ДатаДокумента, , Истина, Истина);
	Если ЗначениеЗаполнено(ДанныеДляПечати.КонтрагентКПП) Тогда
		Content.Buyer.kpp = ДанныеДляПечати.КонтрагентКПП;
	КонецЕсли;
	
	КППГрузоотправителя = "";
	Если ЗначениеЗаполнено(ДанныеДляПечати.Грузоотправитель) И НЕ ТипЗнч(ДанныеДляПечати.Грузоотправитель) = Тип("Строка") Тогда
		ПодставлятьКППГрузоотправителя = МетодСервера(,"ПолучитьЗначениеСвойства", ДанныеДляПечати.Организация, МетодСервера(, "ИдентификаторСвойстваПодставлятьКППГрузоотправителя"));
		Если (НЕ ПодставлятьКППГрузоотправителя = Неопределено И ПодставлятьКППГрузоотправителя) Тогда
			КППГрузоотправителя = ?(ЗначениеЗаполнено(ДанныеДляПечати.Грузоотправитель.КПП), ДанныеДляПечати.Грузоотправитель.КПП, "");
		КонецЕсли;
	КонецЕсли;

	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Seller, ДанныеДляПечати.Организация, ДатаНомер.ДатаДокумента, , Истина, Истина, КППГрузоотправителя);
	
	// Грузоотправителя и грузополучателя заполняем только если в ТЧ есть товары
	Если ДанныеДляПечати.ЕстьТовары Тогда 
		
		Если ЗначениеЗаполнено(ДанныеДляПечати.Грузоотправитель) Тогда 
			ЗаполнитьСтруктурированноеОписаниеГрузоотправителяГрузополучателя(Content.Shipper, ДанныеДляПечати.Грузоотправитель, ДатаНомер.ДатаДокумента); 
			Content.Shipper.SameAsSellerOrBuyer= Ложь;
		Иначе
			Content.Shipper.SameAsSellerOrBuyer= Истина;
		КонецЕсли;
		
		ГрузополучательСсылка = ?(ЗначениеЗаполнено(ДанныеДляПечати.Грузополучатель), ДанныеДляПечати.Грузополучатель, ДанныеДляПечати.Контрагент);
		ЗаполнитьСтруктурированноеОписаниеГрузоотправителяГрузополучателя(Content.Consignee, ГрузополучательСсылка, ДатаНомер.ДатаДокумента, ДанныеДляПечати.АдресДоставки);
		
	КонецЕсли;
	
	ЗаполнитьStructedAdditionalInfos(Content.StructedAdditionalInfos, ПараметрыФормирования.ДополнительныеСведенияСтрокой);

	СтруктураItemInvoice=	МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceItem");
	
	Vat= 	0;
	Total= 	0;
	TotalWithVatExcluded= 0;
	
	Для каждого Стр из ДанныеДляПечати.ТабличнаяЧасть Цикл
		
		Item = СкопироватьСтруктуру(СтруктураItemInvoice);
		
		Item.Product= Стр.НоменклатураПредставление;
		
		Если ЗначениеЗаполнено(Стр.ЕдиницаИзмерения) Тогда
			Item.UnitCode= МетодСервера(, "Item_UnitCode", Стр.ЕдиницаИзмерения, СокрЛП(Стр.КодЕдиницыИзмерения));
		Иначе
			Item.UnitCode= "";
		КонецЕсли;
		
		Item.ЕдиницаИзмеренияСсылка= Стр.ЕдиницаИзмерения;
		
		Если СчетФактуранаАванс(ДокСсылка) = Ложь Тогда 
			Item.TotalWithVatExcluded= Стр.Сумма - ?(Стр.СуммаВключаетНДС, Стр.СуммаНДС, 0);
		КонецЕсли;
		
		Если СчетФактуранаАванс(ДокСсылка) = Ложь И ЗначениеЗаполнено(Стр.ЕдиницаИзмерения) И ЗначениеЗаполнено(Стр.ЕдиницаИзмерения.Код) Тогда
			Item.Quantity= 	?(ЗначениеЗаполнено(Стр.ЕдиницаИзмерения), Стр.Количество, 0);
			Item.price= 	Строка(?(Item.Quantity = 0, Неопределено, Окр(Item.TotalWithVatExcluded/Item.Quantity, Точность)));
		Иначе 
			//Item.Quantity = 0;
			//Item.price =  0;
		КонецЕсли; 
		
		Если Не МенеджерИнтеграции_ЭтоСтавкаБезНДС(Стр.СтавкаНДС) Тогда
			Item.Vat = Стр.СуммаНДС;
		КонецЕсли;
		
		Item.Total = Стр.Сумма + ?(Стр.СуммаВключаетНДС, 0, Стр.СуммаНДС);
		Item.TaxRate = ПредставлениеСтавкиНДС(Стр.СтавкаНДС);
		Item.CountriesOfOrigin = ?(ЗначениеЗаполнено(Стр.СтранаПроисхождения), Стр.СтранаПроисхождения.Код, "");
		Item.СтранаПроисхожденияСсылка = ?(ЗначениеЗаполнено(Стр.СтранаПроисхождения), Стр.СтранаПроисхождения, Справочники.СтраныМира.ПустаяСсылка());
		Item.CustomsDeclarationNumbers = ?(ЗначениеЗаполнено(Стр.НомерГТД), ?(ТипЗнч(Стр.НомерГТД) = Тип("Строка"), СокрЛП(Стр.НомерГТД), СокрЛП(Стр.НомерГТД.Код)), "");
		
		ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Item, Стр);
		
		Content.items.Добавить(Item);
		
		Vat = Vat + Стр.СуммаНДС;
		TotalWithVatExcluded= TotalWithVatExcluded + ?(Item.TotalWithVatExcluded = Неопределено, 0, Item.TotalWithVatExcluded);
		Total = Total + Item.Total;
		
	КонецЦикла;
	
	Content.Totals.Vat = Vat;
	Content.Totals.TotalWithVatExcluded = TotalWithVatExcluded;
	Content.Totals.Total = Total;

	стПодписант= МетодСервера(, "СформироватьПараметрыПодписанта", ПараметрыФормирования.ФИОПодписанта);
	
	ПараметрыОтправкиНаСогласование = ПараметрыФормирования.ПараметрыСогласования;
	
	Если НЕ ПараметрыОтправкиНаСогласование = Неопределено Тогда 
		
		Если ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПодписанта) Тогда
			ФИОПолучателя= ПараметрыОтправкиНаСогласование.ФИОПодписанта;
		Иначе	
			ФИОПолучателя= ПараметрыОтправкиНаСогласование.ФИОПолучателя;
		КонецЕсли;
		
		ПодписантФамилия = ВыделитьСлово(ФИОПолучателя);
		ПодписантИмя = ВыделитьСлово(ФИОПолучателя);
		ПодписантОтчество = ВыделитьСлово(ФИОПолучателя);
		
		стПодписант.ПодписантФамилия = ?(НЕ ПустаяСтрока(ПодписантФамилия), ПодписантФамилия, " ");
		стПодписант.ПодписантИмя = ?(НЕ ПустаяСтрока(ПодписантИмя), ПодписантИмя, " ");
		стПодписант.ПодписантОтчество = ?(НЕ ПустаяСтрока(ПодписантОтчество), ПодписантОтчество, "");
		
	КонецЕсли;
	
	Content.Signer.Surname = стПодписант.ПодписантФамилия;
	Content.Signer.FirstName = стПодписант.ПодписантИмя;
	Content.Signer.Patronymic = стПодписант.ПодписантОтчество;
	Content.Signer.Inn = Content.seller.inn;
	Content.Signer.IsSoleProprietor = Content.seller.IsSoleProprietor;
	Content.Signer.SoleProprietorRegistrationCertificate = ПолучитьДатуНомерСвидетельстваОГосРегистрацииИП(ДанныеДляПечати.Организация);
	
	ДополнитьКонтентДокументаСлужебнойИнформацией(Content, ДанныеДляПечати);
	
КонецПроцедуры

Процедура ЗаполнитьКонтентInvoiceCorrection(Content, ДокСсылка, ПараметрыФормирования) Экспорт
	
	ПараметрыФормированияXML = ПараметрыФормирования.ПараметрыФормированияФайла;
	Точность = ПараметрыФормированияXML.ТочностьЦены;
	
	ДанныеДляПечати = СформироватьДанныеДляПечати(ДокСсылка, Истина);
	ОписаниеДокумента = ДанныеДляПечати.ОписаниеДокумента;
		
	ЭтоИсправление = ОписаниеДокумента.ЭтоИсправление;
	ДатаНомер = ДанныеДляПечати.ДатаНомерДокумента;
	
	Content.InvoiceCorrectionNumber	 = ДатаНомер.НомерДокумента;
	Content.InvoiceCorrectionDate	 = ДатаНомер.ДатаДокумента;
	
	Если ЭтоИсправление Тогда
		Content.RevisionNumber	 = ДатаНомер.НомерИсправления;
		Content.RevisionDate	 = ДатаНомер.ДатаИсправления;
	КонецЕсли;
	
	ДанныеОснований = ДатаНомер.ДатаНомерДокументовОснований;
	
	Если ЗначениеЗаполнено(ДанныеОснований) Тогда
		
		ДанныеОснования = ДанныеОснований[0];
		
		OriginalInvoices = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_OriginalInvoice");
		OriginalInvoices.Number	 = ДанныеОснования.НомерИсходногоДокумента;
		OriginalInvoices.Date	 = ДанныеОснования.ДатаИсходногоДокумента;
		
		Если ДанныеОснования.УчитыватьИсправлениеИсходногоДокумента Тогда 
			OriginalInvoices.InvoiceRevisionNumber	 = ДанныеОснования.НомерИсправленияИсходногоДокумента;
			OriginalInvoices.InvoiceRevisionDate	 = ДанныеОснования.ДатаИсправленияИсходногоДокумента;
		КонецЕсли;
		Content.OriginalInvoices.Добавить(OriginalInvoices);
	
	КонецЕсли;
	
	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Buyer,
		ДанныеДляПечати.Контрагент, ДатаНомер.ДатаДокумента, , Истина, Истина);
	
	Если ЗначениеЗаполнено(ДанныеДляПечати.КонтрагентКПП) Тогда
		Content.Buyer.kpp = ДанныеДляПечати.КонтрагентКПП;
	КонецЕсли;
	
	КППГрузоотправителя =	"";
	Если ЗначениеЗаполнено(ДанныеДляПечати.Грузоотправитель) И НЕ ТипЗнч(ДанныеДляПечати.Грузоотправитель) = Тип("Строка") Тогда
		ПодставлятьКППГрузоотправителя=	МетодСервера(,"ПолучитьЗначениеСвойства", ДокСсылка.Организация, МетодСервера(, "ИдентификаторСвойстваПодставлятьКППГрузоотправителя"));
		Если (НЕ ПодставлятьКППГрузоотправителя = Неопределено И ПодставлятьКППГрузоотправителя) Тогда
			КППГрузоотправителя =	?(ЗначениеЗаполнено(ДанныеДляПечати.Грузоотправитель.КПП), ДанныеДляПечати.Грузоотправитель.КПП, "");
		КонецЕсли;
	КонецЕсли;

	ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(Content.Seller,  ДанныеДляПечати.Организация, ДатаНомер.ДатаДокумента, , Истина, Истина, КППГрузоотправителя);
	
	content.currency = ДанныеДляПечати.ВалютаДокумента.Код;
	content.ВалютаСсылка = ДанныеДляПечати.ВалютаДокумента;
	
	ЗаполнитьStructedAdditionalInfos(Content.StructedAdditionalInfos, ПараметрыФормирования.ДополнительныеСведенияСтрокой);

	СтруктураItemInvoiceCorrection= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceCorrectionItem");

	Для каждого СтрокаДанных из ДанныеДляпечати.ТабличнаяЧасть Цикл 
						
		Item = СкопироватьСтруктуру(СтруктураItemInvoiceCorrection);
		Item.AmountsDec = 		МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_AmountsDiff");
		Item.AmountsInc = 		МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_AmountsDiff");
		Item.OriginalValues = 	МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceItemFields");
		Item.CorrectedValues = 	МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceItemFields");
		
		Item.Product = СтрокаДанных.НоменклатураПредставление;
		
		Если ЗначениеЗаполнено(СтрокаДанных.ЕдиницаИзмеренияДоИзменения) Тогда
			Item.OriginalValues.UnitCode = МетодСервера(, "Item_UnitCode", СтрокаДанных.ЕдиницаИзмеренияДоИзменения, СокрЛП(СтрокаДанных.КодЕдиницыИзмеренияДоИзменения));
		Иначе
			Item.OriginalValues.UnitCode = "";
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаДанных.ЕдиницаИзмерения) Тогда
			Item.CorrectedValues.UnitCode = МетодСервера(, "Item_UnitCode", СтрокаДанных.ЕдиницаИзмерения, СокрЛП(СтрокаДанных.КодЕдиницыИзмерения));
		Иначе
			Item.CorrectedValues.UnitCode = "";	
		КонецЕсли;
							
		Item.OriginalValues.ЕдиницаИзмеренияСсылка =		СтрокаДанных.ЕдиницаИзмеренияДоИзменения;
		Item.CorrectedValues.ЕдиницаИзмеренияСсылка = 	СтрокаДанных.ЕдиницаИзмерения;
		
		Item.OriginalValues.TotalWithVatExcluded = 	СтрокаДанных.СуммаСНДСДоИзменения - СтрокаДанных.СуммаНДСДоИзменения;
		Item.CorrectedValues.TotalWithVatExcluded =	СтрокаДанных.СуммаСНДС - СтрокаДанных.СуммаНДС;
		
		Item.OriginalValues.Quantity =	СтрокаДанных.КоличествоДоИзменения;
		Item.CorrectedValues.Quantity = 	СтрокаДанных.Количество;
		
		Item.OriginalValues.Price =	Строка(?(Item.OriginalValues.Quantity = 0, 0, Окр(Item.OriginalValues.TotalWithVatExcluded / Item.OriginalValues.Quantity, Точность)));
		Item.CorrectedValues.Price =	Строка(?(Item.CorrectedValues.Quantity = 0, 0, Окр(Item.CorrectedValues.TotalWithVatExcluded / Item.CorrectedValues.Quantity, Точность)));
		
		СтавкаДоИзменения = СтрокаДанных.СтавкаНДСДоИзменения;
		Если Не ЗначениеЗаполнено(СтавкаДоИзменения) Тогда
			СтавкаДоИзменения = СтрокаДанных.СтавкаНДС;
		КонецЕсли;
		
		Item.OriginalValues.TaxRate = ПредставлениеСтавкиНДС(СтавкаДоИзменения);
		Item.CorrectedValues.TaxRate = ПредставлениеСтавкиНДС(СтрокаДанных.СтавкаНДС);
		
		Item.OriginalValues.Vat =	СтрокаДанных.СуммаНДСДоИзменения;
		Item.CorrectedValues.Vat =	СтрокаДанных.СуммаНДС;
		
		Item.OriginalValues.Total =	СтрокаДанных.СуммаСНДСДоИзменения;
		Item.CorrectedValues.Total = СтрокаДанных.СуммаСНДС;
		
		ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Item, СтрокаДанных);
		
		Content.items.Добавить(Item);
		
	КонецЦикла;
	
	стПодписант = МетодСервера(, "СформироватьПараметрыПодписанта", ПараметрыФормирования.ФИОПодписанта);
	
	ПараметрыОтправкиНаСогласование = ПараметрыФормирования.ПараметрыСогласования;
	
	Если НЕ ПараметрыОтправкиНаСогласование = Неопределено Тогда 
		
		Если ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПодписанта) Тогда
			ФИОПолучателя = ПараметрыОтправкиНаСогласование.ФИОПодписанта;
		Иначе
			ФИОПолучателя = ПараметрыОтправкиНаСогласование.ФИОПолучателя;
		КонецЕсли;
		
		ПодписантФамилия =				ВыделитьСлово(ФИОПолучателя);
		ПодписантИмя = 					ВыделитьСлово(ФИОПолучателя);
		ПодписантОтчество = 				ВыделитьСлово(ФИОПолучателя);
		стПодписант.ПодписантФамилия =	?(НЕ ПустаяСтрока(ПодписантФамилия), ПодписантФамилия, " ");
		стПодписант.ПодписантИмя = 		?(НЕ ПустаяСтрока(ПодписантИмя), ПодписантИмя, " ");
		стПодписант.ПодписантОтчество = 	?(НЕ ПустаяСтрока(ПодписантОтчество), ПодписантОтчество, "");
		
	КонецЕсли;	
					
	Content.Signer.Surname = 	стПодписант.ПодписантФамилия;
	Content.Signer.FirstName = 	стПодписант.ПодписантИмя;
	Content.Signer.Patronymic = 	стПодписант.ПодписантОтчество;
	Content.Signer.Inn = 		Content.seller.inn;
	Content.Signer.IsSoleProprietor = Content.seller.IsSoleProprietor;
	Content.Signer.SoleProprietorRegistrationCertificate = ПолучитьДатуНомерСвидетельстваОГосРегистрацииИП(ДанныеДляПечати.Организация);
	
	ДополнитьКонтентДокументаСлужебнойИнформацией(Content, ДанныеДляПечати);
	
КонецПроцедуры

#КонецОбласти

#Область МашиночитаемыеДоверенности
 
Функция МЧД_ИмяСправочникаВИнформационнойБазе(СтандартнаяОбработка) Экспорт
	
	Перем Результат;
	
	Возврат Результат;
	
КонецФункции

Функция МЧД_ДанныеДоверенности(Ссылка, СтандартнаяОбработка) Экспорт
	
	Перем Результат;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область ОберткиДляВызоваУниверсальныхМетодов

Функция ДанныеШапки(Ссылка)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДанныеШапки", Ссылка);
	
КонецФункции

Функция ДанныеТабличныхЧастей(Шапка) 
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДанныеТабличныхЧастей", Шапка);
	
КонецФункции

Функция ЕстьРеквизитМетаданных(ОбъектМетаданных, ИмяРеквизита, ПроверятьСтандартныеРеквизиты = Ложь)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЕстьРеквизитМетаданных", ОбъектМетаданных, ИмяРеквизита, ПроверятьСтандартныеРеквизиты);
	
КонецФункции

Процедура ДополнитьТаблицу(ТаблицаПриемник, ТаблицаИсточник)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДополнитьТаблицу", ТаблицаПриемник, ТаблицаИсточник);
	
КонецПроцедуры

Функция РеквизитыЮрФизЛица(ЮрФизЛицо, Период = '00010101', Знач БанковскийСчет = Неопределено)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "РеквизитыЮрФизЛица", ЮрФизЛицо, Период, БанковскийСчет);
	
КонецФункции

Функция ПредставлениеЮрФизЛица(Реквизиты, ШаблонПредставления = "", СПрефиксом = Истина)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ПредставлениеЮрФизЛица", Реквизиты, ШаблонПредставления, СПрефиксом);
	
КонецФункции

Функция ИмяДополнительнойКолонки()
	
	Если ЭтоРедакция11_5() Тогда
		ДопКолонка = ФормированиеПечатныхФорм.ДополнительнаяКолонкаПечатныхФормДокументов();
		ИмяКолонки = ДопКолонка.ИмяКолонки;
	Иначе
		ИмяКолонки = ФормированиеПечатныхФорм.ИмяДополнительнойКолонки();
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ИмяКолонки) Тогда 
		ИмяКолонки = "Код";
	КонецЕсли;
	
	Возврат ИмяКолонки;
	
КонецФункции

Процедура ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(Шапка, ТабличнаяЧасть)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти", Шапка, ТабличнаяЧасть);
	
КонецПроцедуры

Функция ПредставлениеНоменклатуры(Номенклатура, Характеристика = Неопределено, ДополнительныеПараметры = Неопределено)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ПредставлениеНоменклатуры", Номенклатура, Характеристика, ДополнительныеПараметры);
	
КонецФункции

Функция ДополнительныеПараметрыПредставленияНоменклатуры()
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДополнительныеПараметрыПредставленияНоменклатуры");
	Возврат Результат;
	
КонецФункции

Процедура ДополнитьКонтентДокументаСлужебнойИнформацией(Контент, ДанныеШапки)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный"
				, "ДополнитьКонтентДокументаСлужебнойИнформацией"
				, Контент
				, ДанныеШапки);
	
КонецПроцедуры

Процедура ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Контент, СтрокаТЧ, ДокументОтгрузки = Неопределено)

	МетодСервера(
		"Модуль_ИнтеграцияУниверсальный",
		"ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией",
		Контент,
		СтрокаТЧ
	);
	
	Если НЕ ЗначениеЗаполнено(Контент.СлужебнаяИнформация.ДокументОтгрузки)
		И ЗначениеЗаполнено(ДокументОтгрузки) Тогда
		
		Контент.СлужебнаяИнформация.ДокументОтгрузки = ДокументОтгрузки;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ДополнитьКонтентСтрокиОснованийПередачиСлужебнойИнформацией(Контент, СтрокаТЧ)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный"
				, "ДополнитьКонтентСтрокиОснованийПередачиСлужебнойИнформацией"
				, Контент
				, СтрокаТЧ);
	
КонецПроцедуры

Функция ХранилищеКонтактнойИнформацииОбъекта(СсылкаНаОбъект)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ХранилищеКонтактнойИнформацииОбъекта", СсылкаНаОбъект);
	
КонецФункции

Функция АдресЮрФизЛица(ЮрФизЛицо, ВидАдреса, Дата = Неопределено)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "АдресЮрФизЛица", ЮрФизЛицо, ВидАдреса, Дата);
	
КонецФункции

Функция СведенияОбАдресе(Адрес, ДополнительныеПараметры = Неопределено)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "СведенияОбАдресе", Адрес, ДополнительныеПараметры);
	
КонецФункции

Функция ПредставлениеКонтактнойИнформацииОбъекта(СсылкаИлиОбъект, ВидКонтактнойИнформации, Разделитель = ",", Дата = Неопределено, ДополнительныеПараметры = Неопределено)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ПредставлениеКонтактнойИнформацииОбъекта"
		, СсылкаИлиОбъект
		, ВидКонтактнойИнформации
		, Разделитель
		, Дата
		, ДополнительныеПараметры);
	
	Возврат Результат;
	
КонецФункции

Процедура Заполнить_AddressInfo(AddressInfo, СтруктураАдреса)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "Заполнить_AddressInfo", AddressInfo, СтруктураАдреса);
	
КонецПроцедуры

Функция ВидыЭлектронныхДокументов()
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ВидыЭлектронныхДокументов");
	
КонецФункции

Процедура ДобавитьВидДокументаДляОбмена(ВидыДокументов, ОбъектМетаданных, ВидЭД, Ограничения = Неопределено, ПравилоКонвертацииСвойств = Неопределено)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДобавитьВидДокументаДляОбмена"
				, ВидыДокументов
				, ОбъектМетаданных
				, ВидЭД
				, Ограничения
				, ПравилоКонвертацииСвойств);
	
КонецПроцедуры

Функция НовоеПравилоКонвертацииСвойствЭД()
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "НовоеПравилоКонвертацииСвойствЭД");
	
КонецФункции

Функция ЕстьНезаполненныйРеквизитОбъекта(ИмяРеквизита, Объект, МетаданныеОбъекта)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЕстьНезаполненныйРеквизитОбъекта"
					, ИмяРеквизита
					, Объект
					, МетаданныеОбъекта);
	
	Возврат Результат;
	
КонецФункции

Функция СвойствоСтруктуры(СложнаяСтруктура, ПолныйКлюч, ЗначениеПоУмолчанию = Неопределено)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "СвойствоСтруктуры"
					, СложнаяСтруктура
					, ПолныйКлюч
					, ЗначениеПоУмолчанию);
	
	Возврат Результат;
	
КонецФункции

Функция СсылкаОбъектаПоПолямПоиска(ПоляПоиска, ОписаниеОбъектаМетаданных, ОднозначноеСоответствие = Истина)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "СсылкаОбъектаПоПолямПоиска"
					, ПоляПоиска
					, ОписаниеОбъектаМетаданных
					, ОднозначноеСоответствие);
	
	Возврат Результат;
	
КонецФункции

Функция СопоставленныеУчетныеДокументы(ИдентификаторЭД)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "СопоставленныеУчетныеДокументы", ИдентификаторЭД);
	
	Возврат Результат;
	
КонецФункции

Функция СведенияОКонфигурации()
	
	Возврат МетодСервера(, "СведенияОКонфигурации");
	
КонецФункции

Функция СравнитьВерсии(Знач СтрокаВерсии1, Знач СтрокаВерсии2)
	
	Возврат МетодСервера(, "СравнитьВерсии", СтрокаВерсии1, СтрокаВерсии2);
	
КонецФункции

Функция ПустыеЗначенияРеквизита(ИмяРеквизита)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ПустыеЗначенияРеквизита", ИмяРеквизита);
	
	Возврат Результат;
	
КонецФункции

Функция ТипыЮрФизЛиц()
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ТипыЮрФизЛиц");
	
КонецФункции

// Добавляет новую запись в таблицу правил обработки электронных документов.
//
// Параметры:
//  ПравилаОбработки - ТаблицаЗначений - см. ПравилаОбработкиВходящихДокументов;
//  ИдентификаторПравила - Строка - идентификатор правила обработки документов;
//  ВидДокумента		 - Строка - вид документа, к которому относится правило (см. ВидыЭлектронныхДокументов);
//  ДляИсправлений		 - Булево, Неопределено - Ложь, если правило действует только для первичной версии документа;
//                                            Истина, если правило действует для только для исправленной версии документа;
//                                            Неопределено, если правило действует только для первичной и исправительной версий документа;
//
Процедура ДобавитьПравилоОбработкиДокументов(ПравилаОбработки, ИдентификаторПравила, ВидДокумента, ДляИсправлений)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДобавитьПравилоОбработкиДокументов"
		, ПравилаОбработки
		, ИдентификаторПравила
		, ВидДокумента
		, ДляИсправлений);
	
КонецПроцедуры

Функция НоваяТаблицаШтрихкодыУпаковок()
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НоваяТаблицаШтрихкодыУпаковок");
	
	Возврат Результат;
	
КонецФункции

Процедура ДополнитьТаблицуШтрихкодов(ШтрихкодыУпаковок, СведенияОМаркировке, GTIN = "")
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДополнитьТаблицуШтрихкодов"
		, ШтрихкодыУпаковок
		, СведенияОМаркировке
		, GTIN);
	
КонецПроцедуры

Функция НовыеСвойстваНоменклатурыИБ() Экспорт
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НовыеСвойстваНоменклатурыИБ");
	
	Возврат Результат;
	
КонецФункции

Функция XML_КодЕдиницыИзмерения(КодЕдиницыИзмерения)
	
	Результат = МетодСервера(, "XML_КодЕдиницыИзмерения", КодЕдиницыИзмерения);
	
	Возврат Результат;
	
КонецФункции

Функция НаименованиеЕдиницыИзмеренияПоКодуОКЕИ(КодОКЕИ)
	
	Результат = МетодСервера(, "НаименованиеЕдиницыИзмеренияПоКодуОКЕИ", КодОКЕИ);
	
	Возврат Результат;
	
КонецФункции

Функция НовыеСвойстваЭлементаПрослеживаемости()
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НовыеСвойстваЭлементаПрослеживаемости");
	
	Возврат Результат;
	
КонецФункции


#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область СтавкиНДС

Функция МенеджерИнтеграции_ОбщаяРасчетнаяСтавкаНДС(Дата = Неопределено)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ОбщаяРасчетнаяСтавкаНДС", Дата);
	Возврат Результат;
	
КонецФункции

Функция МенеджерИнтеграции_СтавкаБезНДС()
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "СтавкаБезНДС");
	Возврат Результат;
	
КонецФункции

Функция МенеджерИнтеграции_ЭтоСтавкаБезНДС(Ставка)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЭтоСтавкаБезНДС", Ставка);
	Возврат Результат;
	
КонецФункции

Функция МенеджерИнтеграции_ЭтоСтавкаНДСИНА(Знач ПредставлениеСтавки)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЭтоСтавкаНДСИНА", ПредставлениеСтавки);
	Возврат Результат;
	
КонецФункции

Функция СтавкаНДСПоПредставлению(ПредставлениеСтавки, Дата = Неопределено)
	
	Если МенеджерИнтеграции_ЭтоСтавкаНДСИНА(ПредставлениеСтавки) Тогда
		Результат = МенеджерИнтеграции_ОбщаяРасчетнаяСтавкаНДС(Дата);
	Иначе 
		Результат = ЗначениеСтавкиНДС(ПредставлениеСтавки);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗначениеСтавкиНДС(ПредставлениеСтавки)
	
	ЗначениеНДС = Неопределено;
	
	Если ПредставлениеСтавки = Неопределено
		ИЛИ СтрНайти("БЕЗ НДС", ВРег(ПредставлениеСтавки)) > 0 Тогда
		
		ЗначениеНДС = ПредставлениеСтавкиБезНДС();
		
	Иначе
		
		СтрСтавкаНДС = СтрЗаменить(ПредставлениеСтавки, "%", "");
		СтрСтавкаНДС = СтрЗаменить(ПредставлениеСтавки, " ", "");
		СтрСтавкаНДС = СтрЗаменить(ПредставлениеСтавки, "\", "/");
		
		Если СтрНайти("0", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "0";
		ИначеЕсли СтрНайти("5", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "5";
		ИначеЕсли СтрНайти("7", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "7";
		ИначеЕсли СтрНайти("10", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "10";
		ИначеЕсли СтрНайти("18", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "18";
		ИначеЕсли СтрНайти("20", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "20";
		ИначеЕсли СтрНайти("22", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "22";
		ИначеЕсли СтрНайти("10/110", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "10/110";
		ИначеЕсли СтрНайти("18/118", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "18/118";
		ИначеЕсли СтрНайти("20/120", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "20/120";
		ИначеЕсли СтрНайти("22/122", СтрСтавкаНДС) > 0 Тогда
			ЗначениеНДС = "22/122";
		Иначе
			ЗначениеНДС = ПредставлениеСтавки;
		КонецЕсли;
		
	КонецЕсли;
	
	Результат = СтавкаНДСИзСоответствия(ЗначениеНДС);
	
	Возврат Результат;
	
КонецФункции

Функция ПредставлениеСтавкиНДС(ЗначениеСтавки)
	
	НДСИсчисляетсяНалоговымАгентом = (ТипЗнч(ЗначениеСтавки) = Тип("Строка"))
		И МенеджерИнтеграции_ЭтоСтавкаНДСИНА(ЗначениеСтавки);
	
	Если Не ЗначениеЗаполнено(ЗначениеСтавки) Тогда
		
		Результат = "##";
		
	ИначеЕсли НДСИсчисляетсяНалоговымАгентом Тогда
		
		Результат = СтавкаНДСИсчисляетсяНалоговымАгентом();
		
	Иначе
		
		Результат = СтавкаНДСИзСоответствия(, ЗначениеСтавки);
		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Результат = СокрЛП(ЗначениеСтавки);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СтавкаНДСИзСоответствия(ПредставлениеБЭД = "", ПрикладноеЗначение = Неопределено)
	
	Результат = Неопределено;
	
	ПеречислениеСтавокНДС = ЗаполнитьСоответствиеСтавокНДС();
	
	Если ЗначениеЗаполнено(ПредставлениеБЭД) Тогда
		
		Результат = ПеречислениеСтавокНДС.Получить(ПредставлениеБЭД);
		
	Иначе
		
		Для Каждого КлючИЗначение Из ПеречислениеСтавокНДС Цикл
			
			Если КлючИЗначение.Значение = ПрикладноеЗначение Тогда
				
				Результат = КлючИЗначение.Ключ;
				
				Прервать;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗаполнитьСоответствиеСтавокНДС()
	
	Если ИспользуетсяБЭД19() Тогда
		Результат = СоответствиеСтавокНДСИзСправочника();
	Иначе
		Результат = СоответствиеСтавокНДСИзПеречисления();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СоответствиеСтавокНДСИзПеречисления()
	
	Результат = Новый Соответствие;
	
	Результат.Вставить("0", Перечисления.СтавкиНДС.НДС0);
	Результат.Вставить("10", Перечисления.СтавкиНДС.НДС10);
	Результат.Вставить("18", Перечисления.СтавкиНДС.НДС18);
	Результат.Вставить("20", Перечисления.СтавкиНДС.НДС20);
	Результат.Вставить("10/110", Перечисления.СтавкиНДС.НДС10_110);
	Результат.Вставить("18/118", Перечисления.СтавкиНДС.НДС18_118);
	Результат.Вставить("20/120", Перечисления.СтавкиНДС.НДС20_120);
	Результат.Вставить(ПредставлениеСтавкиБезНДС(), Перечисления.СтавкиНДС.БезНДС);
	Результат.Вставить(СтавкаНДСИсчисляетсяНалоговымАгентом(), СтавкаНДСИсчисляетсяНалоговымАгентомПредставление());
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС5") Тогда
		Результат.Вставить("5", Перечисления.СтавкиНДС.НДС5);
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС7") Тогда
		Результат.Вставить("7", Перечисления.СтавкиНДС.НДС7);
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС22") Тогда
		Результат.Вставить("22", Перечисления.СтавкиНДС.НДС22);
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС5_105") Тогда
		Результат.Вставить("5/105", Перечисления.СтавкиНДС.НДС5_105);
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС7_107") Тогда
		Результат.Вставить("7/107", Перечисления.СтавкиНДС.НДС7_107);
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС22_122") Тогда
		Результат.Вставить("22/122", Перечисления.СтавкиНДС.НДС22_122);
	КонецЕсли;
	
	Результат.Вставить(Перечисления.СтавкиНДС.НДС0, "0");
	Результат.Вставить(Перечисления.СтавкиНДС.НДС10, "10");
	Результат.Вставить(Перечисления.СтавкиНДС.НДС18, "18");
	Результат.Вставить(Перечисления.СтавкиНДС.НДС20, "20");
	Результат.Вставить(Перечисления.СтавкиНДС.НДС10_110, "10/110");
	Результат.Вставить(Перечисления.СтавкиНДС.НДС18_118, "18/118");
	Результат.Вставить(Перечисления.СтавкиНДС.НДС20_120, "20/120");
	Результат.Вставить(Перечисления.СтавкиНДС.БезНДС, ПредставлениеСтавкиБезНДС());
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС5") Тогда
		Результат.Вставить(Перечисления.СтавкиНДС.НДС5, "5");
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС7") Тогда
		Результат.Вставить(Перечисления.СтавкиНДС.НДС7, "7");
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС22") Тогда
		Результат.Вставить(Перечисления.СтавкиНДС.НДС22, "22");
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС5_105") Тогда
		Результат.Вставить(Перечисления.СтавкиНДС.НДС5_105, "5/105");
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС7_107") Тогда
		Результат.Вставить(Перечисления.СтавкиНДС.НДС7_107, "7/107");
	КонецЕсли;
	
	Если СуществуетЗначениеПеречисленияСтавкиНДС("НДС22_122") Тогда
		Результат.Вставить(Перечисления.СтавкиНДС.НДС22_122, "22/122");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СоответствиеСтавокНДСИзСправочника()
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СтавкиНДС.Ставка КАК Ставка,
	|	СтавкиНДС.Ссылка КАК Ссылка,
	|	СтавкиНДС.РасчетнаяСтавка КАК РасчетнаяСтавка
	|ИЗ
	|	Справочник.СтавкиНДС КАК СтавкиНДС
	|ГДЕ
	|	НЕ СтавкиНДС.Ссылка = ЗНАЧЕНИЕ(Справочник.СтавкиНДС.БезНДС)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Если ВыборкаДетальныеЗаписи.РасчетнаяСтавка Тогда
			КлючСоответствия = Строка(ВыборкаДетальныеЗаписи.Ставка) + "/" + Строка(ВыборкаДетальныеЗаписи.Ставка + 100);
		Иначе
			КлючСоответствия = Строка(ВыборкаДетальныеЗаписи.Ставка);
		КонецЕсли;
		
		Результат.Вставить(КлючСоответствия, ВыборкаДетальныеЗаписи.Ссылка);
		
	КонецЦикла;
	
	Результат.Вставить(ПредставлениеСтавкиБезНДС(), Справочники.СтавкиНДС.БезНДС);
	Результат.Вставить(СтавкаНДСИсчисляетсяНалоговымАгентом(), СтавкаНДСИсчисляетсяНалоговымАгентомПредставление());
	
	Возврат Результат;
	
КонецФункции

Функция СуществуетЗначениеПеречисленияСтавкиНДС(ИмяЗначенияСтавкаНДС)
	
	ЗначенияПеречисления = Метаданные.Перечисления.СтавкиНДС.ЗначенияПеречисления;
	Результат = (ЗначенияПеречисления.Найти(ИмяЗначенияСтавкаНДС) <> Неопределено);
	
	Возврат Результат;
	
КонецФункции

Функция СтавкаНДСИсчисляетсяНалоговымАгентом()
	
	Возврат "ИсчНалАг";
	
КонецФункции

Функция СтавкаНДСИсчисляетсяНалоговымАгентомПредставление()
	
	Возврат "НДС исчисляется налоговым агентом";
	
КонецФункции

Функция ПредставлениеСтавкиБезНДС()
	
	Возврат "без НДС";
	
КонецФункции

#КонецОбласти

Функция ПредставлениеНомера(НомерОбъекта)
	
	Результат = ПрефиксацияОбъектовКлиентСервер.НомерНаПечать(НомерОбъекта, Ложь, Ложь);
	
	Возврат Результат;
	
КонецФункции

Функция ПредставлениеНомераОбъекта(СсылкаНаОбъект)
	
	МетаданныеОбъекта = СсылкаНаОбъект.Метаданные();
	
	РеквизитыОбъекта = Новый Структура;
	РеквизитыОбъекта.Вставить("Номер");
	
	Если ЕстьРеквизитМетаданных(МетаданныеОбъекта, "ПредставлениеНомера") Тогда
		РеквизитыОбъекта.Вставить("ПредставлениеНомера");
	КонецЕсли;
	
	ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(СсылкаНаОбъект, РеквизитыОбъекта);
	
	Если ЗначенияРеквизитов.Свойство("ПредставлениеНомера") Тогда
		Результат = ЗначенияРеквизитов.ПредставлениеНомера;
	Иначе
		НомерОбъекта = ЗначенияРеквизитов.Номер;
		Результат = ПредставлениеНомера(НомерОбъекта);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВсеДокументыОснования(СсылкаНаОбъект)
	
	МетаданныеОбъекта = СсылкаНаОбъект.Метаданные();
	ИмяДокумента = МетаданныеОбъекта.Имя;
	
	ИмяМетаданныхТаблицыОснований = СтрЗаменить("Документ.%ИмяДокумента%.ТабличнаяЧасть.ДокументыОснования", "%ИмяДокумента%", ИмяДокумента);
	
	Результат = Новый Массив;
	
	Если МетодСервера(, "СуществуетОбъектМетаданных", ИмяМетаданныхТаблицыОснований) Тогда
		
		ТекстЗапроса = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	Т.ДокументОснование КАК ДокументОснование
		|ИЗ
		|	&ПолноеИмяТаблицы КАК Т
		|ГДЕ
		|	Т.Ссылка = &Ссылка";
		
		ПолноеИмяТаблицы = СтрЗаменить("Документ.%ИмяДокумента%.ДокументыОснования", "%ИмяДокумента%", ИмяДокумента);
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПолноеИмяТаблицы", ПолноеИмяТаблицы);
		
		Запрос = Новый Запрос(ТекстЗапроса);
		Запрос.УстановитьПараметр("Ссылка", СсылкаНаОбъект);
		
		Выборка = Запрос.Выполнить().Выбрать();
		
		Пока Выборка.Следующий() Цикл
			Результат.Добавить(Выборка.ДокументОснование);
		КонецЦикла;
		
	ИначеЕсли ЕстьРеквизитМетаданных(МетаданныеОбъекта, "ДокументОснование") Тогда
		
		Основание = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СсылкаНаОбъект, "ДокументОснование");
		Результат.Добавить(Основание);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Получает документ-основание произвольного документа.
// Если оснований несколько, берется только первое.
//
// Параметры:
//  СсылкаНаДокумент - ДокументСсылка - произвольный документ, основание которого требуется получить.
// 
// Возвращаемое значение:
//  ДокументСсылка - документ-основание.
//
Функция ПервыйДокументОснование(СсылкаНаДокумент)
	
	Результат = Неопределено;
	
	МетаданныеДокумента	 = СсылкаНаДокумент.Метаданные();
	ИмяДокумента		 = МетаданныеДокумента.Имя;
	
	ПолноеИмяТаблицыОснований = СтрЗаменить("Документ.%ИмяДокумента%.ТабличнаяЧасть.ДокументыОснования", "%ИмяДокумента%", ИмяДокумента);
	
	Если МетодСервера(, "СуществуетОбъектМетаданных", ПолноеИмяТаблицыОснований) Тогда 
	
		ТекстЗапроса = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	Т.ДокументОснование КАК ДокументОснование
		|ИЗ
		|	Документ.%ИмяДокумента%.ДокументыОснования КАК Т
		|ГДЕ
		|	Т.Ссылка = &Ссылка";
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "%ИмяДокумента%", ИмяДокумента);
		
		Запрос = Новый Запрос(ТекстЗапроса);
		Запрос.УстановитьПараметр("Ссылка", СсылкаНаДокумент);
		
		Выборка = Запрос.Выполнить().Выбрать();
		
		Если Выборка.Следующий() Тогда 
			Результат = Выборка.ДокументОснование;
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитМетаданных(МетаданныеДокумента, "ДокументОснование") Тогда 
		
		Результат = МетодСервера(, "ЗначениеРеквизитаОбъекта", СсылкаНаДокумент, "ДокументОснование");
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Заполняет реквизиты шапки счета на оплату значениями реквизитов документа-основания.
// Для печати счета на оплату многие реквизиты берутся из его документа-основания.
//
// Параметры:
//  ДанныеСчетаНаОплату - Структура - см. функцию ДанныеШапки.
//
Процедура ДополнитьШапкуСчетаНаОплатуПоДокументуОснованию(ДанныеСчетаНаОплату)
	
	СсылкаНаОбъект = ДанныеСчетаНаОплату.ОписаниеДокумента.Ссылка;
	ДокументОснование = ПервыйДокументОснование(СсылкаНаОбъект);
	ДанныеДокументаОснования = ДанныеШапки(ДокументОснование);
	
	КопируемыеРеквизиты = Новый Массив;
	КопируемыеРеквизиты.Добавить("ХозяйственнаяОперация");
	КопируемыеРеквизиты.Добавить("НалогообложениеНДС");
	КопируемыеРеквизиты.Добавить("БанковскийСчетКонтрагента");
	КопируемыеРеквизиты.Добавить("ЦенаВключаетНДС");
	КопируемыеРеквизиты.Добавить("АдресДоставки");
	
	Для Каждого ИмяРеквизита Из КопируемыеРеквизиты Цикл 
		
		Если ЗначениеЗаполнено(ДанныеДокументаОснования[ИмяРеквизита]) Тогда 
			ДанныеСчетаНаОплату.Вставить(ИмяРеквизита, ДанныеДокументаОснования[ИмяРеквизита]);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// Дополняет реквизиты шапки счета-фактуры комиссионеру.
//
// Параметры:
//  Шапка - Структура - см. функцию ДанныеШапки.
//
Процедура ДополнитьШапкуСчетаФактурыКомиссионеру(Шапка)
	
	ОписаниеДокумента = Шапка.ОписаниеДокумента;
	СсылкаНаОбъект = ОписаниеДокумента.Ссылка;
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СФКомиссионеру.Ссылка КАК Ссылка,
	|	ВЫБОР
	|		КОГДА ЕСТЬNULL(ДанныеКонтрагента.ОбособленноеПодразделение, ЛОЖЬ)
	|			ТОГДА ДанныеКонтрагента.ГоловнойКонтрагент
	|		ИНАЧЕ ТЧПокупатели.Покупатель
	|	КОНЕЦ КАК Покупатель,
	|	ВЫБОР
	|		КОГДА ТЧПокупатели.ИННПокупателя ПОДОБНО """"
	|			ТОГДА ВЫБОР
	|					КОГДА ТЧПокупатели.Покупатель ССЫЛКА Справочник.Контрагенты
	|						ТОГДА ЕСТЬNULL(ДанныеКонтрагента.ИНН, """")
	|					КОГДА НЕ ДанныеОрганизацийПокупатель.Ссылка ЕСТЬ NULL
	|						ТОГДА ЕСТЬNULL(ДанныеОрганизацийПокупатель.ИНН, """")
	|					ИНАЧЕ """"
	|				КОНЕЦ
	|		ИНАЧЕ ТЧПокупатели.ИННПокупателя
	|	КОНЕЦ КАК ПокупательИНН,
	|	ВЫБОР
	|		КОГДА ТЧПокупатели.КПППокупателя ПОДОБНО """"
	|			ТОГДА ВЫБОР
	|					КОГДА ТЧПокупатели.Покупатель ССЫЛКА Справочник.Контрагенты
	|						ТОГДА """"
	|					КОГДА НЕ ДанныеОрганизацийПокупатель.Ссылка ЕСТЬ NULL
	|						ТОГДА ЕСТЬNULL(ДанныеОрганизацийПокупатель.КПП, """")
	|					ИНАЧЕ """"
	|				КОНЕЦ
	|		ИНАЧЕ ТЧПокупатели.КПППокупателя
	|	КОНЕЦ КАК ПокупательКПП,
	|	ТЧПокупатели.Покупатель КАК Грузополучатель
	|ИЗ
	|	Документ.СчетФактураКомиссионеру.Покупатели КАК ТЧПокупатели
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.СчетФактураКомиссионеру КАК СФКомиссионеру
	|		ПО (СФКомиссионеру.Ссылка = ТЧПокупатели.Ссылка)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК ДанныеКонтрагента
	|		ПО (ДанныеКонтрагента.Ссылка = ТЧПокупатели.Покупатель)
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Организации КАК ДанныеОрганизацийПокупатель
	|		ПО (ДанныеОрганизацийПокупатель.Ссылка = ТЧПокупатели.Покупатель)
	|ГДЕ
	|	СФКомиссионеру.Ссылка = &СсылкаНаОбъект";
	
	Если 	ЭтоСчетФактураНаРеализациюЧерезКомиссионера(СсылкаНаОбъект)
		Или ЭтоСчетФактураНаРеализациюЧерезКомиссионера_25(СсылкаНаОбъект) Тогда
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса
			, "Документ.СчетФактураКомиссионеру"
			, "Документ.СчетФактураВыданный");
	
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СсылкаНаОбъект);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если НЕ РезультатЗапроса.Пустой() Тогда
		
		Выборка = РезультатЗапроса.Выбрать();
		
		Если Выборка.Количество() > 1 Тогда
			
			ОписаниеОш = НСтр("ru = 'Отправка сводных счетов-фактур не поддерживается!';
			|en = 'Отправка сводных счетов-фактур не поддерживается!'");
			Шапка.ОшибкиВДанных.Добавить(ОписаниеОш);
			
		Иначе
			
			Выборка.Следующий();
			
			Шапка.Контрагент	  = Выборка.Покупатель;
			Шапка.КонтрагентИНН	  = Выборка.ПокупательИНН;
			Шапка.КонтрагентКПП	  = Выборка.ПокупательКПП;
			Шапка.Грузополучатель = Выборка.Грузополучатель;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Заполняет реквизиты шапки счета-фактуры значениями реквизитов документа-основания.
// Для печати счета-фактуры многие реквизиты берутся из его документа-основания.
//
// Параметры:
//  ДанныеСчетаФактуры - Структура - см. функцию ДанныеШапки.
//
Процедура ДополнитьШапкуСчетаФактурыПоДокументуОснованию(ДанныеСчетаФактуры)
	
	СсылкаНаОбъект = ДанныеСчетаФактуры.ОписаниеДокумента.Ссылка;
	
	ДокументОснование		 = ПервыйДокументОснование(СсылкаНаОбъект);
	ДанныеДокументаОснования = ДанныеШапки(ДокументОснование);
	
	КопируемыеРеквизиты = Новый Массив;
	КопируемыеРеквизиты.Добавить("ХозяйственнаяОперация");
	КопируемыеРеквизиты.Добавить("Грузополучатель");
	КопируемыеРеквизиты.Добавить("Грузоотправитель");
	КопируемыеРеквизиты.Добавить("ВалютаДокумента");
	КопируемыеРеквизиты.Добавить("ВалютаДокументаКод");
	КопируемыеРеквизиты.Добавить("НалогообложениеНДС");
	КопируемыеРеквизиты.Добавить("БанковскийСчетОрганизации");
	КопируемыеРеквизиты.Добавить("БанковскийСчетКонтрагента");
	КопируемыеРеквизиты.Добавить("БанковскийСчетГрузоотправителя");
	КопируемыеРеквизиты.Добавить("БанковскийСчетГрузополучателя");
	КопируемыеРеквизиты.Добавить("ЦенаВключаетНДС");
	КопируемыеРеквизиты.Добавить("АдресДоставки");
	КопируемыеРеквизиты.Добавить("ОбособленноеПодразделение");
	КопируемыеРеквизиты.Добавить("ЦифровойИндексОбособленногоПодразделения");
	
	Для Каждого ИмяРеквизита Из КопируемыеРеквизиты Цикл 
		
		Если ЗначениеЗаполнено(ДанныеДокументаОснования[ИмяРеквизита]) Тогда 
			ДанныеСчетаФактуры.Вставить(ИмяРеквизита, ДанныеДокументаОснования[ИмяРеквизита]);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ЭтоСчетФактураКомиссионеру(СсылкаНаДокумент)

	Результат = Ложь;
	
	Если ТипЗнч(СсылкаНаДокумент) = Тип("ДокументСсылка.СчетФактураКомиссионеру") Тогда
		
		Результат = Истина;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции 

Функция ЭтоСчетФактураНаРеализациюЧерезКомиссионера(СсылкаНаДокумент)

	Результат = Ложь;
	
	Если ТипЗнч(СсылкаНаДокумент) = Тип("ДокументСсылка.СчетФактураВыданный") 
		И ИспользуетсяРеализацияЧерезКомиссионера() Тогда
		
		Реквизиты = ЗначенияРеквизитовОбъектаЛокальная(СсылкаНаДокумент, "ДокументОснование,РеализацияЧерезКомиссионера");
		
		Если НЕ ЭтоНакладная(Реквизиты.ДокументОснование) Тогда
		
			Результат = Реквизиты.РеализацияЧерезКомиссионера;
		
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоСчетФактураНаРеализациюЧерезКомиссионера_25(СсылкаНаДокумент)
	
	Результат = Ложь;
	
	Если ТипЗнч(СсылкаНаДокумент) = Тип("ДокументСсылка.СчетФактураВыданный") 
		И ИспользуетсяРеализацияЧерезКомиссионера() Тогда
		
		ОснованиеСФ = ПервыйДокументОснование(СсылкаНаДокумент);
		
		Если ЭтоНакладная(ОснованиеСФ) Тогда
			
			Реквизиты = ЗначенияРеквизитовОбъектаЛокальная(ОснованиеСФ, "ХозяйственнаяОперация");
			
			ЭтоОперацияРеализацияЧерезКомиссионера = (Реквизиты.ХозяйственнаяОперация = ПредопределенноеЗначение("Перечисление.ХозяйственныеОперации.РеализацияЧерезКомиссионера"));
			
			Если ЭтоОперацияРеализацияЧерезКомиссионера Тогда
				
				Результат = Истина;	
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоСчетФактура(ОписаниеМетаданных)
	
	ТипПараметра = ТипЗнч(ОписаниеМетаданных);
	
	Если ТипПараметра = Тип("ОбъектМетаданных") Тогда
		ОбъектМетаданных = ОписаниеМетаданных;
	ИначеЕсли ТипПараметра = Тип("Тип") Тогда
		ОбъектМетаданных = Метаданные.НайтиПоТипу(ОписаниеМетаданных);
	ИначеЕсли ТипПараметра = Тип("Строка") Тогда
		ОбъектМетаданных = Метаданные.НайтиПоПолномуИмени(ОписаниеМетаданных);
	Иначе // Ссылка или объект
		ОбъектМетаданных = ОписаниеМетаданных.Метаданные();
	КонецЕсли;
	
	ИменаСчетовФактур = Новый Массив;
	ИменаСчетовФактур.Добавить("СчетФактураВыданный");
	ИменаСчетовФактур.Добавить("СчетФактураВыданныйАванс");
	ИменаСчетовФактур.Добавить("СчетФактураКомиссионеру");
	ИменаСчетовФактур.Добавить("СчетФактураКомитента");
	ИменаСчетовФактур.Добавить("СчетФактураНалоговыйАгент");
	ИменаСчетовФактур.Добавить("СчетФактураНаНеподтвержденнуюРеализацию0");
	
	Результат = (ИменаСчетовФактур.Найти(ОбъектМетаданных.Имя) <> Неопределено);
	
	Возврат Результат;
	
КонецФункции

Функция ДокументРеализацииСодержитТолькоУслуги(СсылкаНаОбъект)
	
	ШаблонЗапроса =
		"ВЫБРАТЬ
		|	ЕСТЬNULL(МАКСИМУМ(ВЫБОР
		|			КОГДА ТЧ.Номенклатура.ТипНоменклатуры В (&ТипыНоменклатурыУслуги)
		|				ТОГДА ИСТИНА
		|			ИНАЧЕ ЛОЖЬ
		|		КОНЕЦ), Ложь) КАК ЕстьУслуги,
		|	ЕСТЬNULL(МАКСИМУМ(ВЫБОР
		|			КОГДА НЕ ТЧ.Номенклатура.ТипНоменклатуры В (&ТипыНоменклатурыУслуги)
		|				ТОГДА ИСТИНА
		|			ИНАЧЕ ЛОЖЬ
		|		КОНЕЦ), Ложь) КАК ЕстьТовары
		|ИЗ
		|	&ПолноеИмяТаблицы КАК ТЧ
		|ГДЕ
		|	ТЧ.Ссылка = &СсылкаНаОбъект";
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СсылкаНаОбъект);
	Запрос.УстановитьПараметр("ТипыНоменклатурыУслуги", ТипыНоменклатурыУслуги());
	
	ПолноеИмяТаблицы = СтрШаблон("%1.%2"
			, СсылкаНаОбъект.Метаданные().ПолноеИмя()
			, "Товары");
	
	Запрос.Текст = СтрЗаменить(ШаблонЗапроса
			, "&ПолноеИмяТаблицы"
			, ПолноеИмяТаблицы);
	
	Результат = Ложь;
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Результат = Выборка.ЕстьУслуги И НЕ Выборка.ЕстьТовары;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ДокументПродажи_ОплатаВВалюте(ПсевдонимТаблицы)
	
	Если ЭтоРедакция11_5() Тогда
		
		Результат = "ПсевдонимТаблицы.ОплатаВВалюте";
		
	Иначе
		
		Результат =
		"ВЫБОР
		|	КОГДА ПсевдонимТаблицы.ПорядокОплаты В (ЗНАЧЕНИЕ(Перечисление.ПорядокОплатыПоСоглашениям.РасчетыВВалютеОплатаВВалюте), ЗНАЧЕНИЕ(Перечисление.ПорядокОплатыПоСоглашениям.РасчетыВРубляхОплатаВВалюте))
		|		ТОГДА ИСТИНА
		|	ИНАЧЕ ЛОЖЬ
		|КОНЕЦ";
		
	КонецЕсли;
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаОбъект)
	
	ТипСсылки = ТипЗнч(СсылкаНаОбъект);
	Если ТипСсылки = Тип("ДокументСсылка.СчетФактураВыданныйАванс") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ИспользуетсяПередачаТоваровХранителю() 
		И ТипСсылки = Тип("ДокументСсылка.ПередачаТоваровХранителю") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ДокументПродажи = СсылкаНаОбъект;
	Если ЭтоСчетФактура(СсылкаНаОбъект) Тогда
		ДокументПродажи = ПервыйДокументОснование(СсылкаНаОбъект);
	КонецЕсли;
	
	МетаданныеОбъекта = ДокументПродажи.Метаданные();
	
	ТипДокументаПродажи = ТипЗнч(ДокументПродажи);
	
	ПсевдонимТаблицы = "";
	Если ТипДокументаПродажи = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		ПсевдонимТаблицы = "ДокументОснование";
	КонецЕсли;
	
	ПолеВалюта = "Валюта";
	ПолеВалютаВзаиморасчетов = "Валюта";
	Если ЕстьРеквизитМетаданных(МетаданныеОбъекта, "ВалютаВзаиморасчетов") Тогда
		ПолеВалютаВзаиморасчетов = "ВалютаВзаиморасчетов";
	КонецЕсли;
	
	Если ТипДокументаПродажи = Тип("ДокументСсылка.ВозвратТоваровМеждуОрганизациями") Тогда
		ПолеОплатаВВалюте = "ЛОЖЬ";
	Иначе
		ПолеОплатаВВалюте = ТекстЗапроса_ДокументПродажи_ОплатаВВалюте(ПсевдонимТаблицы);
	КонецЕсли;
	
	ТребуемыеРеквизиты = Новый Структура;
	ТребуемыеРеквизиты.Вставить("Валюта", ПолеВалюта);
	ТребуемыеРеквизиты.Вставить("ВалютаВзаиморасчетов", ПолеВалютаВзаиморасчетов);
	ТребуемыеРеквизиты.Вставить("ОплатаВВалюте", ПолеОплатаВВалюте);
	
	ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ДокументПродажи, ТребуемыеРеквизиты);
	
	ВалютаРегл = ВалютаРегламентированногоУчета();
	
	Результат = (ЗначенияРеквизитов.Валюта <> ВалютаРегл
				 Или ЗначенияРеквизитов.ВалютаВзаиморасчетов <> ВалютаРегл)
				И Не ЗначенияРеквизитов.ОплатаВВалюте;
	
	Возврат Результат;
	
КонецФункции

Процедура ПроверитьЗаполнитьВалюту(Шапка)
	
	СсылкаНаОбъект = Шапка.ОписаниеДокумента.Ссылка;
	
	Если ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетФактураВыданныйАванс")
		Или ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаОбъект) Тогда
		
		ВалютаРегл = ВалютаРегламентированногоУчета();
		Шапка.ВалютаДокумента	 = ВалютаРегл;
		Шапка.ВалютаДокументаКод = ВалютаРегл.Код;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ЭтоОбособленноеПодразделение(ЮрФизЛицо)
	
	Результат = Ложь;
	
	Если Не ЗначениеЗаполнено(ЮрФизЛицо) Тогда 
		Возврат Результат;
	КонецЕсли;
	
	МетаданныеОбъекта = ЮрФизЛицо.Метаданные();
	
	Если ЕстьРеквизитМетаданных(МетаданныеОбъекта, "ОбособленноеПодразделение", Ложь) Тогда 
		
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЮрФизЛицо, "ОбособленноеПодразделение");
		
	ИначеЕсли ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.РегистрацииВНалоговомОргане") Тогда 
		
		Результат = Истина;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция РегистрацияПодразделенияВНалоговомОргане(Подразделение, Организация, ДатаДокумента)
	
	Результат = Неопределено;
	
	Если Не ЗначениеЗаполнено(Подразделение) 
		Или ТипЗнч(Подразделение) <> Тип("СправочникСсылка.СтруктураПредприятия") Тогда 
		Возврат Неопределено;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Организация) Тогда 
		
		ТекстОшибки = НСтр("ru = 'Ошибка при получении сведений об обособленном подразделении ""%1"":
                            |Не указана организация!'");
		ТекстОшибки = СтрШаблон(ТекстОшибки, Строка(Подразделение));
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если СравнитьВерсии(ВерсияУТ, "11.5.9.116") >= 0 Тогда
		
		ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Т.РегистрацияВНалоговомОргане КАК РегистрацияВНалоговомОргане
		|ИЗ
		|	РегистрСведений.РегистрацииВНалоговомОргане.СрезПоследних(
		|			&Дата,
		|			Подразделение = &Подразделение
		|				И Организация = &Организация) КАК Т";
		
	Иначе
		
		ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Т.РегистрацияВНалоговомОргане КАК РегистрацияВНалоговомОргане
		|ИЗ
		|	РегистрСведений.РегистрацииВНалоговомОргане КАК Т
		|ГДЕ
		|	Т.Подразделение = &Подразделение
		|	И Т.Организация = &Организация";
		
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Подразделение"	, Подразделение);
	Запрос.УстановитьПараметр("Организация"		, Организация);
	Запрос.УстановитьПараметр("Дата"			, ДатаДокумента);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда 
		Результат = Выборка.РегистрацияВНалоговомОргане;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция РеквизитыПродавца(Организация, Период, БанковскийСчет, ОбособленноеПодразделение)
	
	РеквизитыОрганизации = РеквизитыЮрФизЛица(Организация, Период, БанковскийСчет);
	
	Если ЗначениеЗаполнено(ОбособленноеПодразделение) Тогда 
		
		РеквизитыПодразделения = РеквизитыЮрФизЛица(ОбособленноеПодразделение, Период);
		
		РеквизитыОрганизации.КПП = РеквизитыПодразделения.КПП;
		
	КонецЕсли;
	
	Возврат РеквизитыОрганизации;
	
КонецФункции

Функция РеквизитыГрузоотправителя(Организация, Период, БанковскийСчет, ОбособленноеПодразделение)
	
	РеквизитыОрганизации = РеквизитыЮрФизЛица(Организация, Период, БанковскийСчет);
	
	Если ЗначениеЗаполнено(ОбособленноеПодразделение) Тогда 
		
		РеквизитыПодразделения = РеквизитыЮрФизЛица(ОбособленноеПодразделение, Период);
		
		КопируемыеРеквизиты = Новый Массив;
		КопируемыеРеквизиты.Добавить("Представление");
		КопируемыеРеквизиты.Добавить("СокращенноеНаименование");
		КопируемыеРеквизиты.Добавить("ПолноеНаименование");
		КопируемыеРеквизиты.Добавить("НаименованиеДляПечатныхФорм");
		КопируемыеРеквизиты.Добавить("КПП");
		КопируемыеРеквизиты.Добавить("КодПоОКПО");
		КопируемыеРеквизиты.Добавить("ФактическийАдрес");
		КопируемыеРеквизиты.Добавить("ЗначенияПолейФактическийАдрес");
		
		Для Каждого ИмяСвойства Из КопируемыеРеквизиты Цикл 
			РеквизитыОрганизации[ИмяСвойства] = РеквизитыПодразделения[ИмяСвойства]
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат РеквизитыОрганизации;
	
КонецФункции

Процедура ЗаполнитьИнформациюОбОрганизации(СведенияОбОрганизации, РеквизитыОрганизации)
	
	#Область ОбщиеСвойства
	СведенияОбОрганизации.Name	 = РеквизитыОрганизации.НаименованиеДляПечатныхФорм;
	СведенияОбОрганизации.Inn	 = РеквизитыОрганизации.ИНН;
	СведенияОбОрганизации.Kpp	 = РеквизитыОрганизации.КПП;
	СведенияОбОрганизации.Okpo	 = РеквизитыОрганизации.КодПоОкпо;
	СведенияОбОрганизации.Phone	 = РеквизитыОрганизации.Телефоны;
	
	СведенияОбОрганизации.BankAccountNumber		 = СокрЛП(РеквизитыОрганизации.НомерСчета);
	СведенияОбОрганизации.BankId				 = СокрЛП(РеквизитыОрганизации.БИК);
	СведенияОбОрганизации.BankName				 = ?(ЗначениеЗаполнено(РеквизитыОрганизации.Банк), Строка(РеквизитыОрганизации.Банк), ""); // в некоторых случаях Банк возвращается как ссылка на справочник и компонента игнорирует такое значение, теряя данные, поэтому значение Банк преобразовано к строке 
	#КонецОбласти
	
	ТипОрганизации = ЮрФизЛицо_ТипОрганизацииДляКонтента(РеквизитыОрганизации);
	
	#Область Свойства_ExtendedOrganizationInfo
	Если СведенияОбОрганизации.Свойство("CorrespondentAccount") Тогда 
		СведенияОбОрганизации.CorrespondentAccount = РеквизитыОрганизации.КоррСчет;
	КонецЕсли;
	
	Если СведенияОбОрганизации.Свойство("Email") Тогда
		СведенияОбОрганизации.Email = РеквизитыОрганизации.Email;
	КонецЕсли;
	
	Если СведенияОбОрганизации.Свойство("Type") Тогда 
		СведенияОбОрганизации.Type = ТипОрганизации;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(РеквизитыОрганизации.Свидетельство)
		И СведенияОбОрганизации.Свойство("IndividualEntityRegistrationCertificate") Тогда
		СведенияОбОрганизации.IndividualEntityRegistrationCertificate = РеквизитыОрганизации.Свидетельство;
	КонецЕсли;
	#КонецОбласти
	
	#Область Свойства_OrganizationInfo
	Если СведенияОбОрганизации.Свойство("IsSoleProprietor") Тогда 
		СведенияОбОрганизации.IsSoleProprietor = (ТипОрганизации = "IndividualEntity");
	КонецЕсли;
	#КонецОбласти
	
КонецПроцедуры

Функция ЮрФизЛицо_ТипОрганизацииДляКонтента(Реквизиты, УДАЛИТЬ_ЭтоИП = Неопределено, УДАЛИТЬ_СтранаРегистрацииКод = Неопределено)
	
	Если ЗначениеЗаполнено(Реквизиты.СтранаРегистрации)
		И Реквизиты.СтранаРегистрации <> Справочники.СтраныМира.Россия
		И Не ЗначениеЗаполнено(Реквизиты.ИНН) Тогда
		Результат = "ForeignEntity";
	ИначеЕсли Реквизиты.ТипЮрФизЛица = ТипыЮрФизЛиц().ФизическоеЛицо Тогда 
		Результат = "IndividualEntity";
	Иначе 
		Результат = "LegalEntity";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура АктуализироватьРасчетыСКонтрагентами(ДанныеШапки)
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если ДанныеШапки.ВалютаДокумента = ВалютаРегламентированногоУчета()
		Или СравнитьВерсии(ВерсияУТ, "11.4.1") < 0 Тогда
		Возврат;
	КонецЕсли;
	
	ОписаниеДокумента = ДанныеШапки.ОписаниеДокумента;
	СсылкаНаОбъект = ОписаниеДокумента.Ссылка;
	
	Если НоваяАрхитектураВзаиморасчетов() Тогда
		
		МассивДокументов = Новый Массив;
		МассивДокументов.Добавить(СсылкаНаОбъект);
		
		Если ЭтоРедакция11_5() Тогда
			РегистрыСведений.СуммыДокументовВВалютахУчета.РассчитатьСуммыДокументовВВалютахУчета(МассивДокументов);
		Иначе
			РегистрыСведений.СуммыДокументовВВалютеРегл.РассчитатьСуммыДокументовВВалютеРегл(МассивДокументов);
		КонецЕсли;
		
	Иначе
		
		Запрос = Новый Запрос;
		Запрос.УстановитьПараметр("Ссылка", СсылкаНаОбъект);
		Запрос.Текст = "
		|ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	РасчетыСКлиентами.АналитикаУчетаПоПартнерам КАК АналитикаУчетаПоПартнерам
		|ИЗ
		|	РегистрНакопления.РасчетыСКлиентами КАК РасчетыСКлиентами
		|
		|ГДЕ
		|	РасчетыСКлиентами.Регистратор = &Ссылка
		|	И РасчетыСКлиентами.Активность
		|";
		ТаблицаАналитик = Запрос.Выполнить().Выгрузить();
		МассивАналитикУчетаПоПартнерам = ТаблицаАналитик.ВыгрузитьКолонку("АналитикаУчетаПоПартнерам");
		
		МодульРаспределениеВзаиморасчетовВызовСервера = ОбщегоНазначения.ОбщийМодуль("РаспределениеВзаиморасчетовВызовСервера");
		
		Если МассивАналитикУчетаПоПартнерам.Количество() > 0 Тогда
			
			ОкончаниеПериодаРасчета = КонецМесяца(ОписаниеДокумента.Дата) + 1;
			АналитикиРасчета = МодульРаспределениеВзаиморасчетовВызовСервера.АналитикиРасчета();
			АналитикиРасчета.АналитикиУчетаПоПартнерам = МассивАналитикУчетаПоПартнерам;
			
			Попытка
				МодульРаспределениеВзаиморасчетовВызовСервера.РаспределитьВсеРасчетыСКлиентами(ОкончаниеПериодаРасчета, АналитикиРасчета);
			Исключение
				
				Ошибка = ИнформацияОбОшибке();
				
				ШаблонСообщения = НСтр("ru = 'Не удалось актуализировать состояние взаиморасчетов:
									   |%1
									   |Необходимо выполнить распределение взаиморасчетов вручную'");
				
				ЗаписьЖурналаРегистрации(НСтр("ru = 'Диадок'"),
										 УровеньЖурналаРегистрации.Ошибка,
										 СсылкаНаОбъект.Метаданные(),
										 СсылкаНаОбъект,
										 СтрШаблон(ШаблонСообщения, ПодробноеПредставлениеОшибки(Ошибка)));
				
				ДанныеШапки.ОшибкиВДанных.Добавить(СтрШаблон(ШаблонСообщения, КраткоеПредставлениеОшибки(Ошибка)));
				
			КонецПопытки;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ДобавитьОшибкиВалидацииИзДанныхШапки(ДанныеШапки, Content)
	
	Если Не ЗначениеЗаполнено(ДанныеШапки.ОшибкиВДанных) Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого ТекстОшибки Из ДанныеШапки.ОшибкиВДанных Цикл
		МетодСервера(, "ДобавитьОшибкуВалидации", Content.ОшибкиВДанных, ТекстОшибки);
	КонецЦикла;
	
КонецПроцедуры

// Удаляет позиции товаров с ндс, где нет изменения суммы НДС
//
// Параметры:
//  ТаблицаТоваров - ТаблицаЗначений - исходная таблица товаров
Процедура УдалитьСтрокиНеСоответствующие736Формату(ТаблицаТоваров)
	
	Кэш = ТаблицаТоваров.СкопироватьКолонки();
	
	Для Каждого СтрокаТЧ Из ТаблицаТоваров Цикл 
		
		ТоварСНДС				= СтрокаТЧ.СуммаНДС > 0 Или СтрокаТЧ.СуммаНДСДоИзменения > 0;
		ПравильныйТоварСНДС		= ТоварСНДС И СтрокаТЧ.СуммаНДС <> СтрокаТЧ.СуммаНДСДоИзменения;		
		ПравильныйТоварБезНДС	= СтрокаТЧ.СуммаНДС = 0;
		
		Если ПравильныйТоварСНДС 
			Или ПравильныйТоварБезНДС Тогда
			
			НоваяСтрока = Кэш.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаТЧ);
			
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблицаТоваров = Кэш;
	
КонецПроцедуры

Функция ИсходныйСчетФактура(СчетФактура)
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если СравнитьВерсии(ВерсияУТ, "11.4.9") >= 0 Тогда
		
		ТекстЗапроса =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	Т.ПредыдущийСчетФактура КАК ИсходныйДокумент
		|ИЗ
		|	Документ.СчетФактураВыданный.Товары КАК Т
		|ГДЕ
		|	Т.Ссылка = &Ссылка";
		
	Иначе
		
		ТекстЗапроса =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	Т.ИсходныйДокумент КАК ИсходныйДокумент
		|ИЗ
		|	Документ.СчетФактураВыданный.ДокументыОснования КАК Т
		|ГДЕ
		|	Т.Ссылка = &Ссылка";
		
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Ссылка", СчетФактура);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Результат = Неопределено;
	Если Выборка.Следующий() Тогда
		Результат = Выборка.ИсходныйДокумент;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьЗаголовокАкта172(Content, ДанныеШапки)
	
	ДатаНомер = ДанныеШапки.ДатаНомерДокумента;
	ОписаниеДокумента = ДанныеШапки.ОписаниеДокумента;
	СсылкаНаОбъект = ОписаниеДокумента.Ссылка;
	СтруктураОснования = СтруктурированноеПредставлениеОснования(СсылкаНаОбъект);
	
	ПредставлениеИсполнителя = сформироватьПредставлениеКонтрагентаДляАкта(ДанныеШапки.Организация, ДатаНомер.ДатаДокумента);
	ПредставлениеЗаказчика	 = сформироватьПредставлениеКонтрагентаДляАкта(ДанныеШапки.Контрагент, ДатаНомер.ДатаДокумента);
	ПредставлениеОснования	 = "";
	Если ЗначениеЗаполнено(СтруктураОснования)
		И ЗначениеЗаполнено(СтруктураОснования.Presentation) Тогда
		ПредставлениеОснования = " на основании " + СтруктураОснования.Presentation;
	КонецЕсли;
	
	ЧастиЗаголовка = Новый Массив;
	ЧастиЗаголовка.Добавить("Мы, нижеподписавшиеся, представитель %1 (далее «Исполнитель»), с одной стороны,");
	ЧастиЗаголовка.Добавить("и представитель %2 (далее «Заказчик»), с другой стороны,");
	ЧастиЗаголовка.Добавить("составили настоящий акт о том, что «Исполнитель» выполнил,");
	ЧастиЗаголовка.Добавить("а  «Заказчик» принял следующие работы (услуги)%3:");
	
	ШаблонЗаголовка = СтрСоединить(ЧастиЗаголовка, " ");
	
	Content.Title = СтрШаблон(ШаблонЗаголовка
		, ПредставлениеИсполнителя
		, ПредставлениеЗаказчика
		, ПредставлениеОснования);
	
КонецПроцедуры

Процедура ЗаполнитьДополнительныеСведения(Content, ДанныеШапки, ПараметрыФормирования)
	
	ДопСведения = ПараметрыФормирования.ДополнительныеСведенияСтрокой;
	
	ИдКонтрагента = МетодСервера(, "Контрагент_2_CounteragentBoxID", ДанныеШапки.Контрагент);
	Если МетодСервера(, "НужноФормироватьДопИнфоПоШаблону", ИдКонтрагента) = Истина Тогда
		ДопСведения = МетодСервера(, "СформироватьСтрокуДопИнфоПоШаблону", ДопСведения);
	КонецЕсли;
	
	Content.AdditionalInfo = ДопСведения;
	
КонецПроцедуры

Процедура ЗаполнитьПодписантаАкта172(Content, Шапка, ПараметрыФормирования)
	
	ПараметрыПодписания = МетодСервера(, "ПолучитьПараметрыПодписанияТ12вДиадок", Шапка.Организация);
	
	ПараметрыОтправкиНаСогласование = ПараметрыФормирования.ПараметрыСогласования;
	
	Если НЕ ПараметрыОтправкиНаСогласование = Неопределено Тогда
		Если ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПодписанта) Тогда
			ПараметрыПодписания.ФИОПодписанта 		= ПараметрыОтправкиНаСогласование.ФИОПодписанта;
			ПараметрыПодписания.ДолжностьПодписанта = ПараметрыОтправкиНаСогласование.ДолжностьПодписанта;
		Иначе
			ФИОПолучателя = ПараметрыОтправкиНаСогласование.ФИОПолучателя;
			ПараметрыПодписания.ФИОПодписанта 		= ПараметрыОтправкиНаСогласование.ФИОПолучателя;
			ПараметрыПодписания.ДолжностьПодписанта = ПараметрыОтправкиНаСогласование.ДолжностьПолучателя;
		КонецЕсли;
	КонецЕсли;
	
	СведенияПодписанта = МетодСервера(, "ПредставлениеВизирующегоСтруктурой", ПараметрыПодписания);
	
	МетодСервера(, "ЗаполнитьПодписантаДляXmlTorg12", content.Official, СведенияПодписанта);
	
КонецПроцедуры

Функция РеквизитыКорректировочногоСчетаФактуры(СсылкаНаОбъект)
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	ЕстьПредставлениеНомера = ОбщегоНазначения.ЕстьРеквизитОбъекта("ПредставлениеНомера", СсылкаНаОбъект.Метаданные());
	
	Если СравнитьВерсии(ВерсияУТ, "11.4.9") > 0 Тогда // сведения об исходных документах в ТЧ Товары
		
		ТекстЗапроса =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Таблица.ПредыдущийСчетФактура КАК ИсходныйДокумент,
		|	ЕСТЬNULL(Таблица.ПредыдущийСчетФактура.Номер, """") КАК НомерИсходногоДокумента,
		|	ЕСТЬNULL(Таблица.ПредыдущийСчетФактура.ПредставлениеНомера, """") КАК ПредставлениеНомераИсходногоДокумента,
		|	ЕСТЬNULL(Таблица.ПредыдущийСчетФактура.СчетФактураОснование.Дата, Таблица.ПредыдущийСчетФактура.Дата) КАК ДатаИсходногоДокумента,
		|	ЕСТЬNULL(Таблица.ПредыдущийСчетФактура.НомерИсправления, """") КАК НомерИсправленияИсходногоДокумента,
		|	ЕСТЬNULL(Таблица.ПредыдущийСчетФактура.Исправление, ЛОЖЬ) КАК УчитыватьИсправлениеИсходногоДокумента,
		|	ВЫБОР
		|		КОГДА Таблица.ПредыдущийСчетФактура.Исправление
		|			ТОГДА ЕСТЬNULL(Таблица.ПредыдущийСчетФактура.Дата, ДАТАВРЕМЯ(1, 1, 1))
		|		ИНАЧЕ ДАТАВРЕМЯ(1, 1, 1)
		|	КОНЕЦ КАК ДатаИсправленияИсходногоДокумента
		|ИЗ
		|	Документ.СчетФактураВыданный.Товары КАК Таблица
		|ГДЕ
		|	Таблица.Ссылка = &СсылкаНаОбъект
		|
		|УПОРЯДОЧИТЬ ПО
		|	ИсходныйДокумент";
		
	Иначе // Сведения об исходных документах в ТЧ ДокументыОснования
		
		ТекстЗапроса =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Таблица.ИсходныйДокумент КАК ИсходныйДокумент,
		|	Таблица.НомерИсходногоДокумента КАК НомерИсходногоДокумента,
		|	Таблица.ДатаИсходногоДокумента КАК ДатаИсходногоДокумента,
		|	Таблица.НомерИсправленияИсходногоДокумента КАК НомерИсправленияИсходногоДокумента,
		|	Таблица.ДатаИсправленияИсходногоДокумента КАК ДатаИсправленияИсходногоДокумента,
		|	ЕСТЬNULL(Таблица.ИсходныйДокумент.Исправление, ЛОЖЬ) КАК УчитыватьИсправлениеИсходногоДокумента,
		|	Выразить("""" КАК Строка(500)) КАК ПредставлениеНомераИсходногоДокумента
		|ИЗ
		|	Документ.СчетФактураВыданный.ДокументыОснования КАК Таблица
		|ГДЕ
		|	Таблица.Ссылка = &СсылкаНаОбъект
		|
		|УПОРЯДОЧИТЬ ПО
		|	ИсходныйДокумент";
		
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СсылкаНаОбъект);
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	Результат = Новый Массив;
	
	Пока Выборка.Следующий() Цикл
		
		ОписаниеРеквизитов = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_ДатаНомерДокументаОснования");
		ЗаполнитьЗначенияСвойств(ОписаниеРеквизитов, Выборка, , "НомерИсправленияИсходногоДокумента, ДатаИсправленияИсходногоДокумента");
		
		Если ОписаниеРеквизитов.УчитыватьИсправлениеИсходногоДокумента Тогда
			ОписаниеРеквизитов.НомерИсправленияИсходногоДокумента	 = Формат(Выборка.НомерИсправленияИсходногоДокумента, "ЧГ=0");
			ОписаниеРеквизитов.ДатаИсправленияИсходногоДокумента	 = Выборка.ДатаИсправленияИсходногоДокумента;
		Иначе
			ОписаниеРеквизитов.НомерИсправленияИсходногоДокумента	 = "";
			ОписаниеРеквизитов.ДатаИсправленияИсходногоДокумента	 = "";
		КонецЕсли;
		
		Если ЗначениеЗаполнено(Выборка.ПредставлениеНомераИсходногоДокумента) Тогда
			ОписаниеРеквизитов.НомерИсходногоДокумента = Выборка.ПредставлениеНомераИсходногоДокумента;
		Иначе
			ОписаниеРеквизитов.НомерИсходногоДокумента = ПредставлениеНомера(ОписаниеРеквизитов.НомерДокумента);
		КонецЕсли;
		
		Результат.Добавить(ОписаниеРеквизитов);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция РазделительПакетаЗапросов()
	
	Возврат
	"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|";
	
КонецФункции

Функция ИспользуетсяРеализацияЧерезКомиссионера()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Результат = СравнитьВерсии(ВерсияУТ, "11.5.8") >= 0;
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяРегистрационныйНомерТД()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Результат = СравнитьВерсии(ВерсияУТ, "11.4.2") >= 0;
	
	Возврат Результат;
	
КонецФункции

// В редакции 11.1 в табличную часть "ВидыЗапасов" добавили реквизит "АналитикаУчетаНоменклатуры",
// а реквизиты "Номенклатура" и "Характеристика" помечены на удаление (удалены в редакции 11.2)
// 
// Возвращаемое значение:
//  Булево - Истина, если для передачи сведений о номенклатуре используется реквизит "АналитикаУчетаНоменклатуры".
//
Функция ИспользуетсяАналитикаУчетаНоменклатуры()
	
	ЕстьРеквизитНоменклатура = МетодСервера( , "СуществуетОбъектМетаданных", "Документ.РеализацияТоваровУслуг.ТабличнаяЧасть.ВидыЗапасов.Реквизит.Номенклатура");
	Результат = Не ЕстьРеквизитНоменклатура;
	
	Возврат Результат;
	
КонецФункции

#Область ЗагрузкаДанных

// Список возможных идентификаторов способов обработки входящих документов.
// 
// Возвращаемое значение:
//  Структура - см. тело функции.
//
Функция ИдентификаторыПравилОбработкиДокументов()
	
	ЗначенияПеречисления = Новый Массив;
	ЗначенияПеречисления.Добавить("ПриобретениеТоваровУслуг");
	ЗначенияПеречисления.Добавить("ПриобретениеУслугПрочихАктивов");
	ЗначенияПеречисления.Добавить("ВозвратТоваровОтКлиента");
	ЗначенияПеречисления.Добавить("КорректировкаПриобретения");
	ЗначенияПеречисления.Добавить("СчетФактураПолученный");
	ЗначенияПеречисления.Добавить("СчетФактураПолученныйАванс");
	ЗначенияПеречисления.Добавить("АктОРасхожденияхПослеОтгрузки");
	ЗначенияПеречисления.Добавить("СверкаВзаиморасчетов");
	ЗначенияПеречисления.Добавить("СверкаВзаиморасчетов2_5_11");
	ЗначенияПеречисления.Добавить("ЗаказПоставщику");
	ЗначенияПеречисления.Добавить("ЗаявкаНаРасходованиеДенежныхСредств");
	ЗначенияПеречисления.Добавить("СписаниеБезналичныхДенежныхСредств");
	ЗначенияПеречисления.Добавить("РасходныйКассовыйОрдер");
	
	Результат = Новый Структура;
	Для Каждого Элемент Из ЗначенияПеречисления Цикл
		Результат.Вставить(Элемент, Элемент);
	КонецЦикла;
	
	Возврат Новый ФиксированнаяСтруктура(Результат);
	
КонецФункции

// Заполняет правила обработки входящих товарных накладных и актов об оказании услуг.
//
Процедура ЗаполнитьПравилаОбработкиПервичныхДокументов(ПравилаОбработки)
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	ИдентификаторыПравил = ИдентификаторыПравилОбработкиДокументов();
	
	ДляПервичныхДокументов = Ложь;
	ДляИсправительныхДокументов = Истина;
	ДляПервичныхИИсправительныхДокументов = Неопределено;
	
	// Приобретение товаров, услуг
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.ПриобретениеТоваровУслуг
		, ВидыЭД.ТоварнаяНакладная
		, ДляПервичныхДокументов);
	
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.ПриобретениеТоваровУслуг
		, ВидыЭД.АктОбОказанииУслуг
		, ДляПервичныхДокументов);
	
	// Приобретение услуг и прочих активов
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.ПриобретениеУслугПрочихАктивов
		, ВидыЭД.ТоварнаяНакладная
		, ДляПервичныхДокументов);
	
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.ПриобретениеУслугПрочихАктивов
		, ВидыЭД.АктОбОказанииУслуг
		, ДляПервичныхДокументов);
	
	// Возврат товаров от клиента
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.ВозвратТоваровОтКлиента
		, ВидыЭД.ТоварнаяНакладная
		, ДляПервичныхДокументов);
	
	// Исправление накладной, акта
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.КорректировкаПриобретения
		, ВидыЭД.ТоварнаяНакладная
		, ДляИсправительныхДокументов);
	
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.КорректировкаПриобретения
		, ВидыЭД.АктОбОказанииУслуг
		, ДляИсправительныхДокументов);
	
	// Корректировка накладной, акта
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.КорректировкаПриобретения
		, ВидыЭД.СоглашениеОбИзмененииСтоимости
		, ДляПервичныхИИсправительныхДокументов);
	
КонецПроцедуры

// Заполняет правила обработки входящих счетов-фактур и корректировочных счетов-фактур.
//
Процедура ЗаполнитьПравилаОбработкиСчетовФактур(ПравилаОбработки)
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	ИдентификаторыПравил = ИдентификаторыПравилОбработкиДокументов();
	
	ДляПервичныхИИсправительныхДокументов = Неопределено;
	
	// Счет-фактура
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.СчетФактураПолученный
		, ВидыЭД.СчетФактура
		, ДляПервичныхИИсправительныхДокументов);
	
	// Счет-фактура (аванс)
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.СчетФактураПолученныйАванс
		, ВидыЭД.СчетФактура
		, ДляПервичныхИИсправительныхДокументов);
	
	// Корректировочный счет-фактура
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.СчетФактураПолученный
		, ВидыЭД.КорректировочныйСчетФактура
		, ДляПервичныхИИсправительныхДокументов);
	
КонецПроцедуры

// Заполняет правила обработки входящих Актов о расхождениях после приемки.
//
Процедура ЗаполнитьПравилаОбработкиДокументовОРасхождении(ПравилаОбработки)

	ВидыЭД = ВидыЭлектронныхДокументов();
	ИдентификаторыПравил = ИдентификаторыПравилОбработкиДокументов();
	
	ДляПервичныхИИсправительныхДокументов = Неопределено;
	
	// Акт о расхождениях после приемки
	ДобавитьПравилоОбработкиДокументов(ПравилаОбработки
		, ИдентификаторыПравил.АктОРасхожденияхПослеОтгрузки
		, ВидыЭД.ДокументОРасхождениях
		, ДляПервичныхИИсправительныхДокументов);

КонецПроцедуры

Процедура ЗаполнитьПравилаОбработкиАктовСверки(ПравилаОбработки)
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	ИдентификаторыПравил = ИдентификаторыПравилОбработкиДокументов();
	
	ДляПервичныхИИсправительныхДокументов = Неопределено;
	
	// Акт сверки расчетов
	ДобавитьПравилоОбработкиДокументов(
		ПравилаОбработки,
		ИдентификаторыПравил.СверкаВзаиморасчетов,
		ВидыЭД.АктСверки,
		ДляПервичныхИИсправительныхДокументов
	);
	
	Если ИспользуетсяСверкаВзаиморасчетов2_5_11() Тогда
		
		// Акт сверки расчетов (2.5.11)
		ДобавитьПравилоОбработкиДокументов(
			ПравилаОбработки,
			ИдентификаторыПравил.СверкаВзаиморасчетов2_5_11,
			ВидыЭД.АктСверки,
			ДляПервичныхИИсправительныхДокументов
		);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьПравилаОбработкиСчетовНаОплату(ПравилаОбработки)
	
	ВидыЭД = ВидыЭлектронныхДокументов();
	ИдентификаторыПравил = ИдентификаторыПравилОбработкиДокументов();
	
	ДляПервичныхИИсправительныхДокументов = Неопределено;
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьЗаказыПоставщикам") Тогда
		ДобавитьПравилоОбработкиДокументов(
			ПравилаОбработки,
			ИдентификаторыПравил.ЗаказПоставщику,
			ВидыЭД.СчетНаОплату,
			ДляПервичныхИИсправительныхДокументов);
	КонецЕсли;
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьЗаявкиНаРасходованиеДенежныхСредств") Тогда
		ДобавитьПравилоОбработкиДокументов(
			ПравилаОбработки,
			ИдентификаторыПравил.ЗаявкаНаРасходованиеДенежныхСредств,
			ВидыЭД.СчетНаОплату,
			ДляПервичныхИИсправительныхДокументов);
	КонецЕсли;
	
	ДобавитьПравилоОбработкиДокументов(
		ПравилаОбработки,
		ИдентификаторыПравил.СписаниеБезналичныхДенежныхСредств,
		ВидыЭД.СчетНаОплату,
		ДляПервичныхИИсправительныхДокументов);
	
	ДобавитьПравилоОбработкиДокументов(
		ПравилаОбработки,
		ИдентификаторыПравил.РасходныйКассовыйОрдер,
		ВидыЭД.СчетНаОплату,
		ДляПервичныхИИсправительныхДокументов);
	
КонецПроцедуры

Функция ПартнерПоКонтрагенту(Контрагент)
	
	Результат = Справочники.Партнеры.НеизвестныйПартнер;
	
	Если ЗначениеЗаполнено(Контрагент) Тогда 
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Контрагент, "Партнер");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоНоменклатураУслуга(Номенклатура)
	
	Результат = Ложь;
	
	Если ЗначениеЗаполнено(Номенклатура) Тогда 
		ТипНоменклатуры = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Номенклатура, "ТипНоменклатуры");
		Результат = (ТипНоменклатуры = ПредопределенноеЗначение("Перечисление.ТипыНоменклатуры.Услуга"));
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьНалогообложениеНДС(ДокументОбъект, ДанныеЗаполнения)
	
	Если ДанныеЗаполнения.Свойство("Сумма") И ДанныеЗаполнения.Сумма = 0 Тогда
		
		ДокументОбъект.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДСУПокупателя;
		
		Для Каждого Запись Из ДокументОбъект.Товары Цикл
			Запись.СуммаСНДС = Запись.Сумма;
		КонецЦикла;
		
	ИначеЕсли Не ЕстьСтрокиСНДС(ДокументОбъект.Товары) Тогда
		ДокументОбъект.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ПродажаНеОблагаетсяНДС;
	Иначе
		ДокументОбъект.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ПродажаОблагаетсяНДС;
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьНалогообложениеНДСПлатежныхДокументов(ДокументОбъект, НДСИсчисляетсяНалоговымАгентом)
	
	Если НДСИсчисляетсяНалоговымАгентом Тогда
		НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НалоговыйАгентПоНДС;
	Иначе
		
		Если ТипЗнч(ДокументОбъект) = Тип("ДокументОбъект.ЗаказПоставщику") Тогда
			ТабличнаяЧасть = ДокументОбъект.Товары;
		Иначе
			ТабличнаяЧасть = ДокументОбъект.РасшифровкаПлатежа;
		КонецЕсли;
		
		Если Не ЕстьСтрокиСНДС(ТабличнаяЧасть) Тогда
			НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ПродажаНеОблагаетсяНДС;
		Иначе
			НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ПродажаОблагаетсяНДС;
		КонецЕсли;
		
	КонецЕсли;
	
	ДокументОбъект.НалогообложениеНДС = НалогообложениеНДС;
	
	Если ТипЗнч(ДокументОбъект) = Тип("ДокументОбъект.ЗаказПоставщику") Тогда
		ДокументОбъект.ЗакупкаПодДеятельность = НалогообложениеНДС;
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьНалогообложениеНДСПУиПА(ДокументОбъект)
	
	Если Не ЕстьСтрокиСНДС(ДокументОбъект.Расходы) Тогда
		ДокументОбъект.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ПродажаНеОблагаетсяНДС;
	Иначе
		ДокументОбъект.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ПродажаОблагаетсяНДС;
	КонецЕсли;
	
КонецПроцедуры

Функция ЕстьСтрокиСНДС(ТабличнаяЧасть)

	Результат = Ложь;
	
	ТабСтавкиНДС = ТабличнаяЧасть.Выгрузить(,"СтавкаНДС");
	ТабСтавкиНДС.Свернуть("СтавкаНДС");
	
	СтавкаБезНДС = СтавкаНДСПоПредставлению("без НДС");
	
	Для Каждого СтрокаТЧ Из ТабСтавкиНДС Цикл 
		Если СтрокаТЧ.СтавкаНДС <> СтавкаБезНДС Тогда
			Результат = Истина;
			Прервать;
		КонецЕсли;	
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Заполняет вид деятельности раздельного учета НДС по переданным параметрам заполнения.
//
// Параметры:
//  ДокументОбъект - ДокументОбъект - Обрабатываемый документ;
//  ИмяРеквизитаДляЗаполнения - ИмяРеквизитаДляЗаполнения - имя заполняемого реквизита;
//
Процедура ЗаполнитьВидДеятельностиНДС(ДокументОбъект, ИмяРеквизитаДляЗаполнения = "ЗакупкаПодДеятельность")
	
	Если Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДокументОбъект, ИмяРеквизитаДляЗаполнения)
		Или ЗначениеЗаполнено(ДокументОбъект[ИмяРеквизитаДляЗаполнения]) Тогда
		Возврат;
	КонецЕсли;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	ТипНалогообложения = Неопределено;
	
	Если СравнитьВерсии(ВерсияУТ, "11.4.6") > 0 Тогда
		
		МенеджерОбъекта = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени(ДокументОбъект.Метаданные().ПолноеИмя());
		ПараметрыЗаполнения = МенеджерОбъекта.ПараметрыЗаполненияВидаДеятельностиНДС(ДокументОбъект);
		
		МодульУчетНДС = ОбщегоНазначения.ОбщийМодуль("УчетНДСУП");
		МодульУчетНДС.ЗаполнитьВидДеятельностиНДС(ТипНалогообложения, ПараметрыЗаполнения);
		
	Иначе
		
		ПараметрыЗаполнения = Новый Структура;
		ПараметрыЗаполнения.Вставить("Организация");
		ПараметрыЗаполнения.Вставить("Склад");
		ПараметрыЗаполнения.Вставить("Дата");
		ЗаполнитьЗначенияСвойств(ПараметрыЗаполнения, ДокументОбъект);
		
		ТипНалогообложения = Справочники.Организации.ЗакупкаПодДеятельность(ПараметрыЗаполнения.Организация
			, ПараметрыЗаполнения.Склад
			, ПараметрыЗаполнения.Дата);
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ТипНалогообложения) Тогда
		ДокументОбъект[ИмяРеквизитаДляЗаполнения] = ТипНалогообложения;
	КонецЕсли;
	
КонецПроцедуры

Функция СтруктураЗаполненияПриобретенияТоваровУслуг(ДанныеЭД, ДополнительныеПараметры)
	
	ПараметрыЗаполнения = ОбщиеПараметрыЗаполненияДокументовУчета();
	Если ЗначениеЗаполнено(ДополнительныеПараметры) Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	ДанныеДляЗаполнения = Новый Структура;
	
	Шапка = Новый Структура;
	
	Шапка.Вставить("ЦенаВключаетНДС", ПараметрыЗаполнения.ЦенаВключаетНДС);
	
	// Участники сделки
	Шапка.Вставить("Организация"		, СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент"			, СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("Грузоотправитель"	, СсылкаНаУчастникаСделки(ШапкаЭД.Грузоотправитель));
	Шапка.Вставить("Грузополучатель"	, СсылкаНаУчастникаСделки(ШапкаЭД.Грузополучатель));
	Шапка.Вставить("Партнер"			, ПартнерПоКонтрагенту(Шапка.Контрагент));
	
	// Валюта, сумма
	Шапка.Вставить("Валюта"		, ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("Курс"		, ШапкаЭД.Курс);
	Шапка.Вставить("Кратность"	, ШапкаЭД.Кратность);
	Шапка.Вставить("Сумма"		, ИтогиТЧ.СуммаСНДС);
	
	// Номера, даты
	Шапка.Вставить("НомерВходящегоДокумента", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаВходящегоДокумента"	, ШапкаЭД.Дата);
	
	// Табличная часть
	Товары = Документы.ПриобретениеТоваровУслуг.ПустаяСсылка().Товары.ВыгрузитьКолонки();
	Товары.Колонки.Добавить("НомерПоДаннымПоставщика");
	Товары.Колонки.Добавить("ДатаПоДаннымПоставщика");
	Товары.Колонки.Добавить("НомерПоДаннымКлиента");
	Товары.Колонки.Добавить("ДатаПоДаннымКлиента");
	
	// Маркировка
	ШтрихкодыУпаковок = НоваяТаблицаШтрихкодыУпаковок();
	
	ЗаполнятьСведенияПрослеживаемости = ЗаполнятьСведенияПрослеживаемости();
	
	Для Каждого Стр Из ДанныеЭД.ТоварнаяЧасть Цикл 

		НоменклатураИБ = Стр.Номенклатура;
		НоменклатураКонтрагента = Стр.НоменклатураКонтрагента;

		НоваяСтрока = Товары.Добавить();
		
		ЗаполнитьНоменклатуруПартнера(НоваяСтрока, НоменклатураКонтрагента);
		ЗаполнитьНоменклатуруИнформационнойБазы(НоваяСтрока, НоменклатураИБ);
		
		Если ЭтоНоменклатураУслуга(НоваяСтрока.Номенклатура) Тогда
			НоваяСтрока.СписатьНаРасходы = Истина;
		КонецЕсли;
		
		ПредставлениеСтавки = НоменклатураКонтрагента.СтавкаНДС;
		НоваяСтрока.СтавкаНДС = СтавкаНДСПоПредставлению(ПредставлениеСтавки, ШапкаЭД.Дата);
		
		НоваяСтрока.СуммаНДС	 = Стр.СуммаНДС;
		НоваяСтрока.Количество	 = Стр.Количество;
		НоваяСтрока.Цена		 = Стр.Цена;
		НоваяСтрока.Сумма		 = Стр.СуммаБезНДС;
		НоваяСтрока.СуммаСНДС	 = Стр.СуммаСНДС;
		
		Если НоваяСтрока.Количество = 0 Тогда
			НоваяСтрока.Количество = 1;
			Если НоваяСтрока.Цена = 0 Тогда
				НоваяСтрока.Цена = НоваяСтрока.Сумма;
			КонецЕсли;
		КонецЕсли;
		
		НоваяСтрока.КоличествоУпаковок = НоваяСтрока.Количество;
		Если ЗначениеЗаполнено(НоваяСтрока.Упаковка) Тогда
			ТекКоэффициент = Справочники.УпаковкиЕдиницыИзмерения.КоэффициентУпаковки(НоваяСтрока.Упаковка, НоваяСтрока.Номенклатура);
		Иначе
			ТекКоэффициент = 1;
		КонецЕсли;
		НоваяСтрока.Количество = НоваяСтрока.КоличествоУпаковок * ТекКоэффициент;
		
		ДополнитьТаблицуШтрихкодов(ШтрихкодыУпаковок, Стр.СведенияОМаркировке, НоменклатураКонтрагента.GTIN);
		
		ЕстьСведенияПрослеживаемости = ВТабличнойЧастиЭДЕстьСведенияПрослеживаемости(Стр);
						
		Если ЗаполнятьСведенияПрослеживаемости И ЕстьСведенияПрослеживаемости Тогда
			ПодготовитьСведенияОПрослеживаемости(Стр.СведенияОПрослеживаемости, Стр.ДанныеГТД, НоваяСтрока);
		Иначе
			НоваяСтрока.НомерГТД = НомерГТДПоДаннымЭлектронногоДокумента(Стр.ДанныеГТД);
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеДляЗаполнения.Вставить("Шапка"	, Шапка);
	ДанныеДляЗаполнения.Вставить("Товары"	, Товары);
	ДанныеДляЗаполнения.Вставить("ШтрихкодыУпаковок", ШтрихкодыУпаковок);
	
	Возврат ДанныеДляЗаполнения;
	
КонецФункции

Процедура ПодготовитьСведенияОПрослеживаемости(ДанныеПрослеживаемостиЭД, ДанныеГТД, НоваяСтрока)
	
	Если ДанныеПрослеживаемостиЭД = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	РеквизитСКоличеством = "КоличествоПоРНПТ";
	
	Если ДанныеПрослеживаемостиЭД.Количество() = 1 Тогда
		НоваяСтрока.КоличествоПоРНПТ = ДанныеПрослеживаемостиЭД[0][РеквизитСКоличеством];
	Иначе
		ВсегоПоРНПТ = 0;
		Для Каждого ЭлементПрослеживаемости Из ДанныеПрослеживаемостиЭД Цикл
			ВсегоПоРНПТ = ВсегоПоРНПТ + ЭлементПрослеживаемости[РеквизитСКоличеством];
		КонецЦикла;
		НоваяСтрока.КоличествоПоРНПТ = ВсегоПоРНПТ; 
	КонецЕсли;
		
	РегистрационныеНомера = РегистрационныеНомераДекларацииИлиРНПТ_820(ДанныеПрослеживаемостиЭД, ДанныеГТД); 
		
	Если РегистрационныеНомера.Количество() = 1 Тогда
		НоваяСтрока.НомерГТД = РегистрационныеНомера[0];
	Иначе
		// предполагается, что пользователь сам выберет нужный номер РНПТ
		НоваяСтрока.НомерГТД = Справочники.НомераГТД.ПустаяСсылка();
	КонецЕсли;
			
КонецПроцедуры

Функция РегистрационныеНомераДекларацииИлиРНПТ_820(СведенияПрослеживаемости, ДанныеГТД)
	
	Результат = Новый Массив;
	
	ВсеПартииИзОднойСтраны = (ДанныеГТД.Количество() = 1);
													
	Если ВсеПартииИзОднойСтраны Тогда
		
		// даже, если не нашли ссылку в ИБ
		СтранаПроисхождения = ДанныеГТД[0].СтранаПроисхождения.Ссылка;
		
	Иначе
		
		// чтобы не вносить путаницу будем создавать регистрационный номер с незаполненной страной
		СтранаПроисхождения = Справочники.СтраныМира.ПустаяСсылка();
		
	КонецЕсли;	
						
	Для Каждого ЭлементПрослеживаемости Из СведенияПрослеживаемости Цикл
		
		// ищем регистрационный номер в коллекции "СведПрослеж", т.к. согласно 820@ в коллекции "СвТД" вместо номера будет строка "Прослеж"
		// или создаем все элементы в справочнике НомераГТД
		РегистрационныйНомер = НайтиСоздатьНомерГТД(ЭлементПрослеживаемости.НомерГТДРегистрационныйНомер, СтранаПроисхождения, Истина);
		
		Результат.Добавить(РегистрационныйНомер);
						
	КонецЦикла;
					
	Возврат Результат;
	
КонецФункции

Процедура ПодготовитьСведенияОПрослеживаемостиУКД(Источник, НоваяСтрока)
	
	ДанныеПрослеживаемостиЭД = Источник.СведенияОПрослеживаемости;
	
	Если ДанныеПрослеживаемостиЭД = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	РеквизитСКоличеством = "КоличествоПослеИзменения";
	
	Если ДанныеПрослеживаемостиЭД.Количество() = 1 Тогда
		НоваяСтрока.КоличествоПоРНПТ = ДанныеПрослеживаемостиЭД[0][РеквизитСКоличеством];
	Иначе
		ВсегоПоРНПТ = 0;
		Для Каждого ЭлементПрослеживаемости Из ДанныеПрослеживаемостиЭД Цикл
			ВсегоПоРНПТ = ВсегоПоРНПТ + ЭлементПрослеживаемости[РеквизитСКоличеством];
		КонецЦикла;
		НоваяСтрока.КоличествоПоРНПТ = ВсегоПоРНПТ; 
	КонецЕсли;
		
	РегистрационныеНомера = РегистрационныеНомераДекларацииИлиРНПТ_736(ДанныеПрослеживаемостиЭД, Источник); 
		
	Если РегистрационныеНомера.Количество() = 1 Тогда
		НоваяСтрока.НомерГТД = РегистрационныеНомера[0];
	Иначе
		// предполагается, что пользователь сам выберет нужный номер РНПТ
		НоваяСтрока.НомерГТД = Справочники.НомераГТД.ПустаяСсылка();
	КонецЕсли;
			
КонецПроцедуры

Функция РегистрационныеНомераДекларацииИлиРНПТ_736(ДанныеПрослеживаемостиЭД, Источник)
	
	Результат = Новый Массив;
	
	СтранаПроисхождения = Неопределено;
	
	Если ЗначениеЗаполнено(Источник.ДанныеГТД) Тогда
		
		ВсеПартииИзОднойСтраны = (Источник.ДанныеГТД.Количество() = 1);
		Если ВсеПартииИзОднойСтраны Тогда
			// даже, если не нашли ссылку в ИБ
			СтранаПроисхождения = Источник.ДанныеГТД[0].СтранаПроисхождения.Ссылка;
		Иначе
			// чтобы не вносить путаницу будем создавать регистрационный номер с незаполненной страной
			СтранаПроисхождения = Справочники.СтраныМира.ПустаяСсылка();
		КонецЕсли;
		
	ИначеЕсли ЗначениеЗаполнено(Источник.ДопИнфо) Тогда
		
		СтранаПроисхождения = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НайтиСтрануПроисхожденияПоДаннымДопИнфо", Источник.ДопИнфо);
	
	КонецЕсли;
	
	Для Каждого ЭлементПрослеживаемости Из ДанныеПрослеживаемостиЭД Цикл
		
		// ищем регистрационный номер в коллекции "СведПрослеж", т.к. согласно 820@ в коллекции "СвТД" вместо номера будет строка "Прослеж"
		// или создаем все элементы в справочнике НомераГТД
		РегистрационныйНомер = НайтиСоздатьНомерГТД(ЭлементПрослеживаемости.НомерГТДРегистрационныйНомер, СтранаПроисхождения, Истина);
		
		Результат.Добавить(РегистрационныйНомер);
						
	КонецЦикла;
					
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьПриобретениеТоваровУслуг(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДанныеЗаполнения = ДанныеДляЗагрузки.Шапка;
	
	// Начальное заполнение
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	ДокументОбъект.Автор = Пользователи.АвторизованныйПользователь();
	ДокументОбъект.Заполнить(ДанныеЗаполнения);
	
	// вручную переопределим, если требуется
	ПерезаполнениеЗначенийРеквизитовШапки(ДокументОбъект, ДанныеЗаполнения);
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	// заполнение соглашения
	ЗаполнитьСоглашениеСПоставщиком(ДокументОбъект);
	
	// заполнение договора
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	// заполнение порядка расчетов из договора
	ЗаполнитьПорядокРасчетовИзДоговора(ДокументОбъект);
	
	ДокументОбъект.Товары.Загрузить(ДанныеДляЗагрузки.Товары);
	
	Если ДанныеДляЗагрузки.Свойство("ШтрихкодыУпаковок")
		И ЕстьШтрихкодыУпаковок(ДокументОбъект.Метаданные()) Тогда
		ДокументОбъект.ШтрихкодыУпаковок.Загрузить(ДанныеДляЗагрузки.ШтрихкодыУпаковок);
	КонецЕсли;
	
	Отказ = Ложь;
	
	ЗакупкиСервер.СвязатьНоменклатуруСНоменклатуройПоставщика(ДокументОбъект.Товары, Отказ);
	
	// склады в ТЧ
	СкладГруппа = Справочники.Склады.ЭтоГруппаИСкладыИспользуютсяВТЧДокументовЗакупки(ДокументОбъект.Склад);
	СкладыСервер.ЗаполнитьСкладыВТабличнойЧасти(ДокументОбъект.Склад, СкладГруппа, ДокументОбъект.Товары, Истина);
	
	// Тип налогообложения
	ЗаполнитьНалогообложениеНДС(ДокументОбъект, ДанныеЗаполнения);
	
	// заполнение реквизита ЗакупкаПодДеятельность
	ЗаполнитьВидДеятельностиНДС(ДокументОбъект, "ЗакупкаПодДеятельность");
	
	// пересчет цен и сумм в зависимости от включения НДС в цену
	ПересчитатьЦенуСуммуПриУстановкеФлагаЦенаВключаетНДС(ДокументОбъект, "Товары");
	
	// сумма документа
	ДокументОбъект.СуммаДокумента = ЦенообразованиеКлиентСервер.ПолучитьСуммуДокумента(
		ДокументОбъект.Товары,
		ДокументОбъект.ЦенаВключаетНДС
	);
	
	Если ЭтоРедакция11_5() Тогда
		ОбщегоНазначенияУТ.ЗаполнитьИдентификаторыДокумента(ДокументОбъект, "Товары");
	Иначе
		ВзаиморасчетыСервер.ЗаполнитьИдентификаторыСтрокВТабличнойЧасти(ДокументОбъект.Товары);
	КонецЕсли;
	
КонецПроцедуры

Функция СтруктураЗаполненияПриобретенияУслугПрочихАктивов(ДанныеЭД, ДополнительныеПараметры)
	
	ПараметрыЗаполнения = ОбщиеПараметрыЗаполненияДокументовУчета();
	Если ЗначениеЗаполнено(ДополнительныеПараметры) Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	ДанныеДляЗаполнения = Новый Структура;
	
	Шапка = Новый Структура;
	
	Шапка.Вставить("ЦенаВключаетНДС", ПараметрыЗаполнения.ЦенаВключаетНДС);
	
	// Участники сделки
	Шапка.Вставить("Организация", СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент"	, СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("Партнер"	, ПартнерПоКонтрагенту(Шапка.Контрагент));
	
	// Валюта, сумма
	Шапка.Вставить("Валюта"		, ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("Курс"		, ШапкаЭД.Курс);
	Шапка.Вставить("Кратность"	, ШапкаЭД.Кратность);
	Шапка.Вставить("Сумма"		, ИтогиТЧ.СуммаСНДС);
	
	// Номера, даты
	Шапка.Вставить("НомерВходящегоДокумента", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаВходящегоДокумента"	, ШапкаЭД.Дата);
	
	// Табличная часть
	ТабличнаяЧасть = Документы.ПриобретениеУслугПрочихАктивов.ПустаяСсылка().Расходы.ВыгрузитьКолонки();
	ТабличнаяЧасть.Колонки.Добавить("НомерПоДаннымПоставщика");
	ТабличнаяЧасть.Колонки.Добавить("ДатаПоДаннымПоставщика");
	ТабличнаяЧасть.Колонки.Добавить("НомерПоДаннымКлиента");
	ТабличнаяЧасть.Колонки.Добавить("ДатаПоДаннымКлиента");
	
	Для Каждого Стр Из ДанныеЭД.ТоварнаяЧасть Цикл 
		
		НоменклатураКонтрагента = Стр.НоменклатураКонтрагента;
		
		НоваяСтрока = ТабличнаяЧасть.Добавить();
		
		НоваяСтрока.Содержание = НоменклатураКонтрагента.Описание;
		Если Не ЗначениеЗаполнено(НоваяСтрока.Содержание) Тогда
			НоваяСтрока.Содержание = НоменклатураКонтрагента.Наименование;
		КонецЕсли;
		
		ПредставлениеСтавки = Стр.НоменклатураКонтрагента.СтавкаНДС;
		НоваяСтрока.СтавкаНДС = СтавкаНДСПоПредставлению(ПредставлениеСтавки, ШапкаЭД.Дата);
		
		НоваяСтрока.СуммаНДС	 = Стр.СуммаНДС;
		НоваяСтрока.Количество	 = Стр.Количество;
		НоваяСтрока.Цена		 = Стр.Цена;
		НоваяСтрока.Сумма		 = Стр.СуммаБезНДС;
		НоваяСтрока.СуммаСНДС	 = Стр.СуммаСНДС;
		
		Если Не ЗначениеЗаполнено(НоваяСтрока.Цена) Тогда
			
			Количество = НоваяСтрока.Количество;
			Если Не ЗначениеЗаполнено(Количество) Тогда
				Количество = 1;
			КонецЕсли;
			
			НоваяСтрока.Цена = НоваяСтрока.Сумма / Количество;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеДляЗаполнения.Вставить("Шапка"	, Шапка);
	ДанныеДляЗаполнения.Вставить("Расходы"	, ТабличнаяЧасть);
	
	Возврат ДанныеДляЗаполнения;
	
КонецФункции

Процедура ЗаполнитьПриобретениеУслугПрочихАктивов(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДанныеЗаполнения = ДанныеДляЗагрузки.Шапка;
	
	// Начальное заполнение
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	ДокументОбъект.Заполнить(ДанныеЗаполнения);
	
	// вручную переопределим, если требуется
	ПерезаполнениеЗначенийРеквизитовШапки(ДокументОбъект, ДанныеЗаполнения);
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	// заполнение соглашения
	ЗаполнитьСоглашениеСПоставщиком(ДокументОбъект);
	
	// заполнение договора
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	// заполнение порядка расчетов из договора
	ЗаполнитьПорядокРасчетовИзДоговора(ДокументОбъект);
	
	ДокументОбъект.Расходы.Загрузить(ДанныеДляЗагрузки.Расходы);
	
	Отказ = Ложь;
	
	// Тип налогообложения
	ЗаполнитьНалогообложениеНДСПУиПА(ДокументОбъект);
	
	// заполнение реквизита ЗакупкаПодДеятельность
	ЗаполнитьВидДеятельностиНДС(ДокументОбъект, "ЗакупкаПодДеятельность");
	
	// пересчет цен и сумм в зависимости от включения НДС в цену
	ПересчитатьЦенуСуммуПриУстановкеФлагаЦенаВключаетНДС(ДокументОбъект, "Расходы");
	
	// сумма документа
	ДокументОбъект.СуммаДокумента = ЦенообразованиеКлиентСервер.ПолучитьСуммуДокумента(ДокументОбъект.Расходы, ДокументОбъект.ЦенаВключаетНДС);
	
КонецПроцедуры

Функция СтруктураЗаполненияВозвратаТоваровОтКлиента(ДанныеЭД, ДополнительныеПараметры)
	
	ПараметрыЗаполнения = ОбщиеПараметрыЗаполненияДокументовУчета();
	Если ЗначениеЗаполнено(ДополнительныеПараметры) Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	ДанныеДляЗаполнения = Новый Структура;
	
	Шапка = Новый Структура;
	
	Шапка.Вставить("ЦенаВключаетНДС", ПараметрыЗаполнения.ЦенаВключаетНДС);
	
	// Участники сделки
	Шапка.Вставить("Организация"		, СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент"			, СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("Грузоотправитель"	, СсылкаНаУчастникаСделки(ШапкаЭД.Грузоотправитель));
	Шапка.Вставить("Грузополучатель"	, СсылкаНаУчастникаСделки(ШапкаЭД.Грузополучатель));
	Шапка.Вставить("Партнер"			, ПартнерПоКонтрагенту(Шапка.Контрагент));
	
	// Валюта, сумма
	Шапка.Вставить("Валюта"		, ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("Курс"		, ШапкаЭД.Курс);
	Шапка.Вставить("Кратность"	, ШапкаЭД.Кратность);
	Шапка.Вставить("Сумма"		, ИтогиТЧ.СуммаСНДС);
	
	// Номера, даты
	Шапка.Вставить("НомерВходящегоДокумента", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаВходящегоДокумента"	, ШапкаЭД.Дата);
	
	// Табличная часть
	Товары = Документы.ВозвратТоваровОтКлиента.ПустаяСсылка().Товары.ВыгрузитьКолонки();
	Товары.Колонки.Добавить("НомерПоДаннымПоставщика");
	Товары.Колонки.Добавить("ДатаПоДаннымПоставщика");
	Товары.Колонки.Добавить("НомерПоДаннымКлиента");
	Товары.Колонки.Добавить("ДатаПоДаннымКлиента");
	
	Если Товары.Колонки.Найти("НомерГТД") = Неопределено Тогда
		Товары.Колонки.Добавить("НомерГТД");
	КонецЕсли;
	
	// Маркировка
	ШтрихкодыУпаковок = НоваяТаблицаШтрихкодыУпаковок();
	
	ЗаполнятьСведенияПрослеживаемости = ЗаполнятьСведенияПрослеживаемости();
	
	Для Каждого Стр Из ДанныеЭД.ТоварнаяЧасть Цикл 
		
		НоваяСтрока = Товары.Добавить();
		
		ЗаполнитьНоменклатуруИнформационнойБазы(НоваяСтрока, Стр.Номенклатура);
		
		НоваяСтрока.НомерГТД = НомерГТДПоДаннымЭлектронногоДокумента(Стр.ДанныеГТД);
		
		ПредставлениеСтавки = Стр.НоменклатураКонтрагента.СтавкаНДС;
		НоваяСтрока.СтавкаНДС = СтавкаНДСПоПредставлению(ПредставлениеСтавки, ШапкаЭД.Дата);
		
		НоваяСтрока.СуммаНДС	 = Стр.СуммаНДС;
		НоваяСтрока.Количество	 = Стр.Количество;
		НоваяСтрока.Цена		 = Стр.Цена;
		НоваяСтрока.Сумма		 = Стр.СуммаБезНДС;
		НоваяСтрока.СуммаСНДС	 = Стр.СуммаСНДС;
		
		Если НоваяСтрока.Количество = 0 Тогда
			НоваяСтрока.Количество = 1;
			Если НоваяСтрока.Цена = 0 Тогда
				НоваяСтрока.Цена = НоваяСтрока.Сумма;
			КонецЕсли;
		КонецЕсли;
		
		НоваяСтрока.КоличествоУпаковок = НоваяСтрока.Количество;
		Если ЗначениеЗаполнено(НоваяСтрока.Упаковка) Тогда
			ТекКоэффициент = Справочники.УпаковкиЕдиницыИзмерения.КоэффициентУпаковки(НоваяСтрока.Упаковка, НоваяСтрока.Номенклатура);
		Иначе
			ТекКоэффициент = 1;
		КонецЕсли;
		НоваяСтрока.Количество = НоваяСтрока.КоличествоУпаковок * ТекКоэффициент;
		
		ДополнитьТаблицуШтрихкодов(ШтрихкодыУпаковок, Стр.СведенияОМаркировке);
		
		ЕстьСведенияПрослеживаемости = ВТабличнойЧастиЭДЕстьСведенияПрослеживаемости(Стр);
						
		Если ЗаполнятьСведенияПрослеживаемости И ЕстьСведенияПрослеживаемости Тогда
			ПодготовитьСведенияОПрослеживаемости(Стр.СведенияОПрослеживаемости, Стр.ДанныеГТД, НоваяСтрока);
		Иначе
			НоваяСтрока.НомерГТД = НомерГТДПоДаннымЭлектронногоДокумента(Стр.ДанныеГТД);
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеДляЗаполнения.Вставить("Шапка"	, Шапка);
	ДанныеДляЗаполнения.Вставить("Товары"	, Товары);
	ДанныеДляЗаполнения.Вставить("ШтрихкодыУпаковок", ШтрихкодыУпаковок);
	
	Возврат ДанныеДляЗаполнения;
	
КонецФункции

Процедура ЗаполнитьВозвратТоваровОтКлиента(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДанныеЗаполнения = ДанныеДляЗагрузки.Шапка;
	
	// Начальное заполнение
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	ДокументОбъект.Менеджер = Пользователи.АвторизованныйПользователь();
	ДокументОбъект.Заполнить(ДанныеЗаполнения);
	
	// Стандартная процедура заполнения по какой то причине
	// устанавливает пустую ссылку в реквизит
	Если НЕ ЗначениеЗаполнено(ДокументОбъект.ДокументРеализации) Тогда
		ДокументОбъект.ДокументРеализации = Неопределено;
	КонецЕсли;
	
	// вручную переопределим, если требуется
	ПерезаполнениеЗначенийРеквизитовШапки(ДокументОбъект, ДанныеЗаполнения);
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	// заполнение соглашения
	ЗаполнитьСоглашениеСКлиентом(ДокументОбъект);
	
	// заполнение договора
	ЗаполнитьДоговорСКлиентом(ДокументОбъект);
	
	// заполнение порядка расчетов из договора
	ЗаполнитьПорядокРасчетовИзДоговора(ДокументОбъект);
	
	ДокументОбъект.Товары.Загрузить(ДанныеДляЗагрузки.Товары);
	
	Если ДанныеДляЗагрузки.Свойство("ШтрихкодыУпаковок")
		И ЕстьШтрихкодыУпаковок(ДокументОбъект.Метаданные()) Тогда
		ДокументОбъект.ШтрихкодыУпаковок.Загрузить(ДанныеДляЗагрузки.ШтрихкодыУпаковок);
	КонецЕсли;
	
	// Тип налогообложения
	ЗаполнитьНалогообложениеНДС(ДокументОбъект, ДанныеЗаполнения);
	
	// пересчет цен и сумм в зависимости от включения НДС в цену
	ПересчитатьЦенуСуммуПриУстановкеФлагаЦенаВключаетНДС(ДокументОбъект, "Товары");
	
	// сумма документа
	ДокументОбъект.СуммаДокумента = ЦенообразованиеКлиентСервер.ПолучитьСуммуДокумента(
			ДокументОбъект.Товары,
			ДокументОбъект.ЦенаВключаетНДС);
	
КонецПроцедуры

Функция СтруктураЗаполненияСчетаФактуры(ДанныеЭД, ДополнительныеПараметры)
	
	ПараметрыЗаполнения = ПараметрыЗаполненияСчетаФактуры();
	Если ЗначениеЗаполнено(ДополнительныеПараметры) Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	ФорматЭД = ДанныеЭД.ЭлектронныйДокумент.Формат;
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	Шапка = Новый Структура;
	
	// Номера, даты
	Шапка.Вставить("Номер", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаСоставления", ШапкаЭД.Дата);
	Шапка.Вставить("Дата", ТекущаяДатаСеанса());
	
	Если ФорматЭД.ЭтоИсправление Тогда
		Шапка.Вставить("Исправление"		, Истина);
		Шапка.Вставить("НомерИсправления"	, СвойствоСтруктуры(ШапкаЭД, "НомерИсправления"));
		Шапка.Вставить("ДатаИсправления"	, СвойствоСтруктуры(ШапкаЭД, "ДатаИсправления"));
	КонецЕсли;
	
	ЭтоАванс = ДанныеЭД.ЭтоАванс;
	
	// Вид операции
	Шапка.Вставить("КодВидаОперации", ?(ЭтоАванс, "02", "01"));
	Шапка.Вставить("ПолученВЭлектронномВиде", Истина);
	
	// Участники сделки
	Шапка.Вставить("Организация", СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент"	, СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("ИННКонтрагента"	, ШапкаЭД.Продавец.ИНН);
	Шапка.Вставить("КППКонтрагента"	, ШапкаЭД.Продавец.КПП);
	
	// Документы-основания
	ДокументыОснования = ПараметрыЗаполнения.ДокументОснование;
	Если Не ЗначениеЗаполнено(ДокументыОснования) Тогда
		ИдентификаторЭД = ДанныеЭД.ЭлектронныйДокумент.Идентификатор;
		ДокументыОснования = СопоставленныеУчетныеДокументы(ИдентификаторЭД);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДокументыОснования) Тогда
		Шапка.Вставить("ДокументОснование", ДокументыОснования);
	КонецЕсли;
	
	// Валюта, сумма
	Шапка.Вставить("Валюта"		, ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("Сумма"		, ИтогиТЧ.СуммаСНДС);
	Шапка.Вставить("СуммаНДС"	, ИтогиТЧ.СуммаНДС);
	
	ДанныеДляЗаполнения = Новый Структура;
	ДанныеДляЗаполнения.Вставить("Шапка", Шапка);
	
	// Авансы
	Если ЭтоАванс Тогда
		
		Авансы = Документы.СчетФактураПолученныйАванс.ПустаяСсылка().Авансы.Выгрузить();
		
		Для Каждого Стр Из ДанныеЭД.ТоварнаяЧасть Цикл 
			
			ПредставлениеСтавки = Стр.НоменклатураКонтрагента.СтавкаНДС;
			
			НоваяСтрока = Авансы.Добавить();
			НоваяСтрока.Сумма = Стр.СуммаСНДС;
			НоваяСтрока.СуммаНДС = Стр.СуммаНДС;
			НоваяСтрока.СтавкаНДС = СтавкаНДСПоПредставлению(ПредставлениеСтавки, ШапкаЭД.Дата);
			
		КонецЦикла;
		
		Если Авансы.Количество() > 0 Тогда
			ДанныеДляЗаполнения.Вставить("Авансы", Авансы);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ДанныеДляЗаполнения;
	
КонецФункции

Функция СтруктураЗаполненияКорректировкиПоступления(ДанныеЭД, ДополнительныеПараметры)
	
	ФорматЭД = ДанныеЭД.ЭлектронныйДокумент.Формат;
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	ДанныеДляЗаполнения = Новый Структура;
	
	Шапка = Новый Структура;
	Шапка.Вставить("Организация"		, СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент"			, СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("Грузоотправитель"	, СсылкаНаУчастникаСделки(ШапкаЭД.Грузоотправитель));
	Шапка.Вставить("Грузополучатель"	, СсылкаНаУчастникаСделки(ШапкаЭД.Грузополучатель));
	Шапка.Вставить("Валюта"				, ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("ВалютаВзаиморасчетов", ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("Партнер"	, ПартнерПоКонтрагенту(Шапка.Контрагент));
	Шапка.Вставить("Курс"		, ШапкаЭД.Курс);
	Шапка.Вставить("Кратность"	, ШапкаЭД.Кратность);
	Шапка.Вставить("НомерВходящегоДокумента", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаВходящегоДокумента"	, ШапкаЭД.Дата);
	
	Если ФорматЭД.ЭтоКорректировка Тогда
		СуммаДокумента		= ИтогиТЧ.СуммаСНДСУвеличение + ИтогиТЧ.СуммаСНДСУменьшение;
		ВидКорректировки	= Перечисления.ХозяйственныеОперации.КорректировкаПоСогласованиюСторон;
		
		ДокументыОснования	= СвойствоСтруктуры(ШапкаЭД, "КорректируемыйДокумент.ПервичныйДокумент");
		Если Не ТипЗнч(ДокументыОснования) = Тип("Массив") Тогда 
			ДокументыОснования = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДокументыОснования);
		КонецЕсли;
	Иначе
		СуммаДокумента		= ИтогиТЧ.СуммаСНДС;
		ВидКорректировки	= Перечисления.ХозяйственныеОперации.ИсправлениеОшибок;
		
		ДокументОснование	= СвойствоСтруктуры(ДополнительныеПараметры, "ДокументОснование");
		ДокументыОснования	= ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДокументОснование);
	КонецЕсли;
	
	Шапка.Вставить("Сумма", СуммаДокумента);
	Шапка.Вставить("ВидКорректировки", ВидКорректировки);
	
	Для Каждого Основание Из ДокументыОснования Цикл
		
		Если ТипЗнч(Основание) = Тип("ДокументСсылка.КорректировкаПриобретения") Тогда
			РеквизитыОснования = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Основание, "ДокументОснование");
			ДокОснование = РеквизитыОснования.ДокументОснование;
		Иначе
			ДокОснование = Основание;
		КонецЕсли;
		
		Если ТипЗнч(ДокОснование) = Тип("ДокументСсылка.ПриобретениеТоваровУслуг") Тогда
			Шапка.Вставить("ДокументОснование"	, ДокОснование);
			Шапка.Вставить("НалогообложениеНДС"	, ДокОснование.НалогообложениеНДС);
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Товары = Документы.ПриобретениеТоваровУслуг.ПустаяСсылка().Товары.ВыгрузитьКолонки();
	Товары.Колонки.Добавить("НомерПоДаннымПоставщика");
	Товары.Колонки.Добавить("ДатаПоДаннымПоставщика");
	Товары.Колонки.Добавить("НомерПоДаннымКлиента");
	Товары.Колонки.Добавить("ДатаПоДаннымКлиента");
	
	// Маркировка
	ШтрихкодыУпаковок = НоваяТаблицаШтрихкодыУпаковок();
	
	ЗаполнятьСведенияПрослеживаемости = ЗаполнятьСведенияПрослеживаемости();
	
	ТоварыДокументаОснования = ТабличнаяЧастьТоварыДоКорректировки(ДокОснование);
	
	НомерСтроки = 1;
	
	Для Каждого Стр Из ДанныеЭД.ТоварнаяЧасть Цикл 
		
		НоменклатураИБ = Стр.Номенклатура;
		НоменклатураКонтрагента = Стр.НоменклатураКонтрагента;
		
		НоваяСтрока = Товары.Добавить();
		
		ЗаполнитьНоменклатуруПартнера(НоваяСтрока, НоменклатураКонтрагента);
		ЗаполнитьНоменклатуруИнформационнойБазы(НоваяСтрока, НоменклатураИБ);
		
		ПредставлениеСтавки = НоменклатураКонтрагента.СтавкаНДС;
		НоваяСтрока.СтавкаНДС = СтавкаНДСПоПредставлению(ПредставлениеСтавки, ШапкаЭД.Дата);
		
		НоваяСтрока.СуммаНДС	 = Стр.СуммаНДС;
		НоваяСтрока.Количество	 = Стр.Количество;
		НоваяСтрока.Цена		 = Стр.Цена;
		НоваяСтрока.Сумма		 = Стр.СуммаБезНДС;
		НоваяСтрока.СуммаСНДС	 = Стр.СуммаСНДС;
		
		СтрокаТовары_ПересчитатьКоличество(НоваяСтрока);
		
		Если ЭтоИсправлениеПоступленияТоваровУслуг(ФорматЭД) Тогда
			
			НоваяСтрока.НомерСтроки = НомерСтроки;
			
			НомерСтроки = НомерСтроки + 1;
			
		Иначе
			
			НоваяСтрока.НомерСтроки = УбратьСкорректированноеКоличествоИзИсходнойТаблицы(
				ТоварыДокументаОснования,
				Стр
			);
			
		КонецЕсли;
		
		Если НоваяСтрока.Количество = 0 Тогда
			Товары.Удалить(НоваяСтрока);
			Продолжить;
		КонецЕсли;
		
		ДополнитьТаблицуШтрихкодов(ШтрихкодыУпаковок, Стр.СведенияОМаркировке, НоменклатураКонтрагента.GTIN);
		
		Если ЗаполнятьСведенияПрослеживаемости 
			И ВТабличнойЧастиЭДЕстьСведенияПрослеживаемости(Стр) Тогда
			
			ПодготовитьСведенияОПрослеживаемостиУКД(Стр, НоваяСтрока);
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛок(НоваяСтрока, "НомерГТД") Тогда
			
			НоваяСтрока.НомерГТД = НомерГТДПоДаннымЭлектронногоДокумента(Стр.ДанныеГТД);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если НЕ ЭтоИсправлениеПоступленияТоваровУслуг(ФорматЭД) Тогда
		
		ПараметрыПересчетаСумм = ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокОснование);
		
		Для Каждого СтрокаТЧоснования Из ТоварыДокументаОснования Цикл
			
			ДобавляемаяСтрока = Товары.Добавить();
			ЗаполнитьЗначенияСвойств(ДобавляемаяСтрока, СтрокаТЧоснования);
			
			СтрокаТовары_ПересчитатьКоличество(ДобавляемаяСтрока);
			СтрокаТовары_ПересчитатьСуммы(ДобавляемаяСтрока, ПараметрыПересчетаСумм);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Товары.Сортировать("НомерСтроки, Номенклатура, Характеристика, Серия, Упаковка");
	
	ДанныеДляЗаполнения.Вставить("Шапка"	, Шапка);
	ДанныеДляЗаполнения.Вставить("Товары"	, Товары);
	ДанныеДляЗаполнения.Вставить("ШтрихкодыУпаковок", ШтрихкодыУпаковок);
	
	Возврат ДанныеДляЗаполнения;
	
КонецФункции

Функция ЭтоИсправлениеПоступленияТоваровУслуг(ФорматЭД)
	
	Результат = (ФорматЭД.ЭтоИсправление И НЕ ФорматЭД.ЭтоКорректировка);
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧастьТоварыДоКорректировки(ДокументПриобретения)
	
	ТабличнаяЧастьКорректировки = Документы.КорректировкаПриобретения.ПустаяСсылка().Товары;
	
	Если ЗначениеЗаполнено(ДокументПриобретения) Тогда
		
		Документы.КорректировкаПриобретения.ЗаполнитьТоварыПоИсходнымДанным(
			ДокументПриобретения,
			ТабличнаяЧастьКорректировки
		);
		
	КонецЕсли;
	
	Результат = ТабличнаяЧастьКорректировки.Выгрузить();
	
	Возврат Результат;
	
КонецФункции

// Убирает из исходной таблицы товаров количество, уже отраженное по данным электронного УКД
//
// Параметры:
//  ТоварыДокументаОснования - ТаблицаЗначений - содержимое ТЧ "Товары" до корректировки
//  СтрокаУКД - СтрокаТаблицыЗначений - строка электронного УКД
// 
// Возвращаемое значение:
//  Число - номер скорректированной строки.
//
Функция УбратьСкорректированноеКоличествоИзИсходнойТаблицы(ТоварыДокументаОснования, СтрокаУКД)
	
	Результат = 9999;
	
	НоменклатураИБ = СтрокаУКД.Номенклатура;
	БазовыеПараметрыОтбора = ПараметрыПоискаСтрокТабличнойЧастиПоНоменклатуре(НоменклатураИБ);
	
	ПараметрыОтбора = Новый Структура(БазовыеПараметрыОтбора);
	ПараметрыОтбора.Вставить("Количество", СтрокаУКД.Количество_До);
	ПараметрыОтбора.Вставить("СуммаСНДС", СтрокаУКД.СуммаСНДС_До);
	НайденныеСтрокиТоваров = ТоварыДокументаОснования.НайтиСтроки(ПараметрыОтбора);
	
	Если Не ЗначениеЗаполнено(НайденныеСтрокиТоваров) Тогда
		
		ПараметрыОтбора = Новый Структура(БазовыеПараметрыОтбора);
		ПараметрыОтбора.Вставить("Цена", СтрокаУКД.Цена_До);
		НайденныеСтрокиТоваров = ТоварыДокументаОснования.НайтиСтроки(ПараметрыОтбора);
		
	КонецЕсли;
	
	ТребуетсяРаспределить = СтрокаУКД.Количество_До;
	
	Для Каждого СтрокаТовары Из НайденныеСтрокиТоваров Цикл
		
		Если ТребуетсяРаспределить <= 0 Тогда
			Прервать;
		КонецЕсли;
		
		Распределено = Мин(ТребуетсяРаспределить, СтрокаТовары.Количество);
		ТребуетсяРаспределить = ТребуетсяРаспределить - Распределено;
		СтрокаТовары.Количество = СтрокаТовары.Количество - Распределено;
		
		Результат = СтрокаТовары.НомерСтроки;
		
		Если СтрокаТовары.Количество <= 0 Тогда
			ТоварыДокументаОснования.Удалить(СтрокаТовары);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПараметрыПоискаСтрокТабличнойЧастиПоНоменклатуре(НоменклатураИБ)
	
	Результат = Новый Структура;
	Результат.Вставить("Номенклатура", НоменклатураИБ.Номенклатура);
	
	Если ЗначениеЗаполнено(НоменклатураИБ.Характеристика) Тогда
		Результат.Вставить("Характеристика", НоменклатураИБ.Характеристика);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(НоменклатураИБ.Упаковка) Тогда
		ЕдиницаХраненияНоменклатуры = ЭлектронноеВзаимодействиеУТВызовСервера.ЕдиницаХраненияНоменклатуры(НоменклатураИБ.Номенклатура);
		Если НоменклатураИБ.Упаковка <> ЕдиницаХраненияНоменклатуры Тогда
			Результат.Вставить("Упаковка", НоменклатураИБ.Упаковка);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Новый ФиксированнаяСтруктура(Результат);
	
КонецФункции

Процедура СтрокаТовары_ПересчитатьКоличество(СтрокаТовары)
	
	СтрокаТовары.КоличествоУпаковок = СтрокаТовары.Количество;
	
	Если ЗначениеЗаполнено(СтрокаТовары.Упаковка) Тогда
		ТекКоэффициент = Справочники.УпаковкиЕдиницыИзмерения.КоэффициентУпаковки(СтрокаТовары.Упаковка, СтрокаТовары.Номенклатура);
	Иначе
		ТекКоэффициент = 1;
	КонецЕсли;
	
	СтрокаТовары.Количество = СтрокаТовары.КоличествоУпаковок * ТекКоэффициент;
	
КонецПроцедуры

Процедура СтрокаТовары_ПересчитатьСуммы(СтрокаТовары, ПараметрыПересчетаСумм)
	
	СтруктураДействий = Новый Структура;
	СтруктураДействий.Вставить("ПересчитатьСуммуНДС", ПараметрыПересчетаСумм);
	СтруктураДействий.Вставить("ПересчитатьСуммуСНДС", ПараметрыПересчетаСумм);
	СтруктураДействий.Вставить("ПересчитатьСумму", "Количество");
	
	КэшированныеЗначения = Неопределено;
	
	// НАЧАЛО Николаев Павел 30.06.2025 Выполнение задач №000054247
	//ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(
	ПакетнаяОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(
	// КОНЕЦ Николаев Павел 30.06.2025 Выполнение задач №000054247
		СтрокаТовары,
		СтруктураДействий,
		КэшированныеЗначения
	);
	
КонецПроцедуры

Процедура ЗаполнитьКорректировкуПоступления(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДанныеЗаполнения = ДанныеДляЗагрузки.Шапка;
	
	ДокументОснование = СвойствоСтруктуры(ДанныеЗаполнения, "ДокументОснование");
	
	Если ЗначениеЗаполнено(ДокументОснование) Тогда 
		ДокументОбъект.Заполнить(ДокументОснование);
	Иначе 
		ДокументОбъект.Заполнить(ДанныеЗаполнения);
	КонецЕсли;
	
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	
	ПерезаполнениеЗначенийРеквизитовШапки(ДокументОбъект, ДанныеЗаполнения);
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	ДокументОбъект.Товары.Загрузить(ДанныеДляЗагрузки.Товары);
	Для Каждого Строка Из ДокументОбъект.Товары Цикл 
		Если ДокументОбъект.ЦенаВключаетНДС И Строка.Сумма <> Строка.СуммаСНДС Тогда
			Строка.Сумма = Строка.СуммаСНДС;
			Если Строка.Количество <> 0 Тогда
				Строка.Цена = Окр(Строка.Сумма/Строка.Количество, 2);
			Иначе
				Строка.Цена = Строка.Сумма;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	//СтруктураПоискаСтроки = Новый Структура("Номенклатура, Характеристика, СтавкаНДС, Упаковка");
	//ТаблицаТоваровДокумента = ДокументОбъект.Товары;
	//
	//// Пересчет цен и сумм в зависимости от включения НДС в цену
	//СтруктураПересчетаСуммы = ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект);
	//
	//СтруктураДействий = Новый Структура;
	//СтруктураДействий.Вставить("ПересчитатьСуммуСНДС", СтруктураПересчетаСуммы);
	//
	//Если ДокументОбъект.ЦенаВключаетНДС Тогда
	//	СтруктураДействий.Вставить("ПересчитатьСумму");
	//	СтруктураДействий.Вставить("ПересчитатьСуммуНДС", СтруктураПересчетаСуммы);
	//	СтруктураДействий.Вставить("ПересчитатьЦенуСНДС");
	//КонецЕсли;
	//
	//Для Каждого Стр Из ДанныеДляЗагрузки.Товары Цикл 
	//	ЗаполнитьЗначенияСвойств(СтруктураПоискаСтроки, Стр);
	//	СтрокиКорректировки = ТаблицаТоваровДокумента.НайтиСтроки(СтруктураПоискаСтроки);
	//	Если СтрокиКорректировки.Количество() > 0 Тогда
	//		ЗаполняемаяСтрока = СтрокиКорректировки[0];
	//		Если Стр.Количество = 0 Тогда
	//			ТаблицаТоваровДокумента.Удалить(ЗаполняемаяСтрока);
	//			Продолжить;
	//		КонецЕсли;
	//	Иначе
	//		ЗаполняемаяСтрока = ТаблицаТоваровДокумента.Добавить();
	//	КонецЕсли; 
	//	
	//	ЗаполнитьЗначенияСвойств(ЗаполняемаяСтрока, Стр); 
	//	
	//	ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(ЗаполняемаяСтрока, СтруктураДействий, Неопределено);
	//	
	//КонецЦикла;
	
	Если ДанныеДляЗагрузки.Свойство("ШтрихкодыУпаковок")
		И ЕстьШтрихкодыУпаковок(ДокументОбъект.Метаданные()) Тогда
		ДокументОбъект.ШтрихкодыУпаковок.Загрузить(ДанныеДляЗагрузки.ШтрихкодыУпаковок);
	КонецЕсли;
	
	Отказ = Ложь;
	ЗакупкиСервер.СвязатьНоменклатуруСНоменклатуройПоставщика(ДокументОбъект.Товары, Отказ);
	
	// заполнение соглашения
	ЗаполнитьСоглашениеСПоставщиком(ДокументОбъект);
	
	// заполнение договора
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	// заполнение порядка расчетов из договора
	ЗаполнитьПорядокРасчетовИзДоговора(ДокументОбъект);
	
	// Склады в ТЧ
	Если ЗначениеЗаполнено(ДокументОснование) И Не ЗначениеЗаполнено(ДокументОбъект.Склад) Тогда
		РеквизитыОснования = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ДанныеЗаполнения.ДокументОснование, "Склад");
		Если ЗначениеЗаполнено(РеквизитыОснования.Склад) Тогда
			ДокументОбъект.Склад = ДанныеЗаполнения.ДокументОснование.Склад;
		КонецЕсли
	КонецЕсли;
	СкладГруппа = Справочники.Склады.ЭтоГруппаИСкладыИспользуютсяВТЧДокументовЗакупки(ДокументОбъект.Склад);
	СкладыСервер.ЗаполнитьСкладыВТабличнойЧасти(ДокументОбъект.Склад, СкладГруппа, ДокументОбъект.Товары, Истина);
	
	// Заполним расхождения
	Если ЗначениеЗаполнено(ДокументОбъект.ДокументОснование) Тогда
		ИспользуетсяДокументПоступлениеТоваров = Ложь;
		Если ЗначениеЗаполнено(ДокументОбъект.Договор) Тогда
			ВариантОформленияЗакупок = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументОбъект.Договор, "ВариантОформленияЗакупок");
			ИспользуетсяДокументПоступлениеТоваров = ВариантОформленияЗакупок <> Перечисления.ВариантыОформленияЗакупок.НеРазделять;
		КонецЕсли;
		
		Если ЭтоРедакция11_5() Тогда
			ДокументОбъект.ЗаполнитьРасхождения(ИспользуетсяДокументПоступлениеТоваров);
		Иначе
			Документы.КорректировкаПриобретения.ЗаполнитьРасхождения(ДокументОбъект, ИспользуетсяДокументПоступлениеТоваров);
		КонецЕсли;
	КонецЕсли;
	
	// Сумма документа
	ДокументОбъект.СуммаДокумента = ЦенообразованиеКлиентСервер.ПолучитьСуммуДокумента(ДокументОбъект.Товары, ДокументОбъект.ЦенаВключаетНДС);
	
КонецПроцедуры

Функция СтруктураЗаполненияКорректировочногоСчетаФактуры(ДанныеЭД, ДополнительныеПараметры)
	
	ПараметрыЗаполнения = ПараметрыЗаполненияСчетаФактуры();
	Если ЗначениеЗаполнено(ДополнительныеПараметры) Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	ФорматЭД = ДанныеЭД.ЭлектронныйДокумент.Формат;
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	Шапка = Новый Структура;
	Шапка.Вставить("Номер", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаСоставления", ШапкаЭД.Дата);
	Шапка.Вставить("Дата", ТекущаяДатаСеанса());
	Шапка.Вставить("Корректировочный", Истина);
	
	Шапка.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	
	Шапка.Вставить("Организация", СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент"	, СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("ИННКонтрагента"	, ШапкаЭД.Продавец.ИНН);
	Шапка.Вставить("КППКонтрагента"	, ШапкаЭД.Продавец.КПП);
	
	КодВидаОперации = "01";
	Шапка.Вставить("КодВидаОперации", КодВидаОперации);
	Шапка.Вставить("ПолученВЭлектронномВиде", Истина);
	
	Если ФорматЭД.ЭтоИсправление Тогда 
		Шапка.Вставить("Исправление"		, Истина);
		Шапка.Вставить("НомерИсправления"	, ШапкаЭД.НомерИсправления);
		Шапка.Вставить("ДатаИсправления"	, ШапкаЭД.ДатаИсправления);
	КонецЕсли;
	
	Шапка.Вставить("Сумма", ИтогиТЧ.СуммаСНДСУвеличение + ИтогиТЧ.СуммаСНДСУменьшение);
	
	// Документы-основания
	ДокументыОснования = ПараметрыЗаполнения.ДокументОснование;
	Если Не ЗначениеЗаполнено(ДокументыОснования) Тогда
		ИдентификаторЭД = ДанныеЭД.ЭлектронныйДокумент.Идентификатор;
		ДокументыОснования = СопоставленныеУчетныеДокументы(ИдентификаторЭД);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДокументыОснования) Тогда
		Шапка.Вставить("ДокументОснование", ДокументыОснования);
	КонецЕсли;
	
	СчетФактураОснование = СвойствоСтруктуры(ШапкаЭД, "КорректируемыйДокумент.СчетФактура");
	Если ТипЗнч(СчетФактураОснование) = Тип("Массив") Тогда 
		СчетФактураОснование = ?(ЗначениеЗаполнено(СчетФактураОснование), СчетФактураОснование[0], Неопределено);
	КонецЕсли;
	Шапка.Вставить("СчетФактураОснование", СчетФактураОснование);
	
	ДанныеДляЗаполнения = Новый Структура;
	ДанныеДляЗаполнения.Вставить("Шапка", Шапка);
	
	Возврат ДанныеДляЗаполнения;
	
КонецФункции

Процедура ЗаполнитьСчетФактуруПолученный(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДанныеШапки = ДанныеДляЗагрузки.Шапка;
	
	ДокументОбъект.Заполнить(ДанныеШапки);
	ЗаполнитьЗначенияСвойств(ДокументОбъект, ДанныеШапки);
	
КонецПроцедуры

Процедура ЗаполнитьСчетФактуруПолученныйНаАванс(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДанныеШапки = ДанныеДляЗагрузки.Шапка;
	
	ДокументОбъект.Заполнить(ДанныеШапки);
	ЗаполнитьЗначенияСвойств(ДокументОбъект, ДанныеШапки);
	
	ДокументОбъект.Авансы.Загрузить(ДанныеДляЗагрузки.Авансы);
	
КонецПроцедуры

Функция СтруктураЗаполненияАктаОРасхожденияхПослеОтгрузки(ДанныеЭД, ДополнительныеПараметры)

	ФорматЭД = ДанныеЭД.ЭлектронныйДокумент.Формат;
	ШапкаЭД = ДанныеЭД.Шапка;
	ИтогиТЧ = ДанныеЭД.Итоги;
	
	ДанныеДляЗаполнения = Новый Структура;
	
	Шапка = Новый Структура;
	Шапка.Вставить("Организация", СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель));
	Шапка.Вставить("Контрагент", СсылкаНаУчастникаСделки(ШапкаЭД.Продавец));
	Шапка.Вставить("Грузоотправитель", СсылкаНаУчастникаСделки(ШапкаЭД.Грузоотправитель));
	Шапка.Вставить("Грузополучатель", СсылкаНаУчастникаСделки(ШапкаЭД.Грузополучатель));
	Шапка.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("Партнер", ПартнерПоКонтрагенту(Шапка.Контрагент));
	Шапка.Вставить("НомерВходящегоДокумента", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаВходящегоДокумента", ШапкаЭД.Дата);
	Шапка.Вставить("ХозяйственнаяОперация", Перечисления.ХозяйственныеОперации.РеализацияКлиенту);
	
	НомерДокументаБезПрефикса = НомерДокументаБезПрефикса(ДанныеЭД.Шапка.Основание.Номер);
	
	НомераДокументов = Новый Массив;
	НомераДокументов.Добавить(ДанныеЭД.Шапка.Основание.Номер);
	НомераДокументов.Добавить(НомерДокументаБезПрефикса);
	
	Организация		= Шапка.Организация;
	ДатаДокумента	= НачалоДня(ДанныеЭД.Шапка.Основание.Дата);
	
	ДокументОснование = НайтиОснованиеДокументаОРасхождениях(Организация, ДатаДокумента, НомераДокументов);
	
	ДополнитьПараметрыЗаполненияАктаОРасхожденияхПоДокументуОснованию(ДокументОснование, Шапка);
	
	Товары = Документы.АктОРасхожденияхПослеОтгрузки.ПустаяСсылка().Товары.ВыгрузитьКолонки();
	Товары.Колонки.Добавить("НомерПоДаннымПоставщика");
	Товары.Колонки.Добавить("ДатаПоДаннымПоставщика");
	Товары.Колонки.Добавить("НомерПоДаннымКлиента");
	Товары.Колонки.Добавить("ДатаПоДаннымКлиента");
	
	// Маркировка
	ШтрихкодыУпаковок = НоваяТаблицаШтрихкодыУпаковок();
	
	Для Каждого Стр Из ДанныеЭД.ТоварнаяЧасть Цикл 
		
		НоваяСтрока = Товары.Добавить();
		
		ЗаполнитьНоменклатуруИнформационнойБазы(НоваяСтрока, Стр.Номенклатура);
		
		ПредставлениеСтавки = Стр.НоменклатураКонтрагента.СтавкаНДС;
		НоваяСтрока.СтавкаНДС = СтавкаНДСПоПредставлению(ПредставлениеСтавки, ШапкаЭД.Дата);
		
		НоваяСтрока.СуммаНДС	 = Стр.СуммаНДС;
		НоваяСтрока.Количество	 = Стр.Количество;
		НоваяСтрока.Цена		 = Стр.Цена;
		НоваяСтрока.Сумма		 = Стр.СуммаБезНДС;
		НоваяСтрока.СуммаСНДС	 = Стр.СуммаСНДС;
		
		НоваяСтрока.КоличествоУпаковок = НоваяСтрока.Количество;
		
		Если ЗначениеЗаполнено(НоваяСтрока.Упаковка) Тогда
			ТекКоэффициент = Справочники.УпаковкиЕдиницыИзмерения.КоэффициентУпаковки(НоваяСтрока.Упаковка, НоваяСтрока.Номенклатура);
		Иначе
			ТекКоэффициент = 1;
		КонецЕсли;
		
		НоваяСтрока.Количество = НоваяСтрока.КоличествоУпаковок * ТекКоэффициент;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛок(НоваяСтрока, "НомерГТД") Тогда
			НоваяСтрока.НомерГТД = НомерГТДПоДаннымЭлектронногоДокумента(Стр.ДанныеГТД);
		КонецЕсли;
		
		ДополнитьТаблицуШтрихкодов(ШтрихкодыУпаковок, Стр.СведенияОМаркировке);
		
	КонецЦикла;
	
	ДанныеДляЗаполнения.Вставить("Шапка", Шапка);
	ДанныеДляЗаполнения.Вставить("Товары", Товары);
	ДанныеДляЗаполнения.Вставить("ШтрихкодыУпаковок", ШтрихкодыУпаковок);
	
	Возврат ДанныеДляЗаполнения;

КонецФункции

Процедура ДополнитьПараметрыЗаполненияАктаОРасхожденияхПоДокументуОснованию(ДокументОснование, Шапка)

	Если НЕ ЗначениеЗаполнено(ДокументОснование)
		ИЛИ ТипЗнч(ДокументОснование) <> Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
		Возврат;
	КонецЕсли;
	
	НаборРеквизитов = "НалогообложениеНДС, ЦенаВключаетНДС, Валюта";
	РеквизитыДокументаОснования = РазрешенныеЗначенияРеквизитовОбъекта(ДокументОснование, НаборРеквизитов);
	
	Шапка.Вставить("ДокументОснование",		ДокументОснование);
	Шапка.Вставить("НалогообложениеНДС",	РеквизитыДокументаОснования.НалогообложениеНДС);
	Шапка.Вставить("ЦенаВключаетНДС",		РеквизитыДокументаОснования.ЦенаВключаетНДС);
	Шапка.Вставить("Валюта",				РеквизитыДокументаОснования.Валюта);

КонецПроцедуры

// Возвращает структуру, содержащую значения реквизитов, прочитанные из информационной базы по ссылке на объект.
// Рекомендуется использовать вместо обращения к реквизитам объекта через точку от ссылки на объект
// для быстрого чтения отдельных реквизитов объекта из базы данных.
//
// Если необходимо зачитать реквизит независимо от прав текущего пользователя,
// то следует использовать предварительный переход в привилегированный режим.
//
// Параметры:
//  Ссылка    - ЛюбаяСсылка - объект, значения реквизитов которого необходимо получить.
//            - Строка      - полное имя предопределенного элемента, значения реквизитов которого необходимо получить.
//  Реквизиты - Строка - имена реквизитов, перечисленные через запятую, в формате
//                       требований к свойствам структуры.
//                       Например, "Код, Наименование, Родитель".
//            - Структура
//            - ФиксированнаяСтруктура - в качестве ключа передается
//                       псевдоним поля для возвращаемой структуры с результатом, а в качестве
//                       значения (опционально) фактическое имя поля в таблице.
//                       Если ключ задан, а значение не определено, то имя поля берется из ключа.
//                       Допускается указание имени поля через точку, но при этом параметр КодЯзыка для такого поля
//                       учитываться не будет.
//            - Массив
//            - ФиксированныйМассив - имена реквизитов в формате требований к свойствам структуры.
//
// Возвращаемое значение:
//  Структура - содержит имена (ключи) и значения затребованных реквизитов.
//              Если в параметр Реквизиты передана пустая строка, то возвращается пустая структура.
//              Если в параметр Ссылка передана пустая ссылка, то возвращается структура, 
//              соответствующая именам реквизитов со значениями Неопределено.
//              Если в параметр Ссылка передана ссылка несуществующего объекта (битая ссылка), 
//              то все реквизиты вернутся со значением Неопределено.
//
Функция РазрешенныеЗначенияРеквизитовОбъекта(Ссылка, Реквизиты)
	
	ВыбратьРазрешенныеЗначения	= Истина;
	Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, Реквизиты, ВыбратьРазрешенныеЗначения);
	
	Возврат Результат;

КонецФункции

// Возвращает значения реквизита, прочитанного из информационной базы по ссылке на объект.
// Рекомендуется использовать вместо обращения к реквизитам объекта через точку от ссылки на объект
// для быстрого чтения отдельных реквизитов объекта из базы данных.
//
// Если необходимо зачитать реквизит независимо от прав текущего пользователя,
// то следует использовать предварительный переход в привилегированный режим.
//
// Параметры:
//  Ссылка    - ЛюбаяСсылка - объект, значения реквизитов которого необходимо получить.
//            - Строка      - полное имя предопределенного элемента, значения реквизитов которого необходимо получить.
//  ИмяРеквизита - Строка - имя получаемого реквизита.
//                          Допускается указание имени реквизита через точку, но при этом параметр КодЯзыка для
//                          такого реквизита учитываться не будет.
//
// Возвращаемое значение:
//  Произвольный - если в параметр Ссылка передана пустая ссылка, то возвращается Неопределено.
//                 Если в параметр Ссылка передана ссылка несуществующего объекта (битая ссылка), то возвращается Неопределено.
//
Функция РазрешенноеЗначениеРеквизитаОбъекта(Ссылка, Реквизиты)

	ВыбратьРазрешенныеЗначения	= Истина;
	Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Ссылка, Реквизиты, ВыбратьРазрешенныеЗначения);
	
	Возврат Результат;

КонецФункции

Функция ЗначенияРеквизитовОбъектаЛокальная(Ссылка, Реквизиты)
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЗначенияРеквизитовОбъектаЛокальная", Ссылка, Реквизиты);
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьАктОРасхожденияхПослеОтгрузки(ДокументОбъект, ДанныеДокумента)

	ДанныеШапки = ДанныеДокумента.Шапка;
	
	ДокументОснование = Неопределено;
	
	Если ДанныеШапки.Свойство("ДокументОснование") Тогда
		
		ДокументОснование = ДанныеШапки.ДокументОснование;
		
		ДокументОбъект.Заполнить(ДокументОснование);
		
	Иначе
		
		ДокументОбъект.Заполнить(ДанныеШапки);
		
	КонецЕсли;
	
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	
	ПерезаполнениеЗначенийРеквизитовШапки(ДокументОбъект, ДанныеШапки);
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	ЗаполнитьТабличнуюЧасть_ДокументОРасхождениях(ДанныеДокумента, ДокументОбъект, ДокументОснование);
	
	Если ДанныеДокумента.Свойство("ШтрихкодыУпаковок")
		И ЕстьШтрихкодыУпаковок(ДокументОбъект.Метаданные()) Тогда
		ДокументОбъект.ШтрихкодыУпаковок.Загрузить(ДанныеДокумента.ШтрихкодыУпаковок);
	КонецЕсли;
	
	// заполнение соглашения
	ЗаполнитьСоглашениеСКлиентом(ДокументОбъект);
	
	// заполнение договора
	ЗаполнитьДоговорСКлиентом(ДокументОбъект);
	
	// Склады в ТЧ
	ЗаполнитьСклады_ДокументОРасхождениях(ДокументОбъект, ДокументОснование);
	
	// Пересчет цен и сумм в зависимости от включения НДС в цену
	ОбработатьСтрокитабличнойЧасти_ДокументОРасхождениях(ДокументОбъект);

	
КонецПроцедуры

Процедура ЗаполнитьТабличнуюЧасть_ДокументОРасхождениях(ДанныеДокумента, ДокументОбъект, ДокументОснование)
	
	СтруктураПоискаСтроки = Новый Структура("Номенклатура, Характеристика, СтавкаНДС, Упаковка");
	ТаблицаТоваровДокумента = ДокументОбъект.Товары;
	
	Для Каждого Стр Из ДанныеДокумента.Товары Цикл 
		
		ЗаполнитьЗначенияСвойств(СтруктураПоискаСтроки, Стр);
		СтрокиКорректировки = ТаблицаТоваровДокумента.НайтиСтроки(СтруктураПоискаСтроки);
		
		Если СтрокиКорректировки.Количество() > 0 Тогда
			
			ЗаполняемаяСтрока = СтрокиКорректировки[0];
			
			Если Стр.Количество = 0 Тогда
				ТаблицаТоваровДокумента.Удалить(ЗаполняемаяСтрока);	
				Продолжить;
			КонецЕсли;
			
		Иначе
			
			ЗаполняемаяСтрока = ТаблицаТоваровДокумента.Добавить();
			
		КонецЕсли; 
		
		ЗаполнитьЗначенияСвойств(ЗаполняемаяСтрока, Стр, , "КоличествоПоДокументу, КоличествоУпаковокПоДокументу, СуммаПоДокументу, СуммаНДСПоДокументу, СуммаСНДСПоДокументу");
		
		Если Не ЗначениеЗаполнено(ЗаполняемаяСтрока.Реализация) Тогда
			ЗаполняемаяСтрока.Реализация = ДокументОснование;
		КонецЕсли;
		
	КонецЦикла;

КонецПроцедуры

Процедура ОбработатьСтрокитабличнойЧасти_ДокументОРасхождениях(ДокументОбъект)
	
	НастройкаПересчетаСуммы = ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект);
	
	ВыполняемыеДействия = Новый Структура;
	ВыполняемыеДействия.Вставить("ПересчитатьСуммуСНДС", НастройкаПересчетаСуммы);
	ВыполняемыеДействия.Вставить("ПересчитатьСумму");
	ВыполняемыеДействия.Вставить("ПересчитатьСуммуНДС", НастройкаПересчетаСуммы);
	
	Для Каждого Строка Из ДокументОбъект.Товары Цикл  
		// НАЧАЛО Николаев Павел 30.06.2025 Выполнение задач №000054247
		//ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Строка, ВыполняемыеДействия, Неопределено);
		ПакетнаяОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Строка, ВыполняемыеДействия, Неопределено);
		// КОНЕЦ Николаев Павел 30.06.2025 Выполнение задач №000054247
	КонецЦикла;

КонецПроцедуры

Процедура ЗаполнитьСклады_ДокументОРасхождениях(ДокументОбъект, ДокументОснование)

	Если НЕ ЗначениеЗаполнено(ДокументОснование) Тогда
		Возврат;
	КонецЕсли;
	
	СкладДокументаОснования = РазрешенноеЗначениеРеквизитаОбъекта(ДокументОснование, "Склад");
	СкладГруппаВШапке = Истина;
	
	СкладГруппа = Справочники.Склады.ЭтоГруппаИСкладыИспользуютсяВТЧДокументовПродажи(СкладДокументаОснования);
	СкладыСервер.ЗаполнитьСкладыВТабличнойЧасти(ДокументОснование.Склад, СкладГруппа, ДокументОбъект.Товары, СкладГруппаВШапке);

КонецПроцедуры

Функция НайтиОснованиеДокументаОРасхождениях(Организация, ДатаДокумента, НомерДокумента)

	Результат = Неопределено;
	
	Запрос = Новый Запрос();
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ДанныеПервичныхДокументов.Документ КАК СФ,
	|	ДанныеПервичныхДокументов.Документ.ДокументОснование КАК Документ
	|ИЗ
	|	РегистрСведений.ДанныеПервичныхДокументов КАК ДанныеПервичныхДокументов
	|ГДЕ
	|	ДанныеПервичныхДокументов.Организация = &Организация
	|	И ДанныеПервичныхДокументов.Номер В(&НомерДокумента)
	|	И НАЧАЛОПЕРИОДА(ДанныеПервичныхДокументов.Дата, ДЕНЬ) = &ДатаДокумента
	|	И ТИПЗНАЧЕНИЯ(ДанныеПервичныхДокументов.Документ) = ТИП(Документ.СчетФактураВыданный)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ ПЕРВЫЕ 1
	|	ДанныеПервичныхДокументов.Документ,
	|	ТЧСФ.ДокументОснование
	|ИЗ
	|	РегистрСведений.ДанныеПервичныхДокументов КАК ДанныеПервичныхДокументов
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.СчетФактураПолученный.ДокументыОснования КАК ТЧСФ
	|		ПО (ТЧСФ.Ссылка = ДанныеПервичныхДокументов.Документ)
	|ГДЕ
	|	ДанныеПервичныхДокументов.Организация = &Организация
	|	И ДанныеПервичныхДокументов.Номер В(&НомерДокумента)
	|	И НАЧАЛОПЕРИОДА(ДанныеПервичныхДокументов.Дата, ДЕНЬ) = &ДатаДокумента
	|	И ТИПЗНАЧЕНИЯ(ДанныеПервичныхДокументов.Документ) = ТИП(Документ.СчетФактураПолученный)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ ПЕРВЫЕ 1
	|	НЕОПРЕДЕЛЕНО,
	|	ДанныеПервичныхДокументов.Документ
	|ИЗ
	|	РегистрСведений.ДанныеПервичныхДокументов КАК ДанныеПервичныхДокументов
	|ГДЕ
	|	ДанныеПервичныхДокументов.Организация = &Организация
	|	И ДанныеПервичныхДокументов.Номер В(&НомерДокумента)
	|	И НАЧАЛОПЕРИОДА(ДанныеПервичныхДокументов.Дата, ДЕНЬ) = &ДатаДокумента
	|	И ТИПЗНАЧЕНИЯ(ДанныеПервичныхДокументов.Документ) В (ТИП(Документ.РеализацияТоваровУслуг), ТИП(Документ.ВозвратТоваровПоставщику))";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("ДатаДокумента", ДатаДокумента);
	Запрос.УстановитьПараметр("НомерДокумента", НомерДокумента);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Документ;
	КонецЕсли;
	
	Если ТипЗнч(Результат) = Тип("ДокументСсылка.СчетФактураВыданный") Тогда
		Результат = РазрешенноеЗначениеРеквизитаОбъекта(Результат, "ДокументОснование");
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция СтруктураЗаполненияСверкиВзаиморасчетов2_5_11(ДанныеЭД, ДополнительныеПараметры)
	
	ШапкаЭД = ДанныеЭД.Шапка;
	ТабличнаяЧастьЭД = ДанныеЭД.ТоварнаяЧасть;
	ИтогиЭД = ДанныеЭД.Итоги;
	
	ДанныеОбъекта = НовыйДанныеДляЗаполненияАктаСверкиВзаиморасчетов2_5_11();
	
	// Заполнение шапки документа
	ДанныеОбъекта.ДанныеШапки = ДанныеШапкиАктаСверкиВзаиморасчетовПоДаннымОтправителя(ШапкаЭД);
	
	// Заполнение сведений табличных частей
	ЗаполнитьТаблицыДанныхДляЗаполненияАктаСверкиВзаиморасчетов(
		ДанныеОбъекта,
		ТабличнаяЧастьЭД
	);
	
	Возврат ДанныеОбъекта;
	
КонецФункции

Функция НовыйДанныеДляЗаполненияАктаСверкиВзаиморасчетов2_5_11()
	
	МенеджерСверкаВзаиморасчетов2_5_11 = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Документ.СверкаВзаиморасчетов2_5_11");
	
	ИтоговыеЗаписи = МенеджерСверкаВзаиморасчетов2_5_11.ПустаяСсылка().ИтоговыеЗаписи.ВыгрузитьКолонки();
	ДетальныеЗаписи = МенеджерСверкаВзаиморасчетов2_5_11.ПустаяСсылка().ДетальныеЗаписи.ВыгрузитьКолонки();
	
	ДобавитьКолонкуИдентификатораВТаблицуАктаСверкиВзаиморасчетов(ИтоговыеЗаписи);
	ДобавитьКолонкуИдентификатораВТаблицуАктаСверкиВзаиморасчетов(ДетальныеЗаписи);
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеШапки", Новый Структура);
	Результат.Вставить("ИтоговыеЗаписи", ИтоговыеЗаписи);
	Результат.Вставить("ДетальныеЗаписи", ДетальныеЗаписи);
	
	Возврат Результат;
	
КонецФункции

// Добавляет в таблицу колонку, которая хранит данные об идентификаторе ключевого свойства строки.
//
// Параметры:
//	ТаблицаАкта - ТаблицаЗначений - таблица документа.
//
Процедура ДобавитьКолонкуИдентификатораВТаблицуАктаСверкиВзаиморасчетов(ТаблицаАкта)
	
	ОписаниеТипаИдентификатора = Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(100));
	
	ТаблицаАкта.Колонки.Добавить("ИдентификаторОбъекта", ОписаниеТипаИдентификатора);
	
КонецПроцедуры

Функция ДанныеШапкиАктаСверкиВзаиморасчетовПоДаннымОтправителя(ШапкаЭД)
	
	Результат = Новый Структура;
	
	// Стороны сверки
	Результат.Вставить("Организация", СсылкаНаУчастникаСделки(ШапкаЭД.Получатель));
	Результат.Вставить("Контрагент", СсылкаНаУчастникаСделки(ШапкаЭД.Отправитель));
	Результат.Вставить("Партнер", ПартнерПоКонтрагенту(Результат.Контрагент));
	
	// Валюта
	Результат.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	
	// Даты, номера, суммы
	Результат.Вставить("НомерКонтрагента", ШапкаЭД.Номер);
	Результат.Вставить("НачалоПериода", ШапкаЭД.ДатаНачала);
	Результат.Вставить("КонецПериода", ШапкаЭД.ДатаОкончания);
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьТаблицыДанныхДляЗаполненияАктаСверкиВзаиморасчетов(ДанныеОбъекта, ТабличнаяЧастьЭД)
	
	ИтоговыеЗаписи = ДанныеОбъекта.ИтоговыеЗаписи;
	ДетальныеЗаписи = ДанныеОбъекта.ДетальныеЗаписи;
	
	Для Каждого СтрокаДоговора Из ТабличнаяЧастьЭД.Договор Цикл
		
		СтрокаИтогов = ИтоговыеЗаписи.Добавить();
		
		ЗаполнитьСтрокуТаблицыИтоговыеЗаписиАктаСверкиВзаиморасчетов(СтрокаИтогов, СтрокаДоговора);
		
		Для Каждого СтрокаДокумента Из СтрокаДоговора.Документ Цикл
			
			Для Каждого СтрокаОперации Из СтрокаДокумента.Операция Цикл
				
				Если СтрокаОперации.СуммаДебет = 0
					И СтрокаОперации.СуммаКредит = 0 Тогда
					
					Продолжить;
					
				КонецЕсли;
				
				СтрокаЗаписи = ДетальныеЗаписи.Добавить();
				
				ЗаполнитьСтрокуТаблицыДетальныеЗаписиАктаСверкиВзаиморасчетов(
					СтрокаЗаписи,
					СтрокаДокумента,
					СтрокаОперации
				);
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

// Заполняет строку таблицы Итогов документа СверкаВзаиморасчетов2_5_11
//
// Параметры:
//  СтрокаРасчета - СтрокаТаблицыЗначений из ТЧ ИтоговыеЗаписи
//  ДанныеДоговора - Структура - коллекция, содержащая сведения о договоре взаиморасчетов по данным контрагента
//
Процедура ЗаполнитьСтрокуТаблицыИтоговыеЗаписиАктаСверкиВзаиморасчетов(СтрокаРасчета, ДанныеДоговора)
	
	ЗаполнитьЗначенияСвойств(СтрокаРасчета, ДанныеДоговора);
	
	НомерДоговора = СокрЛП(ДанныеДоговора.Номер);
	
	СтрокаРасчета.ИдентификаторОбъекта = ДанныеДоговора.ИдДоговора;
	СтрокаРасчета.НомерДоговора = НомерДоговора;
	СтрокаРасчета.НомерДоговораКонтрагент = НомерДоговора;
	СтрокаРасчета.ДатаДоговора = ДанныеДоговора.Дата;
	СтрокаРасчета.ДатаДоговораКонтрагент = ДанныеДоговора.Дата;
	СтрокаРасчета.НаименованиеДоговора = ДанныеДоговора.Описание;
	СтрокаРасчета.НаименованиеДоговораКонтрагент = ДанныеДоговора.Описание;
	СтрокаРасчета.НачальноеСальдоДтКонтрагент = ДанныеДоговора.СальдоНачКредит;
	СтрокаРасчета.НачальноеСальдоКтКонтрагент = ДанныеДоговора.СальдоНачДебет;
	СтрокаРасчета.ОборотДтКонтрагент = ДанныеДоговора.ОборотКредит;
	СтрокаРасчета.ОборотКтКонтрагент = ДанныеДоговора.ОборотДебет;
	СтрокаРасчета.КонечноеСальдоДтКонтрагент = ДанныеДоговора.СальдоКонКредит;
	СтрокаРасчета.КонечноеСальдоКтКонтрагент = ДанныеДоговора.СальдоКонДебет;
	
КонецПроцедуры

// Заполняет строку таблицы Итогов документа СверкаВзаиморасчетов2_5_11
//
// Параметры:
//  СтрокаЗаписи - СтрокаТаблицыЗначений из см. ДетальныеЗаписи
//  ДанныеДокумента - Структура - коллекция, содержащая сведения о документе взаиморасчетов по данным контрагента
//  ДанныеОперации - Структура - коллекция, содержащая сведения об операции взаиморасчетов по данным контрагента
//
Процедура ЗаполнитьСтрокуТаблицыДетальныеЗаписиАктаСверкиВзаиморасчетов(СтрокаЗаписи, ДанныеДокумента, ДанныеОперации)
	
	ЗаполнитьЗначенияСвойств(СтрокаЗаписи, ДанныеДокумента);
	
	НомерДокумента = СокрЛП(ДанныеДокумента.Номер);
	
	СтрокаЗаписи.ИдентификаторОбъекта = ДанныеДокумента.ИдДокумента;
	СтрокаЗаписи.НомерДокумента = НомерДокумента;
	СтрокаЗаписи.НомерДокументаКонтрагент = НомерДокумента;
	СтрокаЗаписи.ДатаДокумента = ДанныеДокумента.Дата;
	СтрокаЗаписи.ДатаДокументаКонтрагент = ДанныеДокумента.Дата;
	СтрокаЗаписи.НаименованиеДокументаКонтрагент = ДанныеДокумента.Наименование;
	СтрокаЗаписи.СуммаДебет = ДанныеОперации.СуммаКредит;
	СтрокаЗаписи.СуммаДебетКонтрагент = ДанныеОперации.СуммаКредит;
	СтрокаЗаписи.СуммаКредит = ДанныеОперации.СуммаДебет;
	СтрокаЗаписи.СуммаКредитКонтрагент = ДанныеОперации.СуммаДебет;
	
КонецПроцедуры

Процедура ЗаполнитьСверкуВзаиморасчетов2_5_11(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	ЗаполнитьЗначенияСвойств(ДокументОбъект, ДанныеДляЗагрузки.ДанныеШапки);
	ДокументОбъект.ИнициализироватьДокумент();
	
	МодульЗначениеНастроек = ОбщегоНазначения.ОбщийМодуль("ЗначениеНастроекПовтИсп");
	ВалютаРегламентированногоУчета = МодульЗначениеНастроек.ВалютаРегламентированногоУчетаОрганизации(ДокументОбъект.Организация);
	
	ДокументОбъект.СверкаВВалюте = ЗначениеЗаполнено(ВалютаРегламентированногоУчета)
								И ДокументОбъект.Валюта <> ВалютаРегламентированногоУчета;
	ДокументОбъект.РежимСверкиИтоговВзаиморасчетов = Перечисления.РежимСверкиИтоговВзаиморасчетов.ПоДоговорам;
	
	// Инициализация табличных частей документа по данным шапки.
	МенеджерСверкаВзаиморасчетов2_5_11 = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Документ.СверкаВзаиморасчетов2_5_11");
	МенеджерСверкаВзаиморасчетов2_5_11.ЗаполнитьДанныеПоРасчетам(ДокументОбъект);
	
	// Заполнение табличных частей по данным отправителя.
	ИтоговыеЗаписи = ДокументОбъект.ИтоговыеЗаписи.Выгрузить();
	ДобавитьКолонкиСортировкиТаблицыИтоговАктаСверкиВзаиморасчетов(ИтоговыеЗаписи);
	
	ДанныеШапкиПоУмолчанию = Новый Структура("Партнер");
	ЗаполнитьЗначенияСвойств(ДанныеШапкиПоУмолчанию, ДокументОбъект);
	
	ЕстьРасхождения = ИтоговыеЗаписи.Количество() > ДанныеДляЗагрузки.ИтоговыеЗаписи.Количество();
	
	ИсключаемыеСвойства =
		"НомерДоговора,
		|ДатаДоговора,
		|НаименованиеДоговора,
		|Договор,
		|Партнер,
		|ТипРасчетов,
		|ОбъектРасчетов,
		|Валюта,
		|НачальноеСальдоДт,
		|НачальноеСальдоКт,
		|ОборотДт,
		|ОборотКт,
		|КонечноеСальдоДт,
		|КонечноеСальдоКт";
	
	СопоставлятьСтрокиПустыхИтогов = СопоставлятьСтрокиПустыхИтоговАктаСверкиВзаиморасчетов(
		ИтоговыеЗаписи,
		ДанныеДляЗагрузки.ИтоговыеЗаписи
	);
	
	Для Каждого СтрокаИтогов Из ДанныеДляЗагрузки.ИтоговыеЗаписи Цикл
		
		ОтборСтрокПоИтогу = ДанныеПоискаСведенийДоговораАктаСверкиВзаиморасчетов(СтрокаИтогов);
		ДанныеОтбораЗаполнены = ДанныеПоискаСведенийТаблицыАктаСверкиВзаиморасчетовЗаполнены(ОтборСтрокПоИтогу);
		СтрокиТаблицыПоОтбору = ИтоговыеЗаписи.НайтиСтроки(ОтборСтрокПоИтогу);
		
		Если ДанныеОтбораЗаполнены
			Или СопоставлятьСтрокиПустыхИтогов Тогда
			
			Если СтрокиТаблицыПоОтбору.Количество() = 1 Тогда
				
				СтрокаТаблицы = СтрокиТаблицыПоОтбору[0];
				
			Иначе
				
				СтрокаТаблицы = ИтоговыеЗаписи.Добавить();
				ЗаполнитьЗначенияСвойств(СтрокаТаблицы, ДанныеШапкиПоУмолчанию);
				ЕстьРасхождения = Истина;
				
			КонецЕсли;
			
		Иначе
			
			СтрокаТаблицы = ИтоговыеЗаписи.Добавить();
			ЗаполнитьЗначенияСвойств(СтрокаТаблицы, ДанныеШапкиПоУмолчанию);
			ЕстьРасхождения = Истина;
			
		КонецЕсли;
		
		ЗаполнитьЗначенияСвойств(СтрокаТаблицы, СтрокаИтогов, Неопределено, ИсключаемыеСвойства);
		
	КонецЦикла;
	
	Для Каждого СтрокаТаблицы Из ИтоговыеЗаписи Цикл
		ЗаполнитьКолонкиСортировкиСтрокиТаблицыИтоговАктаСверкиВзаиморасчетов(СтрокаТаблицы);
	КонецЦикла;
	
	ИтоговыеЗаписи.Сортировать(
		"СтрокаСопоставлена Убыв,
		|ОбщаяДата,
		|ОбщийОборотДт,
		|ОбщийОборотКт,
		|ОборотДт,
		|ОборотДтКонтрагент,
		|ОборотКт,
		|ОборотКтКонтрагент"
	);
	
	ДетальныеЗаписи = ДокументОбъект.ДетальныеЗаписи.Выгрузить();
	ДобавитьКолонкиСортировкиТаблицыЗаписейАктаСверкиВзаиморасчетов(ДетальныеЗаписи);
	
	Если Не ЕстьРасхождения Тогда
		ЕстьРасхождения = ДетальныеЗаписи.Количество() > ДанныеДляЗагрузки.ДетальныеЗаписи.Количество();
	КонецЕсли;
	
	ЗаполняемыеСвойства =
		"НомерДокументаКонтрагент,
		|ДатаДокументаКонтрагент,
		|НаименованиеДокументаКонтрагент,
		|СуммаДебетКонтрагент,
		|СуммаКредитКонтрагент";
	
	Для Каждого СтрокаДетальнойЗаписи Из ДанныеДляЗагрузки.ДетальныеЗаписи Цикл
		
		ОтборСтрокПоЗаписи = ДанныеПоискаСведенийДокументаАктаСверкиВзаиморасчетов(СтрокаДетальнойЗаписи);
		СтрокиТаблицыПоОтбору = ДетальныеЗаписи.НайтиСтроки(ОтборСтрокПоЗаписи);
		
		Если СтрокиТаблицыПоОтбору.Количество() = 1 Тогда
			СтрокаЗаписи = СтрокиТаблицыПоОтбору[0];
		Иначе
			СтрокаЗаписи = ДетальныеЗаписи.Добавить();
			
			ЕстьРасхождения = Истина;
		КонецЕсли;
		
		ЗаполнитьЗначенияСвойств(СтрокаЗаписи, СтрокаДетальнойЗаписи, ЗаполняемыеСвойства);
		
	КонецЦикла;
	
	Для Каждого СтрокаЗаписи Из ДетальныеЗаписи Цикл
		ЗаполнитьКолонкиСортировкиСтрокиТаблицыЗаписейАктаСверкиВзаиморасчетов(СтрокаЗаписи);
	КонецЦикла;
	
	ДетальныеЗаписи.Сортировать(
		"СтрокаСопоставлена Убыв,
		|ОбщаяДата,
		|ОбщаяСуммаДт,
		|ОбщаяСуммаКт,
		|СуммаДебет,
		|СуммаКредит"
	);
	
	ДокументОбъект.ИтоговыеЗаписи.Загрузить(ИтоговыеЗаписи);
	ДокументОбъект.ДетальныеЗаписи.Загрузить(ДетальныеЗаписи);
	
	ДокументОбъект.Статус = Перечисления.СтатусыСверокВзаиморасчетов2_5_11.НаСверке;
	ДокументОбъект.ЕстьРасхождения = ЕстьРасхождения;
	
КонецПроцедуры

Процедура ЗаполнитьЗаказПоставщику(ДокументОбъект, ДанныеЗаполнения)
	
	Шапка = ДанныеЗаполнения.Шапка;
	
	ДокументОбъект.Автор = Пользователи.АвторизованныйПользователь();
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	ДокументОбъект.Заполнить(Шапка);
	
	ИспользоватьСтатусы = ПолучитьФункциональнуюОпцию("ИспользоватьСтатусыЗаказовПоставщикам");
	Если ИспользоватьСтатусы Тогда
		ДокументОбъект.Статус = Перечисления.СтатусыЗаказовПоставщикам.НеСогласован;
	КонецЕсли;
	
	// пересчет цен и сумм в зависимости от включения НДС в цену
	ПересчитатьЦенуСуммуПриУстановкеФлагаЦенаВключаетНДС(ДокументОбъект, "Товары");
	
	// сумма документа
	ДокументОбъект.СуммаДокумента = ЦенообразованиеКлиентСервер.ПолучитьСуммуДокумента(
		ДокументОбъект.Товары,
		ДокументОбъект.ЦенаВключаетНДС
	);
	
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	ДокументОбъект.ПорядокРасчетов = ДокументОбъект.Договор.ПорядокРасчетов;
	ДокументОбъект.ВариантПриемкиТоваров = ДокументОбъект.Договор.ВариантПриемкиТоваров;
	
	ДокументОбъект.ГруппаФинансовогоУчета = ДокументОбъект.Договор.ГруппаФинансовогоУчета;
	
	ДокументОбъект.Товары.Загрузить(ДанныеЗаполнения.Товары);
	
	// Тип налогообложения
	ЗаполнитьНалогообложениеНДСПлатежныхДокументов(ДокументОбъект, ДанныеЗаполнения.НДСИсчисляетсяНалоговымАгентом);
	
КонецПроцедуры

Процедура ЗаполнитьЗаявкуНаРасходованиеДС(ДокументОбъект, ДанныеЗаполнения)
	
	ИспользоватьСтатусы = ПолучитьФункциональнуюОпцию("ИспользоватьСтатусыЗаказовПоставщикам");
	Если ИспользоватьСтатусы Тогда
		ДокументОбъект.Статус = Перечисления.СтатусыЗаказовПоставщикам.НеСогласован;
	КонецЕсли;
	
	ДокументОбъект.Автор = Пользователи.АвторизованныйПользователь();
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	ДокументОбъект.Заполнить(ДанныеЗаполнения.Шапка);
	
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	ДокументОбъект.ДоговорСУчастникомГОЗ = Ложь;
	ДокументОбъект.ПлатежиПо275ФЗ = Ложь;
	
	ДокументОбъект.РасшифровкаПлатежа.Загрузить(ДанныеЗаполнения.РасшифровкаПлатежа);
	
	Договор = ДокументОбъект.Договор;
	Если ЗначениеЗаполнено(Договор) Тогда
		
		ДокументОбъект.СтатьяДвиженияДенежныхСредств = Договор.СтатьяДвиженияДенежныхСредств;
		ДокументОбъект.ГруппаФинансовогоУчета = Договор.ГруппаФинансовогоУчета;
		ДокументОбъект.НаправлениеДеятельности = Договор.НаправлениеДеятельности;
		
		Если Договор.ДоговорСУчастникомГОЗ Тогда
			ДокументОбъект.ДоговорСУчастникомГОЗ = Истина;
			ДокументОбъект.ПлатежиПо275ФЗ = Истина;
		КонецЕсли;
		
		Для Каждого Расшифровка Из ДокументОбъект.РасшифровкаПлатежа Цикл
			Расшифровка.СтатьяДвиженияДенежныхСредств = Договор.СтатьяДвиженияДенежныхСредств;
			Расшифровка.НаправлениеДеятельности = Договор.НаправлениеДеятельности;
		КонецЦикла;
	КонецЕсли;
	
	ЗаполнитьНалогообложениеНДСПлатежныхДокументов(ДокументОбъект, ДанныеЗаполнения.НДСИсчисляетсяНалоговымАгентом);
	
КонецПроцедуры

Процедура ЗаполнитьСписаниеБезналичныхДС(ДокументОбъект, ДанныеЗаполнения)
	
	Если ЭтоРедакция11_5() Тогда
		ДокументОбъект.Автор = Пользователи.АвторизованныйПользователь();
	КонецЕсли;
	
	ДокументОбъект.Ответственный = Пользователи.АвторизованныйПользователь();
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	ДокументОбъект.Заполнить(ДанныеЗаполнения.Шапка);
	
	ДокументОбъект.РасшифровкаПлатежа.Загрузить(ДанныеЗаполнения.РасшифровкаПлатежа);
	
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	Договор = ДокументОбъект.Договор;
	Если ЗначениеЗаполнено(Договор) Тогда
		
		ДокументОбъект.СтатьяДвиженияДенежныхСредств = Договор.СтатьяДвиженияДенежныхСредств;
		ДокументОбъект.ГруппаФинансовогоУчета = Договор.ГруппаФинансовогоУчета;
		ДокументОбъект.НаправлениеДеятельности = Договор.НаправлениеДеятельности;
		
		Для Каждого Расшифровка Из ДокументОбъект.РасшифровкаПлатежа Цикл
			Расшифровка.СтатьяДвиженияДенежныхСредств = Договор.СтатьяДвиженияДенежныхСредств;
			Расшифровка.НаправлениеДеятельности = Договор.НаправлениеДеятельности;
		КонецЦикла;
		
	КонецЕсли;
	
	ЗаполнитьНалогообложениеНДСПлатежныхДокументов(ДокументОбъект, ДанныеЗаполнения.НДСИсчисляетсяНалоговымАгентом);
	
КонецПроцедуры

Процедура ЗаполнитьРасходныйКассовыйОрдер(ДокументОбъект, ДанныеЗаполнения)
	
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	
	Если ЭтоРедакция11_5() Тогда
		ДокументОбъект.Автор = Пользователи.АвторизованныйПользователь();
	КонецЕсли;
	
	ДокументОбъект.Заполнить(ДанныеЗаполнения.Шапка);
	ДокументОбъект.РасшифровкаПлатежа.Загрузить(ДанныеЗаполнения.РасшифровкаПлатежа);
	
	ЗаполнитьДоговорСПоставщиком(ДокументОбъект);
	
	Договор = ДокументОбъект.Договор;
	Если ЗначениеЗаполнено(Договор) Тогда
		
		ДокументОбъект.СтатьяДвиженияДенежныхСредств = Договор.СтатьяДвиженияДенежныхСредств;
		ДокументОбъект.ГруппаФинансовогоУчета = Договор.ГруппаФинансовогоУчета;
		ДокументОбъект.НаправлениеДеятельности = Договор.НаправлениеДеятельности;
		
		Для Каждого Расшифровка Из ДокументОбъект.РасшифровкаПлатежа Цикл
			Расшифровка.СтатьяДвиженияДенежныхСредств = Договор.СтатьяДвиженияДенежныхСредств;
			Расшифровка.НаправлениеДеятельности = Договор.НаправлениеДеятельности;
		КонецЦикла;
		
	КонецЕсли;
	
	ЗаполнитьНалогообложениеНДСПлатежныхДокументов(ДокументОбъект, ДанныеЗаполнения.НДСИсчисляетсяНалоговымАгентом);
	
КонецПроцедуры

// Добавляет в таблицу, описывающую табличную часть ИтоговыеЗаписи документа СверкаВзаиморасчетов2_5_11 колонки,
// используемые для сортировки данных.
//
// Параметры:
//  ТаблицаАкта - ТаблицаЗначений - выгруженная табличная часть ИтоговыеЗаписи документа СверкаВзаиморасчетов2_5_11
//
Процедура ДобавитьКолонкиСортировкиТаблицыИтоговАктаСверкиВзаиморасчетов(ТаблицаАкта)
	
	МетаданныеСверкаВзаиморасчетов2_5_11 = ОбщегоНазначения.ОбъектМетаданныхПоПолномуИмени("Документ.СверкаВзаиморасчетов2_5_11");
	
	ОписаниеТаблицы = МетаданныеСверкаВзаиморасчетов2_5_11.ТабличныеЧасти.ИтоговыеЗаписи;
	КолонкиТаблицы = ТаблицаАкта.Колонки;
	
	КолонкиТаблицы.Добавить("ОбщаяДата", ОписаниеТаблицы.Реквизиты.ДатаДоговора.Тип);
	КолонкиТаблицы.Добавить("СтрокаСопоставлена", Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(1)));
	КолонкиТаблицы.Добавить("ОбщийОборотДт", ОписаниеТаблицы.Реквизиты.ОборотДт.Тип);
	КолонкиТаблицы.Добавить("ОбщийОборотКт", ОписаниеТаблицы.Реквизиты.ОборотКт.Тип);
	
КонецПроцедуры

// Возвращает признак необходимости сопоставления пустых строк таблицы ИтоговыеЗаписи
// документа СверкаВзаиморасчетов2_5_11 с пустыми строками таблицы загружаемых данных ЭД
//
// Параметры:
//  ИтоговыеЗаписи - ТаблицаЗначений - выгруженная табличная часть ИтоговыеЗаписи документа СверкаВзаиморасчетов2_5_11
//  ЗагружаемыеИтоговыеЗаписи - см. ОписаниеТаблицыИтоговыеЗаписиАктаСверкиВзаиморасчетов.
//
// Возвращаемое значение:
//  Булево - Истина, если необходимо сопоставлять пустые строки таблиц
//
Функция СопоставлятьСтрокиПустыхИтоговАктаСверкиВзаиморасчетов(ИтоговыеЗаписи, ЗагружаемыеИтоговыеЗаписи)
	
	ОтборПустыхИтогов = Новый Структура;
	ОтборПустыхИтогов.Вставить("ДатаДоговора", Дата(1, 1, 1));
	ОтборПустыхИтогов.Вставить("НомерДоговора", "");
	
	КоличествоСтрокПустыхИтогов = ИтоговыеЗаписи.НайтиСтроки(ОтборПустыхИтогов).Количество();
	
	КоличествоСтрокПустыхЗагружаемыхИтогов = ЗагружаемыеИтоговыеЗаписи.НайтиСтроки(ОтборПустыхИтогов).Количество();
	
	СопоставлятьСтрокиПустыхИтогов = КоличествоСтрокПустыхИтогов = 1
									И КоличествоСтрокПустыхЗагружаемыхИтогов = 1;
	
	Возврат СопоставлятьСтрокиПустыхИтогов;
	
КонецФункции

// Возвращает результат проверки заполнения свойств переданной структуры
//
// Параметры:
//  ДанныеПоиска - Структура - коллекция, используемая в качестве отбора строк в таблице документа.
//
// Возвращаемое значение:
//  Булево - Истина, если все свойства коллекции заполнены.
//
Функция ДанныеПоискаСведенийТаблицыАктаСверкиВзаиморасчетовЗаполнены(ДанныеПоиска)
	
	ДанныеЗаполнены = Ложь;
	
	Для Каждого КлючИЗначение Из ДанныеПоиска Цикл
		
		Если ЗначениеЗаполнено(КлючИЗначение.Значение) Тогда
			
			ДанныеЗаполнены = Истина;
			
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат ДанныеЗаполнены;
	
КонецФункции

// Возвращает структуру, используемую для поиска сведений в таблице ИтоговыеЗаписи документа СверкаВзаиморасчетов2_5_11
//
// Параметры:
//  ДанныеДоговора - Структура - коллекция сведений о договоре взаиморасчетов по данным контрагента, содержащая следующие свойства:
//   * ДатаДоговора - Дата - дата оформления договора по данным контрагента.
//   * НомерДоговора - Строка - номер договора по данным контрагент.
//
// Возвращаемое значение:
//  Структура - коллекция для отбора строк в таблице, содержащая следующие свойства:
//   * ДатаДоговора - Дата - дата искомого договора.
//   * НомерДоговора - Строка - номер искомого договора.
//
Функция ДанныеПоискаСведенийДоговораАктаСверкиВзаиморасчетов(ДанныеДоговора)
	
	ДанныеПоиска = Новый Структура;
	ДанныеПоиска.Вставить("ДатаДоговора", Дата(1, 1, 1));
	ДанныеПоиска.Вставить("НомерДоговора", "");
	
	ЗаполнитьЗначенияСвойств(ДанныеПоиска, ДанныеДоговора);
	
	Возврат ДанныеПоиска;
	
КонецФункции

// Заполняет колонки, используемые для сортировки данных в строке таблицы ИтоговыеЗаписи документа СверкаВзаиморасчетов2_5_11
//
// Параметры:
//  СтрокаТЧ - СтрокаТаблицыЗначений из ТЧ ИтоговыеЗаписи
//
Процедура ЗаполнитьКолонкиСортировкиСтрокиТаблицыИтоговАктаСверкиВзаиморасчетов(СтрокаТЧ)

	СтрокаТЧ.ОбщаяДата = ?(ЗначениеЗаполнено(СтрокаТЧ.ДатаДоговора),
							СтрокаТЧ.ДатаДоговора,
							СтрокаТЧ.ДатаДоговораКонтрагент);
	
	Если (ЗначениеЗаполнено(СтрокаТЧ.ДатаДоговора)
			И ЗначениеЗаполнено(СтрокаТЧ.ДатаДоговораКонтрагент))
		Или (ЗначениеЗаполнено(СтрокаТЧ.НомерДоговора)
			И ЗначениеЗаполнено(СтрокаТЧ.НомерДоговораКонтрагент))
		Или (СтрокаТЧ.НачальноеСальдоДт <> 0
			И СтрокаТЧ.НачальноеСальдоДтКонтрагент <> 0)
		Или (СтрокаТЧ.НачальноеСальдоКт <> 0
			И СтрокаТЧ.НачальноеСальдоКтКонтрагент <> 0)
		Или (СтрокаТЧ.ОборотДт <> 0
			И СтрокаТЧ.ОборотДтКонтрагент <> 0)
		Или (СтрокаТЧ.ОборотКт <> 0
			И СтрокаТЧ.ОборотКтКонтрагент <> 0)
		Или (СтрокаТЧ.КонечноеСальдоДт <> 0
			И СтрокаТЧ.КонечноеСальдоДтКонтрагент <> 0)
		Или (СтрокаТЧ.КонечноеСальдоКт <> 0
			И СтрокаТЧ.КонечноеСальдоКтКонтрагент <> 0) Тогда
		
		СтрокаТЧ.СтрокаСопоставлена = 1;
		
	КонецЕсли;
	
	СтрокаТЧ.ОбщийОборотДт = ?(СтрокаТЧ.ОборотДт <> 0
									Или СтрокаТЧ.СтрокаСопоставлена = 1,
								СтрокаТЧ.ОборотДт,
								СтрокаТЧ.ОборотДтКонтрагент);
	
	СтрокаТЧ.ОбщийОборотКт = ?(СтрокаТЧ.ОборотКт <> 0
									Или СтрокаТЧ.СтрокаСопоставлена = 1,
								СтрокаТЧ.ОборотКт,
								СтрокаТЧ.ОборотКтКонтрагент);
	
КонецПроцедуры

// Добавляет в таблицу, описывающую табличную часть ДетальныеЗаписи
// документа СверкаВзаиморасчетов2_5_11 колонки, используемые для сортировки данных.
//
// Параметры:
//   ТаблицаАкта - ТаблицаЗначений - выгруженная табличная часть ДетальныеЗаписи документа СверкаВзаиморасчетов2_5_11
//
Процедура ДобавитьКолонкиСортировкиТаблицыЗаписейАктаСверкиВзаиморасчетов(ТаблицаАкта)
	
	МетаданныеСверкаВзаиморасчетов2_5_11 = ОбщегоНазначения.ОбъектМетаданныхПоПолномуИмени("Документ.СверкаВзаиморасчетов2_5_11");
	
	ОписаниеТаблицы = МетаданныеСверкаВзаиморасчетов2_5_11.ТабличныеЧасти.ДетальныеЗаписи;
	КолонкиТаблицы = ТаблицаАкта.Колонки;
	
	КолонкиТаблицы.Добавить("ОбщаяДата", ОписаниеТаблицы.Реквизиты.ДатаДокумента.Тип);
	КолонкиТаблицы.Добавить("СтрокаСопоставлена", Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(1)));
	КолонкиТаблицы.Добавить("ОбщаяСуммаДт", ОписаниеТаблицы.Реквизиты.СуммаДебет.Тип);
	КолонкиТаблицы.Добавить("ОбщаяСуммаКт", ОписаниеТаблицы.Реквизиты.СуммаКредит.Тип);
	
КонецПроцедуры

// Возвращает структуру, используемую для поиска сведений в таблице ДетальныеЗаписи документа СверкаВзаиморасчетов2_5_11
//
// Параметры:
//  ДанныеДокумента - Структура - коллекция сведений о документе взаиморасчетов по данным контрагента, содержащая следующие свойства:
//   * ДатаДокумента - Дата - дата оформления документа по данным контрагента.
//   * НомерДокумента - Строка - номер документа по данным контрагент.
//   * СуммаДебет - Число - сумма Дт взаиморасчетов по документу.
//   * СуммаКредит - Число - сумма Кт взаиморасчетов по документу.
//
// Возвращаемое значение:
//  Структура - коллекция для отбора строк в таблице, содержащая следующие свойства:
//   * ДатаДокумента - Дата - дата искомого документа.
//   * НомерДокумента - Строка - номер искомого документа.
//   * СуммаДебет - Число - сумма Дт искомого документа.
//   * СуммаКредит - Число - сумма Кт искомого документа.
//
Функция ДанныеПоискаСведенийДокументаАктаСверкиВзаиморасчетов(ДанныеДокумента)
	
	ДанныеПоиска = Новый Структура;
	ДанныеПоиска.Вставить("ДатаДокумента", Дата(1, 1, 1));
	ДанныеПоиска.Вставить("НомерДокумента", "");
	
	ЗаполнитьЗначенияСвойств(ДанныеПоиска, ДанныеДокумента);
	
	Возврат ДанныеПоиска;
	
КонецФункции

// Заполняет колонки, используемые для сортировки данных в строке таблицы ДетальныеЗаписи документа СверкаВзаиморасчетов2_5_11
//
// Параметры:
//  СтрокаТЧ - СтрокаТаблицыЗначений из см. ОписаниеТаблицыДетальныеЗаписиАктаСверкиВзаиморасчетов.
//
Процедура ЗаполнитьКолонкиСортировкиСтрокиТаблицыЗаписейАктаСверкиВзаиморасчетов(СтрокаТЧ)

	СтрокаТЧ.ОбщаяДата = ?(ЗначениеЗаполнено(СтрокаТЧ.ДатаДокумента),
							СтрокаТЧ.ДатаДокумента,
							СтрокаТЧ.ДатаДокументаКонтрагент);
	
	Если (СтрокаТЧ.СуммаДебет <> 0
			И СтрокаТЧ.СуммаДебетКонтрагент <> 0)
		Или (СтрокаТЧ.СуммаКредит <> 0
			И СтрокаТЧ.СуммаКредитКонтрагент <> 0) Тогда
		
		СтрокаТЧ.СтрокаСопоставлена = 1;
		
	КонецЕсли;
	
	СтрокаТЧ.ОбщаяСуммаДт = ?(СтрокаТЧ.СуммаДебет <> 0
									Или СтрокаТЧ.СтрокаСопоставлена = 1,
								СтрокаТЧ.СуммаДебет,
								СтрокаТЧ.СуммаДебетКонтрагент);
	
	СтрокаТЧ.ОбщаяСуммаКт = ?(СтрокаТЧ.СуммаКредит <> 0
									Или СтрокаТЧ.СтрокаСопоставлена = 1,
								СтрокаТЧ.СуммаКредит,
								СтрокаТЧ.СуммаКредитКонтрагент);
	
КонецПроцедуры

Функция НовыйДанныеДляЗаполненияАктаСверкиВзаиморасчетов()
	
	МенеджерСверкаВзаиморасчетов = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Документ.СверкаВзаиморасчетов");
	
	ГруппировкиРасчеты = МенеджерСверкаВзаиморасчетов.ПустаяСсылка().ГруппировкиРасчеты.ВыгрузитьКолонки();
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеШапки", Новый Структура);
	Результат.Вставить("ГруппировкиРасчеты", ГруппировкиРасчеты);
	
	Возврат Результат;
	
КонецФункции

Функция СтруктураЗаполненияСверкиВзаиморасчетов(ДанныеЭД, ДополнительныеПараметры)
	
	ШапкаЭД = ДанныеЭД.Шапка;
	ТабличнаяЧастьЭД = ДанныеЭД.ТоварнаяЧасть;
	ИтогиЭД = ДанныеЭД.Итоги;
	
	ДанныеОбъекта = НовыйДанныеДляЗаполненияАктаСверкиВзаиморасчетов();
	
	// Заполнение шапки документа
	ДанныеОбъекта.ДанныеШапки = ДанныеШапкиАктаСверкиВзаиморасчетовПоДаннымОтправителя(ШапкаЭД);
	
	// Заполнение сведений табличной части ГруппировкиРасчеты
	ЗаполнитьГруппировкиРасчетыДляЗаполненияАктаСверкиВзаиморасчетов(
		ДанныеОбъекта,
		ТабличнаяЧастьЭД
	);
	
	Возврат ДанныеОбъекта;
	
КонецФункции

Процедура ЗаполнитьГруппировкиРасчетыДляЗаполненияАктаСверкиВзаиморасчетов(ДанныеОбъекта, ТабличнаяЧастьЭД)
	
	ДанныеШапки = ДанныеОбъекта.ДанныеШапки;
	ГруппировкиРасчеты = ДанныеОбъекта.ГруппировкиРасчеты;
	
	Для Каждого СтрокаДоговора Из ТабличнаяЧастьЭД.Договор Цикл
		
		СтрокаЗаписи = ГруппировкиРасчеты.Добавить();
		
		ЗаполнитьСтрокуТаблицыГруппировкиРасчетыДаннымиДоговора(
			СтрокаЗаписи,
			СтрокаДоговора,
			ДанныеШапки
		);
		
		Для Каждого СтрокаДокумента Из СтрокаДоговора.Документ Цикл
			
			Для Каждого СтрокаОперации Из СтрокаДокумента.Операция Цикл
				
				Если СтрокаОперации.СуммаДебет = 0
					И СтрокаОперации.СуммаКредит = 0 Тогда
					
					Продолжить;
					
				КонецЕсли;
				
				ЗаполнитьСтрокуТаблицыГруппировкиРасчетыДаннымиОперации(
					СтрокаЗаписи,
					СтрокаОперации
				);
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьСтрокуТаблицыГруппировкиРасчетыДаннымиДоговора(СтрокаЗаписи, ДанныеДоговора, ДанныеШапки)
	
	Договор = ДоговорПоСтрокеАктаСверки(ДанныеДоговора, ДанныеШапки);
	
	ТипРасчетов = Перечисления.ТипыРасчетовСПартнерами.РасчетыСПоставщиком;
	Если Договор.ТипДоговора = Перечисления.ТипыДоговоров.СПокупателем Тогда
		ТипРасчетов = Перечисления.ТипыРасчетовСПартнерами.РасчетыСКлиентом;
	КонецЕсли;
	
	СтрокаЗаписи.ТипРасчетов = ТипРасчетов;
	СтрокаЗаписи.Договор = Договор;
	СтрокаЗаписи.Партнер = Договор.Партнер;
	СтрокаЗаписи.Валюта = Договор.ВалютаВзаиморасчетов;
	
КонецПроцедуры

Процедура ЗаполнитьСтрокуТаблицыГруппировкиРасчетыДаннымиОперации(СтрокаЗаписи, ДанныеОперации)
	
	СтрокаЗаписи.ОборотПриход = ДанныеОперации.СуммаДебет;
	СтрокаЗаписи.ОборотРасход = ДанныеОперации.СуммаКредит;
	СтрокаЗаписи.КонечноеСальдо = ДанныеОперации.СуммаДебет - ДанныеОперации.СуммаКредит;
	
КонецПроцедуры

Процедура ЗаполнитьСверкуВзаиморасчетов(ДокументОбъект, ДанныеДляЗагрузки)
	
	ДокументОбъект.Дата = ТекущаяДатаСеанса();
	
	ДокументОбъект.УстановитьНовыйНомер();
	
	ЗаполнитьЗначенияСвойств(ДокументОбъект, ДанныеДляЗагрузки.ДанныеШапки);
	ДокументОбъект.ИнициализироватьДокумент();
	
	ДокументОбъект.РасшифровкаПоЗаказам = Ложь;
	ДокументОбъект.РасшифровкаПоПартнерам = Ложь;
	ДокументОбъект.РасшифровкаПоДоговорам = Ложь;
	ДокументОбъект.РазбиватьПоТипамРасчетов = Ложь;
	ДокументОбъект.РазбиватьПоПартнерам = Ложь;
	ДокументОбъект.РазбиватьПоДоговорам = Ложь;
	ДокументОбъект.Статус = Перечисления.СтатусыСверокВзаиморасчетов.НаСверке;
	ДокументОбъект.ЕстьРасхождения = Истина;
	
	ВалютаДокумента = ДанныеДляЗагрузки.ДанныеШапки.Валюта;
	ВалютаРегУчетаОрганизации = ВалютаРегламентированногоУчета();
	
	ГруппировкиРасчеты = ДокументОбъект.ГруппировкиРасчеты.Выгрузить();
	
	Для Каждого СтрокаГруппировки Из ДанныеДляЗагрузки.ГруппировкиРасчеты Цикл
		
		СтрокаЗаписи = ГруппировкиРасчеты.Добавить();
		
		ЗаполнитьЗначенияСвойств(СтрокаЗаписи, СтрокаГруппировки);
		
		Если НЕ ЗначениеЗаполнено(СтрокаЗаписи.Валюта) Тогда
			СтрокаЗаписи.Валюта = ВалютаДокумента;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(СтрокаЗаписи.Партнер) Тогда
			СтрокаЗаписи.Партнер = ДокументОбъект.Партнер;
		КонецЕсли;
		
		КонечноеСальдоРегл = РаботаСКурсамиВалют.ПересчитатьВВалюту(
			СтрокаГруппировки.КонечноеСальдо,
			ВалютаДокумента,
			ВалютаРегУчетаОрганизации,
			ДокументОбъект.Дата
		);
		
		ОборотПриходРегл = РаботаСКурсамиВалют.ПересчитатьВВалюту(
			СтрокаГруппировки.ОборотПриход,
			ВалютаДокумента,
			ВалютаРегУчетаОрганизации,
			ДокументОбъект.Дата
		);
		
		ОборотРасходРегл = РаботаСКурсамиВалют.ПересчитатьВВалюту(
			СтрокаГруппировки.ОборотРасход,
			ВалютаДокумента,
			ВалютаРегУчетаОрганизации,
			ДокументОбъект.Дата
		);
		
		СтрокаЗаписи.ОборотПриходРегл = ОборотПриходРегл;
		СтрокаЗаписи.ОборотРасходРегл = ОборотРасходРегл;
		СтрокаЗаписи.КонечноеСальдоРегл = КонечноеСальдоРегл;
		
	КонецЦикла;
	
	ДокументОбъект.ГруппировкиРасчеты.Загрузить(ГруппировкиРасчеты);
	
КонецПроцедуры

Функция ДоговорПоСтрокеАктаСверки(ДанныеДоговора, ШапкаДокумента)
	
	ВалютаДоговора = ШапкаДокумента.Валюта;
	Организация = ШапкаДокумента.Организация;
	Контрагент = ШапкаДокумента.Контрагент;
	НомерДоговора = ДанныеДоговора.Номер;
	ДатаДоговора = ДанныеДоговора.Дата;
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса_ДоговорПоСтрокеАктаСверки();
	
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Контрагент", Контрагент);
	Запрос.УстановитьПараметр("Дата", ДатаДоговора);
	Запрос.УстановитьПараметр("Номер", НомерДоговора);
	Запрос.УстановитьПараметр("ВалютаВзаиморасчетов", ВалютаДоговора);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Результат = Справочники.ДоговорыКонтрагентов.ПустаяСсылка();
	
	Если НЕ РезультатЗапроса.Пустой() Тогда
		
		Выборка = РезультатЗапроса.Выбрать();
		Выборка.Следующий();
		
		Результат = Выборка.Ссылка;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ДоговорПоСтрокеАктаСверки()
	
	Результат =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ДоговорыКонтрагентов.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ДоговорыКонтрагентов КАК ДоговорыКонтрагентов
	|ГДЕ
	|	ДоговорыКонтрагентов.Организация = &Организация
	|	И ДоговорыКонтрагентов.Контрагент = &Контрагент
	|	И ДоговорыКонтрагентов.Дата = &Дата
	|	И ДоговорыКонтрагентов.Номер = &Номер
	|	И ДоговорыКонтрагентов.ВалютаВзаиморасчетов = &ВалютаВзаиморасчетов
	|	И ДоговорыКонтрагентов.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыДоговоровКонтрагентов.Действует)
	|	И НЕ ДоговорыКонтрагентов.ПометкаУдаления";
	
	Возврат Результат;
	
КонецФункции

// BSLLS:CognitiveComplexity-off

Функция СтруктураЗаполненияЗаказаПоставщику(ДанныеЭД, ДополнительныеПараметры)
	
	ПустаяСсылкаНаДокумент = Документы.ЗаказПоставщику.ПустаяСсылка();
	
	ШапкаЭД = ДанныеЭД.Шапка;
	
	Контрагент = СсылкаНаУчастникаСделки(ШапкаЭД.Продавец);
	Организация = СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель);
	НомерСчетаОрганизации = ШапкаЭД.Покупатель.НомерСчета;
	БанковскийСчетОрганизации = БанковскийСчетОрганизацииПоНомеру(Организация, НомерСчетаОрганизации);
	
	ОплатаВВалюте = Ложь;
	Если ШапкаЭД.Валюта.Код <> "643" Тогда
		ОплатаВВалюте = Истина;
	КонецЕсли;
	
	Шапка = Новый Структура;
	
	Шапка.Вставить("Организация", Организация);
	Шапка.Вставить("Контрагент", Контрагент);
	Шапка.Вставить("Партнер" , ПартнерПоКонтрагенту(Шапка.Контрагент));
	
	Шапка.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("ЦенаВключаетНДС", ДополнительныеПараметры.ЦенаВключаетНДС);
	Шапка.Вставить("СуммаВключаетНДС", ДополнительныеПараметры.ЦенаВключаетНДС);
	Шапка.Вставить("СуммаДокумента", ДанныеЭД.Итоги.СуммаСНДС);
	Шапка.Вставить("БанковскийСчет", БанковскийСчетОрганизации);
	Шапка.Вставить("ХозяйственнаяОперация", Перечисления.ХозяйственныеОперации.ЗакупкаУПоставщика);
	Шапка.Вставить("НомерПоДаннымПоставщика", ШапкаЭД.Номер);
	Шапка.Вставить("ДатаПоДаннымПоставщика", ШапкаЭД.Дата);
	Шапка.Вставить("РегистрироватьЦеныПоставщика", Ложь);
	Шапка.Вставить("ВернутьМногооборотнуюТару", Ложь);
	Шапка.Вставить("СрокВозвратаМногооборотнойТары", 0);
	Шапка.Вставить("ТребуетсяЗалогЗаТару", Ложь);
	Шапка.Вставить("СуммаВозвратнойТары", 0);
	
	// BSLLS:UsingFindElementByString-off
	Шапка.Вставить("Приоритет", Справочники.Приоритеты.НайтиПоНаименованию("Средний"));
	// BSLLS:UsingFindElementByString-on
	
	Шапка.Вставить("СпособДоставки", Перечисления.СпособыДоставки.СиламиПоставщикаДоНашегоСклада);
	Шапка.Вставить("ЕстьКиЗГИСМ", Ложь);
	Шапка.Вставить("ОплатаВВалюте", ОплатаВВалюте);
	
	Если ШапкаЭД.Покупатель.Свойство("АдрТекст") Тогда
		Шапка.Вставить("АдресДоставки", ШапкаЭД.Покупатель.АдрТекст);
	КонецЕсли;
	
	ДатаНачТов = "";
	Если ШапкаЭД.Свойство("ДатаНачТов") Тогда
		ДатаНачТов = ШапкаЭД.ДатаНачТов;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДатаНачТов) Тогда
		Шапка.Вставить("ЖелаемаяДатаПоступления", ДатаНачТов);
		Шапка.Вставить("ДатаПервогоПоступления", ДатаНачТов);
		Шапка.Вставить("ДатаПоступления", ДатаНачТов);
		Шапка.Вставить("ДатаОтгрузки", ДатаНачТов);
	КонецЕсли;
	
	Если ШапкаЭД.Свойство("УсловияОказанияПоставки") Тогда
		Шапка.Вставить("ОсобыеУсловияПеревозки", Истина);
		ОсобыеУсловияПеревозкиОписание = "";
		Для Каждого УсловиеОказаниеПоставки Из ШапкаЭД.УсловияОказанияПоставки Цикл
			ОсобыеУсловияПеревозкиОписание = (ОсобыеУсловияПеревозкиОписание + УсловиеОказаниеПоставки + Символы.ПС);
		КонецЦикла;
		Шапка.Вставить("ОсобыеУсловияПеревозкиОписание", ОсобыеУсловияПеревозкиОписание);
	КонецЕсли;
	
	Товары = ПустаяСсылкаНаДокумент.Товары.ВыгрузитьКолонки();
	
	НДСИсчисляетсяНалоговымАгентом = Ложь;
	
	Для Каждого СтрокаТовара Из ДанныеЭД.ТоварнаяЧасть Цикл
		СтрокаПоДаннымКонтрагента = Товары.Добавить();
		СтрокаПоДаннымКонтрагента.Номенклатура = СтрокаТовара.Номенклатура.Номенклатура;
		Если ЗначениеЗаполнено(СтрокаТовара.Номенклатура.Номенклатура) Тогда
			СтрокаПоДаннымКонтрагента.Упаковка = СтрокаТовара.Номенклатура.Упаковка;
		КонецЕсли;
		СтрокаПоДаннымКонтрагента.КоличествоУпаковок = СтрокаТовара.Количество;
		СтрокаПоДаннымКонтрагента.Количество = СтрокаТовара.Количество;
		СтрокаПоДаннымКонтрагента.Цена = СтрокаТовара.Цена;
		СтрокаПоДаннымКонтрагента.Сумма = СтрокаТовара.СуммаБезНДС;
		СтрокаПоДаннымКонтрагента.СтавкаНДС = ЗначениеСтавкиНДС(СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС);
		СтрокаПоДаннымКонтрагента.СуммаНДС = СтрокаТовара.СуммаНДС;
		СтрокаПоДаннымКонтрагента.Отменено = Ложь;
		СтрокаПоДаннымКонтрагента.ПроцентРучнойСкидки = СтрокаТовара.ПроцентСкидки;
		СтрокаПоДаннымКонтрагента.СуммаРучнойСкидки = СтрокаТовара.СуммаСкидки;
		
		Если ЗначениеЗаполнено(ДатаНачТов) Тогда
			СтрокаПоДаннымКонтрагента.ДатаПоступления = ДатаНачТов;
			Если ЭтоРедакция11_5() Тогда
				СтрокаПоДаннымКонтрагента.ДатаОтгрузки = ДатаНачТов;
			КонецЕсли;
		КонецЕсли;
		
		Если СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС = СтавкаНДСИсчисляетсяНалоговымАгентом() Тогда
			НДСИсчисляетсяНалоговымАгентом = Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеОбъекта = Новый Структура;
	ДанныеОбъекта.Вставить("Шапка", Шапка);
	ДанныеОбъекта.Вставить("Товары", Товары);
	ДанныеОбъекта.Вставить("НДСИсчисляетсяНалоговымАгентом", НДСИсчисляетсяНалоговымАгентом);
	
	Возврат ДанныеОбъекта;
	
КонецФункции
// BSLLS:CognitiveComplexity-on

Функция СтруктураЗаполненияЗаявкиНаРасходованиеДС(ДанныеЭД, ДополнительныеПараметры)
	
	ПустаяСсылкаНаДокумент = Документы.ЗаявкаНаРасходованиеДенежныхСредств.ПустаяСсылка();
	
	ШапкаЭД = ДанныеЭД.Шапка;
	
	Шапка = Новый Структура;
	
	Организация = СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель);
	
	Шапка.Вставить("Организация", Организация);
	Шапка.Вставить("ХозяйственнаяОперация", Перечисления.ХозяйственныеОперации.ОплатаПоставщику);
	Шапка.Вставить("СуммаДокумента", ДанныеЭД.Итоги.СуммаСНДС);
	Шапка.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("ФормаОплатыНаличная", Ложь);
	Шапка.Вставить("ФормаОплатыБезналичная", Истина);
	Шапка.Вставить("ФормаОплатыПлатежнаяКарта", Ложь);
	
	Контрагент = СсылкаНаУчастникаСделки(ШапкаЭД.Продавец);
	Получатель = СсылкаНаУчастникаСделки(ШапкаЭД.Получатель);
	Партнер = ПартнерПоКонтрагенту(Контрагент);
	
	НомерБанковскогоСчета = ШапкаЭД.Продавец.НомерСчета;
	НомерСчетаОрганизации = ШапкаЭД.Покупатель.НомерСчета;
	НомерСчетаПолучателя = ШапкаЭД.Получатель.НомерСчета;
	
	БанковскийСчетКонтрагента = БанковскийСчетКонтрагентаПоНомеру(Контрагент, НомерБанковскогоСчета);
	БанковскийСчетОрганизации = БанковскийСчетОрганизацииПоНомеру(Организация, НомерСчетаОрганизации);
	БанковскийСчетПолучателя = БанковскийСчетОрганизацииПоНомеру(Получатель, НомерСчетаПолучателя);
	
	Шапка.Вставить("БанковскийСчетКонтрагента", БанковскийСчетКонтрагента);
	Шапка.Вставить("БанковскийСчет", БанковскийСчетОрганизации);
	
	Если ШапкаЭД.Свойство("НазначениеПлатежа") Тогда
		Шапка.Вставить("НазначениеПлатежа", ШапкаЭД.НазначениеПлатежа);
	КонецЕсли;
	
	Если ШапкаЭД.Свойство("ЖелательнаяДатаПлатежа") Тогда
		Шапка.Вставить("ЖелательнаяДатаПлатежа", ШапкаЭД.ЖелательнаяДатаПлатежа);
		Шапка.Вставить("ДатаПлатежа", ШапкаЭД.ЖелательнаяДатаПлатежа);
	КонецЕсли;
	
	Шапка.Вставить("Контрагент", Контрагент);
	Шапка.Вставить("Партнер", Партнер);
	Шапка.Вставить("ОрганизацияПолучатель", Получатель);
	Шапка.Вставить("БанковскийСчетПолучатель", БанковскийСчетПолучателя);
	Шапка.Вставить("ФормаОплатыЗаявки", Перечисления.ФормыОплаты.Безналичная);
	
	Если ШапкаЭД.Свойство("КурсВалюты") Тогда
		Шапка.Вставить("КурсКонвертации", ШапкаЭД.КурсВалюты);
	КонецЕсли;
	
	Если ШапкаЭД.Свойство("ИдентификаторПлатежа") Тогда
		Шапка.Вставить("ИдентификаторПлатежа", ШапкаЭД.ИдентификаторПлатежа);
	КонецЕсли;
	
	Если ЭтоРедакция11_5() Тогда
		Шапка.Вставить("ВариантОплаты", Перечисления.ВариантыКонтроляОплатыПоставщику.ПредоплатаДоПоступления);
	КонецЕсли;
	
	Шапка.Вставить("ПеречислениеВБюджет", Ложь);
	Шапка.Вставить("ВидПеречисленияВБюджет", Перечисления.ВидыПеречисленийВБюджет.ИнойПлатеж);
	Шапка.Вставить("НДФЛПоВедомостям", Ложь);
	
	// BSLLS:UsingFindElementByString-off
	Шапка.Вставить("ПриоритетОплаты", Справочники.Приоритеты.НайтиПоНаименованию("Средний"));
	// BSLLS:UsingFindElementByString-on
	
	Шапка.Вставить("ИННПлательщика", ШапкаЭД.Получатель.ИНН);
	Шапка.Вставить("КПППлательщика", ШапкаЭД.Получатель.КПП);
	Шапка.Вставить("СписокФизЛиц", Ложь);
	Шапка.Вставить("ОтражатьКомиссию", Ложь);
	Шапка.Вставить("Закрыта", Ложь);
	Шапка.Вставить("ПеречислениеСотрудникуЧерезБанк", Ложь);
	Шапка.Вставить("ПлатежСКонвертацией", Ложь);
	Шапка.Вставить("СписокКонтрагентов", Ложь);
	Шапка.Вставить("ОперацияССамозанятым", Ложь);
	
	РасшифровкаПлатежа = ПустаяСсылкаНаДокумент.РасшифровкаПлатежа.ВыгрузитьКолонки();
	
	НДСИсчисляетсяНалоговымАгентом = Ложь;
	
	Для Каждого СтрокаТовара Из ДанныеЭД.ТоварнаяЧасть Цикл
		СтрокаРасшифровки = РасшифровкаПлатежа.Добавить();
		СтрокаРасшифровки.Партнер = Партнер;
		СтрокаРасшифровки.Сумма = СтрокаТовара.СуммаСНДС;
		СтрокаРасшифровки.ВалютаВзаиморасчетов = ШапкаЭД.Валюта.Ссылка;
		СтрокаРасшифровки.СуммаВзаиморасчетов = СтрокаТовара.СуммаСНДС;
		СтрокаРасшифровки.СтавкаНДС = ЗначениеСтавкиНДС(СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС);
		СтрокаРасшифровки.СуммаНДС = СтрокаТовара.СуммаНДС;
		СтрокаРасшифровки.Организация = Организация;
		
		Если ЭтоРедакция11_5() Тогда
			СтрокаРасшифровки.Контрагент = Контрагент;
		КонецЕсли;
		
		Если СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС = СтавкаНДСИсчисляетсяНалоговымАгентом() Тогда
			НДСИсчисляетсяНалоговымАгентом = Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеОбъекта = Новый Структура;
	ДанныеОбъекта.Вставить("Шапка", Шапка);
	ДанныеОбъекта.Вставить("РасшифровкаПлатежа", РасшифровкаПлатежа);
	ДанныеОбъекта.Вставить("НДСИсчисляетсяНалоговымАгентом", НДСИсчисляетсяНалоговымАгентом);
	
	Возврат ДанныеОбъекта;
	
КонецФункции

Функция СтруктураЗаполненияСписанияБезналичныхДС(ДанныеЭД, ДополнительныеПараметры)
	
	ПустаяСсылкаНаДокумент = Документы.ЗаявкаНаРасходованиеДенежныхСредств.ПустаяСсылка();
	
	ШапкаЭД = ДанныеЭД.Шапка;
	
	Шапка = Новый Структура;
	
	Организация = СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель);
	Контрагент = СсылкаНаУчастникаСделки(ШапкаЭД.Продавец);
	Получатель = СсылкаНаУчастникаСделки(ШапкаЭД.Получатель);
	Партнер = ПартнерПоКонтрагенту(Контрагент);
	
	НомерБанковскогоСчета = ШапкаЭД.Продавец.НомерСчета;
	НомерСчетаОрганизации = ШапкаЭД.Покупатель.НомерСчета;
	НомерСчетаПолучателя = ШапкаЭД.Получатель.НомерСчета;
	
	БанковскийСчетКонтрагента = БанковскийСчетКонтрагентаПоНомеру(Контрагент, НомерБанковскогоСчета);
	БанковскийСчетОрганизации = БанковскийСчетОрганизацииПоНомеру(Организация, НомерСчетаОрганизации);
	БанковскийСчетПолучателя = БанковскийСчетОрганизацииПоНомеру(Получатель, НомерСчетаПолучателя);
	
	Шапка.Вставить("Организация", Организация);
	Шапка.Вставить("ДатаВходящегоДокумента", ШапкаЭД.Дата);
	Шапка.Вставить("Контрагент", Контрагент);
	Шапка.Вставить("Партнер", Партнер);
	
	Если ШапкаЭД.Свойство("НазначениеПлатежа") Тогда
		Шапка.Вставить("НазначениеПлатежа", ШапкаЭД.НазначениеПлатежа);
	КонецЕсли;
	
	Шапка.Вставить("ХозяйственнаяОперация", Перечисления.ХозяйственныеОперации.ОплатаПоставщику);
	Шапка.Вставить("НомерВходящегоДокумента", ШапкаЭД.Номер);
	Шапка.Вставить("БанковскийСчетПолучатель", БанковскийСчетПолучателя);
	Шапка.Вставить("СуммаДокумента", ДанныеЭД.Итоги.СуммаСНДС);
	Шапка.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("БанковскийСчетКонтрагента", БанковскийСчетКонтрагента);
	Шапка.Вставить("БанковскийСчет", БанковскийСчетОрганизации);
	Шапка.Вставить("ПеречислениеВБюджет", Ложь);
	Шапка.Вставить("НеКонтролироватьЗаполнениеЗаявки", Ложь);
	Шапка.Вставить("ТипПлатежногоДокумента", Перечисления.ТипыПлатежныхДокументов.ПлатежноеПоручение);
	
	Если ШапкаЭД.Свойство("КурсВалюты") Тогда
		Шапка.Вставить("КурсКонвертации", ШапкаЭД.КурсВалюты);
	КонецЕсли;
	
	Шапка.Вставить("ОплатаПоЗаявкам", Ложь);
	
	Если ШапкаЭД.Свойство("ИдентификаторПлатежа") Тогда
		Шапка.Вставить("ИдентификаторПлатежа", ШапкаЭД.ИдентификаторПлатежа);
	КонецЕсли;
	
	Шапка.Вставить("ПроводкиПоРаботникам", Ложь);
	Шапка.Вставить("ПроведеноБанком", Ложь);
	Шапка.Вставить("НДФЛПоВедомостям", Ложь);
	Шапка.Вставить("ИННПлательщика", ШапкаЭД.Получатель.ИНН);
	Шапка.Вставить("КПППлательщика", ШапкаЭД.Получатель.КПП);
	Шапка.Вставить("ОтражатьКомиссию", Ложь);
	Шапка.Вставить("Исправление", Ложь);
	Шапка.Вставить("ПеречислениеСотрудникуЧерезБанк", Ложь);
	Шапка.Вставить("ПлатежСКонвертацией", Ложь);
	Шапка.Вставить("СписокКонтрагентов", Ложь);
	Шапка.Вставить("ОперацияССамозанятым", Ложь);
	
	РасшифровкаПлатежа = ПустаяСсылкаНаДокумент.РасшифровкаПлатежа.ВыгрузитьКолонки();
	
	НДСИсчисляетсяНалоговымАгентом = Ложь;
	
	Для Каждого СтрокаТовара Из ДанныеЭД.ТоварнаяЧасть Цикл
		СтрокаРасшифровки = РасшифровкаПлатежа.Добавить();
		СтрокаРасшифровки.Партнер = Партнер;
		СтрокаРасшифровки.Сумма = СтрокаТовара.СуммаСНДС;
		СтрокаРасшифровки.ВалютаВзаиморасчетов = ШапкаЭД.Валюта.Ссылка;
		СтрокаРасшифровки.СуммаВзаиморасчетов = СтрокаТовара.СуммаСНДС;
		СтрокаРасшифровки.СтавкаНДС = ЗначениеСтавкиНДС(СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС);
		СтрокаРасшифровки.СуммаНДС = СтрокаТовара.СуммаНДС;
		СтрокаРасшифровки.Организация = Организация;
		
		Если ЭтоРедакция11_5() Тогда
			СтрокаРасшифровки.Контрагент = Контрагент;
		КонецЕсли;
		
		Если СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС = СтавкаНДСИсчисляетсяНалоговымАгентом() Тогда
			НДСИсчисляетсяНалоговымАгентом = Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеОбъекта = Новый Структура;
	ДанныеОбъекта.Вставить("Шапка", Шапка);
	ДанныеОбъекта.Вставить("РасшифровкаПлатежа", РасшифровкаПлатежа);
	ДанныеОбъекта.Вставить("НДСИсчисляетсяНалоговымАгентом", НДСИсчисляетсяНалоговымАгентом);
	
	Возврат ДанныеОбъекта;
	
КонецФункции

Функция СтруктураЗаполненияРасходногоКассовогоОрдера(ДанныеЭД, ДополнительныеПараметры)
	
	ПустаяСсылкаНаДокумент = Документы.РасходныйКассовыйОрдер.ПустаяСсылка();
	
	ШапкаЭД = ДанныеЭД.Шапка;
	
	Шапка = Новый Структура;
	
	Организация = СсылкаНаУчастникаСделки(ШапкаЭД.Покупатель);
	Контрагент = СсылкаНаУчастникаСделки(ШапкаЭД.Продавец);
	Партнер = ПартнерПоКонтрагенту(Контрагент);
	
	НомерСчетаОрганизации = ШапкаЭД.Покупатель.НомерСчета;
	БанковскийСчетОрганизации = БанковскийСчетОрганизацииПоНомеру(Организация, НомерСчетаОрганизации);
	
	Шапка.Вставить("Организация", Организация);
	Шапка.Вставить("Касса", Справочники.Кассы.ПолучитьКассуПоУмолчанию(Организация));
	Шапка.Вставить("СуммаДокумента", ДанныеЭД.Итоги.СуммаСНДС);
	Шапка.Вставить("ХозяйственнаяОперация", Перечисления.ХозяйственныеОперации.ОплатаПоставщику);
	Шапка.Вставить("Контрагент", Контрагент);
	Шапка.Вставить("БанковскийСчет", БанковскийСчетОрганизации);
	Шапка.Вставить("Валюта", ШапкаЭД.Валюта.Ссылка);
	Шапка.Вставить("НеКонтролироватьЗаполнениеЗаявки", Ложь);
	
	Если ШапкаЭД.Свойство("КурсВалюты") Тогда
		Шапка.Вставить("КурсКонвертации", ШапкаЭД.КурсВалюты);
	КонецЕсли;
	
	Шапка.Вставить("ОплатаПоЗаявкам", Ложь);
	Шапка.Вставить("ПроводкиПоРаботникам", Ложь);
	Шапка.Вставить("Партнер", Партнер);
	Шапка.Вставить("Исправление", Ложь);
	
	РасшифровкаПлатежа = ПустаяСсылкаНаДокумент.РасшифровкаПлатежа.ВыгрузитьКолонки();
	
	НДСИсчисляетсяНалоговымАгентом = Ложь;
	
	Для Каждого СтрокаТовара Из ДанныеЭД.ТоварнаяЧасть Цикл
		СтрокаРасшифровки = РасшифровкаПлатежа.Добавить();
		СтрокаРасшифровки.Партнер = Партнер;
		СтрокаРасшифровки.Сумма = СтрокаТовара.СуммаСНДС;
		СтрокаРасшифровки.ВалютаВзаиморасчетов = ШапкаЭД.Валюта.Ссылка;
		СтрокаРасшифровки.СуммаВзаиморасчетов = СтрокаТовара.СуммаСНДС;
		СтрокаРасшифровки.СтавкаНДС = ЗначениеСтавкиНДС(СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС);
		СтрокаРасшифровки.СуммаНДС = СтрокаТовара.СуммаНДС;
		СтрокаРасшифровки.Организация = Организация;
		
		Если СтрокаТовара.НоменклатураКонтрагента.СтавкаНДС = СтавкаНДСИсчисляетсяНалоговымАгентом() Тогда
			НДСИсчисляетсяНалоговымАгентом = Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	ДанныеОбъекта = Новый Структура;
	ДанныеОбъекта.Вставить("Шапка", Шапка);
	ДанныеОбъекта.Вставить("РасшифровкаПлатежа", РасшифровкаПлатежа);
	ДанныеОбъекта.Вставить("НДСИсчисляетсяНалоговымАгентом", НДСИсчисляетсяНалоговымАгентом);
	
	Возврат ДанныеОбъекта;
	
КонецФункции

Функция БанковскийСчетКонтрагентаПоНомеру(Контрагент, НомерБанковскогоСчета)
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	БанковскиеСчетаКонтрагентов.Ссылка КАК БанковскийСчет
	|ИЗ
	|	Справочник.БанковскиеСчетаКонтрагентов КАК БанковскиеСчетаКонтрагентов
	|ГДЕ
	|	БанковскиеСчетаКонтрагентов.НомерСчета = &НомерСчета
	|	И БанковскиеСчетаКонтрагентов.Владелец = &Контрагент
	|	И НЕ БанковскиеСчетаКонтрагентов.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("Контрагент", Контрагент);
	Запрос.УстановитьПараметр("НомерСчета", НомерБанковскогоСчета);
	
	РезультатЗапроса = Запрос.Выполнить().Выгрузить();
	
	Если ЗначениеЗаполнено(РезультатЗапроса) Тогда
		Результат = РезультатЗапроса[0].БанковскийСчет;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция БанковскийСчетОрганизацииПоНомеру(Организация, НомерСчета)
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	БанковскиеСчетаОрганизаций.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.БанковскиеСчетаОрганизаций КАК БанковскиеСчетаОрганизаций
	|ГДЕ
	|	БанковскиеСчетаОрганизаций.Владелец = &Организация
	|	И БанковскиеСчетаОрганизаций.НомерСчета = &НомерСчета
	|	И НЕ БанковскиеСчетаОрганизаций.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("НомерСчета", НомерСчета);
	
	РезультатЗапроса = Запрос.Выполнить().Выгрузить();
	
	Если ЗначениеЗаполнено(РезультатЗапроса) Тогда
		Результат = РезультатЗапроса[0].БанковскийСчет;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НомерДокументаБезПрефикса(НомерДокумента)

	УдалитьПрефиксИнформационнойБазы	= Истина;
	УдалитьПользовательскийПрефикс		= Истина;
	
	НомерДокументаБезПрефикса = ПрефиксацияОбъектовКлиентСервер.НомерНаПечать(НомерДокумента, УдалитьПрефиксИнформационнойБазы, УдалитьПользовательскийПрефикс);
	
	Возврат НомерДокументаБезПрефикса;

КонецФункции

Процедура ПроверитьВозможностьЗаполненияСчетаФактурыНаАванс(ДанныеЭД)
	
	Если ДанныеЭД.ЭтоАванс Тогда
		Возврат;
	КонецЕсли;
	
	ИдентификаторЭД = ДанныеЭД.ЭлектронныйДокумент.Идентификатор;
	СоставнойИдентификатор	 = ИдентификаторЭД.СоставнойИдентификатор;
	ИдентификаторОрганизации = ИдентификаторЭД.ИдентификаторОрганизации;
	
	Ошибка = НСтр("ru = 'Электронный документ не является счетом-фактурой на аванс!'");
	
	КомментарийЖР = НСтр("ru = 'Ошибка при отражении в учете документа %1 (boxId = %2):'")
		+ Символы.ПС
		+ Ошибка;
	
	КомментарийЖР = СтрШаблон(КомментарийЖР, СоставнойИдентификатор, ИдентификаторОрганизации);
	
	ЗаписьЖурналаРегистрации(ИмяСобытияЖурналаЗагрузка()
		, УровеньЖурналаРегистрации.Ошибка
		, Метаданные.Документы.СчетФактураПолученныйАванс
		,
		, КомментарийЖР);
	
	ТекстСообщения = Ошибка
		+ Символы.ПС
		+ НСтр("ru = 'Попробуйте выбрать другой вид документа.'");
	
	ВызватьИсключение ТекстСообщения;
	
КонецПроцедуры

Процедура ПересчитатьЦенуСуммуПриУстановкеФлагаЦенаВключаетНДС(ДокументОбъект, ИмяТЧ)
	
	Если Не ДокументОбъект.ЦенаВключаетНДС Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураПересчетаСуммы = ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект);
	
	СтруктураДействий = Новый Структура;
	СтруктураДействий.Вставить("ПересчитатьЦенуСНДС", СтруктураПересчетаСуммы);
	
	Для Каждого Строка Из ДокументОбъект[ИмяТЧ] Цикл 
		
		// НАЧАЛО Николаев Павел 30.06.2025 Выполнение задач №000054247
		//ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Строка, СтруктураДействий, Неопределено);
		ПакетнаяОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Строка, СтруктураДействий, Неопределено);
		// НАЧАЛО Николаев Павел 30.06.2025 Выполнение задач №000054247
		
		Строка.Сумма = Строка.СуммаСНДС;
		
	КонецЦикла;
	
КонецПроцедуры

// Конструктор дополнительных параметров заполнения счетов-фактур.
// 
// Возвращаемое значение:
//  Структура - содержит поля:
//   * ДокументОснование - Неопределено, ДокументСсылка, Массив - документ-основание (список оснований) счета-фактуры.
//
Функция ПараметрыЗаполненияСчетаФактуры()
	
	Результат = Новый Структура;
	Результат.Вставить("ДокументОснование", Неопределено);
	
	Возврат Результат;
	
КонецФункции

// Конструктор дополнительных параметров заполнения документа учета.
// 
// Возвращаемое значение:
//  Структура - содержит поля:
//   * ЦенаВключаетНДС - Булево - Истина, если цена включает НДС.
//
Функция ОбщиеПараметрыЗаполненияДокументовУчета()
	
	Результат = Новый Структура;
	Результат.Вставить("ЦенаВключаетНДС", Ложь);
	
	Возврат Результат;
	
КонецФункции

Процедура ПерезаполнениеЗначенийРеквизитовШапки(ТекущийОбъект, ДанныеЗаполнения)
	
	МетаданныеОбъекта = ТекущийОбъект.Метаданные();
	
	Для Каждого Строка Из ДанныеЗаполнения Цикл
		
		Если ЗначениеЗаполнено(Строка.Ключ) 
			 И МетаданныеОбъекта.Реквизиты.Найти(Строка.Ключ) <> Неопределено
			 И ТекущийОбъект[Строка.Ключ] <> Строка.Значение Тогда
			ТекущийОбъект[Строка.Ключ] = Строка.Значение;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьСоглашениеПоСтатистике(Объект)
	
	Если ЭтоРедакция11_5() Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполняемыеРеквизиты = Новый Структура("Соглашение");
	
	РеквизитыДляОтбора = Новый Структура;
	РеквизитыДляОтбора.Вставить("Партнер", Объект.Партнер);
	РеквизитыДляОтбора.Вставить("Валюта", Объект.Валюта);
	РеквизитыДляОтбора.Вставить("ЦенаВключаетНДС", Объект.ЦенаВключаетНДС);
	
	МодульЗаполнениеСвойствПоСтатистикеСервер = ОбщегоНазначения.ОбщийМодуль("ЗаполнениеСвойствПоСтатистикеСервер");
	МодульЗаполнениеСвойствПоСтатистикеСервер.ПолучитьЗначенияСвойств(
			Объект.Ссылка, ЗаполняемыеРеквизиты, РеквизитыДляОтбора);
	
	Если ЗначениеЗаполнено(ЗаполняемыеРеквизиты.Соглашение) Тогда
		Объект.Соглашение = ЗаполняемыеРеквизиты.Соглашение;
	КонецЕсли;

КонецПроцедуры

Процедура ЗаполнитьДоговорСПоставщиком(Объект)
	
	Если Не ИспользоватьДоговорыСПоставщиками()
		Или ЗначениеЗаполнено(Объект.Договор) Тогда
		Возврат;
	КонецЕсли;
	
	ХозяйственнаяОперация = ПредопределенноеЗначение("Перечисление.ХозяйственныеОперации.ЗакупкаУПоставщика");
	
	ДопПараметры = ЗакупкиСервер.ДополнительныеПараметрыОтбораДоговоров();
	
	Если ЭтоПлатежныйДокумент(Объект) Тогда
		ДопПараметры.ВалютаВзаиморасчетов = Объект.Валюта;
	Иначе
		ДопПараметры.ВалютаВзаиморасчетов = Объект.ВалютаВзаиморасчетов;
	КонецЕсли;
	
	ДопПараметры.Налогообложение = Объект.НалогообложениеНДС;
	
	Объект.Договор = ЗакупкиСервер.ПолучитьДоговорПоУмолчанию(Объект, ХозяйственнаяОперация, ДопПараметры);
	
КонецПроцедуры

Функция ЭтоПлатежныйДокумент(Объект)
	
	ТипыПлатежныхДокументов = Новый Массив;
	ТипыПлатежныхДокументов.Добавить(Тип("ДокументСсылка.ЗаказПоставщику"));
	ТипыПлатежныхДокументов.Добавить(Тип("ДокументСсылка.ЗаявкаНаРасходованиеДенежныхСредств"));
	ТипыПлатежныхДокументов.Добавить(Тип("ДокументСсылка.СписаниеБезналичныхДенежныхСредств"));
	ТипыПлатежныхДокументов.Добавить(Тип("ДокументСсылка.РасходныйКассовыйОрдер"));
	
	Возврат (ТипыПлатежныхДокументов.Найти(ТипЗнч(Объект.Ссылка)) <> Неопределено);
	
КонецФункции

Процедура ЗаполнитьДоговорСКлиентом(Объект)
	
	Если Не ИспользоватьДоговорыСКлиентами()
		Или ЗначениеЗаполнено(Объект.Договор) Тогда
		Возврат;
	КонецЕсли;
	
	МетаданныеОбъекта = Объект.Метаданные();
	
	Если ЕстьРеквизитМетаданных(МетаданныеОбъекта, "ВалютаВзаиморасчетов") Тогда
		ВалютаДляОтбора = Объект.ВалютаВзаиморасчетов;
	ИначеЕсли ЕстьРеквизитМетаданных(МетаданныеОбъекта, "Валюта") Тогда
		ВалютаДляОтбора = Объект.Валюта;
	Иначе
		ВалютаДляОтбора = Неопределено;
	КонецЕсли;
	
	ХозяйственнаяОперация = ПредопределенноеЗначение("Перечисление.ХозяйственныеОперации.РеализацияКлиенту");
	
	Объект.Договор = ПродажиСервер.ПолучитьДоговорПоУмолчанию(Объект, ХозяйственнаяОперация, ВалютаДляОтбора);
	
КонецПроцедуры

Процедура ЗаполнитьНоменклатуруПартнера(СтрокаТЧ, НоменклатураКонтрагента, ПараметрыСправочника = Неопределено)
	
	Если Не ЗначениеЗаполнено(ПараметрыСправочника) Тогда
		ПараметрыСправочника = ПараметрыСправочникаНоменклатурыПартнеров();
	КонецЕсли;
	
	ТекстЗапроса =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Т.Ссылка КАК НоменклатураПартнера
	|ИЗ
	|	ПолноеИмяТаблицы КАК Т
	|ГДЕ
	|	НЕ Т.ПометкаУдаления
	|	И Т.Владелец = &Владелец
	|	И Т.Идентификатор = &Идентификатор";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ПолноеИмяТаблицы", ПараметрыСправочника.ПолноеИмя);
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Владелец", НоменклатураКонтрагента.Владелец);
	Запрос.УстановитьПараметр("Идентификатор", НоменклатураКонтрагента.Идентификатор);
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Выборка.Следующий();
	
	НоменклатураПартнераСсылка = Выборка.НоменклатураПартнера;
	
	ДанныеЗаполнения = Новый Структура;
	ДанныеЗаполнения.Вставить("НоменклатураПоставщика", НоменклатураПартнераСсылка); // До 11.4.12 включительно
	ДанныеЗаполнения.Вставить("НоменклатураПартнера", НоменклатураПартнераСсылка); // 11.4.13+
	
	ЗаполнитьЗначенияСвойств(СтрокаТЧ, ДанныеЗаполнения);
	
КонецПроцедуры

// Заполняет сведения о номенклатуре информационной базы в строке табличной части учетного документа.
//
// Параметры:
//  СтрокаТЧ - СтрокаТабличнойЧасти - строка табличной части документа.
//  НоменклатураИБ - Структура - данные о номенклатуре информационной базы (см. НоваяНоменклатураИнформационнойБазы).
//
Процедура ЗаполнитьНоменклатуруИнформационнойБазы(СтрокаТЧ, НоменклатураИБ)
	
	Если Не ЗначениеЗаполнено(НоменклатураИБ.Номенклатура) Тогда
		Возврат;
	КонецЕсли;
	
	СтрокаТЧ.Номенклатура	 = НоменклатураИБ.Номенклатура;
	СтрокаТЧ.Характеристика	 = НоменклатураИБ.Характеристика;
	СтрокаТЧ.Упаковка		 = НоменклатураИБ.Упаковка;
	
	ЕдиницаХранения = ЕдиницаХраненияНоменклатуры(НоменклатураИБ.Номенклатура);
	Если СтрокаТЧ.Упаковка = ЕдиницаХранения Тогда
		СтрокаТЧ.Упаковка = Справочники.УпаковкиЕдиницыИзмерения.ПустаяСсылка();
	КонецЕсли;
	
КонецПроцедуры

Функция ЕдиницаХраненияНоменклатуры(Номенклатура)
	
	Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Номенклатура, "ЕдиницаИзмерения");
	Возврат Результат;
	
КонецФункции

// Инициализирует и возвращает структуру дополнительных параметров отбора соглашений.
// 
// Возвращаемое значение:
//  Структура - содержит свойства:
//   * ТолькоДействующее - Булево - Истина, если подбирать только действующие соглашения
//   * УчитыватьГруппыСкладов - Булево
//   * ИсключитьГруппыСкладовДоступныеВЗаказах - Булево
//   * ХозяйственныеОперации - ПеречислениеСсылка.ХозяйственныеОперации, Массив
//   * ВыбранноеСоглашение
//
Функция ДополнительныеПараметрыОтбораСоглашений()
	
	Результат = Новый Структура();
	Результат.Вставить("ТолькоДействующее",                       Истина);
	Результат.Вставить("УчитыватьГруппыСкладов",                  Истина);
	Результат.Вставить("ИсключитьГруппыСкладовДоступныеВЗаказах", Истина);
	Результат.Вставить("ХозяйственныеОперации",                   Неопределено);
	Результат.Вставить("ВыбранноеСоглашение",   Справочники.СоглашенияСПоставщиками.ПустаяСсылка());
	
	Возврат Результат;
	
КонецФункции

// Получает соглашение с поставщиком по умолчанию.
//
// Параметры:
//  Партнер - СправочникСсылка.Партнеры - поставщик
// 
// Возвращаемое значение:
//  СправочникСсылка.СоглашенияСПоставщиками, Неопределено - соглашение по умолчанию.
//
Функция СоглашениеСПоставщикомПоУмолчанию(Партнер)
	
	Результат = Неопределено;
	
	ДопПараметры = ДополнительныеПараметрыОтбораСоглашений();
	ДопПараметры.УчитыватьГруппыСкладов = Истина;
	ДопПараметры.ИсключитьГруппыСкладовДоступныеВЗаказах = Истина;
	
	УсловияЗакупок = ЗакупкиСервер.ПолучитьУсловияЗакупокПоУмолчанию(Партнер, ДопПараметры);
	
	Если ЗначениеЗаполнено(УсловияЗакупок) Тогда
		Результат = УсловияЗакупок.Соглашение;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьСоглашениеСПоставщиком(Объект)
	
	Если Не ИспользоватьСоглашенияСПоставщиками()
		Или ЗначениеЗаполнено(Объект.Соглашение) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьСоглашениеПоСтатистике(Объект);
	
	Если Не ЗначениеЗаполнено(Объект.Соглашение) Тогда
		Объект.Соглашение = СоглашениеСПоставщикомПоУмолчанию(Объект.Партнер);
	КонецЕсли;
	
КонецПроцедуры

// Получает соглашение с клиентом по умолчанию.
//
// Параметры:
//  Партнер - СправочникСсылка.Партнеры - клиент
// 
// Возвращаемое значение:
//  СправочникСсылка.СоглашенияСКлиентами, Неопределено - соглашение по умолчанию.
//
Функция СоглашениеСКлиентомПоУмолчанию(Партнер)
	
	Результат = Неопределено;
	
	ДопПараметры = ДополнительныеПараметрыОтбораСоглашений();
	ДопПараметры.УчитыватьГруппыСкладов = Истина;
	
	УсловияПродаж = ПродажиСервер.ПолучитьУсловияПродажПоУмолчанию(Партнер, ДопПараметры);
	
	Если ЗначениеЗаполнено(УсловияПродаж) Тогда
		Результат = УсловияПродаж.Соглашение;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьСоглашениеСКлиентом(Объект)
	
	Если Не ИспользоватьСоглашенияСКлиентами()
		Или ЗначениеЗаполнено(Объект.Соглашение) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьСоглашениеПоСтатистике(Объект);
	
	Если Не ЗначениеЗаполнено(Объект.Соглашение) Тогда
		Объект.Соглашение = СоглашениеСКлиентомПоУмолчанию(Объект.Партнер);
	КонецЕсли;
	
КонецПроцедуры

// Возвращает структуру, содержащую поля значений, используемых для пересчета сумм НДС в строках табличной части 
// документа.
//
// Параметры:
//	Объект - ДанныеФормыСтруктура - данные документа.
//
// Возвращаемое значение:
//	Структура - структура, используемая для пересчета сумм НДС в табличной части документа:
//		* ЦенаВключаетНДС - Булево - Истина - НДС включен в цену товара.
//		* НалогообложениеНДС - ПеречислениеСсылка.ТипыНалогообложенияНДС - налогообложение документа.
//
Функция ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект)
	
	Если ДокументОбъект = Неопределено Тогда
		Результат = ПараметрыПересчетаСуммПоУмолчанию();
		Возврат Результат;
	КонецЕсли;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если СравнитьВерсии(ВерсияУТ, "11.4.6.1") >= 0 Тогда // Для версии УТ 11.4.6 и КА/ERP 2.4.6
		// НАЧАЛО Николаев Павел 30.06.2025 Выполнение задач №000054247
		//Результат = ОбработкаТабличнойЧастиКлиентСервер.ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект);
		Результат = ПакетнаяОбработкаТабличнойЧастиКлиентСервер.ПараметрыПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект);
		// КОНЕЦ Николаев Павел 30.06.2025 Выполнение задач №000054247
	// НАЧАЛО Николаев Павел 30.06.2025 Выполнение задач №000054247
	//Иначе // Для более старших версий         
	//	Результат = ОбработкаТабличнойЧастиКлиентСервер.ПолучитьСтруктуруПересчетаСуммыНДСВСтрокеТЧ(ДокументОбъект);
	// КОНЕЦ Николаев Павел 30.06.2025 Выполнение задач №000054247
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПараметрыПересчетаСуммПоУмолчанию()
	
	Результат = Новый Структура;
	Результат.Вставить("ЦенаВключаетНДС", Ложь);
	Результат.Вставить("НалогообложениеНДС", Перечисления.ТипыНалогообложенияНДС.ПродажаОблагаетсяНДС);
	
	Возврат Результат;
	
КонецФункции

// Создает элементы справочника НомераГТД
//
// Параметры:
//  ДанныеГТД - Массив - содержит структуры с ключами:
//    * СтранаПроисхождения - Структура - см. НовыйКлючевыеСвойстваСтранаМира;
//       ** Ссылка - СправочникСсылка.СтраныМира
//       ** Код - Строка - код по ОКСМ
//    * НомерГТД - Структура - см. НовыйКлючевыеСвойстваГТД:
//       ** Ссылка - СправочникСсылка.НомераГТД
//       ** РегистрационныйНомер - Строка
//
Процедура ПолучитьНомераГТД(ДанныеГТД)
	
	Для Каждого Элемент Из ДанныеГТД Цикл 
		
		ЭтоСтранаЕАЭС = ОбработкаОбъект.СтранаЧленТС(Элемент.СтранаПроисхождения.Код);
		
		Если ЗначениеЗаполнено(Элемент.НомерГТД.Ссылка)
			ИЛИ ЭтоСтранаЕАЭС Тогда
			
			Продолжить;
			
		КонецЕсли;
		
		НомерТаможеннойДекларации = Элемент.НомерГТД.РегистрационныйНомер;
		СтранаПроисхождения = Элемент.СтранаПроисхождения.Ссылка;
		
		Элемент.НомерГТД.Ссылка = НайтиСоздатьНомерГТД(НомерТаможеннойДекларации, СтранаПроисхождения);
		
	КонецЦикла;
	
КонецПроцедуры

// Получает ссылку на элемент справочника НомераГТД по номеру таможенной декларации.
// Если элемента с заданным номером ТД нет, создает новый.
//
// Параметры:
//  НомерТаможеннойДекларации - Строка - номер таможенной декларации;
//  СтранаПроисхождения - СправочникСсылка.СтраныМира - страна происхождения.
// 
// Возвращаемое значение:
//  СправочникСсылка.НомераГТД
//
Функция НайтиСоздатьНомерГТД(НомерТаможеннойДекларации, СтранаПроисхождения, ЭтоРНПТ = Ложь)
	
	ТекстЗапроса =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	НомераГТД.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.НомераГТД КАК НомераГТД
	|ГДЕ
	|	НомераГТД.Код = &НомерТаможеннойДекларации
	|	И НомераГТД.СтранаПроисхождения = &СтранаПроисхождения
	|	И НЕ НомераГТД.ПометкаУдаления";
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("НомерТаможеннойДекларации", НомерТаможеннойДекларации);
	Запрос.УстановитьПараметр("СтранаПроисхождения", СтранаПроисхождения);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		
		Результат = Выборка.Ссылка;
		
	Иначе
		
		ЗначенияЗаполнения = Новый Структура;
		ЗначенияЗаполнения.Вставить("Код");
		ЗначенияЗаполнения.Вставить("СтранаПроисхождения");
		ЗначенияЗаполнения.Вставить("РегистрационныйНомер");
		ЗначенияЗаполнения.Вставить("СтранаВвозаНеРФ");
		ЗначенияЗаполнения.Вставить("ПорядковыйНомерТовара");
		
		РеквизитыТД = Справочники.НомераГТД.РегистрационныйНомерИСтранаВвоза(НомерТаможеннойДекларации);
		
		ЗаполнитьЗначенияСвойств(ЗначенияЗаполнения, РеквизитыТД);
		ЗначенияЗаполнения.Код 					= НомерТаможеннойДекларации;
		ЗначенияЗаполнения.СтранаПроисхождения 	= СтранаПроисхождения;
		Если ЭтоРНПТ Тогда
			ЗначенияЗаполнения.РегистрационныйНомер = НомерТаможеннойДекларации;
		КонецЕсли;
		
		НомерГТД = Справочники.НомераГТД.СоздатьЭлемент();
		
		ЗаполняемыеСвойства = "Код, СтранаПроисхождения, РегистрационныйНомер, СтранаВвозаНеРФ, ПорядковыйНомерТовара";
		
		ЗаполнитьЗначенияСвойств(НомерГТД, ЗначенияЗаполнения, ЗаполняемыеСвойства);
		
		Если ИспользуетсяТипНомераТД() Тогда
			
			НомерГТД.ТипНомераГТД = ?(ЭтоРНПТ, Перечисления.ТипыНомеровГТД.НомерРНПТ, Перечисления.ТипыНомеровГТД.НомерГТД);
			
		Иначе
			
			НомерГТД.РНПТ = ЭтоРНПТ;
			
		КонецЕсли;
		
		Попытка
			
			НомерГТД.Записать();
			Результат = НомерГТД.Ссылка;
			
		Исключение
			
			Ошибка = ИнформацияОбОшибке();
			
			ЗаписьЖурналаРегистрации(ИмяСобытияЖурналаЗагрузка()
				, УровеньЖурналаРегистрации.Ошибка
				, НомерГТД.Метаданные()
				,
				, ПодробноеПредставлениеОшибки(Ошибка));
			
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НомерГТДПоДаннымЭлектронногоДокумента(ДанныеГТД)
	
	Результат = Неопределено;
	
	Если НЕ ЗначениеЗаполнено(ДанныеГТД) Тогда
		Возврат Результат;
	КонецЕсли;
	
	ПолучитьНомераГТД(ДанныеГТД);
	
	Для Каждого Элемент Из ДанныеГТД Цикл
		
		Если ЗначениеЗаполнено(Элемент.НомерГТД.Ссылка) Тогда
			
			Результат = Элемент.НомерГТД.Ссылка;
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ИмяСобытияЖурналаЗагрузка()
	
	Результат = НСтр("ru = 'Диадок'; en = 'Diadoc'");
	Возврат Результат;
	
КонецФункции

Функция СсылкаНаУчастникаСделки(СвойстваИлиМассив)
	
	Результат = МетодСервера(
		"Модуль_ИнтеграцияУниверсальный",
		"СсылкаНаУчастникаЭДО",
		СвойстваИлиМассив
	);
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьПорядокРасчетовИзДоговора(ДокументОбъект)
	
	Если Не ЗначениеЗаполнено(ДокументОбъект.Договор) Тогда 
		Возврат;
	КонецЕсли;
	
	ПорядокРасчетов = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументОбъект.Договор, "ПорядокРасчетов");
	
	Если ЗначениеЗаполнено(ПорядокРасчетов) Тогда 
		ДокументОбъект.ПорядокРасчетов = ПорядокРасчетов;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ПараметрыИНастройкиИнформационнойБазы

Функция ПараметрыСправочникаНоменклатурыПартнеров()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если ЭтоРедакция11_5() Тогда
		
		ИмяТаблицы = "НоменклатураКонтрагентов";
		Если СравнитьВерсии(ВерсияУТ, "11.5.6") < 0 Тогда
			ИмяТаблицы = "НоменклатураПоставщиков";
		КонецЕсли;
		
	Иначе
		
		ИмяТаблицы = "НоменклатураКонтрагентов";
		Если СравнитьВерсии(ВерсияУТ, "11.4.13") < 0 Тогда
			ИмяТаблицы = "НоменклатураПоставщиков";
		КонецЕсли;
		
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("Имя", ИмяТаблицы);
	Результат.Вставить("ПолноеИмя", "Справочник." + ИмяТаблицы);
	
	Возврат Результат;
	
КонецФункции
	
Функция МенеджерИнтеграции_ОписанияПодсистемКонфигурации()
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ОписанияПодсистемКонфигурации");
	Возврат Результат;
	
КонецФункции

Функция ВерсияТорговойПодсистемы()
	
	ИмяПодсистемы = "УправлениеТорговлей";
	Если СтандартныеПодсистемыСервер.ЭтоБазоваяВерсияКонфигурации() Тогда 
		ИмяПодсистемы = ИмяПодсистемы + "Базовая";
	КонецЕсли;
	
	ПодсистемыКонфигурации = МенеджерИнтеграции_ОписанияПодсистемКонфигурации();
	ОписаниеПодсистемы = ПодсистемыКонфигурации.Получить(ИмяПодсистемы);
	
	Результат = ОписаниеПодсистемы.Версия;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоРедакция11_5()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Результат = (СравнитьВерсии(ВерсияУТ, "11.5") >= 0);
	
	Возврат Результат;
	
КонецФункции

Функция ОбщийМодульОбработкаТабличнойЧастиСервер()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	ИспользуетсяПакетнаяОбработкаТЧ = (СравнитьВерсии(ВерсияУТ, "11.5.22") >= 0);
	
	ИмяМодуля = "ОбработкаТабличнойЧастиСервер";
	Если ИспользуетсяПакетнаяОбработкаТЧ Тогда
		ИмяМодуля = "ПакетнаяОбработкаТабличнойЧастиСервер";
	КонецЕсли;
	
	Результат = ОбщегоНазначения.ОбщийМодуль(ИмяМодуля);
	
	Возврат Результат;
	
КонецФункции

Функция ОбщийМодульОбработкаТабличнойЧастиКлиентСервер()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	ИспользуетсяПакетнаяОбработкаТЧ = (СравнитьВерсии(ВерсияУТ, "11.5.22") >= 0);
	
	ИмяМодуля = "ОбработкаТабличнойЧастиКлиентСервер";
	Если ИспользуетсяПакетнаяОбработкаТЧ Тогда
		ИмяМодуля = "ПакетнаяОбработкаТабличнойЧастиКлиентСервер";
	КонецЕсли;
	
	Результат = ОбщегоНазначения.ОбщийМодуль(ИмяМодуля);
	
	Возврат Результат;
	
КонецФункции

Функция ВерсияБиблиотекиЭлектронныхДокументов()
	
	Результат = ОбновлениеИнформационнойБазыБЭД.ВерсияБиблиотеки();
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяБЭД19()
	
	Результат = Ложь;
	
	ВерсияБЭД = ВерсияБиблиотекиЭлектронныхДокументов();
	
	Если СравнитьВерсии(ВерсияБЭД, "1.9") >= 0 Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяКорректировкаСчетаФактурыНаАванс()
	
	Результат = Ложь;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если СравнитьВерсии(ВерсияУТ, "11.4.6") >= 0 Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяОтчетДавальцу()
	
	Результат = Ложь;
	
	Если Метаданные.Документы.Найти("ОтчетДавальцу") <> Неопределено Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяПередачаТоваровХранителю()
	
	Результат = Ложь;
	
	Если Метаданные.Документы.Найти("ПередачаТоваровХранителю") <> Неопределено Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяНоменклатураКонтрагентовБЭД()
	
	Результат = Ложь;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если СравнитьВерсии(ВерсияУТ, "11.4.6") >= 0 Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НоваяАрхитектураВзаиморасчетов()
	
	Результат = Ложь;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Если СравнитьВерсии(ВерсияУТ, "11.4.6") >= 0 Тогда
		Результат = ПолучитьФункциональнуюОпцию("НоваяАрхитектураВзаиморасчетов");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОснованиеСчетаФактурыУказываетсяВТабличнойЧасти()
	
	Результат = Истина;
	
	МетаданныеОбъекта = Метаданные.Документы.СчетФактураВыданный;
	Если МетаданныеОбъекта.ТабличныеЧасти.Найти("ДокументыОснования") = Неопределено Тогда
		Результат = Ложь;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ХозяйственнаяОперацияДокумента(СсылкаНаОбъект)
	
	МетаданныеОбъекта = СсылкаНаОбъект.Метаданные();
	
	ИмяРеквизита = "ХозяйственнаяОперация";
	
	Если МетаданныеОбъекта = Метаданные.Документы.КорректировкаРеализации Тогда
		
		ВерсияУТ = ВерсияТорговойПодсистемы();
		
		Если СравнитьВерсии(ВерсияУТ, "11.3.2") >= 0 Тогда
			ИмяРеквизита = "ВидКорректировки";
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитМетаданных(МетаданныеОбъекта, ИмяРеквизита) Тогда
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СсылкаНаОбъект, ИмяРеквизита);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьСоглашенияСПоставщиками()
	
	Результат = ПолучитьФункциональнуюОпцию("ИспользоватьСоглашенияСПоставщиками");
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьСоглашенияСКлиентами()
	
	Результат = ПолучитьФункциональнуюОпцию("ИспользоватьСоглашенияСКлиентами");
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьДоговорыСПоставщиками()
	
	Результат = ПолучитьФункциональнуюОпцию("ИспользоватьДоговорыСПоставщиками");
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьДоговорыСКлиентами()
	
	Результат = ПолучитьФункциональнуюОпцию("ИспользоватьДоговорыСКлиентами");
	
	Возврат Результат;
	
КонецФункции

Функция ЕстьШтрихкодыУпаковок(ОбъектМетаданных)
	
	Результат = (ОбъектМетаданных.ТабличныеЧасти.Найти("ШтрихкодыУпаковок") <> Неопределено);
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьБазовыеЕдиницыИзмерения()
	
	Результат = Константы.ВыводитьБазовыеЕдиницыИзмерения.Получить();
	Возврат Результат;
	
КонецФункции

#КонецОбласти

Функция МодульЛокализацииОбъекта(ПолноеИмя)
	
	МенеджерОбъекта = Неопределено;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Если СравнитьВерсии(ВерсияУТ, "11.4.6") >= 0 Тогда
		МенеджерОбъекта = ОбщегоНазначенияУТ.ПолучитьМодульЛокализации(ПолноеИмя);
	КонецЕсли;
	
	Если МенеджерОбъекта = Неопределено Тогда
		МенеджерОбъекта = Документы[ПолноеИмя];
	КонецЕсли;
	
	Возврат МенеджерОбъекта;
	
КонецФункции

Процедура ДополнитьНомерЦифровымИндексомОП(ДатаНомер, ЦифровойИндексОП, СсылкаНаОбъект)
	
	Если Не ЭтоСчетФактура(СсылкаНаОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	ПредставлениеНомераХранитсяВИнформационнойБазе = ОбщегоНазначения.ЕстьРеквизитОбъекта("ПредставлениеНомера", СсылкаНаОбъект.Метаданные());
	
	Если НЕ ПредставлениеНомераХранитсяВИнформационнойБазе Тогда
		МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДополнитьНомерЦифровымИндексомОП", ДатаНомер, ЦифровойИндексОП);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ПодготовкаСпискаИсходящихДокументов

Функция ПодготовитьВТДляСпискаОтправкиДокументов(ТипыДокументов, Запрос) Экспорт
	
	МассивВременныхТаблиц= Новый Массив;
	
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "1" ИЛИ ТипыДокументов = "2" ИЛИ ТипыДокументов = "5" ИЛИ ТипыДокументов = "6" Тогда
		ПодготовитьВТ_СЧФ_Основная(Запрос, МассивВременныхТаблиц);
		ПодготовитьВТ_УПД_СЧФДОП(Запрос, МассивВременныхТаблиц, ТипыДокументов);
		ПодготовитьВТ_УКД_КСЧФДИС(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	КонецЕсли;
	
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "1" Тогда
		ПодготовитьВТ_Накладные(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "2" Тогда
		ПодготовитьВТ_СчетаФактуры(Запрос, МассивВременныхТаблиц, ТипыДокументов);
	КонецЕсли;
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "3" Тогда
		ПодготовитьВТ_СчетНаОплату(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "4" Тогда
		ПодготовитьВТ_АктСверкиВзаиморасчетов(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	
	Возврат МассивВременныхТаблиц;
	
КонецФункции

Процедура ПодготовитьВТ_Накладные(Запрос, МассивВременныхТаблиц)
	
	ПодготовитьВТ_Накладные_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_КорректировкаРеализации(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_ВозвратПоставщику(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_ОтчетКомитенту(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_АктВыполненныхРабот(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_ОтчетДавальцу(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_Накладные_РеализацияУслугПрочихАктивов(Запрос, МассивВременныхТаблиц); 
	
	Если ИспользуетсяПередачаТоваровХранителю() Тогда
		ПодготовитьВТ_Накладные_ПередачаТоваровХранителю(Запрос, МассивВременныхТаблиц);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	&ТребуетсяСФ КАК ТребуетсяСФ,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	Документ.ЗаказКлиента КАК ЗаказКлиента,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.РеализацияТоваровУслуг КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	МАКСИМУМ(ВложенныйЗапрос.ЕстьНДС) КАК ЕстьНДС,
	|	МАКСИМУМ(ВложенныйЗапрос.ЕстьТовары) КАК ЕстьТовары,
	|	МАКСИМУМ(ВложенныйЗапрос.ЕстьУслуги) КАК ЕстьУслуги,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС,
	|	""РеализацияТоваровУслуг"" КАК Тип
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС,
	|		ВЫБОР
	|			КОГДА Товары.СтавкаНДС <> &Ставка_БезНДС
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ КАК ЕстьНДС,
	|		ВЫБОР
	|			КОГДА Товары.Номенклатура.ТипНоменклатуры В (&ТипыНоменклатурыУслуги)
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ КАК ЕстьУслуги,
	|		ВЫБОР
	|			КОГДА НЕ Товары.Номенклатура.ТипНоменклатуры В (&ТипыНоменклатурыУслуги)
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ КАК ЕстьТовары
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг.Товары КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.ЗаказКлиента КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_Накладные_РеализацияТоваровУслуг
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|ГДЕ
	|	НЕ ВТ_Основная.ЗаказКлиента В (&ПустыеЗначенияЗаказКлиента)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	ВТ_Основная.Документ
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВЫБОР
	|		КОГДА НЕ ЕСТЬNULL(СуммаНДСЗначение.ЕстьТовары, ЛОЖЬ)
	|			ТОГДА &ВидыЭД_АктОбОказанииУслуг
	|		ИНАЧЕ &ВидыЭД_ТоварнаяНакладная
	|	КОНЕЦ КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	СуммаНДСЗначение.Тип КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_РеализацияТоваровУслуг
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ НЕ ВТ_Основная.ТребуетсяСФ
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП
	|			ИЛИ ВТ_Основная.ФормироватьУПД = &КлючНастройки_СЧФДОП
	|				И НЕ ЕСТЬNULL(СуммаНДСЗначение.ЕстьНДС, ЛОЖЬ))
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТребуетсяСФ", ТекстЗапроса_РТУ_ТребуетсяСчетФактура());
	
	Запрос.УстановитьПараметр("ПустыеЗначенияЗаказКлиента", ПустыеЗначенияРеквизита("Документ.РеализацияТоваровУслуг.Реквизит.ЗаказКлиента"));
	Запрос.УстановитьПараметр("ТипыНоменклатурыУслуги", ТипыНоменклатурыУслуги());
	Запрос.УстановитьПараметр("Ставка_БезНДС", МенеджерИнтеграции_СтавкаБезНДС());
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Запрос.Параметры.Удалить("ПустыеЗначенияЗаказКлиента");
	
	МассивВременныхТаблиц.Добавить("Накладные_РеализацияТоваровУслуг");
	МассивВременныхТаблиц.Добавить("Связь_Накладные_РеализацияТоваровУслуг");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_ПередачаТоваровХранителю(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	ЛОЖЬ КАК ТребуетсяСФ,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	Документ.ЗаказКлиента КАК ЗаказКлиента,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.ПередачаТоваровХранителю КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|	И Документ.ХозяйственнаяОперация = ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ПередачаНаКомиссию)";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.ЗаказКлиента КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_Накладные_ПередачаТоваровХранителю
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|ГДЕ
	|	НЕ ВТ_Основная.ЗаказКлиента В (&ПустыеЗначенияЗаказКлиента)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	ВТ_Основная.Документ
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_ТоварнаяНакладная КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	0 КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""ПередачаТоваровХранителю"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_ПередачаТоваровХранителю
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|ГДЕ
	|	(ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП
	|			ИЛИ ВТ_Основная.ФормироватьУПД = &КлючНастройки_СЧФДОП)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.УстановитьПараметр("ПустыеЗначенияЗаказКлиента", ПустыеЗначенияРеквизита("Документ.ПередачаТоваровХранителю.Реквизит.ЗаказКлиента"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Запрос.Параметры.Удалить("ПустыеЗначенияЗаказКлиента");
	
	МассивВременныхТаблиц.Добавить("Накладные_ПередачаТоваровХранителю");
	МассивВременныхТаблиц.Добавить("Связь_Накладные_ПередачаТоваровХранителю");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_КорректировкаРеализации(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка 			КАК Документ,
	|	&ЭтоИсправление КАК ЭтоИсправление,
	|	&ЭтоКорректировка КАК ЭтоКорректировка,
	|	Документ.Организация 		КАК Организация,
	|	Документ.Контрагент 		КАК Контрагент,
	|	Документ.СуммаДокумента 	КАК СуммаДокументаЗначение,
	|	Документ.Валюта 			КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") 	КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") 			КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен 	КАК Проведен,
	|	Документ.Дата 		КАК Дата,
	|	Документ.Номер 		КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.КорректировкаРеализации КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО (Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект)
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО (Документ.Ссылка = СтатусОтправляемыхВПФ.Объект)
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УКД_КСЧФДИС_ДокументыОснования КАК УКД_КСЧФДИС_ДокументыОснования
	|		ПО (Документ.Ссылка = УКД_КСЧФДИС_ДокументыОснования.ДокументОснование)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УКД_КСЧФДИС_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	МАКСИМУМ(ВложенныйЗапрос.ЕстьТовары) КАК ЕстьТовары,
	|	МАКСИМУМ(ВложенныйЗапрос.ЕстьУслуги) КАК ЕстьУслуги,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		ВЫБОР
	|			КОГДА Товары.Номенклатура.ТипНоменклатуры В (&ТипыНоменклатурыУслуги)
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ ЕстьУслуги,
	|		ВЫБОР
	|			КОГДА НЕ Товары.Номенклатура.ТипНоменклатуры В (&ТипыНоменклатурыУслуги)
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ ЕстьТовары,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КорректировкаРеализации.Товары КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ 	КАК Документ,
	|	ВТ_Основная.Документ	КАК ДокументСвязь  
	|ПОМЕСТИТЬ Связь_Накладные_КорректировкаРеализации
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВЫБОР
	|		КОГДА ВТ_Основная.ЭтоКорректировка
	|			ТОГДА &ВидыЭД_СоглашениеОбИзмененииСтоимости
	|		КОГДА НЕ ЕСТЬNULL(СуммаНДСЗначение.ЕстьТовары, ЛОЖЬ)
	|			ТОГДА &ВидыЭД_АктОбОказанииУслуг
	|		ИНАЧЕ &ВидыЭД_ТоварнаяНакладная
	|	КОНЕЦ КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""КорректировкаРеализации"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_КорректировкаРеализации
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП
	|			ИЛИ ВТ_Основная.ФормироватьУПД = &КлючНастройки_СЧФДОП
	|				И ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) = 0)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	Запрос.УстановитьПараметр("ТипыНоменклатурыУслуги", ТипыНоменклатурыУслуги());
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ЭтоИсправление", ТекстЗапроса_КРТУ_ЭтоИсправление());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ЭтоКорректировка", ТекстЗапроса_КРТУ_ЭтоКорректировка());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("Накладные_КорректировкаРеализации");
	МассивВременныхТаблиц.Добавить("Связь_Накладные_КорректировкаРеализации");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_ВозвратПоставщику(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.ВозвратТоваровПоставщику КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ВозвратТоваровПоставщику.Товары КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	&ВидыЭД_ТоварнаяНакладная КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен 	КАК Проведен,
	|	ВТ_Основная.Дата 		КАК Дата,
	|	ВТ_Основная.Номер 		КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""ВозвратТоваровПоставщику"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_ВозвратПоставщику
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("Накладные_ВозвратПоставщику");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_ОтчетКомитенту(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаВознаграждения КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID_,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	0 КАК СуммаНДСТовары,
	|	Документ.СуммаНДСВознаграждения КАК СуммаНДСУслуги,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.ОтчетКомитенту КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (Документ.СуммаДокумента <> 0 ИЛИ Документ.УдержатьВознаграждение)
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ 		КАК Документ,
	|	СчетНаОплатуКлиенту.Ссылка 	КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_Накладные_ОтчетКомитенту
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетНаОплатуКлиенту КАК СчетНаОплатуКлиенту
	|		ПО ВТ_Основная.Документ = СчетНаОплатуКлиенту.ДокументОснование
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	&ВидыЭД_АктОбОказанииУслуг КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен 	КАК Проведен,
	|	ВТ_Основная.Дата 		КАК Дата,
	|	ВТ_Основная.Номер 		КАК Номер,
	|	ВТ_Основная.СуммаНДСТовары  КАК СуммаНДСТовары,
	|	ВТ_Основная.СуммаНДСУслуги  КАК СуммаНДСУслуги,
	|	""ОтчетКомитентуОПродажах"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_ОтчетКомитенту
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("Накладные_ОтчетКомитенту");
	МассивВременныхТаблиц.Добавить("Связь_Накладные_ОтчетКомитенту");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_АктВыполненныхРабот(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные,
	|	Документ.ЗаказКлиента КАК ЗаказКлиента
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.АктВыполненныхРабот КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Услуги.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот.Услуги КАК Услуги
	|			ПО ВТ_Основная.Документ = Услуги.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.ЗаказКлиента КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_Накладные_АктВыполненныхРабот
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|ГДЕ
	|	ВТ_Основная.ЗаказКлиента НЕ В (&ПустыеЗначенияЗаказКлиента)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	ВТ_Основная.Документ
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	&ВидыЭД_АктОбОказанииУслуг КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен 	КАК Проведен,
	|	ВТ_Основная.Дата 		КАК Дата,
	|	ВТ_Основная.Номер 		КАК Номер,
	|	0 							КАК СуммаНДСТовары,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСУслуги,
	|	""АктВыполненныхРабот"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_АктВыполненныхРабот
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.УстановитьПараметр("ПустыеЗначенияЗаказКлиента", ПустыеЗначенияРеквизита("Документ.АктВыполненныхРабот.Реквизит.ЗаказКлиента"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Запрос.Параметры.Удалить("ПустыеЗначенияЗаказКлиента");
	
	МассивВременныхТаблиц.Добавить("Накладные_АктВыполненныхРабот");
	МассивВременныхТаблиц.Добавить("Связь_Накладные_АктВыполненныхРабот");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_ОтчетДавальцу(Запрос, МассивВременныхТаблиц)
	
	Если Не ИспользуетсяОтчетДавальцу() Тогда
		Возврат;
	КонецЕсли;
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.ОтчетДавальцу КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетДавальцу.Продукция КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	&ВидыЭД_АктОбОказанииУслуг КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен 	КАК Проведен,
	|	ВТ_Основная.Дата 		КАК Дата,
	|	ВТ_Основная.Номер 		КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""ОтчетДавальцу"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_ОтчетДавальцу
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("Накладные_ОтчетДавальцу");
	
КонецПроцедуры

Процедура ПодготовитьВТ_Накладные_РеализацияУслугПрочихАктивов(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД,
	|	НастройкиЭДО.ОснованиеФормированияСчета КАК ОснованиеФормированияСчета,
	|	НастройкиЭДО.ОтправлятьНепроведенные КАК ОтправлятьНепроведенные
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.РеализацияУслугПрочихАктивов КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК СтатусОтправляемыхВПФ
	|		ПО Документ.Ссылка = СтатусОтправляемыхВПФ.Объект
	|			И (СтатусОтправляемыхВПФ.Свойство = &СвойствоДокументаСтатусОтправляемыхВПФ)
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП_ДокументыОснования КАК УПД_СЧФДОП_ДокументыОснования
	|		ПО Документ.Ссылка = УПД_СЧФДОП_ДокументыОснования.ДокументОснование
	|ГДЕ
	|	УПД_СЧФДОП_ДокументыОснования.ДокументОснование ЕСТЬ NULL
	|	И (ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|			ИЛИ ЕСТЬNULL(СтатусОтправляемыхВПФ.Значение, """") ПОДОБНО ""%[НУ]%"")
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияУслугПрочихАктивов.Доходы КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ";
	
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + ТекстЗапросаВременнаяТаблица_ЕстьСЧФ();
	ТекстЗапроса = ТекстЗапроса + РазделительПакетаЗапросов() + 
	"ВЫБРАТЬ
	|	ВТ_Основная.Документ,
	|	&ВидыЭД_АктОбОказанииУслуг КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен 	КАК Проведен,
	|	ВТ_Основная.Дата 		КАК Дата,
	|	ВТ_Основная.Номер 		КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""РеализацияУслугПрочихАктивов"" КАК Тип,
	|	&ФункцияДокумента КАК ФункцияУПД
	|ПОМЕСТИТЬ Накладные_РеализацияУслугПрочихАктивов
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕстьСЧФ КАК ВТ_ЕстьСЧФ
	|		ПО ВТ_Основная.Документ = ВТ_ЕстьСЧФ.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|ГДЕ
	|	(ВТ_ЕстьСЧФ.Документ ЕСТЬ НЕ NULL 
	|			ИЛИ ВТ_Основная.ФормироватьУПД <> &КлючНастройки_СЧФДОП)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ЕстьСЧФ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ФункцияДокумента", ТекстЗапроса_ФункцияПервичногоДокумента("ВТ_Основная"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("Накладные_РеализацияУслугПрочихАктивов");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СЧФ_ОсновнаяДокументОснованиеВ_ТЧ(Запрос)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	&ЭтоИсправление КАК ЭтоИсправление,
	|	&СФ_ЭтоКорректировка КАК ЭтоКорректировка,
	|	ЛОЖЬ КАК ЭтоАванс,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|ГДЕ
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	&ЭтоИсправление,
	|	&СФАванс_ЭтоКорректировка,
	|	ИСТИНА,
	|	Документ.Организация,
	|	Документ.Контрагент,
	|	&ВалютаРегл,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """"),
	|	"""",
	|	Документ.Проведен,
	|	Документ.Дата,
	|	Документ.Номер,
	|	НастройкиЭДО.ФормироватьУПД
	|ИЗ
	|	Документ.СчетФактураВыданныйАванс КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|ГДЕ
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	&ЭтоИсправление,
	|	&СФКомиссионеру_ЭтоКорректировка,
	|	ИСТИНА,
	|	Документ.Организация,
	|	&СФКомиссионеру_Контрагент,
	|	Документ.Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """"),
	|	"""",
	|	Документ.Проведен,
	|	Документ.Дата,
	|	Документ.Номер,
	|	НастройкиЭДО.ФормироватьУПД
	|ИЗ
	|	Документ.СчетФактураКомиссионеру КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И &СФКомиссионеру_Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|ГДЕ
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.ЭтоАванс КАК ЭтоАванс,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ВТ_Основная.ФормироватьУПД КАК ФормироватьУПД,
	|	ДокументыОснования.ДокументОснование КАК ДокументОснование,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ДокументОснованиеID
	|ПОМЕСТИТЬ ДанныеДокументаИДокументыОснования
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетФактураВыданный.ДокументыОснования КАК ДокументыОснования
	|		ПО ВТ_Основная.Документ = ДокументыОснования.Ссылка
	|			И (ДокументыОснования.ДокументОснование <> НЕОПРЕДЕЛЕНО)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО (ДокументыОснования.ДокументОснование = ИдентификаторДокументаВДиадок.Объект)
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_Основная.Документ,
	|	ВТ_Основная.ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка,
	|	ВТ_Основная.ЭтоАванс,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен,
	|	ВТ_Основная.Дата,
	|	ВТ_Основная.Номер,
	|	ВТ_Основная.ФормироватьУПД,
	|	НЕОПРЕДЕЛЕНО,
	|	""""
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетФактураВыданныйАванс КАК ДокументыОснования
	|		ПО ВТ_Основная.Документ = ДокументыОснования.Ссылка
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_Основная.Документ,
	|	ВТ_Основная.ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка,
	|	ВТ_Основная.ЭтоАванс,
	|	ВТ_Основная.Организация,
	|	ВТ_Основная.Контрагент,
	|	ВТ_Основная.Валюта,
	|	ВТ_Основная.ID,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен,
	|	ВТ_Основная.Дата,
	|	ВТ_Основная.Номер,
	|	ВТ_Основная.ФормироватьУПД,
	|	НЕОПРЕДЕЛЕНО,
	|	""""
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетФактураКомиссионеру КАК ДокументыОснования
	|		ПО ВТ_Основная.Документ = ДокументыОснования.Ссылка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаСНДС) КАК СуммаДокументаЗначение,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммыДокОснованийЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ КАК Документ,
	|		РасшифровкаТЧ.СуммаНДС КАК СуммаНДС,
	|		РасшифровкаТЧ.Сумма КАК СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетФактураВыданныйАванс.Авансы КАК РасшифровкаТЧ
	|			ПО ДанныеДокументаИДокументыОснования.Документ = РасшифровкаТЧ.Ссылка
	|	
	|	//##Метка_ОтчетДавальцу##
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ,
	|		Товары.СуммаНДС,
	|		Товары.СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг.Товары КАК Товары
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ,
	|		Товары.СуммаНДС,
	|		Товары.СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КорректировкаРеализации.Товары КАК Товары
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ,
	|		Услуги.СуммаНДС,
	|		Услуги.СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот.Услуги КАК Услуги
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Услуги.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ,
	|		Доходы.СуммаНДС,
	|		Доходы.СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияУслугПрочихАктивов.Доходы КАК Доходы
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Доходы.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ,
	|		Товары.СуммаНДС,
	|		Товары.СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ВозвратТоваровПоставщику.Товары КАК Товары
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ДанныеДокументаИДокументыОснования.Документ КАК Документ,
	|	ВЫБОР
	|		КОГДА ДанныеДокументаИДокументыОснования.ЭтоКорректировка
	|			ТОГДА &ВидыЭД_КорректировочныйСчетФактура
	|		ИНАЧЕ &ВидыЭД_СчетФактура
	|	КОНЕЦ КАК ВидЭД,
	|	ДанныеДокументаИДокументыОснования.ЭтоИсправление КАК ЭтоИсправление,
	|	ДанныеДокументаИДокументыОснования.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ДанныеДокументаИДокументыОснования.ЭтоАванс КАК ЭтоАванс,
	|	ДанныеДокументаИДокументыОснования.Организация КАК Организация,
	|	ДанныеДокументаИДокументыОснования.Контрагент КАК Контрагент,
	|	ЕСТЬNULL(СуммыДокОснованийЗначение.СуммаДокументаЗначение, 0) КАК СуммаДокументаЗначение,
	|	ДанныеДокументаИДокументыОснования.Валюта КАК Валюта,
	|	ДанныеДокументаИДокументыОснования.ID КАК ID_,
	|	ДанныеДокументаИДокументыОснования.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ДанныеДокументаИДокументыОснования.Проведен КАК Проведен,
	|	ДанныеДокументаИДокументыОснования.Дата КАК Дата,
	|	ДанныеДокументаИДокументыОснования.Номер КАК Номер,
	|	ЕСТЬNULL(СуммыДокОснованийЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""СчетФактураВыданный"" КАК Тип,
	|	ДанныеДокументаИДокументыОснования.ФормироватьУПД КАК ФормироватьУПД,
	|	ДанныеДокументаИДокументыОснования.ДокументОснование КАК ДокументОснование,
	|	ДанныеДокументаИДокументыОснования.ДокументОснованиеID КАК ДокументОснованиеID
	|ПОМЕСТИТЬ СЧФ_Основная
	|ИЗ
	|	ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммыДокОснованийЗначение КАК СуммыДокОснованийЗначение
	|		ПО ДанныеДокументаИДокументыОснования.Документ = СуммыДокОснованийЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ДанныеДокументаИДокументыОснования
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммыДокОснованийЗначение";
	
	ТекстЗапроса_ОтчетДавальцу = "";
	
	Если ИспользуетсяОтчетДавальцу() Тогда
		
		ТекстЗапроса_ОтчетДавальцу = 
		"ОБЪЕДИНИТЬ ВСЕ
		|		
		|	ВЫБРАТЬ
		|		ДанныеДокументаИДокументыОснования.Документ КАК Документ,
		|		Продукция.СуммаНДС  КАК СуммаНДС,
		|		Продукция.СуммаСНДС КАК СуммаСНДС
		|	ИЗ
		|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
		|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетДавальцу.Продукция КАК Продукция
		|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Продукция.Ссылка
		|";
		
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "//##Метка_ОтчетДавальцу##", ТекстЗапроса_ОтчетДавальцу);
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ЭтоИсправление", ТекстЗапроса_СчетФактура_ЭтоИсправление());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СФ_ЭтоКорректировка", ТекстЗапроса_СчетФактура_ЭтоКорректировка());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СФАванс_ЭтоКорректировка", ТекстЗапроса_СчетФактураАванс_ЭтоКорректировка());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СФКомиссионеру_ЭтоКорректировка", ТекстЗапроса_СчетФактураКомиссионеру_ЭтоКорректировка());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&СФКомиссионеру_Контрагент", ТекстЗапроса_СчетФактураКомиссионеру_Контрагент());
	
	Запрос.УстановитьПараметр("ВалютаРегл", ВалютаРегламентированногоУчета());
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Запрос.Параметры.Удалить("ВалютаРегл");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СЧФ_ОсновнаяДокументОснованиеВ_Шапке(Запрос)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	Документ.Ссылка КАК Документ,
	|	Документ.Исправление КАК ЭтоИсправление,
	|	Документ.Корректировочный КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер,
	|	Документ.ДокументОснование КАК ДокументОснование,
	|	НастройкиЭДО.ФормироватьУПД КАК ФормироватьУПД
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|ГДЕ
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ТИПЗНАЧЕНИЯ(ВТ_Основная.ДокументОснование) КАК ДокументОснованиеТип
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная";
	
	Запрос.Текст = ТекстЗапроса;
	РезультатЗапроса = Запрос.Выполнить();
	
	ТекстЗапросаКонтрагенты= "";
	
	Если НЕ РезультатЗапроса.Пустой() Тогда
		
		Выборка= РезультатЗапроса.Выбрать();
		РезультатЗапроса= Неопределено;
		
		Пока Выборка.Следующий() Цикл
			
			МетаданныеДокумента= Метаданные.НайтиПоТипу(Выборка.ДокументОснованиеТип);
			
			Если МетаданныеДокумента <> Неопределено И МетаданныеДокумента.Реквизиты.Найти("Контрагент") <> Неопределено Тогда
				
				ТекстЗапросаКонтрагенты= ТекстЗапросаКонтрагенты + "
				|	
				|	ОБЪЕДИНИТЬ
				|	
				|ВЫБРАТЬ
				|	ВТ_Основная.ДокументОснование,
				|	ДанныеДокументаОснования.Контрагент,
				|	ОтборКонтрагенты.ФормироватьУПД
				|ИЗ
				|	ВТ_Основная КАК ВТ_Основная
				|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ " + МетаданныеДокумента.ПолноеИмя() + " КАК ДанныеДокументаОснования
				|		ПО ВТ_Основная.ДокументОснование = ДанныеДокументаОснования.Ссылка
				|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ОтборКонтрагенты КАК ОтборКонтрагенты
				|		ПО ДанныеДокументаОснования.Контрагент = ОтборКонтрагенты.Контрагент";
				
			КонецЕсли;
			
			МетаданныеДокумента= Неопределено;
			
		КонецЦикла;
	КонецЕсли;
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	ВложенныйЗапрос.Документ,
	|	МАКСИМУМ(ВложенныйЗапрос.Контрагент) КАК Контрагент,
	|	МАКСИМУМ(ВложенныйЗапрос.ФормироватьУПД) КАК ФормироватьУПД
	|ПОМЕСТИТЬ ВТ_ДокументыС_ОтборомПоКонтрагентам
	|ИЗ
	|	(ВЫБРАТЬ
	|		NULL КАК Документ,
	|		NULL КАК Контрагент,
	|		NULL КАК ФормироватьУПД
	|	ГДЕ
	|		ЛОЖЬ " + ТекстЗапросаКонтрагенты + ") КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	ВТ_Основная.*,
	|	ВТ_ДокументыС_ОтборомПоКонтрагентам.Контрагент,
	|	ЕСТЬNULL(ВТ_ДокументыС_ОтборомПоКонтрагентам.ФормироватьУПД, ВТ_Основная.ОрганизацияФормироватьУПД) КАК ФормироватьУПД,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ДокументОснованиеID
	|ПОМЕСТИТЬ ДанныеДокументаИДокументыОснования
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетФактураВыданный КАК СчетФактураВыданный
	|		ПО ВТ_Основная.Документ = СчетФактураВыданный.Ссылка
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ДокументыС_ОтборомПоКонтрагентам КАК ВТ_ДокументыС_ОтборомПоКонтрагентам
	|		ПО ВТ_Основная.ДокументОснование = ВТ_ДокументыС_ОтборомПоКонтрагентам.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО (ВТ_Основная.ДокументОснование = ИдентификаторДокументаВДиадок.Объект)
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ДокументыС_ОтборомПоКонтрагентам
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаСНДС) КАК СуммаДокументаЗначение,
	|	СУММА(ВложенныйЗапрос.СуммаНДС)  КАК СуммаНДС
	|ПОМЕСТИТЬ СуммыДокОснованийЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ КАК Документ,
	|		Товары.СуммаНДС  КАК СуммаНДС,
	|		Товары.СуммаСНДС КАК СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг.Товары КАК Товары
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Товары.Ссылка
	|
	|   ОБЪЕДИНИТЬ ВСЕ
	|
	|   ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ КАК Документ,
	|		Товары.СуммаНДС  КАК СуммаНДС,
	|		Товары.СуммаСНДС КАК СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КорректировкаРеализации.Товары КАК Товары
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|		
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ КАК Документ,
	|		Услуги.СуммаНДС  КАК СуммаНДС,
	|		Услуги.СуммаСНДС КАК СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот.Услуги КАК Услуги
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Услуги.Ссылка
	|		
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ДанныеДокументаИДокументыОснования.Документ КАК Документ,
	|		Товары.СуммаНДС  КАК СуммаНДС,
	|		Товары.СуммаСНДС КАК СуммаСНДС
	|	ИЗ
	|		ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ВозвратТоваровПоставщику.Товары КАК Товары
	|			ПО ДанныеДокументаИДокументыОснования.ДокументОснование = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ДанныеДокументаИДокументыОснования.Документ,
	|	ВЫБОР
	|		КОГДА ДанныеДокументаИДокументыОснования.ЭтоКорректировка
	|			ТОГДА &ВидыЭД_КорректировочныйСчетФактура
	|		ИНАЧЕ &ВидыЭД_СчетФактура
	|	КОНЕЦ КАК ВидЭД,
	|	ДанныеДокументаИДокументыОснования.ЭтоИсправление КАК ЭтоИсправление,
	|	ДанныеДокументаИДокументыОснования.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ДанныеДокументаИДокументыОснования.Организация,
	|	ДанныеДокументаИДокументыОснования.Контрагент,
	|	ЕСТЬNULL(СуммыДокОснованийЗначение.СуммаДокументаЗначение, 0) КАК СуммаДокументаЗначение,
	|	ДанныеДокументаИДокументыОснования.Валюта,
	|	ДанныеДокументаИДокументыОснования.ID КАК ID_,
	|	ДанныеДокументаИДокументыОснования.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ДанныеДокументаИДокументыОснования.Проведен 	КАК Проведен,
	|	ДанныеДокументаИДокументыОснования.Дата 		КАК Дата,
	|	ДанныеДокументаИДокументыОснования.Номер 		КАК Номер,
	|	ЕСТЬNULL(СуммыДокОснованийЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""СчетФактураВыданный"" КАК Тип,
	|	ДанныеДокументаИДокументыОснования.ФормироватьУПД      КАК ФормироватьУПД,
	|	ВЫБОР
	|		КОГДА ДанныеДокументаИДокументыОснования.ФормироватьУПД = &КлючНастройки_СЧФДОП И ДанныеДокументаИДокументыОснования.ЭтоКорректировка
	|			ТОГДА &ФункцияЭД_КСЧФДИС
	|		КОГДА ДанныеДокументаИДокументыОснования.ФормироватьУПД = &КлючНастройки_СЧФДОП
	|			ТОГДА &ФункцияЭД_СЧФДОП
	|		КОГДА ДанныеДокументаИДокументыОснования.ФормироватьУПД = &КлючНастройки_СЧФ_ДОП И ДанныеДокументаИДокументыОснования.ЭтоКорректировка
	|			ТОГДА &ФункцияЭД_КСЧФ
	|		КОГДА ДанныеДокументаИДокументыОснования.ФормироватьУПД = &КлючНастройки_СЧФ_ДОП
	|			ТОГДА &ФункцияЭД_СЧФ
	|		ИНАЧЕ &ФункцияЭД_ПоУмолчанию
	|	КОНЕЦ КАК ФункцияУПД,
	|	ДанныеДокументаИДокументыОснования.ДокументОснование   КАК ДокументОснование,
	|	ДанныеДокументаИДокументыОснования.ДокументОснованиеID КАК ДокументОснованиеID
	|ПОМЕСТИТЬ СЧФ_Основная
	|ИЗ
	|	ДанныеДокументаИДокументыОснования КАК ДанныеДокументаИДокументыОснования
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммыДокОснованийЗначение КАК СуммыДокОснованийЗначение
	|		ПО ДанныеДокументаИДокументыОснования.Документ = СуммыДокОснованийЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ДанныеДокументаИДокументыОснования
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммыДокОснованийЗначение";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
КонецПроцедуры

Процедура ПодготовитьВТ_Связь_СчетаФактуры(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	СЧФ_Основная.Документ КАК Документ,
	|	СЧФ_Основная.ДокументОснование КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_СчетаФактуры
	|ИЗ
	|	СЧФ_Основная КАК СЧФ_Основная
	|ГДЕ
	|	НЕ СЧФ_Основная.ДокументОснование В (&ПустыеЗначенияДокументОснованиеСЧФ)
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	СЧФ_Основная.Документ,
	|	РеализацияТоваровУслуг.ЗаказКлиента
	|ИЗ
	|	СЧФ_Основная КАК СЧФ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
	|		ПО СЧФ_Основная.ДокументОснование = РеализацияТоваровУслуг.Ссылка
	|			И (НЕ РеализацияТоваровУслуг.ЗаказКлиента В (&ПустыеЗначенияЗаказКлиентаРТУ))
	|
	|ОБЪЕДИНИТЬ
	|
	|ВЫБРАТЬ
	|	СЧФ_Основная.Документ,
	|	АктВыполненныхРабот.ЗаказКлиента
	|ИЗ
	|	СЧФ_Основная КАК СЧФ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот КАК АктВыполненныхРабот
	|		ПО СЧФ_Основная.ДокументОснование = АктВыполненныхРабот.Ссылка
	|			И (НЕ АктВыполненныхРабот.ЗаказКлиента В (&ПустыеЗначенияЗаказКлиентаАкт))";
	
	Запрос.УстановитьПараметр("ПустыеЗначенияДокументОснованиеСЧФ"	, ПустыеЗначенияРеквизита("Документ.СчетФактураВыданный.Реквизит.ДокументОснование"));
	Запрос.УстановитьПараметр("ПустыеЗначенияЗаказКлиентаРТУ"		, ПустыеЗначенияРеквизита("Документ.РеализацияТоваровУслуг.Реквизит.ЗаказКлиента"));
	Запрос.УстановитьПараметр("ПустыеЗначенияЗаказКлиентаАкт"		, ПустыеЗначенияРеквизита("Документ.АктВыполненныхРабот.Реквизит.ЗаказКлиента"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Запрос.Параметры.Удалить("ПустыеЗначенияДокументОснованиеСЧФ");
	Запрос.Параметры.Удалить("ПустыеЗначенияЗаказКлиентаРТУ");
	Запрос.Параметры.Удалить("ПустыеЗначенияЗаказКлиентаАкт");
	
	МассивВременныхТаблиц.Добавить("Связь_СчетаФактуры");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СЧФ_Основная(Запрос, МассивВременныхТаблиц)
	
	Если ОснованиеСчетаФактурыУказываетсяВТабличнойЧасти() Тогда
		ПодготовитьВТ_СЧФ_ОсновнаяДокументОснованиеВ_ТЧ(Запрос);
	Иначе
		ПодготовитьВТ_СЧФ_ОсновнаяДокументОснованиеВ_Шапке(Запрос);
	КонецЕсли;
	
	ПодготовитьВТ_Связь_СчетаФактуры(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_УПД_СЧФДОП(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	СЧФ_Основная.Документ КАК Документ
	|ПОМЕСТИТЬ ВТ_ПодходящиеСЧФ
	|ИЗ
	|	СЧФ_Основная КАК СЧФ_Основная
	|ГДЕ
	|	СЧФ_Основная.ФормироватьУПД = &КлючНастройки_СЧФДОП
	|	И НЕ СЧФ_Основная.ЭтоКорректировка
	|
	|СГРУППИРОВАТЬ ПО
	|	СЧФ_Основная.Документ
	|
	|ИМЕЮЩИЕ
	|	МИНИМУМ(ВЫБОР
	|			КОГДА СЧФ_Основная.ID_ = СЧФ_Основная.ДокументОснованиеID
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ) = ИСТИНА
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СЧФ_Основная.Документ КАК Документ,
	|	&ВидыЭД_СчетФактура КАК ВидЭД,
	|	СЧФ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	СЧФ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	СЧФ_Основная.Организация КАК Организация,
	|	СЧФ_Основная.Контрагент КАК Контрагент,
	|	СЧФ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	СЧФ_Основная.СуммаНДСТовары КАК СуммаНДСТовары,
	|	СЧФ_Основная.СуммаНДСУслуги КАК СуммаНДСУслуги,
	|	СЧФ_Основная.Валюта КАК Валюта,
	|	СЧФ_Основная.Проведен КАК Проведен,
	|	СЧФ_Основная.Дата КАК Дата,
	|	СЧФ_Основная.Номер КАК Номер,
	|	СЧФ_Основная.ID_ КАК ID_,
	|	ВЫБОР
	|		КОГДА СЧФ_Основная.ЭтоАванс
	|			ТОГДА &ФункцияЭД_СЧФ
	|		ИНАЧЕ &ФункцияЭД_СЧФДОП
	|	КОНЕЦ КАК ФункцияУПД,
	|	""СчетФактураВыданный"" КАК Тип,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	ЛОЖЬ КАК ЭтоСчет
	|ПОМЕСТИТЬ УПД_СЧФДОП
	|ИЗ
	|	ВТ_ПодходящиеСЧФ КАК ВТ_ПодходящиеСЧФ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СЧФ_Основная КАК СЧФ_Основная
	|		ПО ВТ_ПодходящиеСЧФ.Документ = СЧФ_Основная.Документ
	|ГДЕ
	|	НЕ СЧФ_Основная.ЭтоАванс
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СЧФ_Основная.ДокументОснование КАК ДокументОснование
	|ПОМЕСТИТЬ УПД_СЧФДОП_ДокументыОснования
	|ИЗ
	|	ВТ_ПодходящиеСЧФ КАК ВТ_ПодходящиеСЧФ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СЧФ_Основная КАК СЧФ_Основная
	|		ПО ВТ_ПодходящиеСЧФ.Документ = СЧФ_Основная.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ПодходящиеСЧФ";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "5" Тогда
		МассивВременныхТаблиц.Добавить("УПД_СЧФДОП");
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодготовитьВТ_УКД_КСЧФДИС(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	СЧФ_Основная.Документ КАК Документ
	|ПОМЕСТИТЬ ВТ_ПодходящиеСЧФ
	|ИЗ
	|	СЧФ_Основная КАК СЧФ_Основная
	|ГДЕ
	|	СЧФ_Основная.ФормироватьУПД = &КлючНастройки_СЧФДОП
	|	И СЧФ_Основная.ЭтоКорректировка
	|
	|СГРУППИРОВАТЬ ПО
	|	СЧФ_Основная.Документ
	|
	|ИМЕЮЩИЕ
	|	МИНИМУМ(ВЫБОР
	|			КОГДА СЧФ_Основная.ID_ = СЧФ_Основная.ДокументОснованиеID
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ) = ИСТИНА
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СЧФ_Основная.Документ КАК Документ,
	|	&ВидыЭД_КорректировочныйСчетФактура КАК ВидЭД,
	|	СЧФ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	СЧФ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	СЧФ_Основная.Организация КАК Организация,
	|	СЧФ_Основная.Контрагент КАК Контрагент,
	|	СЧФ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	СЧФ_Основная.СуммаНДСТовары КАК СуммаНДСТовары,
	|	СЧФ_Основная.СуммаНДСУслуги КАК СуммаНДСУслуги,
	|	СЧФ_Основная.Валюта КАК Валюта,
	|	СЧФ_Основная.Проведен КАК Проведен,
	|	СЧФ_Основная.Дата КАК Дата,
	|	СЧФ_Основная.Номер КАК Номер,
	|	СЧФ_Основная.ID_ КАК ID_,
	|	ВЫБОР
	|		КОГДА СЧФ_Основная.ЭтоАванс
	|			ТОГДА &ФункцияЭД_КСЧФ
	|		ИНАЧЕ &ФункцияЭД_КСЧФДИС
	|	КОНЕЦ КАК ФункцияУПД,
	|	""СчетФактураВыданный"" КАК Тип,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	ЛОЖЬ КАК ЭтоСчет
	|ПОМЕСТИТЬ УКД_КСЧФДИС
	|ИЗ
	|	ВТ_ПодходящиеСЧФ КАК ВТ_ПодходящиеСЧФ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СЧФ_Основная КАК СЧФ_Основная
	|		ПО ВТ_ПодходящиеСЧФ.Документ = СЧФ_Основная.Документ
	|ГДЕ
	|	НЕ СЧФ_Основная.ЭтоАванс
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СЧФ_Основная.ДокументОснование КАК ДокументОснование
	|ПОМЕСТИТЬ УКД_КСЧФДИС_ДокументыОснования
	|ИЗ
	|	ВТ_ПодходящиеСЧФ КАК ВТ_ПодходящиеСЧФ
	|		ЛЕВОЕ СОЕДИНЕНИЕ СЧФ_Основная КАК СЧФ_Основная
	|		ПО ВТ_ПодходящиеСЧФ.Документ = СЧФ_Основная.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_ПодходящиеСЧФ";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Если ПустаяСтрока(ТипыДокументов) ИЛИ ТипыДокументов = "6" Тогда
		МассивВременныхТаблиц.Добавить("УКД_КСЧФДИС");
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетаФактуры(Запрос, МассивВременныхТаблиц, ТипыДокументов)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СЧФ_Основная.Документ КАК Документ,
	|	ВЫБОР
	|		КОГДА СЧФ_Основная.ЭтоКорректировка
	|			ТОГДА &ВидыЭД_КорректировочныйСчетФактура
	|		ИНАЧЕ &ВидыЭД_СчетФактура
	|	КОНЕЦ КАК ВидЭД,
	|	СЧФ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	СЧФ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	СЧФ_Основная.Организация КАК Организация,
	|	СЧФ_Основная.Контрагент КАК Контрагент,
	|	СЧФ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	СЧФ_Основная.СуммаНДСТовары КАК СуммаНДСТовары,
	|	СЧФ_Основная.СуммаНДСУслуги КАК СуммаНДСУслуги,
	|	СЧФ_Основная.Валюта КАК Валюта,
	|	СЧФ_Основная.Проведен КАК Проведен,
	|	СЧФ_Основная.Дата КАК Дата,
	|	СЧФ_Основная.Номер КАК Номер,
	|	СЧФ_Основная.ID_ КАК ID_,
	|	ВЫБОР
	|		КОГДА СЧФ_Основная.ФормироватьУПД В (&КлючНастройки_СЧФДОП, &КлючНастройки_СЧФ, &КлючНастройки_СЧФ_ДОП)
	|				И СЧФ_Основная.ЭтоКорректировка
	|			ТОГДА &ФункцияЭД_КСЧФ
	|		КОГДА СЧФ_Основная.ФормироватьУПД В (&КлючНастройки_СЧФДОП, &КлючНастройки_СЧФ, &КлючНастройки_СЧФ_ДОП)
	|			ТОГДА &ФункцияЭД_СЧФ
	|		ИНАЧЕ &ФункцияЭД_ПоУмолчанию
	|	КОНЕЦ КАК ФункцияУПД,
	|	""СчетФактураВыданный"" КАК Тип,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	ЛОЖЬ КАК ЭтоСчет
	|ПОМЕСТИТЬ СчетФактураВыданный
	|ИЗ
	|	СЧФ_Основная КАК СЧФ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ УПД_СЧФДОП КАК УПД_СЧФДОП
	|		ПО СЧФ_Основная.Документ = УПД_СЧФДОП.Документ
	|		ЛЕВОЕ СОЕДИНЕНИЕ УКД_КСЧФДИС КАК УКД_КСЧФДИС
	|		ПО СЧФ_Основная.Документ = УКД_КСЧФДИС.Документ
	|ГДЕ
	|	УПД_СЧФДОП.Документ ЕСТЬ NULL
	|	И УКД_КСЧФДИС.Документ ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СЧФ_Основная";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("СчетФактураВыданный");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетНаОплату(Запрос, МассивВременныхТаблиц)
	
	ПодготовитьВТ_СчетНаОплату_СчетНаОплатуПокупателю(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_СчетНаОплату_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_СчетНаОплату_КорректировкаРеализации(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_СчетНаОплату_АктВыполненныхРабот(Запрос, МассивВременныхТаблиц);
	ПодготовитьВТ_СчетНаОплату_ЗаказПокупателя(Запрос, МассивВременныхТаблиц);
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетНаОплату_СчетНаОплатуПокупателю(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.ДокументОснование КАК ДокументОснование,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.ЧастичнаяОплата КАК ЧастичнаяОплата,
	|	&ПроцентОплаты КАК ПроцентОплаты,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.СчетНаОплатуКлиенту КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификаторСчета)
	|ГДЕ
	|	НастройкиЭДО.ОснованиеФормированияСчета = &ОснованиеСчета_Счет
	|	И ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДСТовары) КАК СуммаНДСТовары,
	|	СУММА(ВложенныйЗапрос.СуммаНДСУслуги) КАК СуммаНДСУслуги
	|ПОМЕСТИТЬ СуммыДокОснованийЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДСТовары,
	|		0 КАК СуммаНДСУслуги
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг.Товары КАК Товары
	|			ПО ВТ_Основная.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ВТ_Основная.Документ,
	|		0,
	|		Услуги.СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот.Услуги КАК Услуги
	|			ПО ВТ_Основная.ДокументОснование = Услуги.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ВТ_Основная.Документ,
	|		Товары.СуммаНДС,
	|		0
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЗаказКлиента.Товары КАК Товары
	|			ПО ВТ_Основная.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ВТ_Основная.Документ,
	|		Товары.СуммаНДС,
	|		0
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетКомиссионера.Товары КАК Товары
	|			ПО ВТ_Основная.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ВТ_Основная.Документ,
	|		Товары.СуммаНДС,
	|		0
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетКомиссионераОСписании.Товары КАК Товары
	|			ПО ВТ_Основная.ДокументОснование = Товары.Ссылка
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ВТ_Основная.Документ,
	|		Товары.СуммаНДС,
	|		0
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетКомитенту.Товары КАК Товары
	|			ПО ВТ_Основная.ДокументОснование = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.ДокументОснование КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_СчетНаОплату_СчетНаОплатуПокупателю
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|ГДЕ
	|	НЕ ВТ_Основная.ДокументОснование В (&ПустыеЗначенияДокументОснование)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_СчетНаОплату КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ЕСТЬNULL(СуммыДокОснованийЗначение.СуммаНДСТовары, 0) * ВЫБОР КОГДА ВТ_Основная.ЧастичнаяОплата ТОГДА ВТ_Основная.ПроцентОплаты / 100 ИНАЧЕ 1 КОНЕЦ КАК СуммаНДСТовары,
	|	ЕСТЬNULL(СуммыДокОснованийЗначение.СуммаНДСУслуги, 0) * ВЫБОР КОГДА ВТ_Основная.ЧастичнаяОплата ТОГДА ВТ_Основная.ПроцентОплаты / 100 ИНАЧЕ 1 КОНЕЦ КАК СуммаНДСУслуги,
	|	""СчетНаОплату"" КАК Тип,
	|	&ФункцияЭД_ПоУмолчанию КАК ФункцияУПД
	|ПОМЕСТИТЬ СчетНаОплату_СчетНаОплатуПокупателю
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммыДокОснованийЗначение КАК СуммыДокОснованийЗначение
	|		ПО ВТ_Основная.Документ = СуммыДокОснованийЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммыДокОснованийЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПроцентОплаты", ТекстЗапроса_СчетНаОплату_ПроцентОплаты());
	
	Запрос.УстановитьПараметр("ПустыеЗначенияДокументОснование", ПустыеЗначенияРеквизита("Документ.СчетНаОплатуКлиенту.Реквизит.ДокументОснование"));
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	Запрос.Параметры.Удалить("ПустыеЗначенияДокументОснование");
	
	МассивВременныхТаблиц.Добавить("СчетНаОплату_СчетНаОплатуПокупателю");
	МассивВременныхТаблиц.Добавить("Связь_СчетНаОплату_СчетНаОплатуПокупателю");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетНаОплату_РеализацияТоваровУслуг(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.РеализацияТоваровУслуг КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификаторСчета)
	|ГДЕ
	|	НастройкиЭДО.ОснованиеФормированияСчета = &ОснованиеСчета_ДокументПродажи
	|	И ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг.Товары КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.Документ КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_СчетНаОплату_РасходнаяНакладная
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_СчетНаОплату КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""СчетНаОплату"" КАК Тип,
	|	&ФункцияЭД_ПоУмолчанию КАК ФункцияУПД
	|ПОМЕСТИТЬ СчетНаОплату_РасходнаяНакладная
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("СчетНаОплату_РасходнаяНакладная");
	МассивВременныхТаблиц.Добавить("Связь_СчетНаОплату_РасходнаяНакладная");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетНаОплату_КорректировкаРеализации(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	&ЭтоИсправление КАК ЭтоИсправление,
	|	&ЭтоКорректировка КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.КорректировкаРеализации КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификаторСчета)
	|ГДЕ
	|	НастройкиЭДО.ОснованиеФормированияСчета = &ОснованиеСчета_ДокументПродажи
	|	И ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КорректировкаРеализации.Товары КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.Документ КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_СчетНаОплату_КорректировкаРеализации
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_СчетНаОплату КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""СчетНаОплату"" КАК Тип,
	|	&ФункцияЭД_ПоУмолчанию КАК ФункцияУПД
	|ПОМЕСТИТЬ СчетНаОплату_КорректировкаРеализации
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ЭтоИсправление", ТекстЗапроса_КРТУ_ЭтоИсправление());
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ЭтоКорректировка", ТекстЗапроса_КРТУ_ЭтоКорректировка());
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("СчетНаОплату_КорректировкаРеализации");
	МассивВременныхТаблиц.Добавить("Связь_СчетНаОплату_КорректировкаРеализации");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетНаОплату_АктВыполненныхРабот(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.АктВыполненныхРабот КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификаторСчета)
	|ГДЕ
	|	НастройкиЭДО.ОснованиеФормированияСчета = &ОснованиеСчета_ДокументПродажи
	|	И ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Услуги.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот.Услуги КАК Услуги
	|			ПО ВТ_Основная.Документ = Услуги.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.Документ КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_СчетНаОплату_АктВыполненныхРабот
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_СчетНаОплату КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	0 КАК СуммаНДСТовары,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСУслуги,
	|	""СчетНаОплату"" КАК Тип,
	|	&ФункцияЭД_ПоУмолчанию КАК ФункцияУПД
	|ПОМЕСТИТЬ СчетНаОплату_АктВыполненныхРабот
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("СчетНаОплату_АктВыполненныхРабот");
	МассивВременныхТаблиц.Добавить("Связь_СчетНаОплату_АктВыполненныхРабот");
	
КонецПроцедуры

Процедура ПодготовитьВТ_СчетНаОплату_ЗаказПокупателя(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	Документ.СуммаДокумента КАК СуммаДокументаЗначение,
	|	Документ.Валюта КАК Валюта,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.ЗаказКлиента КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификаторСчета)
	|ГДЕ
	|	НастройкиЭДО.ОснованиеФормированияСчета = &ОснованиеСчета_Заказ
	|	И ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВложенныйЗапрос.Документ КАК Документ,
	|	СУММА(ВложенныйЗапрос.СуммаНДС) КАК СуммаНДС
	|ПОМЕСТИТЬ СуммаНДСЗначение
	|ИЗ
	|	(ВЫБРАТЬ
	|		ВТ_Основная.Документ КАК Документ,
	|		Товары.СуммаНДС КАК СуммаНДС
	|	ИЗ
	|		ВТ_Основная КАК ВТ_Основная
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЗаказКлиента.Товары КАК Товары
	|			ПО ВТ_Основная.Документ = Товары.Ссылка) КАК ВложенныйЗапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	ВложенныйЗапрос.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	ВТ_Основная.Документ КАК ДокументСвязь
	|ПОМЕСТИТЬ Связь_СчетНаОплату_ЗаказПокупателя
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_СчетНаОплату КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	ВТ_Основная.СуммаДокументаЗначение КАК СуммаДокументаЗначение,
	|	ВТ_Основная.Валюта КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	ВТ_Основная.СостояниеОтправкиДополнительныхВПФ КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	ЕСТЬNULL(СуммаНДСЗначение.СуммаНДС, 0) КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""СчетНаОплату"" КАК Тип,
	|	&ФункцияЭД_ПоУмолчанию КАК ФункцияУПД
	|ПОМЕСТИТЬ СчетНаОплату_ЗаказПокупателя
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ЛЕВОЕ СОЕДИНЕНИЕ СуммаНДСЗначение КАК СуммаНДСЗначение
	|		ПО ВТ_Основная.Документ = СуммаНДСЗначение.Документ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ СуммаНДСЗначение";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("СчетНаОплату_ЗаказПокупателя");
	МассивВременныхТаблиц.Добавить("Связь_СчетНаОплату_ЗаказПокупателя");
	
КонецПроцедуры

Процедура ПодготовитьВТ_АктСверкиВзаиморасчетов(Запрос, МассивВременныхТаблиц)
	
	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Документ,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	Документ.Организация КАК Организация,
	|	Документ.Контрагент КАК Контрагент,
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") КАК ID,
	|	Документ.Проведен КАК Проведен,
	|	Документ.Дата КАК Дата,
	|	Документ.Номер КАК Номер
	|ПОМЕСТИТЬ ВТ_Основная
	|ИЗ
	|	Документ.СверкаВзаиморасчетов КАК Документ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ НастройкиЭДО КАК НастройкиЭДО
	|		ПО (Документ.Дата МЕЖДУ &ДатаНачала И &ДатаОкончания)
	|			И (НЕ Документ.ПометкаУдаления)
	|			И Документ.Организация = НастройкиЭДО.Организация
	|			И Документ.Контрагент = НастройкиЭДО.Контрагент
	|			И (Документ.Проведен
	|				ИЛИ НастройкиЭДО.ОтправлятьНепроведенные = ИСТИНА)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДополнительныеСведения КАК ИдентификаторДокументаВДиадок
	|		ПО Документ.Ссылка = ИдентификаторДокументаВДиадок.Объект
	|			И (ИдентификаторДокументаВДиадок.Свойство = &СвойствоДокументаИдентификатор)
	|ГДЕ
	|	ЕСТЬNULL(ИдентификаторДокументаВДиадок.Значение, """") = """"
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Основная.Документ КАК Документ,
	|	&ВидыЭД_АктСверки КАК ВидЭД,
	|	ВТ_Основная.ЭтоИсправление КАК ЭтоИсправление,
	|	ВТ_Основная.ЭтоКорректировка КАК ЭтоКорректировка,
	|	ВТ_Основная.Организация КАК Организация,
	|	ВТ_Основная.Контрагент КАК Контрагент,
	|	0 КАК СуммаДокументаЗначение,
	|	"""" КАК Валюта,
	|	ВТ_Основная.ID КАК ID_,
	|	"""" КАК СостояниеОтправкиДополнительныхВПФ,
	|	ВТ_Основная.Проведен КАК Проведен,
	|	ВТ_Основная.Дата КАК Дата,
	|	ВТ_Основная.Номер КАК Номер,
	|	0 КАК СуммаНДСТовары,
	|	0 КАК СуммаНДСУслуги,
	|	""АктСверкиВзаиморасчетов"" КАК Тип,
	|	&ФункцияЭД_ПоУмолчанию КАК ФункцияУПД
	|ПОМЕСТИТЬ АктСверкиВзаиморасчетов
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВТ_Основная";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.Выполнить();
	
	МассивВременныхТаблиц.Добавить("АктСверкиВзаиморасчетов");
	
КонецПроцедуры

Функция ТекстЗапроса_РТУ_ТребуетсяСчетФактура(ПсевдонимТаблицы = "Документ")
	
	Результат =
	"ВЫБОР
	|	КОГДА ПсевдонимТаблицы.ХозяйственнаяОперация <> ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ПередачаНаКомиссию)
	|		ТОГДА ИСТИНА
	|	ИНАЧЕ ЛОЖЬ
	|КОНЕЦ";
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_КРТУ_ЭтоИсправление(ПсевдонимТаблицы = "Документ")
	
	Результат = "ЛОЖЬ";
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_КРТУ_ЭтоКорректировка(ПсевдонимТаблицы = "Документ")
	
	Результат =
	"ВЫБОР
	|	КОГДА ПсевдонимТаблицы.ВидКорректировки = ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.КорректировкаПоСогласованиюСторон)
	|		ТОГДА ИСТИНА
	|	ИНАЧЕ ЛОЖЬ
	|КОНЕЦ";
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактураНалоговыйАгент_ЭтоКорректировка(ПсевдонимТаблицы = "Документ")
	
	Результат = "ЛОЖЬ";
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактураКомиссионеру_ЭтоКорректировка(ПсевдонимТаблицы = "Документ")
	
	Результат = "ЛОЖЬ";
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактураКомиссионеру_Контрагент(ПсевдонимТаблицы = "Документ")
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	ЕстьРасчетыЧерезПосредника = СравнитьВерсии(ВерсияУТ, "11.4.8") > 0;
	
	Если ЕстьРасчетыЧерезПосредника Тогда
		Результат = "ПсевдонимТаблицы.Контрагент";
	Иначе
		Результат = "ПсевдонимТаблицы.Комиссионер";
	КонецЕсли;
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактураАванс_ЭтоКорректировка(ПсевдонимТаблицы = "Документ")
	
	Результат = "ЛОЖЬ";
	
	Если ИспользуетсяКорректировкаСчетаФактурыНаАванс() Тогда
		Результат = "ПсевдонимТаблицы.Корректировочный";
		ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактура_ЭтоКорректировка(ПсевдонимТаблицы = "Документ")
	
	Результат = "ПсевдонимТаблицы.Корректировочный";
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактура_ЭтоИсправление(ПсевдонимТаблицы = "Документ")
	
	Результат = "ПсевдонимТаблицы.Исправление";
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_ФункцияПервичногоДокумента(ПсевдонимТаблицы = "ВТ_Основная")
	
	Результат = 
	"	ВЫБОР
	|		КОГДА ПсевдонимТаблицы.ЭтоКорректировка
	|				И ПсевдонимТаблицы.ФормироватьУПД В (&КлючНастройки_СЧФДОП, &КлючНастройки_ДОП, &КлючНастройки_СЧФ_ДОП)
	|			ТОГДА &ФункцияЭД_ДИС
	|		КОГДА ПсевдонимТаблицы.ФормироватьУПД В (&КлючНастройки_СЧФДОП, &КлючНастройки_ДОП, &КлючНастройки_СЧФ_ДОП)
	|			ТОГДА &ФункцияЭД_ДОП
	|		ИНАЧЕ &ФункцияЭД_ПоУмолчанию
	|	КОНЕЦ";
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетНаОплату_ПроцентОплаты(ПсевдонимТаблицы = "Документ")
	
	Результат =
	"ВЫБОР
	|	КОГДА ПсевдонимТаблицы.ДокументОснование ССЫЛКА Документ.ОтчетКомитенту
	|				И ПсевдонимТаблицы.ДокументОснование.СуммаВознаграждения = 0
	|		ТОГДА 0
	|	КОГДА ПсевдонимТаблицы.ДокументОснование ССЫЛКА Документ.ОтчетКомитенту
	|		ТОГДА ПсевдонимТаблицы.СуммаДокумента * 100 / ПсевдонимТаблицы.ДокументОснование.СуммаВознаграждения // СуммаВознаграждения <> 0
	|	КОГДА ПсевдонимТаблицы.ДокументОснование.СуммаДокумента = 0
	|		ТОГДА 0
	|	КОГДА (ПсевдонимТаблицы.ДокументОснование ССЫЛКА Документ.ЗаказКлиента
	|				ИЛИ ПсевдонимТаблицы.ДокументОснование ССЫЛКА Документ.ЗаявкаНаВозвратТоваровОтКлиента)
	|				И ПсевдонимТаблицы.ДокументОснование.ТребуетсяЗалогЗаТару
	|		ТОГДА ПсевдонимТаблицы.СуммаДокумента * 100 / (ПсевдонимТаблицы.ДокументОснование.СуммаДокумента + ПсевдонимТаблицы.ДокументОснование.СуммаВозвратнойТары)
	|	ИНАЧЕ ПсевдонимТаблицы.СуммаДокумента * 100 / ПсевдонимТаблицы.ДокументОснование.СуммаДокумента
	|КОНЕЦ";
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапросаВременнаяТаблица_ЕстьСЧФ()
	
	Результат =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_Основная.Документ КАК Документ
	|ПОМЕСТИТЬ ВТ_ЕстьСЧФ
	|ИЗ
	|	ВТ_Основная КАК ВТ_Основная
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СчетФактураВыданный.ДокументыОснования КАК СЧФ_ДокументыОснования
	|		ПО ВТ_Основная.Документ = СЧФ_ДокументыОснования.ДокументОснование
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.СчетФактураВыданный КАК СЧФ
	|		ПО (СЧФ_ДокументыОснования.Ссылка = СЧФ.Ссылка)
	|ГДЕ
	|	СЧФ.Ссылка ЕСТЬ НЕ NULL 
	|	И НЕ СЧФ.ПометкаУдаления
	|	И (СЧФ.Проведен
	|			ИЛИ ВТ_Основная.ОтправлятьНепроведенные = ИСТИНА)";
	
	Возврат Результат;
	
КонецФункции

Функция ТипыНоменклатурыУслуги()
	
	Результат = Новый Массив;
	Результат.Добавить(Перечисления.ТипыНоменклатуры.Услуга);
	Результат.Добавить(Перечисления.ТипыНоменклатуры.Работа);
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоУслуга(Номенклатура)
	
	ТипыНоменклатурыУслуги = ТипыНоменклатурыУслуги();
	
	Результат =  НЕ (ТипыНоменклатурыУслуги.Найти(Номенклатура.ТипНоменклатуры) = Неопределено); 
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьТекстЗапросаДляСпискаТоваровСГТД(ДокументСсылка, КолонкаКодаТовара)
	
	Если ИспользуетсяРегистрационныйНомерТД() Тогда
		НомерГТДПредставление = "РеализацияТоваровУслугВидыЗапасов.НомерГТД.РегистрационныйНомер";
	Иначе
		НомерГТДПредставление = "РеализацияТоваровУслугВидыЗапасов.НомерГТД.Код";
	КонецЕсли;
	
	ЭтоНакладная = ЭтоНакладная(ДокументСсылка);
	
	Если Не ИспользуетсяАналитикаУчетаНоменклатуры() Тогда
		
		Результат = 
			"ВЫБРАТЬ
			|	РеализацияТоваровУслугВидыЗапасов.НомерСтроки,
			|	РеализацияТоваровУслугВидыЗапасов.Номенклатура КАК Номенклатура,
			|	ВЫБОР
			|		КОГДА ПОДСТРОКА(РеализацияТоваровУслугВидыЗапасов.Номенклатура.НаименованиеПолное, 1, 10) = """"
			|			ТОГДА РеализацияТоваровУслугВидыЗапасов.Номенклатура.Наименование
			|		ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.Номенклатура.НаименованиеПолное
			|	КОНЕЦ КАК НоменклатураПредставление,
			|	РеализацияТоваровУслугВидыЗапасов.Характеристика,
			|	РеализацияТоваровУслугВидыЗапасов.Серия КАК Серия,
			|  	РеализацияТоваровУслугВидыЗапасов.Номенклатура." + КолонкаКодаТовара + ",
			|	РеализацияТоваровУслугВидыЗапасов.Количество,
			|	%ТекстЗапросаКоличествоПоРНПТ% КАК КоличествоПоРНПТ,
			|   ВЫБОР
			|		КОГДА РеализацияТоваровУслугВидыЗапасов.Количество = 0
			|       	ТОГДА 0
			|		ИНАЧЕ
			|			ВЫБОР
			|				КОГДА РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС
			|					ТОГДА РеализацияТоваровУслугВидыЗапасов.СуммаСНДС
			|				ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.СуммаСНДС - РеализацияТоваровУслугВидыЗапасов.СуммаНДС
			|			КОНЕЦ / РеализацияТоваровУслугВидыЗапасов.Количество
			|	КОНЕЦ,
			|	ВЫБОР
			|		КОГДА РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС
			|			ТОГДА РеализацияТоваровУслугВидыЗапасов.СуммаСНДС
			|		ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.СуммаСНДС - РеализацияТоваровУслугВидыЗапасов.СуммаНДС
			|	КОНЕЦ,
			|	РеализацияТоваровУслугВидыЗапасов.СтавкаНДС,
			|	РеализацияТоваровУслугВидыЗапасов.СуммаНДС,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД.СтранаПроисхождения,
			|	" + НомерГТДПредставление + " Как НомерГТДПредставление,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД.Код КАК НомерГТДРегистрационныйНомер,
			|	РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС КАК СуммаВключаетНДС,
			|	РеализацияТоваровУслугВидыЗапасов.Номенклатура.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
			|	&ПолеУпаковка КАК Упаковка,
			|	РеализацияТоваровУслугВидыЗапасов.Номенклатура.ЕдиницаИзмерения.Код КАК КодЕдиницыИзмерения,
			| " + ТекстЗапроса_ЕдиницаИзмеренияПрослеживаемости("РеализацияТоваровУслугВидыЗапасов", "ЕдиницаИзмеренияПрослеживаемости") + ",
			|	РеализацияТоваровУслугВидыЗапасов.Упаковка.ЕдиницаИзмерения КАК ВидУпаковки,
			|	" + ПолучитьТекстЗапроса_КоэффициентУпаковки("РеализацияТоваровУслугВидыЗапасов") + "
			|	РеализацияТоваровУслугВидыЗапасов.КоличествоУпаковок,
			|	ЛОЖЬ,
			|	0, 0,
			|	0, // Брутто
			| " + ТекстЗапроса_ПрослеживаемыйТовар("РеализацияТоваровУслугВидыЗапасов", "ПрослеживаемыйТовар") + ",
			|	1,
			|	""Property"",
			|	&ТекстЗапросаСуммаСкидок
			|ИЗ
			|	Документ.РеализацияТоваровУслуг.ВидыЗапасов КАК РеализацияТоваровУслугВидыЗапасов
			|ГДЕ
			|	РеализацияТоваровУслугВидыЗапасов.Ссылка = &Ссылка
			|	И &ТекстЗапросаОтборНаМногооборотнуюТару";
		
		Если ЭтоНакладная Тогда
			ТекстЗапросаОтборНаМногооборотнуюТару = ТекстЗапроса_ОтборНаМногооборотнуюТару("РеализацияТоваровУслугВидыЗапасов.Ссылка", "РеализацияТоваровУслугВидыЗапасов.Номенклатура");
		Иначе
			ТекстЗапросаОтборНаМногооборотнуюТару = "Истина";
		КонецЕсли;
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаОтборНаМногооборотнуюТару"	, ТекстЗапросаОтборНаМногооборотнуюТару);
		
	Иначе
		
		Результат = 
			"ВЫБРАТЬ
			|	МИНИМУМ(РеализацияТоваровУслугВидыЗапасов.НомерСтроки),
			|	Аналитика.Номенклатура КАК Номенклатура,
			|	ВЫБОР
			|		КОГДА ПОДСТРОКА(Аналитика.Номенклатура.НаименованиеПолное, 1, 10) = """"
			|			ТОГДА Аналитика.Номенклатура.Наименование
			|		ИНАЧЕ Аналитика.Номенклатура.НаименованиеПолное
			|	КОНЕЦ КАК НоменклатураПредставление,
			|	Аналитика.Характеристика,
			|	Аналитика.Серия КАК Серия,
			|	Аналитика.Номенклатура." + КолонкаКодаТовара + ",
			|	СУММА(&ТекстЗапросаКоличество),
			|	СУММА(%ТекстЗапросаКоличествоПоРНПТ%),
			|	&ПолеЦена КАК Цена,
			| 	СУММА(&ТекстЗапросаСумма),
			|	РеализацияТоваровУслугВидыЗапасов.СтавкаНДС,
			|	СУММА(&ТекстЗапросаСуммаНДС),
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД.СтранаПроисхождения,
			|	" + НомерГТДПредставление + " Как НомерГТДПредставление,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД.Код КАК НомерГТДРегистрационныйНомер,
			|	РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС,
			| " + ТекстЗапроса_РеквизитыЕдиницыИзмерения("Ссылка") + " КАК ЕдиницаИзмерения,
			|	&ПолеУпаковка КАК Упаковка,
			| " + ТекстЗапроса_РеквизитыЕдиницыИзмерения("Код") + " КАК КодЕдиницыИзмерения,
			| " + ТекстЗапроса_ЕдиницаИзмеренияПрослеживаемости("Аналитика") + " КАК ЕдиницаИзмеренияПрослеживаемости,
			|	РеализацияТоваровУслугВидыЗапасов.Упаковка.ЕдиницаИзмерения КАК ВидУпаковки,
			|	" + ПолучитьТекстЗапроса_КоэффициентУпаковки("РеализацияТоваровУслугВидыЗапасов") + "
			|	СУММА(РеализацияТоваровУслугВидыЗапасов.КоличествоУпаковок),
			|	ЛОЖЬ,
			|   СУММА(ВЫБОР КОГДА РеализацияТоваровУслугВидыЗапасов.Упаковка.Вес есть NULL 
			|                 ТОГДА  РеализацияТоваровУслугВидыЗапасов.Количество * " + ПолучитьТекстЗапроса_НоменклатураВес("Аналитика") + "
			|                 Иначе РеализацияТоваровУслугВидыЗапасов.КоличествоУпаковок *   РеализацияТоваровУслугВидыЗапасов.Упаковка.Вес 
			|         КОНЕЦ),
			//|   СУММА(РеализацияТоваровУслугВидыЗапасов.Количество *  " + ПолучитьТекстЗапроса_НоменклатураВес("Аналитика") + "), 
			|   СУММА(РеализацияТоваровУслугВидыЗапасов.Количество * &ТекстЗапросаВесНоменклатуры),
			|   &ТекстЗапросаМассаБруттоГТД, // Брутто
			| " + ТекстЗапроса_ПрослеживаемыйТовар("Аналитика", "ПрослеживаемыйТовар") + ",
			|	1,
			|	""Property"",
			|	СУММА(&ТекстЗапросаСуммаСкидок)
			|ИЗ
			|	Документ.РеализацияТоваровУслуг.ВидыЗапасов КАК РеализацияТоваровУслугВидыЗапасов
			|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.КлючиАналитикиУчетаНоменклатуры КАК Аналитика
			|		ПО РеализацияТоваровУслугВидыЗапасов.АналитикаУчетаНоменклатуры = Аналитика.Ссылка
			|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыВВалютеУчета
			|			ПО  СуммыВВалютеУчета.Регистратор  = &Ссылка 
			|				и РеализацияТоваровУслугВидыЗапасов.ИдентификаторСтроки = СуммыВВалютеУчета.ИдентификаторСтроки
			|			И (СуммыВВалютеУчета.Активность)
			|				И (&ПересчитыватьВВалютуРегл)
			|ГДЕ
			|	РеализацияТоваровУслугВидыЗапасов.Ссылка = &Ссылка
			|	И &ТекстЗапросаОтборНаМногооборотнуюТару
			|
			|СГРУППИРОВАТЬ ПО
			|	Аналитика.Номенклатура,
			|	Аналитика.Характеристика,
			|	Аналитика.Серия,
			|	Аналитика.Номенклатура." + КолонкаКодаТовара + ",
			|	РеализацияТоваровУслугВидыЗапасов.СтавкаНДС,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД.СтранаПроисхождения,
			|	РеализацияТоваровУслугВидыЗапасов.НомерГТД.Код,
			|	" + НомерГТДПредставление + ",
			|	РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС,
			|	РеализацияТоваровУслугВидыЗапасов.Упаковка.ЕдиницаИзмерения,
			|	РеализацияТоваровУслугВидыЗапасов.Упаковка,
			|	&ПолеЦена,
			|	" + СтрЗаменить(ПолучитьТекстЗапроса_КоэффициентУпаковки("РеализацияТоваровУслугВидыЗапасов"), "КАК КоличествоВОдномМесте", "") + "
			|	ВЫБОР
			|		КОГДА ПОДСТРОКА(Аналитика.Номенклатура.НаименованиеПолное, 1, 10) = """"
			|			ТОГДА Аналитика.Номенклатура.Наименование
			|		ИНАЧЕ Аналитика.Номенклатура.НаименованиеПолное
			|	КОНЕЦ,
			| " + ТекстЗапроса_РеквизитыЕдиницыИзмерения("Ссылка") + ",
			| " + ТекстЗапроса_ЕдиницаИзмеренияПрослеживаемости("Аналитика") + ",
			| " + ТекстЗапроса_ПрослеживаемыйТовар("Аналитика") + ",
			| " + ТекстЗапроса_РеквизитыЕдиницыИзмерения("Код");
		
		ТекстЗапросаСуммаНДС = 
		"ЕСТЬNULL(СуммыВВалютеУчета.СуммаНДСРегл, РеализацияТоваровУслугВидыЗапасов.СуммаНДС)";
		
		ТекстЗапросаСумма = 
		"ВЫБОР
		|	КОГДА СуммыВВалютеУчета.СуммаБезНДСРегл ЕСТЬ NULL
		|		ТОГДА ВЫБОР
		|				КОГДА РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС
		|					ТОГДА РеализацияТоваровУслугВидыЗапасов.СуммаСНДС
		|				ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.СуммаСНДС - РеализацияТоваровУслугВидыЗапасов.СуммаНДС
		|			КОНЕЦ
		|	ИНАЧЕ ВЫБОР
		|			КОГДА РеализацияТоваровУслугВидыЗапасов.Ссылка.ЦенаВключаетНДС
		|				ТОГДА СуммыВВалютеУчета.СуммаБезНДСРегл + СуммыВВалютеУчета.СуммаНДСРегл
		|			ИНАЧЕ СуммыВВалютеУчета.СуммаБезНДСРегл
		|		КОНЕЦ
		|КОНЕЦ";
		
		Если (Метаданные.Имя = "УправлениеТорговлей" И МетодСервера(, "СравнитьВерсии", ПараметрыКлиентСервер.ВерсияКонфигурации, "11.2") < 0)
				Или (Метаданные.Имя = "УправлениеПредприятием" И МетодСервера(, "СравнитьВерсии", ПараметрыКлиентСервер.ВерсияКонфигурации, "2.1") < 0) Тогда
			ТекстЗапросаМассаБруттоГТД = "
			|		СУММА(ВЫБОР
			|				КОГДА &ЗаполненаЕдиницаИзмеренияВеса
			|					ТОГДА ВЫБОР
			|							КОГДА РеализацияТоваровУслугВидыЗапасов.Упаковка.Вес ЕСТЬ NULL 
			|								ТОГДА РеализацияТоваровУслугВидыЗапасов.Количество * Аналитика.Номенклатура.Вес
			|							ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.КоличествоУпаковок * РеализацияТоваровУслугВидыЗапасов.Упаковка.Вес
			|						КОНЕЦ
			|				ИНАЧЕ 0
			|			КОНЕЦ)";
			
			ТекстЗапросаВесНоменклатуры = "Аналитика.Номенклатура.Вес";
		Иначе
			
			ТекстЗапросаМассаБруттоГТД = "
			|   СУММА(ВЫБОР
			|			КОГДА &ЗаполненаЕдиницаИзмеренияВеса
			|				ТОГДА ВЫБОР
			|						КОГДА РеализацияТоваровУслугВидыЗапасов.Упаковка.Вес ЕСТЬ NULL 
			|							ТОГДА РеализацияТоваровУслугВидыЗапасов.Количество
			|						ИНАЧЕ ВЫБОР 
			|							КОГДА РеализацияТоваровУслугВидыЗапасов.Упаковка.ТипИзмеряемойВеличины = Значение(Перечисление.ТипыИзмеряемыхВеличин.Вес)
			|								ТОГДА 0
			|							ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.КоличествоУпаковок
			|						КОНЕЦ
			|					КОНЕЦ * " + ТекстЗапросаВесУпаковки("РеализацияТоваровУслугВидыЗапасов.Упаковка", "Аналитика.Номенклатура") + "
			|			ИНАЧЕ 0
			|		КОНЕЦ)";
			
			ТекстЗапросаВесНоменклатуры = ТекстЗапросаВесУпаковки("Аналитика.Номенклатура.ЕдиницаИзмерения", "Аналитика.Номенклатура");
			
		КонецЕсли;
		
		Если ДокументСсылка.Метаданные().ТабличныеЧасти.Найти("ВидыЗапасов").Реквизиты.Найти("СуммаРучнойСкидки") = Неопределено Тогда
			ТекстЗапросаСуммаСкидок = "0";
		Иначе
			ТекстЗапросаСуммаСкидок = "РеализацияТоваровУслугВидыЗапасов.СуммаРучнойСкидки + РеализацияТоваровУслугВидыЗапасов.СуммаАвтоматическойСкидки"
		КонецЕсли;
		
		Если ЭтоНакладная Тогда
			ТекстЗапросаОтборНаМногооборотнуюТару = ТекстЗапроса_ОтборНаМногооборотнуюТару("РеализацияТоваровУслугВидыЗапасов.Ссылка", "Аналитика.Номенклатура");
		Иначе
			ТекстЗапросаОтборНаМногооборотнуюТару = "Истина";
		КонецЕсли;
		
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаКоличество"		, ТекстЗапроса_Количество("РеализацияТоваровУслугВидыЗапасов"));
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаСуммаНДС"			, ТекстЗапросаСуммаНДС);
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаСуммаСкидок"		, ТекстЗапросаСуммаСкидок);
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаСумма"				, ТекстЗапросаСумма);
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаМассаБруттоГТД"	, ТекстЗапросаМассаБруттоГТД);
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаВесНоменклатуры"	, ТекстЗапросаВесНоменклатуры);
		Результат = СтрЗаменить(Результат, "&ТекстЗапросаОтборНаМногооборотнуюТару"	, ТекстЗапросаОтборНаМногооборотнуюТару);
		
	КонецЕсли;
	
	ЕстьРеквизитКоличествоПоРНПТ = МетодСервера( , "СуществуетОбъектМетаданных", "Документ.РеализацияТоваровУслуг.ТабличнаяЧасть.ВидыЗапасов.Реквизит.КоличествоПоРНПТ");
	Если ЕстьРеквизитКоличествоПоРНПТ Тогда
		Если МетодСервера( , "СуществуетОбъектМетаданных", "ФункциональнаяОпция.ИспользуетсяУчетВЕдиницеИзмеренияТНВЭД")
			И НЕ ПолучитьФункциональнуюОпцию("ИспользуетсяУчетВЕдиницеИзмеренияТНВЭД") Тогда
			ТекстЗапросаКоличествоПоРНПТ = "РеализацияТоваровУслугВидыЗапасов.Количество";
		Иначе
			ТекстЗапросаКоличествоПоРНПТ = "РеализацияТоваровУслугВидыЗапасов.КоличествоПоРНПТ";
		КонецЕсли; 
	Иначе
		ТекстЗапросаКоличествоПоРНПТ = "0";
	КонецЕсли;
	
	ПсевдонимТаблицы = "РеализацияТоваровУслугВидыЗапасов";
	Результат = СтрЗаменить(Результат, "&ПолеЦена", ВыражениеЦена(ПсевдонимТаблицы));
	Результат = СтрЗаменить(Результат, "&ПолеУпаковка", ВыражениеУпаковка(ПсевдонимТаблицы));
	Результат = СтрЗаменить(Результат, "%ТекстЗапросаКоличествоПоРНПТ%"	, ТекстЗапросаКоличествоПоРНПТ);
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_РеквизитыЕдиницыИзмерения(ИмяРеквизита)
	
	Текст =
	"ВЫБОР
	|	КОГДА &ВесВБазовыхЕИ ИЛИ (РеализацияТоваровУслугВидыЗапасов.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиНоменклатуры.ПустаяСсылка))
	|		ТОГДА Аналитика.Номенклатура.ЕдиницаИзмерения#ИмяРеквизита 
	|	КОГДА РеализацияТоваровУслугВидыЗапасов.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.Упаковка)
	|		ТОГДА РеализацияТоваровУслугВидыЗапасов.Упаковка.ЕдиницаИзмерения#ИмяРеквизита
	|	ИНАЧЕ РеализацияТоваровУслугВидыЗапасов.Упаковка#ИмяРеквизита
	|КОНЕЦ";
	
	Если ИмяРеквизита = "Ссылка" Тогда
		Текст = СтрЗаменить(Текст, "#ИмяРеквизита", "");
	Иначе
		Текст = СтрЗаменить(Текст, "#ИмяРеквизита", "." + ИмяРеквизита);
	КонецЕсли;
	
	Возврат Текст;
	
КонецФункции

//Возвращает текст отбора на переданную табличную часть.
//Отбор исключает товары с видом Тара и  если в документе стоит признак ВернутьМногооборотнуюТару
//
//Параметры
//  ИмяПоляСсылка		 - Строка - имя поля с сылкой на РТУ
//  ИмяПоляНоменклатура	 - Строка - имя поля с сылкой на Номенклатуру
//	
//Возвращаемое значение:
// Строка - отбор для табличной части
//
Функция ТекстЗапроса_ОтборНаМногооборотнуюТару(ИмяПоляСсылка, ИмяПоляНоменклатура)
	
	Результат = "Истина";
	
	ИспользоватьМногооборотнуюТару = 
		МетодСервера(, "СуществуетОбъектМетаданных", "ФункциональнаяОпция.ИспользоватьМногооборотнуюТару")
		И ПолучитьФункциональнуюОпцию("ИспользоватьМногооборотнуюТару");
	
	Если ИспользоватьМногооборотнуюТару Тогда
		
		ШаблонЗапроса = 
		"ВЫБОР 
		|	КОГДА ВЫРАЗИТЬ(%1 КАК Документ.РеализацияТоваровУслуг).ВернутьМногооборотнуюТару = Истина 
		|		И ВЫРАЗИТЬ(%2 КАК Справочник.Номенклатура).ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.МногооборотнаяТара)
		|		ТОГДА Ложь 
		|	ИНАЧЕ Истина
		|КОНЕЦ";	
		
		Результат = СтрШаблон(ШаблонЗапроса, ИмяПоляСсылка, ИмяПоляНоменклатура);
		
	КонецЕсли; 
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьТекстЗапроса_ОтборПоУслугамРаботам(ИмяТаблицы = "РеализацияТоваровУслугТовары")
	
	Если Метаданные.Перечисления.ТипыНоменклатуры.ЗначенияПеречисления.Найти("Работа") <> Неопределено Тогда 
		
		ТекстЗапроса = " ( 
		|(РеализацияТоваровУслугТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга))
		| или 
		|(РеализацияТоваровУслугТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Работа))
		|)";
		
	Иначе		
		ТекстЗапроса = "(РеализацияТоваровУслугТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга))";	
	КонецЕсли;
	
	Если ИмяТаблицы <> "РеализацияТоваровУслугТовары" Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "РеализацияТоваровУслугТовары", ИмяТаблицы);
	КонецЕсли;
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция ПолучитьТекстЗапроса_КоэффициентУпаковки(НаименованиеТаблицы)

	Результат =
	"НаименованиеТаблицы.Упаковка.Числитель /
	|	ВЫБОР 
	|		КОГДА НаименованиеТаблицы.Упаковка.Знаменатель = 0
	|			ТОГДА 1
	|		ИНАЧЕ НаименованиеТаблицы.Упаковка.Знаменатель
	|	КОНЕЦ КАК КоличествоВОдномМесте";
	
	Результат = СтрЗаменить(Результат, "НаименованиеТаблицы", НаименованиеТаблицы);
	Результат = Результат + ",";
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьТекстЗапроса_НоменклатураВес(НаименованиеТаблицы)
	
	Если МетодСервера(,"СуществуетОбъектМетаданных", "Справочник.Номенклатура.Реквизит.ВесЗнаменатель") Тогда
		Результат = "НаименованиеТаблицы.Номенклатура.ВесЗнаменатель * НаименованиеТаблицы.Номенклатура.ВесЧислитель";
	Иначе
		Результат = "НаименованиеТаблицы.Номенклатура.Вес";
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "НаименованиеТаблицы", НаименованиеТаблицы);
	
	Возврат Результат;
	
КонецФункции

// Функция - Текст запроса коэффициента упаковки
//
// Параметры:
//  ИсточникУпаковки	 - Строка	 - Таблица с упаковкой. Например, "ВЫРАЗИТЬ(&Упаковка КАК Справочник.УпаковкаЕдиницаИзмерения)"
//  ИсточникНоменклатуры - Строка	 - Таблица с номенклатурой. Если этот параметр не заполнен, то ИсточникУпаковки
//  	должен гарантированно ссылаться на упаковку / единицу измерения типа "Упаковка".
//  	Например, "ТаблицаТовары.Номенклатура"
//  ВесУпаковкиВЕдиницеКонстанты	 - 	Булево	- если ЛОЖЬ, то текст запроса вернет вес в кг, иначе - в константе - единице измерения веса . 
// 
// Возвращаемое значение:
//  Строка - строка текста запроса, которая вернет вес либо в единице константы, либо в кг (см. параметр ВесУпаковкиВЕдиницеКонстанты)
//
Функция ТекстЗапросаВесУпаковки(ИсточникУпаковки, ИсточникНоменклатуры, ВесУпаковкиВЕдиницеКонстанты = Истина)
	
	Если ИсточникНоменклатуры = Неопределено Тогда 
		
		ТекстЗапроса = "ВЫРАЗИТЬ((ЕСТЬNULL(ТаблицаТовары.Упаковка.Вес, 0) * (ЕСТЬNULL(ТаблицаТовары.Упаковка.ВесЕдиницаИзмерения.Числитель, 0) / ЕСТЬNULL(ТаблицаТовары.Упаковка.ВесЕдиницаИзмерения.Знаменатель,1)) / &КоличествоКгВЕдиницеИзмеренияВеса) КАК Число(15,7))";
		
	Иначе
		
		ТекстЗапроса =
		"ВЫРАЗИТЬ(ВЫБОР
		|			КОГДА ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.Вес)
		|					И ТаблицаТовары.Номенклатура.ВесИспользовать
		|					И ЕСТЬNULL(ТаблицаТовары.Упаковка.Знаменатель, 0) <> 0
		|				ТОГДА ВЫРАЗИТЬ(ТаблицаТовары.Упаковка.Числитель / ТаблицаТовары.Упаковка.Знаменатель КАК ЧИСЛО(15, 7))
		|			КОГДА ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.Объем)
		|					И ТаблицаТовары.Номенклатура.ВесИспользовать
		|					И ЕСТЬNULL(ТаблицаТовары.Упаковка.Знаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ОбъемЕдиницаИзмерения.Числитель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ОбъемЕдиницаИзмерения.Знаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ОбъемЧислитель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ОбъемЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель, 0) <> 0
		|				ТОГДА ВЫРАЗИТЬ((ВЫРАЗИТЬ((ВЫРАЗИТЬ(ТаблицаТовары.Упаковка.Числитель / ТаблицаТовары.Упаковка.Знаменатель КАК ЧИСЛО(15, 7))) / (ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ОбъемЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ОбъемЕдиницаИзмерения.Знаменатель КАК ЧИСЛО(15, 7))) КАК ЧИСЛО(15, 7))) / (ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ОбъемЧислитель / ТаблицаТовары.Номенклатура.ОбъемЗнаменатель КАК ЧИСЛО(15, 7))) * (ТаблицаТовары.Номенклатура.ВесЧислитель / ТаблицаТовары.Номенклатура.ВесЗнаменатель) * (ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель) КАК ЧИСЛО(15, 7))
		|			КОГДА ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.Площадь)
		|					И ТаблицаТовары.Номенклатура.ВесИспользовать
		|					И ЕСТЬNULL(ТаблицаТовары.Упаковка.Знаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ПлощадьЕдиницаИзмерения.Числитель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ПлощадьЕдиницаИзмерения.Знаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ПлощадьЧислитель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ПлощадьЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель, 0) <> 0
		|				ТОГДА ВЫРАЗИТЬ((ВЫРАЗИТЬ((ВЫРАЗИТЬ(ТаблицаТовары.Упаковка.Числитель / ТаблицаТовары.Упаковка.Знаменатель КАК ЧИСЛО(15, 7))) / (ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ПлощадьЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ПлощадьЕдиницаИзмерения.Знаменатель КАК ЧИСЛО(15, 7))) КАК ЧИСЛО(15, 7))) / (ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ПлощадьЧислитель / ТаблицаТовары.Номенклатура.ПлощадьЗнаменатель КАК ЧИСЛО(15, 7))) * (ТаблицаТовары.Номенклатура.ВесЧислитель / ТаблицаТовары.Номенклатура.ВесЗнаменатель) * (ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель) КАК ЧИСЛО(15, 7))
		|			КОГДА ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.Длина)
		|					И ТаблицаТовары.Номенклатура.ВесИспользовать
		|					И ЕСТЬNULL(ТаблицаТовары.Упаковка.Знаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ДлинаЕдиницаИзмерения.Числитель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ДлинаЕдиницаИзмерения.Знаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ДлинаЧислитель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ДлинаЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель, 0) <> 0
		|				ТОГДА ВЫРАЗИТЬ((ВЫРАЗИТЬ((ВЫРАЗИТЬ(ТаблицаТовары.Упаковка.Числитель / ТаблицаТовары.Упаковка.Знаменатель КАК ЧИСЛО(15, 7))) / (ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ДлинаЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ДлинаЕдиницаИзмерения.Знаменатель КАК ЧИСЛО(15, 7))) КАК ЧИСЛО(15, 7))) / (ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ДлинаЧислитель / ТаблицаТовары.Номенклатура.ДлинаЗнаменатель КАК ЧИСЛО(15, 7))) * (ТаблицаТовары.Номенклатура.ВесЧислитель / ТаблицаТовары.Номенклатура.ВесЗнаменатель) * (ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель) КАК ЧИСЛО(15, 7))
		|			КОГДА ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.Упаковка)
		|					И ЕСТЬNULL(ТаблицаТовары.Упаковка.ВесЕдиницаИзмерения.Знаменатель, 0) <> 0
		|				ТОГДА ВЫРАЗИТЬ(ТаблицаТовары.Упаковка.Вес * ТаблицаТовары.Упаковка.ВесЕдиницаИзмерения.Числитель / ТаблицаТовары.Упаковка.ВесЕдиницаИзмерения.Знаменатель КАК ЧИСЛО(15, 7))
		|			КОГДА (ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.КоличествоШтук)
		|					ИЛИ ТаблицаТовары.Упаковка.ТипИзмеряемойВеличины = ЗНАЧЕНИЕ(Перечисление.ТипыИзмеряемыхВеличин.ПустаяСсылка)
		|					ИЛИ ЕСТЬNULL(ТаблицаТовары.Упаковка.Ссылка, ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)) = ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка))
		|					И ТаблицаТовары.Номенклатура.ВесИспользовать
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЗнаменатель, 0) <> 0
		|					И ЕСТЬNULL(ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель, 0) <> 0
		|				ТОГДА ВЫРАЗИТЬ(ТаблицаТовары.Номенклатура.ВесЧислитель / ТаблицаТовары.Номенклатура.ВесЗнаменатель * (ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Числитель / ТаблицаТовары.Номенклатура.ВесЕдиницаИзмерения.Знаменатель) КАК ЧИСЛО(15, 7))
		|			ИНАЧЕ 0
		|		КОНЕЦ / (ВЫРАЗИТЬ(&КоличествоКгВЕдиницеИзмеренияВеса КАК ЧИСЛО(15, 7))) КАК ЧИСЛО(15, 7))";
		
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ТаблицаТовары.Номенклатура", ИсточникНоменклатуры);
		
	КонецЕсли;
	
	Если ВесУпаковкиВЕдиницеКонстанты Тогда
		ЕдиницаИзмеренияВеса = Константы.ЕдиницаИзмеренияВеса.Получить();
		Если Не ЗначениеЗаполнено(ЕдиницаИзмеренияВеса) Тогда
			ВызватьИсключение НСтр("ru = 'Не задано значение константы ""Единица измерения веса"". Обратитесь к администратору.'");
		КонецЕсли;
		СтруктураРезультат = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ЕдиницаИзмеренияВеса, "Числитель, Знаменатель");
		КоличествоКгВЕдиницеИзмеренияВеса = СтруктураРезультат.Числитель / СтруктураРезультат.Знаменатель;
	Иначе
		КоличествоКгВЕдиницеИзмеренияВеса = 1;
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&КоличествоКгВЕдиницеИзмеренияВеса", Формат(КоличествоКгВЕдиницеИзмеренияВеса, "ЧРД=.; ЧГ=0"));
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ТаблицаТовары.Упаковка", ИсточникУпаковки);
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция ВыражениеПустаяУпаковка()
	Возврат "ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)";
КонецФункции

Функция ВыражениеУпаковка(ПсевдонимТаблицы = "")
	
	Результат =
	"ВЫБОР
	|	КОГДА &ИспользоватьБазовыеЕИ
	|		ТОГДА ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)
	|	ИНАЧЕ ПсевдонимТаблицы.Упаковка
	|КОНЕЦ";
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Результат, ПсевдонимТаблицы);
	
	Возврат Результат;
	
КонецФункции

Функция ВыражениеЦена(ПсевдонимТаблицы)
	
	ТекстЗапроса = "ПсевдонимТаблицы.Цена";
	
	ТекстЗапроса =
	"ВЫБОР
	|	КОГДА &ИспользоватьБазовыеЕИ
	|		ТОГДА ВЫБОР КОГДА ПсевдонимТаблицы.Количество <> 0 ТОГДА (ПсевдонимТаблицы.СуммаСНДС - ПсевдонимТаблицы.СуммаНДС) / ПсевдонимТаблицы.Количество ИНАЧЕ 0 КОНЕЦ
	|	ИНАЧЕ ПсевдонимТаблицы.Цена
	|КОНЕЦ";
	
	ПодставитьПсевдонимТаблицыВВыражениеЗапроса(ТекстЗапроса, ПсевдонимТаблицы);
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция ТекстЗапроса_Количество(НаименованиеТаблицы)
	
	ТекстЗапроса =
	"ВЫБОР
	|	КОГДА &ВесВБазовыхЕИ
	|		ТОГДА &НаименованиеТаблицы.Количество
	|	ИНАЧЕ &НаименованиеТаблицы.КоличествоУпаковок
	|КОНЕЦ";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&НаименованиеТаблицы", НаименованиеТаблицы); 
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция ТекстЗапроса_ЕдиницаИзмерения(НаименованиеТаблицыС_Упаковкой, НаименованиеТаблицыС_ЕдИзм = Неопределено)
	
	ТекстЗапроса =
	"ВЫБОР
	|	КОГДА &ВесВБазовыхЕИ
	|			ИЛИ &НаименованиеТаблицыС_Упаковкой.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)
	|		ТОГДА &НаименованиеТаблицыС_ЕдИзм.Номенклатура.ЕдиницаИзмерения
	|	КОГДА &НаименованиеТаблицыС_Упаковкой.Упаковка.ЕдиницаИзмерения <> ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка)
	|		ТОГДА &НаименованиеТаблицыС_Упаковкой.Упаковка.ЕдиницаИзмерения
	|	ИНАЧЕ &НаименованиеТаблицыС_Упаковкой.Упаковка
	|КОНЕЦ";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&НаименованиеТаблицыС_Упаковкой", НаименованиеТаблицыС_Упаковкой); 
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&НаименованиеТаблицыС_ЕдИзм", 
	?(НаименованиеТаблицыС_ЕдИзм = Неопределено, НаименованиеТаблицыС_Упаковкой, НаименованиеТаблицыС_ЕдИзм)); 
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция ТекстЗапроса_ЕдиницаИзмеренияПрослеживаемости(НаименованиеТаблицыСНоменклатурой = "",  ИмяПоля = "")
	
	ИспользуетсяУчетПрослеживаемыхТоваров = ИспользуетсяУчетПрослеживаемыхТоваров();
	
	Если ИспользуетсяУчетПрослеживаемыхТоваров Тогда
		
		ТекстЗапроса =
			"ЕСТЬNULL(&НаименованиеТаблицыСНоменклатурой.Номенклатура.ЕдиницаИзмеренияТНВЭД, &ПустаяЕИ) &ИмяПоля";
		
	Иначе
		
		ТекстЗапроса =
			"&ПустаяЕИ &ИмяПоля";
		
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&НаименованиеТаблицыСНоменклатурой", НаименованиеТаблицыСНоменклатурой);
	
	Если ЗначениеЗаполнено(ИмяПоля) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяПоля", " КАК " + ИмяПоля);
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяПоля", "");
	КонецЕсли; 
	
	Возврат ТекстЗапроса;
	
КонецФункции

Функция ТекстЗапроса_ПрослеживаемыйТовар(НаименованиеТаблицыСНоменклатурой, ИмяПоля = "")
	
	ИспользуетсяУчетПрослеживаемыхТоваров = ИспользуетсяУчетПрослеживаемыхТоваров();
	
	Если ИспользуетсяУчетПрослеживаемыхТоваров Тогда
		
		ТекстЗапроса =
			"ВЫБОР
			|		КОГДА &ДатаДокумента >= &ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров
			|			ТОГДА ЕСТЬNULL(&НаименованиеТаблицыСНоменклатурой.Номенклатура.КодТНВЭД.ПрослеживаемыйТовар, Ложь)
			|		ИНАЧЕ Ложь
			|	КОНЕЦ &ИмяПоля";
		
	Иначе
		
		ТекстЗапроса =
			"Ложь &ИмяПоля";
		
	КонецЕсли;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&НаименованиеТаблицыСНоменклатурой", НаименованиеТаблицыСНоменклатурой);
	
	Если ЗначениеЗаполнено(ИмяПоля) Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяПоля", " КАК " + ИмяПоля);
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ИмяПоля", "");
	КонецЕсли; 
	
	Возврат ТекстЗапроса;
	
КонецФункции

// Возвращает значение даты начала использования учета прослеживаемых, импортных товаров.
//
// Возвращаемое значение:
//	Дата - дата начала использования учета прослеживаемых, импортных товаров.
//
Функция ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров()
	
	ДатаНачалаУчета = Дата(2399, 1, 1);
	
	ИспользуетсяУчетПрослеживаемыхТоваров = ИспользуетсяУчетПрослеживаемыхТоваров();
	Если ИспользуетсяУчетПрослеживаемыхТоваров Тогда
		ДатаНачалаУчета = НачалоМесяца(Константы.ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров.Получить());
	КонецЕсли;
	
	Возврат ДатаНачалаУчета;
	
КонецФункции

Функция ИспользуетсяУчетПрослеживаемыхТоваров()
	
	Результат = Ложь;
	
	ЕстьПрослеживаемостьВКонфигурации = МетодСервера(, "СуществуетОбъектМетаданных", "ФункциональнаяОпция.ИспользоватьУчетПрослеживаемыхИмпортныхТоваров");
	Если ЕстьПрослеживаемостьВКонфигурации Тогда
		
		Результат = ПолучитьФункциональнуюОпцию("ИспользоватьУчетПрослеживаемыхИмпортныхТоваров");
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции 

Функция ЗаполнятьСведенияПрослеживаемости()
	
	ВестиУчет 		= ИспользуетсяУчетПрослеживаемыхТоваров();
	ДатаНачалаУчета = ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров();
		
	Результат = ВестиУчет
				И ЗначениеЗаполнено(ДатаНачалаУчета)
				И ДатаНачалаУчета <= ТекущаяДата();
			
	Возврат Результат;
	
КонецФункции

Функция ВТабличнойЧастиЭДЕстьСведенияПрослеживаемости(СтрокаТЧ)
	
	Результат = СтрокаТЧ.СведенияОПрослеживаемости <> Неопределено
				И СтрокаТЧ.СведенияОПрослеживаемости.Количество() > 0;
				
	Возврат Результат;
	
КонецФункции

Процедура ПроверитьИсправитьИмяТаблицыСуммыРегл(ТекстЗапроса)
	
	Если ЭтоРедакция11_5() Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "РегистрСведений.СуммыДокументовВВалютеРегл", "РегистрСведений.СуммыДокументовВВалютахУчета");
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодставитьПсевдонимТаблицыВВыражениеЗапроса(Выражение, ПсевдонимТаблицы)
	
	ПодстрокаПоиска = "ПсевдонимТаблицы";
	Если Не ЗначениеЗаполнено(ПсевдонимТаблицы) Тогда
		ПодстрокаПоиска = ПодстрокаПоиска + ".";
	КонецЕсли;
	
	Выражение = СтрЗаменить(Выражение, ПодстрокаПоиска, ПсевдонимТаблицы);
	
КонецПроцедуры

#КонецОбласти

Функция ПолучитьТекстЗапросаДокументыОснованияСчетаФактурыВыданного() Экспорт
	
	Если ОснованиеСчетаФактурыУказываетсяВТабличнойЧасти() Тогда
		
		Возврат "ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	СчетФактураВыданныйДокументыОснования.ДокументОснование
		|ИЗ
		|	Документ.СчетФактураВыданный.ДокументыОснования КАК СчетФактураВыданныйДокументыОснования
		|ГДЕ
		|	СчетФактураВыданныйДокументыОснования.Ссылка = &СсылкаНаОбъект";
		
	Иначе
		
		Возврат "ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	СчетФактураВыданный.ДокументОснование
		|ИЗ
		|	Документ.СчетФактураВыданный КАК СчетФактураВыданный
		|ГДЕ
		|	СчетФактураВыданный.Ссылка = &СсылкаНаОбъект";
		
	КонецЕсли;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
//{ СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

Функция СкопироватьСтруктуру(Знач СтруктураИсходник)
	
	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "СкопироватьСтруктуру", СтруктураИсходник);

КонецФункции

Функция ОтрезатьПравуюЗапятую(стр)
	
	Результат = СокрЛП(стр);
	
	Пока СтрЗаканчиваетсяНа(Результат, ",") Цикл
		Результат = Лев(Результат, СтрДлина(Результат) - 1);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ДобавитьРазделитель(Стр, Префикс = "")
	
	Результат = "";
	
	Если ЗначениеЗаполнено(Стр) Тогда
		Результат = Префикс + СокрЛП(Стр) + ", ";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоКСФ(СсылкаНаОбъект) Экспорт
	Возврат ?(ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетФактураВыданныйАванс"), Ложь, СсылкаНаОбъект.Корректировочный);
КонецФункции

Функция ЭтоИсправление(СсылкаНаОбъект) Экспорт
	
	Возврат НЕ СчетФактуранаАванс(СсылкаНаОбъект) И СсылкаНаОбъект.Исправление
	
КонецФункции	

Функция ЭтоНакладная(СсылкаНаОбъект) Экспорт
	
	Возврат ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.РеализацияТоваровУслуг")
	
КонецФункции	

Функция ЭтоСчетНаОплату(СсылкаНаОбъект) Экспорт
	
	Возврат (НЕ Метаданные.Документы.Найти("СчетНаОплатуКлиенту") = Неопределено
			  И ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетНаОплатуКлиенту"));
			  
КонецФункции

Функция ЭтоЗаказПокупателя(СсылкаНаОбъект) Экспорт
	
	Возврат (НЕ Метаданные.Документы.Найти("ЗаказКлиента") = Неопределено
			  И ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.ЗаказКлиента"));
			  
КонецФункции

Функция ЭтоКорректировкаРеализации(СсылкаНаОбъект) Экспорт
	
	Возврат (НЕ Метаданные.Документы.Найти("КорректировкаРеализации") = Неопределено
			  И ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.КорректировкаРеализации"));	
КонецФункции

Функция СчетФактуранаАванс(СФ) Экспорт
	
	Возврат ТипЗнч(СФ) = Тип("ДокументСсылка.СчетФактураВыданныйАванс")
	
КонецФункции	

Функция ЭтоВозвратТоваровОтКлиента(СсылкаНаОбъект) Экспорт
	
	Возврат (НЕ Метаданные.Документы.Найти("ВозвратТоваровОтКлиента") = Неопределено
			  И ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.ВозвратТоваровОтКлиента"));
	
КонецФункции

Функция ТаблицаЗначений_2_МассивСтруктур(ТаблЗнач)
	
	Результат = Новый Массив;
	СтрокаКлючей = "";
	Для Каждого Колонка Из ТаблЗнач.Колонки Цикл
		СтрокаКлючей = СтрокаКлючей + ?(ПустаяСтрока(СтрокаКлючей), "", ",") + Колонка.Имя;
	КонецЦикла;
	
	Если ТипЗнч(ТаблЗнач) = Тип("ТаблицаЗначений") Тогда
		
		Для Каждого Стр Из ТаблЗнач Цикл
			ЭлементСписка = Новый  Структура(СтрокаКлючей);
			ЗаполнитьЗначенияСвойств(ЭлементСписка, Стр);
			Результат.Добавить(ЭлементСписка);
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВыделитьСлово(ИсходнаяСтрока)
	
	Буфер = СокрЛ(ИсходнаяСтрока);
	ПозицияПослПробела = Найти(Буфер, " ");
	
	Если ПозицияПослПробела = 0 Тогда
		ИсходнаяСтрока = "";
		Возврат Буфер;
	КонецЕсли;
	
	ВыделенноеСлово = СокрЛП(Лев(Буфер, ПозицияПослПробела));
	ИсходнаяСтрока = Сред(ИсходнаяСтрока, ПозицияПослПробела + 1);
	
	Возврат ВыделенноеСлово;
	
КонецФункции

Функция ИнициализацияТаблицыСтрокТовары(ЭтоКСФ)
	//
	Товары = Новый ТаблицаЗначений();
	
	ОписаниеЧисла = Новый ОписаниеТипов("Число");
	
	Товары.Колонки.Добавить("Номенклатура");
	Товары.Колонки.Добавить("НоменклатураПредставление");
	Товары.Колонки.Добавить("Содержание");
	Товары.Колонки.Добавить("Характеристика");
	Товары.Колонки.Добавить("Серия", Новый ОписаниеТипов("СправочникСсылка.СерииНоменклатуры"));
	Товары.Колонки.Добавить("КодНоменклатуры");  //для торг-12
	Товары.Колонки.Добавить("СтранаПроисхождения");
	Товары.Колонки.Добавить("НомерГТД");
	Товары.Колонки.Добавить("Количество", ОписаниеЧисла);
	Товары.Колонки.Добавить("ЕдиницаИзмерения");
	Товары.Колонки.Добавить("Упаковка");
	Товары.Колонки.Добавить("ЕдиницаИзмеренияСсылка");
	Товары.Колонки.Добавить("КодЕдиницыИзмерения"); // для торг-12
	Товары.Колонки.Добавить("ВидУпаковки");//Для торг-12
	Товары.Колонки.Добавить("КоличествоВОдномМесте");//Для торг-12
	Товары.Колонки.Добавить("КоличествоМест", ОписаниеЧисла);//Для торг-12
	Товары.Колонки.Добавить("Цена");
	Товары.Колонки.Добавить("СтавкаНДС");
	Товары.Колонки.Добавить("СуммаНДС", ОписаниеЧисла);
	Товары.Колонки.Добавить("Сумма", ОписаниеЧисла);
	Товары.Колонки.Добавить("СуммаСНДС", ОписаниеЧисла);
	Товары.Колонки.Добавить("СуммаВключаетНДС");
	Товары.Колонки.Добавить("ЭтоУслуга");
	Товары.Колонки.Добавить("Масса", ОписаниеЧисла); //Для торг-12
	Товары.Колонки.Добавить("МассаНетто", ОписаниеЧисла); //Для торг-12
	Товары.Колонки.Добавить("ПризнакТоварРаботаУслуга"); // для УПД
	Товары.Колонки.Добавить("СуммаСкидок", ОписаниеЧисла); // для УПД
	Товары.Колонки.Добавить("ПрослеживаемыйТовар"); // для УПД, для прослеживаемости	
	Товары.Колонки.Добавить("ДанныеПрослеживаемости"); // для УПД, для прослеживаемости	
	Товары.Колонки.Добавить("ДокументОтгрузки"); // для УПД 820 строка шапки 5а
	
	Если ЭтоКСФ Тогда 
		Товары.Колонки.Добавить("СтранаПроисхожденияДоИзменения");
		Товары.Колонки.Добавить("НомерГТДДоИзменения");
		Товары.Колонки.Добавить("КоличествоДоИзменения", ОписаниеЧисла);
		Товары.Колонки.Добавить("ЕдиницаИзмеренияДоИзменения");
		Товары.Колонки.Добавить("УпаковкаДоИзменения");
		Товары.Колонки.Добавить("КодЕдиницыИзмеренияДоИзменения");
		Товары.Колонки.Добавить("ЦенаДоИзменения");
		Товары.Колонки.Добавить("СтавкаНДСДоИзменения");
		Товары.Колонки.Добавить("СуммаНДСДоИзменения", ОписаниеЧисла);
		Товары.Колонки.Добавить("СуммаСНДСДоИзменения", ОписаниеЧисла);
		Товары.Колонки.Добавить("НомерСтрокиИсходногоСФ", ОписаниеЧисла);
	КонецЕсли;
	
	Возврат Товары;
	
КонецФункции

Функция ПодготовитьДопДанныеДляСозданияНакладной(СтруктураВходящиеДанные) Экспорт
	
	ВозвращаемаяСтруктура = Новый Структура();
	
	Если МетодСервера(,"ЕстьРеквизитТипаОбъекта", ТипЗнч(СтруктураВходящиеДанные.Соглашение), "ВидЦеныПоставщика") Тогда 
		ВозвращаемаяСтруктура.Вставить("ВидЦеныПоставщика", СтруктураВходящиеДанные.Соглашение.ВидЦеныПоставщика);
	КонецЕсли;
	
	Если СтруктураВходящиеДанные.Свойство("ВидОперации")
		И СтруктураВходящиеДанные.ВидОперации = "ПоступлениеУслугИПрочихАктивов" Тогда
		
		СтруктураОбъекта = Новый Структура("Соглашение, Договор", СтруктураВходящиеДанные.Соглашение, СтруктураВходящиеДанные.ДоговорКонтрагента);
		ПорядокРасчетовПоУмолчанию = ВзаиморасчетыСервер.ПорядокРасчетовПоУмолчанию(СтруктураОбъекта);
		
		ВозвращаемаяСтруктура.Вставить("ПорядокРасчетовПоУмолчанию", ПорядокРасчетовПоУмолчанию);
		
		Если ЗначениеЗаполнено(СтруктураВходящиеДанные.ДоговорКонтрагента) Тогда
			
			// BSLLS:SetPrivilegedMode-off
			УстановитьПривилегированныйРежим(Истина);
			// BSLLS:SetPrivilegedMode-on
			
			ГрафикИсполненияВДоговоре = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СтруктураВходящиеДанные.ДоговорКонтрагента, "ЗаданГрафикИсполнения");
			
			Если ПривилегированныйРежим() Тогда
				УстановитьПривилегированныйРежим(Ложь);
			КонецЕсли;
			
			ВозвращаемаяСтруктура.Вставить("ГрафикИсполненияВДоговоре", ГрафикИсполненияВДоговоре);
			
		КонецЕсли;
		
		//в текущем случае значение первого параметра значения не играет
		ДоступныеПорядкиРасчетов = ВзаиморасчетыСервер.ДоступныеПорядкиРасчетовПоДокументу(ПорядокРасчетовПоУмолчанию, ПорядокРасчетовПоУмолчанию, Ложь);
		
		ВозвращаемаяСтруктура.Вставить("ДоступныеПорядкиРасчетов", ДоступныеПорядкиРасчетов);
						
	КонецЕсли;
			
	Возврат ВозвращаемаяСтруктура; 
	
КонецФункции

Функция ПараметрыМассыПрописью(Масса, КоэффициентПересчетаВТонны)

	МассаТонны = Масса * КоэффициентПересчетаВТонны;
	Если МассаТонны > 1 Тогда
		Коэффициент = 1;
		СтрокаФормат = "т, т, т, ж, кг, кг, кг, м, " + ?(Окр(МассаТонны) = МассаТонны, "0", "3");
	ИначеЕсли МассаТонны * 1000 > 1 Тогда
		Коэффициент = 1000;
		СтрокаФормат = "кг, кг, кг, м, г, г, г, м, " + ?(Окр(МассаТонны * Коэффициент) = МассаТонны * Коэффициент, "0", "3");
	Иначе
		Коэффициент = 1000000;
		СтрокаФормат = "г, г, г, м, г, г, г, м, 0";
	КонецЕсли;
	
	Возврат Новый Структура("Формат, Коэффициент", СтрокаФормат, Коэффициент*КоэффициентПересчетаВТонны);
	
КонецФункции

Функция СписокЗначений_В_Массив(Список)
	
	Результат = Новый Массив;
	Для Каждого Элемент Из Список Цикл
		Результат.Добавить(Элемент.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ЕстьРеквизитИлиСвойствоОбъектаЛок(Объект, ИмяРеквизита)
	
	Результат = ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Объект, ИмяРеквизита);
	
	Возврат Результат;
	
КонецФункции

//} СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
//{ ПОЛУЧЕНИЕ ДАННЫХ

Функция ПолучитьВидКИ(ЮрФизЛицо, Юрадрес) Экспорт
	
	ВидАдреса = Неопределено;
	
	ТипЮрФизЛица = ТипЗнч(ЮрФизЛицо);
	
	Если ТипЮрФизЛица = Тип("СправочникСсылка.Организации") Тогда 
		
		ВидАдреса = ?(ЮрАдрес, "ЮрАдресОрганизации", "ФактАдресОрганизации");
		
	ИначеЕсли ТипЮрФизЛица = Тип("СправочникСсылка.Контрагенты") Тогда 
		
		ВидАдреса = ?(ЮрАдрес, "ЮрАдресКонтрагента", "ФактАдресКонтрагента");
		
	ИначеЕсли ТипЮрФизЛица = Тип("СправочникСсылка.ФизическиеЛица") Тогда 
		
		ВидАдреса = ?(ЮрАдрес, "АдресПоПропискеФизическиеЛица", "АдресМестаПроживанияФизическиеЛица");
		
	ИначеЕсли ТипЮрФизЛица = Тип("СправочникСсылка.РегистрацииВНалоговомОргане") Тогда 
		
		ВидАдреса = "ФактАдресОбособленногоПодразделения";
		
	Иначе 
		ВызватьИсключение НСтр("ru = 'Некорректный параметр функции ""ПолучитьВидКИ""'");
	КонецЕсли;
	
	Результат = Справочники.ВидыКонтактнойИнформации[ВидАдреса];
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧастьИсправительногоСчетаФактуры(СсылкаНаОбъект)
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	ИмяОбъекта = СсылкаНаОбъект.Метаданные().Имя;
	МенеджерОбъекта = МодульЛокализацииОбъекта(ИмяОбъекта);
	
	ОбъектыПечати = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(СсылкаНаОбъект);
	ПараметрыПечати = Новый Структура;
	ДанныеДляПечати = МенеджерОбъекта.ПолучитьДанныеДляПечатнойФормыУПД(ПараметрыПечати, ОбъектыПечати);
	
	Товары = ИнициализацияТаблицыСтрокТовары(Ложь);
	
	РезультатЗапросаПоТЧ		 = ДанныеДляПечати.РезультатПоТабличнойЧасти;
	ЕстьКоличествоПоРНПТ		 = (РезультатЗапросаПоТЧ.Колонки.Найти("КоличествоПоРНПТ") <> Неопределено);
	ЗаполнитьПрослеживаемость	 = ИспользуетсяУчетПрослеживаемыхТоваров() И ЕстьКоличествоПоРНПТ;
	СуммаВключаетНДС = СсылкаНаОбъект.ДокументОснование.ЦенаВключаетНДС;
	Если ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетФактураВыданный") 
		или ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.СчетФактураКомиссионеру") Тогда
		ДокументОтгрузки = СсылкаНаОбъект.ДокументОснование;
	Иначе
		ДокументОтгрузки = СсылкаНаОбъект;
	КонецЕсли;
	
	ВыборкаДокументы = РезультатЗапросаПоТЧ.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	ВыборкаДокументы.Следующий();
	
	Выборка = ВыборкаДокументы.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		НоваяСтрока = Товары.Добавить();
		
		ЗаполнитьЗначенияСвойств(НоваяСтрока, Выборка);
		
		НоваяСтрока.КодНоменклатуры					= Выборка.НоменклатураКод;
		НоваяСтрока.ЕдиницаИзмеренияСсылка			= Выборка.ЕдиницаИзмерения;
		НоваяСтрока.КодЕдиницыИзмерения				= Выборка.ЕдиницаИзмеренияКод;
		НоваяСтрока.СуммаВключаетНДС 				= СуммаВключаетНДС;
		НоваяСтрока.ДокументОтгрузки 				= ДокументОтгрузки;
		
		Если СуммаВключаетНДС Тогда
			НоваяСтрока.Сумма = НоваяСтрока.СуммаСНДС;
		Иначе
			НоваяСтрока.Сумма = НоваяСтрока.СуммаСНДС - НоваяСтрока.СуммаНДС;
		КонецЕсли;
		
		ДополнительныеПараметры = ДополнительныеПараметрыПредставленияНоменклатуры();
		ДополнительныеПараметры.Содержание = НоваяСтрока.Содержание;
		
		НоваяСтрока.НоменклатураПредставление = ПредставлениеНоменклатуры(Выборка.НоменклатураНаименование, Выборка.ХарактеристикаНаименование, ДополнительныеПараметры);
		
		Если ЗаполнитьПрослеживаемость Тогда
			ЗаполнитьСведенияПрослеживаемостиДляИсправительногоСчетФактура(НоваяСтрока, Выборка);
		КонецЕсли;
		
		НоваяСтрока.ЭтоУслуга = ЭтоУслуга(НоваяСтрока.Номенклатура);
		
	КонецЦикла;
	
	Возврат Товары;	
	
КонецФункции

Функция ТабличнаяЧастьКорректировочногоДокумента(СсылкаНаОбъект)
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	ИмяОбъекта = СсылкаНаОбъект.Метаданные().Имя;
	МенеджерОбъекта = МодульЛокализацииОбъекта(ИмяОбъекта);
	
	ОбъектыПечати = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(СсылкаНаОбъект);
	ПараметрыПечати = Новый Структура;
	ДанныеДляПечати = МенеджерОбъекта.ПолучитьДанныеДляПечатнойФормыУКД(ПараметрыПечати, ОбъектыПечати);
	
	Товары = ИнициализацияТаблицыСтрокТовары(Истина);
	
	РезультатЗапросаПоТЧ		 = ДанныеДляПечати.РезультатПоТабличнойЧасти;
	ЕстьНомерСтрокиИсходногоСФ	 = (РезультатЗапросаПоТЧ.Колонки.Найти("НомерСтрокиИсходногоСФ") <> Неопределено);
	ЕстьКоличествоПоРНПТ		 = (РезультатЗапросаПоТЧ.Колонки.Найти("КоличествоПоРНПТ") <> Неопределено);
	ЗаполнитьПрослеживаемость	 = ИспользуетсяУчетПрослеживаемыхТоваров() И ЕстьКоличествоПоРНПТ;
	
	ВыборкаДокументы = РезультатЗапросаПоТЧ.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	ВыборкаДокументы.Следующий();
	
	Выборка = ВыборкаДокументы.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		СтрокаБезРасхождений = ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Выборка, "РазницаБезНДСУвеличение")
			И Выборка.РазницаБезНДСУвеличение = Выборка.РазницаБезНДСУменьшение
			И Выборка.РазницаНДСУвеличение = Выборка.РазницаНДСУменьшение
			И Выборка.РазницаСНДСУвеличение = Выборка.РазницаСНДСУменьшение
		;
		
		Если СтрокаБезРасхождений Тогда
			Продолжить;
		КонецЕсли;
		
		НоваяСтрока = Товары.Добавить();
		
		ЗаполнитьЗначенияСвойств(НоваяСтрока, Выборка);
		
		Если Не ЕстьНомерСтрокиИсходногоСФ Тогда
			НоваяСтрока.НомерСтрокиИсходногоСФ = Товары.Количество();
		КонецЕсли;
		
		НоваяСтрока.КодНоменклатуры					= Выборка.НоменклатураКод;
		НоваяСтрока.ЕдиницаИзмеренияСсылка			= Выборка.ЕдиницаИзмерения;
		НоваяСтрока.КодЕдиницыИзмерения				= Выборка.ЕдиницаИзмеренияКод;
		НоваяСтрока.ЕдиницаИзмеренияДоИзменения		= Выборка.ЕдиницаИзмерения;
		НоваяСтрока.КодЕдиницыИзмеренияДоИзменения	= Выборка.ЕдиницаИзмеренияКод;
		НоваяСтрока.СтавкаНДСДоИзменения			= Выборка.СтавкаНДС;
		НоваяСтрока.КоличествоДоИзменения			= Выборка.КоличествоДо;
		НоваяСтрока.ЦенаДоИзменения					= Выборка.ЦенаДо;
		НоваяСтрока.СуммаНДСДоИзменения				= Выборка.СуммаНДСДо;
		НоваяСтрока.СуммаСНДСДоИзменения			= Выборка.СуммаСНДСДо;
		
		ДополнительныеПараметры = ДополнительныеПараметрыПредставленияНоменклатуры();
		ДополнительныеПараметры.Содержание = НоваяСтрока.Содержание;
		
		НоваяСтрока.НоменклатураПредставление = ПредставлениеНоменклатуры(Выборка.НоменклатураНаименование, Выборка.ХарактеристикаНаименование, ДополнительныеПараметры);
		
		НоваяСтрока.НомерГТД = НомерГТДСтрокой(Выборка.НомерГТД);
		
		Если ЗаполнитьПрослеживаемость Тогда
			ЗаполнитьСведенияПрослеживаемостиДляОтправкиПоКорректировке(НоваяСтрока, Выборка);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Товары;
	
КонецФункции

Функция НомерГТДСтрокой(НомерГТД)

	Результат = НомерГТД;
	
	Если ЗначениеЗаполнено(НомерГТД)
		И ТипЗнч(НомерГТД) = Тип("СправочникСсылка.НомераГТД") Тогда
		
		Результат = СокрЛП(НомерГТД.Код);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьДанныеТЧ(Документы, ВалютаРегламентированногоУчета = Неопределено, КолонкаКодаТовара = Неопределено, ФормированиеСФ = Неопределено, СчетФактура = неопределено, ФункцияУПД = "") Экспорт
	
	// BSLLS:SetPrivilegedMode-off
	УстановитьПривилегированныйРежим(Истина);
	// BSLLS:SetPrivilegedMode-on
	
	Если Не ЗначениеЗаполнено(КолонкаКодаТовара) Тогда 
		КолонкаКодаТовара = ИмяДополнительнойКолонки();
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ВалютаРегламентированногоУчета) Тогда 
		ВалютаРегламентированногоУчета = ВалютаРегламентированногоУчета();
	КонецЕсли;
	
	ЭтоКСФ = Ложь;
	//Документы - список документов либо одичночнй документ
	Если СчетФактуранаАванс(СчетФактура)
		Или ЭтоСчетФактураКомиссионеру(СчетФактура)
		Или ЭтоСчетФактураНаРеализациюЧерезКомиссионера(СчетФактура) 
		Тогда
		ДокументыИтератор = Новый СписокЗначений;
		ДокументыИтератор.Добавить(СчетФактура);
	ИначеЕсли ТипЗнч(Документы) = Тип("СписокЗначений") Тогда 
		ДокументыИтератор = Документы;
	Иначе 
		ДокументыИтератор = Новый СписокЗначений;
		ДокументыИтератор.Добавить(Документы);
	КонецЕсли;
	
	Товары = ИнициализацияТаблицыСтрокТовары(ЭтоКСФ);
	ДокументОтгрузки = Новый Структура("Ссылка");
	ЗапросТЧ = Новый Запрос;
	
	Для каждого Документ из ДокументыИтератор Цикл
		
		СсылкаНаДокумент = Документ.Значение;
		Если ТипЗнч(СсылкаНаДокумент) = Тип("ДокументСсылка.СчетНаОплатуКлиенту") Тогда // Счет не имеет своей табличной части
			СсылкаНаДокумент = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СсылкаНаДокумент, "ДокументОснование");
		КонецЕсли;
		
		ДокументОтгрузки.Ссылка = СсылкаНаДокумент;
		
		ТипСсылки = ТипЗнч(СсылкаНаДокумент);
		
		Если ТипСсылки = Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
			
			СформироватьЗапрос_ДанныеТЧ_ПоРеализацияТоваровУслуг(ЗапросТЧ, КолонкаКодаТовара, СсылкаНаДокумент);
							
		ИначеЕсли ТипСсылки = Тип("ДокументСсылка.АктВыполненныхРабот") Тогда
			
			СформироватьЗапрос_ДанныеТЧ_ПоАктВыполненныхРабот(ЗапросТЧ, КолонкаКодаТовара, СсылкаНаДокумент, ВалютаРегламентированногоУчета);
			
		ИначеЕсли НЕ ЭтоКСФ
			И Метаданные.Документы.Найти("КорректировкаРеализации") <> Неопределено 
			И ТипСсылки = Тип("ДокументСсылка.КорректировкаРеализации") Тогда 
			
			СформироватьЗапрос_ДанныеТЧ_ПоКорректировкаРеализации(ЗапросТЧ, КолонкаКодаТовара);
			
		ИначеЕсли ТипСсылки = Тип("ДокументСсылка.ОтчетКомитенту") Тогда 
			
			СформироватьЗапрос_ДанныеТЧ_ПоОтчетКомитенту(ЗапросТЧ, КолонкаКодаТовара);
							
		ИначеЕсли ТипСсылки = Тип("ДокументСсылка.ВозвратТоваровПоставщику") Тогда 
			
			СформироватьЗапрос_ДанныеТЧ_ПоВозвратТоваровПоставщику(ЗапросТЧ, КолонкаКодаТовара, СсылкаНаДокумент);
							
		ИначеЕсли ТипСсылки = Тип("ДокументСсылка.ЗаказКлиента") Тогда
			
			СформироватьЗапрос_ДанныеТЧ_ПоЗаказКлиента(ЗапросТЧ, КолонкаКодаТовара);
			
		ИначеЕсли ТипСсылки = Тип("ДокументСсылка.СчетФактураВыданныйАванс") Тогда 
			
			СформироватьЗапрос_ДанныеТЧ_ПоСчетФактураВыданныйАванс(ЗапросТЧ, КолонкаКодаТовара);
			
		ИначеЕсли ИспользуетсяОтчетДавальцу()
			И ТипСсылки = Тип("ДокументСсылка.ОтчетДавальцу") Тогда
			
			СформироватьЗапрос_ДанныеТЧ_ОтчетДавальцу(ЗапросТЧ, СсылкаНаДокумент);
			
		ИначеЕсли ТипСсылки = Тип("ДокументСсылка.РеализацияУслугПрочихАктивов") Тогда
			
			СформироватьЗапрос_ДанныеТЧ_РеализацияУслугПрочихАктивов(ЗапросТЧ, СсылкаНаДокумент);
			
		ИначеЕсли ЭтоСчетФактураКомиссионеру(СсылкаНаДокумент) 
			ИЛИ ЭтоСчетФактураНаРеализациюЧерезКомиссионера(СсылкаНаДокумент) Тогда
			
			СформироватьЗапрос_ДанныеТЧ_СчетФактураКомиссионеру(ЗапросТЧ, СсылкаНаДокумент);
			
		ИначеЕсли ИспользуетсяПередачаТоваровХранителю()
			И ТипСсылки = Тип("ДокументСсылка.ПередачаТоваровХранителю") Тогда
			
			СформироватьЗапрос_ДанныеТЧ_ПоПередачаТоваровХранителю(ЗапросТЧ, КолонкаКодаТовара, СсылкаНаДокумент);	
			
		Иначе
			
			ЗапросТЧ.Текст = "";
			
		КонецЕсли;
		
		Если ПустаяСтрока(ЗапросТЧ.Текст) Тогда
			Возврат Товары;
		КонецЕсли;
		
		Если МетодСервера(, "СуществуетОбъектМетаданных", "Справочник.УпаковкиЕдиницыИзмерения") Тогда
			ЗапросТЧ.Текст = СтрЗаменить(ЗапросТЧ.Текст, "Справочник.УпаковкиНоменклатуры", "Справочник.УпаковкиЕдиницыИзмерения");
		КонецЕсли;
		
		ЗапросТЧ.Текст = СтрЗаменить(ЗапросТЧ.Текст, "Номенклатура.Код КАК КодНоменклатуры", "Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры");
		
		ПроверитьИсправитьИмяТаблицыСуммыРегл(ЗапросТЧ.Текст);
		
		ВыводитьБазовыеЕИ = ИспользоватьБазовыеЕдиницыИзмерения();
		
		ЗапросТЧ.УстановитьПараметр("ЗаполненаЕдиницаИзмеренияВеса", ЗначениеЗаполнено(Константы.ЕдиницаИзмеренияВеса.Получить()));
		ЗапросТЧ.УстановитьПараметр("Ссылка", СсылкаНаДокумент);
		ЗапросТЧ.УстановитьПараметр("ИспользоватьБазовыеЕИ", ВыводитьБазовыеЕИ);
		
		РезультатЗапроса = ЗапросТЧ.Выполнить();
		Если НЕ РезультатЗапроса.Пустой() Тогда
			
			Выборка = РезультатЗапроса.Выбрать();
			РезультатЗапроса = Неопределено;
			
			Пока Выборка.Следующий() Цикл
				
				НоваяСтрока = Товары.Добавить();
				ЗаполнитьЗначенияСвойств(НоваяСтрока, Выборка, , "НомерГТД, СтранаПроисхождения");
				
				НоваяСтрока.КодЕдиницыИзмерения    = СокрЛП(Выборка.КодЕдиницыИзмерения);
				НоваяСтрока.ЕдиницаИзмеренияСсылка = Выборка.ЕдиницаИзмерения;
				
				Если ЗначениеЗаполнено(Выборка.СтранаПроисхождения) Тогда
					
					НоваяСтрока.НомерГТД = Выборка.НомерГТДПредставление;
					
					Если (ЗначениеЗаполнено(Выборка.НомерГТД) И ЗначениеЗаполнено(Выборка.НомерГТД.Код))
						ИЛИ НЕ МетодСервера(,"СтранаЧленТС", Выборка.СтранаПроисхождения.Код) Тогда
						НоваяСтрока.СтранаПроисхождения = Выборка.СтранаПроисхождения;
					КонецЕсли;
					
				КонецЕсли;
				
				ДополнительныеПараметры = ДополнительныеПараметрыПредставленияНоменклатуры();
				ДополнительныеПараметры.Содержание = НоваяСтрока.Содержание;
				
				НоваяСтрока.НоменклатураПредставление = ПредставлениеНоменклатуры(Выборка.НоменклатураПредставление, Выборка.Характеристика, ДополнительныеПараметры);
				
				НоваяСтрока.ДанныеПрослеживаемости = Новый Массив;
				Если НоваяСтрока.ПрослеживаемыйТовар = Истина Тогда
				
					ЭлементПрослеживаемости = НовыеСвойстваЭлементаПрослеживаемости();
					ЗаполнитьЗначенияСвойств(ЭлементПрослеживаемости, Выборка);
					НоваяСтрока.ДанныеПрослеживаемости.Добавить(ЭлементПрослеживаемости);
					
				КонецЕсли;
				
				ЗаполнитьЗначенияСвойств(ДокументОтгрузки, Выборка);
				
				НоваяСтрока.ДокументОтгрузки = ДокументОтгрузки.Ссылка;
				
			КонецЦикла; 
			
		КонецЕсли; 
	КонецЦикла;
	
	Если ДокументыИтератор.Количество() > 1 Тогда
		
		КолонкиСуммирования = "Количество, КоличествоМест, СуммаНДС, Сумма, Масса, МассаНетто";
		КолонкиСуммированияСтруктурой = Новый Структура(КолонкиСуммирования);
		
		КолонкиГруппировок 	= "";
		Разделитель 		= "";
		Для каждого Колонка Из Товары.Колонки Цикл
			Если НЕ КолонкиСуммированияСтруктурой.Свойство(Колонка.Имя) Тогда
				КолонкиГруппировок = КолонкиГруппировок + Разделитель + Колонка.Имя;
				Разделитель = ", ";
			КонецЕсли; 
		КонецЦикла;
		
		Товары.Свернуть(КолонкиГруппировок, КолонкиСуммирования);
		
	КонецЕсли;

	Возврат Товары;
	
КонецФункции
	
Процедура СформироватьЗапрос_ДанныеТЧ_ПоРеализацияТоваровУслуг(Запрос, КолонкаКодаТовара, СсылкаНаДокумент)
	
	КонстантаВыводитьСерииВПечатныхФормах = Ложь;
	Если МетодСервера(, "ЕстьМетаданныеКонфигурации_ДД", "Константы.ВыводитьСерииВПечатныхФормах") Тогда
		КонстантаВыводитьСерииВПечатныхФормах = Константы.ВыводитьСерииВПечатныхФормах.Получить();
	КонецЕсли;
	
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Подзапрос.Номенклатура 								КАК Номенклатура,
		|	Подзапрос.НоменклатураПредставление 				КАК НоменклатураПредставление,
		|	Подзапрос.Характеристика 							КАК Характеристика,			
		|	Подзапрос.КодНоменклатуры 							КАК КодНоменклатуры,
		|	Подзапрос.Цена 										КАК Цена,
		|	Подзапрос.СтавкаНДС 								КАК СтавкаНДС,
		|	Подзапрос.НомерГТД 									КАК НомерГТД,
		|	Подзапрос.СтранаПроисхождения 						КАК СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление 					КАК НомерГТДПредставление,
		|	Подзапрос.НомерГТДРегистрационныйНомер 				КАК НомерГТДРегистрационныйНомер,
		|	Подзапрос.СуммаВключаетНДС 							КАК СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения 							КАК ЕдиницаИзмерения,
		|	Подзапрос.Упаковка		 							КАК Упаковка,
		|	Подзапрос.КодЕдиницыИзмерения 						КАК КодЕдиницыИзмерения,
		|	Подзапрос.ЕдиницаИзмеренияПрослеживаемости.Наименование 	КАК ЕдиницаИзмеренияПрослеживаемости,
		|	Подзапрос.ЕдиницаИзмеренияПрослеживаемости.Код 		КАК КодЕдиницыИзмеренияПрослеживаемости,
		|	Подзапрос.ВидУпаковки 								КАК ВидУпаковки,
		|	Подзапрос.КоличествоВОдномМесте 					КАК КоличествоВОдномМесте,
		|	Подзапрос.ЭтоУслуга 								КАК ЭтоУслуга,
		|	Подзапрос.Порядок 									КАК Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга 					КАК ПризнакТоварРаботаУслуга,
		|	Подзапрос.ПрослеживаемыйТовар 						КАК ПрослеживаемыйТовар,
		|";
	
	Если КонстантаВыводитьСерииВПечатныхФормах = Истина Тогда 
		
		Запрос.Текст = Запрос.Текст + "
		|	Подзапрос.НомерСтроки 								КАК НомерСтроки,
		|	Подзапрос.Серия 									КАК Серия,
		|	Подзапрос.Количество 								КАК Количество,
		|	Подзапрос.КоличествоПоРНПТ 							КАК КоличествоПоРНПТ,
		|	Подзапрос.Сумма 									КАК Сумма,
		|	Подзапрос.СуммаНДС 									КАК СуммаНДС,
		|	Подзапрос.КоличествоМест 							КАК КоличествоМест,
		|	Подзапрос.МассаНетто 								КАК МассаНетто,
		|	Подзапрос.Брутто 									КАК Масса,
		|	Подзапрос.СуммаСкидок 								КАК СуммаСкидок
		|";
		
	Иначе
		
		Запрос.Текст = Запрос.Текст + "
		|	МИНИМУМ(Подзапрос.НомерСтроки) 						КАК НомерСтроки,
		|	NULL 												КАК Серия,
		|	СУММА(Подзапрос.Количество) 						КАК Количество,
		|	СУММА(Подзапрос.КоличествоПоРНПТ) 					КАК КоличествоПоРНПТ,
		|	СУММА(Подзапрос.Сумма) 								КАК Сумма,
		|	СУММА(Подзапрос.СуммаНДС) 							КАК СуммаНДС,
		|	СУММА(Подзапрос.КоличествоМест) 					КАК КоличествоМест,
		|	СУММА(Подзапрос.МассаНетто) 						КАК МассаНетто,
		|	СУММА(Подзапрос.Брутто) 							КАК Масса,
		|	СУММА(Подзапрос.СуммаСкидок) 						КАК СуммаСкидок
		|";
		
	КонецЕсли;
	
	Запрос.Текст = Запрос.Текст + "
		|ИЗ
		|	(ВЫБРАТЬ
		|		РеализацияТоваровУслугТовары.НомерСтроки 	КАК НомерСтроки,
		|		РеализацияТоваровУслугТовары.Номенклатура 	КАК Номенклатура,
		|		ВЫБОР
		|			КОГДА ПОДСТРОКА(РеализацияТоваровУслугТовары.Номенклатура.НаименованиеПолное, 1, 10) = """"
		|				ТОГДА РеализацияТоваровУслугТовары.Номенклатура.Наименование
		|			ИНАЧЕ РеализацияТоваровУслугТовары.Номенклатура.НаименованиеПолное
		|		КОНЕЦ КАК НоменклатураПредставление,
		|		РеализацияТоваровУслугТовары.Характеристика КАК Характеристика,
		|		РеализацияТоваровУслугТовары.Серия КАК Серия,
		|   	РеализацияТоваровУслугТовары.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|		ВЫБОР КОГДА &ВесВБазовыхЕИ 
		|            ТОГДА РеализацияТоваровУслугТовары.Количество
		|            ИНАЧЕ РеализацияТоваровУслугТовары.КоличествоУпаковок
		|       КОНЕЦ КАК Количество,
		|		0 КАК КоличествоПоРНПТ,
		|		РеализацияТоваровУслугТовары.Цена КАК Цена,
		|		ВЫБОР
		|			КОГДА СуммыВВалютеУчета.СуммаБезНДСРегл ЕСТЬ NULL 
		|				ТОГДА РеализацияТоваровУслугТовары.Сумма
		|			КОГДА РеализацияТоваровУслугТовары.Ссылка.ЦенаВключаетНДС
		|				ТОГДА СуммыВВалютеУчета.СуммаБезНДСРегл + СуммыВВалютеУчета.СуммаНДСРегл
		|			ИНАЧЕ СуммыВВалютеУчета.СуммаБезНДСРегл
		|		КОНЕЦ КАК Сумма,
		|		РеализацияТоваровУслугТовары.СтавкаНДС КАК СтавкаНДС,
		|		ЕСТЬNULL(СуммыВВалютеУчета.СуммаНДСРегл, РеализацияТоваровУслугТовары.СуммаНДС) КАК СуммаНДС,
		|		"""" КАК НомерГТД,
		|		"""" КАК СтранаПроисхождения,
		|		"""" КАК НомерГТДПредставление,
		|		"""" КАК НомерГТДРегистрационныйНомер,
		|		РеализацияТоваровУслугТовары.Ссылка.ЦенаВключаетНДС КАК СуммаВключаетНДС,
		|		ВЫБОР КОГДА &ВесВБазовыхЕИ или (РеализацияТоваровУслугТовары.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиНоменклатуры.ПустаяСсылка))
		|           ТОГДА РеализацияТоваровУслугТовары.Номенклатура.ЕдиницаИзмерения 
		|           ИНАЧЕ РеализацияТоваровУслугТовары.Упаковка.ЕдиницаИзмерения
		|       КОНЕЦ КАК ЕдиницаИзмерения,
		|		&ПолеУпаковка КАК Упаковка,
		|		ВЫБОР КОГДА &ВесВБазовыхЕИ ИЛИ (РеализацияТоваровУслугТовары.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиНоменклатуры.ПустаяСсылка))
		|           ТОГДА РеализацияТоваровУслугТовары.Номенклатура.ЕдиницаИзмерения.код 
		|           ИНАЧЕ РеализацияТоваровУслугТовары.Упаковка.ЕдиницаИзмерения.код
		|       КОНЕЦ КАК КодЕдиницыИзмерения,
		|		ЗНАЧЕНИЕ(Справочник.УпаковкиНоменклатуры.ПустаяСсылка) КАК ЕдиницаИзмеренияПрослеживаемости,
		|		РеализацияТоваровУслугТовары.Упаковка.ЕдиницаИзмерения КАК ВидУпаковки,
		|		" + ПолучитьТекстЗапроса_КоэффициентУпаковки("РеализацияТоваровУслугТовары") + "
		|			РеализацияТоваровУслугТовары.КоличествоУпаковок КАК КоличествоМест,
		|		ИСТИНА КАК ЭтоУслуга,
		|       ВЫБОР КОГДА РеализацияТоваровУслугТовары.Упаковка.Вес ЕСТЬ NULL 
		|       	ТОГДА РеализацияТоваровУслугТовары.Количество * " + ПолучитьТекстЗапроса_НоменклатураВес("РеализацияТоваровУслугТовары") + "
		|           ИНАЧЕ РеализацияТоваровУслугТовары.КоличествоУпаковок * РеализацияТоваровУслугТовары.Упаковка.Вес 
		|       КОНЕЦ Как Масса,
		|       РеализацияТоваровУслугТовары.Количество * &ТекстЗапросаВесНоменклатуры как МассаНетто, 
		|		&ТекстЗапросаМассаБрутто КАК Брутто,
		|		Ложь КАК ПрослеживаемыйТовар,
		|		1 КАК Порядок,
		|		ВЫБОР 
		|			КОГДА РеализацияТоваровУслугТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга) 
		|				ТОГДА ""Service"" 
		|			ИНАЧЕ ""Job"" 
		|		КОНЕЦ КАК ПризнакТоварРаботаУслуга,
		|		РеализацияТоваровУслугТовары.СуммаРучнойСкидки + РеализацияТоваровУслугТовары.СуммаАвтоматическойСкидки КАК СуммаСкидок
		|	ИЗ
		|		Документ.РеализацияТоваровУслуг.Товары КАК РеализацияТоваровУслугТовары
		|					ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыВВалютеУчета
		|			ПО  СуммыВВалютеУчета.Регистратор  = &Ссылка 
		|				И РеализацияТоваровУслугТовары.ИдентификаторСтроки = СуммыВВалютеУчета.ИдентификаторСтроки
		|			И (СуммыВВалютеУчета.Активность)
		|				И (&ПересчитыватьВВалютуРегл)
		|	ГДЕ
		|		РеализацияТоваровУслугТовары.Ссылка = &Ссылка
		|		И " + ПолучитьТекстЗапроса_ОтборПоУслугамРаботам() + "
		|		И " + ТекстЗапроса_ОтборНаМногооборотнуюТару("РеализацияТоваровУслугТовары.Ссылка", "РеализацияТоваровУслугТовары.Номенклатура") + "
		|	
		|	ОБЪЕДИНИТЬ ВСЕ
		|	
		|	"+ ПолучитьТекстЗапросаДляСпискаТоваровСГТД(СсылкаНаДокумент, КолонкаКодаТовара)+") КАК Подзапрос
		|";
		
	Если КонстантаВыводитьСерииВПечатныхФормах = Истина Тогда 
		
		Запрос.Текст = Запрос.Текст + "";
		
	Иначе 
		
		Запрос.Текст = Запрос.Текст + "
		|	СГРУППИРОВАТЬ ПО
		|	Подзапрос.Номенклатура,
		|	Подзапрос.НоменклатураПредставление,
		|	Подзапрос.Характеристика,
		|	Подзапрос.КодНоменклатуры,
		|	Подзапрос.Цена,
		|	Подзапрос.СтавкаНДС,
		|	Подзапрос.НомерГТД,
		|	Подзапрос.СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление,
		|	Подзапрос.НомерГТДРегистрационныйНомер,
		|	Подзапрос.СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения,
		|	Подзапрос.Упаковка,
		|	Подзапрос.КодЕдиницыИзмерения,
		|	Подзапрос.ЕдиницаИзмеренияПрослеживаемости.Наименование,
		|	Подзапрос.ЕдиницаИзмеренияПрослеживаемости.Код,
		|	Подзапрос.ВидУпаковки,
		|	Подзапрос.КоличествоВОдномМесте,
		|	Подзапрос.ЭтоУслуга,
		|	Подзапрос.Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга,
		|	Подзапрос.ПрослеживаемыйТовар
		|";
		
	КонецЕсли;	
	
	Запрос.Текст = Запрос.Текст + "
		|
		|УПОРЯДОЧИТЬ ПО
		|	порядок,
		|	НомерСтроки";
	
	Запрос.УстановитьПараметр("ПустаяЕИ"										, Справочники.УпаковкиЕдиницыИзмерения.ПустаяСсылка());
	Запрос.УстановитьПараметр("ВесВБазовыхЕИ"									, ИспользоватьБазовыеЕдиницыИзмерения());
	Запрос.УстановитьПараметр("ПересчитыватьВВалютуРегл"						, ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаДокумент));
	Запрос.УстановитьПараметр("ДатаДокумента"									, СсылкаНаДокумент.Дата);
	Запрос.УстановитьПараметр("ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров"	, ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров());
	
	Если 	(Метаданные.Имя = "УправлениеТорговлей" И МетодСервера(, "СравнитьВерсии", ПараметрыКлиентСервер.ВерсияКонфигурации, "11.2") < 0)
		Или (Метаданные.Имя = "УправлениеПредприятием" И МетодСервера(, "СравнитьВерсии", ПараметрыКлиентСервер.ВерсияКонфигурации, "2.1") < 0) Тогда
		
		ТекстЗапросаМассаБрутто = "
		|		ВЫБОР
		|			КОГДА &ЗаполненаЕдиницаИзмеренияВеса
		|				ТОГДА ВЫБОР
		|						КОГДА РеализацияТоваровУслугТовары.Упаковка.Вес ЕСТЬ NULL 
		|							ТОГДА РеализацияТоваровУслугТовары.Количество * РеализацияТоваровУслугТовары.Номенклатура.Вес
		|						ИНАЧЕ РеализацияТоваровУслугТовары.КоличествоУпаковок * РеализацияТоваровУслугТовары.Упаковка.Вес
		|					КОНЕЦ
		|			ИНАЧЕ 0
		|		КОНЕЦ";
		
		ТекстЗапросаВесНоменклатуры = "РеализацияТоваровУслугТовары.Номенклатура.Вес";
		
	Иначе
		
		ТекстЗапросаМассаБрутто = "
		|		ВЫБОР
		|			КОГДА &ЗаполненаЕдиницаИзмеренияВеса
		|				ТОГДА ВЫБОР
		|						КОГДА РеализацияТоваровУслугТовары.Упаковка.Вес ЕСТЬ NULL 
		|							ТОГДА РеализацияТоваровУслугТовары.Количество
		|						ИНАЧЕ ВЫБОР 
		|							КОГДА РеализацияТоваровУслугТовары.Упаковка.ТипИзмеряемойВеличины = Значение(Перечисление.ТипыИзмеряемыхВеличин.Вес)
		|								ТОГДА 0
		|							ИНАЧЕ РеализацияТоваровУслугТовары.КоличествоУпаковок
		|						КОНЕЦ
		|					КОНЕЦ * " + ТекстЗапросаВесУпаковки("РеализацияТоваровУслугТовары.Упаковка", "РеализацияТоваровУслугТовары.Номенклатура") + "
		|			ИНАЧЕ 0
		|		КОНЕЦ";
		
		ТекстЗапросаВесНоменклатуры = ТекстЗапросаВесУпаковки("РеализацияТоваровУслугТовары.Номенклатура.ЕдиницаИзмерения", "РеализацияТоваровУслугТовары.Номенклатура");

	КонецЕсли;
	
	
	ПсевдонимТаблицы = "РеализацияТоваровУслугТовары";
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеЦена", ВыражениеЦена(ПсевдонимТаблицы));
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеУпаковка(ПсевдонимТаблицы));
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ТекстЗапросаМассаБрутто", ТекстЗапросаМассаБрутто);
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ТекстЗапросаВесНоменклатуры", ТекстЗапросаВесНоменклатуры);
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоАктВыполненныхРабот(Запрос, КолонкаКодаТовара, СсылкаНаДокумент, ВалютаРегламентированногоУчета)
	
	Запрос.Текст = "ВЫБРАТЬ
		|	Подзапрос.НомерСтроки 				КАК НомерСтроки,
		|	Подзапрос.Номенклатура 				КАК Номенклатура,
		|	Подзапрос.НоменклатураПредставление КАК НоменклатураПредставление,
		|	Подзапрос.Содержание 				КАК Содержание,
		|	Подзапрос.Характеристика 			КАК Характеристика,
		|	НЕОПРЕДЕЛЕНО 						КАК Серия,
		|   Подзапрос.КодНоменклатуры 			КАК КодНоменклатуры, 
		|	Подзапрос.Количество 				КАК Количество,
		|	Подзапрос.Цена 						КАК Цена,
		|	Подзапрос.Сумма 					КАК Сумма,
		|	Подзапрос.СтавкаНДС 				КАК СтавкаНДС,
		|	Подзапрос.СуммаНДС 					КАК СуммаНДС,
		|	Подзапрос.НомерГТД 					КАК НомерГТД,
		|	Подзапрос.СтранаПроисхождения 		КАК СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление 	КАК НомерГТДПредставление,
		|	Подзапрос.СуммаВключаетНДС 			КАК СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения 			КАК ЕдиницаИзмерения,
		|	&ПолеУпаковка	 					КАК Упаковка,
		|	Подзапрос.КодЕдиницыИзмерения 		КАК КодЕдиницыИзмерения,
		|	"""" 								КАК ВидУпаковки,
		|	0 									КАК КоличествоВОдномМесте,
		|	0 									КАК КоличествоМест,
		|	Подзапрос.ЭтоУслуга 				КАК ЭтоУслуга,
		|	Подзапрос.Порядок 					КАК Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга 	КАК ПризнакТоварРаботаУслуга,
		|	Подзапрос.СуммаСкидок 				КАК СуммаСкидок
		|ИЗ
		|	(ВЫБРАТЬ
		|		АктВыполненныхРаботУслуги.НомерСтроки 	КАК НомерСтроки,
		|		АктВыполненныхРаботУслуги.Номенклатура 	КАК Номенклатура,
		|		ВЫБОР
		|			КОГДА ПОДСТРОКА(АктВыполненныхРаботУслуги.Номенклатура.НаименованиеПолное, 1, 10) = """"
		|				ТОГДА АктВыполненныхРаботУслуги.Номенклатура.Наименование
		|			ИНАЧЕ АктВыполненныхРаботУслуги.Номенклатура.НаименованиеПолное
		|		КОНЕЦ КАК НоменклатураПредставление,
		|		АктВыполненныхРаботУслуги.Содержание 	 КАК Содержание,
		|		АктВыполненныхРаботУслуги.Характеристика КАК Характеристика,
		|   	АктВыполненныхРаботУслуги.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|		АктВыполненныхРаботУслуги.Количество 	КАК Количество,
		|		АктВыполненныхРаботУслуги.Цена 			КАК Цена,
		|		ВЫБОР
		|			КОГДА СуммыВВалютеУчета.СуммаБезНДСРегл ЕСТЬ NULL 
		|				ТОГДА АктВыполненныхРаботУслуги.Сумма
		|			КОГДА АктВыполненныхРаботУслуги.Ссылка.ЦенаВключаетНДС
		|				ТОГДА СуммыВВалютеУчета.СуммаБезНДСРегл + СуммыВВалютеУчета.СуммаНДСРегл
		|			ИНАЧЕ СуммыВВалютеУчета.СуммаБезНДСРегл
		|		КОНЕЦ 									КАК Сумма,
		|		АктВыполненныхРаботУслуги.СтавкаНДС 	КАК СтавкаНДС,
		|		ЕСТЬNULL(СуммыВВалютеУчета.СуммаНДСРегл, АктВыполненныхРаботУслуги.СуммаНДС) КАК СуммаНДС,
		|		"""" КАК НомерГТД,
		|		"""" КАК СтранаПроисхождения,
		|		"""" КАК НомерГТДПредставление,
		|		АктВыполненныхРаботУслуги.Ссылка.ЦенаВключаетНДС 		КАК СуммаВключаетНДС,
		|		АктВыполненныхРаботУслуги.Номенклатура.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
		|		АктВыполненныхРаботУслуги.Номенклатура.ЕдиницаИзмерения.Код КАК КодЕдиницыИзмерения,
		|		ИСТИНА КАК ЭтоУслуга,
		|		1 КАК Порядок,
		|		ВЫБОР 
		|			КОГДА АктВыполненныхРаботУслуги.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга) 
		|				ТОГДА ""Service"" 
		|			ИНАЧЕ ""Job"" 
		|		КОНЕЦ КАК ПризнакТоварРаботаУслуга,
		|		АктВыполненныхРаботУслуги.СуммаРучнойСкидки + АктВыполненныхРаботУслуги.СуммаАвтоматическойСкидки КАК СуммаСкидок
		|	ИЗ
		|		Документ.АктВыполненныхРабот.Услуги КАК АктВыполненныхРаботУслуги
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыВВалютеУчета
		|			ПО СуммыВВалютеУчета.Регистратор = &Ссылка 
		|				И АктВыполненныхРаботУслуги.ИдентификаторСтроки = СуммыВВалютеУчета.ИдентификаторСтроки
		|			И (СуммыВВалютеУчета.Активность)
		|				И (&ПересчитыватьВВалютуРегл)
		|	ГДЕ
		|		АктВыполненныхРаботУслуги.Ссылка = &Ссылка
		|	
		|	) КАК Подзапрос
		|
		|УПОРЯДОЧИТЬ ПО
		|	Порядок,
		|	НомерСтроки";
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеПустаяУпаковка());
	Запрос.УстановитьПараметр("ПересчитыватьВВалютуРегл", ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаДокумент));
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоКорректировкаРеализации(Запрос, КолонкаКодаТовара)
	
	Запрос.Текст = "ВЫБРАТЬ
		|	Подзапрос.НомерСтроки				КАК НомерСтроки,
		|	Подзапрос.Номенклатура				КАК Номенклатура,
		|	Подзапрос.НоменклатураПредставление КАК НоменклатураПредставление,
		|	Подзапрос.Содержание 				КАК Содержание,
		|	Подзапрос.Характеристика			КАК Характеристика,
		|	Подзапрос.Серия						КАК Серия,
		|	Подзапрос.КодНоменклатуры			КАК КодНоменклатуры,
		|	Подзапрос.Количество				КАК Количество,
		|	Подзапрос.Цена 						КАК Цена,
		|	Подзапрос.Сумма 					КАК Сумма,
		|	Подзапрос.СтавкаНДС 				КАК СтавкаНДС,
		|	Подзапрос.СуммаНДС 					КАК СуммаНДС,
		|	Подзапрос.НомерГТД 					КАК НомерГТД,
		|	Подзапрос.СтранаПроисхождения 		КАК СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление 	КАК НомерГТДПредставление,
		|	Подзапрос.СуммаВключаетНДС 			КАК СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения 			КАК ЕдиницаИзмерения,
		|	Подзапрос.Упаковка 					КАК Упаковка,
		|	Подзапрос.КодЕдиницыИзмерения 		КАК КодЕдиницыИзмерения,
		|	"""" 								КАК ВидУпаковки,
		|	0 									КАК КоличествоВОдномМесте,
		|	0 									КАК КоличествоМест,
		|	Подзапрос.ЭтоУслуга 				КАК ЭтоУслуга,
		|	Подзапрос.Порядок 					КАК Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга 	КАК ПризнакТоварРаботаУслуга
		|ИЗ
		|	(ВЫБРАТЬ
		|		КорректировкаРеализацииТовары.НомерСтроки	КАК НомерСтроки,
		|		КорректировкаРеализацииТовары.Номенклатура	КАК Номенклатура,
		|		ВЫБОР
		|			КОГДА ПОДСТРОКА(КорректировкаРеализацииТовары.Номенклатура.НаименованиеПолное, 1, 10) = """"
		|				ТОГДА КорректировкаРеализацииТовары.Номенклатура.Наименование
		|			ИНАЧЕ КорректировкаРеализацииТовары.Номенклатура.НаименованиеПолное
		|		КОНЕЦ 											КАК НоменклатураПредставление,
		|		КорректировкаРеализацииТовары.Содержание		КАК Содержание,
		|		КорректировкаРеализацииТовары.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|		КорректировкаРеализацииТовары.Характеристика	КАК Характеристика,
		|		КорректировкаРеализацииТовары.Серия				КАК Серия,
		|		ВЫБОР КОГДА &ВесВБазовыхЕИ 
		|            ТОГДА КорректировкаРеализацииТовары.Количество
		|            ИНАЧЕ КорректировкаРеализацииТовары.КоличествоУпаковок
		|       КОНЕЦ КАК Количество,
		|		КорректировкаРеализацииТовары.Цена				КАК Цена,
		|		КорректировкаРеализацииТовары.Сумма				КАК Сумма,
		|		КорректировкаРеализацииТовары.СтавкаНДС			КАК СтавкаНДС,
		|		КорректировкаРеализацииТовары.СуммаНДС			КАК СуммаНДС,
		|		"""" 											КАК НомерГТД,
		|		"""" 											КАК СтранаПроисхождения,
		|		"""" 											КАК НомерГТДПредставление,
		|		КорректировкаРеализацииТовары.Ссылка.ЦенаВключаетНДС 			КАК СуммаВключаетНДС,
		|		ВЫБОР КОГДА &ВесВБазовыхЕИ или (КорректировкаРеализацииТовары.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиНоменклатуры.ПустаяСсылка))
		|           ТОГДА КорректировкаРеализацииТовары.Номенклатура.ЕдиницаИзмерения 
		|           ИНАЧЕ КорректировкаРеализацииТовары.Упаковка.ЕдиницаИзмерения
		|       КОНЕЦ КАК ЕдиницаИзмерения,
		|		&ПолеУпаковка 							КАК Упаковка,
		|		ВЫБОР КОГДА &ВесВБазовыхЕИ ИЛИ (КорректировкаРеализацииТовары.Упаковка = ЗНАЧЕНИЕ(Справочник.УпаковкиНоменклатуры.ПустаяСсылка))
		|           ТОГДА КорректировкаРеализацииТовары.Номенклатура.ЕдиницаИзмерения.код 
		|           ИНАЧЕ КорректировкаРеализацииТовары.Упаковка.ЕдиницаИзмерения.код
		|       КОНЕЦ КАК КодЕдиницыИзмерения,
		|		ВЫБОР 
		|			КОГДА КорректировкаРеализацииТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар) 
		|				ТОГДА ЛОЖЬ
		|			ИНАЧЕ ИСТИНА
		|		КОНЕЦ КАК ЭтоУслуга,
		|		1 		КАК Порядок,
		|		ВЫБОР 
		|			КОГДА КорректировкаРеализацииТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Товар) 
		|				ТОГДА ""Property"" 
		|			КОГДА КорректировкаРеализацииТовары.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга) 
		|				ТОГДА ""Service"" 
		|			ИНАЧЕ ""Job"" 
		|		КОНЕЦ КАК ПризнакТоварРаботаУслуга
		|	ИЗ
		|		Документ.КорректировкаРеализации.Товары КАК КорректировкаРеализацииТовары
		|	ГДЕ
		|		КорректировкаРеализацииТовары.Ссылка = &Ссылка) КАК Подзапрос
		|
		|УПОРЯДОЧИТЬ ПО
		|	Порядок,
		|	НомерСтроки";
	
	ПсевдонимТаблицы = "КорректировкаРеализацииТовары";
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеЦена", ВыражениеЦена(ПсевдонимТаблицы));
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеУпаковка(ПсевдонимТаблицы));
	Запрос.УстановитьПараметр("ВесВБазовыхЕИ", ИспользоватьБазовыеЕдиницыИзмерения());

КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоОтчетКомитенту(Запрос, КолонкаКодаТовара)
	
	Запрос.Текст = "ВЫБРАТЬ
		|	1 										КАК НомерСтроки,
		|	ОтчетКомитенту.СуммаВознаграждения		КАК Сумма,
		|	ОтчетКомитенту.СуммаНДСВознаграждения	КАК СуммаНДС,
		|	ОтчетКомитенту.СтавкаНДСВознаграждения	КАК СтавкаНДС,
		|	ОтчетКомитенту.Услуга					КАК Номенклатура,
		|	ВЫБОР
		|		КОГДА ОтчетКомитенту.Услуга = ЗНАЧЕНИЕ(Справочник.номенклатура.ПустаяСсылка)
		|			ТОГДА ""Комиссионное вознаграждение""
		|		КОГДА ПОДСТРОКА(ОтчетКомитенту.Услуга.НаименованиеПолное, 1, 10) = """"
		|			ТОГДА ОтчетКомитенту.Услуга.Наименование
		|		ИНАЧЕ ОтчетКомитенту.Услуга.НаименованиеПолное
		|	КОНЕЦ 									КАК НоменклатураПредставление,
		|	ВЫБОР
		|		КОГДА ОтчетКомитенту.Услуга = ЗНАЧЕНИЕ(Справочник.номенклатура.ПустаяСсылка)
		|			ТОГДА """"
		|		ИНАЧЕ ОтчетКомитенту.Услуга." + КолонкаКодаТовара + "
		|	КОНЕЦ 							КАК КодНоменклатуры,
		|	НЕОПРЕДЕЛЕНО 					КАК Характеристика,
		|	НЕОПРЕДЕЛЕНО 					КАК Серия,
		|	0 								КАК Количество,
		|	"""" 							КАК Цена,
		|	"""" 							КАК НомерГТД,
		|	"""" 							КАК СтранаПроисхождения,
		|	"""" 							КАК НомерГТДПредставление,
		|	ОтчетКомитенту.ЦенаВключаетНДС 	КАК СуммаВключаетНДС,
		|	"""" 							КАК ЕдиницаИзмерения,
		|	&ПолеУпаковка					КАК Упаковка,
		|	"""" 							КАК КодЕдиницыИзмерения,
		|	"""" 							КАК ВидУпаковки,
		|	0 								КАК КоличествоВОдномМесте,
		|	0 								КАК КоличествоМест,
		|	ИСТИНА							КАК ЭтоУслуга,
		|	1 								КАК порядок,
		|	""Service"" 					КАК ПризнакТоварРаботаУслуга
		|ИЗ
		|	Документ.ОтчетКомитенту КАК ОтчетКомитенту
		|ГДЕ
		|	ОтчетКомитенту.Ссылка = &Ссылка";
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеПустаяУпаковка());
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоВозвратТоваровПоставщику(Запрос, КолонкаКодаТовара, СсылкаНаДокумент)
	
	Запрос.Текст = "ВЫБРАТЬ
		|	Подзапрос.НомерСтроки				КАК НомерСтроки,
		|	Подзапрос.Номенклатура				КАК Номенклатура,
		|	Подзапрос.НоменклатураПредставление КАК НоменклатураПредставление,
		|	Подзапрос.Характеристика 			КАК Характеристика,
		|	Подзапрос.Серия 					КАК Серия,
		|   Подзапрос.КодНоменклатуры 			КАК КодНоменклатуры, 
		|	Подзапрос.Количество 				КАК Количество,
		|	Подзапрос.Цена 						КАК Цена,
		|	Подзапрос.Сумма 					КАК Сумма,
		|	Подзапрос.СтавкаНДС 				КАК СтавкаНДС,
		|	Подзапрос.СуммаНДС 					КАК СуммаНДС,
		|	Подзапрос.НомерГТД 					КАК НомерГТД,
		|	Подзапрос.СтранаПроисхождения 		КАК СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление 	КАК НомерГТДПредставление,
		|	Подзапрос.СуммаВключаетНДС 			КАК СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения 			КАК ЕдиницаИзмерения,
		|	Подзапрос.Упаковка 					КАК Упаковка,
		|	Подзапрос.КодЕдиницыИзмерения 		КАК КодЕдиницыИзмерения,
		|	Подзапрос.ВидУпаковки 				КАК ВидУпаковки,
		|	Подзапрос.КоличествоВОдномМесте 	КАК КоличествоВОдномМесте,
		|	Подзапрос.КоличествоМест 			КАК КоличествоМест,
		|	Подзапрос.ЭтоУслуга 				КАК ЭтоУслуга,
		|	Подзапрос.порядок 					КАК Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга 	КАК ПризнакТоварРаботаУслуга
		|ИЗ
		|	(ВЫБРАТЬ
		|		ВозвратТоваровПоставщикуТовары.НомерСтроки 	КАК НомерСтроки,
		|		ВозвратТоваровПоставщикуТовары.Номенклатура КАК Номенклатура,
		|		ВЫБОР
		|			КОГДА ПОДСТРОКА(ВозвратТоваровПоставщикуТовары.Номенклатура.НаименованиеПолное, 1, 10) = """"
		|				ТОГДА ВозвратТоваровПоставщикуТовары.Номенклатура.Наименование
		|			ИНАЧЕ ВозвратТоваровПоставщикуТовары.Номенклатура.НаименованиеПолное
		|		КОНЕЦ 											КАК НоменклатураПредставление,
		|		ВозвратТоваровПоставщикуТовары.Характеристика 	КАК Характеристика,
		|		ВозвратТоваровПоставщикуТовары.Серия 			КАК Серия,
		|   	ВозвратТоваровПоставщикуТовары.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|		ВозвратТоваровПоставщикуТовары.Количество 		КАК Количество,
		// ++ для совместимости с функцией ПолучитьТекстЗапросаДляСпискаТоваровСГТД()
		|		0 КАК КоличествоПоРНПТ,
		// -- для совместимости
		|		ВозвратТоваровПоставщикуТовары.Цена 			КАК Цена,
		|		ВозвратТоваровПоставщикуТовары.Сумма 			КАК Сумма,
		|		ВозвратТоваровПоставщикуТовары.СтавкаНДС 		КАК СтавкаНДС,
		|		ВозвратТоваровПоставщикуТовары.СуммаНДС 		КАК СуммаНДС,
		|		"""" КАК НомерГТД,
		|		"""" КАК СтранаПроисхождения,
		|		"""" КАК НомерГТДПредставление,
		// ++ для совместимости с функцией ПолучитьТекстЗапросаДляСпискаТоваровСГТД()
		|		"""" КАК НомерГТДРегистрационныйНомер,
		// -- для совместимости	
		|		ВозвратТоваровПоставщикуТовары.Ссылка.ЦенаВключаетНДС 				КАК СуммаВключаетНДС,
		|		ВозвратТоваровПоставщикуТовары.Номенклатура.ЕдиницаИзмерения 		КАК ЕдиницаИзмерения,
		|		&ПолеУпаковка							 							КАК Упаковка,
		|		ВозвратТоваровПоставщикуТовары.Номенклатура.ЕдиницаИзмерения.Код 	КАК КодЕдиницыИзмерения,
		// ++ для совместимости с функцией ПолучитьТекстЗапросаДляСпискаТоваровСГТД()
		|		"""" КАК ЕдиницаИзмеренияПрослеживаемости,
		// -- для совместимости				
		|		ВозвратТоваровПоставщикуТовары.Упаковка 							КАК ВидУпаковки,
		|		" + ПолучитьТекстЗапроса_КоэффициентУпаковки("ВозвратТоваровПоставщикуТовары") + "
		|		ВозвратТоваровПоставщикуТовары.КоличествоУпаковок КАК КоличествоМест,
		|		ЛОЖЬ КАК ЭтоУслуга,
		// ++ для совместимости с функцией ПолучитьТекстЗапросаДляСпискаТоваровСГТД()
		|		0 КАК Масса,
		|		0 КАК МассаНетто,
		|		0 КАК МассаБрутто,
		|		Ложь КАК ПрослеживаемыйТовар,
		// -- для совместимости
		|		1 КАК порядок,
		|		""Property"" КАК ПризнакТоварРаботаУслуга,
		// ++ для совместимости с функцией ПолучитьТекстЗапросаДляСпискаТоваровСГТД()
		|		0 КАК СуммаСкидок
		// -- для совместимости
		|	ИЗ
		|		Документ.ВозвратТоваровПоставщику.Товары КАК ВозвратТоваровПоставщикуТовары
		|	ГДЕ
		|		ВозвратТоваровПоставщикуТовары.Ссылка = &Ссылка
		|		И "+ПолучитьТекстЗапроса_ОтборПоУслугамРаботам() + "
		|	
		|	ОБЪЕДИНИТЬ ВСЕ
		|	
		|	" + ПолучитьТекстЗапросаДляСпискаТоваровСГТД(СсылкаНаДокумент, КолонкаКодаТовара) + ") КАК Подзапрос
		|
		|УПОРЯДОЧИТЬ ПО
		|	Порядок,
		|	НомерСтроки";
	
	ПсевдонимТаблицы = "ВозвратТоваровПоставщикуТовары";
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеЦена", ВыражениеЦена(ПсевдонимТаблицы));
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеУпаковка(ПсевдонимТаблицы));
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст
		, "Документ.РеализацияТоваровУслуг.ВидыЗапасов"
		, "Документ.ВозвратТоваровПоставщику.ВидыЗапасов");
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст
		, "РеализацияТоваровУслугТовары"
		, "ВозвратТоваровПоставщикуТовары");
		
	Запрос.УстановитьПараметр("ПустаяЕИ"				, Справочники.УпаковкиЕдиницыИзмерения.ПустаяСсылка());
	Запрос.УстановитьПараметр("ВесВБазовыхЕИ"			, ИспользоватьБазовыеЕдиницыИзмерения());
	Запрос.УстановитьПараметр("ПересчитыватьВВалютуРегл", ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаДокумент));
	Запрос.УстановитьПараметр("ДатаДокумента"			, СсылкаНаДокумент.Дата);
	Запрос.УстановитьПараметр("ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров", ДатаНачалаУчетаПрослеживаемыхИмпортныхТоваров());

КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоЗаказКлиента(Запрос, КолонкаКодаТовара)
	
	Запрос.Текст = "ВЫБРАТЬ
		|	Подзапрос.НомерСтроки 				КАК НомерСтроки,
		|	Подзапрос.Номенклатура				КАК Номенклатура,
		|	Подзапрос.НоменклатураПредставление КАК НоменклатураПредставление,
		|	Подзапрос.Содержание				КАК Содержание,
		|	Подзапрос.Характеристика 			КАК Характеристика,
		|	Подзапрос.Серия 					КАК Серия,
		|   Подзапрос.КодНоменклатуры 			КАК КодНоменклатуры, 
		|	Подзапрос.Количество 				КАК Количество,
		|	Подзапрос.Цена 						КАК Цена,
		|	Подзапрос.Сумма 					КАК Сумма,
		|	Подзапрос.СтавкаНДС 				КАК СтавкаНДС,
		|	Подзапрос.СуммаНДС 					КАК СуммаНДС,
		|	Подзапрос.НомерГТД 					КАК НомерГТД,
		|	Подзапрос.СтранаПроисхождения 		КАК СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление 	КАК НомерГТДПредставление,
		|	Подзапрос.СуммаВключаетНДС 			КАК СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения 			КАК ЕдиницаИзмерения,
		|	Подзапрос.Упаковка 					КАК Упаковка,
		|	Подзапрос.КодЕдиницыИзмерения 		КАК КодЕдиницыИзмерения,
		|	Подзапрос.ВидУпаковки 				КАК ВидУпаковки,
		|	Подзапрос.КоличествоВОдномМесте 	КАК КоличествоВОдномМесте,
		|	Подзапрос.КоличествоМест 			КАК КоличествоМест,
		|	Подзапрос.ЭтоУслуга 				КАК ЭтоУслуга,
		|	Подзапрос.порядок 					КАК Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга 	КАК ПризнакТоварРаботаУслуга,
		|	Подзапрос.СуммаСкидок 				КАК СуммаСкидок
		|ИЗ
		|	(ВЫБРАТЬ
		|		ЗаказКлиентаТовары.НомерСтроки	КАК НомерСтроки,
		|		ЗаказКлиентаТовары.Номенклатура КАК Номенклатура,
		|		ВЫБОР
		|			КОГДА ПОДСТРОКА(ЗаказКлиентаТовары.Номенклатура.НаименованиеПолное, 1, 10) = """"
		|				ТОГДА ЗаказКлиентаТовары.Номенклатура.Наименование
		|			ИНАЧЕ ЗаказКлиентаТовары.Номенклатура.НаименованиеПолное
		|		КОНЕЦ 								КАК НоменклатураПредставление,
		|		ЗаказКлиентаТовары.Содержание 		КАК Содержание,
		|		ЗаказКлиентаТовары.Характеристика 	КАК Характеристика,
		|		ЗаказКлиентаТовары.Серия 			КАК Серия,
		|   	ЗаказКлиентаТовары.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|		ЗаказКлиентаТовары.Количество 		КАК Количество,
		|		ЗаказКлиентаТовары.Цена 			КАК Цена,
		|		ЗаказКлиентаТовары.Сумма 			КАК Сумма,
		|		ЗаказКлиентаТовары.СтавкаНДС 		КАК СтавкаНДС,
		|		ЗаказКлиентаТовары.СуммаНДС 		КАК СуммаНДС,
		|		"""" КАК НомерГТД,
		|		"""" КАК СтранаПроисхождения,
		|		"""" КАК НомерГТДПредставление,
		|		ЗаказКлиентаТовары.Ссылка.ЦенаВключаетНДС 				КАК СуммаВключаетНДС,
		|		ЗаказКлиентаТовары.Номенклатура.ЕдиницаИзмерения 		КАК ЕдиницаИзмерения,
		|		&ПолеУпаковка 											КАК Упаковка,
		|		ЗаказКлиентаТовары.Номенклатура.ЕдиницаИзмерения.Код 	КАК КодЕдиницыИзмерения,
		|		ЗаказКлиентаТовары.Упаковка 							КАК ВидУпаковки,
		|		" + ПолучитьТекстЗапроса_КоэффициентУпаковки("ЗаказКлиентаТовары") + "
		|		ЗаказКлиентаТовары.КоличествоУпаковок 					КАК КоличествоМест,
		|		ЛОЖЬ 			КАК ЭтоУслуга,
		|		1 				КАК порядок,
		|		""Property"" 	КАК ПризнакТоварРаботаУслуга,
		|		ЗаказКлиентаТовары.СуммаРучнойСкидки + ЗаказКлиентаТовары.СуммаАвтоматическойСкидки КАК СуммаСкидок
		|	ИЗ
		|		Документ.ЗаказКлиента.Товары КАК ЗаказКлиентаТовары
		|	ГДЕ
		|		ЗаказКлиентаТовары.Ссылка = &Ссылка И ЗаказКлиентаТовары.Отменено = ЛОЖЬ
		|	
		|	) КАК Подзапрос
		|
		|УПОРЯДОЧИТЬ ПО
		|	Порядок,
		|	НомерСтроки";
	
	ПсевдонимТаблицы = "ЗаказКлиентаТовары";
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеЦена", ВыражениеЦена(ПсевдонимТаблицы));
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеУпаковка(ПсевдонимТаблицы));
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоСчетФактураВыданныйАванс(Запрос, КолонкаКодаТовара)
	
	Запрос.Текст =    
		"ВЫБРАТЬ
		|	СчетФактураВыданныйАвансы.НомерСтроки	КАК НомерСтроки,
		|	СчетФактураВыданныйАвансы.Номенклатура	КАК Номенклатура,
		|	ВЫБОР
		|		КОГДА СчетФактураВыданныйАвансы.Номенклатура <> ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
		|            И ПОДСТРОКА(СчетФактураВыданныйАвансы.Номенклатура.НаименованиеПолное, 1, 10) <> """"
		|			ТОГДА СчетФактураВыданныйАвансы.Номенклатура.НаименованиеПолное
		|		КОГДА СчетФактураВыданныйАвансы.Номенклатура <> ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)  
		|			ТОГДА СчетФактураВыданныйАвансы.Номенклатура.Наименование
		|		ИНАЧЕ ""Предварительная оплата""
		|	КОНЕЦ 			КАК НоменклатураПредставление,
		|	СчетФактураВыданныйАвансы.Содержание 	КАК Содержание,
		|	НЕОПРЕДЕЛЕНО 	КАК Характеристика,
		|	НЕОПРЕДЕЛЕНО 	КАК Серия,
		|  	СчетФактураВыданныйАвансы.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|	0 			КАК Количество,
		|	0 			КАК Цена,
		|	СчетФактураВыданныйАвансы.Сумма,
		|	СчетФактураВыданныйАвансы.СтавкаНДС,
		|	СчетФактураВыданныйАвансы.СуммаНДС,
		|	"""" 		КАК НомерГТД,
		|	"""" 		КАК СтранаПроисхождения,
		|	"""" 		КАК НомерГТДПредставление,
		|	ИСТИНА 		КАК СуммаВключаетНДС,
		|	"""" 		КАК ЕдиницаИзмерения,
		|	&ПолеУпаковка КАК Упаковка,
		|	"""" 		КАК КодЕдиницыИзмерения,
		|	"""" 		КАК ВидУпаковки,
		|	0 			КАК КоличествоВОдномМесте,
		|	0 			КАК КоличествоМест,
		|	ИСТИНА 		КАК ЭтоУслуга,
		|	""Service""	КАК ПризнакТоварРаботаУслуга
		|ИЗ
		|	Документ.СчетФактураВыданныйАванс.Авансы КАК СчетФактураВыданныйАвансы
		|ГДЕ
		|	СчетФактураВыданныйАвансы.Ссылка = &Ссылка
		|
		|УПОРЯДОЧИТЬ ПО
		|	НомерСтроки";
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеПустаяУпаковка());
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ОтчетДавальцу(Запрос, СсылкаНаДокумент)
	
	Запрос.УстановитьПараметр("ПересчитыватьВВалютуРегл", ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаДокумент));
	
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	1 КАК НомерСтроки,
	|	Подзапрос.Номенклатура КАК Номенклатура,
	|	МАКСИМУМ(Подзапрос.НоменклатураПредставление) КАК НоменклатураПредставление,
	|	МАКСИМУМ(Подзапрос.Содержание) КАК Содержание,
	|	Подзапрос.Характеристика КАК Характеристика,
	|	НЕОПРЕДЕЛЕНО КАК Серия,
	|	МАКСИМУМ(Подзапрос.КодНоменклатуры) КАК КодНоменклатуры,
	|	1 КАК Количество,
	|	СУММА(Подзапрос.Сумма) КАК Цена,
	|	СУММА(Подзапрос.Сумма) КАК Сумма,
	|	Подзапрос.СтавкаНДС КАК СтавкаНДС,
	|	СУММА(Подзапрос.СуммаНДС) КАК СуммаНДС,
	|	НЕОПРЕДЕЛЕНО КАК НомерГТД,
	|	НЕОПРЕДЕЛЕНО КАК СтранаПроисхождения,
	|	НЕОПРЕДЕЛЕНО КАК НомерГТДПредставление,
	|	Подзапрос.СуммаВключаетНДС КАК СуммаВключаетНДС,
	|	Подзапрос.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
	|	Подзапрос.Упаковка КАК Упаковка,
	|	МАКСИМУМ(Подзапрос.КодЕдиницыИзмерения) КАК КодЕдиницыИзмерения,
	|	"""" КАК ВидУпаковки,
	|	0 КАК КоличествоВОдномМесте,
	|	0 КАК КоличествоМест,
	|	ИСТИНА КАК ЭтоУслуга,
	|	МАКСИМУМ(Подзапрос.ПризнакТоварРаботаУслуга) КАК ПризнакТоварРаботаУслуга,
	|	0 КАК СуммаСкидок
	|ИЗ
	|	(ВЫБРАТЬ
	|		ДанныеТЧ.Ссылка.Номенклатура КАК Номенклатура,
	|		ДанныеТЧ.Ссылка.Содержание КАК НоменклатураПредставление,
	|		ДанныеТЧ.Ссылка.Содержание КАК Содержание,
	|		ДанныеТЧ.Ссылка.Характеристика КАК Характеристика,
	|		ДанныеТЧ.Ссылка.Номенклатура.Код КАК КодНоменклатуры,
	|		ДанныеТЧ.Цена КАК Цена,
	|		ВЫБОР
	|			КОГДА СуммыВВалютеУчета.СуммаБезНДСРегл ЕСТЬ NULL
	|				ТОГДА ДанныеТЧ.Сумма
	|			КОГДА ДанныеТЧ.Ссылка.ЦенаВключаетНДС
	|				ТОГДА СуммыВВалютеУчета.СуммаБезНДСРегл + СуммыВВалютеУчета.СуммаНДСРегл
	|			ИНАЧЕ СуммыВВалютеУчета.СуммаБезНДСРегл
	|		КОНЕЦ КАК Сумма,
	|		ДанныеТЧ.Ссылка.СтавкаНДС КАК СтавкаНДС,
	|		ЕСТЬNULL(СуммыВВалютеУчета.СуммаНДСРегл, ДанныеТЧ.СуммаНДС) КАК СуммаНДС,
	|		ДанныеТЧ.Ссылка.ЦенаВключаетНДС КАК СуммаВключаетНДС,
	|		ДанныеТЧ.Ссылка.Номенклатура.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
	|		&ПолеУпаковка КАК Упаковка,
	|		ДанныеТЧ.Ссылка.Номенклатура.ЕдиницаИзмерения.Код КАК КодЕдиницыИзмерения,
	|		ВЫБОР
	|			КОГДА ДанныеТЧ.Ссылка.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга)
	|				ТОГДА ""Service""
	|			ИНАЧЕ ""Job""
	|		КОНЕЦ КАК ПризнакТоварРаботаУслуга
	|	ИЗ
	|		Документ.ОтчетДавальцу.Продукция КАК ДанныеТЧ
	|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыВВалютеУчета
	|			ПО (СуммыВВалютеУчета.Регистратор = &Ссылка)
	|				И ДанныеТЧ.ИдентификаторСтроки = СуммыВВалютеУчета.ИдентификаторСтроки
	|				И (СуммыВВалютеУчета.Активность)
	|				И (&ПересчитыватьВВалютуРегл)
	|	ГДЕ
	|		ДанныеТЧ.Ссылка = &Ссылка) КАК Подзапрос
	|
	|СГРУППИРОВАТЬ ПО
	|	Подзапрос.Номенклатура,
	|	Подзапрос.Характеристика,
	|	Подзапрос.СтавкаНДС,
	|	Подзапрос.ЕдиницаИзмерения,
	|	Подзапрос.Упаковка,
	|	Подзапрос.СуммаВключаетНДС";
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеПустаяУпаковка());
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_РеализацияУслугПрочихАктивов(Запрос, СсылкаНаДокумент)
	
	Запрос.УстановитьПараметр("ПересчитыватьВВалютуРегл", ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаДокумент));
	
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Подзапрос.НомерСтроки КАК НомерСтроки,
	|	НЕОПРЕДЕЛЕНО КАК Номенклатура,
	|	Подзапрос.НоменклатураПредставление КАК НоменклатураПредставление,
	|	Подзапрос.Содержание КАК Содержание,
	|	НЕОПРЕДЕЛЕНО КАК Характеристика,
	|	НЕОПРЕДЕЛЕНО КАК Серия,
	|	НЕОПРЕДЕЛЕНО КАК КодНоменклатуры,
	|	Подзапрос.Количество КАК Количество,
	|	Подзапрос.Цена КАК Цена,
	|	Подзапрос.Сумма КАК Сумма,
	|	Подзапрос.СтавкаНДС КАК СтавкаНДС,
	|	Подзапрос.СуммаНДС КАК СуммаНДС,
	|	НЕОПРЕДЕЛЕНО КАК НомерГТД,
	|	НЕОПРЕДЕЛЕНО КАК СтранаПроисхождения,
	|	НЕОПРЕДЕЛЕНО КАК НомерГТДПредставление,
	|	Подзапрос.СуммаВключаетНДС КАК СуммаВключаетНДС,
	|	Подзапрос.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
	|	Подзапрос.Упаковка КАК Упаковка,
	|	Подзапрос.КодЕдиницыИзмерения КАК КодЕдиницыИзмерения,
	|	"""" КАК ВидУпаковки,
	|	0 КАК КоличествоВОдномМесте,
	|	0 КАК КоличествоМест,
	|	ИСТИНА КАК ЭтоУслуга,
	|	""Service"" КАК ПризнакТоварРаботаУслуга,
	|	0 КАК СуммаСкидок
	|ИЗ
	|	(ВЫБРАТЬ
	|		ДанныеТЧ.НомерСтроки КАК НомерСтроки,
	|		ДанныеТЧ.Содержание КАК НоменклатураПредставление,
	|		ДанныеТЧ.Содержание КАК Содержание,
	|		ДанныеТЧ.Количество КАК Количество,
	|		ДанныеТЧ.Цена КАК Цена,
	|		ВЫБОР
	|			КОГДА СуммыРегл.СуммаБезНДСРегл ЕСТЬ NULL
	|				ТОГДА ДанныеТЧ.Сумма
	|			КОГДА ДанныеТЧ.Ссылка.ЦенаВключаетНДС
	|				ТОГДА СуммыРегл.СуммаБезНДСРегл + СуммыРегл.СуммаНДСРегл
	|			ИНАЧЕ СуммыРегл.СуммаБезНДСРегл
	|		КОНЕЦ КАК Сумма,
	|		ДанныеТЧ.СтавкаНДС КАК СтавкаНДС,
	|		ЕСТЬNULL(СуммыРегл.СуммаНДСРегл, ДанныеТЧ.СуммаНДС) КАК СуммаНДС,
	|		ДанныеТЧ.Ссылка.ЦенаВключаетНДС КАК СуммаВключаетНДС,
	|		ДанныеТЧ.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
	|		&ПолеУпаковка КАК Упаковка,
	|		ЕСТЬNULL(ДанныеТЧ.ЕдиницаИзмерения.Код, НЕОПРЕДЕЛЕНО) КАК КодЕдиницыИзмерения
	|	ИЗ
	|		Документ.РеализацияУслугПрочихАктивов.Доходы КАК ДанныеТЧ
	|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыРегл
	|			ПО (СуммыРегл.Регистратор = &Ссылка)
	|				И ДанныеТЧ.ИдентификаторСтроки = СуммыРегл.ИдентификаторСтроки
	|				И (СуммыРегл.Активность)
	|				И (&ПересчитыватьВВалютуРегл)
	|	ГДЕ
	|		ДанныеТЧ.Ссылка = &Ссылка) КАК Подзапрос
	|
	|УПОРЯДОЧИТЬ ПО
	|	НомерСтроки";
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ПолеУпаковка", ВыражениеПустаяУпаковка());
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_СчетФактураКомиссионеру(Запрос, СсылкаНаОбъект)
	
	ТекстЗапросаТаблицаПокупателей = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СФКомиссионеру.Ссылка КАК СчетФактура,
	|	СФКомиссионеру.ДокументОснование КАК ОтчетПоКомиссии,
	|	ТЧПокупатели.Покупатель КАК Покупатель,
	|	ТЧПокупатели.НомерСчетаФактуры КАК НомерСчетаФактуры
	|ПОМЕСТИТЬ ТаблицаПокупателей
	|ИЗ
	|	Документ.СчетФактураКомиссионеру.Покупатели КАК ТЧПокупатели
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.СчетФактураКомиссионеру КАК СФКомиссионеру
	|		ПО (СФКомиссионеру.Ссылка = ТЧПокупатели.Ссылка)
	|ГДЕ
	|	СФКомиссионеру.Ссылка = &СсылкаНаОбъект
	|;";
	
	Если ЭтоСчетФактураНаРеализациюЧерезКомиссионера(СсылкаНаОбъект) Тогда
		
		ТекстЗапросаТаблицаПокупателей = СтрЗаменить(ТекстЗапросаТаблицаПокупателей
			, "Документ.СчетФактураКомиссионеру"
			, "Документ.СчетФактураВыданный");
			
	КонецЕсли;
	
	ТекстЗапроса = ТекстЗапросаТаблицаПокупателей + "
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ДанныеДокументов.Ссылка КАК Ссылка,
	|	ДанныеДокументов.Валюта КАК Валюта,
	|	ДанныеДокументов.Организация КАК Организация,
	|	ДанныеДокументов.Подразделение КАК Подразделение,
	|	ЗНАЧЕНИЕ(Справочник.Склады.ПустаяСсылка) КАК Склад
	|ПОМЕСТИТЬ ТаблицаДанныхДокументов
	|ИЗ
	|	Документ.ОтчетКомиссионера КАК ДанныеДокументов
	|ГДЕ
	|	ДанныеДокументов.Ссылка В(&ОснованияСФ)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Ссылка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТаблицаТоваров.Ссылка КАК Ссылка,
	|	ТаблицаТоваров.АналитикаУчетаНоменклатуры КАК АналитикаУчетаНоменклатуры,
	|	ТаблицаТоваров.Упаковка КАК Упаковка,
	|	МАКСИМУМ(ТаблицаТоваров.НомерСтроки) КАК НомерСтроки
	|ПОМЕСТИТЬ СтрокиТоваров
	|ИЗ
	|	Документ.ОтчетКомиссионера.Товары КАК ТаблицаТоваров
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТаблицаДанныхДокументов КАК ДанныеДокументов
	|		ПО ТаблицаТоваров.Ссылка = ДанныеДокументов.Ссылка
	|
	|СГРУППИРОВАТЬ ПО
	|	ТаблицаТоваров.Ссылка,
	|	ТаблицаТоваров.АналитикаУчетаНоменклатуры,
	|	ТаблицаТоваров.Упаковка
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ТаблицаТоваров.Ссылка,
	|	ТаблицаТоваров.АналитикаУчетаНоменклатуры,
	|	ТаблицаТоваров.Упаковка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТаблицаДокумента.Ссылка КАК Ссылка,
	|	СтрокиТоваров.НомерСтроки КАК НомерСтроки,
	|	ТаблицаДокумента.АналитикаУчетаНоменклатуры.Номенклатура КАК Номенклатура,
	|	ТаблицаДокумента.АналитикаУчетаНоменклатуры.Характеристика КАК Характеристика,
	|	ТаблицаДокумента.АналитикаУчетаНоменклатуры.Серия КАК Серия,
	|	ТаблицаДокумента.НомерГТД КАК НомерГТД,
	|	СУММА(ТаблицаДокумента.Количество) КАК Количество,
	|	СУММА(ТаблицаДокумента.КоличествоУпаковок) КАК КоличествоУпаковок,
	|	СУММА(ЕСТЬNULL(СуммыВВалютеУчета.СуммаБезНДСРегл, ТаблицаДокумента.СуммаСНДС - ТаблицаДокумента.СуммаНДС)) КАК СуммаБезНДС,
	|	ТаблицаДокумента.СтавкаНДС КАК СтавкаНДС,
	|	СУММА(ЕСТЬNULL(СуммыВВалютеУчета.СуммаНДСРегл, ТаблицаДокумента.СуммаНДС)) КАК СуммаНДС,
	|	ТаблицаДокумента.Упаковка КАК Упаковка,
	|	ТаблицаДокумента.ДатаСчетаФактурыКомиссионера КАК ДатаСчетаФактурыКомиссионера,
	|	ТаблицаДокумента.Покупатель КАК Покупатель,
	|	ТаблицаДокумента.НомерСчетаФактурыКомиссионера КАК НомерСчетаФактурыКомиссионера,
	|	ЛОЖЬ КАК ЭтоУслуга
	|ПОМЕСТИТЬ ОтчетКомиссионераТаблицаТоваров
	|ИЗ
	|	Документ.ОтчетКомиссионера.ВидыЗапасов КАК ТаблицаДокумента
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТаблицаДанныхДокументов КАК ДанныеДокументов
	|		ПО ТаблицаДокумента.Ссылка = ДанныеДокументов.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыВВалютеУчета
	|		ПО ТаблицаДокумента.Ссылка = СуммыВВалютеУчета.Регистратор
	|			И ТаблицаДокумента.ИдентификаторСтроки = СуммыВВалютеУчета.ИдентификаторСтроки
	|			И (СуммыВВалютеУчета.Активность)
	|			И (&ПересчитыватьВВалютуРегл)
	|		ЛЕВОЕ СОЕДИНЕНИЕ СтрокиТоваров КАК СтрокиТоваров
	|		ПО ТаблицаДокумента.Ссылка = СтрокиТоваров.Ссылка
	|			И ТаблицаДокумента.АналитикаУчетаНоменклатуры = СтрокиТоваров.АналитикаУчетаНоменклатуры
	|			И ТаблицаДокумента.Упаковка = СтрокиТоваров.Упаковка
	|
	|СГРУППИРОВАТЬ ПО
	|	ТаблицаДокумента.Ссылка,
	|	СтрокиТоваров.НомерСтроки,
	|	ТаблицаДокумента.АналитикаУчетаНоменклатуры.Номенклатура,
	|	ТаблицаДокумента.АналитикаУчетаНоменклатуры.Характеристика,
	|	ТаблицаДокумента.АналитикаУчетаНоменклатуры.Серия,
	|	ТаблицаДокумента.СтавкаНДС,
	|	ТаблицаДокумента.Упаковка,
	|	ТаблицаДокумента.НомерГТД,
	|	ТаблицаДокумента.ДатаСчетаФактурыКомиссионера,
	|	ТаблицаДокумента.Покупатель,
	|	ТаблицаДокумента.НомерСчетаФактурыКомиссионера
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ТаблицаДокумента.Ссылка,
	|	ТаблицаДокумента.НомерСтроки,
	|	ТаблицаДокумента.Номенклатура,
	|	ТаблицаДокумента.Характеристика,
	|	ЗНАЧЕНИЕ(Справочник.СерииНоменклатуры.ПустаяСсылка),
	|	НЕОПРЕДЕЛЕНО,
	|	СУММА(ТаблицаДокумента.Количество),
	|	СУММА(ТаблицаДокумента.КоличествоУпаковок),
	|	СУММА(ЕСТЬNULL(СуммыВВалютеУчета.СуммаБезНДСРегл, ТаблицаДокумента.СуммаПродажи - ТаблицаДокумента.СуммаПродажиНДС)),
	|	ТаблицаДокумента.СтавкаНДС,
	|	СУММА(ЕСТЬNULL(СуммыВВалютеУчета.СуммаНДСРегл, ТаблицаДокумента.СуммаПродажиНДС)),
	|	ТаблицаДокумента.Упаковка,
	|	ТаблицаДокумента.ДатаСчетаФактурыКомиссионера,
	|	ТаблицаДокумента.Покупатель,
	|	ТаблицаДокумента.НомерСчетаФактурыКомиссионера,
	|	ИСТИНА
	|ИЗ
	|	Документ.ОтчетКомиссионера.Товары КАК ТаблицаДокумента
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТаблицаДанныхДокументов КАК ДанныеДокументов
	|		ПО ТаблицаДокумента.Ссылка = ДанныеДокументов.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовВВалютеРегл КАК СуммыВВалютеУчета
	|		ПО ТаблицаДокумента.Ссылка = СуммыВВалютеУчета.Регистратор
	|			И ТаблицаДокумента.ИдентификаторСтроки = СуммыВВалютеУчета.ИдентификаторСтроки
	|			И (СуммыВВалютеУчета.Активность)
	|			И (&ПересчитыватьВВалютуРегл)
	|ГДЕ
	|	ТаблицаДокумента.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга)
	|
	|СГРУППИРОВАТЬ ПО
	|	ТаблицаДокумента.Ссылка,
	|	ТаблицаДокумента.НомерСтроки,
	|	ТаблицаДокумента.Номенклатура,
	|	ТаблицаДокумента.Характеристика,
	|	ТаблицаДокумента.СтавкаНДС,
	|	ТаблицаДокумента.Упаковка,
	|	ТаблицаДокумента.ДатаСчетаФактурыКомиссионера,
	|	ТаблицаДокумента.Покупатель,
	|	ТаблицаДокумента.НомерСчетаФактурыКомиссионера
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТаблицаДокумента.Ссылка КАК Ссылка,
	|	ТаблицаДокумента.НомерСтроки КАК НомерСтроки,
	|	ТаблицаДокумента.Номенклатура КАК Номенклатура,
	|	ТаблицаДокумента.Характеристика КАК Характеристика,
	|	ТаблицаДокумента.Серия КАК Серия,
	|	ТаблицаДокумента.Упаковка КАК Упаковка,
	|	ТаблицаДокумента.НомерГТД КАК НомерГТД,
	|	ТаблицаДокумента.Количество КАК Количество,
	|	ТаблицаДокумента.КоличествоУпаковок КАК КоличествоУпаковок,
	|	ТаблицаДокумента.СуммаБезНДС КАК СуммаБезНДС,
	|	ТаблицаДокумента.СтавкаНДС КАК СтавкаНДС,
	|	ТаблицаДокумента.СуммаНДС КАК СуммаНДС,
	|	ТаблицаДокумента.ДатаСчетаФактурыКомиссионера КАК ДатаСчетаФактурыКомиссионера,
	|	ТаблицаДокумента.НомерСчетаФактурыКомиссионера КАК НомерСчетаФактурыКомиссионера,
	|	ТаблицаДокумента.Покупатель КАК Покупатель,
	|	ТаблицаДокумента.ЭтоУслуга КАК ЭтоУслуга
	|ПОМЕСТИТЬ ТаблицаТоваров
	|ИЗ
	|	ОтчетКомиссионераТаблицаТоваров КАК ТаблицаДокумента
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТаблицаПокупателей КАК ТаблицаПокупателей
	|		ПО ТаблицаДокумента.Ссылка = ТаблицаПокупателей.ОтчетПоКомиссии
	|			И ТаблицаДокумента.Покупатель = ТаблицаПокупателей.Покупатель
	|			И ТаблицаДокумента.НомерСчетаФактурыКомиссионера = ТаблицаПокупателей.НомерСчетаФактуры
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТаблицаТоваров.Ссылка КАК Ссылка,
	|	ТаблицаТоваров.НомерСтроки КАК НомерСтроки,
	|	ТаблицаТоваров.Номенклатура КАК Номенклатура,
	|	ДанныеНоменклатуры.НаименованиеПолное КАК НоменклатураПредставление,
	|	ВЫБОР
	|		КОГДА &КолонкаКодов = ""Артикул""
	|			ТОГДА ДанныеНоменклатуры.Артикул
	|		ИНАЧЕ ДанныеНоменклатуры.Код
	|	КОНЕЦ КАК КодНоменклатуры,
	|	ВЫБОР
	|		КОГДА НЕ ДанныеУпаковки.Ссылка ЕСТЬ NULL
	|			ТОГДА ДанныеУпаковки.ЕдиницаИзмерения
	|		КОГДА ДанныеНоменклатуры.Ссылка ЕСТЬ NULL
	|			ТОГДА ТаблицаТоваров.Упаковка
	|		ИНАЧЕ ДанныеНоменклатуры.ЕдиницаИзмерения
	|	КОНЕЦ КАК ЕдиницаИзмерения,
	|	ВЫБОР
	|		КОГДА НЕ ДанныеУпаковки.Ссылка ЕСТЬ NULL
	|			ТОГДА ДанныеУпаковки.ЕдиницаИзмерения.Код
	|		КОГДА ДанныеНоменклатуры.Ссылка ЕСТЬ NULL
	|			ТОГДА ТаблицаТоваров.Упаковка.ЕдиницаИзмерения.Код
	|		ИНАЧЕ ДанныеНоменклатуры.ЕдиницаИзмерения.Код
	|	КОНЕЦ КАК КодЕдиницыИзмерения,
	|	ТаблицаТоваров.Характеристика КАК Характеристика,
	|	ТаблицаТоваров.Серия КАК Серия,
	|	ТаблицаТоваров.НомерГТД КАК НомерГТД,
	|	НЕОПРЕДЕЛЕНО КАК СтранаПроисхождения,
	|	ТаблицаТоваров.СтавкаНДС КАК СтавкаНДС,
	|	ВЫБОР
	|		КОГДА &ВыводитьБазовыеЕдиницыИзмерения
	|			ТОГДА ТаблицаТоваров.Количество
	|		ИНАЧЕ ТаблицаТоваров.КоличествоУпаковок
	|	КОНЕЦ КАК Количество,
	|	ВЫБОР
	|		КОГДА ТаблицаТоваров.Количество = 0
	|			ТОГДА 0
	|		КОГДА &ВыводитьБазовыеЕдиницыИзмерения
	|			ТОГДА ТаблицаТоваров.СуммаБезНДС / ТаблицаТоваров.Количество
	|		ИНАЧЕ ТаблицаТоваров.СуммаБезНДС / ТаблицаТоваров.КоличествоУпаковок
	|	КОНЕЦ КАК Цена,
	|	ТаблицаТоваров.СуммаБезНДС КАК Сумма,
	|	ТаблицаТоваров.СуммаНДС КАК СуммаНДС,
	|	ЛОЖЬ КАК СуммаВключаетНДС,
	|	ТаблицаТоваров.ЭтоУслуга КАК ЭтоУслуга,
	|	ТаблицаТоваров.Покупатель КАК Покупатель,
	|	ТаблицаТоваров.ДатаСчетаФактурыКомиссионера КАК ДатаСчетаФактурыКомиссионера,
	|	ТаблицаТоваров.НомерСчетаФактурыКомиссионера КАК НомерСчетаФактурыКомиссионера
	|ИЗ
	|	ТаблицаТоваров КАК ТаблицаТоваров
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК ДанныеНоменклатуры
	|		ПО ТаблицаТоваров.Номенклатура = ДанныеНоменклатуры.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.УпаковкиЕдиницыИзмерения КАК ДанныеУпаковки
	|		ПО (НЕ &ВыводитьБазовыеЕдиницыИзмерения)
	|			И ТаблицаТоваров.Упаковка = ДанныеУпаковки.Ссылка
	|
	|УПОРЯДОЧИТЬ ПО
	|	ТаблицаТоваров.Ссылка,
	|	ТаблицаТоваров.НомерСтроки";
	
	ДокументОснование = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СсылкаНаОбъект, "ДокументОснование");
	ОснованияСФ = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДокументОснование);
	
	КолонкаКодов = ИмяДополнительнойКолонки();
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("СсылкаНаОбъект", СсылкаНаОбъект);
	Запрос.УстановитьПараметр("ОснованияСФ", ОснованияСФ);
	Запрос.УстановитьПараметр("КолонкаКодов", КолонкаКодов);
	Запрос.УстановитьПараметр("ВыводитьБазовыеЕдиницыИзмерения", ИспользоватьБазовыеЕдиницыИзмерения());
	Запрос.УстановитьПараметр("ПересчитыватьВВалютуРегл", ПересчитыватьВВалютуРегламентированногоУчета(СсылкаНаОбъект));
	
КонецПроцедуры

Процедура СформироватьЗапрос_ДанныеТЧ_ПоПередачаТоваровХранителю(Запрос, КолонкаКодаТовара, СсылкаНаДокумент)
	
		Запрос.Текст = "ВЫБРАТЬ
		|	Подзапрос.НомерСтроки 				КАК НомерСтроки,
		|	Подзапрос.Номенклатура				КАК Номенклатура,
		|	Подзапрос.НоменклатураПредставление КАК НоменклатураПредставление,
		|	Подзапрос.Содержание				КАК Содержание,
		|	Подзапрос.Характеристика 			КАК Характеристика,
		|	Подзапрос.Серия 					КАК Серия,
		|   Подзапрос.КодНоменклатуры 			КАК КодНоменклатуры, 
		|	Подзапрос.Количество 				КАК Количество,
		|	Подзапрос.Цена 						КАК Цена,
		|	Подзапрос.Сумма 					КАК Сумма,
		|	Подзапрос.СтавкаНДС 				КАК СтавкаНДС,
		|	Подзапрос.СуммаНДС 					КАК СуммаНДС,
		|	Подзапрос.НомерГТД 					КАК НомерГТД,
		|	Подзапрос.СтранаПроисхождения 		КАК СтранаПроисхождения,
		|	Подзапрос.НомерГТДПредставление 	КАК НомерГТДПредставление,
		|	Подзапрос.СуммаВключаетНДС 			КАК СуммаВключаетНДС,
		|	Подзапрос.ЕдиницаИзмерения 			КАК ЕдиницаИзмерения,
		|	Подзапрос.КодЕдиницыИзмерения 		КАК КодЕдиницыИзмерения,
		|	Подзапрос.ВидУпаковки 				КАК ВидУпаковки,
		|	Подзапрос.КоличествоВОдномМесте 	КАК КоличествоВОдномМесте,
		|	Подзапрос.КоличествоУпаковок 		КАК КоличествоУпаковок,
		|	Подзапрос.ЭтоУслуга 				КАК ЭтоУслуга,
		|	Подзапрос.порядок 					КАК Порядок,
		|	Подзапрос.ПризнакТоварРаботаУслуга 	КАК ПризнакТоварРаботаУслуга,
		|	Подзапрос.СуммаСкидок 				КАК СуммаСкидок
		|ИЗ
		|	(ВЫБРАТЬ
		|		ПередачаТоваровХранителюТовары.НомерСтроки	КАК НомерСтроки,
		|		ПередачаТоваровХранителюТовары.Номенклатура КАК Номенклатура,
		|		ВЫБОР
		|			КОГДА ПОДСТРОКА(ПередачаТоваровХранителюТовары.Номенклатура.НаименованиеПолное, 1, 10) = """"
		|				ТОГДА ПередачаТоваровХранителюТовары.Номенклатура.Наименование
		|			ИНАЧЕ ПередачаТоваровХранителюТовары.Номенклатура.НаименованиеПолное
		|		КОНЕЦ 								КАК НоменклатураПредставление,
		|		НЕОПРЕДЕЛЕНО									КАК Содержание,
		|		ПередачаТоваровХранителюТовары.Характеристика 	КАК Характеристика,
		|		ПередачаТоваровХранителюТовары.Серия 			КАК Серия,
		|   	ПередачаТоваровХранителюТовары.Номенклатура." + КолонкаКодаТовара + " КАК КодНоменклатуры,
		|		ПередачаТоваровХранителюТовары.Количество 		КАК Количество,
		|		ПередачаТоваровХранителюТовары.Цена 			КАК Цена,
		|		ПередачаТоваровХранителюТовары.Сумма 			КАК Сумма,
		|		ЗНАЧЕНИЕ(Справочник.СтавкиНДС.БезНДС)           КАК СтавкаНДС,
		|		0 	 КАК СуммаНДС,
		|		"""" КАК НомерГТД,
		|		"""" КАК СтранаПроисхождения,
		|		"""" КАК НомерГТДПредставление,
		|		ЛОЖЬ КАК СуммаВключаетНДС,
		|		ПередачаТоваровХранителюТовары.Номенклатура.ЕдиницаИзмерения 		КАК ЕдиницаИзмерения,
		|		ПередачаТоваровХранителюТовары.Номенклатура.ЕдиницаИзмерения.Код 	КАК КодЕдиницыИзмерения,
		|		ПередачаТоваровХранителюТовары.Упаковка 							КАК ВидУпаковки,
		|		" + ПолучитьТекстЗапроса_КоэффициентУпаковки("ПередачаТоваровХранителюТовары") + "
		|		ПередачаТоваровХранителюТовары.КоличествоУпаковок 					КАК КоличествоУпаковок,
		|		ЛОЖЬ 			КАК ЭтоУслуга,
		|		1 				КАК порядок,
		|		""Property"" 	КАК ПризнакТоварРаботаУслуга,
		|		0 КАК СуммаСкидок
		|	ИЗ
		|		Документ.ПередачаТоваровХранителю.Товары КАК ПередачаТоваровХранителюТовары
		|	ГДЕ
		|		ПередачаТоваровХранителюТовары.Ссылка = &Ссылка
		|	
		|	) КАК Подзапрос
		|
		|УПОРЯДОЧИТЬ ПО
		|	Порядок,
		|	НомерСтроки";
				
КонецПроцедуры

Функция ПолучитьБанковскийСчет(ДокументСсылка)
	
	ДокТоварный= ПолучитьДокТоварный(ДокументСсылка);
			
	Возврат ?(ДокТоварный.Метаданные().Реквизиты.Найти("БанковскийСчетОрганизации") = Неопределено, Неопределено, ДокТоварный.БанковскийСчетОрганизации);
	
КонецФункции

Функция ПолучитьБанковскийСчетГрузоотправителя(ДокументСсылка)
	
	ДокТоварный= ПолучитьДокТоварный(ДокументСсылка);
					
	Возврат ?(ДокТоварный.Метаданные().Реквизиты.Найти("БанковскийСчетГрузоотправителя") = Неопределено, Неопределено, ДокТоварный.БанковскийСчетГрузоотправителя);
	
КонецФункции

Функция ПолучитьБанковскийСчетПокупателя(ДокументСсылка)
	
	ДокТоварный = ПолучитьДокТоварный(ДокументСсылка);
	
	Возврат ?(ДокТоварный.Метаданные().Реквизиты.Найти("БанковскийСчетКонтрагента") = Неопределено, Неопределено, ДокТоварный.БанковскийСчетКонтрагента);
	
КонецФункции

Функция ПолучитьБанковскийСчетГрузополучателя(ДокументСсылка)
	
	ДокТоварный= ПолучитьДокТоварный(ДокументСсылка);
					
	Возврат ?(ДокТоварный.Метаданные().Реквизиты.Найти("БанковскийСчетГрузополучателя") = Неопределено, Неопределено, ДокТоварный.БанковскийСчетГрузополучателя);
	
КонецФункции

Функция ПолучитьДокТоварный(ДокументСсылка)
	
	Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		ДокТоварный= ДокументСсылка.ДокументОснование;
		Пока ТипЗнч(ДокТоварный) = Тип("ДокументСсылка.КорректировкаРеализации") Цикл 
			ДокТоварный= ДокТоварный.ДокументОснование;
		КонецЦикла;
	Иначе
		ДокТоварный= ДокументСсылка;
	КонецЕсли;
	
	Возврат ДокТоварный;
	
КонецФункции

Функция ПолучитьПодразделениеИзДокумента(ДокСсылка)
	Возврат ""; // в типовой конфе обособленное подразделение указывается как Неопределено
	//Возврат ДокСсылка.Подразделение.Наименование;
КонецФункции

Функция ПолучитьМассивСсылокРТУПоСчетуНаОплату(ДокументСсылка) Экспорт
	
	МассивРТУ = Новый Массив;
	
	Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.СчетНаОплатуКлиенту") 
		И НЕ ДокументСсылка.Метаданные().Реквизиты.Найти("ДокументОснование") = Неопределено Тогда
		
		Если  ЗначениеЗаполнено(ДокументСсылка.ДокументОснование)
			И ТипЗнч(ДокументСсылка.ДокументОснование) = Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
			
			МассивРТУ.Добавить(ДокументСсылка.ДокументОснование);
			
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.ЗаказКлиента") Тогда
		
		Запрос = Новый Запрос;
		
		Запрос.Текст = 
			"ВЫБРАТЬ
			|	РеализацияТоваровУслуг.Ссылка КАК РеализацияСсылка
			|ИЗ
			|	Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
			|ГДЕ
			|	РеализацияТоваровУслуг.ЗаказКлиента = &ЗаказКлиента
			|	И РеализацияТоваровУслуг.ПометкаУдаления = ЛОЖЬ";
			
		Запрос.УстановитьПараметр("ЗаказКлиента", ДокументСсылка);
		
		РезультатЗапроса= Запрос.Выполнить().Выгрузить();
		
		МассивРТУ = РезультатЗапроса.ВыгрузитьКолонку("РеализацияСсылка");
		
	КонецЕсли;
	
	Возврат МассивРТУ;		
	
КонецФункции

Функция ПолучитьТаблицуНакладныхПоСчетуФактуреВыданному(СчетФактура) Экспорт
	
	ТекстЗапроса = 
	
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документ.Ссылка КАК Ссылка,
	|	Документ.Дата КАК Дата,
	|	Документ.Договор КАК Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	Документ.Дата,
	|	Документ.Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КорректировкаРеализации КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	Документ.Дата,
	|	Документ.Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ВозвратТоваровОтКлиента КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	Документ.Дата,
	|	Документ.Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ВозвратТоваровПоставщику КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	Документ.Дата,
	|	Документ.Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.АктВыполненныхРабот КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	Документ.Дата,
	|	Документ.Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РеализацияУслугПрочихАктивов КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Документ.Ссылка,
	|	Документ.Дата,
	|	Документ.Договор
	|ИЗ
	|	Документ.СчетФактураВыданный КАК Таблица
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетКомитенту КАК Документ
	|		ПО Таблица.ДокументОснование = Документ.Ссылка
	|ГДЕ
	|	Таблица.Ссылка = &СчетФактура";
	
	Если ИспользуетсяОтчетДавальцу() Тогда
		
		ТекстЗапроса = ТекстЗапроса + РазделительОбъединенияЗапросов() +
			"ВЫБРАТЬ
			|	Документ.Ссылка КАК Ссылка,
			|	Документ.Дата КАК Дата,
			|	Документ.Договор КАК Договор
			|ИЗ
			|	Документ.СчетФактураВыданный КАК Таблица
			|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ОтчетДавальцу КАК Документ
			|		ПО Таблица.ДокументОснование = Документ.Ссылка
			|ГДЕ
			|	Таблица.Ссылка = &СчетФактура";
			
	КонецЕсли;
	
	ТекстЗапроса = ТекстЗапроса + "
		|УПОРЯДОЧИТЬ ПО
		|	Дата";
	
	Если ОснованиеСчетаФактурыУказываетсяВТабличнойЧасти() Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Документ.СчетФактураВыданный", "Документ.СчетФактураВыданный.ДокументыОснования");
	КонецЕсли;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("СчетФактура", СчетФактура);
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция РазделительОбъединенияЗапросов()
	
	Результат = Символы.ПС + "ОБЪЕДИНИТЬ ВСЕ" + Символы.ПС;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьСсылкуСчетаФактурыВыданногоПоДокументуОснованию(ДокументСсылка) Экспорт
	
	СФСсылка= 	Неопределено;
	Запрос= 	Новый Запрос;
	
	Если ОснованиеСчетаФактурыУказываетсяВТабличнойЧасти() Тогда
		Запрос.Текст= 
			"ВЫБРАТЬ
			|	СчетФактураВыданныйДокументыОснования.Ссылка
			|ИЗ
			|	Документ.СчетФактураВыданный.ДокументыОснования КАК СчетФактураВыданныйДокументыОснования
			|ГДЕ
			|	СчетФактураВыданныйДокументыОснования.ДокументОснование = &ДокументОснование
			|	И СчетФактураВыданныйДокументыОснования.Ссылка.ПометкаУдаления = ЛОЖЬ";
	Иначе 
		Запрос.Текст= 
			"ВЫБРАТЬ
			|	СчетФактураВыданный.Ссылка
			|ИЗ
			|	Документ.СчетФактураВыданный КАК СчетФактураВыданный
			|ГДЕ
			|	СчетФактураВыданный.ДокументОснование = &ДокументОснование
			|	И СчетФактураВыданный.ПометкаУдаления = ЛОЖЬ";
	КонецЕсли;
	
	Запрос.УстановитьПараметр("ДокументОснование", ДокументСсылка);
	
	Выборка= Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		СФСсылка= Выборка.Ссылка;
	КонецЕсли;
	
	Возврат СФСсылка;
	
КонецФункции

Функция ПолучитьДокОснованиеКорректировкиРеализации(ДокументСсылка) Экспорт
	
	ДокументОснование= Неопределено;
	
	Если НЕ ДокументСсылка.Метаданные().Реквизиты.Найти("ДокументРеализации") = Неопределено Тогда
		ДокументОснование= ДокументСсылка.ДокументРеализации;
	Иначе
		ДокументОснование= ДокументСсылка.ДокументОснование;
	КонецЕсли;
	
	Возврат ДокументОснование;
	
КонецФункции

Функция ПолучитьСчетНаОплатуНаОснованииРТУ(ДокументСсылка) Экспорт
	
	Запрос= Новый Запрос;
	
	Запрос.Текст=
		"ВЫБРАТЬ
		|	СчетНаОплатуКлиенту.Ссылка
		|ИЗ
		|	Документ.СчетНаОплатуКлиенту КАК СчетНаОплатуКлиенту
		|ГДЕ
		|	СчетНаОплатуКлиенту.ДокументОснование = &ДокументОснование
		|	И СчетНаОплатуКлиенту.ПометкаУдаления = ЛОЖЬ";
		
	Запрос.УстановитьПараметр("ДокументОснование", ДокументСсылка);
	
	Выборка= Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
	Возврат Неопределено;			
	
КонецФункции

Функция ПолучитьЗаказПокупателяНаОснованииРТУ(ДокументСсылка) Экспорт
	
	Если НЕ Метаданные.Документы.РеализацияТоваровУслуг.Реквизиты.Найти("ЗаказКлиента") = Неопределено
		  И ТипЗнч(ДокументСсылка.ЗаказКлиента) = Тип("ДокументСсылка.ЗаказКлиента") Тогда
		  
		Возврат ДокументСсылка.ЗаказКлиента;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ПолучитьСФОснованиеПоИСФ_КСФ(ИСФ_КСФ) Экспорт
	
	ДокОснование = ПервыйДокументОснование(ИСФ_КСФ);
	Если Не ЗначениеЗаполнено(ДокОснование) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ВидОперации = ХозяйственнаяОперацияДокумента(ДокОснование);
	
	Если ВидОперации = Перечисления.ХозяйственныеОперации.КорректировкаПоСогласованиюСторон Тогда 
		ДокОснование = ПервыйДокументОснование(ДокОснование);
	Иначе
		Пока ТипЗнч(ДокОснование) = Тип("ДокументСсылка.КорректировкаРеализации")
			И ХозяйственнаяОперацияДокумента(ДокОснование) = Перечисления.ХозяйственныеОперации.ИсправлениеОшибок Цикл 
			
			ДокОснование = ПервыйДокументОснование(ДокОснование);
			
		КонецЦикла;
	КонецЕсли;
	
	Результат = ПолучитьСсылкуСчетаФактурыВыданногоПоДокументуОснованию(ДокОснование);
	
	Возврат Результат;

КонецФункции

Функция ПолучитьДанныеДляФормированияУПД(ДокСсылка, ФункцияУПД)
	
	Если ФункцияУПД = "СЧФДОП" ИЛИ ФункцияУПД = "СЧФ" Тогда
		
		ДанныеДляФормированияСчетФактуры = СформироватьДанныеДляПечати(ДокСсылка, Ложь);
		
		ДанныеДляФормированияУПД = Новый Структура("ДанныеШапки", Новый Структура);
		
		Для Каждого КлючИЗначение Из ДанныеДляФормированияСчетФактуры Цикл
			
			Если КлючИЗначение.Ключ = "ТабличнаяЧасть" Тогда
				ДанныеДляФормированияУПД.Вставить("ТаблицаТоваров", КлючИЗначение.Значение);
			Иначе
				ДанныеДляФормированияУПД.ДанныеШапки.Вставить(КлючИЗначение.Ключ, КлючИЗначение.Значение);
			КонецЕсли;
			
		КонецЦикла;
		
		ТаблицаНакладных = ПолучитьТаблицуНакладныхПоСчетуФактуреВыданному(ДокСсылка);
		Если ЗначениеЗаполнено(ТаблицаНакладных) Тогда
			
			Накладная = ТаблицаНакладных[0].Ссылка;
			
			Если ФункцияУПД = "СЧФДОП" Тогда 
				
				ИсправляемыйДокументРеализации = ПолучитьИсправляемыйДокументРеализации(Накладная);
				
				ДанныеДляФормированияУПД.ДанныеШапки.Вставить("ДатаОтгрузки", ИсправляемыйДокументРеализации.Дата);
				
			КонецЕсли;
			
		КонецЕсли;
		
	Иначе
		
		ДанныеШапки	 = ДанныеШапки(ДокСсылка);
		
		ПроверитьЗаполнитьВалюту(ДанныеШапки);
		
		ДанныеТЧ	 = ПолучитьДанныеТЧ(ДокСсылка, Неопределено, Неопределено, Истина, , ФункцияУПД);
		ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(ДанныеШапки, ДанныеТЧ);
		
		ДанныеДляФормированияУПД = Новый Структура;
		ДанныеДляФормированияУПД.Вставить("ДанныеШапки"		, ДанныеШапки);
		ДанныеДляФормированияУПД.Вставить("ТаблицаТоваров"	, ДанныеТЧ);
		
	КонецЕсли;
	
	Если ФункцияУПД = "СЧФДОП" Тогда
		ДанныеДляФормированияУПД.ДанныеШапки.Вставить("Функция", "InvoiceAndBasic");
	ИначеЕсли ФункцияУПД = "ДОП" Тогда
		ДанныеДляФормированияУПД.ДанныеШапки.Вставить("Функция", "Basic");
	ИначеЕсли ФункцияУПД = "СЧФ" Тогда
		ДанныеДляФормированияУПД.ДанныеШапки.Вставить("Функция", "Invoice");
	КонецЕсли;
	ДанныеДляФормированияУПД.ДанныеШапки.Вставить("ДатаНомерСвидетельстваОРегистрации", ПолучитьДатуНомерСвидетельстваОГосРегистрацииИП(ДанныеДляФормированияУПД.ДанныеШапки.Организация));
	
	Если ДанныеДляФормированияУПД.ДанныеШапки.НДСИсчисляетсяНалоговымАгентом Тогда
		МетодСервера(, "ЗаполнитьВТаблице_СтавкаНДС_ИсчНалАг", ДанныеДляФормированияУПД.ТаблицаТоваров);
	КонецЕсли;
	
	Возврат ДанныеДляФормированияУПД;
	
КонецФункции

Функция ДанныеДляФормированияУКД(ДокСсылка, ДопПараметры)
	
	Если ДопПараметры.ФункцияУПД = "КСЧФДИС" ИЛИ ДопПараметры.ФункцияУПД = "КСЧФ" Тогда
		
		СчетФактура		 = ДокСсылка;
		ТаблицаНакладных = ПолучитьТаблицуНакладныхПоСчетуФактуреВыданному(ДокСсылка);
		Накладная		 = ТаблицаНакладных[0].Ссылка;
		
		ИсходныйСчетФактура = ИсходныйСчетФактура(ДокСсылка);
		ТаблицаИсправляемыхДокументов = ПолучитьТаблицуНакладныхПоСчетуФактуреВыданному(ИсходныйСчетФактура);
		ИсправляемыеДокументы = ТаблицаИсправляемыхДокументов.ВыгрузитьКолонку("Ссылка");
		
	Иначе
		
		СчетФактура = Неопределено;
		Накладная	= ДокСсылка;
		
		ТаблицаНакладных = Новый ТаблицаЗначений;
		ТаблицаНакладных.Колонки.Добавить("Ссылка");
		ТаблицаНакладных.Колонки.Добавить("Дата");
		
		НоваяСтрока = ТаблицаНакладных.Добавить();
		НоваяСтрока.Ссылка  = Накладная;
		НоваяСтрока.Дата  	= Накладная.Дата;
		
		СвязанныеДокументы = ТаблицаСвязанныхКорректировочныхДокументов(ДокСсылка);
		Если СвязанныеДокументы.Количество() > 1 Тогда
			ИсправляемыйДокумент = СвязанныеДокументы[1].Подчиненный;
		Иначе
			ИсправляемыйДокумент = ПолучитьИсправляемыйДокументРеализации(ДокСсылка);
		КонецЕсли;
		
		ИсправляемыеДокументы = Новый Массив;
		ИсправляемыеДокументы.Добавить(ИсправляемыйДокумент);
		
	КонецЕсли;
	
	Шапка = ДанныеШапки(ДокСсылка);
	
	ПроверитьЗаполнитьВалюту(Шапка);
	
	ТабличнаяЧасть = ТабличнаяЧастьКорректировочногоДокумента(ДокСсылка);
	ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(Шапка, ТабличнаяЧасть);
	
	Шапка.Вставить("Функция", ?(ДопПараметры.ФункцияУПД = "КСЧФДИС", "InvoiceAndBasic", ?(ДопПараметры.ФункцияУПД = "КСЧФ", "Invoice", "Basic")));
	
	Если ДопПараметры.ФункцияУПД = "КСЧФДИС" ИЛИ ДопПараметры.ФункцияУПД = "ДИС" Тогда
		ДатаУведомления = ТаблицаНакладных[0].Дата;
		Если ДатаУведомления <> Шапка.ДатаНомерДокумента.ДатаДокумента Тогда
			Шапка.Вставить("ДатаУведомления", ДатаУведомления);
		КонецЕсли;
	КонецЕсли;
	
	Если МетодСервера(, "ЕстьРеквизитТипаОбъекта", ТипЗнч(Накладная), "СопроводительныеДокументы") Тогда
		Шапка.Вставить("ИныеСведенияОбИзмененииСтоимости", Накладная.СопроводительныеДокументы);
	КонецЕсли;
					
	РеквизитыПередаточныхДокументов		= Новый Массив;
	ДобавленныеПередаточныеДокументы	= Новый Соответствие;
	
	ТипПередаточногоДокумента = "УПД "; 
	
	Шапка.Вставить("ОснованияПередачи", Новый Массив);
	Шапка.Вставить("ПередаточныеДокументы", Новый Массив);
	Шапка.Вставить("ИсправляемыеДокументы", ИсправляемыеДокументы);
	
	Для Каждого СтрокаТЗ Из ТаблицаНакладных Цикл
		
		СтруктураОснования = СтруктурированноеПредставлениеОснованияДляУКД(СтрокаТЗ.Ссылка);
		
		Если ЗначениеЗаполнено(СтруктураОснования)
			И ЗначениеЗаполнено(СтруктураОснования.GroundName)
			И ЗначениеЗаполнено(СтруктураОснования.GroundDate) Тогда
			
			ItemCorrectionBases = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_CorrectionBase");
			
			ЗаполнитьCorrectionBaseПоДаннымОснования(ItemCorrectionBases, СтруктураОснования);
			
			Шапка.ОснованияПередачи.Добавить(ItemCorrectionBases);	
			
		КонецЕсли;
	
		ИсправляемыйДокументРеализации = ПолучитьИсправляемыйДокументРеализации(СтрокаТЗ.Ссылка);
		Если ДопПараметры.ФункцияУПД = "КСЧФДИС" ИЛИ ДопПараметры.ФункцияУПД = "КСЧФ" Тогда 
			ПередаточныйДокумент = ПолучитьСсылкуСчетаФактурыВыданногоПоДокументуОснованию(ИсправляемыйДокументРеализации);
		Иначе
			ПередаточныйДокумент = ИсправляемыйДокументРеализации;
		КонецЕсли;
		
		Шапка.ПередаточныеДокументы.Добавить(ПередаточныйДокумент);
		
		Если ДобавленныеПередаточныеДокументы[ПередаточныйДокумент] <> Истина Тогда
			
			ДобавленныеПередаточныеДокументы.Вставить(ПередаточныйДокумент, Истина);
			
			Новый_BaseDocument = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_BaseDocumentName");
		
			Новый_BaseDocument.BaseDocumentName		= ТипПередаточногоДокумента;
			Новый_BaseDocument.BaseDocumentNumber 	= ПредставлениеНомераОбъекта(ПередаточныйДокумент);
			Новый_BaseDocument.BaseDocumentDate 	= Формат(ПередаточныйДокумент.Дата, "ДФ=dd.MM.yyyy");
			
			РеквизитыПередаточныхДокументов.Добавить(Новый_BaseDocument);
																				
		КонецЕсли;
		
	КонецЦикла;
	
	Шапка.Вставить("РеквизитыПередаточныхДокументов", РеквизитыПередаточныхДокументов); 
	
	Если Шапка.НДСИсчисляетсяНалоговымАгентом Тогда
		МетодСервера(, "ЗаполнитьВТаблице_СтавкаНДС_ИсчНалАг", ТабличнаяЧасть);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("Шапка", 			Шапка);
	Результат.Вставить("ТабличнаяЧасть", 	ТабличнаяЧасть);
	
	Возврат Результат;
	
КонецФункции

Функция ЗаполнитьСтруктуруОснования(ДоговорКонтрагента, ЭтоУПД)
	
	Ground = Новый Структура;
	Ground.Вставить("GroundName", 	"");
	Ground.Вставить("GroundNumber", "");
	Ground.Вставить("GroundDate", 	"");
	Ground.Вставить("Основание", 	"");
	
	Если ЗначениеЗаполнено(ДоговорКонтрагента) Тогда
			
		Если ЭтоУПД Тогда
			ПредставлениеДоговора= "Договор";
		Иначе
			ПредставлениеДоговора= ДоговорКонтрагента.Наименование;
		КонецЕсли;
			
		Ground.Вставить("GroundName", 	ПредставлениеДоговора);
		Ground.Вставить("GroundNumber", ДоговорКонтрагента.Номер);
		Ground.Вставить("GroundDate", 	ДоговорКонтрагента.Дата);
		Ground.Вставить("Основание", 	ДоговорКонтрагента);
		
	КонецЕсли;
	
	Возврат Ground;
	
КонецФункции

Процедура ЗаполнитьCorrectionBaseПоДаннымОснования(CorrectionBase, СтруктураОснования)
	
	CorrectionBase.BaseDocumentName		= СтруктураОснования.GroundName;
	CorrectionBase.BaseDocumentNumber	= СтруктураОснования.GroundNumber;
	CorrectionBase.BaseDocumentDate		= СтруктураОснования.GroundDate;
	CorrectionBase.AdditionalInfo		= "";
	
КонецПроцедуры

Функция ПолучитьИсправляемыйДокументРеализации(ДокРеализации)
	
	Если ЗначениеЗаполнено(ДокРеализации) 
		И ТипЗнч(ДокРеализации) = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		Результат = ПолучитьИсправляемыйДокументРеализации(ДокРеализации.ДокументОснование);
		
	ИначеЕсли ТипЗнч(ДокРеализации) = Тип("ДокументСсылка.ВозвратТоваровОтКлиента")
		И ЕстьРеквизитМетаданных(ДокРеализации.Метаданные(), "ДокументРеализации")
		И ТипЗнч(ДокРеализации.ДокументРеализации) = Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
		
		Результат = ДокРеализации.ДокументРеализации;
		
	Иначе
		
		Результат = ДокРеализации;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТаблицаСвязанныхКорректировочныхДокументов(ДокКорретировки)
	
	ТекстЗапроса = "
	|ВЫБРАТЬ
	|	ДанныеДокумента.Ссылка КАК Ссылка,
	|	ДанныеДокумента.ВидКорректировки КАК ХозяйственнаяОперация
	|
	|ПОМЕСТИТЬ ТаблицаДанныхДокумента
	|ИЗ
	|	Документ.КорректировкаРеализации КАК ДанныеДокумента
	|ГДЕ
	|	ДанныеДокумента.Ссылка = &Ссылка;
	|
	|//////////////////////////////////////////////////////////////////
	|
	|ВЫБРАТЬ
	|	ДанныеДокумента.Ссылка КАК Основной,
	|	ДанныеДокумента.Ссылка КАК Подчиненный,
	|	ДанныеДокумента.Ссылка.МоментВремени КАК МоментВремени
	|ИЗ
	|	ТаблицаДанныхДокумента КАК ДанныеДокумента
	|ГДЕ
	|	ДанныеДокумента.Ссылка = &Ссылка
	|	И ДанныеДокумента.ХозяйственнаяОперация В (
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.РеализацияПерепоставленногоТовара),
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВозвратНедопоставленногоТовара)
	|	)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ТекущийДокумент.Ссылка,
	|	ТекущийДокумент.ДокументОснование,
	|	ТекущийДокумент.ДокументОснование.МоментВремени
	|ИЗ
	|	Документ.КорректировкаРеализации КАК ТекущийДокумент
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТаблицаДанныхДокумента КАК ДанныеДокумента
	|		ПО ТекущийДокумент.Ссылка = ДанныеДокумента.Ссылка
	|ГДЕ
	|	ТекущийДокумент.Ссылка = &Ссылка
	|	И НЕ ДанныеДокумента.ХозяйственнаяОперация В (
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.РеализацияПерепоставленногоТовара),
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВозвратНедопоставленногоТовара)
	|	)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ТекущийДокумент.Ссылка,
	|	ПредыдущиеДокументы.Ссылка,
	|	ПредыдущиеДокументы.МоментВремени
	|ИЗ
	|	Документ.КорректировкаРеализации КАК ТекущийДокумент
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТаблицаДанныхДокумента КАК ДанныеДокумента
	|		ПО ТекущийДокумент.Ссылка = ДанныеДокумента.Ссылка
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КорректировкаРеализации КАК ПредыдущиеДокументы
	|		ПО ТекущийДокумент.ДокументОснование = ПредыдущиеДокументы.ДокументОснование
	|			И (ТекущийДокумент.Дата >= ПредыдущиеДокументы.Дата
	|				ИЛИ ТекущийДокумент.Дата = ПредыдущиеДокументы.Дата
	|					И ТекущийДокумент.Ссылка >= ПредыдущиеДокументы.Ссылка)
	|ГДЕ
	|	ТекущийДокумент.Ссылка = &Ссылка
	|	И ПредыдущиеДокументы.Проведен
	|	И НЕ ДанныеДокумента.ХозяйственнаяОперация В (
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.РеализацияПерепоставленногоТовара),
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВозвратНедопоставленногоТовара)
	|	)
	|
	|УПОРЯДОЧИТЬ ПО
	|	МоментВремени УБЫВ;
	|
	|//////////////////////////////////////////////////////////////////
	|
	|УНИЧТОЖИТЬ ТаблицаДанныхДокумента";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Ссылка", ДокКорретировки);
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Функция ВалютаРегламентированногоУчета() Экспорт
	
	Результат = ЗначениеНастроекПовтИсп.ПолучитьВалютуРегламентированногоУчета(); 
	
	Возврат Результат;
	
КонецФункции

// См. метод "ПередФормированиемПечатнойФормыСчетаНаОплату" в "Модуль_ИнтеграцияУниверсальный"
//
// Параметры:
//  МенеджерПечати			 - ДокументМенеджер	- менеджер, из которого будем вызывать типовой метод печати
//  ПараметрыМетодаПечати	 - Структура 		- см. метод "НовыйПараметрыТиповогоМетодаПечати" в модуле "Модуль_ИнтеграцияУниверсальный"
//
Процедура ПередФормированиемПечатнойФормыСчетаНаОплату(МенеджерПечати, ПараметрыМетодаПечати) Экспорт
	
	МенеджерПечати = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Обработка.ПечатьСчетовНаОплату");
							
	НовыйЭлементТаблицы = ПараметрыМетодаПечати.КоллекцияПечатныхФорм.Добавить();
	НовыйЭлементТаблицы.ИмяМакета 	= "СчетНаОплату";
	НовыйЭлементТаблицы.ИмяВРЕГ 	= ВРег(НовыйЭлементТаблицы.ИмяМакета);
	
	ПараметрыОтправки = Новый Структура;
	
	ПараметрыОтправки.Вставить("Получатель"	, "");
	ПараметрыОтправки.Вставить("Тема"		, "");
	ПараметрыОтправки.Вставить("Текст"		, "");
							
	ПараметрыМетодаПечати.ПараметрыВывода.Вставить("ДоступнаПечатьПоКомплектно"	, Ложь);
	ПараметрыМетодаПечати.ПараметрыВывода.Вставить("ПараметрыОтправки"			, ПараметрыОтправки);
	
КонецПроцедуры //ПередФормированиемПечатнойФормыСчетаНаОплату()

// См. метод "ПриЗаполненииМетыНеформализованногоСчетаНаОплату" в "Модуль_ИнтеграцияУниверсальный"
//
// Параметры:
//  СчетНаОплату	- ДокументСсылка	- документ, на основании которого формируем печатную форму
//  Мета			- Структура			- см. метод "НовыйРезультатВизуализацииСчетаНаОплату" в "Модуль_ИнтеграцияУниверсальный"
//
Процедура ПриЗаполненииМетыНеформализованногоСчетаНаОплату(СчетНаОплату, Мета) Экспорт

	ИмяТипаДокумента = СчетНаОплату.Метаданные().Имя;
	
	МассивОбъектов = Новый Массив;
	МассивОбъектов.Добавить(СчетНаОплату);
	
	СтруктураДанныхДляПечати = Документы[ИмяТипаДокумента].ПолучитьДанныеДляПечатнойФормыСчетаНаОплату(, МассивОбъектов);
	
	ТабличнаяЧасть = СтруктураДанныхДляПечати.РезультатПоТабличнойЧасти.Выгрузить();
	
	ЕстьСтавкаБезНДС = Ложь;
	Для Каждого ЭлементТЗ Из ТабличнаяЧасть Цикл
		Если МенеджерИнтеграции_ЭтоСтавкаБезНДС(ЭлементТЗ.СтавкаНДС) Тогда
			ЕстьСтавкаБезНДС = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	ШапкаСчета = СтруктураДанныхДляПечати.РезультатПоШапке.Выгрузить()[0];
	ВсегоНДС = ТабличнаяЧасть.Итог("СуммаНДС");
	
	Если ВсегоНДС > 0 И ШапкаСчета.ЧастичнаяОплата Тогда
		Точность = 2;
		ВсегоНДС = Окр(ВсегоНДС * ШапкаСчета.ПроцентОплаты / 100, Точность);
	КонецЕсли;
	
	Мета.СуммаНДС		= ВсегоНДС;
	Мета.ДокументБезНДС	= (ЕстьСтавкаБезНДС И ВсегоНДС = 0);
	Мета.Сумма			= ШапкаСчета.СуммаДокумента;

КонецПроцедуры // ПриЗаполненииМетыНеформализованногоСчетаНаОплату()

// См. метод "ПередФормированиемПечатнойФормыСверкиВзаиморасчетов" в "Модуль_ИнтеграцияУниверсальный"
//
// Параметры:
//  МенеджерПечати			 - ДокументМенеджер	- менеджер, из которого будем вызывать типовой метод печати
//  ПараметрыМетодаПечати	 - Структура 		- См. метод "НовыйПараметрыТиповогоМетодаПечати" в модуле "Модуль_ИнтеграцияУниверсальный"
//
Процедура ПередФормированиемПечатнойФормыСверкиВзаиморасчетов(МенеджерПечати, ПараметрыМетодаПечати) Экспорт
	
	Если ТипЗнч(ПараметрыМетодаПечати.МассивОбъектов[0]) = Тип("ДокументСсылка.СверкаВзаиморасчетов") Тогда
		МенеджерПечати = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Документ.СверкаВзаиморасчетов");
	ИначеЕсли ИспользуетсяСверкаВзаиморасчетов2_5_11()
		И ТипЗнч(ПараметрыМетодаПечати.МассивОбъектов[0]) = Тип("ДокументСсылка.СверкаВзаиморасчетов2_5_11") Тогда
		МенеджерПечати = ОбщегоНазначения.МенеджерОбъектаПоПолномуИмени("Документ.СверкаВзаиморасчетов2_5_11");
	Иначе
		Возврат;
	КонецЕсли;
	
	НоваяПФ = ПараметрыМетодаПечати.КоллекцияПечатныхФорм.Добавить();
	
	НоваяПФ.ИмяМакета 	= "АктСверкиВзаимныхРасчетов";
	НоваяПФ.ИмяВРЕГ		= ВРЕГ(НоваяПФ.ИмяМакета);
	НоваяПФ.Экземпляров = 1;
	
	ПараметрыОтправки = Новый Структура;
	
	ПараметрыОтправки.Вставить("Получатель"	, "");
	ПараметрыОтправки.Вставить("Тема"		, "");
	ПараметрыОтправки.Вставить("Текст"		, "");
	
	ПараметрыМетодаПечати.ПараметрыВывода.Вставить("ДоступнаПечатьПоКомплектно"	, Ложь);
	ПараметрыМетодаПечати.ПараметрыВывода.Вставить("ПараметрыОтправки"			, ПараметрыОтправки);
	
КонецПроцедуры // ПередФормированиемПечатнойФормыСверкиВзаиморасчетов()

Функция ИспользуетсяСверкаВзаиморасчетов2_5_11()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Результат = СравнитьВерсии(ВерсияУТ, "11.5.11.79") >= 0;
	Возврат Результат;
	
КонецФункции

Функция ИспользуетсяСверкаВзаиморасчетов2_4()
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Результат = ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ВерсияУТ, "11.5.20.00") >= 0;
	Возврат Результат;
	
КонецФункции

Функция МетаданныеДокументаСверкаВзаиморасчетов()
	
	Если ИспользуетсяСверкаВзаиморасчетов2_4() Тогда
		Результат = Метаданные.Документы.СверкаВзаиморасчетов2_4;
	Иначе
		Результат = Метаданные.Документы.СверкаВзаиморасчетов;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьПлатежноРасчетныеДокументы(ДокументСсылка, МассивОснований)
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Ссылка", ДокументСсылка);
	Запрос.Текст = "ВЫБРАТЬ
	|	ПРС.ДатаПлатежноРасчетногоДокумента КАК ДатаПлатежноРасчетногоДокумента,
	|	ПРС.НомерПлатежноРасчетногоДокумента КАК НомерПлатежноРасчетногоДокумента
	|ИЗ
	|	Документ.СчетФактураВыданный.ПлатежноРасчетныеДокументы КАК ПРС
	|ГДЕ
	|	ПРС.Ссылка = &Ссылка
	|	И ПРС.НомерПлатежноРасчетногоДокумента <> """"
	|	И ПРС.ДатаПлатежноРасчетногоДокумента <> ДАТАВРЕМЯ(1, 1, 1)";
	
	Если Метаданные.Документы.Найти("СчетФактураВыданныйАванс") <> Неопределено И ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.СчетФактураВыданныйАванс") Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "Документ.СчетФактураВыданный.ПлатежноРасчетныеДокументы", "Документ.СчетФактураВыданныйАванс");
	КонецЕсли;
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

Функция ПараметрыСчетаФактуры(СчетФактура) Экспорт
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	Если СравнитьВерсии(ВерсияУТ, "11.4.10") < 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ТипЗнч(СчетФактура) = Тип("ДокументСсылка.СчетФактураВыданный") Тогда
		ТекстЗапроса = ТекстЗапроса_СчетФактураВыданный();
	ИначеЕсли ТипЗнч(СчетФактура) = Тип("ДокументСсылка.СчетФактураВыданныйАванс") Тогда
		ТекстЗапроса = ТекстЗапроса_СчетФактураВыданныйАванс();
	ИначеЕсли ТипЗнч(СчетФактура) = Тип("ДокументСсылка.СчетФактураНалоговыйАгент") Тогда
		ТекстЗапроса = ТекстЗапроса_СчетФактураНалоговыйАгент();
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("Ссылка", СчетФактура);
	Запрос.УстановитьПараметр("ВалютаРегл", ВалютаРегламентированногоУчета());
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если Не РезультатЗапроса.Пустой() Тогда
		
		Выборка = РезультатЗапроса.Выбрать();
		Выборка.Следующий();
		
		Результат = Новый Структура;
		Результат.Вставить("СуммаДокумента", Выборка.Сумма);
		Результат.Вставить("СуммаУвеличение", Выборка.СуммаУвеличение);
		Результат.Вставить("СуммаУменьшение", Выборка.СуммаУменьшение);
		Результат.Вставить("СуммаНДСДокумента", Выборка.СуммаНДС);
		Результат.Вставить("СуммаНДСУвеличение", Выборка.СуммаНДСУвеличение);
		Результат.Вставить("СуммаНДСУменьшение", Выборка.СуммаНДСУменьшение);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗапроса_СчетФактураВыданный()

	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СчетФактураВыданныйТовары.Ссылка КАК Ссылка,
	|	СчетФактураВыданныйТовары.Ссылка.Валюта КАК Валюта,
	|	СчетФактураВыданныйТовары.Ссылка.Исправление КАК ЭтоИсправление,
	|	СчетФактураВыданныйТовары.Ссылка.Корректировочный КАК ЭтоКорректировка,
	|	СУММА(СчетФактураВыданныйТовары.Сумма) КАК Сумма,
	|	СУММА(СчетФактураВыданныйТовары.СуммаУвеличение) КАК СуммаУвеличение,
	|	СУММА(СчетФактураВыданныйТовары.СуммаУменьшение) КАК СуммаУменьшение,
	|	СУММА(СчетФактураВыданныйТовары.СуммаНДС) КАК СуммаНДС,
	|	СУММА(СчетФактураВыданныйТовары.СуммаНДСУвеличение) КАК СуммаНДСУвеличение,
	|	СУММА(СчетФактураВыданныйТовары.СуммаНДСУменьшение) КАК СуммаНДСУменьшение
	|ИЗ
	|	Документ.СчетФактураВыданный.Товары КАК СчетФактураВыданныйТовары
	|ГДЕ
	|	СчетФактураВыданныйТовары.Ссылка = &Ссылка
	|
	|СГРУППИРОВАТЬ ПО
	|	СчетФактураВыданныйТовары.Ссылка,
	|	СчетФактураВыданныйТовары.Ссылка.Валюта,
	|	СчетФактураВыданныйТовары.Ссылка.Исправление,
	|	СчетФактураВыданныйТовары.Ссылка.Корректировочный";
	
	Возврат ТекстЗапроса;

КонецФункции

Функция ТекстЗапроса_СчетФактураВыданныйАванс()

	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СчетФактураВыданныйАвансАвансы.Ссылка КАК Ссылка,
	|	&ВалютаРегл КАК Валюта,
	|	СчетФактураВыданныйАвансАвансы.Ссылка.Исправление КАК ЭтоИсправление,
	|	СчетФактураВыданныйАвансАвансы.Ссылка.Корректировочный КАК ЭтоКорректировка,
	|	СУММА(СчетФактураВыданныйАвансАвансы.Сумма) КАК Сумма,
	|	СУММА(0) КАК СуммаУвеличение,
	|	СУММА(0) КАК СуммаУменьшение,
	|	СУММА(СчетФактураВыданныйАвансАвансы.СуммаНДС) КАК СуммаНДС,
	|	СУММА(0) КАК СуммаНДСУвеличение,
	|	СУММА(0) КАК СуммаНДСУменьшение
	|ИЗ
	|	Документ.СчетФактураВыданныйАванс.Авансы КАК СчетФактураВыданныйАвансАвансы
	|ГДЕ
	|	СчетФактураВыданныйАвансАвансы.Ссылка = &Ссылка
	|
	|СГРУППИРОВАТЬ ПО
	|	СчетФактураВыданныйАвансАвансы.Ссылка,
	|	СчетФактураВыданныйАвансАвансы.Ссылка.Исправление,
	|	СчетФактураВыданныйАвансАвансы.Ссылка.Корректировочный";
	
	Возврат ТекстЗапроса;

КонецФункции

Функция ТекстЗапроса_СчетФактураНалоговыйАгент()

	ТекстЗапроса =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СчетФактураНалоговыйАгентРасшифровкаСуммы.Ссылка КАК Ссылка,
	|	СчетФактураНалоговыйАгентРасшифровкаСуммы.Ссылка.Валюта КАК Валюта,
	|	ЛОЖЬ КАК ЭтоИсправление,
	|	ЛОЖЬ КАК ЭтоКорректировка,
	|	СУММА(СчетФактураНалоговыйАгентРасшифровкаСуммы.Сумма) КАК Сумма,
	|	СУММА(0) КАК СуммаУвеличение,
	|	СУММА(0) КАК СуммаУменьшение,
	|	СУММА(СчетФактураНалоговыйАгентРасшифровкаСуммы.СуммаНДС) КАК СуммаНДС,
	|	СУММА(0) КАК СуммаНДСУвеличение,
	|	СУММА(0) КАК СуммаНДСУменьшение
	|ИЗ
	|	Документ.СчетФактураНалоговыйАгент.РасшифровкаСуммы КАК СчетФактураНалоговыйАгентРасшифровкаСуммы
	|ГДЕ
	|	СчетФактураНалоговыйАгентРасшифровкаСуммы.Ссылка = &Ссылка
	|
	|СГРУППИРОВАТЬ ПО
	|	СчетФактураНалоговыйАгентРасшифровкаСуммы.Ссылка,
	|	СчетФактураНалоговыйАгентРасшифровкаСуммы.Ссылка.Валюта";
	
	Возврат ТекстЗапроса;

КонецФункции

Функция ПараметрыСчетаНаОплату(ДокументСсылка) Экспорт
	
	Возврат Неопределено;
	
КонецФункции

// Вызывается из Модуль_ИнтеграцияУниверсальный.ЗаполнитьОбстФормСЧФ820.
//
// Параметры:
//  СчетФактура			 - ДокументСсылка.СчетФактураВыданный	 - счет фактура на основании которой вычисляется ОбстФормСЧФ;
//
// Возвращаемое значение:
//  Строка - вычисленное значение ОбстФормСЧФ
//
Функция ПолучитьОбстоятельствоФормированияСчетФактуры(СчетФактура) Экспорт
	
	Результат		 = Неопределено;
	
	КонстантаИспользоватьКомиссиюПриПродажах = Ложь;
	Если МетодСервера(, "ЕстьМетаданныеКонфигурации_ДД", "Константы.ИспользоватьКомиссиюПриПродажах") Тогда
		КонстантаИспользоватьКомиссиюПриПродажах = Константы.ИспользоватьКомиссиюПриПродажах.Получить();
	КонецЕсли;
	
	Если КонстантаИспользоватьКомиссиюПриПродажах Тогда
		
		Если ЭтоСчетФактура(СчетФактура) Тогда
			ДокументыОснования = ВсеДокументыОснования(СчетФактура);
		Иначе
			ДокументыОснования = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(СчетФактура);
		КонецЕсли;
		
		ОперацияПередачаНаКомиссию = ПредопределенноеЗначение("Перечисление.ХозяйственныеОперации.ПередачаНаКомиссию");
		
		Для каждого ДокументОснования Из ДокументыОснования Цикл
			ВидОперации = ХозяйственнаяОперацияДокумента(ДокументОснования);
			
			Если ВидОперации = ОперацияПередачаНаКомиссию Тогда
				Результат = "4";
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		ЕстьОтчетКомитенту = МетодСервера(, "ЕстьМетаданныеКонфигурации_ДД", "Документы.ОтчетКомитенту");
		
		Если НЕ ЗначениеЗаполнено(Результат) И ЕстьОтчетКомитенту Тогда
			
			Для каждого ДокументОснования Из ДокументыОснования Цикл
				
				Если ТипЗнч(ДокументОснования) = Тип("ДокументСсылка.ОтчетКомитенту") Тогда
					Результат = "6";
					Прервать;
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Заполняет реквизит ДанныеПрослеживаемости и ПрослеживаемыйТовар в ТаблицеТоваров
//
// Параметры
//  СтрокаТовары	- СтрокаТаблицыЗначений	 	- строка таблицы товаров, см. ИнициализацияТаблицыСтрокТовары()
//  Источник	- ВыборкаДетальныеЗаписи	- РезультатПоТабличнойЧасти документа методом ПолучитьДанныеДляПечатнойФормыУКД()
//
Процедура ЗаполнитьСведенияПрослеживаемостиДляОтправкиПоКорректировке(СтрокаТовары, Источник)
	
	Если ЗначениеЗаполнено(Источник.КоличествоПоРНПТ) ИЛИ ЗначениеЗаполнено(Источник.КоличествоПоРНПТДо) Тогда
		
		ДанныеПрослеживаемости = Новый Массив;
		
		СтрокаПрослеживаемости = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НовыеСвойстваЭлементаПрослеживаемостиПоКорректировке");
		СтрокаПрослеживаемости.НомерГТДРегистрационныйНомер			 = НомерГТДСтрокой(Источник.НомерГТД);
		СтрокаПрослеживаемости.КодЕдиницыИзмеренияПрослеживаемости	 = СокрЛП(Источник.ЕдиницаИзмеренияТНВЭДКод);
		СтрокаПрослеживаемости.КоличествоПослеИзменения				 = Источник.КоличествоПоРНПТ;
		СтрокаПрослеживаемости.КоличествоДоИзменения				 = Источник.КоличествоПоРНПТДо; 
		
		ЭтоУменьшение = СтрокаПрослеживаемости.КоличествоДоИзменения > СтрокаПрослеживаемости.КоличествоПослеИзменения; 
		Если ЭтоУменьшение Тогда
			КоличествоУменьшение = СтрокаПрослеживаемости.КоличествоДоИзменения - СтрокаПрослеживаемости.КоличествоПослеИзменения;
			СтрокаПрослеживаемости.КоличествоУменьшение = КоличествоУменьшение;
		КонецЕсли; 
		
		ЭтоУвеличение = СтрокаПрослеживаемости.КоличествоПослеИзменения > СтрокаПрослеживаемости.КоличествоДоИзменения;
		Если ЭтоУвеличение Тогда
			КоличествоУвеличение = СтрокаПрослеживаемости.КоличествоПослеИзменения - СтрокаПрослеживаемости.КоличествоДоИзменения;
			СтрокаПрослеживаемости.КоличествоУвеличение = КоличествоУвеличение;
		КонецЕсли; 
		
		ДанныеПрослеживаемости.Добавить(СтрокаПрослеживаемости);
		СтрокаТовары.ДанныеПрослеживаемости	 = ДанныеПрослеживаемости;
		СтрокаТовары.ПрослеживаемыйТовар		 = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

// Заполняет реквизит ДанныеПрослеживаемости и ПрослеживаемыйТовар в ТаблицеТоваров
//
// Параметры
//  СтрокаТовары	- СтрокаТаблицыЗначений	 	- строка таблицы товаров, см. ИнициализацияТаблицыСтрокТовары()
//  Источник	- ВыборкаДетальныеЗаписи	- РезультатПоТабличнойЧасти документа методом ПолучитьДанныеДляПечатнойФормыУПД()
//
Процедура ЗаполнитьСведенияПрослеживаемостиДляИсправительногоСчетФактура(СтрокаТовары, Источник)
	
	Если ЗначениеЗаполнено(Источник.КоличествоПоРНПТ) ИЛИ ЗначениеЗаполнено(Источник.КоличествоПоРНПТДо) Тогда
		
		ДанныеПрослеживаемости = Новый Массив;
		
		СтрокаПрослеживаемости = НовыеСвойстваЭлементаПрослеживаемости();
		СтрокаПрослеживаемости.ЕдиницаИзмеренияПрослеживаемости			 = Источник.ЕдиницаИзмерения;
		СтрокаПрослеживаемости.КодЕдиницыИзмеренияПрослеживаемости	 = СокрЛП(Источник.ЕдиницаИзмеренияКод);
		СтрокаПрослеживаемости.КоличествоПоРНПТ				 = Источник.КоличествоПоРНПТ;
		СтрокаПрослеживаемости.НомерГТДРегистрационныйНомер				 = СокрЛП(Строка(Источник.НомерГТД)); 
		
		ДанныеПрослеживаемости.Добавить(СтрокаПрослеживаемости);
		СтрокаТовары.ДанныеПрослеживаемости	 = ДанныеПрослеживаемости;
		СтрокаТовары.ПрослеживаемыйТовар		 = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ЭтоВыбытиеИзОборотаМаркируемойПродукции(ДокументСсылка) Экспорт
	
	Результат = Ложь;
	
	ВерсияУТ = ВерсияТорговойПодсистемы();
	
	Если ЭтоРедакция11_5() Тогда
		НачальнаяВерсияУТ = "11.5.7.288";
	Иначе
		НачальнаяВерсияУТ = "11.4.14.95";
	КонецЕсли;
		
	ОтсутствуетУчетВыбытия = СравнитьВерсии(ВерсияУТ, НачальнаяВерсияУТ) < 0;
	
	Если ОтсутствуетУчетВыбытия Тогда
		Возврат Результат;
	КонецЕсли;
	
	Если ЭтоСчетФактура(ДокументСсылка) Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ВЫБОР
		|		КОГДА ДокументыОснования.ДокументОснование ССЫЛКА Документ.КорректировкаРеализации
		|			ТОГДА ВЫРАЗИТЬ(ДокументыОснования.ДокументОснование КАК Документ.КорректировкаРеализации).ДокументОснование
		|		ИНАЧЕ ДокументыОснования.ДокументОснование
		|	КОНЕЦ КАК ПервичныйДокумент
		|ПОМЕСТИТЬ ВТ_ПервичныеДокументы
		|ИЗ
		|	Документ.СчетФактураВыданный.ДокументыОснования КАК ДокументыОснования
		|ГДЕ
		|	ДокументыОснования.Ссылка = &Ссылка
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_ПервичныеДокументы.ПервичныйДокумент КАК ПервичныйДокумент,
		|	ВЫБОР
		|		КОГДА ВТ_ПервичныеДокументы.ПервичныйДокумент ССЫЛКА Документ.РеализацияТоваровУслуг
		|			ТОГДА ВЫРАЗИТЬ(ВТ_ПервичныеДокументы.ПервичныйДокумент КАК Документ.РеализацияТоваровУслуг).ВариантВыбытияМаркируемойПродукции
		|		ИНАЧЕ НЕОПРЕДЕЛЕНО
		|	КОНЕЦ КАК ВариантВыбытияМаркируемойПродукции
		|ПОМЕСТИТЬ ВТ_ВариантыВыбытияМаркируемойПродукции
		|ИЗ
		|	ВТ_ПервичныеДокументы КАК ВТ_ПервичныеДокументы
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	МИНИМУМ(ВЫБОР
		|			КОГДА ВТ_ВариантыВыбытияМаркируемойПродукции.ВариантВыбытияМаркируемойПродукции = ЗНАЧЕНИЕ(Перечисление.ВариантыВыбытияМаркируемойПродукции.ИспользованиеПокупателемДляСобственныхНужд)
		|				ТОГДА ИСТИНА
		|			ИНАЧЕ ЛОЖЬ
		|		КОНЕЦ) КАК ВыбытиеМаркируемойПродукции
		|ИЗ
		|	ВТ_ВариантыВыбытияМаркируемойПродукции КАК ВТ_ВариантыВыбытияМаркируемойПродукции";
		
		Запрос.УстановитьПараметр("Ссылка", ДокументСсылка);
		
		Выборка = Запрос.Выполнить().Выбрать();
		
		Если Выборка.Следующий() Тогда
			Результат = Выборка.ВыбытиеМаркируемойПродукции;
		КонецЕсли;
		
	ИначеЕсли ЭтоНакладная(ДокументСсылка) Тогда
		
		ПризнакВыбытия = Перечисления.ВариантыВыбытияМаркируемойПродукции.ИспользованиеПокупателемДляСобственныхНужд;
		Значение = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "ВариантВыбытияМаркируемойПродукции");
		Результат = (Значение = ПризнакВыбытия);
		
	ИначеЕсли ЭтоКорректировкаРеализации(ДокументСсылка) Тогда
		
		ПризнакВыбытия = Перечисления.ВариантыВыбытияМаркируемойПродукции.ИспользованиеПокупателемДляСобственныхНужд;
		Значение = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка.ДокументОснование, "ВариантВыбытияМаркируемойПродукции");
		Результат = (Значение = ПризнакВыбытия);
		
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции

//} ПОЛУЧЕНИЕ ДАННЫХ
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
//{ РАБОТА С КОНТЕНТОМ

////////////////////////////////////////////////////////////////////////////////
//{ ОБЩИЕ ПРОЦЕДУРЫ И ФУНКЦИИ

Процедура ЗаполнитьСтруктурированноеОписаниеАдреса(AddressContent, СведенияОбОрганизации, ЮрФизЛицо, ЮрАдрес, ДатаДокумента, ПредставлениеАдреса = "")
	
	ВладелецКИ	 = ХранилищеКонтактнойИнформацииОбъекта(ЮрФизЛицо);
	ВидАдреса	 = ПолучитьВидКИ(ВладелецКИ, Юрадрес);
	Адрес		 = АдресЮрФизЛица(ВладелецКИ, ВидАдреса, ДатаДокумента);
	
	Если Не ЗначениеЗаполнено(Адрес.ЗначенияПолей) Тогда 
		Возврат;
	КонецЕсли;
	
	СтруктураАдреса = СведенияОбАдресе(Адрес.ЗначенияПолей);
	
	// Обертка для корректного заполнения адреса доставки (грузополучателя) по данным документа:
	// код страны определяем по фактическому адресу грузополучателя, а представление подставляем из документа
	Если ЗначениеЗаполнено(ПредставлениеАдреса) Тогда 
		СтруктураАдреса.СохраненноеПредставление = ПредставлениеАдреса;
	// используем пользовательское представление адреса	
	Иначе
		СтруктураАдреса.СохраненноеПредставление = ?(ЮрАдрес, СведенияОбОрганизации.ЮридическийАдрес, СведенияОбОрганизации.ФактическийАдрес);	
	КонецЕсли;
	
	Заполнить_AddressInfo(AddressContent, СтруктураАдреса);
	
КонецПроцедуры

Функция ПолучитьДатуНомерСвидетельстваОГосРегистрацииИП(Организация)

	Возврат МетодСервера("Модуль_ИнтеграцияУниверсальный", "ПолучитьДатуНомерСвидетельстваОГосРегистрацииИП", Организация);
	
КонецФункции

Функция СформироватьПредставлениеКонтрагентаДляАкта(Организация, ДатаОперации)
	
	СведенияОбОрганизации = РеквизитыЮрФизЛица(Организация, ДатаОперации);
	Возврат ОтрезатьПравуюЗапятую(ДобавитьРазделитель(СведенияОбОрганизации.ПолноеНаименование) + ДобавитьРазделитель(СведенияОбОрганизации.ИНН, "ИНН: ") + ДобавитьРазделитель(СведенияОбОрганизации.КПП, "КПП: "));
	
КонецФункции

// Формирует структуру с данными, которые необходимы для визуализации ПФ счета на оплату. 
// Табличный документ формируется средствами Диадок
//
// Параметры:
//  СчетНаОплату - ДокументСсылка	- документ, на основании которого формируем печатную форму 
// 
// Возвращаемое значение:
//   Структура
//
Функция РезультатВизуализацииСчетаНаОплатуСредствамиДиадок(ДокСсылка, ПараметрыОтправкиНаСогласование =  Неопределено, ФИОПодписанта = "") Экспорт
	
	Если ТипЗнч(ДокСсылка) = Тип("ДокументСсылка.СчетНаОплатуКлиенту") Тогда
		ДокСсылкаДляПечати = ДокСсылка.ДокументОснование;
	Иначе
		ДокСсылкаДляПечати = ДокСсылка;	
	КонецЕсли;
	
	ДанныеШапки = ДанныеШапки(ДокСсылкаДляПечати);
	ДатаНомер = ДанныеШапки.ДатаНомерДокумента;
	
	Макет = ОбработкаОбъект.ПолучитьМакет("СчетНаОплату");
	Шапка1 = Макет.ПолучитьОбласть("ШапкаСчета1");
	Таб = Новый ТабличныйДокумент;
	
	МассивОбъектов= Новый Массив;
	МассивОбъектов.Добавить(ДокСсылка);
	
	Попытка
		ИмяТипаДокумента = ДокСсылка.Метаданные().Имя;
		СтруктураДанныхДляПечати = Документы[ИмяТипаДокумента].ПолучитьДанныеДляПечатнойФормыСчетаНаОплату(, МассивОбъектов);
	Исключение
	КонецПопытки;
	
	Если СтруктураДанныхДляПечати <> Неопределено Тогда
		
		ВыборкаШапка = СтруктураДанныхДляПечати.РезультатПоШапке.Выбрать();
		ВыборкаШапка.Следующий();
		
		Шапка1.Параметры.БанкПолучателя 	 = ВыборкаШапка.НаименованиеБанка;
		Шапка1.Параметры.БИКБанкаПолучателя  = ВыборкаШапка.БИКБанк;
		Шапка1.Параметры.СчетБанкаПолучателя = ВыборкаШапка.КоррСчетБанка;
		Шапка1.Параметры.СчетПолучателя 	 = ВыборкаШапка.НомерБанковскогоСчета;
		
		СведенияОбОрганизации= РеквизитыЮрФизЛица(ВыборкаШапка.Организация, ВыборкаШапка.Дата);
		
		Шапка1.Параметры.ИННПолучателя 		 = СведенияОбОрганизации.ИНН;
		Шапка1.Параметры.КПППолучателя 		 = СведенияОбОрганизации.КПП;
		
		Если СведенияОбОрганизации.Свойство("НаименованиеДляПечатныхФорм") Тогда
			Шапка1.Параметры.Получатель		= СведенияОбОрганизации.НаименованиеДляПечатныхФорм;
		Иначе
			Шапка1.Параметры.Получатель 	= СведенияОбОрганизации.ПолноеНаименование;
		КонецЕсли;
		
	Иначе
		
		СведенияОбОрганизации = РеквизитыЮрФизЛица(ДанныеШапки.Организация, ДатаНомер.ДатаДокумента);
		Если ЗначениеЗаполнено(СведенияОбОрганизации.Банк) Тогда
			Шапка1.Параметры.БанкПолучателя 	 = СведенияОбОрганизации.Банк;
			Шапка1.Параметры.БИКБанкаПолучателя  = СведенияОбОрганизации.БИК;
			Шапка1.Параметры.СчетБанкаПолучателя = СведенияОбОрганизации.КоррСчет;
			Шапка1.Параметры.СчетПолучателя 	 = СведенияОбОрганизации.НомерСчета;
			Шапка1.Параметры.ИННПолучателя 		 = СведенияОбОрганизации.ИНН;
			Шапка1.Параметры.КПППолучателя 		 = СведенияОбОрганизации.КПП;
			Если СведенияОбОрганизации.Свойство("НаименованиеДляПечатныхФорм") Тогда
				Шапка1.Параметры.Получатель		= СведенияОбОрганизации.НаименованиеДляПечатныхФорм;
			Иначе
				Шапка1.Параметры.Получатель 	= СведенияОбОрганизации.ПолноеНаименование;
			КонецЕсли;
		КонецЕсли;	
		
	КонецЕсли;
	
	Таб.Вывести(Шапка1);
	
	ДатаНомер = ДанныеШапки.ДатаНомерДокумента;
	НомерДокумента = ДатаНомер.НомерДокумента;
	ДатаДокумента  = ДатаНомер.ДатаДокумента;
	
	Шапка2 =  Макет.ПолучитьОбласть("ШапкаСчета2");
	Шапка2.Параметры.Заголовок = СтрШаблон("Счет на оплату № %1 от %2",
		НомерДокумента,
		Формат(ДатаДокумента, "ДФ=dd.MM.yyyy"));
	Таб.Вывести(Шапка2);
	
	ОписаниеОрганизации = ПредставлениеЮрФизЛица(СведенияОбОрганизации, "НаименованиеДляПечатныхФорм,ИНН,ЮридическийАдрес,Телефоны,НомерСчета,Банк,БИК,КоррСчет");
	Шапка3 =  Макет.ПолучитьОбласть("ШапкаСчета3");
	Шапка3.Параметры.Поставщик = ОписаниеОрганизации;
	Таб.Вывести(Шапка3);
	
	СведенияОбКонтрагенте = РеквизитыЮрФизЛица(ДанныеШапки.Контрагент, ДатаДокумента);
	
	ОписаниеКонтрагента = ПредставлениеЮрФизЛица(СведенияОбКонтрагенте, "НаименованиеДляПечатныхФорм,ИНН,ЮридическийАдрес,Телефоны,НомерСчета,Банк,БИК,КоррСчет");
	Шапка4 =  Макет.ПолучитьОбласть("ШапкаСчета4");
	Шапка4.Параметры.Покупатель = ОписаниеКонтрагента;
	Таб.Вывести(Шапка4);
	
	ВыводитьКолонкуСкидка = Ложь;

	Если ВыводитьКолонкуСкидка Тогда
		ШапкаТаблицы =  Макет.ПолучитьОбласть("ШапкаТаблицы");
		СтрокаТаблицы = Макет.ПолучитьОбласть("Строка");
	Иначе
		ШапкаТаблицы =  Макет.ПолучитьОбласть("ШапкаТаблицыБезСкидок");
		СтрокаТаблицы = Макет.ПолучитьОбласть("СтрокаБезСкидок");
	КонецЕсли;
	
	Таб.Вывести(ШапкаТаблицы);
	
	ЦенаВключаетНДС = ДанныеШапки.ЦенаВключаетНДС;
	
	СуммаВсего = 0;
	СуммаВсегоНДС = 0;
	ц = 0;
	
	ЕстьСтавкаБезНДС = Ложь;
	
	Если ЗначениеЗаполнено(СтруктураДанныхДляПечати) И СтруктураДанныхДляПечати.Свойство("РезультатПоТабличнойЧасти") Тогда
		
		Выборка = СтруктураДанныхДляПечати.РезультатПоТабличнойЧасти.Выбрать();
		
		ВсегоНаименований = Выборка.Количество();
		
		ЕстьСодержание = СтруктураДанныхДляПечати.РезультатПоТабличнойЧасти.Колонки.Найти("Содержание") <> Неопределено;
		
		Пока Выборка.Следующий() Цикл
			
			СтрокаТаблицы.Параметры.Заполнить(Выборка);
			
			Если ЕстьСодержание И ЗначениеЗаполнено(Выборка.Содержание) Тогда
				СтрокаТаблицы.Параметры.Товар = Выборка.Содержание;
			Иначе
				СтрокаТаблицы.Параметры.Товар = ?(ЗначениеЗаполнено(Выборка.НаименованиеПолное), Выборка.НаименованиеПолное, СокрЛП(Выборка.Номенклатура));
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(Выборка.Артикул) Тогда
				СтрокаТаблицы.Параметры.Артикул = Выборка.Код;
			КонецЕсли; 
			
			Если МенеджерИнтеграции_ЭтоСтавкаБезНДС(Выборка.СтавкаНДС) Тогда
				ЕстьСтавкаБезНДС = Истина;
			КонецЕсли;
			
			Таб.Вывести(СтрокаТаблицы);
			
			СуммаВсего	  = СуммаВсего	  + Выборка.Сумма;
			СуммаВсегоНДС = СуммаВсегоНДС + Выборка.СуммаНДС;
			
		КонецЦикла; 
		
	Иначе
		
		ТаблицаТоваров = ПолучитьДанныеТЧ(ДокСсылкаДляПечати, Неопределено, Неопределено, Ложь);
		ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(ДанныеШапки, ТаблицаТоваров);
		
		Если ДанныеШапки.НДСИсчисляетсяНалоговымАгентом Тогда
			МетодСервера(, "ЗаполнитьВТаблице_СтавкаНДС_ИсчНалАг", ТаблицаТоваров);
		КонецЕсли;
		
		ВсегоНаименований = ТаблицаТоваров.Количество();
		
		Для Каждого Стр Из ТаблицаТоваров Цикл
			
			Ц = Ц + 1;
			СтрокаТаблицы.Параметры.НомерСтроки = Ц;
			СтрокаТаблицы.Параметры.Артикул = Стр.КодНоменклатуры;
			СтрокаТаблицы.Параметры.Товар = Стр.НоменклатураПредставление;
			СтрокаТаблицы.Параметры.Количество = Стр.Количество;
			СтрокаТаблицы.Параметры.ЕдиницаИзмерения = Строка(Стр.ЕдиницаИзмерения);
			
			Если Стр.Цена = 0
				И Стр.Сумма <> 0
				И Стр.Количество <> 0 Тогда
				// Пропатчим цену. Запрос в функции "ПолучитьДанныеТЧ" вытаскивает документы не из Счета, а из РТУ, из ТЧ "Виды запасов", в которой нет цены.
				// Чтобы не ломать все остальное, посчитаем цену здесь
				Если ЦенаВключаетНДС Тогда
					Стр.Цена = Окр(Стр.Сумма / Стр.Количество, 2);
				Иначе
					Стр.Цена = Окр((Стр.Сумма - Стр.СуммаНДС) / Стр.Количество, 2);
				КонецЕсли;
			КонецЕсли;
			
			СтрокаТаблицы.Параметры.Цена = Стр.Цена;
			
			Если ЦенаВключаетНДС Тогда
				СтрокаТаблицы.Параметры.Сумма = Стр.сумма;
			Иначе	
				СтрокаТаблицы.Параметры.Сумма = Стр.сумма - Стр.СуммаНДС;
			КонецЕсли;
			
			Если МенеджерИнтеграции_ЭтоСтавкаБезНДС(Стр.СтавкаНДС) Тогда
				ЕстьСтавкаБезНДС= Истина;
			КонецЕсли;
			
			СуммаВсего = СуммаВсего + Стр.Сумма;
			СуммаВсегоНДС = СуммаВсегоНДС + Стр.СуммаНДС;
			
			Таб.Вывести(СтрокаТаблицы);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Подвал = Макет.ПолучитьОбласть("Подвал");
	Таб.Вывести(Подвал);
				
	Подвал = Макет.ПолучитьОбласть("ПодвалИтого");
	
	Подвал.Параметры.Всего = СуммаВсего;
	
	Таб.Вывести(Подвал);
	
	ПодвалНДС = Макет.ПолучитьОбласть("ПодвалНДС");
	Если ДанныеШапки.НДСИсчисляетсяНалоговымАгентом Тогда
		ПодвалНДС.Параметры.НДС = "НДС исчисляется налоговым агентом";
		ПодвалНДС.Параметры.ВсегоНДС = "-";	
	ИначеЕсли СуммаВсегоНДС <> 0 Тогда
		ПодвалНДС.Параметры.НДС = ?(ЦенаВключаетНДС, "В том числе НДС:", "Сумма НДС:");
		ПодвалНДС.Параметры.ВсегоНДС = Формат(СуммаВсегоНДС, "ЧЦ=15; ЧДЦ=2");
	Иначе
		ПодвалНДС.Параметры.НДС = "Без налога (НДС)";
		ПодвалНДС.Параметры.ВсегоНДС = "-";
	КонецЕсли;
	Таб.Вывести(ПодвалНДС);
	
	СуммаИтого = СуммаВсего + ?(ЦенаВключаетНДС, 0, СуммаВсегоНДС);
	
	ПодвалИтого = Макет.ПолучитьОбласть("ПодвалИтогоКОплате");
	ПодвалИтого.Параметры.ИтогоКОплате = СуммаИтого;
	Таб.Вывести(ПодвалИтого);
	
	ПодвалСуммаПрописью = Макет.ПолучитьОбласть("ПодвалСуммаПрописью");
	ПодвалСуммаПрописью.Параметры.ИтоговаяСтрока = "Всего наименований " + ВсегоНаименований
	+ ", на сумму "+ Формат(СуммаИтого, "ЧЦ=15; ЧДЦ=2") + " " + СокрП(ДанныеШапки.ВалютаДокумента);
	
	ПодвалСуммаПрописью.Параметры.СуммаПрописью = ЧислоПрописью(СуммаИтого, , МетодСервера("ПечатныеФормы", "ПараметрыИсчисленияВалюты", ДанныеШапки.ВалютаДокумента.Код));
	Таб.Вывести(ПодвалСуммаПрописью);
	
	ПодвалСчета = Макет.ПолучитьОбласть("ПодвалСчета");
	Если НЕ ПараметрыОтправкиНаСогласование = Неопределено Тогда 
		ПодвалСчета.Параметры.ФИОРуководителя = ?(ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПодписанта), ПараметрыОтправкиНаСогласование.ФИОПодписанта,
												?(ЗначениеЗаполнено(ПараметрыОтправкиНаСогласование.ФИОПолучателя), ПараметрыОтправкиНаСогласование.ФИОПолучателя, "_"));
	Иначе 	
		ПодвалСчета.Параметры.ФИОРуководителя =  ФИОПодписанта;
	КонецЕсли;	
	Таб.Вывести(ПодвалСчета);
	
	Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НовыйРезультатВизуализацииСчетаНаОплату");
	
	Результат.Таблица			= Таб;
	Результат.СписокОшибок		= "";
	Результат.ИмяФайла			= МетодСервера("Модуль_ИнтеграцияУниверсальный", "ИмяФайлаПечатнойФормы", "Счет", НомерДокумента, ДатаДокумента);
	Результат.Дата				= ДатаДокумента;
	Результат.Номер				= НомерДокумента;
	Результат.Сумма				= СуммаИтого;
	Результат.СуммаНДС			= СуммаВсегоНДС;
	Результат.ДокументБезНДС	= ?(ЕстьСтавкаБезНДС И СуммаВсегоНДС = 0, Истина, Ложь);
	
	Возврат Результат;	
	
КонецФункции //РезультатВизуализацииСчетаНаОплатуСредствамиДиадок()

Функция СформироватьМетуСчетаНаОплату(ДокументСсылка) Экспорт
	
	Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РеализацияТоваровУслуг")
		ИЛИ ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.АктВыполненныхРабот") Тогда
		Документ = ДокументСсылка;
	Иначе
		Документ = ДокументСсылка.ДокументОснование;
	КонецЕсли;
	
	Шапка = ДанныеШапки(ДокументСсылка);
	ТабличнаяЧасть = ПолучитьДанныеТЧ(Документ, Неопределено, Неопределено, Ложь);
	ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(Шапка, ТабличнаяЧасть);
	
	ДатаНомер = Шапка.ДатаНомерДокумента;
	
	МетаСчетаНаОплату = Новый Структура;
	МетаСчетаНаОплату.Вставить("Дата"		, ДатаНомер.ДатаДокумента);
	МетаСчетаНаОплату.Вставить("Номер"		, ДатаНомер.НомерДокумента);
	МетаСчетаНаОплату.Вставить("Сумма"		, Шапка.СуммаДокумента);
	МетаСчетаНаОплату.Вставить("СуммаНДС"	, ТабличнаяЧасть.Итог("СуммаНДС"));
	
	Возврат МетаСчетаНаОплату;
	
КонецФункции

Функция СформироватьМетуАктаВыполненныхРабот(ДокументСсылка) Экспорт
	
	Шапка			 = ДанныеШапки(ДокументСсылка);
	ТабличнаяЧасть	 = ПолучитьДанныеТЧ(ДокументСсылка, Неопределено, Неопределено, Ложь);
	ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(Шапка, ТабличнаяЧасть);
	
	ДатаНомер = Шапка.ДатаНомерДокумента;
	
	ТабличнаяЧасть.Колонки.Добавить("СуммаБезНДС");
	
	ИтогСумма=			0;
	ИтогСуммаБезНДС=	0;
	ЕстьСтавкаБезНДС= 	Ложь;
	
	Для каждого Стр Из ТабличнаяЧасть Цикл
		
		СуммаСНДС= 		(Стр.Сумма + ?(Шапка.ЦенаВключаетНДС, 0, Стр.СуммаНДС));
		СуммаБезНДС= 	СуммаСНДС - Стр.СуммаНДС;
		
		ИтогСумма=			ИтогСумма 		+ СуммаСНДС;
		ИтогСуммаБезНДС=	ИтогСуммаБезНДС + СуммаБезНДС;
		
		Если МенеджерИнтеграции_ЭтоСтавкаБезНДС(Стр.СтавкаНДС) Тогда
			ЕстьСтавкаБезНДС= Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	МетаАктаВыполненныхРабот = Новый Структура;
	МетаАктаВыполненныхРабот.Вставить("Дата"			, ДатаНомер.ДатаДокумента);
	МетаАктаВыполненныхРабот.Вставить("Номер"			, ДатаНомер.НомерДокумента);
	МетаАктаВыполненныхРабот.Вставить("Сумма"			, Шапка.СуммаДокумента);
	МетаАктаВыполненныхРабот.Вставить("СуммаНДС"		, ИтогСумма - ИтогСуммаБезНДС);
	МетаАктаВыполненныхРабот.Вставить("ДокументБезНДС"	, ?(ЕстьСтавкаБезНДС И (ИтогСумма - ИтогСуммаБезНДС) = 0, Истина, Ложь));
	
	Возврат МетаАктаВыполненныхРабот;
	
КонецФункции

Функция СформироватьМетуАктаСверки(ДокументСсылка) Экспорт
	
	МетаАктаСверки=	Новый Структура;
	МетаАктаСверки.Вставить("Дата", 	ДокументСсылка.Дата);
	МетаАктаСверки.Вставить("Номер", 	ПредставлениеНомераОбъекта(ДокументСсылка));
	
	Возврат МетаАктаСверки;
	
КонецФункции

// Возвращает значения реквизитов документа, которые используются
// для указания основания передачи.
//
// Параметры:
//  СсылкаНаДокумент - ДокументСсылка - ссылка на документ отгрузки.
// 
// Возвращаемое значение:
//  Структура - содержит реквизиты документа продажи:
//    * Договор
//    * Соглашение
//    * ЗаказКлиента
//    * Основание
//    * ОснованиеНомер
//    * ОснованиеДата
//
Функция РеквизитыДокументаДляПолученияСтруктурыОснованияПередачи(СсылкаНаДокумент)
	
	ИменаРеквизитов = Новый Массив;
	ИменаРеквизитов.Добавить("Договор");
	ИменаРеквизитов.Добавить("Соглашение");
	ИменаРеквизитов.Добавить("ЗаказКлиента");
	ИменаРеквизитов.Добавить("Основание");
	ИменаРеквизитов.Добавить("ОснованиеНомер");
	ИменаРеквизитов.Добавить("ОснованиеДата");
	
	МетаданныеДокумента = СсылкаНаДокумент.Метаданные();
	
	ТребуемыеРеквизиты = Новый Структура;
	
	Для Каждого ИмяРеквизита Из ИменаРеквизитов Цикл
		
		Если ОбщегоНазначения.ЕстьРеквизитОбъекта(ИмяРеквизита, МетаданныеДокумента) Тогда
			ТребуемыеРеквизиты.Вставить(ИмяРеквизита);
		КонецЕсли;
		
	КонецЦикла;
	
	Результат = Новый Структура;
	Если ЗначениеЗаполнено(ТребуемыеРеквизиты) Тогда
		Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(СсылкаНаДокумент, ТребуемыеРеквизиты);
	КонецЕсли;
	
	Результат.Вставить("Ссылка", СсылкаНаДокумент);
	
	Возврат Результат;
	
КонецФункции

// Возвращает значение реквизита, который следует использовать в качестве 
// основания передачи. Это может быть заказ, договор или соглашение.
//
// Параметры:
//  РеквизитыДокумента - Структура - см. РеквизитыДокументаДляПолученияСтруктурыОснованияПередачи
// 
// Возвращаемое значение:
//  ЛюбаяСсылка	 - ссылка на заказ, договор или соглашение.
//  Неопределено - если у документа отсутствуют реквизиты, в которых указывается
//                 основание передачи, или ни один из этих реквизитов не заполнен.
//
Функция ОснованиеПередачиДокумента(РеквизитыДокумента)
	
	Если РеквизитыДокумента.Свойство("ЗаказКлиента")
		И ЗначениеЗаполнено(РеквизитыДокумента.ЗаказКлиента) Тогда
		
		Результат = РеквизитыДокумента.ЗаказКлиента;
		
	ИначеЕсли РеквизитыДокумента.Свойство("Договор")
		И ЗначениеЗаполнено(РеквизитыДокумента.Договор) Тогда
		
		Результат = РеквизитыДокумента.Договор;
		
	ИначеЕсли РеквизитыДокумента.Свойство("Соглашение")
		И ЗначениеЗаполнено(РеквизитыДокумента.Соглашение) Тогда
		
		Результат = РеквизитыДокумента.Соглашение;
		
	Иначе
		
		Результат = Неопределено;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Формирует описание основания передачи с помощью программного
// интерфейса менеджера объекта. Так как программный интерфейс
// возвращает структуру, в которой отсутствует отдельное поле
// с названием вида основания передачи, в наименование основания
// дублируется его представление.
//
// Параметры:
//  СсылкаНаДокумент - ДокументСсылка - ссылка на документ отгрузки.
// 
// Возвращаемое значение:
//  Структура	 - см. НоваяСтруктураОснованияПередачи()
//  Неопределено - если в программном интерфейсе менеджера объекта
//                 отсутствует метод СтруктураОснованияДляПечати
//
Функция СтруктураОснованияДляПечати(СсылкаНаДокумент)
	
	МенеджерДокумента = Документы[СсылкаНаДокумент.Метаданные().Имя];
	
	Попытка
		СтруктураОснования = МенеджерДокумента.СтруктураОснованияДляПечати(СсылкаНаДокумент);
	Исключение
		СтруктураОснования = Неопределено;
	КонецПопытки;
	
	Результат = Неопределено;
	
	Если ЗначениеЗаполнено(СтруктураОснования) 
		И ЗначениеЗаполнено(СтруктураОснования.Основание) Тогда
		
		Результат = НоваяСтруктураОснованияПередачи(СсылкаНаДокумент);
		Результат.Presentation	 = СтруктураОснования.Основание;
		Результат.GroundName	 = СтруктураОснования.Основание;
		Результат.GroundNumber	 = СтруктураОснования.ОснованиеНомер;
		Результат.GroundDate	 = СтруктураОснования.ОснованиеДата;
		Результат.ПредставлениеВТекстеОшибки = "реквизитах печати";
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Формирует описание основания передачи по реквизитам объекта.
//
// Параметры:
//  ОснованиеПередачи - ЛюбаяСсылка - ссылка на объект, являющийся основанием-передачи.
// 
// Возвращаемое значение:
//  Структура	 - см. НоваяСтруктураОснованияПередачи()
//  Неопределено - если передан пустой параметр
//
Функция ОписаниеОснованияПередачи(ОснованиеПередачи)
	
	Если Не ЗначениеЗаполнено(ОснованиеПередачи) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	МетаданныеОснования = ОснованиеПередачи.Метаданные();
	ЭтоДокумент = ОбщегоНазначения.ЭтоДокумент(МетаданныеОснования);
	
	ТребуемыеРеквизиты = Новый Структура;
	ТребуемыеРеквизиты.Вставить("GroundNumber"	, "Номер");
	ТребуемыеРеквизиты.Вставить("GroundDate"	, "Дата");
	
	Если Не ЭтоДокумент Тогда
		
		ВыражениеНаименование = "Наименование";
		Если ОбщегоНазначения.ЕстьРеквизитОбъекта("НаименованиеДляПечати", МетаданныеОснования) Тогда
			ВыражениеНаименование = "НаименованиеДляПечати";
		КонецЕсли;
		
		ТребуемыеРеквизиты.Вставить("GroundName"	, ВыражениеНаименование);
		ТребуемыеРеквизиты.Вставить("Presentation"	, ВыражениеНаименование);
		
	КонецЕсли;
	
	ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОснованиеПередачи, ТребуемыеРеквизиты);
	
	Результат = НоваяСтруктураОснованияПередачи(ОснованиеПередачи);
	ЗаполнитьЗначенияСвойств(Результат, ЗначенияРеквизитов);
	
	Если ЭтоДокумент Тогда
		Результат.GroundNumber = ПредставлениеНомера(Результат.GroundNumber);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Результат.GroundName) Тогда
		Результат.GroundName = МетаданныеОснования.Синоним;
	КонецЕсли;
	
	Результат.GroundName	 = СокрЛП(Результат.GroundName);
	Результат.Presentation	 = СокрЛП(Результат.Presentation);
	
	Возврат Результат;
	
КонецФункции

// Инициализирует структуру, описывающую основание передачи товаров, работ, услуг.
//
// Параметры:
//  ОснованиеПередачи - ЛюбаяСсылка, Неопределено - источник сведений об основании передачи.
// 
// Возвращаемое значение:
//  Структура - содержит ключи:
//   * GroundName	 - Строка - Наименование документа-основания
//   * GroundNumber	 - Строка - Номер основания передачи
//   * GroundDate	 - Дата	  - Дата основания передачи
//   * Presentation	 - Строка - Представление документа-основания
//   * ОснованиеСсылка - ЛюбаяСсылка, Неопределено - источник сведений об основании передачи.
//   * ПредставлениеВТекстеОшибки - Строка - представление вида основания передачи в родительном падеже.
//                                  Используется при формировании сообщений в механизме валидации контента.
//
Функция НоваяСтруктураОснованияПередачи(ОснованиеПередачи = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("GroundName"		, "");
	Результат.Вставить("GroundNumber"	, "");
	Результат.Вставить("GroundDate"		, Дата(1, 1, 1));
	Результат.Вставить("Presentation"	, "");
	
	Результат.Вставить("ОснованиеСсылка"			, ОснованиеПередачи);
	Результат.Вставить("ПредставлениеВТекстеОшибки"	, "основании");
	
	Возврат Результат;
	
КонецФункции

// Удаляет из наименования основания сведения о дате и номере.
//
// Параметры:
//  ОписаниеОснованияПередачи - Структура - см. функцию НоваяСтруктураОснованияПередачи.
//
Процедура УдалитьДатуНомерИзНаименованияОснованияПередачи(ОписаниеОснованияПередачи)
	
	Если Не ЗначениеЗаполнено(ОписаниеОснованияПередачи) Тогда
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОписаниеОснованияПередачи.GroundDate)
		И ЗначениеЗаполнено(ОписаниеОснованияПередачи.GroundNumber) Тогда
		
		ПозНомера = Найти(ОписаниеОснованияПередачи.GroundName, "№");
		
		Если ПозНомера > 0 Тогда
			
			// Удаляем из наименования номер и дату, т.к. если они есть в реквизитах основания.
			ОписаниеОснованияПередачи.GroundName = СокрЛП(Лев(ОписаниеОснованияПередачи.GroundName, ПозНомера - 1));
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Проверяет, заполнено ли представление основания передачи.
// Если не заполнено, формирует представление наименованию, дате и номеру.
//
// Параметры:
//  ОписаниеОснованияПередачи - Структура - см. НоваяСтруктураОснованияПередачи.
//
Процедура ПроверитьЗаполнитьПредставлениеОснованияПередачи(ОписаниеОснованияПередачи)
	
	Если Не ЗначениеЗаполнено(ОписаниеОснованияПередачи) Тогда
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОписаниеОснованияПередачи.Presentation) Тогда
		Возврат;
	КонецЕсли;
	
	ЧастиПредставления = Новый Массив;
	ЧастиПредставления.Добавить(ОписаниеОснованияПередачи.GroundName);
	
	Если ЗначениеЗаполнено(ОписаниеОснованияПередачи.GroundNumber) Тогда
		ЧастиПредставления.Добавить("№");
		ЧастиПредставления.Добавить(ОписаниеОснованияПередачи.GroundNumber);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОписаниеОснованияПередачи.GroundDate) Тогда
		ЧастиПредставления.Добавить("от");
		ЧастиПредставления.Добавить(Формат(ОписаниеОснованияПередачи.GroundDate, "ДФ=dd.MM.yyyy"));
	КонецЕсли;
	
	ОписаниеОснованияПередачи.Presentation = СтрСоединить(ЧастиПредставления, " ");
	
КонецПроцедуры

// Формирует структуру сведений об основании передачи товаров,
// услуг, работ по документу.
//
// Параметры:
//  ДокументСсылка - ДокументСсылка - документ передачи товаров, услуг, работ.
// 
// Возвращаемое значение:
//  Структура - см. НоваяСтруктураОснованияПередачи
//
Функция СтруктурированноеПредставлениеОснования(ДокументСсылка)
	
	РеквизитыДокумента = РеквизитыДокументаДляПолученияСтруктурыОснованияПередачи(ДокументСсылка);
	
	ЕстьРеквизитыДляПечати = РеквизитыДокумента.Свойство("Основание")
								И РеквизитыДокумента.Свойство("ОснованиеДата")
								И РеквизитыДокумента.Свойство("ОснованиеНомер");
	
	Если ЕстьРеквизитыДляПечати И ЗначениеЗаполнено(РеквизитыДокумента.Основание) Тогда
		
		// Использовать для заполенения реквизиты для печати
		Результат = НоваяСтруктураОснованияПередачи(ДокументСсылка);
		
		Результат.GroundName	 = РеквизитыДокумента.Основание;
		Результат.GroundNumber	 = РеквизитыДокумента.ОснованиеНомер;
		Результат.GroundDate	 = РеквизитыДокумента.ОснованиеДата;
		Результат.Presentation	 = РеквизитыДокумента.Основание;
		
		Результат.ПредставлениеВТекстеОшибки = "реквизитах печати";
		
	Иначе
		
		// Использовать программный интерфейс конфигурации
		Результат = СтруктураОснованияДляПечати(ДокументСсылка);
		
		Если Не ЗначениеЗаполнено(Результат) Тогда
			
			// Выбрать основание передачи из реквизитов документа (заказ, договор или соглашение)
			ОснованиеПередачи = ОснованиеПередачиДокумента(РеквизитыДокумента);
			
			// Сформировать описание основания передачи
			Результат = ОписаниеОснованияПередачи(ОснованиеПередачи);
			
		КонецЕсли;
		
	КонецЕсли;
	
	УдалитьДатуНомерИзНаименованияОснованияПередачи(Результат);
	ПроверитьЗаполнитьПредставлениеОснованияПередачи(Результат);
	
	Возврат Результат;
	
КонецФункции

// Формирует структуру сведений об основании передачи товаров, услуг, работ при заполнении УКД.
// Информация берется только из реквизитов печати или договора (в указанном порядке) 
//
// Параметры:
//  ДокументСсылка - ДокументСсылка - документ передачи товаров, услуг, работ.
// 
// Возвращаемое значение:
//  Структура - см. НоваяСтруктураОснованияПередачи
//
Функция СтруктурированноеПредставлениеОснованияДляУКД(ДокументСсылка)
	
	РеквизитыДокумента = РеквизитыДокументаДляПолученияСтруктурыОснованияПередачи(ДокументСсылка);
	
	Результат = ОписаниеОснованияПоРеквизитамПечатиДокумента(РеквизитыДокумента);
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		
		Договор = СвойствоСтруктуры(РеквизитыДокумента, "Договор", Неопределено);
		
		Результат = ОписаниеОснованияПередачи(Договор);
		
	КонецЕсли;
	
	УдалитьДатуНомерИзНаименованияОснованияПередачи(Результат);
	ПроверитьЗаполнитьПредставлениеОснованияПередачи(Результат);
	
	Возврат Результат;
	
КонецФункции

Функция ОписаниеОснованияПоРеквизитамПечатиДокумента(РеквизитыДокумента)
	
	Перем Результат;
	
	ЕстьРеквизитыДляПечати = РеквизитыДокумента.Свойство("Основание")
							И РеквизитыДокумента.Свойство("ОснованиеДата")
							И РеквизитыДокумента.Свойство("ОснованиеНомер");
	
	Если ЕстьРеквизитыДляПечати
		И ЗначениеЗаполнено(РеквизитыДокумента.Основание) Тогда
		
		// Использовать для заполенения реквизиты для печати
		Результат = НоваяСтруктураОснованияПередачи(РеквизитыДокумента.Ссылка);
		
		Результат.GroundName	 = РеквизитыДокумента.Основание;
		Результат.GroundNumber	 = РеквизитыДокумента.ОснованиеНомер;
		Результат.GroundDate	 = РеквизитыДокумента.ОснованиеДата;
		Результат.Presentation	 = РеквизитыДокумента.Основание;
		
		Результат.ПредставлениеВТекстеОшибки = "реквизитах печати";
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьСведенияОПередаче(Content, ДокументСсылка, ФункцияУПД, ДопПараметры)
	
	КоллекцияДокументНосительОснования = Новый Массив;
	
	Если ФункцияУПД = "СЧФДОП" ИЛИ ФункцияУПД = "СЧФ" Тогда
		
		МетаданныеДокумента = ДокументСсылка.Метаданные();
		
		// в этом случае параметр ДокументСсылка это Счет фактура выданный
		Если МетаданныеДокумента.ТабличныеЧасти.Найти("ДокументыОснования") <> Неопределено Тогда
			
			Для Каждого СтрокаТЧ Из ДокументСсылка.ДокументыОснования Цикл
				КоллекцияДокументНосительОснования.Добавить(СтрокаТЧ.ДокументОснование);	
			КонецЦикла;			
			
		ИначеЕсли МетаданныеДокумента.Реквизиты.Найти("ДокументОснование") <> Неопределено Тогда
			КоллекцияДокументНосительОснования.Добавить(ДокументСсылка.ДокументОснование);
		КонецЕсли;
		
	Иначе
		// в этом случае параметр ДокументСсылка это документ реализации
		КоллекцияДокументНосительОснования.Добавить(ДокументСсылка);
	КонецЕсли;
	
	УказыватьОтсутствиеОснованияУПД = СвойствоСтруктуры(ДопПараметры, "УказыватьОтсутствиеОснованияУПД", Ложь);
	
	TransferBases = Content.TransferInfo.TransferBases;
	
	Для Каждого ДокументОснование Из КоллекцияДокументНосительОснования Цикл
		
		ДобавитьОснованиеПередачи = Истина;
		
		СтруктураОснования = СтруктурированноеПредставлениеОснования(ДокументОснование);
		
		TransferBase = СформироватьОписаниеОснованияПередачи(СтруктураОснования, УказыватьОтсутствиеОснованияУПД);
		
		TransferBaseЗаполнено = ОписаниеОснованияПередачиЗаполнено(TransferBase);
		Если TransferBaseЗаполнено Тогда
			ДобавитьОснованиеПередачи = КоллекцияСодержитОписаниеОснованияПередачи(TransferBases, TransferBase);
		КонецЕсли;
		
		Если TransferBaseЗаполнено 
			И ДобавитьОснованиеПередачи Тогда
			
			СлужебнаяИнформация = МетодСервера("Модуль_ИнтеграцияУниверсальный", "НоваяСлужебнаяИнформацияОснованияПередачи");
			
			Если ЗначениеЗаполнено(СтруктураОснования) Тогда
				СлужебнаяИнформация.ОснованиеСсылка	= СтруктураОснования.ОснованиеСсылка;
				СлужебнаяИнформация.ПредставлениеВТекстеОшибки	= СтруктураОснования.ПредставлениеВТекстеОшибки;
			КонецЕсли;
			
			ДополнитьКонтентСтрокиОснованийПередачиСлужебнойИнформацией(TransferBase, СлужебнаяИнформация);
			
			TransferBases.Добавить(TransferBase);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если TransferBases.Количество() = 0 Тогда
	
		TransferBase = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_TransferBase");
		Content.TransferInfo.TransferBases.Добавить(TransferBase);
			
		ЗаполнитьTransferBaseКакОтсутствует(TransferBase);
		
	КонецЕсли;
	
КонецПроцедуры

Функция КоллекцияСодержитОписаниеОснованияПередачи(TransferBases, TransferBase)
	
	Результат = Ложь;
	
	НайденноеОснование = МассивСтруктур_Найти(TransferBases, TransferBase);
	Если НайденноеОснование = Неопределено Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОписаниеОснованияПередачиЗаполнено(TransferBase)
	
	Результат = Ложь;
	
	Если ЗначениеЗаполнено(TransferBase)
		И ЗначениеЗаполнено(TransferBase.DocumentName) Тогда
		
		Результат = Истина;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СформироватьОписаниеОснованияПередачи(ОписаниеОснования, УказыватьОтсутствиеОснованияУПД)
	
	Результат = Неопределено;
	
	Если ОписаниеОснования <> Неопределено Тогда
		
		Результат = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_TransferBase");
		
		Если Не ЗначениеЗаполнено(ОписаниеОснования.GroundDate) И УказыватьОтсутствиеОснованияУПД Тогда
			ЗаполнитьTransferBaseКакОтсутствует(Результат);
		Иначе
			ЗаполнитьTransferBaseПоДаннымОснования(Результат, ОписаниеОснования);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция МассивСтруктур_Найти(Массив, ИскомаяСтруктура)
	
	Результат = Неопределено;
	
	Для Каждого СтруктураИзМассива Из Массив Цикл
		
		Результат = СтруктураИзМассива;

		Для Каждого ЭлементСтруктуры Из ИскомаяСтруктура Цикл

			ИмяСвойстваСтруктуры = ЭлементСтруктуры.Ключ;
			ИскомоеЗначение = ЭлементСтруктуры.Значение;

			Если СтруктураИзМассива[ИмяСвойстваСтруктуры] <> ИскомоеЗначение Тогда
				Результат = Неопределено;
				Прервать;
			КонецЕсли;

		КонецЦикла;

	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьTransferBaseПоДаннымОснования(TransferBase, СтруктураОснования)
	
	TransferBase.DocumentName	= СтруктураОснования.GroundName;
	TransferBase.DocumentNumber	= СтруктураОснования.GroundNumber;
	TransferBase.DocumentDate	= СтруктураОснования.GroundDate;
	TransferBase.DocumentInfo	= "";
	
КонецПроцедуры

Процедура ЗаполнитьTransferBaseКакОтсутствует(TransferBase)
	
	TransferBase.DocumentName	= МетодСервера("Модуль_ИнтеграцияУниверсальный", "ДокументОснованиеОтгрузкиОтсутствует");
	TransferBase.DocumentNumber	= "";
	TransferBase.DocumentDate	= Дата(1, 1, 1);
	TransferBase.DocumentInfo	= "";

КонецПроцедуры

Процедура ЗаполнитьСтруктурированноеОписаниеГрузоотправителяГрузополучателя(OrganizationInfo, ЮрФизЛицо, ДатаДокумента, ПредставлениеАдреса = "")
	
	СведенияОбОрганизации = РеквизитыЮрФизЛица(ЮрФизЛицо, ДатаДокумента);
	
	Если МетодСервера(, "ЕстьРеквизитИлиСвойствоОбъектаЛок", ЮрФизЛицо, "СтранаРегистрации") Тогда
		СведенияОбОрганизации.Вставить("СтранаРегистрации", ЮрФизЛицо.СтранаРегистрации);
	КонецЕсли;
	
	Если стрДлина(СведенияОбОрганизации.ИНН) <> 12 Тогда
		НаименованиеОрганизации = СведенияОбОрганизации.ПолноеНаименование;
		ЭтоИП = Ложь;
	Иначе
		Если типЗнч(юрФизЛицо) = тип("СправочникСсылка.Организации") Тогда
			//для нашей организации - данные берем из нормализованного представления физлица
			НаименованиеОрганизации = юрФизЛицо.наименованиеПолное;
		Иначе
			НаименованиеОрганизации = СведенияОбОрганизации.ПолноеНаименование;
		КонецЕсли;
		
		ЭтоИП = Истина;
		
	КонецЕсли;
	
	OrganizationInfo.Name = НаименованиеОрганизации;
	OrganizationInfo.IsSoleProprietor = ЭтоИП;
	OrganizationInfo.SameAsSellerOrBuyer = Ложь;
	OrganizationInfo.Ссылка = ЮрФизЛицо;
	
	ЗаполнитьСтруктурированноеОписаниеАдреса(OrganizationInfo.Address, СведенияОбОрганизации, ЮрФизЛицо, Ложь, ДатаДокумента, ПредставлениеАдреса);
	
КонецПроцедуры

Функция ПолучитьГоловноеЮрлицо(ЮрФизЛицо)
	
	Результат = ЮрФизЛицо;
	
	Если ЭтоОбособленноеПодразделение(ЮрФизЛицо) Тогда 
		
		Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.РегистрацииВНалоговомОргане") Тогда
			ИмяРеквизита = "Владелец";
		ИначеЕсли ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Контрагенты") Тогда
			ИмяРеквизита = "ГоловнойКонтрагент";
		Иначе 
			ИмяРеквизита = "ГоловнаяОрганизация";
		КонецЕсли;
		
		Результат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЮрФизЛицо, ИмяРеквизита);
		
	КонецЕсли;
	
	Возврат Результат
	
КонецФункции

Процедура ЗаполнитьСтруктурированоеОписаниеЮрФизЛица(OrganizationInfo, ЮрФизЛицо, ДатаДокумента, БанковскийСчетОрганизации = Неопределено,  ЮрАдрес, АдресДляСФ = Ложь, КППГрузоотправителя = "", ПредставлениеАдреса = "")

	СведенияОбОрганизации = РеквизитыЮрФизЛица(ЮрФизЛицо, ДатаДокумента,  БанковскийСчетОрганизации);
	
	ЗаполнитьИнформациюОбОрганизации(OrganizationInfo, СведенияОбОрганизации);
	ЗаполнитьСтруктурированноеОписаниеАдреса(OrganizationInfo.Address, СведенияОбОрганизации, ЮрФизЛицо, ЮрАдрес, ДатаДокумента, ПредставлениеАдреса);
	
	OrganizationInfo.вставить("Ссылка", ЮрФизЛицо);

КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo(ExtendedOrganizationInfo, ЮрФизЛицо, ДатаДокумента, ЭтоПродавец = Ложь, БанковскийСчет= Неопределено, КППГрузоотправителя = "", ОбособленноеПодразделение = Неопределено, ПредставлениеАдреса = "")
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда 
		
		Если ЭтоПродавец Тогда 
			СведенияОбОрганизации = РеквизитыПродавца(ЮрФизЛицо, ДатаДокумента, БанковскийСчет, ОбособленноеПодразделение);
		Иначе 
			СведенияОбОрганизации = РеквизитыГрузоотправителя(ЮрФизЛицо, ДатаДокумента, БанковскийСчет, ОбособленноеПодразделение);
		КонецЕсли;
		
		ИсточникСведений = ?(ЗначениеЗаполнено(ОбособленноеПодразделение), ОбособленноеПодразделение, ЮрФизЛицо); // Вероятно, используется в ПМ
		ExtendedOrganizationInfo.Вставить("Ссылка", ИсточникСведений);
		
	Иначе 
		
		СведенияОбОрганизации = РеквизитыЮрФизЛица(ЮрФизЛицо, ДатаДокумента, БанковскийСчет);
		ExtendedOrganizationInfo.Вставить("Ссылка", ЮрФизЛицо);
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(КППГрузоотправителя) Тогда
		СведенияОбОрганизации.КПП = КППГрузоотправителя;
	КонецЕсли;
	
	ЗаполнитьИнформациюОбОрганизации(ExtendedOrganizationInfo, СведенияОбОрганизации);
	ЗаполнитьСтруктурированноеОписаниеАдреса(ExtendedOrganizationInfo.Address, СведенияОбОрганизации, ЮрФизЛицо, ЭтоПродавец, ДатаДокумента, ПредставлениеАдреса);
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда 
		ExtendedOrganizationInfo.Okdp = ЮрФизЛицо.КодОКВЭД;
	КонецЕсли;
	
	Если ExtendedOrganizationInfo.Type = "IndividualEntity"
		И ДатаДокумента >= ДатаРедакции425() Тогда
		
		Если ЮрФизЛицо.Метаданные().Реквизиты.Найти("ДатаРегистрации") <> Неопределено Тогда
			ДатаРегистрацииОрганизации = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЮрФизЛицо, "ДатаРегистрации");
		Иначе
			ДатаРегистрацииОрганизации = "";
		КонецЕсли;
		
		ExtendedOrganizationInfo.IndividualEntityRegistrationCertificate = СтрокаНомерСВ_2026(
			СведенияОбОрганизации.ОГРН,
			ДатаРегистрацииОрганизации
		);
		
	КонецЕсли;
	
КонецПроцедуры

Функция ДатаРедакции425()
	
	Возврат '20260101';
	
КонецФункции

Функция СтрокаНомерСВ_2026(ОГРН, ДатаРегистрации)
	
	ШаблонСтрокиНомерСВ = "%1;%2";
	
	Результат = СтрШаблон(
		ШаблонСтрокиНомерСВ,
		ОГРН,
		ДатаСтрокойЛок(ДатаРегистрации)
	);
	
	Возврат Результат;
	
КонецФункции

Функция ДатаСтрокойЛок(ДатаСтрока)
	
	ПустоеЗначение = "";
	
	Если ПустаяСтрока(ДатаСтрока) Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	Попытка
		Значение = Дата(ДатаСтрока);
	Исключение
		Значение = ДатаСтрока;
	КонецПопытки;
	
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	Возврат Формат(Значение, "ДФ=dd.MM.yyyy");
	
КонецФункции

// Заполняет массив структур поля StructedAdditionalInfos
//
// Параметры:
//  StructedAdditionalInfos - Массив - Коллекция дополнительных сведений
//  ДопСведения             - Строка - Дополнительные сведения в формате "ключ1=значение1;ключ2=значение2"
//
Процедура ЗаполнитьStructedAdditionalInfos(StructedAdditionalInfos, ДопСведения)
	
	МетодСервера("Модуль_ИнтеграцияУниверсальный", "ЗаполнитьStructedAdditionalInfos", StructedAdditionalInfos, ДопСведения);
	
КонецПроцедуры

Функция ЮрФизЛицо_АдресЭлектроннойПочты(ЮрФизЛицо, ДатаДокумента, ЭтоОрганизация)
	
	ВидКИ = ?(ЭтоОрганизация
				, Справочники.ВидыКонтактнойИнформации.EmailОрганизации
				, Справочники.ВидыКонтактнойИнформации.EmailКонтрагента);
	
	Результат = ПредставлениеКонтактнойИнформацииОбъекта(ЮрФизЛицо, ВидКИ, , ДатаДокумента);
	
	Возврат Результат;
	
КонецФункции

Функция СформироватьДанныеДляПечати(ДокументСсылка, ЭтоКСФ)
	
	ЭтоСФ = ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.СчетФактураВыданный");
	ЭтоСФНаАванс = СчетФактуранаАванс(ДокументСсылка);
	
	//сформируем список документов-оснований
	ДокументыОснования = Новый СписокЗначений;
	
	Если ЭтоСФ Тогда
		
		Если ОснованиеСчетаФактурыУказываетсяВТабличнойЧасти() Тогда
			МассивОснований = ОбщегоНазначения.ВыгрузитьКолонку(ДокументСсылка.ДокументыОснования, "ДокументОснование", Истина);
			ДокументыОснования.ЗагрузитьЗначения(МассивОснований);
		Иначе
			ДокументыОснования.Добавить(ДокументСсылка.ДокументОснование);
		КонецЕсли;
		
	Иначе
		ДокументыОснования.Добавить(ДокументСсылка);
	КонецЕсли;
	
	Результат = ДанныеШапки(ДокументСсылка);
	
	ЭтоИСФ = Ложь;
	
	ЭтоИСФ = ЭтоСФ И ЭтоИсправление(ДокументСсылка);
			
	//формируем ТЧ
	Если ЭтоКСФ Тогда 
		
		ДанныеТабличнойЧасти = ТабличнаяЧастьКорректировочногоДокумента(ДокументСсылка);
		
	ИначеЕсли ЭтоИСФ Тогда
		
		ДанныеТабличнойЧасти = ТабличнаяЧастьИсправительногоСчетаФактуры(ДокументСсылка);
		
	Иначе
			
		ДанныеТабличнойЧасти = ПолучитьДанныеТЧ(ДокументыОснования, Неопределено, Неопределено, Истина, ?(ЭтоСФ ИЛИ ЭтоСФНаАванс, ДокументСсылка, Неопределено));
				
	КонецЕсли;
	
	ДополнитьДанныеШапкиХарактеристикамиТабличнойЧасти(Результат, ДанныеТабличнойЧасти);
	
	//формируем шапку
	Если ЭтоСФ ИЛИ ЭтоСФНаАванс Тогда
		Результат.Вставить("СФНаАванс", ЭтоСФНаАванс);
	КонецЕсли;
	
	Если Результат.НДСИсчисляетсяНалоговымАгентом Тогда
		МетодСервера(, "ЗаполнитьВТаблице_СтавкаНДС_ИсчНалАг", ДанныеТабличнойЧасти);
	КонецЕсли;
	
	ПроверитьЗаполнитьВалюту(Результат);
	
	Результат.Вставить("ТабличнаяЧасть", ТаблицаЗначений_2_МассивСтруктур(ДанныеТабличнойЧасти));
	
	//формируем платежно расчетные документы
	ПлатежноРасчетныеДокументы = ПолучитьПлатежноРасчетныеДокументы(ДокументСсылка, СписокЗначений_В_Массив(ДокументыОснования));
	Результат.Вставить("ДатаНомерДокументовОплаты", ТаблицаЗначений_2_МассивСтруктур(ПлатежноРасчетныеДокументы));
	
	Возврат Результат;
	
КонецФункции

// Возвращает информацию о стране происхождения товара в виде структуры.
//
// Параметры:
//  СтрокаДанных - СтрокаТаблицыЗначений - Строка табличной части Товары.
//
// Возвращаемое значение:
//   Структура - Сведения о стране происхождения товара для поля StructedAdditionalInfo:
//      * СтранаПроисхождения - СправочникСсылка.СтраныМира - Страна происхождения товара.
//      * НомерГТД            - СправочникСсылка.НомераГТД - Номер грузовой таможенной декларации.
//
Функция КоллекцияСведенийОСтранеПроисхождения(СтрокаДанных)
	
	Результат = Новый Структура;
	Результат.Вставить("СтранаПроисхождения"	, СтрокаДанных.СтранаПроисхождения);
	Результат.Вставить("НомерГТД"				, СтрокаДанных.НомерГТД);
	
	Возврат Результат;

КонецФункции

//} ОБЩИЕ ПРОЦЕДУРЫ И ФУНКЦИИ
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
//{ Get content

////////////////////////////////////////////////////////////////////////////////
//{ Get UniversalCorrectionDocument content

Процедура ЗаполнитьInvoiceForCorrectionInfo(Invoices, ДатаНомерДокумента)
	
	Item_InvoiceForCorrectionInfo = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceForCorrectionInfo");
	
	Item_InvoiceForCorrectionInfo.InvoiceNumber	 = ДатаНомерДокумента.НомерИсходногоДокумента;
	Item_InvoiceForCorrectionInfo.InvoiceDate	 = ДатаНомерДокумента.ДатаИсходногоДокумента;
	
	Если ДатаНомерДокумента.УчитыватьИсправлениеИсходногоДокумента Тогда 
		
		Item_InvoiceRevisionInfo = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceRevisionInfo");
		
		Item_InvoiceRevisionInfo.RevisionNumber	 = ДатаНомерДокумента.НомерИсправленияИсходногоДокумента;
		Item_InvoiceRevisionInfo.RevisionDate	 = ДатаНомерДокумента.ДатаИсправленияИсходногоДокумента;
		
		Item_InvoiceForCorrectionInfo.InvoiceRevisions.Добавить(Item_InvoiceRevisionInfo);
		
	КонецЕсли;
	
	Invoices.Добавить(Item_InvoiceForCorrectionInfo);
	
КонецПроцедуры

Процедура ЗаполнитьInvoiceCorrectionTable(InvoiceCorrectionTable, ТаблицаТоваров, Точность, ДокументОтгрузки)
	
	ВсеСтроки_СтавкаНДС_БезНДС	 = Истина;
	ВсеСтроки_СтавкаНДС_ИсчНалАг = Истина;
	
	ЕстьУвеличение = Ложь;
	ЕстьУменьшение = Ложь;
	
	TotalsDec_Total 				= 0;
	TotalsDec_TotalWithVatExcluded 	= 0;
	TotalsDec_Vat 					= 0;
	
	TotalsInc_Total 				= 0;
	TotalsInc_TotalWithVatExcluded 	= 0;
	TotalsInc_Vat 					= 0;
	
	СтруктураItemInvoiceCorrection = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_ExtendedInvoiceCorrectionItem");
	
	СтавкаНДС_БезНДС   = МетодСервера(, "СтавкаНДС_БезНДС");
	СтавкаНДС_ИсчНалАг = МетодСервера(, "СтавкаНДС_ИсчНалАг");
	
	НомерСтроки = 0;
	
	Для Каждого СтрокаДанных из ТаблицаТоваров Цикл 
		
		НомерСтроки = НомерСтроки + 1;
		
		Item = СкопироватьСтруктуру(СтруктураItemInvoiceCorrection);
		ДополнитьКонтентСтрокиДокументаСлужебнойИнформацией(Item, СтрокаДанных, ДокументОтгрузки);
		InvoiceCorrectionTable.Items.Добавить(Item);
		
		Item.AmountsDec			= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_AmountsDiff");
		Item.AmountsInc			= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_AmountsDiff");
		Item.OriginalValues		= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceItemFields");
		Item.CorrectedValues	= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_InvoiceItemFields");
		
		Item.Product		= СтрокаДанных.НоменклатураПредставление;
		Item.ItemVendorCode	= СтрокаДанных.КодНоменклатуры;
		
		СтавкаДоИзменения = СтрокаДанных.СтавкаНДСДоИзменения;
		Если Не ЗначениеЗаполнено(СтавкаДоИзменения) Тогда
			СтавкаДоИзменения = СтрокаДанных.СтавкаНДС;
		КонецЕсли;
		
		Item.CorrectedValues.TaxRate = ПредставлениеСтавкиНДС(СтрокаДанных.СтавкаНДС);
		Item.OriginalValues.TaxRate  = ПредставлениеСтавкиНДС(СтавкаДоИзменения);
		
		Если Item.CorrectedValues.TaxRate <> СтавкаНДС_БезНДС Тогда
			ВсеСтроки_СтавкаНДС_БезНДС = Ложь;
		КонецЕсли;
		Если Item.CorrectedValues.TaxRate <> СтавкаНДС_ИсчНалАг Тогда
			ВсеСтроки_СтавкаНДС_ИсчНалАг = Ложь;
		КонецЕсли;
		
		Item.OriginalValues.TotalWithVatExcluded  = СтрокаДанных.СуммаСНДСДоИзменения - СтрокаДанных.СуммаНДСДоИзменения;
		Item.CorrectedValues.TotalWithVatExcluded = СтрокаДанных.СуммаСНДС - СтрокаДанных.СуммаНДС;
		
		Если ЗначениеЗаполнено(СтрокаДанных.КоличествоДоИзменения) Тогда
			
			Item.OriginalValues.Quantity = СтрокаДанных.КоличествоДоИзменения;
			
			// Price всегда расчетный, даже если ставка "без НДС", т.к. в ТЧ Расхождения, на базе которой получаются данные, цены нет
			Item.OriginalValues.Price = Окр(Item.OriginalValues.TotalWithVatExcluded / Item.OriginalValues.Quantity, Точность);
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаДанных.ЕдиницаИзмеренияДоИзменения) Тогда
			
			Item.OriginalValues.UnitCode = XML_КодЕдиницыИзмерения(СокрЛП(СтрокаДанных.КодЕдиницыИзмеренияДоИзменения));
									
			Если ЗначениеЗаполнено(СтрокаДанных.ЕдиницаИзмеренияДоИзменения.Наименование) Тогда
				Item.OriginalValues.UnitName = СтрокаДанных.ЕдиницаИзмеренияДоИзменения.Наименование;
			Иначе
				Item.OriginalValues.UnitName = НаименованиеЕдиницыИзмеренияПоКодуОКЕИ(Item.OriginalValues.UnitCode);
			КонецЕсли;
			
			Item.OriginalValues.ЕдиницаИзмеренияССылка = СтрокаДанных.ЕдиницаИзмеренияДоИзменения;
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаДанных.Количество) Тогда
			
			// если до количество было, а теперь его не стало - заполняем 0, а не "-"!!!
			Если ЗначениеЗаполнено(СтрокаДанных.КоличествоДоИзменения) И Не ЗначениеЗаполнено(СтрокаДанных.Количество) Тогда
				Item.CorrectedValues.Quantity = 0;
			Иначе
				Item.CorrectedValues.Quantity 	= СтрокаДанных.Количество;
				// Price всегда расчетный, даже если ставка "без НДС", т.к. в ТЧ Расхождения, на базе которой получаются данные, цены нет
				Item.CorrectedValues.Price		= ?(ЗначениеЗаполнено(Item.CorrectedValues.Quantity), Окр(Item.CorrectedValues.TotalWithVatExcluded / Item.CorrectedValues.Quantity, Точность), 0);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаДанных.КодЕдиницыИзмерения) Тогда
			
			Item.CorrectedValues.UnitCode = XML_КодЕдиницыИзмерения(СокрЛП(СтрокаДанных.КодЕдиницыИзмерения));
									
			Если ЗначениеЗаполнено(СтрокаДанных.ЕдиницаИзмерения.Наименование) Тогда
				Item.CorrectedValues.UnitName = СтрокаДанных.ЕдиницаИзмерения.Наименование;
			Иначе
				Item.CorrectedValues.UnitName = НаименованиеЕдиницыИзмеренияПоКодуОКЕИ(Item.CorrectedValues.UnitCode);
			КонецЕсли;
			
			Item.CorrectedValues.ЕдиницаИзмеренияССылка = СтрокаДанных.ЕдиницаИзмерения;
			
		КонецЕсли;
		
		
		// НДС и сумма с НДС до изменения
		Если Item.OriginalValues.TaxRate = СтавкаНДС_ИсчНалАг Тогда
			Item.OriginalValues.Vat = 0;
			Item.OriginalValues.Total = 0;
		Иначе
			
			Если Item.OriginalValues.TaxRate = СтавкаНДС_БезНДС Тогда
				Item.OriginalValues.Vat = Неопределено;
			Иначе
				Item.OriginalValues.Vat = СтрокаДанных.СуммаНДСДоИзменения;
			КонецЕсли;
			
			Item.OriginalValues.Total   = СтрокаДанных.СуммаСНДСДоИзменения;
			
		КонецЕсли;
		
		// НДС и сумма с НДС после изменения
		Если Item.CorrectedValues.TaxRate = СтавкаНДС_ИсчНалАг Тогда
			Item.CorrectedValues.Vat = 0;
			Item.CorrectedValues.Total = 0;
		Иначе
			
			Если Item.CorrectedValues.TaxRate = СтавкаНДС_БезНДС Тогда
				Item.CorrectedValues.Vat = Неопределено;
			Иначе
				Item.CorrectedValues.Vat = СтрокаДанных.СуммаНДС;
			КонецЕсли;
			
			Item.CorrectedValues.Total = СтрокаДанных.СуммаСНДС;
			
		КонецЕсли;
		
		ЭтоНДСИНА = (Item.CorrectedValues.TaxRate = СтавкаНДС_ИсчНалАг);
		Если ЭтоНДСИНА Тогда
			ЭтоСтрокаНаУвеличение = (Item.CorrectedValues.TotalWithVatExcluded >= Item.OriginalValues.TotalWithVatExcluded);
		Иначе
			ЭтоСтрокаНаУвеличение = (Item.CorrectedValues.Total >= Item.OriginalValues.Total);
		КонецЕсли;
		
		Если ЭтоСтрокаНаУвеличение Тогда
			
			// Увеличение
			ЕстьУвеличение = Истина;
			
			Item.AmountsDec.TotalWithVatExcluded = Неопределено;
			Item.AmountsDec.Vat = Неопределено;
			Item.AmountsDec.Total = Неопределено;
			
			Item.AmountsInc.TotalWithVatExcluded = Item.CorrectedValues.TotalWithVatExcluded - Item.OriginalValues.TotalWithVatExcluded;
			
			Если Item.CorrectedValues.TaxRate = СтавкаНДС_ИсчНалАг Тогда
				Item.AmountsInc.Vat = Неопределено;
				Item.AmountsInc.Total = Неопределено;
			Иначе
				
				Если Item.CorrectedValues.TaxRate = СтавкаНДС_БезНДС Тогда
					Item.AmountsInc.Vat = 0;
				Иначе
					Item.AmountsInc.Vat = МетодСервера(, "ПривестиКЧислу", Item.CorrectedValues.Vat) - МетодСервера(, "ПривестиКЧислу", Item.OriginalValues.Vat);
				КонецЕсли;
				
				Item.AmountsInc.Total = Item.CorrectedValues.Total - Item.OriginalValues.Total;
				
			КонецЕсли;
			
		Иначе
			
			// Уменьшение
			ЕстьУменьшение = Истина;
			
			Item.AmountsInc.TotalWithVatExcluded = Неопределено;
			Item.AmountsInc.Vat = Неопределено;
			Item.AmountsInc.Total = Неопределено;
			
			Item.AmountsDec.TotalWithVatExcluded = - Item.CorrectedValues.TotalWithVatExcluded + Item.OriginalValues.TotalWithVatExcluded;
			
			Если Item.CorrectedValues.TaxRate = СтавкаНДС_ИсчНалАг Тогда
				Item.AmountsDec.Vat = Неопределено;
				Item.AmountsDec.Total = Неопределено;
			Иначе
				
				Если Item.CorrectedValues.TaxRate = СтавкаНДС_БезНДС Тогда
					Item.AmountsDec.Vat = 0;
				Иначе
					Item.AmountsDec.Vat = - МетодСервера(, "ПривестиКЧислу", Item.CorrectedValues.Vat) + МетодСервера(, "ПривестиКЧислу", Item.OriginalValues.Vat);
				КонецЕсли;
				
				Item.AmountsDec.Total = -Item.CorrectedValues.Total + Item.OriginalValues.Total;
				
			КонецЕсли;
			
		КонецЕсли;
		
		TotalsDec_Total 				= TotalsDec_Total + МетодСервера(, "ПривестиКЧислу", Item.AmountsDec.Total);
		TotalsDec_TotalWithVatExcluded 	= TotalsDec_TotalWithVatExcluded + МетодСервера(, "ПривестиКЧислу", Item.AmountsDec.TotalWithVatExcluded);
		TotalsDec_Vat 					= TotalsDec_Vat + МетодСервера(, "ПривестиКЧислу", Item.AmountsDec.Vat);
		
		TotalsInc_Total 				= TotalsInc_Total + МетодСервера(, "ПривестиКЧислу", Item.AmountsInc.Total);
		TotalsInc_TotalWithVatExcluded 	= TotalsInc_TotalWithVatExcluded + МетодСервера(, "ПривестиКЧислу", Item.AmountsInc.TotalWithVatExcluded);
		TotalsInc_Vat 					= TotalsInc_Vat + МетодСервера(, "ПривестиКЧислу", Item.AmountsInc.Vat);
		
		Item.OriginalNumber = СтрокаДанных.НомерСтрокиИсходногоСФ;
			
		СведенияОСтранеПроисхожденияТовара	= КоллекцияСведенийОСтранеПроисхождения(СтрокаДанных);
		ДопИнформацияОСтранеПроисхождения	= МетодСервера("Модуль_ИнтеграцияУниверсальный", "СведенияОСтранеПроисхожденияСтрокой", СведенияОСтранеПроисхожденияТовара);
		
		ЗаполнитьStructedAdditionalInfos(Item.StructedAdditionalInfos, ДопИнформацияОСтранеПроисхождения);
		
	КонецЦикла;	  
	
	Если ЕстьУвеличение Тогда
		InvoiceCorrectionTable.TotalsInc.Total 					= TotalsInc_Total; 
		InvoiceCorrectionTable.TotalsInc.TotalWithVatExcluded	= TotalsInc_TotalWithVatExcluded; 
		InvoiceCorrectionTable.TotalsInc.Vat 					= ?(ВсеСтроки_СтавкаНДС_БезНДС, Неопределено, ?(ВсеСтроки_СтавкаНДС_ИсчНалАг, 0, TotalsInc_Vat)); 
	КонецЕсли;
	
	Если ЕстьУменьшение Тогда
		InvoiceCorrectionTable.TotalsDec.Total					= TotalsDec_Total; 
		InvoiceCorrectionTable.TotalsDec.TotalWithVatExcluded	= TotalsDec_TotalWithVatExcluded; 
		InvoiceCorrectionTable.TotalsDec.Vat 					= ?(ВсеСтроки_СтавкаНДС_БезНДС, Неопределено, ?(ВсеСтроки_СтавкаНДС_ИсчНалАг, 0, TotalsDec_Vat)); 
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьEventContent(EventContent, ШапкаУКД)
	
	Для Каждого ПередаточныйДокумент Из ШапкаУКД.РеквизитыПередаточныхДокументов Цикл	
		Новый_BaseDocument = МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_BaseDocumentName");
		ЗаполнитьЗначенияСвойств(Новый_BaseDocument, ПередаточныйДокумент);
		EventContent.TransferDocDetails736.Добавить(Новый_BaseDocument);	
	КонецЦикла;
	Если ШапкаУКД.Свойство("ИныеСведенияОбИзмененииСтоимости") Тогда
		EventContent.CostChangeInfo = ШапкаУКД.ИныеСведенияОбИзмененииСтоимости;
	КонецЕсли;
	Если ШапкаУКД.Свойство("ДатаУведомления") Тогда
		EventContent.NotificationDate = ШапкаУКД.ДатаУведомления;
	КонецЕсли;
	
	EventContent.OperationContent = "Изменение стоимости товаров и услуг";
	
	CorrectionBases 				= Новый Массив;
	СтруктураItemCorrectionBases 	= МетодСервера("Модуль_ИнтеграцияУниверсальный", "Новый_CorrectionBase");
	
	ОснованияПередачи = ШапкаУКД.ОснованияПередачи;
	Если ОснованияПередачи.Количество() > 0 Тогда
		СкопироватьСтруктуру(СтруктураItemCorrectionBases);
		Для Каждого ОснованиеПередачи Из ОснованияПередачи Цикл
			ItemCorrectionBases = СкопироватьСтруктуру(СтруктураItemCorrectionBases);
			ЗаполнитьЗначенияСвойств(ItemCorrectionBases, ОснованиеПередачи);
			CorrectionBases.Добавить(ItemCorrectionBases);
		КонецЦикла;
	Иначе
		СтруктураItemCorrectionBases.BaseDocumentName = МетодСервера("Модуль_ИнтеграцияУниверсальный", "ОснованиеПередачи_БезДокументаОснования");
		CorrectionBases.Добавить(СтруктураItemCorrectionBases);
	КонецЕсли;
	
	EventContent.CorrectionBases = CorrectionBases;
	
КонецПроцедуры

//} Get UniversalCorrectionDocument content
////////////////////////////////////////////////////////////////////////////////

//} Get content
////////////////////////////////////////////////////////////////////////////////

//} РАБОТА С КОНТЕНТОМ
////////////////////////////////////////////////////////////////////////////////
