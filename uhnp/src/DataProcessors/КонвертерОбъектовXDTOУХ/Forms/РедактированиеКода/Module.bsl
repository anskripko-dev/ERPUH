
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ТекстПрограммы = Параметры.ТекстПрограммы;
	Если НЕ ПустаяСтрока(Параметры.Заголовок) Тогда
		ЭтаФорма.Заголовок = Параметры.Заголовок;
		ЭтаФорма.АвтоЗаголовок = Ложь;
	КонецЕсли;
	НастроитьПодсказкуФормы(Параметры.ИмяПодсказки, Параметры.ТекстПодсказки);
КонецПроцедуры

&НаКлиенте
Процедура Отменить(Команда)
	Закрыть(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура СохранитьИЗакрыть(Команда)
	Закрыть(ТекстПрограммы);
КонецПроцедуры

&НаСервере
Процедура НастроитьПодсказкуФормы(ИмяПодсказки, ТекстПодсказки)
	Элементы.ДекорацияПодсказка.Заголовок = ТекстПодсказки;
	Элементы.ДекорацияПодсказка.Видимость = ЗначениеЗаполнено(ТекстПодсказки);
	УстановитьВидимостьПодсказкиПоИмениПодсказки(ИмяПодсказки);
КонецПроцедуры

&НаСервере
Процедура УстановитьВидимостьПодсказкиПоИмениПодсказки(ИмяПодсказки)
	ДекорацияТипаФункции = Элементы.Найти("Декорация" + ИмяПодсказки);
	Если ДекорацияТипаФункции <> Неопределено Тогда
		ДекорацияТипаФункции.Видимость = Истина;
	КонецЕсли;
КонецПроцедуры