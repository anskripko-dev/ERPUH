//++ НЕ УТКА

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	ПланСчетовОбъект = Справочники.ПланыСчетовМеждународногоУчета.Международный.ПолучитьОбъект();
	Если Не ЗначениеЗаполнено(ПланСчетовОбъект.ВалютаПредставления) Тогда
		ПланСчетовОбъект.ВалютаПредставления = ЭтотОбъект.Значение;
		ПланСчетовОбъект.Записать();
	КонецЕсли;
	
	#Область УХ_Встраивание
	Если Не ДополнительныеСвойства.ТекущееЗначение = Значение Тогда    	
		ОбновитьПовторноИспользуемыеЗначения();   	
	КонецЕсли; 
	#КонецОбласти
	
КонецПроцедуры

#Область УХ_Встраивание
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда		
		Возврат;   	
	КонецЕсли;
	
	ЗначениеКонстанты = Константы.ВалютаУправленческогоУчета.Получить();
	ДополнительныеСвойства.Вставить("ТекущееЗначение", ЗначениеКонстанты);
			
КонецПроцедуры
#КонецОбласти

#КонецОбласти

#КонецЕсли

//-- НЕ УТКА
